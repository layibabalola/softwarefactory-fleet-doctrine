# Cloudvore universal provider-governor proposal receipt — 2026-08-18

Status: **PROPOSED / ZERO AUTHORITY / HARD_CLOSED**

This receipt records doctrine publication of a design and conformance profile on branch
`codex/cloudvore-provider-governor-shadow-profile-0818` and PR #17. It is not installation, SHADOW
execution, containment, a provider drill, project adoption, or fleet adoption. Publication itself
has no Cloudvore runtime or product-ref authority.

## Frozen intake

- Ratified portable subject: `224a6705d81dfbc670313cdcef4d825216f2b380`
- Tree: `569957a2b62eb0e2e99c1490a9cbec0002894e42`
- Parent: `fa8e19d0630b2a246bcb46a112f247b160e97c1f`
- Cloudvore source design SHA-256:
  `424A56C8700813C06BF31AC3B5B8C34B323F1EF6B1F38685E51BB89764957887`
- Source design size: 17,321 bytes
- Tracked source design:
  `cloudvore/provider-governor/provenance/HUB-DESIGN-provider-capacity-governor-shadow-adoption-0818.md`
- Strict shadow schema: `cloudvore/provider-governor/cloudvore-shadow-profile-v1.schema.json`
- Proposal manifest:
  `cloudvore/provider-governor/cloudvore-provider-governor-proposal-manifest-v1.json`
- Accepted additive universal reconciliation: candidate
  `874605e43531c9aa230ee16851f8107a8e0d9cec`, tree
  `cafc358fd7b60812070cf9a465d7de38b88487c8`, canonical merge
  `488cf0dc0c2c2ddd1ab024c6377e1fd6d61eef1d`
- Accepted R14 manifest SHA-256:
  `A2B4024F76F526014D174EA8B3BF9315777F26E8314039F8814F79EC1C864382` / 9,082 bytes
- Repair base master: commit `58efc8e3f3c1163c443ebe71be3b7a4aede42ce9`, tree
  `20c77898460584d27035744fccf4b50cfd17ac36`

The machine-readable profile pins all 13 accepted Git-object paths and the focused test recomputes
every size and SHA-256 from the accepted commit. It parses the accepted `RULINGS.md` Git blob rather
than trusting copied prose. That check corrects the original proposal's overstatement: the ruling
disagrees with accepted Git bytes for `README.md` and `metrics/README.md`; its 64-character usage
example hash is correct. The 65-character value existed only in PR #17's prior profile and is
retained as adverse transcription evidence; the tracked source design also carries the correct
64-character value. The profile therefore says
`BLOCKED_TWO_RULING_MISMATCHES_ONE_PRIOR_PROFILE_TRANSCRIPTION_MISMATCH`. Mutable branch bytes are not an
installation source, and Cloudvore must not choose silently. Fleet adjudication must correct or
explicitly distinguish the two ruling tuple errors before a local installer may claim a complete
exact intake. The later R14 law establishes canonical Git-blob hashing for its own 34-subject
manifest and prevents repeating this EOL-sensitive mistake; it does not rewrite the earlier two
printed values.

## Candidate reconciliation

Live `origin/master`, tracker #3, and current candidate refs/PRs were read before this proposal
branch was created and again before publication. Universal reconciliation PR #10 advanced and
merged while the proposal was being authored, so the branch was rebased onto canonical merge
`488cf0dc...`. The candidate set was reconciled as follows:

- merged PR #10 is the accepted additive portable reconciliation at exact R14 candidate commit
  `874605e43531c9aa230ee16851f8107a8e0d9cec`; its reference runtime remains deployment-inert and
  grants no Cloudvore adoption;
- draft PR #7, Conjugal provider-capacity runtime extension, inspected at commit
  `9e4599d5655d856cf2dc45b87a2857419c8ebe3b`; and
- project disposition PRs #6 and #11.

Cloudvore consumes the ratified `224a670...` governor plus accepted R14 as an additive portable
contract. It does not adopt, supersede, or fork the unmerged project candidates. The proposed
`cloudvore-project-profile-proposal-v1.json` validates against R14's canonical
`universal-project-profile-v1` schema but remains an inert proposal: its state-root HMAC is a
deliberately non-production proposal identity and must be replaced by an installed machine-local
identity under review. Provider adapters may translate native identity, cache, usage, reset,
refusal, and terminal fields only; they cannot reinterpret admission, authority, quality,
independence credit, budget-stop meaning, role, model, or effort.

## Observed local seam evidence

The source design binds the observed Cloudvore launch surfaces and exact hashes: Claude direct
wrappers (`1D3921F3...` latest inspected), Codex direct wrappers (`A12262DD...`), Kimi adapter
(`40F92619...`), Grok adapter/WSL boundary (`FB175B43...` / `46B64C4E...`), and the deterministic
plan-only provider router (`48E21615...`). None currently proves entry through one pinned host-wide
quota-domain supervisor. Therefore every family remains pending and the Cloudvore disposition
remains `DISTINGUISH(..., PENDING_LOCAL_ADAPTER_AND_DRILLS)`.

The doctrine branch and PR were intentionally created and updated, and the canonical hosted
workflow is intentionally extended to validate this profile on its four existing OS/Python cells.
No provider, Cloudvore hosted product workflow, Cloudvore product test, scheduler, credential,
launcher, process, Cloudvore runtime/product Git ref, landing, release, or gate was invoked or
mutated to create this proposal. A later implementation must produce its own exact
installed-manifest, fake/no-inference, SHADOW, containment, capacity, context,
routing-if-applicable, independent-review, and dated receipt evidence.

The committed proposal-manifest/receipt blobs and the current-master/proposed-merge result are
commit-derived evidence and therefore cannot self-certify inside their own preimage. Their exact
commit, tree, blob, SHA-256, size, base, and merge-result tuple must be published on PR #17 and
tracker #3 for independent reproduction after each head changes.
