# Cloudvore universal provider-governor proposal receipt — 2026-08-18

Status: **PROPOSED / ZERO AUTHORITY / HARD_CLOSED**

This receipt records publication of a design and conformance profile, not installation, SHADOW
execution, containment, a provider drill, project adoption, or fleet adoption.

## Frozen intake

- Ratified portable subject: `224a6705d81dfbc670313cdcef4d825216f2b380`
- Tree: `569957a2b62eb0e2e99c1490a9cbec0002894e42`
- Parent: `fa8e19d0630b2a246bcb46a112f247b160e97c1f`
- Cloudvore source design SHA-256:
  `424A56C8700813C06BF31AC3B5B8C34B323F1EF6B1F38685E51BB89764957887`
- Source design size: 17,321 bytes

The machine-readable profile pins all 13 accepted Git-object paths and the focused test recomputes
every size and SHA-256 from the accepted commit. It also records an exact adverse finding: the
artifact tuple printed in `RULINGS.md` disagrees with the accepted Git bytes for `README.md`,
`metrics/README.md`, and `examples/provider-usage-events-v1.jsonl`; the final declared hash is 65
hex characters. The profile therefore says `BLOCKED_DECLARED_TUPLE_MISMATCH`. Mutable branch bytes
are not an installation source, and Cloudvore must not choose one side silently. Fleet adjudication
must correct or explicitly distinguish the declared tuple before a local installer may claim a
complete exact intake.

## Candidate reconciliation

Live `origin/master` and tracker #3 were read before this proposal branch was created. The following
unmerged candidates were treated as evidence inputs only:

- draft PR #10, universal provider-control reconciliation, inspected at R14 commit
  `874605e43531c9aa230ee16851f8107a8e0d9cec`;
- draft PR #7, Conjugal provider-capacity runtime extension, inspected at commit
  `9e4599d5655d856cf2dc45b87a2857419c8ebe3b`; and
- project disposition PRs #6 and #11.

Cloudvore does not adopt, supersede, or fork those candidates. The only normative portable input is
the ratified `224a670...` subject. Any later canonical reconciled successor requires its own fleet
adjudication and a new Cloudvore disposition. Provider adapters may translate native identity,
cache, usage, reset, refusal, and terminal fields only; they cannot reinterpret admission,
authority, quality, independence credit, budget-stop meaning, role, model, or effort.

## Observed local seam evidence

The source design binds the observed Cloudvore launch surfaces and exact hashes: Claude direct
wrappers (`1D3921F3...` latest inspected), Codex direct wrappers (`A12262DD...`), Kimi adapter
(`40F92619...`), Grok adapter/WSL boundary (`FB175B43...` / `46B64C4E...`), and the deterministic
plan-only provider router (`48E21615...`). None currently proves entry through one pinned host-wide
quota-domain supervisor. Therefore every family remains pending and the Cloudvore disposition
remains `DISTINGUISH(..., PENDING_LOCAL_ADAPTER_AND_DRILLS)`.

No provider, hosted workflow, product test, scheduler, credential, launcher, process, Git ref,
landing, release, or gate was invoked or mutated to create this proposal. A later implementation
must produce its own exact installed-manifest, fake/no-inference, SHADOW, containment, capacity,
context, routing-if-applicable, independent-review, and dated receipt evidence.
