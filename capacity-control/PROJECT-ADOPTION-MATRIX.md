# Project adoption matrix

**Every row is pending local review and ratification.** The shared candidate is test data until a
project pins reviewed bytes outside the mutable doctrine checkout and proves its own launcher and
quality bars. Each project publishes its result through its sole `specs/<project>.md` writer.

| Project | Required integration seam | Project-specific non-regression proof |
|---|---|---|
| Cloudvore | Put the external broker in front of every retained Claude/Kimi/Grok/Codex launcher; frozen resume custody remains unchanged. Require an exact OPUS dispatch before review inference and structurally reject direct provider invocation outside the pinned supervisor. | Existing exact-model credit, one-writer worktrees, mutation-first review, inbox delivery, and 3× identical product bar remain unchanged. Pilot context boundaries before enforcing them. |
| Conjugal | Add the account-domain lease above the repo-local dead-man mutex. Run a deterministic route/mail/cursor reducer before provider admission; classify session-limit responses into the shared capacity latch and clear event-caused backoff at reset. | `claude-opus-5/max` and `claude-fable-5/max` mappings, compatible-runner law, exact route, independent review, continuity, first-red, and dead-man suites remain unchanged. IDLE performs zero inference. |
| MLV-App | Bind liveness to PID, process start, transcript session, and epoch; single-flight each lane; make registrar/watchers deterministic and terminate orphan heartbeats. Broker every provider-bearing ignition. | Dead process plus fresh lease becomes dark; no duplicate claimant; Fable failure cannot block an unambiguous successor; 1,000 synthetic sweeps perform zero model calls. Existing approval and evidence gates remain unchanged. |
| AirMyPC / AudioMile | Add the shared Anthropic quota-domain lease above per-seat caps. Require frozen subject packets for OPUS and bounded turns for headless work. Keep role/profile qualification separate from capacity. | Exact-byte subject, author recusal, two-key distinct-family review, live-hardware, ARMED-6, and `RUN_GO` remain unchanged. No Sonnet/Kimi/Grok result receives Opus credit. |
| agent-bridge | Put account-domain admission in front of warden ignition. Generate addressed lane capsules from board/lease/WAL state; polling and health remain model-free. | Same seat/stand-down/dispatch decisions on frozen replay; every first-bracket dispatch retained; protected invariants unchanged; independent Opus raw-evidence review survives capsule use. |

Projects on hosts not mounted here receive no fabricated integration claim. Their portable seam is
defined above; only their own runtime evidence can change `pending` to `ADOPT`, `DISTINGUISH`, or
`REJECT`.

## Zero-discretionary-reserve migration request (2026-08-23)

The owner has proposed a 0% discretionary capacity reserve so useful bounded work can naturally drain
Claude quota before account rotation. This does not grant a bulk edit or adoption credit. Each row
above must separately evaluate
[`zero-discretionary-capacity-reserve-r1`](../ruling-candidates/zero-discretionary-capacity-reserve-r1.md)
and publish `ADOPT`, `DISTINGUISH`, or `REJECT` through its project-owned spec.

Migration preserves the hard 100% estimated ceiling, fresh account-bound telemetry, conservative
request/terminal reservations, serial single-flight, exact model/effort/role/review/quality/functionality,
and every existing rollback and proof obligation. A fixed 20% or 30% capacity floor is the only policy
dimension proposed for replacement.
