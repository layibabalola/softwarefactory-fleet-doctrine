# Ruling candidate: exhausted-model failback to Opus R22

Status: **PROPOSED ONLY — ZERO AUTHORITY**

Exact Fable model exhaustion earns zero credit but may continue unchanged authorized work on exact
Opus through the natural scheduler. Preserve objective, ordered core, role, effort, tools, bounds,
seriality, result contract, quality, and acceptance. Immediately before admission, signed
same-domain model-free capacity no more than 300 seconds old must prove in every window:
`utilization + reservations + conservative estimate <= 100`. Reserve is zero; the ceiling is
absolute. Stale, unsigned, cross-domain, ambiguous, overlapping, live-lease, closed-gate, or
over-ceiling evidence is `HOLD`; authentication is not capacity evidence.

Exact exhaustion binds route/session, `claude-fable-5`, one-turn terminal
`terminal_reason=api_error`/429, `assistant_error=rate_limit`, exact model text, no verdict,
zero review tokens, and all packet/authorization/artifact/core/assertion hashes. Before clearing
the hold, atomically replace the consumed packet with a closed-key no-work carrier using native
`authority=read-only-review`, `actionable_work=false`, empty subjects, and
`result_contract=route-review-result.v1`. Bind exact terminal carrier bytes/hash; never revive a
consumed route, receipt, or lease.

Each row binds normalized path, integer bytes, and SHA-256; canonical ordered-row JSON defines the
core. Fable and Opus carry identical three-row core plus one capsule: at most four subjects, 24 KiB
each, 32 KiB total. Exact lanes are Fable/coordinator, Opus/executor, Sonnet/verifier with models
`claude-fable-5`, `claude-opus-5`, `claude-sonnet-5`; all use effort `max`, at most 12
turns/900 seconds, broker tools exactly `Read` plus `StructuredOutput`, and result
`route-review-result.v1`. Packet/preclaim/argv/admission prove those values; any type, key, core,
or execution drift is `HOLD`.

The closed capsule binds ancestry, prior lanes, clean Git/local PASS, terminal hashes, all three on-lane core rows,
and native semantics parsed from exact carrier bytes. It must not freeze a live capacity sample.
It binds immutable observer/admission/policy/warden/planner identities and the
post-route/preclaim capacity-materialization boundary:
`route valid -> signed observer -> unchanged preclaim -> permit -> admission`. Dynamic admission
checks sample age/domain, zero reserve, reservations, estimates, and ceiling, avoiding the circular
gate that required capacity before the scheduler could run its observer.

The executed matrix covers exhaustion, ambiguity, credit/tokens, event classification, capacity,
ceiling, gate, overlap, canary, rotation, output, capsule, replay, and terminal binding. Reorder,
replacement, and bytes are independent core mutations; effort, bounds, and identity are independent execution mutations.
Account/domain mismatch remains one bounded rollback-safe no-credential
transaction with a distinct successor and fresh signed observation.

R20 naturally proved zero-credit Fable exhaustion; Opus consumed 34,814 output tokens and returned
`ACCEPT` with four actionable findings, so R20 earns no acceptance. R21 repaired packet-row,
terminal-discriminator, carrier-semantics, and mutation-isolation defects. R22 preserves them and
repairs frozen-capacity and aggregate-fit liveness. Fable must ACCEPT R22 or prove exact zero-credit
exhaustion; then Opus and Sonnet must ACCEPT unchanged core. Findings create a forward subject and
restart at Fable. Ratification requires canonical `master`; each project must publish honest
`ADOPT`, `DISTINGUISH`, or `REJECT` with its own transaction and production proof. This grants
no launch, rotation, gate, merge, installation, or adoption authority.
