name: R26 adoption ledger

on:
  pull_request:
    paths:
      - ".github/workflows/adoption-ledger.yml"
      - "capacity-control/PROJECT-ADOPTION-MATRIX.md"
      - "specs/**"
      - "adoption/**"
      - "tests/test_current_adoption_ledger.py"
      - "tests/test_current_intake_epoch.py"
      - "tools/check_current_intake_epoch.py"
      - "ruling-candidates/current-intake-epoch-r1.md"
      - "tools/check_adoption_ledger.py"
  push:
    branches: [master]
    paths:
      - ".github/workflows/adoption-ledger.yml"
      - "capacity-control/PROJECT-ADOPTION-MATRIX.md"
      - "specs/**"
      - "adoption/**"
      - "tests/test_current_adoption_ledger.py"
      - "tests/test_current_intake_epoch.py"
      - "tools/check_current_intake_epoch.py"
      - "ruling-candidates/current-intake-epoch-r1.md"
      - "tools/check_adoption_ledger.py"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: r26-adoption-ledger-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  evidence:
    env:
      R26_SCOPE_EVENT: ${{ github.event_name }}
      R26_SCOPE_BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event_name == 'push' && github.event.before || '' }}
    strategy:
      fail-fast: false
      # Exact project/spec drift is checked once on each PR. The complete matrix
      # runs only on canonical master and explicit landing verification.
      matrix: ${{ fromJSON(github.event_name == 'pull_request' && '{"os":["ubuntu-latest"],"python-version":["3.14"]}' || '{"os":["windows-latest","ubuntu-latest"],"python-version":["3.13","3.14"]}') }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - name: Check out full evidence history
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: ${{ matrix.python-version }}

      # Historical suites run in all four cells of the always-on intake workflow.
      - name: Run current ledger controls
        run: python -m unittest discover -s tests -p "test_current_adoption_ledger.py" -v

      - name: Verify history, controls and current project-owned Git evidence
        run: python tools/check_current_intake_epoch.py
