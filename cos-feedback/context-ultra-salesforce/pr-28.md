schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 28
head: 94ab4200316f2df74d67ba59c88af5dab50d615e
reviewed_utc: 2026-09-19T05:49:29Z
event: pr-opened
verdict: not-merge-ready
ci_summary: kmp-parity-drift-check FAIL×2 (jobs never started — GitHub account payments failed / spending limit); windows-app build-and-selftest IN_PROGRESS on run 35424890958 (Publish locked restore on self-hosted); mergeable MERGEABLE / UNSTABLE

## Blockers

- **CI account billing blocks hosted checks** — both `kmp-parity-drift-check` / `check` runs on this head (`35424890954`, `35424894083`) failed in ~3–39s with empty steps. Annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." Fix Billing & plans (or raise the Actions spending limit), then re-run the failed workflow on head `94ab4200`. Do not merge while this required check stays red for non-code reasons.
- **Wait for `windows-app` / `build-and-selftest`** — still IN_PROGRESS on run `35424890958` (Publish locked restore). This tip exists to make `audit.self-attestation-integrity-dimension-runs` pick the 2026-09-17 hardening report; confirm that pin (and the rest of the battery) PASS under `--expect-repo` before merge.

## Improvements

- **Close superseded siblings after this lands** — open PRs #24 (`r806-compile-fix`), #25 (`r806-pr2-win-runner`), and #26 (`r806-pr4-claude-md`) are still OPEN; body says this tip replaces them. After #28 merges (or if you abandon the split series), close those three so the R806 queue is not triple-booked.
- **Reconcile open PR #10** — `ci(windows-app): route to the self-hosted Windows runner via vars.WIN_RUNNER` also rewrites the same `runs-on` line (and drops the `HOSTED_CI` gate). After #28, close or rebase #10 so the two do not fight over `.github/workflows/windows-app.yml`.
- **Refresh the R363.3 comment** above `runs-on` in `windows-app.yml` — it still only documents `HOSTED_CI=paused`. Sibling legs (`kmp-tests.yml`) name the runner variable and the R408 dependabot guard in the same block; mirror that for `WIN_RUNNER`.
- **Dependabot `runs-on` behavior change** — old tip used empty `runs-on` for `dependabot[bot]`; new tip always falls through to `windows-latest` (matches R408 on `kmp-tests.yml`). Confirm that is intentional for this workflow’s cost posture.

## Notes

- Tip delta: +19/−17 across 3 files on branch `r806-consolidated-fix` → `main`; author layibabalola (co-authored Claude Haiku 4.5); single commit `94ab4200`.
  - `src/SfTools.App/SelfTestCli.cs`: `GetRepoRoot()` → `NeedRepoRoot(out var …)` skip pattern, plus audit-report pick sorted by mtime DESC then filename ASC (was path-string DESC). Content looks correct for the stated pin failure mode.
  - `.github/workflows/windows-app.yml`: `runs-on: ${{ (github.actor != 'dependabot[bot]' && vars.WIN_RUNNER) || 'windows-latest' }}` — same shape as `vars.KMP_RUNNER` in `kmp-tests.yml` (R363.3 / R408).
  - `CLAUDE.md`: R806 entry no longer claims Session 1+; documents Session 0 platform constraint and pending LogOn / desktop-path decision.
- Content review found no code merge blockers; posture is blocked by hosted-CI billing + pending selftest green. Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.
