schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 28
head: f91bcb15d98a9ddfcddac82170631448d0c41eb3
reviewed_utc: 2026-09-19T05:58:07Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: all required checks FAIL on head f91bcb15 — kmp-parity-drift-check×2 and windows-app build-and-selftest never started (GitHub account payments failed / spending limit); mergeable MERGEABLE / UNSTABLE

## Blockers

- **CI account billing blocks every check** — `kmp-parity-drift-check` / `check` (runs `35425272828`, `35425274476`) and `windows-app` / `build-and-selftest` (run `35425272829`) all failed in ~2–3s with empty steps. Annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." Fix Billing & plans (or raise the Actions spending limit), then re-run failed workflows on head `f91bcb15`. Do not merge while required checks stay red for non-code reasons.
- **Selftest gate still unproven on this tip** — prior tip had `build-and-selftest` IN_PROGRESS on the self-hosted runner; this tip never got a runner at all. After billing is fixed, confirm `audit.self-attestation-integrity-dimension-runs` (and the rest of the battery) PASS under `--expect-repo` before merge. Tip claims NEEDS GATE for the new try-catch paths.

## Improvements

- **Restore or explicitly drop WIN_RUNNER + CLAUDE.md from this consolidated PR** — prior head `94ab4200` carried `.github/workflows/windows-app.yml` (`vars.WIN_RUNNER` routing) and `CLAUDE.md` (R806 Session claim fix). Force-push to `f91bcb15` dropped both; tip delta vs `main` is only `SelfTestCli.cs` + three `.claude/audits/R806-swarm-*.md` files. Body still says this tip replaces #24/#25/#26 and includes those fixes. Either re-add the yml/CLAUDE commits, or update the body and keep #25/#26 (and #10) as the carriers.
- **Close superseded siblings after the real consolidated tip lands** — #24 (`r806-compile-fix`), #25 (`r806-pr2-win-runner`), #26 (`r806-pr4-claude-md`) remain OPEN. Close them once #28 (or the split series) actually covers their scope so the R806 queue is not triple-booked.
- **Reconcile open PR #10** — `ci(windows-app): route to the self-hosted Windows runner via vars.WIN_RUNNER` still OPEN and overlaps the dropped yml change. Close or rebase after WIN_RUNNER lands somewhere so the two do not fight over `windows-app.yml`.
- **Bare `catch` on mtime select** — `SelfTestCli.cs` audit-report select uses `catch { return null; }` (filters inaccessible files). Prefer catching `UnauthorizedAccessException` / `IOException` / `FileNotFoundException` (or log-and-skip) so unexpected failures are not silently dropped; ReadAllText path already returns `failed to read audit report: {TypeName}` which is better.
- **Optional: extract `SelectNewestFile`** — swarm UX audit suggested a shared helper if more pins need mtime-DESC + name-ASC; not blocking for this tip.

## Notes

- Push delta vs prior CoS head `94ab4200`: force-push replaced the three-file consolidated tip with swarm pre-code audits + exception hardening on the audit-report pin only (+585/−4 across 4 files on branch `r806-consolidated-fix` → `main`; single commit `f91bcb15`).
  - `src/SfTools.App/SelfTestCli.cs`: still `NeedRepoRoot(out var …)` + mtime DESC / filename ASC sort; **new** try-catch around `GetLastWriteTimeUtc` (skip bad files) and `ReadAllText` (diagnostic return); clarifying sort comment added (addresses R806-swarm-failure-modes + UX comment ask).
  - `.claude/audits/R806-swarm-compliance.md`: APPROVED (pattern + sort + totality).
  - `.claude/audits/R806-swarm-ux.md`: APPROVED WITH COMMENT (tiebreaker comment — now present on tip).
  - `.claude/audits/R806-swarm-failure-modes.md`: NEEDS FIX findings; tip implements the critical try-catch remediations.
- Content review: no new code merge blockers in the SelfTestCli pin; posture remains blocked by hosted-CI billing + unproven selftest green. Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.
