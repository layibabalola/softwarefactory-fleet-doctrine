schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 34
head: 6c3a7e3ae5d62be6966e5870e0725c7c48ac17ce
reviewed_utc: 2026-09-20T02:22:46Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS (two check jobs); windows-app build-and-selftest IN_PROGRESS on run 35483767312; mergeable MERGEABLE / mergeStateStatus UNSTABLE; ahead_by 2 / behind_by 0; 0 GitHub reviews

## Blockers

(none for content — Phase 1b library methods + structural pins are coherent scaffolding; wait on `build-and-selftest`)

## Improvements

- **PR body overclaims ExportCaseCli wiring** — Summary says Haiku-swarm gates were added to `ExportCaseCli.cs`. Diff touches only `CaseAnalysisTemplate.cs`, `SelfTestCli.cs`, and `tools/hooks/pre-push.ps1`. Tip `ExportCaseCli.cs` has zero references to `RequiresHaikuSwarmBeforePdf` / `RunPostValidationChecklist` / `CaseAnalysisTemplate`. Label as library-only Phase 1b (or wire the export path in a follow-on).

- **Gates are not on the PDF export path** — `RequiresHaikuSwarmBeforePdf` and `RunPostValidationChecklist` are unused statics. Until a caller blocks export on fail, R784 "NO EXCEPTIONS" is documentation + pins, not enforcement. Wire before claiming mandate enforcement.

- **Swarm gate is R809-hardcoded and path-fragile** — Looks for `R809-swarm-*.md` under `caseFolder/../../../.claude/audits` (relative climb, not repo-root resolve). Wrong for any other case id, and breaks if case folder depth differs. Prefer case-id-agnostic artifact names + `NeedRepoRoot`/`GetRepoRoot` style resolution. Also `f.Contains("ux")` false-positives any path substring.

- **Pins are structural source substring checks** — `case.haiku-swarm-gate-enforced` / `case.runbook-validation-checklist-passed` only `File.ReadAllText` + `Contains` on `CaseAnalysisTemplate.cs`. They cannot fail a broken gate implementation. Add behavioral pins (temp case folder missing/present artifacts; brief markdown failing/passing checklist keys).

- **Checklist regexes are too loose to be gates** — `timeline-correlation` matches any `when`/`since`/`update`; `metadata-intact` matches any `case`/`summary`; `confidence-calibrated` any of four words. These will pass almost any English brief. Tighten to required section headers or structured fields, or downgrade labels from gate to advisory.

- **R-claim change is on the wrong pre-push path** — Live installer is `tools/git-hooks/pre-push.bat` → `tools/git-hooks/pre-push.ps1`, which already blocks main pushes without `refs/r-claims/R#`. This PR only edits `tools/hooks/pre-push.ps1` (parallel/stale copy). Either delete/sync the duplicate or edit `tools/git-hooks/pre-push.ps1` so the claimed R688+R697 fix actually lands on the installed hook.

- **`timers.safetimer-disposal` catch{} regression** — Replaced `finally` post-dispose no-op checks with empty `catch { }` then unconditional Start/Stop/Dispose. Swallows construction/tick failures and weakens the pin. Prefer keep `finally` (or assert no throw without swallowing).

## Notes

- Branch `r809-phase1b-impl` → `main`; two commits (`3f11861c` Phase 1b gates + pins; `6c3a7e3a` pre-push R-claim on `tools/hooks/`). +126/−12 across 3 files.
- Phase 1b methods themselves are readable scaffolding: three-artifact presence check + Phase-1 revalidation checklist returning keyed results. Fine as library until export wire-in (PR body already notes Phase 2).
- Author layibabalola (co-authored Haiku on first commit). Review via `gh`. No GitHub PR comment posted.
