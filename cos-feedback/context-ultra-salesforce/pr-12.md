schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 12
head: 435e42c4f573783c6347e26da2b23972ad1db64c
reviewed_utc: 2026-09-14T19:05:00Z
event: pr-opened
verdict: info
ci_summary: MERGED at 2026-09-14T19:03:53Z (merge commit 284eb37); windows-app build-and-selftest FAILED (NU1004 lock file); kmp-parity-drift-check pass; conformance pass; jvm-test/assemble-debug still pending at review

## Blockers

- **Post-merge CI: `windows-app` / `build-and-selftest` → Publish (locked restore)** — `NU1004`: `Microsoft.NET.ILLink.Tasks` moved `[10.0.11,)` → `[10.0.12,)`; `packages.lock.json` out of sync so `RestoreLockedMode=true` fails before selftests run. Unblock: regenerate/update the App lock file for ILLink 10.0.12 (`dotnet restore` with lock update / `--force-evaluate` per project policy), commit lockfile, verify `dotnet publish -p:RestoreLockedMode=true` locally, open a follow-up PR onto `main`. Not caused by the R780 diff itself (SDK/transitive bump on the runner); still blocks green publish on the landed tip.

## Improvements

- `src/SfTools.App/AutomationHealth.cs` `ComputeBackoffDelayMs`: XML/doc says formula uses `2^min(failures-2, 3)` then caps at 4× base. With the outer `Math.Min(..., baseIntervalMs * 4)`, `Math.Min(failures - 2, 3)` is dead width (8× never returned). Prefer `Math.Min(failures - 2, 2)` (or drop the second cap) so comment and code agree; add/keep a selftest for failures=5 → exactly 4× base.
- Six files only add `using SfTools.Core.Storage;` (`CaseLedgerForm`, `CaseOracle`, `CaseRecordView`, `ChatImport`, `EscalationComposerForm`, `InvestigateRunner`). Confirm each is required for compile after a type move; drop any unused usings if IDE/analyzer flags them.
- PR body claims “Failed: 4 (pre-existing)” and R780 selftests green locally — after lockfile fix, re-run the Windows gate and either clear those four or file explicit waivers so the next tip is honest about residual red.

## Notes

- Scope of #12 vs `main`: +98/−1 across 9 files — `FollowUpsCli` `InSendWindow(nowLocal, …)` string match for `businessweek.one-source`; new `ComputeBackoffDelayMs` helper for health selftests; new `android/kmp/conformance/kmp-parity-baseline.txt` (77 mirrored SHA256s, `#parity-hash=v2-lf-normalized`); Storage usings.
- Earlier Phase-1 design work (health key split, `ForgivenBlip`/`BackoffExpiresAtUtc`, silence-on-backoff) is described in the first commit message but is not in this PR’s file diff vs base — already on `main` or squash-adjacent; this tip is the test/baseline follow-through.
- Auto-merged ~6s after open; consumers may ignore this file for open-work routing; left for audit + the NU1004 unblock.
- CoS reviewed via `gh` (Cloud Agents not on plan). No GitHub PR comment posted.
