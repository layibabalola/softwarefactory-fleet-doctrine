schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 38
head: 2751548f9b93381c2b298a188baf5f6dd2b76ff7
reviewed_utc: 2026-09-24T19:20:55Z
event: pr-opened
verdict: not-merge-ready
ci_summary: windows-app build-and-selftest SKIPPED (HOSTED_CI=paused, run 36047244975); kmp-parity-drift-check PASS (36047290769 + 36047244946); MERGEABLE/CLEAN; 0 reviews; claimed R835 pins absent and selftest assertion mangled so a green battery is not expected if the leg runs

## Blockers

- **R835 Phase 2 pins missing** — title/body claim two new SelfTest gate pins (`mandate.artifact-timestamps-valid`, `mandate.consensus-enforced`). Tip `SelfTestCli.cs` has neither string; only incidental R835 wording in an existing message. Unblock: add the pins that validate ISO8601 timestamps + explicit consensus markers against swarm artifacts, or retitle/body to drop the R835 Phase 2 claim.
- **SelfTest assertion corrupted by bulk replace** — `realrun.error-masking-fixed` now does `script.Contains("Exit codes: 126")` while `tools/realrun.ps1` still contains `exit 126`. That pin fails as soon as selftest runs. Same tip rewrote ContractCheck exit-message strings to `"ok must Exit codes: 0"` etc. (messages only). Unblock: revert SelfTestCli to assert the literal PowerShell `exit 126` (and any other code literals), keep "Exit codes:" wording for XML docs only.
- **Scratch files on the tip** — root carries `temp-add-codes.ps1`, `temp-test-sha.txt`, and `test-defang.cs` (one-off codegen / defang scratch). Delete them from the branch before merge.
- **Stale `.gate-stamp.sha` committed** — file adds `1dab247af69103557fe9b8588c714a56ee419fe3`. PR body itself flags that stamp as stale / R591-bypass residue. Do not land a known-stale stamp; refresh after a real green local/CI gate or omit from this PR.

## Improvements

- **Doc-only R801 lost per-CLI semantics** — ~58 `*Cli.cs` files now share the generic line `0 = success, 1 = could-not-run, 2 = partial-errors, 3+ = critical`, overwriting useful specifics (e.g. AdoCli signed-out/PAT, Confluence sign-in). Prefer a shared schema sentence plus the CLI-specific mapping, or a single `docs/exit-codes.md` pointer, instead of identical one-liners that no longer match `return` sites.
- **Misplaced / wrong-target docs** — `CaptureSetupCli` puts an Exit-codes line on private `BuildUnavailableDialog`; `ContractCheckCli` stamps nested helpers (`RawPayloads`, `ContractBaseline`) that are not CLIs. Several files insert the line after `</summary>` rather than inside the class summary. Clean up so only public CLI entrypoints document process exit codes.
- **`McpProxyCli` comment mangling** — bulk replace turned prose like `ssh exit 255` / `exit 36` into `Exit codes: 255` / `Exit codes: 36` inside method summaries. Harmless at runtime but noisy; restore natural "exit N" wording in narrative comments.
- **CI does not cover this tip** — `windows-app` only listens to `push` (not `pull_request`) and is skipped while `HOSTED_CI=paused`. kmp-parity-drift-check is unrelated to these C# CLI docs. After fixing SelfTest, either clear `HOSTED_CI=paused` and re-push / `workflow_dispatch` windows-app, or rely on a proven local offline selftest before merge.
- **Stacked loghealth commit** — tip also carries `tools/loghealth/known-benign.md` WATCH note on export-door 300s timeouts (2026-09-20 + 2026-09-24). Fine as ledger data; keep separate from R801/R835 if you want a clean review surface.

## Notes

- Scope: **+227/−72**, 66 files, 2 commits on `r-839-r801-r835-phase2` → `main`. Head `2751548f`. No process `return` / `Environment.Exit` behavior changes found; almost entirely XML-doc rewrite + broken SelfTest string replace + scratch/stamp/loghealth.
- PR body test plan ("CI validates R801 exit code docs across all 42 files" / "CI validates R835 Phase 2 pins") is not evidenced on this tip: no R835 pins, windows-app skipped.
- Cursor Cloud Agents not used (plan gap); review via gh. No GitHub PR comment posted.
