schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 25
head: c2f020786c36e59547fba9b5be3659795d34ffa7
reviewed_utc: 2026-09-19T01:51:59Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: build-and-selftest IN_PROGRESS/pending on windows-app run 35413902135 (job 105818657446); mergeable MERGEABLE; 0 GitHub reviews

## Blockers

(none for content — one-line `runs-on` rewrite matching the existing R363.3 / R408 pattern in `kmp-tests.yml` and `macos-app.yml`)

## Improvements

- **Wait for CI** — confirm `build-and-selftest` PASS on head `c2f020786c36e59547fba9b5be3659795d34ffa7` before merge. Repo var `WIN_RUNNER` is already set to `ultra-magnus`, so non-dependabot runs of this tip should land on that self-hosted Windows runner (not `windows-latest`).
- **Refresh the R363.3 comment** above `runs-on` — it still only documents `HOSTED_CI=paused` and the 2026-09-01 reset. Sibling legs name the runner variable and the R408 dependabot guard in the same block; mirror that here for `WIN_RUNNER` so the next reader does not think hosted-only is still the intent.
- **Reconcile open PR #10** — `ci(windows-app): route to the self-hosted Windows runner via vars.WIN_RUNNER` also edits `.github/workflows/windows-app.yml` for the same `runs-on` shape and additionally removes the `HOSTED_CI` gate. After #25 merges, close or rebase #10 so the two do not fight over the same line.

## Notes

- Tip delta: +1/−1 in `.github/workflows/windows-app.yml` only.
  - Before: `runs-on: ${{ (github.actor != 'dependabot[bot]') && 'windows-latest' || '' }}`
  - After: `runs-on: ${{ (github.actor != 'dependabot[bot]' && vars.WIN_RUNNER) || 'windows-latest' }}`
- Behavior change vs main: non-bot jobs pick `vars.WIN_RUNNER` when set (today: `ultra-magnus`), else `windows-latest`; dependabot always gets `windows-latest` (R408). Old tip used empty `runs-on` for dependabot.
- `if: ${{ vars.HOSTED_CI != 'paused' }}` is unchanged on this tip (contrast #10, which deletes that gate).
- Branch `r806-pr2-win-runner` → `main`; author layibabalola (co-authored Claude Haiku 4.5); review via gh. No GitHub PR comment posted. Related open sibling in the R806 series: PR #24 (`R806 PR 1: Fix compile error in SelfTestCli`).