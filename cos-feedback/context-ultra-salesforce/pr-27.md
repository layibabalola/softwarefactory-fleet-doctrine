schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 27
head: 1a6567d0a17401575fe404d93a5fececbe8f8a1d
reviewed_utc: 2026-09-19T01:54:12Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS (2 jobs); windows-app build-and-selftest IN_PROGRESS on run 35413968898 (Publish locked restore); mergeable MERGEABLE

## Blockers

(none for content — Phase 0 library + selftest pins + required swarm audits; no publish-path wiring yet, so no runtime regression risk on this tip)

## Improvements

- **Wire gates before claiming publish enforcement** — `KcsValidation` is only referenced from the three new selftest pins. `KcsPublish`, `KcsPublishCli`, `KcsArticles`, and review/health paths do not call `AttachmentExists` / `SwarmCited` / `CrossRefExists`. PR body says gates run "before KB articles ship"; Phase 0 as landed is API + pins only. Follow-up should call all three from the R259 publish gate (or staged path) and fail closed with the returned reason strings.
- **`AttachmentExists` evidence check is loose** — `hasAttachments` uses `EnumerateFiles("*.*")` over all descendants, so any file (including the transcript) satisfies the gate and `hasTranscripts` is effectively dead. Prefer explicit evidence patterns (e.g. `transcripts/**`, `attachments/**`, or known extensions) so an empty-ish folder with a stray file does not pass.
- **`SwarmCited` default audit path is cwd-relative** — default `".claude/audits"` breaks when the process cwd is not the repo root (WinForms / CLI publish). Mirror `DeriveLocalCaseFolder` / `NeedRepoRoot` and require an absolute `auditFolder` from the caller (selftests already pass one).
- **`CrossRefExists` case-ID regex is rigid** — `\b[0-9]{8}\b` rejects longer Salesforce Case Numbers and 15/18-char record Ids. Confirm the product case-id shape and widen (or accept an explicit `case_id:` / `CaseNumber` field) before wiring to publish.
- **Wait for CI** — confirm `build-and-selftest` PASS on head `1a6567d0` (new pins must run under `--expect-repo`) before merge.

## Notes

- Tip delta: +385/−2 across `src/SfTools.Core/Kcs/KcsValidation.cs` (new), `src/SfTools.App/SelfTestCli.cs` (3 pins + `NeedRepoRoot` fix on the audit-dimension pin), and `.claude/audits/R808-swarm-{compliance,failure-modes,ux}.md` (unanimous APPROVED; satisfies `gate.haiku-pre-code-swarm-enforced`).
- Gate API shape is sound: null = pass, string = fail reason; no throws; claim-gated early return on `SwarmCited` when title lacks `[Case-Validated]`.
- `NeedRepoRoot(out var repoRoot)` swap for the audit-dimension pin matches the battery's skip convention (good drive-by).
- Branch `r808-kcs-phase0` → `main`; author layibabalola (co-authored Claude Haiku 4.5); review via gh (no Cloud Agent). No GitHub PR comment posted.
