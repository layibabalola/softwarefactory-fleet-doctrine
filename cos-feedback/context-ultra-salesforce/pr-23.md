schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 23
head: fd886a69eaab4c392faa4a5a47e1e1fa3c07b829
reviewed_utc: 2026-09-19T00:33:30Z
event: pr-opened
verdict: merge-ready
ci_summary: no checks reported on PR tip branch claude/ecstatic-diffie-7b1ef9 (statusCheckRollup empty); mergeable MERGEABLE / CLEAN — same post-merge-gate pattern as recent CUS PRs

## Blockers

(none for content — Phase 1 foundation: pre-push path fix + docs + DryRun-tested WinRM script; no workflow or app-code edits)

## Improvements

- **`tools/github-runner-migrate-logon.ps1` interactive LogOn still passes `$null` password** — remote `Win32_Service.Change(..., $NewUser, $null, ...)` is fine for `NT AUTHORITY\NETWORK SERVICE` rollback, but the interactive-user branch is explicitly a placeholder ("production script needs secure credential passing"). Before any non-DryRun deploy (Phase 1 Task deploy / Phase 2), add secure credential input (e.g. `Get-Credential` / encrypted secret) and fail closed if password is missing for a non-builtin account; keep DryRun as the only safe default.
- **CLAUDE.md gate-stamp path disagrees with the code** — Laws entry says project-root `C:\ContextUltraSalesforce\.gate-stamp.sha`; `build.ps1` (R690) and this PR's `pre-push.ps1` both use `Join-Path (git rev-parse --show-toplevel) ".git\gate-stamp"`. Align the Laws text (and ROADMAP wording) to `.git\gate-stamp` so operators do not hunt the wrong file.
- **Prefer `Get-CimInstance` over `Get-WmiObject`** in the remote scriptblock (`Win32_Service`); WMI cmdlets are deprecated on modern PowerShell 7 hosts and can be absent or remoting-fragile.
- **Example host typo** — SYNOPSIS example uses `ultramanus.delinea.internal`; elsewhere the machine is UltraMagnus. Fix the example so copy-paste DryRun hits the right box.
- **Phase 1 still open after this tip** — ROADMAP R806 lists Task 2 (machine DPAPI / HKLM keystore) and Task 5 (interactive session persistence for 30+ min jobs) as Phase 1 preflight; this PR only lands Task 1 (gate-stamp path), Task 3 (MCP port docs), script skeleton, and ROADMAP/CLAUDE entries. Title/body already say foundation — keep follow-ups as separate PRs; do not treat merge as "preflight complete."
- **Selftest pins named but not added** — ROADMAP cites `gate.runner-logon-idempotent` and `audit.runner-state-verified`; neither appears in this tip. Either land stubs/pins with the deploy PR or drop the claim until they exist.

## Notes

- Tip delta: +198/−1 across `CLAUDE.md`, `docs/ROADMAP.md`, `tools/git-hooks/pre-push.ps1`, `tools/github-runner-migrate-logon.ps1` (new, 180 lines). Three commits: foundation + Task 3 ports + Task 1 gate-stamp path.
- Pre-push fix is the real bugfix: hook previously read `%APPDATA%\ContextUltra\Salesforce\.last-gate-passed` while `build.ps1` writes `.git\gate-stamp`, so R686–R691 enforcement was silently broken. Alignment looks correct on this tip.
- MCP reservation is documentation-only (8765 dev, 8766–8770 runner); no listener/bind code in this PR.
- Script strengths: `#requires -Version 7.0`, DryRun, idempotent same-user short-circuit, TEMP log, WinRM preflight via `Test-WSMan`, best-effort Start-Service on failure path.
- Scoped isolation exception in CLAUDE.md is appropriately labeled infrastructure-tier; rollback story (NETWORK SERVICE inverse) is documented but depends on the password path above for interactive users.
- Branch `claude/ecstatic-diffie-7b1ef9` → `main`; author layibabalola (co-authored Claude Haiku 4.5); Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.