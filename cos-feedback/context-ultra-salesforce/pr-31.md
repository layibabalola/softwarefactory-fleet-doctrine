schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 31
head: 02930d4038b10a32b60226bfa08327d36448415c
reviewed_utc: 2026-09-20T00:47:54Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS (two check jobs); windows-app build-and-selftest IN_PROGRESS on run 35479651191; mergeable MERGEABLE; 0 GitHub reviews

## Blockers

(none for content — Phase 1 scaffolding + docs/audits + structural selftest pins; no content blockers on tip `02930d40`)

## Improvements

- **Wire gates into export before claiming enforcement** — `CaseAnalysisTemplate` is new and unused: no calls from `ExportCaseCli.cs`, `PdfRenderer.cs`, `CaseExportRules.cs`, or `CallRunbook.cs` on this tip. Phase 1b (Haiku swarm before PDF) is the right place; until then R809 does not actually block PDF export. Call `ValidateAllGates` + `ValidateSwarmArtifactsExist` on the export path and fail closed with `GenerateGateFeedback`.

- **Gate 1 keyword set is a no-op for hex** — `IsErrorCodeVerified` treats `"0x"` as a verification keyword. Any brief that already contains a hex code will pass because the same token that triggered detection also satisfies "verified". Require stronger markers (e.g. `verified as`, `winerror.h`, `ERROR_`, `confirmed as`) and drop bare `"0x"`.

- **Gate 4 path + scope** — `ValidateSwarmArtifactsExist` climbs `caseFolder/../../../.claude/audits` and accepts any `R*-swarm-*.md`. Prefer a resolved repo-root audits path (or case-scoped artifacts) and require the three R809-named swarm files for the active R-number, not an unrelated historical audit.

- **Selftests are existence-only** — the four new pins only assert method names / substrings in `CaseAnalysisTemplate.cs`. Add behavioral fixtures (pass/fail markdown samples) so Gate 1–3 regressions fail the pin, not just a rename.

- **HasMandatoryContextSections / HasThreeWayScopeTest are loose** — section regex alternation and keyword OR-matching (`ssh`, `privilege`, `app.*specific`) can pass without the intended Notepad/PuTTY/SecureCRT triad or a real Security & Hardening header. Tighten to required headers + explicit triad tokens when Phase 1b hardens enforcement.

## Notes

- Tip `02930d40` (+606/−3, 7 files): CLAUDE.md R784 case-analysis clause; ROADMAP R809; three swarm audits under `.claude/audits/`; new `src/SfTools.Core/CaseAnalysis/CaseAnalysisTemplate.cs`; four structural pins in `SelfTestCli.cs`. Branch `claude/wizardly-mestorf-293839` → `main`.
- Intent matches the stated root cause (error-code misID before peer review): doctrine + validator library first, runtime gate in Phase 1b. Do not treat this merge as "swarm enforced on export" until wiring lands.
- Customer-case narrative in audits is process evidence only (Law 4); no secrets in the diff.
- Author layibabalola (co-authored Claude Haiku 4.5). Review via `gh` (Cursor Cloud Agents not used). No GitHub PR comment posted.