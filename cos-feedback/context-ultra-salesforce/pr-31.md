schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 31
head: 51d3e31f6b8f8a62c2357984ba1879dba99e1a86
reviewed_utc: 2026-09-20T00:55:27Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS (two check jobs); windows-app build-and-selftest IN_PROGRESS on run 35479978357; mergeable MERGEABLE; mergeStateStatus UNSTABLE; 0 GitHub reviews

## Blockers

(none for content — tip `51d3e31f` is a one-line Contains() compatibility fix on Gate 1; Phase 1 scaffolding unchanged)

## Improvements

- **Wire gates into export before claiming enforcement** — `CaseAnalysisTemplate` is still unused: no calls from `ExportCaseCli.cs`, `PdfRenderer.cs`, `CaseExportRules.cs`, or `CallRunbook.cs`. Phase 1b should call `ValidateAllGates` + `ValidateSwarmArtifactsExist` on the export path and fail closed with `GenerateGateFeedback`.

- **Gate 1 keyword set is still a no-op for hex** — tip `51d3e31f` only swapped `Contains(keyword, StringComparison.OrdinalIgnoreCase)` for `ToLowerInvariant` + `Contains(keyword)` (likely older BCL). The keyword list still includes bare `"0x"`, so any brief with a hex code still auto-passes verification. Drop `"0x"` and require stronger markers (`verified as`, `winerror.h`, `ERROR_`, `confirmed as`).

- **Gate 4 path + scope** — `ValidateSwarmArtifactsExist` still climbs `caseFolder/../../../.claude/audits` and accepts any `R*-swarm-*.md`. Prefer a resolved repo-root audits path and require the three R809-named swarm files for the active R-number.

- **Selftests are existence-only** — four pins still only assert method names / substrings in `CaseAnalysisTemplate.cs`. Add behavioral fixtures (pass/fail markdown samples).

- **HasMandatoryContextSections / HasThreeWayScopeTest are loose** — section regex alternation and keyword OR-matching can pass without the Notepad/PuTTY/SecureCRT triad or a real Security & Hardening header. Tighten in Phase 1b.

## Notes

- Push delta vs prior CoS head `02930d40`: single file `CaseAnalysisTemplate.cs` (+2/−1) — `IsErrorCodeVerified` pre-lowercases content before keyword `Contains`. Does **not** address prior CoS Improvements.
- Full tip still +607/−3 across 7 files (CLAUDE.md R784, ROADMAP R809, three `.claude/audits/R809-swarm-*.md`, new template, four structural SelfTestCli pins). Branch `claude/wizardly-mestorf-293839` → `main`.
- Verdict unchanged from prior pass (`merge-when-ci-green`). Do not treat merge as "swarm enforced on export" until wiring lands.
- Author layibabalola. Review via `gh`. No GitHub PR comment posted.