schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 24
head: 24e18f12d1faf1cffcb11a2d3c39fd980b990832
reviewed_utc: 2026-09-19T01:51:07Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS (2 check runs); windows-app build-and-selftest IN_PROGRESS on run 35413845788; mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews

## Blockers

(none for content — tip replaces undefined `GetRepoRoot()` with the established `NeedRepoRoot(out var …)` skip pattern in the `audit.self-attestation-integrity-dimension-runs` pin; matches dozens of sibling call sites in `SelfTestCli.cs`)

## Improvements

- **Wait for `build-and-selftest`** — this PR exists to fix a CS0103 compile break; do not merge until `windows-app` / `build-and-selftest` is green on head `24e18f12` (still IN_PROGRESS at review time).
- **Restore trailing newline** on `src/SfTools.App/SelfTestCli.cs` — tip ends with "No newline at end of file"; re-add the final newline so the delta stays a pure call-site fix.
- **PR body vs tip** — Summary claims "Added missing auditDir variable definition"; tip only swaps `GetRepoRoot` → `NeedRepoRoot` and `auditDir` was already present. Tighten the body so R806 follow-ups do not chase a phantom change.

## Notes

- Tip delta: +2/−5 in `src/SfTools.App/SelfTestCli.cs` only. Branch `r806-compile-fix` → `main`; author layibabalola (co-authored Claude Haiku 4.5); single commit.
- Pre-change pattern `var repoRoot = GetRepoRoot(); if (repoRoot is null) return "SKIP: not in repo";` was the CS0103 source (`GetRepoRoot` not defined). Replacement `if (NeedRepoRoot(out var repoRoot) is { } skip) return skip;` is idiomatic for this file.
- R806 naming in title/body is sequencing context only; this tip does not land Phase 1 Tasks 2/5 from ROADMAP (those remain separate PRs after #23 foundation).
- Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.
