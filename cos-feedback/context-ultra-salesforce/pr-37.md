schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 37
head: 2e463cebdd78fa56ec6bf7de10077651885b6469
reviewed_utc: 2026-09-20T22:41:31Z
event: pr-opened
verdict: not-merge-ready
ci_summary: windows-app run 35542366382 FAILED on tip 2e463ceb — build-and-selftest died at "Install git hooks" with `pwsh: command not found` on self-hosted UltraMagnus; kmp-parity-drift-check PASS (runs 35542367537 + 35542392977); mergeable MERGEABLE but branch behind main by 5; 0 GitHub reviews

## Blockers

- **windows-app CI red on first UltraMagnus run** — run [35542366382](https://github.com/layibabalola/context-ultra-salesforce/actions/runs/35542366382) job `build-and-selftest` failed at step "Install git hooks (R690/R785 enforcement infrastructure)" with `pwsh: command not found`. Checkout + `setup-dotnet` succeeded; publish/selftest never ran. Unblock: install PowerShell Core (`pwsh`) on UltraMagnus for the runner LogOn account (`ULTRA-MAGNUS\obabalola`) and ensure it is on PATH for Actions, **or** change the hooks step (and any other `shell: pwsh` steps) to `shell: powershell` if Windows PowerShell 5.1 is the intended runtime on this box. Re-run `windows-app` via workflow_dispatch or a tip push after the runner fix.
- **Branch behind `main` by 5** — compare `main...2e463ceb` is ahead 4 / behind 5. Rebase or merge `main` before treating this as mergeable; stacked R809 commits may conflict with landed main work.

## Improvements

- **Pin runner labels** — `runs-on` is bare `self-hosted`. Prefer an explicit label set that can only match UltraMagnus Windows (e.g. `[self-hosted, Windows, UltraMagnus]` or whatever labels that runner advertises) so a future Linux/mac self-hosted runner cannot claim `windows-app`.
- **Title vs payload mismatch** — title/body sell R806 workflow-only (`windows-app.yml` `runs-on` flip + comment). Diff also carries R809 Phase 1b (Haiku swarm + post-validation helpers), PDF redaction gate, selftest pins, and pre-push R-claim check (R688+R697). Split into R806-only vs R809 PRs, or retitle/body to list every R-claim on the tip.
- **Gates 6/7 not wired into export** — `RequiresHaikuSwarmBeforePdf` and `RunPostValidationChecklist` exist on `CaseAnalysisTemplate` but `ExportCaseCli.RenderMarkdownToPdf` only calls `MainForm.Redact`. Commit message itself says ready for Phase 2 wire-in. Either call both gates before PDF write (fail closed) or drop the "enforced" framing until wired; selftest pins today only grep source strings.
- **`RequiresHaikuSwarmBeforePdf` path / naming** — resolves `caseFolder/../../../.claude/audits` and matches hardcoded `R809-swarm-*.md`. That is repo-global and R809-named, not per-case; fragile relative depth. Prefer a repo-root-relative audit path and case-scoped (or generation-scoped) artifact names once wired.
- **Post-validation checklist is keyword-loose** — confidence/timeline/metadata checks are broad regexes (`when`, `case`, `summary`, …) that pass almost any brief. Tighten to structured markers the template already emits, or mark checklist items as advisory until Phase 2.
- **Redaction empty-string refuse** — `if (redactedHtml.Length == 0) return false` treats empty output as failure. Confirm `MainForm.Redact` never returns empty on legitimate all-redacted or empty-input HTML; if it can, distinguish "redact failed" from "empty after redact".
- **`timers.safetimer-disposal` weakened** — try/finally became try/catch-swallow then post-dispose Start/Stop/Dispose. Prefer keep finally (or assert no throw) so a Start/tick failure cannot skip disposal checks.
- **Dependabot `runs-on: ''` still present** — unchanged empty-string pattern when actor is dependabot; confirm that still intentionally skips rather than erroring on self-hosted.

## Notes

- Scope: **+154/−16** across 5 files on `r806-workflow-update` → `main`. Four commits on the PR tip: R809 Haiku swarm + validation gates; R809 pre-push R-claim verify; R809 PDF redaction gate; R806 `windows-app.yml` → `self-hosted`.
- R806 intent is sound (cut hosted Windows minutes; runner LogOn migrated to interactive user per body). First self-hosted execution proved the shell/runtime gap before any compile/selftest value.
- Redaction wiring in `ExportCaseCli` is the one export-path behavior change that is live; Gate 6/7 are library + source-pin only on this tip.
- Cursor Cloud Agents not used (workflow + focused C#/hook review via gh). No GitHub PR comment posted.