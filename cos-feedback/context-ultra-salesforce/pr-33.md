schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 33
head: a0b61e772d22bf055ac896fa661567488075f7c0
reviewed_utc: 2026-09-20T01:40:25Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS (two check jobs); windows-app build-and-selftest IN_PROGRESS on run 35481927728; mergeable MERGEABLE; ahead_by 3 / behind_by 0; 0 GitHub reviews

## Blockers

(none for content — Phase 0 schema + manager + behavioral Create/ValidateAccess pins look coherent; wait on `build-and-selftest`)

## Improvements

- **UI is not wired** — `EvidenceAttachmentManager` is only referenced from `SelfTestCli.cs`. No Form/`CaseRecordForm`/export path calls `TryAddEvidenceFromDialog` or `ValidateAllEvidence`. Pin `kcs.evidence-attachment-form-wired` only reflects that the type/method exist (`typeof(...) is null` is dead). PR body "UI form wiring complete" overclaims. Hook a real control (or drop the claim until Phase 0b).

- **Redaction pin is a no-op; SOQL serializer ships absolute paths** — `kcs.evidence-attachment-redaction-compliant` only asserts `Evidences.Count == 1`. `SerializeEvidenceForSoql` JSON-serializes full `EvidenceAnchor` including `LocalPath`. That is the opposite of "paths filtered before SOQL/clipboard". Strip or hash-bucket paths (or run `Redact`) before any Salesforce/clipboard/log egress, and assert the redacted shape in the pin.

- **No Salesforce field mapping yet** — `CaseRecord.Evidences` persists with local CaseRecord JSON, but nothing maps to `case__Evidence_Anchors__c` (or any SOQL write). Fine for a pure local Phase 0 if labeled that way; do not treat SOQL roundtrip pins as proof of org storage.

- **Several pins remain structural** — `gate.r807-blockers-resolved-design-locked` and `kcs.evidence-anchor-health-reporting-wired` are source substring / "MSIX" comment checks. Keep the good behavioral pins (`Create`, missing-file `Inaccessible`, order/deletion); replace the rest with real assertions or rename them so they do not sound like design/enforcement gates.

- **Naming collision risk** — new type `SfTools.Core.CaseAnalysis.EvidenceAnchor` sits beside existing `Theory.EvidenceAnchor` (string). Prefer `EvidenceFileAnchor` / `AttachedEvidence` (or similar) before the type spreads across forms and docs.

## Notes

- Diff +500/−2 across 4 files: new `EvidenceAnchor.cs` (+115), new `EvidenceAttachmentManager.cs` (+60), `CaseRecord.Evidences` (+6), `SelfTestCli` (+319/−2) with 12 R807 pins. Branch `r807-case-analysis-evidence-phase0` → `main`. Three commits; tip `a0b61e77` fixes compile / `NeedRepoRoot` / `AutomationHealth.Report` call shapes.
- Core model is solid for Phase 0: required fields, SHA256, `AccessStatus` Accessible/Inaccessible/Stale, `ValidateAccess` fail-safe. MSIX "awareness" is absolute-path-as-reference commentary only (no overlay resolver).
- Author layibabalola (Co-Authored-By Claude Haiku 4.5). Review via `gh`. No GitHub PR comment posted.