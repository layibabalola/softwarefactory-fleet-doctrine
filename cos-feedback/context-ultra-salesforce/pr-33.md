schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 33
head: 75f219549ba7c1f3355425fb0ac0b5c8cc3702e3
reviewed_utc: 2026-09-20T01:47:04Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS (two check jobs); windows-app build-and-selftest IN_PROGRESS on run 35482229337; mergeable MERGEABLE; ahead_by 4 / behind_by 0; 0 GitHub reviews

## Blockers

(none for content — tip `75f21954` is brief-log-sweep hygiene on the catch path; Phase 0 schema + manager + behavioral Create/ValidateAccess pins still coherent; wait on `build-and-selftest`)

## Improvements

- **UI is not wired** — `EvidenceAttachmentManager` is only referenced from `SelfTestCli.cs`. No Form/`MainForm`/export path calls `TryAddEvidenceFromDialog` or `ValidateAllEvidence`. Pin `kcs.evidence-attachment-form-wired` only reflects that the type/method exist (`typeof(...) is null` is dead). PR body "UI form wiring complete" overclaims. Hook a real control (or drop the claim until Phase 0b).

- **Redaction pin is a no-op; SOQL serializer ships absolute paths** — `kcs.evidence-attachment-redaction-compliant` only asserts `Evidences.Count == 1`. `SerializeEvidenceForSoql` JSON-serializes full `EvidenceAnchor` including `LocalPath`. That is the opposite of "paths filtered before SOQL/clipboard". Strip or hash-bucket paths (or run `Redact`) before any Salesforce/clipboard/log egress, and assert the redacted shape in the pin.

- **No Salesforce field mapping yet** — `CaseRecord.Evidences` persists with local CaseRecord JSON, but nothing maps to `case__Evidence_Anchors__c` (or any SOQL write). Fine for a pure local Phase 0 if labeled that way; do not treat SOQL roundtrip pins as proof of org storage.

- **Several pins remain structural** — `gate.r807-blockers-resolved-design-locked` and `kcs.evidence-anchor-health-reporting-wired` are source substring / "MSIX" comment checks. Keep the good behavioral pins (`Create`, missing-file `Inaccessible`, order/deletion); replace the rest with real assertions or rename them so they do not sound like design/enforcement gates.

- **Naming collision risk** — new type `SfTools.Core.CaseAnalysis.EvidenceAnchor` sits beside existing `Theory.EvidenceAnchor` (string). Prefer `EvidenceFileAnchor` / `AttachedEvidence` (or similar) before the type spreads across forms and docs.

## Notes

- Push delta vs prior CoS head `a0b61e77`: single file `EvidenceAttachmentManager.cs` (+2/−1) — catch path now `FileLog.Code` + `ExceptionText.Brief(ex)` into `AutomationHealth.Report` (brief-log-sweep). Does **not** address prior CoS Improvements.
- Full tip still +501/−2 across 4 files: `EvidenceAnchor.cs` (+115), `EvidenceAttachmentManager.cs` (+61), `CaseRecord.Evidences` (+6), `SelfTestCli` (+319/−2) with 12 R807 pins. Branch `r807-case-analysis-evidence-phase0` → `main`. Four commits.
- Core model unchanged and solid for Phase 0: required fields, SHA256, `AccessStatus` Accessible/Inaccessible/Stale, `ValidateAccess` fail-safe. MSIX "awareness" remains absolute-path-as-reference commentary only.
- Verdict unchanged from prior pass (`merge-when-ci-green`). Author layibabalola. Review via `gh`. No GitHub PR comment posted.