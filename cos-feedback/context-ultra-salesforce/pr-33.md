schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 33
head: 9069775473b31b2f3a3f2ba25fe09aaa55076c32
reviewed_utc: 2026-09-20T02:26:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS (two check jobs); windows-app build-and-selftest IN_PROGRESS on run 35483920126; mergeable MERGEABLE / UNSTABLE; ahead tip 90697754; 0 GitHub reviews

## Blockers

(none for content — tip `90697754` adds swarm audit artifacts + failure-modes hotfixes + pin rename; Phase 0 schema + manager + export pre-check still coherent; wait on `build-and-selftest`)

## Improvements

- **Claimed `IsEmpty` fix did not land** — commit `5ef0934f` message says `Evidences.Count == 0` was added to `CaseRecord.IsEmpty`, but tip `CaseRecord.cs` still omits `Evidences` from the predicate (patch was EOF blank only). Re-apply before treating failure-modes CRITICAL as closed.

- **UI attach path still unwired** — `TryAddEvidenceFromDialog` / drag-drop still only exist on `EvidenceAttachmentManager` + SelfTest. New `ExportCaseCli` pre-export call to `ValidateAllEvidence` is a real production path for validation, not for attach. Pin `kcs.evidence-attachment-form-wired` remains structural. Hook a Form control or drop the "UI form wiring complete" claim.

- **SOQL serializer still ships absolute paths** — pin renamed to `kcs.evidence-attachment-exception-safe` (good; matches what it asserts). `SerializeEvidenceForSoql` still JSON-serializes full `EvidenceAnchor` including `LocalPath`. Strip/hash-bucket paths (or `Redact`) before any Salesforce/clipboard/log egress when Phase 1 writes SOQL.

- **Health-report guard incomplete on attach catch** — `ValidateAllEvidence` now wraps `AutomationHealth.Report` in try/catch; `TryAddEvidenceFromDialog` catch still calls `Report` unguarded (failure-modes finding only half-fixed).

- **No Salesforce field mapping yet** — `CaseRecord.Evidences` is local JSON only; nothing maps to `case__Evidence_Anchors__c`. Fine for local Phase 0 if labeled that way.

- **Several pins remain structural** — `gate.r807-blockers-resolved-design-locked` and `kcs.evidence-anchor-health-reporting-wired` stay source/substring checks. Keep behavioral pins; rename or replace the rest.

- **Naming collision risk** — `SfTools.Core.CaseAnalysis.EvidenceAnchor` vs `Theory.EvidenceAnchor` (string). Prefer `EvidenceFileAnchor` / `AttachedEvidence` before wider spread.

## Notes

- Push delta vs prior CoS head `75f21954`: four commits — pin rename (`5ef0934f`), three `.claude/audits/R807-swarm-*.md` CONDITIONAL-PASS artifacts (`a111ba49`), ExportCaseCli pre-export evidence validation + ValidateAllEvidence health try/catch (`26f2e1c8`), Diagnostics using fix on manager (`90697754`).
- Prior CoS Improvements partially addressed: redaction pin **renamed** (not made redaction-real); export path now calls `ValidateAllEvidence`. IsEmpty / full UI attach / path redaction / type rename still open.
- Full tip still +1480/−2 across 8 files (3 audit md + EvidenceAnchor + manager + ExportCaseCli + SelfTestCli + CaseRecord). Branch `r807-case-analysis-evidence-phase0` → `main`.
- Verdict unchanged (`merge-when-ci-green`). Author layibabalola. Review via `gh`. No GitHub PR comment posted.