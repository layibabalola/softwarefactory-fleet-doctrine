schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 16
head: 348f29b0911fecbdf2d33d6d254b416d73d31bf5
reviewed_utc: 2026-09-16T23:04:38Z
event: pr-opened
verdict: not-merge-ready
ci_summary: no hosted checks triggered (tools/ + docs/ only; windows-app and kmp-parity path filters do not match); local selftest claimed green in PR body

## Blockers

- `tools/hardening-audit-scheduled.ps1`: three-argument `Join-Path` (`$RepoRoot, ".worktrees", "hardening-audit-nightly"`, same for `.git/hardening-audit.lock` and `logs/hardening-audit.log`) requires PowerShell 7+. Windows PowerShell 5.1 (common Task Scheduler host) errors on the third positional. Nest two-arg `Join-Path` calls (as `schedule-standup.ps1` does) or add `#Requires -Version 7` and ensure Phase 2 registers `pwsh.exe`.
- `tools/hardening-audit-scheduled.ps1` DryRun path: after `git worktree add` (Step 4) and HEAD check, `-DryRun` does `exit 0` before Step 7 cleanup, so the audit worktree is left behind (finally only clears mutex/lock). Move DryRun before worktree create, or run the same remove/prune path before exiting.

## Improvements

- Initialize `$Mutex = $null` (and optionally `$AuditLockFile` ownership flag) before `try` so StrictMode `finally` does not throw when failure happens before mutex construction.
- Step 7 post-audit `git worktree remove` does not re-check `git worktree list` the way the hotfix added for Step 3; mirror the verify-or-exit-1 pattern so a failed cleanup cannot collide on the next run.
- ROADMAP lists selftest pins (`audit.scheduled-worktree-determinism`, `audit.scheduled-mutex-acquired`, `audit.scheduled-network-verified`, `audit.scheduled-logging-enabled`) but this PR does not add them; at least pin lock TTL recovery, fetch timeout flags, and removal verification.
- Prefer setting `$global:LASTEXITCODE` / `return` and a single exit at script end over many `exit` calls inside `try` (clearer finally semantics across hosts).
- File missing final newline (`\ No newline at end of file`).

## Notes

- Scope: new `tools/hardening-audit-scheduled.ps1` (+144) and one ROADMAP R792 bullet; branch stacks Phase 1 safeguards (`71ae6290`) plus hotfix (`348f29b0`) for lock 4h TTL, git HTTP timeouts, and pre-create worktree removal verification.
- Hotfix delta vs first commit matches the three claimed fixes; remaining issues are compatibility and control-flow gaps around those safeguards.
- Soft `exit 0` on mutex busy / fresh lock is intentional defer (no escalation); document that for ops so a stuck peer is not mistaken for a healthy green task.
- Phase 2 (`HardeningAuditScheduler.cs` / task registration) still queued per ROADMAP; fix Join-Path (or pin pwsh) before registering the scheduled task.
- No GitHub PR review comments posted (CoS bus only).