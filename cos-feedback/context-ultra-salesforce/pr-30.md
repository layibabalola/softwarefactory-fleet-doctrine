schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 30
head: 0fb6cf6bb5fc8da1c2f42bccb6268634b111df69
reviewed_utc: 2026-09-19T23:35:20Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip 0fb6cf6 windows-app build-and-selftest FAILED in ~4s with empty steps and no runner_name (hosted billing/spend; same class as prior tips and #29 kmp). Path-filter fix worked: this push did re-fire CI.

## Blockers

- **`build.ps1` still writes `.git\gate-stamp`** — tip `0fb6cf6` only adds `build.ps1` to workflow `on.push.paths`. Writer assignment is still `Join-Path (git rev-parse --show-toplevel) ".git\gate-stamp"`; the R806 comment still claims project-root `.gate-stamp.sha` and still embeds literal `` `n `` in one line. Both `tools/git-hooks/pre-push.ps1` and `tools/hooks/pre-push.ps1` read `.gate-stamp.sha`. After a green gate, pre-push will BLOCK (missing stamp at the new path). Fix: set `$gateStampPath` to `.gate-stamp.sha` under the repo root so writer + both hooks agree.

- **Stamp content format still disagrees** — `build.ps1` still writes `"$commitSha`:$timestamp"` (SHA + colon + ISO). `tools/hooks/pre-push.ps1` does `$stampSha = $stampContent.Trim()` and compares to `$localSha` (expects bare SHA). `tools/git-hooks/pre-push.ps1` splits on `:` and takes `[0]` (tolerates SHA:timestamp). Pick one format (bare SHA vs `SHA:timestamp`) and make writer + both hooks match before merge.

- **Hosted CI still cannot start** — run 35476498749 job `build-and-selftest` failed instantly with empty steps and blank `runner_name`. Clear spending limit / payment so `windows-latest` can start; until then CI silence is not green.

## Improvements

- **Rebase / update branch** — tip remains behind `main` (`4d052ca`). Main already carries later R806 / WIN_RUNNER work; landing without rebase risks fighting those workflow edits.

- **Hook install overwrite order** — CI copies `tools/git-hooks/*` then `tools/hooks/*` into `.git/hooks/`. Both trees ship `pre-push.ps1`; the second wins. Confirm which script the presence pins and the real `pre-push` wrapper invoke, so the stamp-format fix lands in the copy that actually runs.

## Notes

- Prior CoS Improvement on path filters is addressed: `build.ps1` is now in `on.push.paths`, and this tip did re-fire `windows-app`. Remaining blockers are writer path, stamp format, and hosted billing.
- Tip delta vs prior reviewed head `5a9c146`: +1 line in `.github/workflows/windows-app.yml` only.
- Full tip vs `main`: `.github/workflows/windows-app.yml` (hook install step + `build.ps1` path), `build.ps1` (comment-only so far), `tools/git-hooks/pre-push.ps1`, `tools/hooks/pre-push.ps1`.
- Related open work: PR #29 (R806 consolidated fixes) still red; this PR cannot unblock its hook pins until writer path + format align.
- Branch `claude/eloquent-chaplygin-e35931` → `main`; author layibabalola (co-authored Claude Haiku 4.5). Review via `gh`. No GitHub PR comment posted.