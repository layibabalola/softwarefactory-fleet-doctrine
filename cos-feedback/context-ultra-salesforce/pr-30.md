schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 30
head: 22ffdd3e400e26f13b416e27dadb49a4d2015196
reviewed_utc: 2026-09-19T23:37:57Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip 22ffdd3e windows-app build-and-selftest FAILED in ~3s with empty steps and blank runner_name (hosted billing/spend; same class as prior tips and #29). Path filter still fires CI.

## Blockers

- **`build.ps1` still writes `.git\gate-stamp`** — tip `22ffdd3e` restores the CI hook-install step and keeps `build.ps1` on `on.push.paths`, but the writer is still `Join-Path (git rev-parse --show-toplevel) ".git\gate-stamp"`. The R806 comment still claims project-root `.gate-stamp.sha` and still embeds literal `` `n `` in one line. Both `tools/git-hooks/pre-push.ps1` and `tools/hooks/pre-push.ps1` read `.gate-stamp.sha`. After a green gate, pre-push will BLOCK (missing stamp at the new path). Fix: set `$gateStampPath` to `.gate-stamp.sha` under the repo root so writer + both hooks agree.

- **Stamp content format still disagrees** — `build.ps1` still writes `"$commitSha`:$timestamp"` (SHA + colon + ISO). `tools/hooks/pre-push.ps1` does `$stampSha = $stampContent.Trim()` and compares to `$localSha` (expects bare SHA). `tools/git-hooks/pre-push.ps1` splits on `:` and takes `[0]` (tolerates SHA:timestamp). Pick one format (bare SHA vs `SHA:timestamp`) and make writer + both hooks match before merge.

- **Hosted CI still cannot start** — run 35476597168 job `build-and-selftest` failed instantly with empty steps and blank `runner_name`. Clear spending limit / payment so `windows-latest` can start; until then CI silence is not green.

## Improvements

- **Rebase / update branch** — tip is behind `main` by 5 commits (`ahead_by` 3 / `behind_by` 5). Main already carries later R806 / WIN_RUNNER work; landing without rebase risks fighting those workflow edits.

- **Hook install overwrite order** — CI copies `tools/git-hooks/*` then `tools/hooks/*` into `.git/hooks/`. Both trees ship `pre-push.ps1`; the second wins. Confirm which script the presence pins and the real `pre-push` wrapper invoke, so the stamp-format fix lands in the copy that actually runs.

## Notes

- Tip `22ffdd3e` is a sibling rewrite of prior reviewed head `0fb6cf6` (same parent `5a9c146`, same commit message). Net change vs that head: restores the missing `Install git hooks` step (including `New-Item` for `.git/hooks`) that `0fb6cf6` had dropped while keeping `build.ps1` on path filters. Verdict unchanged.
- Prior CoS Improvements (rebase; overwrite order) not addressed. Writer path + stamp format blockers unchanged.
- Related open work: PR #29 (R806 consolidated fixes) still red; this PR cannot unblock its hook pins until writer path + format align.
- Branch `claude/eloquent-chaplygin-e35931` → `main`; author layibabalola (co-authored Claude Haiku 4.5). Review via `gh`. No GitHub PR comment posted.