schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 30
head: 5a9c146f5374306c5a417380e42b1fe83a950939
reviewed_utc: 2026-09-19T23:33:42Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: no checks on tip 5a9c146; prior tip ca7a7db windows-app build-and-selftest FAILED in ~5s with empty steps/no runner (likely hosted billing/spend, same class as #29 kmp); second push is build.ps1-only so path filters may not re-fire windows-app

## Blockers

- **`build.ps1` still writes `.git\gate-stamp`** — tip commit `5a9c146` claims "Update build.ps1 to write gate-stamp to project root (.gate-stamp.sha)" but only rewrote the comment (and baked literal `` `n `` into one line). Assignment remains `Join-Path (git rev-parse --show-toplevel) ".git\gate-stamp"`. Both `tools/git-hooks/pre-push.ps1` and `tools/hooks/pre-push.ps1` now read `.gate-stamp.sha`. After a green gate, pre-push will BLOCK (missing stamp at the new path). Fix: change `$gateStampPath` to `.gate-stamp.sha` under the repo root, and keep writer + both hooks on the same path.

- **Stamp content format still disagrees** — `build.ps1` still writes `"$commitSha`:$timestamp"` (SHA + colon + ISO). `tools/hooks/pre-push.ps1` now does `$stampSha = $stampContent.Trim()` and compares to `$localSha` (expects bare SHA). `tools/git-hooks/pre-push.ps1` still splits on `:` and takes `[0]` (tolerates SHA:timestamp). Pick one format (bare SHA vs `SHA:timestamp`) and make writer + both hooks match before merge.

## Improvements

- **Rebase / update branch** — tip is 2 ahead / 5 behind `main` (diverged). Main already carries later R806 / WIN_RUNNER work from #29 era; landing this without rebase risks fighting those workflow edits and leaving CI routing stale on this branch.

- **Re-fire `windows-app` after the writer fix** — workflow `on.push.paths` only includes `src/**`, `Directory.Build.props`, `.github/workflows/windows-app.yml`. A `build.ps1`-only push will not start `build-and-selftest`. Touch the workflow file or open a follow-up push that includes a watched path after the stamp-path fix so hosted CI actually validates the hook-install step.

- **Hosted CI billing** — prior branch run 35430301853 failed instantly with empty job steps and no `runner_name` (same failure mode as #29's billing-blocked kmp job). Clear spending limit / payment so `windows-latest` can start before treating CI silence as green.

- **Hook install overwrite order** — CI copies `tools/git-hooks/*` then `tools/hooks/*` into `.git/hooks/`. Both trees ship `pre-push.ps1`; the second wins. Confirm which script the presence pins and the real `pre-push` wrapper invoke, so the stamp-format fix lands in the copy that actually runs.

## Notes

- Intent of the tip is sound: install hooks in CI before `--selftest`, and move gate-stamp to project root for runner/APPDATA isolation. First commit (`ca7a7db`) correctly updates both pre-push scripts + adds the workflow install step; the second commit did not finish the writer side.
- Tip delta vs `main`: +20/−7 across 4 files — `.github/workflows/windows-app.yml`, `build.ps1`, `tools/git-hooks/pre-push.ps1`, `tools/hooks/pre-push.ps1`.
- Related open work: PR #29 (R806 consolidated fixes) still red on checks; this PR is meant to unblock hook-related gate pins there, but cannot until writer path + format are aligned.
- Branch `claude/eloquent-chaplygin-e35931` → `main`; author layibabalola (co-authored Claude Haiku 4.5). Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
