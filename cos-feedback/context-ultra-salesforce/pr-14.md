schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 14
head: 961813be1c32ac9b200d22ccec115d3bf665a554
reviewed_utc: 2026-09-16T20:17:45Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: build-and-selftest FAILED (NU1004 packages.lock vs Microsoft.NET.ILLink.Tasks 10.0.12); kmp-parity-drift-check PASS

## Blockers

- **Required CI red** — `windows-app` / `build-and-selftest` (run 35145525995) failed at Publish (locked restore): NU1004 — `Microsoft.NET.ILLink.Tasks` lock expects `[10.0.11,)` but restore resolved `[10.0.12,)`. Same lock drift as #13 / main; this PR does not touch lockfiles. Regenerate `src/SfTools.App/packages.lock.json` (CI SDK) on this branch or land a lockfix on main first, then re-run.

## Improvements

- `tools/session-resume-coordination-gate.ps1` `GetActiveRClaims`: defined and selftest-pinned, but never called from gate logic or `CheckGitClaims`. Wire discovery into claim enumeration (or drop the pin until Phase 3) so Phase 2 is not discovery-in-name-only.
- `GetActiveRClaims` / `CheckSessionDivergence`: empty `catch {}` swallows git/file errors. Prefer `Write-Warning` on failure so a silent empty claim set or missed divergence is visible.
- `src/SfTools.App/SelfTestCli.cs` pins `gate.r-discovery-dynamic-refs` / `gate.divergence-detection-enabled`: string-contains sniff only. Acceptable as presence pins; a fixture that seeds a fake `refs/r-claims/*` listing (or temp resume file with mismatched `session_id`) would prove behavior.
- Divergence path: `otherSessionId.Substring(0,12)` assumes length at least 12; guard with length check if short session ids are possible (same pattern already used for `$SessionId` elsewhere in this script).

## Notes

- Scope: R789B Phase 2 — dynamic R-claim discovery helper + non-blocking session divergence warning; +94/−0 across `tools/session-resume-coordination-gate.ps1` and `SelfTestCli.cs`.
- Divergence check is informational only (warn + proceed); overlaps partially with existing `CheckConcurrentClaims` memory scan.
- Local gate selftests claimed green in PR body; hosted `build-and-selftest` never reached selftest because restore failed.
- No GitHub PR review comments posted (CoS bus only).
