schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 17
head: 15e2be3aeab2d56cba21e2c7735349042eea5904
reviewed_utc: 2026-09-17T05:28:44Z
event: pr-opened
verdict: not-merge-ready
ci_summary: no GitHub Checks on tip (check-runs empty; status pending with no contexts); mergeStateStatus DIRTY / CONFLICTING; compare main...tip diverged (ahead 6, behind 20)

## Blockers

- **Merge: CONFLICTING / DIRTY vs `main`** — tip `15e2be3a` is 20 commits behind and 6 ahead. Rebase (or update-branch) onto current `main` and resolve conflicts before any merge attempt.
- **Scope collision with open #16** — #16 (`r792-hotfix`, MERGEABLE) already carries a fuller `tools/hardening-audit-scheduled.ps1` (mutex, lock TTL, logging, removal verify). This PR's 62-line R790 wrapper would fight or regress that path. Land #16 (or fold its safeguards here) and drop/replace the thinner script; do not merge both as-written.
- `tools/hardening-audit-scheduled.ps1`: three-argument `Join-Path $RepoRoot ".worktrees" "hardening-audit-nightly"` needs PowerShell 7+. Nest two-arg `Join-Path` (fleet pattern) or `#Requires -Version 7` before Task Scheduler registration.
- `tools/hardening-audit-scheduled.ps1`: after `build.ps1 --hardening-audit` there is no `git worktree remove` / prune. Each successful run leaves `.worktrees/hardening-audit-nightly` (and force-remove-then-add next time races). Always clean up in `finally` (even on DryRun after create).
- `tools/hardening-audit-scheduled.ps1` `finally`: single `Pop-Location` after two `Push-Location` calls leaves the outer location stack wrong on success. Track push depth or use `Push-Location -StackName` / restore `$PWD` explicitly.

## Improvements

- `tools/session-resume-coordination-gate.ps1` branch match: `return "R$($matches[1] -or $matches[2])"` is not a valid capture pick (boolean `-or` on strings). Use `if ($matches[1]) { "R$($matches[1])" } else { "R$($matches[2])" }` (or a single `[Rr](\d+)` pattern).
- `build.ps1` R691 retirement: hardcoded `$shippedRItems` plus `git log --oneline --all | Select-String` for "shipped" is brittle and can false-positive. Prefer parsing `docs/ROADMAP.md` SHIPPED/COMPLETE lines; never `git branch -D` a name that is only `remotes/origin/...` without stripping the remote prefix.
- `src/SfTools.App/AutomationHealth.cs` `ComputeBackoffDelayMs`: comment claims cap at `2^3 = 8x` then `Math.Min(..., base*4)` makes the 8x path dead. Align formula + XML doc with the intended 4x (or 8x) contract and pin it in selftest.
- Six App files only add `using SfTools.Core.Storage;` with no other diff — confirm each is required on this tip after rebase (drop drive-bys if `main` already compiles without them).
- ROADMAP / PR body claim selftest pins `audit.scheduled-worktree-determinism` / `audit.scheduled-git-object-reads`; this PR does not add those pins. Add them or stop claiming them.
- Hardcoded `$RepoRoot = "C:\ContextUltraSalesforce"` — resolve from script location / `git rev-parse --show-toplevel` so the scheduled task is portable.
- PR packs R780 backoff, R690/R691 session gates, loghealth note, and R790 in one branch (`claude/hungry-sutherland-057cd0`). Prefer splitting mergeable slices after rebase so review and revert stay tractable.

## Notes

- Title/body focus on R790 audit non-blocking redesign; tip also includes R780 `ComputeBackoffDelayMs`, session-resume gate, session-retirement watch, post-gate retirement in `build.ps1`, and known-benign export-timeout note.
- Design direction (dedicated audit worktree on `origin/main`, no hourly dirty-tree retry) matches the stated failure of 2026-09-16; production hardening for that path is further along on #16.
- No GitHub PR review comments posted (CoS bus only).
