schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 35
head: 9069775473b31b2f3a3f2ba25fe09aaa55076c32
reviewed_utc: 2026-09-20T03:08:35Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS; windows-app build-and-selftest FAIL on run 35483920126 (same tip SHA; 1023 checks / 4 failed); mergeable MERGEABLE / mergeStateStatus UNSTABLE; ahead_by 8 / behind_by 0; 0 GitHub reviews

## Blockers

- **`build-and-selftest` red (4 fails)** — run 35483920126 on tip `90697754`: `phase2.pre-push-hook-present`, `phase2.pre-push-r-claim-extraction`, `deploy.stale-guard` (verdict-probe could not resolve HEAD/HEAD~1), `audit.self-attestation-integrity-dimension-runs` (dimension missing from `docs/audits/r160-bulkclose-seam-map.md`). None of the failing pins touch the R807 evidence files in this diff; likely ambient / shared with recent `main` reds. Unblock the required check (fix hooks + audit map on a dedicated PR, or re-run if `deploy.stale-guard` is checkout flaky) before merge.

## Improvements

- **Claimed `IsEmpty` fix did not land** — commit `5ef0934f` message says `Evidences.Count == 0` was added to `CaseRecord.IsEmpty`, but tip `CaseRecord.cs` still omits `Evidences` from the predicate (EOF blank only). Re-apply before treating failure-modes CRITICAL as closed.

- **UI attach path still unwired** — `TryAddEvidenceFromDialog` / drag-drop still only exist on `EvidenceAttachmentManager` + SelfTest. `ExportCaseCli` pre-export `ValidateAllEvidence` is a real production path for validation, not for attach. Pin `kcs.evidence-attachment-form-wired` remains structural. Hook a Form control or drop the "UI form wiring complete" claim.

- **SOQL serializer still ships absolute paths** — pin renamed to `kcs.evidence-attachment-exception-safe` (good). `SerializeEvidenceForSoql` still JSON-serializes full `EvidenceAnchor` including `LocalPath`. Strip/hash-bucket paths (or `Redact`) before any Salesforce/clipboard/log egress when Phase 1 writes SOQL.

- **Health-report guard incomplete on attach catch** — `ValidateAllEvidence` wraps `AutomationHealth.Report` in try/catch; `TryAddEvidenceFromDialog` catch still calls `Report` unguarded (failure-modes finding only half-fixed).

- **No Salesforce field mapping yet** — `CaseRecord.Evidences` is local JSON only; nothing maps to `case__Evidence_Anchors__c`. Fine for local Phase 0 if labeled that way.

- **Several pins remain structural** — `gate.r807-blockers-resolved-design-locked` and `kcs.evidence-anchor-health-reporting-wired` stay source/substring checks. Keep behavioral pins; rename or replace the rest.

- **Naming collision risk** — `SfTools.Core.CaseAnalysis.EvidenceAnchor` vs `Theory.EvidenceAnchor` (string). Prefer `EvidenceFileAnchor` / `AttachedEvidence` before wider spread.

## Notes

- **Duplicate of open #33** — same tip SHA `9069775473b31b2f3a3f2ba25fe09aaa55076c32` as https://github.com/layibabalola/context-ultra-salesforce/pull/33 (`r807-case-analysis-evidence-phase0`). This PR is branch `claude/r807-impl` → `main`. Close or supersede one to avoid double merge / double CI noise. CoS feedback for #33 already covers this tip; this file is the #35 bus slot.
- Tip still +1480/−2 across 8 files: 3× `.claude/audits/R807-swarm-*.md` CONDITIONAL-PASS, `EvidenceAnchor.cs`, `EvidenceAttachmentManager.cs`, `ExportCaseCli.cs` pre-export validation, `SelfTestCli.cs` pins, `CaseRecord.Evidences`.
- Phase 0 schema + manager + export pre-check remain coherent; content posture unchanged from #33 CoS review. Author layibabalola (co-authored Haiku). Review via `gh`. No GitHub PR comment posted.