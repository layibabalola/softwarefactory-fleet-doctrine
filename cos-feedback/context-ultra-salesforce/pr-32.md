schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 32
head: 02b631749a1c59ade633011c383d45c895f8bdb7
reviewed_utc: 2026-09-20T01:08:14Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check PASS (two check jobs); windows-app build-and-selftest IN_PROGRESS on run 35480558281; mergeable MERGEABLE; 0 GitHub reviews

## Blockers

(none for content — tip wires Phase 1 validators into the PDF export path as intended for R809 Phase 1b)

## Improvements

- **Warn-only is not fail-closed** — `ExportCaseCli.cs` calls `ValidateAllGates` + `ValidateSwarmArtifactsExist` then still `RenderMarkdownToPdf` on failure; issues only land in `notes`. Pin `case.haiku-swarm-gate-enforced-on-export` overclaims "enforced". If R809 wants customer PDFs blocked on gate failure, skip/abort render when `gateFailures.Count > 0` or `!swarmOk`; if warn-only is intentional (copy-not-send), rename the pin and document that export continues.

- **Selftests still structural** — both new pins only `File.ReadAllText` `ExportCaseCli.cs` and assert substrings (`ValidateSwarmArtifactsExist`, `ValidateAllGates`, `GenerateGateFeedback`, `notes.Add`). The `notes.Add` check is especially weak (that call exists elsewhere in the file). Prefer a fixture that runs validation against sample markdown / a temp case folder and asserts note text or a hard fail.

- **Prior Gate 1 / Gate 4 / section looseness still open** — this tip does not touch `CaseAnalysisTemplate.cs`. Still: bare `"0x"` in the verified-keyword set; swarm path still `caseFolder/../../../.claude/audits` accepting any `R*-swarm-*.md`; `HasMandatoryContextSections` / `HasThreeWayScopeTest` still loose OR/regex matches. Fine to defer to R807 / a later phase, but do not treat Phase 1b as closing those CoS items from pr-31.

## Notes

- Diff +43/−0 across 2 files: `ExportCaseCli.cs` (+19), `SelfTestCli.cs` (+24). Branch `claude/fervent-chandrasekhar-8772dd` → `main`. Head `02b63174`.
- Addresses prior CoS Improvement on pr-31: wire `ValidateAllGates` + `ValidateSwarmArtifactsExist` + `GenerateGateFeedback` into the export path before PDF render. Swarm check is separate from `ValidateAllGates` (which still only returns the three content gates).
- PR body marks Gate pass / merge checklist incomplete; CI `build-and-selftest` still pending at review time.
- Author layibabalola (Co-Authored-By Claude Haiku 4.5). Review via `gh`. No GitHub PR comment posted.