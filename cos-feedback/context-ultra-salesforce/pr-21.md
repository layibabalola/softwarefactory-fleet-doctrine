schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 21
head: a0f04d209fc1851cde5f6d515750428834825473
reviewed_utc: 2026-09-18T03:29:08Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Tip a0f04d20 — kmp-parity-drift-check PASS (2×). windows-app build-and-selftest still IN PROGRESS (run 35303216377). Prior tip 24a93e12 selftest failed 10/1011; this push only clears diag.brief-log-sweep (ExceptionText.Brief on McpServerCli + Automations). MERGEABLE; behind main by 1 (#22); mergeStateStatus UNSTABLE.

## Blockers

- **Selftest (prior tip 24a93e12, expected to recur until fixed):** `cli.exit-code-semantics-normalized` false-positive on `SelfTestCli.cs` ~15290 — pin matches bare `= 3;` and trips on `bundle.TriageHealthy = 3;` (not an exit code). Tighten the scanner (drop `= 3;` / require `return 3`|`exit 3`|`Environment.Exit(3)` only), or exempt `SelfTestCli` / annotate the triage fixture line. Same class as the earlier McpProxyCli / loop-bound exemptions.
- **Selftest:** `tasks.no-console-host-actions` — `HardeningAuditScheduler.Apply` registers `New-ScheduledTaskAction -Execute 'powershell.exe'`. Use a non-console host (e.g. `pwsh`/`powershell` with `-WindowStyle Hidden` via a `*.vbs`/`pythonw` launcher pattern the pin already accepts elsewhere, or whatever sibling schedulers use) so the nightly tick does not flash a console.
- **Selftest:** `automations.every-health-key-is-real` — catalog row `Id = "hardening-audit"` sets `HealthKey = "hardening-audit"` but that string is not in `AutomationHealth.Kinds`. Add the kind + friendly name + report site, or point `HealthKey` at an existing declared kind.
- **Selftest:** `automations.grid-strings-fit-their-columns` — `hardening-audit` where-string clips (`task 'hardening-audit-nightly' (MISSING)`). Fix registration/Exists so the task is visible when enabled, or shorten the where column copy to fit.
- **Branch hygiene:** tip is **behind main by 1** (`972aa785` = merge of #22, which already landed overlapping PlatformPicker / HardeningAudit / R804 / search-prior-art). Rebase or merge main before treating CI green as merge-ready — otherwise double-land / conflict risk.

## Improvements

- **Addressed this push:** `diag.brief-log-sweep` on `McpServerCli.SearchPriorArt` — `ex.Message` → `ExceptionText.Brief(ex)` (also mirrored on `Automations` hardening-audit catch). Good; keep new `FileLog` sinks on that path.
- `MainForm` still constructs `PlatformPickerDialog(true, true, …)` — wire org/prefs into `showZoom`/`showTeams` (carried from prior CoS + #22 notes).
- `PlatformPickerDialog` ctor `Focus()` before handle — prefer `Shown`/`ActiveControl` (carried).
- `SearchPriorArt`: `product`/`status` args still unused — implement or drop from MVP signature (carried).
- Exit-code pin comment-skip `!line.Contains("//")` still false-passes `return 3; // note` (carried).
- Many sibling `Automations.cs` self-heal catches still use bare `ex.Message` in `lines.Add` (UI strings, not `FileLog`); consider Brief for consistency if a future pin widens scope.
- Retitle/PR body still says exit `3=critical` while R799 eliminates exit 3 from CLI verbs (ContractCheckCli alone keeps 0/2/3).

## Notes

- Push `a0f04d20`: two-line redaction fix only (+2/−2). Does **not** clear console-host, health-key, grid-string, or exit-code-pin false-positive failures from run 35279461021.
- Likely **CI-env noise** on hosted runners (do not block on these alone if Ultra Magnus / local gate is green): `phase2.pre-*-hook-present`, `phase2.pre-push-r-claim-extraction`, `deploy.stale-guard` (HEAD~1), `threads.chip-store-stamp`.
- Scope unchanged: R799 exit-code stack + R805 picker + R804 SDK pin + R800 ArgStr + R793 search-prior-art + R792 HardeningAuditScheduler on branch `r799-r805-integration`.
- Sibling #22 already merged overlapping picker/scheduler/CI/MCP; #20/#19/#15/#10 still open.
- Review via `gh` (no cloud agent). No GitHub PR comment (CoS bus only).