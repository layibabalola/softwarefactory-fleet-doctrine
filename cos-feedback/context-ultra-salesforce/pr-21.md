schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 21
head: 24a93e12f7cc23ef9c357257023b4f9d4cbe5fa9
reviewed_utc: 2026-09-17T21:59:37Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 24a93e12 — PASS: kmp-parity-drift-check (2× check). Still pending: windows-app build-and-selftest (run 35279461021). MERGEABLE; mergeStateStatus UNSTABLE. 0 GitHub reviews. PR body claims 1005 pins + 4 pre-existing failures from r792/r793 — confirm on selftest log when the job finishes.

## Blockers

(none)

## Improvements

- `src/SfTools.App/MainForm.cs` (~6673): `new PlatformPickerDialog(true, true, defaultPlatform)` hardcodes both platforms on. Dialog ctor already accepts org-policy gates (`showZoom`/`showTeams`); wire those from prefs/config so a Teams-only (or Zoom-only) org cannot pick the disabled platform.
- `src/SfTools.App/PlatformPickerDialog.cs` ctor: `Focus()` on Zoom/Teams before the form has a handle is usually a no-op. Prefer `Shown`/`Load` to ActiveControl, or set `AcceptButton` to the default platform button. Selftest `R805.picker-preselects-default` already admits it cannot verify focus without showing the form.
- `src/SfTools.App/McpServerCli.cs` `SearchPriorArt`: `product` and `status` parameters are accepted (and routed from `McpRouter`) but never applied to the case filter — either implement or drop from the MVP signature so callers are not misled. Prefer a typed match record over `List<dynamic>`.
- `src/SfTools.App/SelfTestCli.cs` pin `cli.exit-code-semantics-normalized`: line skip is `!line.Contains("//")`, so `return 3; // note` would false-pass. Prefer stripping comments or matching only executable tokens (`return 3;` / `Environment.Exit(3)`).
- `src/SfTools.App/HardeningAuditScheduler.cs`: `Exists()` uses `Combined.Contains("True")` (substring risk); PowerShell registration builds the script via string concat — keep path escaping under review if script paths can contain quotes. Hardcoded `C:\\ContextUltraSalesforce` fallback is fine for this install layout but document it.
- Retitle/clarify PR body exit-code line: body still says `3=critical` while R799 + the new selftest pin eliminate exit 3 from CLI verbs (ContractCheckCli alone keeps 0/2/3). Align narrative with the pin.

## Notes

- Scope: branch `r799-r805-integration` → `main`; tip `24a93e12` (merge of R799 stack + r792-phase2-scheduler). +958/−98 across 66 files. Ahead stacked commits include R799 exit-code normalization (~50 `*Cli.cs`), R805 `PlatformPickerDialog` + MainForm schedule-call wire-up, R804 SDK `10.0.400` pin + lock regenerate step, R800 `ArgStr` default-arg fix, R793 `search-prior-art` MCP MVP, R792 Phase 2 `HardeningAuditScheduler` + Automations self-heal.
- R805 copy-not-send posture looks intact: picker is a choice gate; `ScheduleCall.RunAsync` now takes `platformOverride` and passes it into `ComposeAsync`; cancel returns without status toast.
- Sibling #20 was ROADMAP-only R805; this PR is the code land for picker + the stacked R-items above.
- Review via `gh` (no cloud agent). No GitHub PR comment posted (CoS bus only).
