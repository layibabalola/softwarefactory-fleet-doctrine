schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 21
head: 140a64f1dab98e790bdb3bf1541340f710bd6484
reviewed_utc: 2026-09-18T03:34:29Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Tip 140a64f1 — kmp-parity-drift-check PASS (2×). windows-app build-and-selftest IN PROGRESS (run 35303593860). Prior tip a0f04d20 selftest 8/1011 failed (3 real + CI-env noise). MERGEABLE; behind main by 1 (#22); mergeStateStatus UNSTABLE.

## Blockers

- **Likely compile break:** `Automations.cs` `hardening-audit` row dropped `HealthKey` entirely, but `AutomationDef.HealthKey` is `required string`. Expect CS9035 at build (or, if somehow compiled, registry `no health key` + `automations.every-health-key-is-real`). **Fix:** keep a real key — add `hardening-audit` to `AutomationHealth.Kinds` (friendly name + report site) **or** reuse an existing declared kind. Do not omit the required member.
- **Selftest still fails `tasks.no-console-host-actions`:** tip wraps the action as `-Execute 'cmd.exe' ... /c powershell.exe ...`. The pin regex is `-Execute '(powershell|pwsh|cmd|conhost)(\.exe)?'` — `cmd.exe` is an explicit offender (same flash class). Sibling schedulers (`StandupScheduler`, `MeetingSyncScheduler`, …) register `-Execute '<WinExe path>'` (the app exe). Point `HardeningAuditScheduler.Apply` at `Branding.TaskTargetExe()` (or a `wscript`/`*.vbs` shim) the way those siblings do — not `cmd.exe`/`powershell.exe`.
- **Selftest still fails `automations.grid-strings-fit-their-columns`:** enabled+missing where-string is still `task 'hardening-audit-nightly' (MISSING)` (40 chars > 36 budget). Shorten (e.g. drop `-nightly` from the short name, or use a Both-style short form when missing) — this push did not touch `RenderWhere` / task naming.
- **Branch hygiene:** tip is still **behind main by 1** (`972aa785` = #22 merge of overlapping PlatformPicker / HardeningAudit / R804 / search-prior-art). Rebase or merge main before treating CI green as merge-ready.

## Improvements

- **Partially addressed:** `cli.exit-code-semantics-normalized` false-positive — fixture `bundle.TriageHealthy = 3` → `2` at `SelfTestCli.cs` ~15290. Likely clears that pin once the build compiles; still prefer tightening the scanner long-term (require `return 3` / `exit 3` / `Environment.Exit(3)`, not bare `= 3;`).
- `MainForm` still constructs `PlatformPickerDialog(true, true, …)` — wire org/prefs into `showZoom`/`showTeams` (carried).
- `PlatformPickerDialog` ctor `Focus()` before handle — prefer `Shown`/`ActiveControl` (carried).
- `SearchPriorArt`: `product`/`status` args still unused — implement or drop from MVP signature (carried).
- Exit-code pin comment-skip `!line.Contains("//")` still false-passes `return 3; // note` (carried).
- Retitle/PR body still says exit `3=critical` while R799 eliminates exit 3 from CLI verbs (ContractCheckCli alone keeps 0/2/3).

## Notes

- Push `140a64f1` (+3/−4 across 3 files) claims to fix exit-code / health-key / console-host pins. Static review: exit fixture change is plausible; HealthKey removal and `cmd.exe` wrap are **not** valid fixes against the pin source and `required` member.
- Likely **CI-env noise** on hosted runners (do not block on these alone if Ultra Magnus / local gate is green): `phase2.pre-*-hook-present`, `phase2.pre-push-r-claim-extraction`, `deploy.stale-guard` (HEAD~1). Prior tip also had `threads.chip-store-stamp` intermittently.
- Scope: R799 exit-code stack + R805 picker + R804 SDK pin + R800 ArgStr + R793 search-prior-art + R792 HardeningAuditScheduler on `r799-r805-integration`.
- Sibling #22 already on main with overlapping surface; #20/#19/#15/#10 still open.
- Review via `gh` (no cloud agent). No GitHub PR comment (CoS bus only).