schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 19
head: bfd97592e76334397e7595290ae24fbf50da5c0b
reviewed_utc: 2026-09-17T05:43:04Z
event: pr-opened
verdict: not-merge-ready
ci_summary: no checks reported yet on r790-final-merge (status pending / empty rollup)

## Blockers

- **PR body contradicts the diff** — Description says conflict resolution kept R792 Phase 1's enhanced `tools/hardening-audit-scheduled.ps1` (mutex, lock file, network fetch timeout, persistent logging) and preserved both R790/R792 in `docs/ROADMAP.md`. Actual tip vs `main` (`68753ec5`): **one file**, `tools/hardening-audit-scheduled.ps1` (+19/−101), and the patch **replaces** the R792-enhanced script on `main` with the thinner R790 wrapper. `docs/ROADMAP.md` is **not** in the diff. Fix: rewrite the PR body to match the tip, or replace the tip with a true merge that keeps R792 safeguards (and any intended ROADMAP edits).
- **Regression of production safeguards already on `main`** — Tip drops gate mutex (`Global\ContextUltraSalesforce-gate`), `.git/hardening-audit.lock` concurrency guard, timed `git fetch origin main`, persistent `logs/hardening-audit.log`, verified worktree remove, post-audit worktree remove + prune, and `finally` mutex/lock cleanup. Those are the R792 hardening already landed on `main`. Do not merge this tip as a "conflict-resolved merge" unless intentionally reverting R792 and documenting that as the chosen Option B outcome.
- **No origin refresh before worktree add** — R790 wrapper assumes `origin/main` is already current; R792 on `main` fetches first with connect/low-speed timeouts. Without that fetch, a stale remote-tracking ref makes the "deterministic at origin/main" check a local lie. Restore the timed fetch (or equivalent) before `git worktree add`.

## Improvements

- Tip leaves the audit worktree in place after the audit (R792 cleaned up). Prefer remove + `git worktree prune` in `finally` so overnight runs do not accumulate stale trees.
- `finally { Pop-Location }` with two `Push-Location`s in the success path is fragile on early failure; match R792's guarded `Pop-Location -ErrorAction SilentlyContinue` (or a single location stack discipline).
- After correcting the tip, add or re-run selftest pins named in the commit message (`audit.scheduled-worktree-determinism`, `audit.scheduled-git-object-reads`) so hosted CI proves the scheduled path, not only the narrative.

## Notes

- Scope: tip `bfd97592` on `r790-final-merge` → `main`; ahead 1 / behind 0; mergeable CLEAN; single commit (authored 2026-09-16, committed 2026-09-17).
- Intent of R790 (dedicated `.worktrees/hardening-audit-nightly` at `origin/main`, no dirty-tree hourly-retry block) is already embodied by the R792 script on `main`; this PR does not add new architecture beyond stripping safeguards.
- Open siblings: #15 (R789 Phase 2), #10 (WIN_RUNNER routing). Unrelated to this tip.
- Hosted `windows-app` / `build-and-selftest` has not appeared on this head yet; recent `main` / #14 tips still carried NU1004 ILLink lock drift — watch when checks start.
- No GitHub PR review comments posted (CoS bus only).