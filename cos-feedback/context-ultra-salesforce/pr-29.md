schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 29
head: ac5eae6611397e8a882fe5451a8d1a65b7a05cc7
reviewed_utc: 2026-09-19T06:12:34Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: kmp-parity-drift-check FAILED (billing/spend limit — job never started; not code drift); windows-app build-and-selftest IN_PROGRESS on run 35425900911 (selftest battery running after publish+lock restore green); mergeable MERGEABLE

## Blockers

(none for content — tip is focused R806 infra/docs/pin fix)

- **Hosted CI billing** — `kmp-parity-drift-check` (push+PR) failed with "recent account payments have failed or your spending limit needs to be increased." That workflow is designed never to fail on drift (warning-only), so this red is account infra, not parity. Fix Billing & plans / spending limit so future hosted jobs can start. Does not by itself block merge if `windows-app` is the required gate and goes green.

## Improvements

- **Wait for `windows-app` / `build-and-selftest`** — confirm PASS on head `ac5eae66` before merge (publish + locked restore already green; selftest battery in progress on run 35425900911).
- **Set `vars.WIN_RUNNER` (owner)** — expression matches `kmp-tests.yml` R363.3 pattern (`(actor != dependabot && vars.WIN_RUNNER) || 'windows-latest'`). Without the var, job stays on hosted `windows-latest` (fallback works; self-hosted routing inactive). PR body still says the var is required — optional in code; set it when UltraMagnus is ready.
- **Dependabot routing change** — prior tip used `''` for dependabot (no runner). New tip sends dependabot to hosted `windows-latest`, matching KMP R408 "bots ride hosted." Confirm that cost trade-off is intended for this workflow.
- **Audit pin mtime collision** — PR body notes `audit.self-attestation-integrity-dimension-runs` can fail when a fresh worktree gives identical mtimes across reports. New sort `(mtime DESC, name ASC)` fixes the r806-vs-2026-date wrong-pick when mtimes differ; if clone-time collisions keep flaking CI, consider preferring date-prefixed report names or content markers over filesystem mtime alone.

## Notes

- Tip delta: +284/−17 across 6 files — `SelfTestCli.cs` (NeedRepoRoot + audit report sort), `windows-app.yml` (WIN_RUNNER routing), `CLAUDE.md` (R806 Session 0 correction + measurement gate), three `.claude/audits/R806-swarm-*.md` artifacts.
- `SelfTestCli.cs`: replaces bare `GetRepoRoot()` null-skip with `NeedRepoRoot` (R342 idiom used widely in the same file); picks newest audit report by UTC mtime then filename — addresses filename-only sort preferring `r806-` over dated hardening reports.
- `CLAUDE.md`: corrects false "Session 1+" claim; documents Windows Services Session 0 platform constraint and PR-3 measurement gate for whether redesign is needed.
- Swarm audits are compliance/failure-mode/UX notes only; Law 1 data; no workflow authority.
- Branch `claude/gifted-hamilton-9239cd` → `main`; author layibabalola (co-authored Claude Haiku 4.5). Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.