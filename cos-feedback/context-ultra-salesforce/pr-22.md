schema: cos-feedback.v1
project: context-ultra-salesforce
repo: layibabalola/context-ultra-salesforce
pr: 22
head: 2a091c2c27f93a66261f3bdc784463e9f7c79a5a
reviewed_utc: 2026-09-17T23:37:42Z
event: pr-opened
verdict: info
ci_summary: already merged to main (972aa785); kmp-parity-drift-check PASS on tip; post-merge windows-app build-and-selftest (run 35287405438) still in progress — R804 unlock-restore step SUCCESS, Publish (locked) still running (no NU1004 yet)

## Blockers

(none for merge — PR already landed; watch post-merge `windows-app` on main)

## Improvements

- **`MainForm.ScheduleCallFromRecordAsync` always passes `PlatformPickerDialog(true, true, …)`** — swarm audits claim org-policy gating (`AllowedPlatforms` / hide restricted buttons). GUI path never reads org availability; both Zoom and Teams always show. Wire prefs/org allow-list into `showZoom`/`showTeams`, or drop the claim from audits/Phase-1 docs so Phase 2 owns it honestly.
- **Resync comment vs code** — comment says picker is shown after the case is fresh; this method only uses the already-loaded `CaseRecord` and does no SOQL before `ShowDialog`. Fresh thread/recipients happen later inside `ScheduleCall.ComposeAsync` (after platform choice), which is the real act — reword the comment/swarm notes so they match that order.
- **Pre-select focus in ctor** — `Focus()` runs in `PlatformPickerDialog` constructor before the form is shown; WinForms often ignores focus until handle creation / `Shown`. Prefer `Shown` handler or `ActiveControl` after `Load` so default Zoom/Teams highlight is reliable; selftest `R805.picker-preselects-default` only constructs dialogs and does not assert focus.
- **R804 lock hygiene** — workflow pins `dotnet-version: 10.0.400` and adds an unlock `dotnet restore` before `RestoreLockedMode` publish, but this PR did not commit a regenerated `packages.lock.json`. Prefer committing the honest lock after pin so local/CI agree without rewriting the lock on every CI run; keep the unlock step only as a drift safety net.
- **Kitchen-sink branch / retitle debt** — tip branch `r792-phase2-scheduler` landed R805 picker + R792 `HardeningAuditScheduler`/Automations + R804 CI pin + `search-prior-art` MCP (R793/R800) + misc selftest/loghealth under an R805-only title. Future PRs: one concern per PR, or title/body that lists every landed R.
- **Open sibling #21** — `R799 + R805 Phase 1 + R804/R800/R793…` still open and overlaps PlatformPicker / HardeningAudit / workflow / MCP files now on main via #22. Rebase or close to avoid double-merge conflict noise.

## Notes

- Opened and merged within seconds (created ~23:33:48Z, merged 23:33:54Z as `972aa785`); no pre-merge `windows-app` gate on the PR tip — same race pattern as #13.
- R805 core: new `PlatformPickerDialog.cs`; `MainForm` modal picker then `ScheduleCall.RunAsync(..., platformOverride)`; four structural selftest pins (`accessible`, `null-on-cancel`, `preselects-default`, `confirm-before-send-downstream`); guide-capture exempt for the dialog; three `.claude/audits/R805-swarm-*.md` artifacts.
- Cancel/`Escape` → null/`DialogResult.Cancel` with silent return looks correct; MCP `schedule-call` still takes optional `platform` and redacts via `MainForm.Redact` / `ForMcpAsync` (parity path unchanged by GUI picker).
- Signed-out guard (`client is null`) still runs before the picker.
- Other open siblings unrelated to this tip's merge race: #20 (R805 ROADMAP-only), #19 (R790), #15 (R789 Phase 2), #10 (WIN_RUNNER).
- No GitHub PR review comments posted (CoS bus only).