schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 138
head: ac41dbf66a0a429b77c22528b050810742033576
reviewed_utc: 2026-09-17T19:25:30Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: All observed checks IN_PROGRESS at review (Repo Hygiene windows+ubuntu, Protected Check Route, Factory Bridge Regressions, Batch Compile, Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeStateStatus BLOCKED pending required checks; 0 reviews; +60/-6 in tools/repo_hygiene/test_brokered_closeout.py only.

## Blockers

(none)

## Improvements

- After `Repo Hygiene Python (windows-latest)` finishes on this PR, paste the wall time next to a recent master sample in the PR body (PR already frames the 9–18% claim honestly against 77s intra-arm spread — close the loop with one CI receipt before merge).
- Optional: add a one-line comment or tiny unit assertion that `_TEMPLATE_CACHE` keys on `json.dumps(config_updates, sort_keys=True)` so a future `config_updates` shape that is not JSON-stable cannot silently collide templates (today's call sites look fine).

## Notes

- Scope: test-fixture only. `init_repo()` builds one template per distinct `(config_updates, remote)` shape per process, `shutil.copytree`s it, and re-points `origin` when `remote=True`. Production `brokered_closeout.py` untouched — correct given the PR's stated next target (caching resolved refs / `git rev-parse`).
- Local claim: 249/249 pass interleaved A,B,A,B on Windows; mean ~565s → ~489s. Honest about not fixing the ~5.2x Windows/ubuntu gap (~17.8k git spawns, ~7.3k `rev-parse`).
- Related context: #136 Defender exclusion on Windows hygiene; this PR is the next measured slice on the same cost line.
- CoS via gh (test-only; no Cloud Agent). No GitHub PR review comment (bus only).
