schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 181
head: 89fb9af46109b6e4a0b7c1d2a09e413988dc6644
reviewed_utc: 2026-09-26T19:57:03Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 89fb9af4 (LANE-MODEL-CURRENCY-1; +548/−27, 6 files, 2 commits; ahead 2 / behind 0). mergeable MERGEABLE; mergeStateStatus BLOCKED on unsettled required checks. Protected Check Route success; Repo Hygiene (ubuntu/windows), Batch Compile, Factory Bridge, Windows GUI Pilot, Product Oracles, CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS (runs under 36267750290 / 36267750358 / 36267749362).

## Blockers

(none)

## Improvements

- Claude `-ModelOverride` path still leaves `resolvedModel` as `unknown` until post-exit `modelUsage` family match against the *table alias* (not the override id). Codecs path sets `resolvedModel` from the override at launch. Either mirror that for Claude overrides, or extend `test_model_override_on_a_claude_lane_bypasses_the_alias` to pin the intended post-exit `resolvedModel` / `unknown` contract when the override id is outside `^claude-<alias>-`.
- Receipt schema drop of the legacy `model` key (replaced by `requestedModel` / `resolvedModel` / `auxiliaryModels` / `pinnedByOverride`) — audit hub/conductor/brief consumers that still read `.model` so they do not silently lose identity after merge.
- `agents/orchestration-tiering.md` astra row still narrates the 2026-09-15 `xhigh` probe as reachable history beside the new high-effort rule; consider a one-line "historical; effort now high" clarifier so a skim does not re-introduce xhigh as current policy.

## Notes

- Owner directive 2026-09-26: latest model per tier at high effort (never max/xhigh). Table: Claude rows use floating CLI aliases (`opus`/`sonnet`/`fable`); Codex rows use `tier` resolved at launch via new `tools/coordination/resolve-codex-tier.py` (fail-closed on missing/malformed/no-match cache; `MLV_CODEX_MODELS_CACHE` test override). Sonnet and astra defaults moved to high; astra role label shortened to `design-arbiter`.
- Receipt identity: `requestedModel` = alias/tier asked; `resolvedModel` = proven run (Codex at launch; Claude from `modelUsage` entries whose `canonicalModel` matches `^claude-<alias>-`); auxiliaries segregated; never copy requested into resolved. Round-2 commit addresses sol pre-review aggregate-`modelUsage` / StrictMode indexer pitfalls.
- Tests: unit+CLI for resolver (incl. near-name `solar`≠`sol`, non-UTF8 fail-closed); containment coverage for family/auxiliary/unknown, Codex resolve, override pin, fail-closed missing cache; guardrails assert no pinned `claude-*`/`gpt-*` ids and every row `effort = 'high'`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: MERGEABLE / behind 0; wait for hosted Suite + Factory Bridge + GUI/Oracles + CodeQL.
