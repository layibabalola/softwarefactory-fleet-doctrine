schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 181
head: 6225057def053a0a962a0bb41f10c6b029f93683
reviewed_utc: 2026-09-26T21:46:07Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 6225057def05 (merge master into product/LANE-MODEL-CURRENCY-1; catches #182 SOFTWARE-FACTORY-IS-THE-KERNEL-1 docs only — CLAUDE.md + agents/factory-kernel-instance.md; ahead 4 / behind 0). MERGEABLE / BLOCKED. Tip CI settling (runs 36273908952 / 36273908979 / 36273907217): Protected Check Route SUCCESS; CodeQL Analyze (actions/python) SUCCESS; CodeQL workflow NEUTRAL; Repo Hygiene (ubuntu/windows), Batch Compile, Factory Bridge, Windows GUI Pilot, Windows Product Oracles, Analyze (c-cpp) in_progress.

## Blockers

(none)

## Improvements

- Claude `-ModelOverride` path still leaves `resolvedModel` as `unknown` until post-exit `modelUsage` family match against the *table alias* (not the override id). Codecs path sets `resolvedModel` from the override at launch. Either mirror that for Claude overrides, or extend `test_model_override_on_a_claude_lane_bypasses_the_alias` to pin the intended post-exit `resolvedModel` / `unknown` contract when the override id is outside `^claude-<alias>-`.
- Receipt schema drop of the legacy `model` key (replaced by `requestedModel` / `resolvedModel` / `auxiliaryModels` / `pinnedByOverride`) — audit hub/conductor/brief consumers that still read `.model` so they do not silently lose identity after merge.
- `agents/orchestration-tiering.md` astra row still narrates the 2026-09-15 `xhigh` probe as reachable history beside the new high-effort rule; consider a one-line "historical; effort now high" clarifier so a skim does not re-introduce xhigh as current policy.

## Notes

- Push 6225057def05 is a merge-of-master only (no LANE-MODEL-CURRENCY-1 product delta vs 1a2887419f69). Addresses prior CoS Improvement: branch was behind master by 2 after #182; now ahead 4 / behind 0.
- Soft Improvements (Claude ModelOverride resolvedModel contract; legacy receipt `.model` consumer audit; orchestration-tiering xhigh clarifier) unchanged — merge did not touch them.
- Owner directive 2026-09-26: latest model per tier at high effort (never max/xhigh). Table: Claude rows use floating CLI aliases (`opus`/`sonnet`/`fable`); Codex rows use `tier` resolved at launch via `tools/coordination/resolve-codex-tier.py`. Hermetic Codex models cache in `test_lane_containment` `prepare()` remains from tip 1a288741.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green** unchanged: wait for tip Suite + Factory Bridge + GUI/Oracles + CodeQL c-cpp; then merge (branch already up to date with master).