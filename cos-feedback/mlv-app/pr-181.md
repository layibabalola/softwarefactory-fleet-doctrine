schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 181
head: 1a2887419f69e71179e25ae66c065b457c3e3445
reviewed_utc: 2026-09-26T21:09:55Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 1a288741 (LANE-MODEL-CURRENCY-1 round 1c hermetic Codex models cache; +573/−28, 6 files, 3 commits; ahead 3 / behind 2 vs master after #182). MERGEABLE / BEHIND. Prior tip 89fb9af4 Windows Repo Hygiene FAILED (4 codex-lane containment tests exit 127, sol->unknown — missing ~/.codex/models_cache.json on hosted runner). Tip CI settling: Protected Check Route success; Repo Hygiene (ubuntu/windows), Batch Compile, Factory Bridge, Windows GUI Pilot, Product Oracles, CodeQL Analyze (actions/c-cpp/python) queued or in_progress (runs 36271903051 / 36271903067 / 36271901466).

## Blockers

(none)

## Improvements

- Claude `-ModelOverride` path still leaves `resolvedModel` as `unknown` until post-exit `modelUsage` family match against the *table alias* (not the override id). Codecs path sets `resolvedModel` from the override at launch. Either mirror that for Claude overrides, or extend `test_model_override_on_a_claude_lane_bypasses_the_alias` to pin the intended post-exit `resolvedModel` / `unknown` contract when the override id is outside `^claude-<alias>-`.
- Receipt schema drop of the legacy `model` key (replaced by `requestedModel` / `resolvedModel` / `auxiliaryModels` / `pinnedByOverride`) — audit hub/conductor/brief consumers that still read `.model` so they do not silently lose identity after merge.
- `agents/orchestration-tiering.md` astra row still narrates the 2026-09-15 `xhigh` probe as reachable history beside the new high-effort rule; consider a one-line "historical; effort now high" clarifier so a skim does not re-introduce xhigh as current policy.
- Branch is behind master by 2 (#182 SOFTWARE-FACTORY-IS-THE-KERNEL-1 merged); update branch before merge once tip CI is green.

## Notes

- Push 1a288741 addresses the prior-tip Windows Repo Hygiene failure: `prepare()` now writes a fixture Codex models cache and sets `MLV_CODEX_MODELS_CACHE` so hosted runners are hermetic (fail-closed tests still override after `prepare()`). Soft Improvements from the pr-opened pass are unchanged.
- Owner directive 2026-09-26: latest model per tier at high effort (never max/xhigh). Table: Claude rows use floating CLI aliases (`opus`/`sonnet`/`fable`); Codex rows use `tier` resolved at launch via `tools/coordination/resolve-codex-tier.py`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green** unchanged: wait for tip Suite + Factory Bridge + GUI/Oracles + CodeQL; then update branch (behind 2) and merge.
