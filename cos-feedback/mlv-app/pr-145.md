schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 145
head: 0ff63131e02d7128ec05892e4e077e1c296373db
reviewed_utc: 2026-09-21T16:07:01Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 0ff63131e02d (5 commits on product/ATTR3-FOOTAGE-BIND-1-prB; +1234/−239 across 9 files vs master): Protected Check Route PASS; Analyze actions/c-cpp/python, Factory Bridge Regressions, Batch Compile, Repo Hygiene Python ubuntu+windows, Windows GUI Pilot, Windows Product Oracles IN_PROGRESS; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which covers Expand-AttrCudaTemplate / token-collision suites, owner-decision-before-I/O ordering, and the id-only resolve+bake path)

## Improvements

- Soft: `ConvertTo-AttrCudaGeneratorPsLiteral` remains duplicated in both generators (`attr3-stage-smoke-runner-job.ps1` ~:110 and `playback-attr-3-cuda-job.ps1` ~:455) — prefer one export from `AttrCudaArtifacts.psm1` (still absent there; carried from #144 / prior CoS). Round 3's `Expand-AttrCudaTemplate` correctly leaves quoting to the caller, so this helper is still the right consolidation target.
- Soft: generator-side `Resolve-AttrCudaOwnerPython` / `Resolve-AttrCudaOwnerClipParts` still mirror the presence-job resolver invocation pattern; shared helper next to `Test-AttrCudaFootagePart` still optional.
- Soft: still **0 GitHub reviews** on the Bachelor owner-footage path; short human glance before merge is cheap (NA-4 / consent-table surface). Local sol/astra rounds are not a substitute for a GitHub review stamp.

## Notes

- Round 3 (1b432366 + 0ff63131 vs prior CoS head 175997e85): structural close of the token-collision class. New `Expand-AttrCudaTemplate` in `AttrCudaArtifacts.psm1` substitutes every `__TOKEN__` in one `[regex]::Replace` MatchEvaluator pass so a substituted value is never rescanned; unknown/unused tokens fail closed (`ATTRCUDA_TEMPLATE_UNKNOWN_TOKEN` / `ATTRCUDA_TEMPLATE_UNUSED_TOKEN`). All three emitters (playback attribution job, smoke-runner job, presence job) now use it instead of chained `.Replace()`. Tests: clip-id shaped like `__EMBEDDED_FUNCTIONS__` still emits/parses/runs literally; behaviour + split_route suites assert the token-map bake sites.
- Round 3 also tightens ordering: owner/fixture decision + owner resolve now precede Import-Module / embedded-function load as well as SourceCommit / PresentMon / smoke-runner closure work, so a refused owner id fails before unrelated I/O; docs id-only prose refreshed.
- Round 2 substance retained: ValidatePattern plain basenames + quote-escape at bake; ClipPath path-free refuse after classification; resolve before provenance.
- Round 1 substance retained: id-only resolve → bake pathBase64/length/sha256 → live `Test-AttrCudaFootagePart` before deploy/PresentMon/smoke; non-PASS → `OWNER_FOOTAGE_NOT_VERIFIED` exit 19 (index+status only). Depends on merged PR-A #143. Fixture route unchanged.
- Prior CoS Improvements (ConvertTo dup, shared resolver helper, human GitHub review) **not** addressed on this head — orthogonal to the round-3 structural templater.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat silent on this push: verdict unchanged and prior CoS Improvements not clearly addressed.