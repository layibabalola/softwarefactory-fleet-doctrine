schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 145
head: ac9a933cf157566a6ba4e9e8cdfb2cc509ffb5cb
reviewed_utc: 2026-09-21T17:45:57Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip ac9a933cf157 (8 commits on product/ATTR3-FOOTAGE-BIND-1-prB; +2062/−254 across 11 files vs master): Protected Check Route PASS; Analyze actions+python PASS; Analyze c-cpp, Factory Bridge Regressions, Batch Compile, Repo Hygiene Python ubuntu+windows, Windows GUI Pilot, Windows Product Oracles IN_PROGRESS; CodeQL NEUTRAL/skipping; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which covers private-directory / leak-proof / link-refusal suites and AttrCudaOwnerFootage embedding)

## Improvements

- Soft: `ConvertTo-AttrCudaGeneratorPsLiteral` remains duplicated in both generators (`attr3-stage-smoke-runner-job.ps1` ~:110 and `playback-attr-3-cuda-job.ps1` ~:501) — prefer one export from `AttrCudaArtifacts.psm1` (still absent there; carried from #144 / prior CoS). Round 4 does not touch quoting helpers.
- Soft: generator-side `Resolve-AttrCudaOwnerPython` / `Resolve-AttrCudaOwnerClipParts` still mirror the presence-job resolver invocation pattern; shared helper next to `Test-AttrCudaFootagePart` still optional.
- Soft: still **0 GitHub reviews** on the bachelor owner-footage path; short human glance before merge is cheap (NA-4 / consent-table surface). Local sol/astra rounds are not a substitute for a GitHub review stamp.

## Notes

- Round 4 (9205da02 + 15c16a23 vs prior CoS head 0ff63131): closes B1 (unverified siblings) and B2 (footage path published) by construction. After the per-part content gate, the attribution job hard-links each verified owner part into a private job-owned directory under neutral index-derived names, asserts same-file identity via `GetFileInformationByHandle` (volume serial + 64-bit file index), holds `FileShare.Read` handles through the smoke run, re-verifies content on the link paths before setting the smoke child's clip path, and cleans up in `finally` only when live hard-link count is ≥2 (never last name of owner bytes). Refusal tokens: `OWNER_PARTS_NOT_CONTIGUOUS` (exit 20), `OWNER_FOOTAGE_LINK_CROSS_VOLUME` (21), other link failure (22), post-link `OWNER_FOOTAGE_NOT_VERIFIED` (19). Tests: sentinel leak-proof (owner directory string absent from smoke cmdline and published artifacts), sibling/sidecar isolation, contig/gap/over-100, mocked cross-volume and identity-mismatch, both-parts same-inode assertion, ParamBlock AST census (no I/O defaults; no path-shaped ValidatePattern/ValidateScript).
- Round 4b (ac9a933c): hub ruling — move private-directory helpers out of shared `AttrCudaArtifacts.psm1` into new `AttrCudaOwnerFootage.psm1`, embedded only by `playback-attr-3-cuda-job.ps1` (not in NoFootageTokensTests NEW_SCRIPTS). Round 4's fragment-composed name inside the shared module defeated the test's purpose; Artifacts module now has zero composed footage-word fragments. `Get-AttrCudaFileIdentity` lives with its callers so Set-Item mocks in OwnerFootagePrivateDirectoryLeakAndRefusalTests still apply. `attr3_publish_write_scan.ps1` allowlist now derives from both modules by default. Fixture-route emitted job sha256-identical to 15c16a23 for same inputs.
- Prior rounds retained: Expand-AttrCudaTemplate single-pass; owner/fixture decision before Import-Module / provenance; ValidatePattern basenames + quote-escape; id-only resolve → bake → live `Test-AttrCudaFootagePart` before deploy. Depends on merged PR-A #143. Fixture route unchanged.
- Prior CoS Improvements (ConvertTo dup, shared resolver helper, human GitHub review) **not** addressed on this head — orthogonal to round-4 private-directory / module-split work.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat silent on this push: verdict unchanged and prior CoS Improvements not clearly addressed.