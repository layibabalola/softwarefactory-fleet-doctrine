schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 145
head: 175997e85b31a4170639c4b12a7bddd57dee82ed
reviewed_utc: 2026-09-21T15:07:48Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 175997e85 (3 commits on product/ATTR3-FOOTAGE-BIND-1-prB; +820/−189 across 7 files vs master): Protected Check Route PASS; CodeQL skipping; Analyze actions/c-cpp/python, Factory Bridge Regressions, Batch Compile, Repo Hygiene Python ubuntu+windows, Windows GUI Pilot, Windows Product Oracles IN_PROGRESS/pending; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which exercises the new token-injection refusals, ClipPath-after-classification, and resolver-before-provenance suites)

## Improvements

- Soft: `ConvertTo-AttrCudaGeneratorPsLiteral` remains duplicated in both generators (`attr3-stage-smoke-runner-job.ps1` ~:110 and `playback-attr-3-cuda-job.ps1` ~:438) — prefer one export from `AttrCudaArtifacts.psm1` (still absent there; carried from #144 / prior CoS on this PR).
- Soft: generator-side `Resolve-AttrCudaOwnerPython` / `Resolve-AttrCudaOwnerClipParts` still mirror the presence-job resolver invocation pattern; shared helper next to `Test-AttrCudaFootagePart` still optional.
- Soft: still **0 GitHub reviews** on the Bachelor owner-footage path; short human glance before merge is cheap (NA-4 / consent-table surface). Round 2 closed sol/astra CHANGES_REQUESTED locally; that is not a substitute for a GitHub review stamp.

## Notes

- Round 2 (dbb7de10 + 175997e85 vs prior CoS head e87ba0f4): closes sol/astra token-injection and disclosure blockers. `-ConsentReceiptFileName` / `-BasePackageZip` / `-BasePackageExeName` / `-PresentMonName` now `ValidatePattern` plain basenames (`\z`) AND are quote-escaped at `Replace()` bake sites; negative tests refuse quote+newline values. Parameter-level `-ClipPath` `ValidatePattern` removed so forward-slash owner paths reach path-free `PLAYBACK_ATTR3_CLIPPATH_REFUSED` instead of binding-echo disclosure; fixture arm validates in body after classification. Owner/fixture decision + resolver now run before SourceCommit cat-file / PresentMon sha checks so a resolver refusal is not masked by bogus provenance; ordering test covers both failures at once. Docs section 4 rewritten for id-only route; split_route heading pin updated.
- Round 1 substance unchanged: id-only resolve → bake pathBase64/length/sha256 → live `Test-AttrCudaFootagePart` before deploy/PresentMon/smoke; non-PASS → `OWNER_FOOTAGE_NOT_VERIFIED` exit 19 (index+status only). Depends on merged PR-A #143.
- Prior CoS Improvements (ConvertTo dup, shared resolver helper, human GitHub review) **not** addressed on this head — orthogonal to the sol/astra round-2 fixes.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat silent on this push: verdict unchanged and prior CoS Improvements not clearly addressed.