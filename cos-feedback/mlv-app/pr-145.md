schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 145
head: e87ba0f4d57de214b742d92e2386805088dcc3df
reviewed_utc: 2026-09-21T14:32:52Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip e87ba0f4 (1 commit on product/ATTR3-FOOTAGE-BIND-1-prB; +634/−167 across 6 files vs master): Analyze (actions) + Protected Check Route PASS; CodeQL NEUTRAL/skipping; Factory Bridge Regressions, Batch Compile, Repo Hygiene Python ubuntu+windows, Windows GUI Pilot, Windows Product Oracles, Analyze c-cpp/python IN_PROGRESS; mergeable_state BLOCKED; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which exercises the new owner-content authentication / shared-verifier / resolver-ordering suites)

## Improvements

- Soft: `ConvertTo-AttrCudaGeneratorPsLiteral` remains duplicated in both generators (`attr3-stage-smoke-runner-job.ps1` ~:110 and `playback-attr-3-cuda-job.ps1` ~:408) — prefer one export from `AttrCudaArtifacts.psm1` (still absent there; carried from #144 CoS Improvements).
- Soft: generator-side `Resolve-AttrCudaOwnerPython` / `Resolve-AttrCudaOwnerClipParts` mirror the presence-job resolver invocation pattern; if drift risk matters, consider one shared helper next to `Test-AttrCudaFootagePart` (not blocking — PR-B already unified the per-part verifier).
- Soft: still **0 GitHub reviews** on the Bachelor owner-footage path; short human glance before merge is cheap (NA-4 / consent-table surface).

## Notes

- Depends on merged PR-A #143 (`resolve_consented_clip.py` + presence probe). This PR-B replaces the blanket owner-id refusal with id-only resolve → bake pathBase64/length/sha256 → live `Test-AttrCudaFootagePart` before deploy/PresentMon/smoke; non-PASS → `OWNER_FOOTAGE_NOT_VERIFIED` exit 19 (index+status only, never path).
- `-ClipPath` / `-FixtureSha256` refused for owner ids; fixture arm unchanged. Shared verifier extracted into `AttrCudaArtifacts.psm1` and embedded in both presence + attribution jobs (byte-identical via `Get-AttrCudaEmbeddedFunctionSource`).
- Tests: behaviour (owner resolve/refuse, content auth pass/mismatch/missing, shared embed identity, resolver-before-closure ordering), split_route (CLIPPATH_REFUSED), claim-carrier pins dropped for the two files that only named the old refusal.
- PR body: hub verified locally (cuda behaviour+split_route+presence; never_authorized; resolver+verifier) before commit; hook prose / control hashes deferred to a later PR.
- Prior #144 CoS Improvement on ConvertTo dup **not** addressed on this head (orthogonal to footage-bind).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).