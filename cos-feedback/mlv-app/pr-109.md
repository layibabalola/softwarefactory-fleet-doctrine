schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 109
head: ca2ff99c602e217892883e39d78737e2d06aae4c
reviewed_utc: 2026-09-14T06:59:30Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route + Analyze (actions) SUCCESS on head ca2ff99c (no gitlink); Factory Bridge, Repo Hygiene (ubuntu+windows), Batch Compile, Windows GUI Pilot, Windows Product Oracles, Analyze (c-cpp/python) still in progress; CodeQL neutral; mergeStateStatus BLOCKED pending checks

## Blockers

(none on content — wait for required CI to finish green)

## Improvements

- `Invoke-Lane.ps1` receipt field `complete     =($null -eq ...` still missing the space after `=` (cosmetic; pre-existing style on this line).
- Repo-wide stash gate: `test_non_empty_stash_keeps_worktree` pins keep-on-stash, but a leftover stash anywhere in the shared repo will accumulate linked worktrees. Optional follow-up: document in the helper synopsis, or scope stash inspection if git allows without weakening the gate.
- `Invoke-Lane.ps1` `-RetireWorktree` is opt-in while `Invoke-Workstream.ps1` always uses the gate. Confirm every non-workstream lane entrypoint that creates linked worktrees passes `-RetireWorktree`, or disk hygiene will still depend on workstream-only callers.
- PR body says "8 falsifier cases"; tip actually has 10 (`nested-worktree`, `stash`, `rundir-inside`, `unresolvable merge-target` included). Align the count in the description.

## Notes

- Supersedes closed #108. Tip is exactly 2 commits over `fork/master` (`ff57a10f` + `ca2ff99c`); tree has **no** mode-160000 gitlink (verified via recursive tree API). Protected Check Route already SUCCESS — checkout works.
- Fable APPROVE on #108 (`b075c114`) is VOID for these SHAs per PR body; fresh review over `46edb202..ca2ff99c` is expected before merge.
- SAFE gate still looks sound: fail closed on probe errors, never `--force`, quarantine ignored non-debris before `git clean -fdX`, never delete branch refs, record `worktreeDisposition`. Scratch moves off shared `%TEMP%` to `C:\mlvtmp\lane-scratch` (DiskGuard-registered); `$scratchDir = $null` before try so finally cannot remove a parent-scope path.
- Behaviour change: committed-but-unpushed work keeps the worktree (safe); looping dispatchers that never push will accumulate worktrees via receipts — intentional and visible.
- DiskGuard flip correctly out of scope.
- CoS review via `gh` (no Cloud Agent on plan). No GitHub PR comment posted. Prior bus file for #108 remains on master for the closed PR; consumers should ignore feedback whose PR is no longer open.