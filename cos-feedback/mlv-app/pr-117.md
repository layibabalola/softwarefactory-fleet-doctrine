schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 117
head: 3b699bbbbf0a98d0ef2e0ea67ffd3317d4a3e942
reviewed_utc: 2026-09-15T08:41:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Hosted checks in progress after tip push (CodeQL Analyze actions/c-cpp/python, Factory Bridge Regressions, Protected Check Route, Repo Hygiene Python ubuntu/windows, Batch Compile). No content blockers.

## Blockers

(none)

## Improvements

(none)

## Notes

- Push tip `3b699bbb` addresses Sol PR #117 BLOCKER: with `is_error=false`, result TEXT no longer classifies a refusal; only numeric `api_error_status=429` still classifies. Prior CoS head was `5ddbbc2b` (oauth-expired pattern only).
- Original measured gap unchanged: PLAY-COUNTERS-CPU-B OAuth-expired envelope (`is_error=true`, `api_error_status=null`) now matches `provider-auth` via `failed to authenticate|oauth session expired`; receipt `state=refused`, lane exit 125.
- Diff scope: `tools/coordination/lane-provider-refusal.ps1` (+7/−1), `tools/coordination/test_coordination_guardrails.py` (+27). Tests: real expired envelope; quoting phrase on success is not refusal; Sol repro (`is_error=false` + status 401 + quoted auth text) stays null; 429 path kept. MERGEABLE.
- Local claim: refusal/envelope tests green; wait for hosted CI before merge.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan. Verdict unchanged from pr-opened pass (still merge-when-ci-green).
