schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 132
head: 5638f3ec5c09267dc9d4b274a015c38fb08f24eb
reviewed_utc: 2026-09-16T19:20:58Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Repo Hygiene Python (ubuntu-latest) PASS on tip (inventory fix landed); Factory Bridge, Protected Check Route, Windows GUI Pilot, CodeQL (actions/c-cpp/python) PASS; Repo Hygiene (windows), Batch Compile, Windows Product Oracles still pending; mergeStateStatus BEHIND (5 commits on master)

## Blockers

- **Branch behind master (mergeStateStatus BEHIND):** tip is still 5 commits behind `master` (`9c914fab` Merge PR #128 GPU-PROVENANCE-SELF-BINDING-1, plus related merge/profiling commits). Merge or rebase `master` into `sync/upstream-877dea2` and re-push before merge; do not land while BEHIND.
- **Required product checks still unsettled on this tip:** Batch Compile, Windows Product Oracles, and Repo Hygiene Python (windows-latest) were still pending at review time. Acceptance in the PR body requires every hosted required check green (including Batch Compile) and unchanged `tests/fixtures/golden/` hashes. Do not merge until those settle green on an updated (non-BEHIND) tip.

## Improvements

- **Retarget Arm64 release FFmpeg (still open after inventory land):** `ffmpeg-macos-arm64` is now declared and consumed by `MLVApp.pro` arm64 local install, but `.github/workflows/macOS-Arm64.yml` still extracts `ffmpeg-macos-x86_64` / `ffmpegOSX.zip`. Point the Arm64 release extract at `--payload-id ffmpeg-macos-arm64 --archive-reference ffmpegOSXarm.zip`, add that consumer under the new payload (and drop or demote the x86_64 payload's `macos-arm64` consumer marked `active-release-workflow-architecture-mismatch`), then rewrite/drop the redistribution blocker "The macOS Arm64 release consumes the x86_64-only ffmpegOSX.zip payload."
- **`src/mlv/video_mlv.c` conflict surface:** unchanged since prior tip — keeps fork TLS / dual-ISO / telemetry and upstream OpenJPH decode / JP2K export. No conflict markers. Still worth a focused human pass on the five conflict hunks named in the PR body before treating the merge as content-complete.
- **Codec-id follow-up `f25af4d4` still looks correct:** `ExportCodecIds.h` has `CODEC_MLV_LJ92` and `CODEC_MLV_JP2K_{LOW..LOSSLESS}` (5–9); spot-check UI lists in `ExportSettingsDialog.cpp` expose the new JP2K MLV options end-to-end.

## Notes

- Push since prior CoS head `f25af4d4`: single commit `5638f3ec` — registers upstream `platform/qt/FFmpeg/ffmpegOSXarm.zip` as payload `ffmpeg-macos-arm64` in `tools/gates/vendored-native-payloads.json` and bumps the committed payload_count pin 9→10 in `tools/repo_hygiene/test_vendored_native_payloads.py`. That clears the prior hygiene inventory-mismatch blocker (ubuntu hygiene now PASS).
- Scope unchanged: PROD-UPSTREAM-SYNC-1 real merge of upstream `ilia3101/MLV-App` `877dea2c` into fork parent `e60a1384` (32 upstream commits; merge commit `f1290cee`). Diff still ~+47k across ~95 files, dominated by vendored OpenJPH / Cineform / sse2neon.
- Kind is product (`src/` + `platform/`). No golden re-bless claimed. CoS review via `gh` (no Cloud Agent this pass). No GitHub PR review comment posted (bus only).
