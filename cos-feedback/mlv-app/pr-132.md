schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 132
head: faf2d247b94a912c8a5611f4896aa6854f519b42
reviewed_utc: 2026-09-16T22:18:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Protected Check Route PASS on tip faf2d247; Factory Bridge, Batch Compile, Repo Hygiene (ubuntu/windows), Windows Product Oracles, Windows GUI Pilot, CodeQL (actions/c-cpp/python) still IN_PROGRESS or queued; mergeStateStatus BLOCKED (required checks unsettled).

## Blockers

(none)

## Improvements

- **Retarget Arm64 release FFmpeg (still open):** `ffmpeg-macos-arm64` is declared and consumed by `MLVApp.pro` arm64 local install, but `.github/workflows/macOS-Arm64.yml` still extracts `ffmpeg-macos-x86_64` / `ffmpegOSX.zip` (line ~65 on tip). Point the Arm64 release extract at `--payload-id ffmpeg-macos-arm64 --archive-reference ffmpegOSXarm.zip`, add that consumer under the new payload (and drop or demote the x86_64 payload's `macos-arm64` consumer marked `active-release-workflow-architecture-mismatch`), then rewrite/drop the redistribution blocker about Arm64 consuming the x86_64-only zip.
- **`src/mlv/video_mlv.c` conflict hunks still deserve a focused human pass:** sync r3 closed the Sol round-2 gap (tests now drive production `openMlvClip` / `getMlvRawFrameUint16`), but the original five merge conflict hunks (fork TLS / dual-ISO / telemetry vs upstream OpenJPH) remain a large surface. Spot-check those hunks once before treating content as complete.

## Notes

- Push since prior CoS head `93a1633f`: sync r3 `50a656bb` (reject CURV payloads that are not a whole number of uint16 samples — closes the sizeof+3 silent-truncate case), sync r3 `49c19ccf` (integration regressions in `tests/pipeline/test_upstream_sync_hardening.cpp` through real clip open and frame reader, plus `ojph_decoder_test_stub` so JPEG2000 branch compiles under `ENABLE_JPEG2K`), and merge commit `faf2d247` bringing fork `master` (PR #131 PLAYBACK-ATTR-3-CUDA) into `sync/upstream-877dea2`.
- Prior CoS Improvement on helper-only tests is **addressed**: `UpstreamSyncHardening.MalformedCurvBlockFailsTheRealClipOpen`, `Jpeg2000FrameSizeIsGatedByTheAdmittedCapacity`, `Jpeg2000ChannelTableIsRejectedBeforeAnyDecode`, and `CompressedExportBranchesUseTheCodecNeutralReader` fail if production wiring is reverted.
- Arm64 FFmpeg workflow retarget remains an Improvement, not a merge blocker for this product sync.
- Scope unchanged: PROD-UPSTREAM-SYNC-1 real merge of upstream `ilia3101/MLV-App` `877dea2c` into fork parent `e60a1384` (32 upstream commits; merge commit `f1290cee`). Diff still ~+48k / 100 files, dominated by vendored OpenJPH / Cineform / sse2neon, plus now PLAYBACK-ATTR-3 tooling from #131.
- Kind is product (`src/` + `platform/`). No golden re-bless claimed. Codec-id follow-up `f25af4d4` still looks correct (`ExportCodecIds.h` has `CODEC_MLV_LJ92` and `CODEC_MLV_JP2K_*` 5–9). CoS review via `gh` (no Cloud Agent this pass). No GitHub PR review comment posted (bus only).
