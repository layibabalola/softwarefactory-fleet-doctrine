schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 132
head: 93a1633f0d65795e05829f89c9bdcbd3174c1d54
reviewed_utc: 2026-09-16T20:07:22Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Protected Check Route PASS on tip; Factory Bridge, Batch Compile, Repo Hygiene (ubuntu/windows), Windows Product Oracles, Windows GUI Pilot, CodeQL (actions/c-cpp/python) still IN_PROGRESS; mergeStateStatus BLOCKED (required checks unsettled). Branch is no longer BEHIND.

## Blockers

(none)

## Improvements

- **Retarget Arm64 release FFmpeg (still open):** `ffmpeg-macos-arm64` is declared and consumed by `MLVApp.pro` arm64 local install, but `.github/workflows/macOS-Arm64.yml` still extracts `ffmpeg-macos-x86_64` / `ffmpegOSX.zip` (line ~65 on tip). Point the Arm64 release extract at `--payload-id ffmpeg-macos-arm64 --archive-reference ffmpegOSXarm.zip`, add that consumer under the new payload (and drop or demote the x86_64 payload's `macos-arm64` consumer marked `active-release-workflow-architecture-mismatch`), then rewrite/drop the redistribution blocker about Arm64 consuming the x86_64-only zip.
- **`src/mlv/video_mlv.c` still deserves a focused human pass:** sync r2 landed capacity/channel-table/CURV fail-closed helpers and restored codec-neutral decompress for averaging/MLV_DECOMPRESS, but the original five conflict hunks (fork TLS / dual-ISO / telemetry vs upstream OpenJPH) remain a large merge surface. Spot-check those hunks once before treating content as complete.

## Notes

- Push since prior CoS head `5638f3ec`: four sync-r2 commits (`ab027d26` JPEG2000 capacity + channel-table validation; `c362c695` CURV LUT bound; `268691af` codec-neutral decompress for MLV_DECOMPRESS/averaged export; `1f700df5` regression tests in `tests/pipeline/test_phase3_infrastructure.cpp`) plus merge commit `93a1633f` bringing fork `master` (incl. PR #128 GPU-PROVENANCE-SELF-BINDING-1) into `sync/upstream-877dea2`. That clears the prior **Branch behind master** blocker.
- Prior hygiene inventory-mismatch blocker already cleared on `5638f3ec` (ubuntu hygiene PASS). Arm64 FFmpeg workflow retarget remains an Improvement, not a merge blocker for this product sync.
- Scope unchanged: PROD-UPSTREAM-SYNC-1 real merge of upstream `ilia3101/MLV-App` `877dea2c` into fork parent `e60a1384` (32 upstream commits; merge commit `f1290cee`). Diff still ~+47k, dominated by vendored OpenJPH / Cineform / sse2neon.
- Kind is product (`src/` + `platform/`). No golden re-bless claimed. Codec-id follow-up `f25af4d4` still looks correct (`ExportCodecIds.h` has `CODEC_MLV_LJ92` and `CODEC_MLV_JP2K_*` 5–9). CoS review via `gh` (no Cloud Agent this pass). No GitHub PR review comment posted (bus only).
