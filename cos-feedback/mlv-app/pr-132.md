schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 132
head: f25af4d4c01a9a81bc5d5d3f22d4ebfe4004c7bf
reviewed_utc: 2026-09-16T18:50:23Z
event: pr-opened
verdict: not-merge-ready
ci_summary: Repo Hygiene Python (ubuntu-latest) FAILED (2 fail + 2 error, unmanifested ffmpegOSXarm.zip); Protected Check Route + Analyze (actions/python) pass; Batch Compile, Factory Bridge, Windows GUI/Oracles, hygiene-windows, Analyze (c-cpp) still pending; mergeStateStatus BEHIND (5 commits on master)

## Blockers

- **Repo Hygiene red — unmanifested arm64 FFmpeg archive:** `tools/repo_hygiene/test_vendored_native_payloads.py` fails because `platform/qt/FFmpeg/ffmpegOSXarm.zip` is tracked in git (upstream arm64 ffmpeg, ~17.6 MiB) but absent from `tools/gates/vendored-native-payloads.json`. Same root cause surfaces as 2 ERRORs (`test_committed_manifest_integrity_passes_but_redistribution_remains_blocked`, `test_windows_release_target_admits_wow64_raw2mlv_compat_group_explicitly`) and 2 FAILs (`test_arm64_release_target_rejects_committed_x86_64_ffmpeg_before_packaging`, `test_windows_compat_group_rejects_mutated_dependency_hash`) — later asserts never run past the inventory mismatch. Unblock: add a declared payload (e.g. `ffmpeg-macos-arm64`) with path/bytes/sha256/member_summary/selected_members, point `macos-arm64` consumers in `MLVApp.pro` / arm64 release workflow at that id (pro already unzips `ffmpegOSXarm.zip` on `QT_ARCH=arm64`), refresh redistribution blockers that still say arm64 consumes `ffmpegOSX.zip`, then re-run hygiene.
- **Branch behind master (mergeStateStatus BEHIND):** tip is 5 commits behind `master` (`9c914fab` Merge PR #128 GPU-PROVENANCE-SELF-BINDING-1, plus related merge/profiling commits). Merge or rebase `master` into `sync/upstream-877dea2` and re-push before merge; do not land while BEHIND.
- **Required product checks still unsettled:** Batch Compile, Factory Bridge Regressions, Windows Product Oracles, Windows GUI Pilot, Repo Hygiene (windows), Analyze (c-cpp) were still pending at review time. Acceptance in the PR body requires every hosted required check green (including Batch Compile) and unchanged `tests/fixtures/golden/` hashes. Do not merge until those settle green on an updated tip.

## Improvements

- **`src/mlv/video_mlv.c` conflict surface:** head (~10649 lines) keeps fork TLS / dual-ISO / telemetry paths and adds upstream OpenJPH decode (`ojph_decoder_*`) plus JP2K export modes. No conflict markers remain. Still worth a focused human pass on the five conflict hunks called out in the PR body (decode class flags, export_mode LJ92/JP2K, seek/block paths) before treating the merge as content-complete — mechanical “both sides kept” is not a substitute for line-level review there.
- **After inventory lands:** drop or rewrite the redistribution blocker “macOS Arm64 release consumes the x86_64-only ffmpegOSX.zip” once arm64 packaging actually uses `ffmpegOSXarm.zip`; leave it only if arm64 workflows still extract the x86_64 zip.
- **Codec-id follow-up commit `f25af4d4` looks correct:** `ExportCodecIds.h` has `CODEC_MLV_LJ92` and `CODEC_MLV_JP2K_{LOW..LOSSLESS}` (5–9); `MLVBlender.c` / `video_mlv.h` use `MLV_LJ92`; `macros.h` / `mlv.h` gain `MLV_VIDEO_CLASS_FLAG_JPEG2K` and `isMlvLj92` / `isMlvJpeg2000`. Spot-check UI lists in `ExportSettingsDialog.cpp` still expose the new JP2K MLV options end-to-end.

## Notes

- Scope: PROD-UPSTREAM-SYNC-1 — real merge of upstream `ilia3101/MLV-App` `877dea2cb9413bd0542abb622af517cf12db63d3` into fork parent `e60a1384` (32 upstream commits; parents on merge commit `f1290cee`). Tip `f25af4d4` is the codec-id rename follow-up only. Diff ~+47k/− across 93 files, dominated by vendored OpenJPH / Cineform / sse2neon.
- Conflict files named in the merge message (workflows, README, `MLVApp.pro`, `ExportSettingsDialog.h`, blender, macros, mlv.h, `video_mlv.c`) are marker-clean on tip; workflow job id remains fork `build`; `MLVApp.pro` retains fork sources and adds OpenJPH.
- Kind is product (`src/` + `platform/`). No golden re-bless claimed. CoS review via `gh` (no Cloud Agent launched this pass). No GitHub PR review comment posted (bus only).