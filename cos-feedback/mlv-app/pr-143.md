schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 143
head: 84960dbd35b36eed257d7b565290b4526729145a
reviewed_utc: 2026-09-21T09:12:28Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 84960dbd (+1146/−0, 5 new files, ahead 2 / behind 0): Protected Check Route PASS; Analyze (actions) PASS; CodeQL skipping; Analyze (c-cpp/python), Batch Compile, Factory Bridge Regressions, Repo Hygiene Python (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles IN_PROGRESS; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which is the only runner that executes `test_attr3_footage_presence_job.py`)

## Improvements

- **No CI coverage of the real generator→resolver child path.** `test_attr3_footage_presence_job.py` imports `Attr3FootagePresenceJob.psm1` with fabricated parts only (correct for injection-seam removal). The production `attr3-footage-presence-job.ps1` child-process invoke of `resolve_consented_clip.py` (`--emit-json` temp file + finally-delete + clipId mismatch guard) is untested in CI. Add a thin integration row (synthetic consent table + mocked/`spec_bytes` resolver, or a fixture fork-ref) so a regression that reintroduces a skip-resolver flag or drops the emit-file delete is caught on windows-latest.
- **Presence-job suite is Windows-only** (`@unittest.skipUnless(... == "nt")`). Expected (Bachelor target), but means ubuntu Repo Hygiene never exercises base64 embed / quote-breakout / FOOTAGE_* exits — treat windows-latest red as blocking for this PR even if ubuntu is green.
- **`resolve()` KeyError on malformed spec parts.** Missing `path` / `length` / `sha256` keys raise bare `KeyError` instead of a typed `ResolveError`. Prefer a `SPEC_PART_SCHEMA` refusal so the generator's `ATTR3_PRESENCE_RESOLVE_REFUSED` fold stays machine-readable.
- **PR-A does not close ATTR3-FOOTAGE-BIND-1.** CUDA owner route (PR-B) and hook prose / control-hash (PR-C) still required before any owner-clip CUDA consumption. Keep `playback-attr-3-cuda` owner-clip refusal until those land.
- Soft: still **0 GitHub reviews** on a path-handling generator that embeds consented paths (base64) into Bachelor jobs. Not a CoS merge blocker for additive tooling that changes no existing file, but a short human glance before merge is still cheap.

## Notes

- Scope is additive only: new resolver, presence-job generator + psm1 module, and two hygiene test modules. No existing file touched; no playback / publish / CUDA open.
- Round-2 head correctly closes three hub defects: (1) base64 path embed instead of backslash-only allowlist (real `output-budget.json` paths use `/`; TEMP may contain `~`); (2) environment-independent presence tests forcing forward-slash + `~` parts; (3) removal of public `-InjectResolvedPartsJsonPathForTests` by splitting job-text into `New-Attr3FootagePresenceJob`.
- Resolver design matches card: `git --no-replace-objects show` of full ref `refs/remotes/fork/master:tools/gates/output-budget.json` (4 MiB cap); cross-check length + sha256 (case-folded) + `sha256(norm(path))` against hook `OWNER_CONSENTED_FOOTAGE` via `verify_consented_footage.load_consent_table` + hook's own `norm`; CLI summary path-free (`sha256_12` only); `--emit-json` is the only path channel.
- Hub-claimed M16-1243 bachelor proof (`RESULT=FOOTAGE_ABSENT`, both parts MISSING) is outside this PR's CI; CoS did not re-run bachelor. `Test-Path` still cannot distinguish absent vs ACL deny (acknowledged in PR body).
- Prerequisite: NA4 (#134) already **merged** on master (`2026-09-21T07:31:52Z`), so worktree hook + `load_consent_table` exist for this tip. Spec still pinned to `refs/remotes/fork/master` — hub hosts must keep that remote populated.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).