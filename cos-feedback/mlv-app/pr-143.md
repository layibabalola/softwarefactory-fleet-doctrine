schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 143
head: 119c4adce78562f0abc4941df888f6d7e89bbb8d
reviewed_utc: 2026-09-21T10:33:28Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 119c4adc (round 5: +21/−1 Attr3FootagePresenceJob.psm1, +29/−0 test_attr3_footage_presence_job.py; PR ahead 5 / behind 0, +1602/−0 across 6 files): all checks PENDING (Protected Check Route, Analyze actions/c-cpp/python, Batch Compile, Factory Bridge Regressions, Repo Hygiene Python ubuntu+windows); mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which is the only runner that executes `test_attr3_footage_presence_job.py`, including the new length-mismatch+ACL-deny row)

## Improvements

- **No CI coverage of the real generator→resolver child path.** Still open from prior CoS. `test_attr3_footage_presence_job.py` imports `Attr3FootagePresenceJob.psm1` with fabricated parts only. The production `attr3-footage-presence-job.ps1` child-process invoke of `resolve_consented_clip.py` (`--emit-json` temp file + finally-delete + clipId mismatch guard) remains untested in CI. Add a thin integration row so a regression that reintroduces a skip-resolver flag or drops the emit-file delete is caught on windows-latest.
- **Presence-job suite is Windows-only** (`@unittest.skipUnless(... == "nt")`). Expected (Bachelor target), but means ubuntu Repo Hygiene never exercises base64 embed / quote-breakout / FOOTAGE_* exits — treat windows-latest red as blocking for this PR even if ubuntu is green. Round 5's additional ACL row keeps this Windows-bound.
- **`resolve()` KeyError on malformed spec parts.** Still open. Missing `path` / `length` / `sha256` keys raise bare `KeyError` instead of a typed `ResolveError`. Round 4 sanitizes summary *output* for wrong-typed length/sha256 (`<malformed>`), but a missing key still blows up untyped before that path. Prefer a `SPEC_PART_SCHEMA` refusal so the generator's `ATTR3_PRESENCE_RESOLVE_REFUSED` fold stays machine-readable.
- **PR-A does not close ATTR3-FOOTAGE-BIND-1.** CUDA owner route (PR-B) and hook prose / control-hash (PR-C) still required before any owner-clip CUDA consumption. Keep `playback-attr-3-cuda` owner-clip refusal until those land.
- Soft: still **0 GitHub reviews** on a path-handling generator that embeds consented paths (base64) into Bachelor jobs. Not a CoS merge blocker for additive tooling that changes no existing product file, but a short human glance before merge is still cheap.

## Notes

- Round-5 tip `119c4adc` closes a LENGTH_MISMATCH honesty hole left after round 4: when `Get-Item` succeeds but length differs, the probe previously assigned `LENGTH_MISMATCH`/`FOOTAGE_MISMATCH` without ever reading a content byte. Now it `OpenRead`s (and consumes one byte when non-empty) first; `UnauthorizedAccessException`/`PermissionDenied` → `ACCESS_DENIED`, other failures → `UNREADABLE`, so the overall result falls back to `FOOTAGE_INDETERMINATE` (exit 3) instead of a false MISMATCH. New Windows ACL test `test_length_mismatch_with_denied_content_read_is_footage_indeterminate` asserts PART=0 ACCESS_DENIED + RESULT=FOOTAGE_INDETERMINATE and no LENGTH_MISMATCH/FOOTAGE_MISMATCH tokens. Exception text still never reaches job output.
- Scope remains additive for the feature: id-only resolver, presence-job generator + psm1, two hygiene test modules, claim-carrier pin from round 3. No playback / publish / CUDA open. Round 5 touches only the psm1 probe + its hygiene test.
- Round-4 hardening still stands (streaming SPEC_CAP kill, full-ref only / `--ref` removed, `<malformed>` summary sanitization, wrapped FS ops, NOT_FOUND/ACCESS_DENIED/FOOTAGE_INDETERMINATE). Round-2 still carries base64 path embed, environment-independent presence tests, removal of public `-InjectResolvedPartsJsonPathForTests`.
- Resolver design unchanged this round: `git --no-replace-objects show` of full ref `refs/remotes/fork/master:tools/gates/output-budget.json` (4 MiB streaming cap); cross-check length + sha256 (case-folded) + `sha256(norm(path))` against hook `OWNER_CONSENTED_FOOTAGE`; CLI summary path-free; `--emit-json` is the only path channel.
- Hub-claimed M16-1243 bachelor proof (`RESULT=FOOTAGE_ABSENT`) remains outside this PR's GitHub CI. Round 4/5 INDETERMINATE splits may change how access-denied cases classify on a bachelor re-run — CoS did not re-run bachelor.
- Prerequisite: NA4 (#134) already merged on master. Spec still pinned to `refs/remotes/fork/master`.
- Prior CoS Improvements (generator→resolver CI coverage, KeyError typing, PR-B/C, soft human glance) are **not** addressed by round 5; the LENGTH_MISMATCH readability fix is a hub-sourced contract close on top of round 4, not those CoS Improvement bullets.
- Verdict unchanged (`merge-when-ci-green`). Chat silent per later-push hygiene (no verdict change; no prior CoS Improvement clearly closed).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).