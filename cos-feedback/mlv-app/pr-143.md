schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 143
head: 040ec6388ea686fba81a4b5dd813680174f652c6
reviewed_utc: 2026-09-21T10:10:38Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 040ec638 (+126/−10 resolve_consented_clip.py, +48/−15 Attr3FootagePresenceJob.psm1, +4/−2 attr3-footage-presence-job.ps1, +74/−7 test_attr3_footage_presence_job.py, +189/−4 test_resolve_consented_clip.py; ahead 4 / behind 0): Protected Check Route PASS; Analyze (actions/c-cpp/python), Batch Compile, Factory Bridge Regressions, Repo Hygiene Python (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles PENDING; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which is the only runner that executes `test_attr3_footage_presence_job.py`, including the new ACL-denial and file-lock UNREADABLE rows)

## Improvements

- **No CI coverage of the real generator→resolver child path.** Still open from prior CoS. `test_attr3_footage_presence_job.py` imports `Attr3FootagePresenceJob.psm1` with fabricated parts only. The production `attr3-footage-presence-job.ps1` child-process invoke of `resolve_consented_clip.py` (`--emit-json` temp file + finally-delete + clipId mismatch guard) remains untested in CI. Add a thin integration row so a regression that reintroduces a skip-resolver flag or drops the emit-file delete is caught on windows-latest.
- **Presence-job suite is Windows-only** (`@unittest.skipUnless(... == "nt")`). Expected (Bachelor target), but means ubuntu Repo Hygiene never exercises base64 embed / quote-breakout / FOOTAGE_* exits — treat windows-latest red as blocking for this PR even if ubuntu is green. Round 4's new ACL (`icacls`) and exclusive-lock rows make this even more Windows-bound.
- **`resolve()` KeyError on malformed spec parts.** Still open. Missing `path` / `length` / `sha256` keys raise bare `KeyError` instead of a typed `ResolveError`. Round 4 sanitizes summary *output* for wrong-typed length/sha256 (`<malformed>`), but a missing key still blows up untyped before that path. Prefer a `SPEC_PART_SCHEMA` refusal so the generator's `ATTR3_PRESENCE_RESOLVE_REFUSED` fold stays machine-readable.
- **PR-A does not close ATTR3-FOOTAGE-BIND-1.** CUDA owner route (PR-B) and hook prose / control-hash (PR-C) still required before any owner-clip CUDA consumption. Keep `playback-attr-3-cuda` owner-clip refusal until those land.
- Soft: still **0 GitHub reviews** on a path-handling generator that embeds consented paths (base64) into Bachelor jobs. Not a CoS merge blocker for additive tooling that changes no existing product file, but a short human glance before merge is still cheap.

## Notes

- Round-4 tip `040ec638` (keys sol + astra) hardens five defects the hub committed unchanged after sonnet lane verification (targeted suites 49 OK; `test_mlv_never_authorized` 283 OK — hub-claimed, CoS did not re-run):
  1. **Bounded streaming spec read:** `_read_spec_bytes` now uses `Popen` + 64 KiB chunks and `kill`s the child the instant `SPEC_CAP_BYTES` is exceeded (no full-buffer-then-measure). Tests cover incremental kill via `_FakeInfiniteProc`.
  2. **Full-ref contract:** CLI `--ref` removed; `resolve()` always validates `refs/remotes/...` via `_validate_full_ref` / `InvalidRefError` (short refs, `HEAD`, `..`, `:`, `@{`, `^`, `~` refused).
  3. **No path leaks through summary diagnostics:** `_safe_summary_length` / `_safe_summary_sha_prefix` replace non-int / non-64-lowercase-hex fields with literal `<malformed>` before JSON emit; tests smuggle a path token in length/sha256 and assert it never reaches stdout/stderr.
  4. **Probe FS wrap:** every `Get-Item` / `Get-FileHash` on a part is try/catch'd; exception text never reaches job output (only fixed status tokens).
  5. **Honest verdicts:** per-part `NOT_FOUND` / `ACCESS_DENIED` / `UNREADABLE` / `LENGTH_MISMATCH` / `SHA256_MISMATCH` / `PASS`; overall adds `FOOTAGE_INDETERMINATE` (exit 3). Behaviour change vs round 2/3: PASS+NOT_FOUND is now INDETERMINATE, not MISMATCH. New tests for ACL deny (`icacls`) and exclusive-lock UNREADABLE.
- Scope remains additive for the feature: id-only resolver, presence-job generator + psm1, two hygiene test modules, claim-carrier pin from round 3. No playback / publish / CUDA open.
- Round-2 still carries the three hub defect closes: base64 path embed, environment-independent presence tests, removal of public `-InjectResolvedPartsJsonPathForTests`.
- Resolver design: `git --no-replace-objects show` of full ref `refs/remotes/fork/master:tools/gates/output-budget.json` (4 MiB streaming cap); cross-check length + sha256 (case-folded) + `sha256(norm(path))` against hook `OWNER_CONSENTED_FOOTAGE`; CLI summary path-free; `--emit-json` is the only path channel.
- Hub-claimed M16-1243 bachelor proof (`RESULT=FOOTAGE_ABSENT`) remains outside this PR's GitHub CI; note that round-4's NOT_FOUND/INDETERMINATE split may change how that proof classifies on a re-run if access-denied was previously folded into absence — CoS did not re-run bachelor.
- Prerequisite: NA4 (#134) already merged on master. Spec still pinned to `refs/remotes/fork/master`.
- Prior CoS Improvements (generator→resolver CI coverage, KeyError typing, PR-B/C) are **not** addressed by round 4; path-leak / Test-Path honesty defects that round 4 closes were hub-sourced, not those CoS Improvement bullets.
- Verdict unchanged from round 3 (`merge-when-ci-green`). Chat silent per later-push hygiene (no verdict change; no prior CoS Improvement clearly closed).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
