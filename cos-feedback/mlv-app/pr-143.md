schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 143
head: c1fc2f7c3d59fa0f5ed7fcaccfacf1da9a415bdd
reviewed_utc: 2026-09-21T09:32:10Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip c1fc2f7c (+1150/−0, 6 files, ahead 3 / behind 0): Protected Check Route PASS; CodeQL skipping; Analyze (actions/c-cpp/python), Batch Compile, Factory Bridge Regressions, Repo Hygiene Python (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles IN_PROGRESS; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which is the only runner that executes `test_attr3_footage_presence_job.py`)

## Improvements

- **No CI coverage of the real generator→resolver child path.** `test_attr3_footage_presence_job.py` imports `Attr3FootagePresenceJob.psm1` with fabricated parts only (correct for injection-seam removal). The production `attr3-footage-presence-job.ps1` child-process invoke of `resolve_consented_clip.py` (`--emit-json` temp file + finally-delete + clipId mismatch guard) is untested in CI. Add a thin integration row (synthetic consent table + mocked/`spec_bytes` resolver, or a fixture fork-ref) so a regression that reintroduces a skip-resolver flag or drops the emit-file delete is caught on windows-latest.
- **Presence-job suite is Windows-only** (`@unittest.skipUnless(... == "nt")`). Expected (Bachelor target), but means ubuntu Repo Hygiene never exercises base64 embed / quote-breakout / FOOTAGE_* exits — treat windows-latest red as blocking for this PR even if ubuntu is green.
- **`resolve()` KeyError on malformed spec parts.** Missing `path` / `length` / `sha256` keys raise bare `KeyError` instead of a typed `ResolveError`. Prefer a `SPEC_PART_SCHEMA` refusal so the generator's `ATTR3_PRESENCE_RESOLVE_REFUSED` fold stays machine-readable.
- **PR-A does not close ATTR3-FOOTAGE-BIND-1.** CUDA owner route (PR-B) and hook prose / control-hash (PR-C) still required before any owner-clip CUDA consumption. Keep `playback-attr-3-cuda` owner-clip refusal until those land.
- Soft: still **0 GitHub reviews** on a path-handling generator that embeds consented paths (base64) into Bachelor jobs. Not a CoS merge blocker for additive tooling that changes no existing product file, but a short human glance before merge is still cheap.

## Notes

- Round-3 tip `c1fc2f7c` adds four entries to `_CLAIM_CARRIERS_PINNED` in `tools/repo_hygiene/test_mlv_never_authorized.py`: `resolve_consented_clip.py`, `Attr3FootagePresenceJob.psm1`, `attr3-footage-presence-job.ps1`, `test_attr3_footage_presence_job.py`. Hub CI-form had failed `test_the_anti_needle_runs_over_the_carriers_the_walk_returns` because these new consented-route carriers were unpinned; each was read with no exclusive-route assert, so PINNED (not excluded) is the correct #134 response. +4/−0 on that one hygiene file; product tooling unchanged this round.
- Scope remains additive for the feature: id-only resolver, presence-job generator + psm1, two hygiene test modules, plus the claim-carrier pin. No playback / publish / CUDA open.
- Round-2 still carries the three hub defect closes: base64 path embed, environment-independent presence tests, removal of public `-InjectResolvedPartsJsonPathForTests`.
- Resolver design unchanged: `git --no-replace-objects show` of full ref `refs/remotes/fork/master:tools/gates/output-budget.json` (4 MiB cap); cross-check length + sha256 (case-folded) + `sha256(norm(path))` against hook `OWNER_CONSENTED_FOOTAGE`; CLI summary path-free; `--emit-json` is the only path channel.
- Hub-claimed M16-1243 bachelor proof (`RESULT=FOOTAGE_ABSENT`) remains outside this PR's GitHub CI; CoS did not re-run bachelor.
- Prerequisite: NA4 (#134) already merged on master. Spec still pinned to `refs/remotes/fork/master`.
- Prior CoS Improvements (generator→resolver CI coverage, KeyError typing, PR-B/C) are **not** addressed by round 3.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
