schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: 254cafaacf7978ea4232f6d45c565985c2064aba
reviewed_utc: 2026-09-25T00:23:19Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 254cafaa (+4111/−20, 11 files, 18 commits vs master): all tip checks IN_PROGRESS — Protected Check Route, Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python (Actions runs 36077257041 / 36077257081 / 36077253679); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 254cafaa, especially Repo Hygiene Python windows/ubuntu which exercise the round-8 SkipEvidenceCollection / syscall-window / quiet-state-predicate / expression-aware census suite.)

## Improvements

- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at syscall-adjacent `systemTimesCapturedAtUtc` elapsed math, during-leg `-SkipEvidenceCollection`, and the six-site clean-iff-quiet∧¬provisional predicate remains cheap.
- Soft (carry): prior CoS soft items on cumulative top-consumer decision input (round 7) and mid-leg sampling / round-4–6 Improvements remain addressed; no new soft carry from this tip beyond the review glance above.

## Notes

- Round 8 (`254cafaa` vs prior CoS head `a058a43f`): +524/−46 across 7 files (3 commits: `c7fcae8f`, `2d731fd9`, `254cafaa`). Addresses Sol/astra round-7 residual findings:
  - **Item 1 (BLOCKER = fable minor 1 — window misalignment):** `Get-HostLoadSystemTimes` stamps `capturedAtUtc` immediately adjacent to the `GetSystemTimes` syscall; `Get-HostLoadNonSubjectCpuLoadPercent` / verdict intervals compute elapsed from `systemTimesCapturedAtUtc` on both sides, never from `Get-HostLoadSnapshot`'s top-of-function `capturedAtUtc` (which previously included CIM/Get-Process evidence work and misaligned busy vs wall windows).
  - **Item 2 (MAJOR — evidence enumeration inside measured legs):** `-SkipEvidenceCollection` on `Get-HostLoadSnapshot` skips the entire CIM + `Get-Process` evidence block for during-leg samples (syscall + subject-handle only); before/after brackets keep full receipt evidence. Tests assert skip calls neither CIM nor Get-Process, and omit-switch still runs both.
  - **Item 3 (MAJOR — contradictory legs accepted as clean):** canonical predicate at all six readers across five scripts (`compare-machine-perf`, `compare-release-gui-smoke-ab`, `run-release-cuda-playback-ab`, `run-ultramagnus-p3-validation` ×2, `summarize-local-cuda-proof`): clean iff `state=="quiet"` AND `provisional==false`; every other combination (including exceeded/false) is provisional. Exact-repro tests for exceeded/false at each site.
  - **Item 4 (MAJOR — census hardcoded-false detector):** expression-aware RHS normalize (strip balanced outer parens + whitespace) catches `($false)`, `-not $true` / `!$true`, `[bool]0` / `[bool]0.0`; prior bare-`$false` regex missed parenthesized wrap.
  - **Item 5 (fable minor 2):** `Wait-SystemCpuSettle` catch stores exception type only (not raw Message) so receipt cannot embed host/namespace identifiers.
- Scope unchanged: exceeded/unknown ⇒ PROVISIONAL; default bar 75%. PR discloses unrelated `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged; prior soft Improvements not newly addressed this push — **chat quiet** per later-push rule.
