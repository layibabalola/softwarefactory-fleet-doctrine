schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: 33c489f6dbe17cc17bb822a4a05bec7f0694d8d7
reviewed_utc: 2026-09-23T10:41:27Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 33c489f6 (+527/−1, 4 files, 1 commit vs master): all tip checks IN_PROGRESS — Protected Check Route, Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python (Actions runs 35850155239 / 35850155245 / 35850152761); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 33c489f6, especially Repo Hygiene Python windows/ubuntu which exercise the new `test_playback_host_load_gate.py` pwsh probes.)

## Improvements

- Soft: `Get-HostLoadSnapshot` ranks top consumers via `Get-Process | Sort-Object CPU` (cumulative CPU time since process start), not instantaneous load. The bar itself correctly uses `Win32_Processor.LoadPercentage`. Name-only top list is fine as evidence context; if it is ever used to decide who to kill or pause, switch to a per-interval delta (e.g. two samples) so interactive apps that have been up for hours do not dominate.
- Soft: host-load samples bracket the process (before `Process::Start`, after exit) only. A mid-leg spike that settles before the after-snapshot can still mark the run `quiet` while fps was polluted. Stronger gate would sample once mid-playback (or max of N ticks) without failing the run. Not a merge blocker for v1.
- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at the provisional-refusal wiring in `compare-release-gui-smoke-ab.ps1` / `run-release-cuda-playback-ab.ps1` remains cheap.

## Notes

- Scope: gates fps *comparisons* on host load for bachelor-as-interactive-workstation. `run-release-gui-smoke.ps1` records `hostLoad` (schema `mlvapp-gui-smoke-host-load.v1`) with quiet/exceeded/unknown; exceeded or unknown ⇒ PROVISIONAL (never fail the smoke, only mark). Compare and CUDA A/B refuse any provisional leg as a regression/acceptance signal. Missing `hostLoad` (pre-gate results) reads as UNKNOWN, never quiet.
- Default bar `-HostLoadCpuPercentBar 75`; fixture in `test_playback_host_load_gate.py` mirrors the measured 96% sample from 2026-09-22 and must exceed. Top consumers recorded by process name only (no paths/command lines).
- Tests: spliced-verbatim pwsh probes (same precedent as attr3 CUDA behaviour tests); skip cleanly when pwsh is absent. Ubuntu hygiene will skip the pwsh classes; windows hygiene is the real gate for those.
- PR discloses unrelated `tools/repo_hygiene/test_brokered_closeout.py` timeouts under concurrent host load, filed separately as `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap). Same class of problem one subsystem over — track separately, do not block this PR on it.
- Pairs conceptually with clip-length validity (`PLAYBACK-CHECK-CLIP-LENGTH-1`); both are preconditions for fps claims on this board.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
