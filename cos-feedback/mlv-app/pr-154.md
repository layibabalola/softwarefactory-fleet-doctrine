schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: b1a4ab5545063801c83843932e1cc1c98625ebfb
reviewed_utc: 2026-09-25T02:27:40Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip b1a4ab55 (+4696/−20, 11 files, 22 commits vs master): Protected Check Route SUCCESS; Analyze (actions) SUCCESS; CodeQL neutral; Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows Product Oracles, Windows GUI Pilot, Analyze c-cpp/python IN_PROGRESS (Actions runs 36086278862 / 36086278921 / 36086276195); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on b1a4ab55, especially Repo Hygiene Python windows/ubuntu which exercise the round-9 producer-alignment / census-allowlist / sanitized-catch / reader-predicate suite plus the hub Windows-only gate on two snapshot tests that reddened ubuntu on 254cafaa.)

## Improvements

- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at round-9 syscall+subjectCpu back-to-back in `Get-HostLoadSystemTimes`, evidence-after-syscall ordering in `Get-HostLoadSnapshot`, and the inverted `$true`-or-derived census allowlist remains cheap.
- Soft (carry): prior CoS soft items on cumulative top-consumer decision input (round 7) and mid-leg sampling / round-4–8 Improvements remain addressed; no new soft carry from this tip beyond the review glance above.

## Notes

- Round 9 + hub (`b1a4ab55` vs prior CoS head `254cafaa`): +654/−69 across 2 files (4 commits: `1da4aece`, `e38e75c1`, `5b2da490`, `b1a4ab55`). Addresses Sol/fable round-8 residual findings:
  - **Item 1 (sol BLOCKER — subject CPU read after evidence gap):** `Get-HostLoadSystemTimes` now takes `-SubjectProcess`/`-SubjectNotYetStarted` and reads subject `TotalProcessorTime` immediately after the `GetSystemTimes` syscall (null-checks only between); returns `subjectCpuSeconds` with idle/kernel/user/`capturedAtUtc`. `Get-HostLoadSnapshot` calls that helper FIRST, wires all decision fields from it, and runs CIM/Get-Process evidence strictly after — closing sol's delayed-subject-read quiet undercount (96%→74%) and fable's evidence-stretched final interval. Producer-alignment tests execute the real chain (sentinel override, slow Get-CimInstance mock, CPU-spinning child), not only synthetic consumer fixtures.
  - **Item 2 (sol MAJOR — census false-shape blacklist incomplete):** census inverted to `$true`-or-derived allowlist after RHS normalize; any other recognized constant (`$false`, `$null`, quoted strings, bare numbers including `0`, `[bool]` casts including `[bool]1`, negations) is unbound. Tests cover sol's bare-`0` repro plus `$null`/`''`/`'false'`/`[bool]1`; production `$true` hardcode still passes.
  - **Item 3 minors:** executable regression tests for sanitized catches (`Wait-SystemCpuSettle` + two screenshot metadata catches — type name only); reader-predicate census pins exactly six canonical `provisional -or state -ne "quiet"` sites and rejects the pre-round-7 `-eq "unknown"` drift.
  - **Hub (`b1a4ab55`):** gates `test_a_throwing_get_process_during_top_consumers_gathering_does_not_block_collection` and `test_skip_evidence_collection_calls_neither_cim_nor_get_process` on `os.name == "nt"` (both assert `SYSTEM_TIMES_COLLECTED=True` via Windows-only GetSystemTimes); siblings already carried this gate. Fixes the ubuntu Repo Hygiene red observed on prior tip `254cafaa`.
- Scope unchanged: exceeded/unknown ⇒ PROVISIONAL; default bar 75%. PR discloses unrelated `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged; prior soft Improvements not newly addressed this push — **chat quiet** per later-push rule.
