schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: 36d4804eb4ab665a23300983b9bfd9efd93f5f34
reviewed_utc: 2026-09-25T03:00:33Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 36d4804e (+4772/−20, 11 files, 23 commits vs master): all tip checks IN_PROGRESS/queued — Protected Check Route, Batch Compile, Repo Hygiene Python windows/ubuntu (Actions run 36088544080); Factory Bridge Regressions (36088544069); Analyze actions/python/c-cpp (36088541289). Windows Product Oracles / Windows GUI Pilot not yet listed at review time. mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 36d4804e, especially Repo Hygiene Python windows which exercises the new before-bracket evidence-then-syscall ordering pins.)

## Improvements

- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at the hub BEFORE/AFTER syscall-ordering split in `Get-HostLoadSnapshot` (evidence-then-syscall when `-SubjectNotYetStarted`, syscall-then-evidence otherwise) remains cheap.
- Soft (carry): prior CoS soft items on cumulative top-consumer decision input (round 7) and mid-leg sampling / round-4–9 Improvements remain addressed; no new soft carry from this tip beyond the review glance above.

## Notes

- Round 10 hub (`36d4804e` vs prior CoS head `b1a4ab55`): +133/−57 across 2 files (1 commit). Addresses sol r9 contract BLOCKER (PARK/SPLIT ruling 2026-09-25) on BEFORE-bracket interval dilution:
  - **`Get-HostLoadSnapshot` ordering split:** AFTER bracket and during-leg samples keep round-9 syscall-first (evidence latency must not stretch the final interval past leg end). BEFORE bracket (`-SubjectNotYetStarted`, the only production caller starts `Process.Start` immediately after) now takes evidence first, then system-times counters last — so evidence latency is not folded into the first interval as pre-leg quiet that dilutes an early-leg load burst (sol: 96%→quiet). `subjectCpuSeconds` is a constant 0.0 on that path, so moving the syscall later cannot re-open round 8's subject-CPU inflation blocker.
  - **Tests:** `test_slow_evidence_collection_does_not_delay_the_syscall_timestamp` retargeted to AFTER shape (`-SubjectProcess` on a real idle child); new `test_before_bracket_takes_system_times_after_slow_evidence` (Windows-only, END_GAP_MS < 400 after dual 900ms CIM mocks); new `test_the_before_bracket_call_site_uses_the_evidence_first_shape` pins the sole `$hostLoadBefore = Get-HostLoadSnapshot` call keeps `-SubjectNotYetStarted` and does not pass `-SkipEvidenceCollection`.
- Scope unchanged: exceeded/unknown ⇒ PROVISIONAL; default bar 75%. PR discloses unrelated `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged; prior soft Improvements not newly addressed this push — **chat quiet** per later-push rule.
