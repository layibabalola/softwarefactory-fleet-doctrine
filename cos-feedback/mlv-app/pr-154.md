schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: a058a43f6ef7ecbc053367a97c9dfe44ea5279b5
reviewed_utc: 2026-09-24T23:00:17Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip a058a43f (+3630/−17, 11 files, 15 commits vs master): all tip checks IN_PROGRESS — Protected Check Route, Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python (Actions runs 36070387946 / 36070387944 / 36070383785); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on a058a43f, especially Repo Hygiene Python windows/ubuntu which exercise the rewritten GetSystemTimes host-load suite and the mutation-sensitive ObservedMaxSampleGap / P3 speedValidated executable guards.)

## Improvements

- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at GetSystemTimes busy-time math, subject-handle (not PID re-lookup) after-exit reads, and the fps-delta / console-column refusal paths remains cheap.
- Soft (carry): prior CoS soft item on cumulative `Get-Process | Sort-Object CPU` top-consumer ranking as decision input is **addressed** in round 7 — decision path is GetSystemTimes + subject handle only; `cpuLoadPercent` / `topCpuConsumers` retained for receipt evidence and never feed quiet/exceeded. Prior mid-leg sampling / round-4–6 Improvements remain addressed.

## Notes

- Round 7 (`a058a43f` vs prior CoS head `d3859b85`): +1141/−932 across 6 files (3 commits). Addresses Sol/astra round-6/7 findings:
  - **Item 1 (BLOCKER, both keys):** replace per-process enumeration/summing BY CONSTRUCTION. `Get-HostLoadSystemTimes` (one Win32 `GetSystemTimes` syscall); `Get-HostLoadNonSubjectCpuLoadPercent` = ((busyDelta − subjectDelta) / (elapsed × logicalProcessors)) × 100, clamped [0,100], `$null` (never raw fallback) when either side uncollectable. Subject CPU read from held `Process` handle (`-SubjectProcess`), not `Get-Process -Id` (fails after exit). `Get-HostLoadVerdict` computes every consecutive-pair interval itself. PID-reuse / one-sided loader / throwing-getter-as-zero class is not expressible. Explicit null-check on disposed-handle property getter (live-caught PowerShell silent `$null`).
  - **Item 5 (MAJOR):** sampling moved out of `Wait-GuiSmokeProcessBounded` (drops `-SampleIntervalMs`/`-OnSample`, single bounded wait) into `run-release-gui-smoke.ps1` inline chunk loop — no callback that can never return.
  - **Item 6 (MINOR, sol):** remaining raw `$_.Exception.Message` catches in `gui-smoke-process-boundary.psm1` sanitized to exception type.
  - **Item 2 (MAJOR):** `compare-machine-perf.ps1` `New-PlaybackAbSummaryRow` refuses `playback_fps_delta_pct` to `$null` with `playback_fps_delta_refused_reason` when either leg provisional; per-leg fps intentionally left visible.
  - **Item 3 (MAJOR):** `run-local-gpu-capability.ps1` console column order moves `fps` + `host_load_provisional` early so `Format-Table -AutoSize` cannot silently drop them (live-caught 120-col discard).
  - **Item 4:** two new executable mutation-sensitive guards — ObservedMaxSampleGap (`-gt`→`-lt` fails; wiring-only stayed green) and P3 `speedValidated` (`-not` flip fails; wiring-only stayed green).
- Scope unchanged: exceeded/unknown ⇒ PROVISIONAL; default bar 75%. PR discloses unrelated `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged; prior soft Improvement on cumulative top-consumer decision input clearly addressed — **chat report** per later-push rule.
