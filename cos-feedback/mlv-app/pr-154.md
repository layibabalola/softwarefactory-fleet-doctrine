schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: 6d60d7ce5cb34ffcd205f55589d67ac52287f281
reviewed_utc: 2026-09-24T18:31:23Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 6d60d7ce (+2527/−15, 9 files, 6 commits vs master): all tip checks IN_PROGRESS — Protected Check Route, Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python (Actions runs 36041734383 / 36041734388 / 36041730176); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft. Windows GUI Pilot / Windows Product Oracles not yet listed on this tip.

## Blockers

(none in the reviewed tip — wait for required checks on 6d60d7ce, especially Repo Hygiene Python windows/ubuntu which exercise `test_playback_host_load_gate.py` and the new process-boundary wait-bound / OnSample-sanitize tests in `test_process_hygiene_contract.py`.)

## Improvements

- Soft: `Get-HostLoadSnapshot` still ranks top consumers via `Get-Process | Sort-Object CPU` (cumulative CPU time since process start), not instantaneous load. Round 4/5 correctly judge the bar via matched-window non-subject load; the name-only top list remains cumulative evidence context. If that list is ever used to decide who to kill or pause, switch it to a per-interval delta.
- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at matched-window `totalCpuSeconds` subtraction, re-import state+provisional checks, and the New-*Row census remains cheap.
- Soft (carry): prior mid-leg sampling / round-4 sampling-coverage Improvements remain **addressed**. Round 5 closes Sol findings on top (see Notes); prior CoS soft items above still open.

## Notes

- Round 5 (`6d60d7ce` vs prior CoS head `9481e01d`): +704/−29 across 7 files (1 commit). Addresses Sol round-5 findings on the same card:
  - **Matched-window CPU subtraction (Sol MAJOR):** `Get-HostLoadNonSubjectCpuLoadPercent` no longer subtracts an interval-average subject share from a point `cpuLoadPercent`. Requires `totalCpuSeconds` on both snapshots; subtracts commensurate cumulative deltas. Missing `totalCpuSeconds` (Sol's literal 96%/12.8s/4s/4cpu repro) falls back to RAW. Snapshot now sums process `TotalProcessorTime` into `totalCpuSeconds` (per-process access failures skipped).
  - **UNKNOWN/provisional gap at re-import consumers (Sol BLOCKER):** `Get-PlaybackAbLegHostLoadProvisional`, p3 `Import-EvidencePacket` clip check, and `summarize-local-cuda-proof` leg loop now refuse when `hostLoadState` is missing/blank/`unknown` even if `hostLoadProvisional=false` — closing the producer/consumer inconsistency round 4 fixed only at producers.
  - **Blind-interval bound real (Sol MAJOR):** `Wait-GuiSmokeProcessBounded` uses wall-clock `Stopwatch` (counts OnSample time). Receipt adds `observedMaxSampleGapMs`; `Get-HostLoadVerdict -ObservedMaxSampleGapMs` forces unknown when observed gap > 1.5× declared cadence.
  - **New-ProfileRow / New-FieldLogRow + census (Sol MAJOR):** both emit `host_load_provisional` (playback rows `$true` / unrecorded; export field-log `$null`). Executable regex census fails if any `New-*Row` in `compare-machine-perf.ps1` ships real `presented_fps` without `host_load_provisional` (allowlist: `New-RemoteCdngSummaryRow`).
  - **Whole-file ParseFile sweep (fable / hub-addendum):** Parser.ParseFile over every `.ps1`/`.psm1` this card touches (deferred in round 4, now present).
  - **OnSample failure sanitize:** exception type name only in `processBoundary.failures` (same class as round-4 hostLoad error sanitize).
- New/expanded tests cover: wait-bound despite slow OnSample, OnSample sanitize, observed-gap unknown/quiet/compat, missing-totalCpuSeconds → RAW 96, matched-window arithmetic (25%), provisional=false+unknown/missing-state refuse at compare/p3/summarizer, New-*Row census + detector self-proof, profile/field-log host_load_provisional emission, ParseFile sweep. Author notes pre-round-5 stash repro of wait-bound test (3377ms vs 2400ms threshold).
- Scope unchanged: gates fps comparisons/acceptance on host load; exceeded/unknown ⇒ PROVISIONAL. Default bar 75%; interior sampling default 4000ms.
- PR discloses unrelated `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap). Track separately.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged from prior tip; prior soft Improvements (cumulative top-consumer ranking, human glance) still open — **chat quiet** per later-push rule.
