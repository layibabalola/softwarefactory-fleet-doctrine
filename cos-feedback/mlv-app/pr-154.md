schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: 33273d8b275103a5df4eb1155621897359b0b5cd
reviewed_utc: 2026-09-23T16:38:11Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 33273d8b (+1292/−9, 8 files, 4 commits vs master): Protected Check Route SUCCESS; CodeQL completed neutral; other tip checks IN_PROGRESS — Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python, Windows GUI Pilot, Windows Product Oracles (Actions runs 35889973121 / 35889973205 / 35889966818); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 33273d8b, especially Repo Hygiene Python windows/ubuntu which exercise `test_playback_host_load_gate.py` and the new process-boundary sampling contract test.)

## Improvements

- Soft: `Get-HostLoadSnapshot` still ranks top consumers via `Get-Process | Sort-Object CPU` (cumulative CPU time since process start), not instantaneous load. The bar itself correctly uses `Win32_Processor.LoadPercentage`. Name-only top list is fine as evidence context; if it is ever used to decide who to kill or pause, switch to a per-interval delta (e.g. two samples) so interactive apps that have been up for hours do not dominate.
- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at mid-leg sampling wiring in `gui-smoke-process-boundary.psm1` / `run-release-gui-smoke.ps1` and the p3 `speedValidated` provisional fold remains cheap.
- Soft (carry): prior mid-leg sampling Improvement is **addressed** in this tip (see Notes). Leaving the cumulative-CPU ranking and human-glance items open.

## Notes

- Round 3 (`ce74df4050c2`) + follow-up (`33273d8b` vs prior CoS head `2a1cc341`): +743/−26 across 8 files (2 commits). Addresses the prior CoS soft Improvement on mid-leg spikes: `Wait-GuiSmokeProcessBounded` gains optional `-SampleIntervalMs`/`-OnSample` (default 0 preserves prior single blocking wait); `run-release-gui-smoke.ps1` samples during the leg (default 4000ms), records `hostLoad.during` + `duringSampleIntervalMs`, and `Get-HostLoadVerdict` takes peak across before/during/after (also unknown if any sample uncollected). Fail-toward-provisional on schema drift: present-but-missing `provisional`/`state` reads provisional/unknown in compare GUI A/B, CUDA A/B `Get-SmokeSummaryHostLoadFields`, and p3 validation. Refusal bound everywhere: `New-PlaybackAbAnalysis` sets `dominantBottleneck=host-load-provisional` when proof failures match; `compare-machine-perf.ps1` refuses cached analysis and surfaces `host_load_provisional` on summary rows. Follow-up folds Sol's p3-speed-validation finding: `speedValidated` is false when any clip has `hostLoadProvisional` (marks, never fails the run by host load alone). New tests cover mid-leg burst vs bracketing, uncollected interior → unknown, sampling contract on the wait path, degenerate hostLoad blocks, analysis refusal, machine-perf refusal, and p3 field parsing / speedValidated fold. Process-boundary live-wait sampling test is Windows/`pwsh`-gated.
- Scope unchanged: gates fps *comparisons*/acceptance signals on host load for bachelor-as-interactive-workstation. Exceeded or unknown ⇒ PROVISIONAL (never fail the smoke for load alone). Missing or degenerate `hostLoad` reads UNKNOWN/provisional, never quiet.
- Default bar `-HostLoadCpuPercentBar 75`; interior sampling default `-HostLoadSampleIntervalMs 4000` (0 disables). Top consumers still name-only.
- PR discloses unrelated `tools/repo_hygiene/test_brokered_closeout.py` timeouts under concurrent host load, filed separately as `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap). Track separately; do not block this PR on it.
- Pairs conceptually with clip-length validity (`PLAYBACK-CHECK-CLIP-LENGTH-1`); both are preconditions for fps claims on this board.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior CoS Improvement on mid-leg sampling clearly addressed; cumulative-CPU ranking and human-glance remain open. Verdict unchanged.
