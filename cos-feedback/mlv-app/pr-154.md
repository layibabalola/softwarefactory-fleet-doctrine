schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: 9481e01d0c8d89496c06202dbbbaeddd3808a0fc
reviewed_utc: 2026-09-23T17:52:49Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 9481e01d (+1852/−15, 9 files, 5 commits vs master): Protected Check Route SUCCESS; other tip checks IN_PROGRESS — Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python, Windows GUI Pilot, Windows Product Oracles (Actions runs 35898483812 / 35898483906 / 35898478344); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 9481e01d, especially Repo Hygiene Python windows/ubuntu which exercise `test_playback_host_load_gate.py` including the new sampling-coverage and unknown/provisional invariant tests.)

## Improvements

- Soft: `Get-HostLoadSnapshot` still ranks top consumers via `Get-Process | Sort-Object CPU` (cumulative CPU time since process start), not instantaneous load. Round 4's `nonSubjectCpuLoadPercent` now judges the *bar* on subject-excluded load; the name-only top list remains cumulative evidence context. If that list is ever used to decide who to kill or pause, switch it to a per-interval delta.
- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at sampling-coverage wiring in `Get-HostLoadVerdict` (`-SampleIntervalMs`) and the duplicated unknown→provisional invariant across compare/CUDA/p3 readers remains cheap.
- Soft (carry): prior mid-leg sampling Improvement remains **addressed** (round 3). Round 4 adds a stronger coverage rule on top (see Notes).

## Notes

- Round 4 (`9481e01d` vs prior CoS head `33273d8b`): +583/−29 across 6 files (1 commit). Addresses Sol round-4 findings on the same card:
  - **Sampling-coverage guarantee (Sol BLOCKER):** `Get-HostLoadVerdict` takes optional `-SampleIntervalMs`. When the caller declares a cadence, quiet is allowed only if sampling was enabled (`>0`) *and* produced ≥1 interior sample; disabled (`0`) or blind (enabled but zero during samples) forces unknown/provisional. Residual inter-tick bursts remain disclosed via `duringSampleIntervalMs`, not silently assumed away. Real call site passes `$HostLoadSampleIntervalMs`.
  - **Unknown/provisional invariant (Sol major):** compare GUI A/B, CUDA A/B `Get-SmokeSummaryHostLoadFields`, and p3 `Get-SmokeSummaryHostLoadFields` now force `provisional=true` whenever `state=unknown`, closing the inconsistent `provisional=false` + missing/blank state → clean-reading UNKNOWN hole.
  - **Remaining fps consumers bound (Sol major):** CUDA `-RequireCandidateImprovesPresentedFps` refuses the fps comparison itself when either leg is provisional (`candidate-presented-fps-improvement-not-evaluated`); p3 speed-leg floor breach becomes a warning (not `clipFailures`) when provisional, with `$hostLoadFields` computed before the floor check; packet import independently rejects provisional/unrecorded `hostLoadProvisional` per clip; `summarize-local-cuda-proof.ps1` independently checks baseline/candidate `hostLoadProvisional` before quoting presented fps.
  - **Collection sanitize/bound (Sol/fable minor):** CIM queries use `-OperationTimeoutSec` (default 2); snapshot `error` records exception *type* only, never raw message; full-serialized privacy test added.
  - **Subject exclusion (fable minor):** optional `-SubjectProcessId` + `Get-HostLoadNonSubjectCpuLoadPercent` (fail-closed to raw load); interior samples attach `nonSubjectCpuLoadPercent`; verdict judges the bar on that when present so the subject's own decode work cannot alone push a quiet host past the bar.
- New/expanded tests cover: sampling disabled/blind/enabled quiet/burst, omit-SampleIntervalMs backward compat, subject-heavy quiet vs busy-host provisional, non-subject percent fail-closed cases, sanitized error shape, full JSON privacy, provisional=false+missing-state refuse across compare/CUDA/p3 fields, p3 floor-order + warning branch, import independent host-load check, CUDA improvement guard, summarizer host-load guard. Real Windows snapshot test now accepts collected=true *or* false (restricted hosts).
- Scope unchanged: gates fps comparisons/acceptance on host load; exceeded/unknown ⇒ PROVISIONAL (marks, never fails the smoke for load alone). Default bar 75%; interior sampling default 4000ms.
- PR discloses unrelated `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap). Track separately.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged from prior tip; prior soft Improvements (cumulative top-consumer ranking, human glance) still open.
