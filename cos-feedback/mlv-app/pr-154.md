schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: d3859b854c5ff71a9247f38a827e2db6f06fdc04
reviewed_utc: 2026-09-24T21:08:42Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip d3859b85 (+3420/−16, 11 files, 13 commits vs master): Protected Check Route SUCCESS; remaining tip checks IN_PROGRESS — Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows Product Oracles, Windows GUI Pilot, Analyze actions/c-cpp/python (Actions runs 36059494601 / 36059494567 / 36059491451); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on d3859b85, especially Repo Hygiene Python windows/ubuntu which exercise the expanded `test_playback_host_load_gate.py` suite and the new process-boundary overrun-stop / stdout-drain sanitize tests in `test_process_hygiene_contract.py`.)

## Improvements

- Soft: `Get-HostLoadSnapshot` still ranks top consumers via `Get-Process | Sort-Object CPU` (cumulative CPU time since process start), not instantaneous load. Round 4–6 correctly judge the bar via matched-window / matched-PID non-subject load; the name-only top list remains cumulative evidence context. If that list is ever used to decide who to kill or pause, switch it to a per-interval delta.
- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at matched-by-PID subtraction, `unreadableProcessCount` refuse-to-quiet wiring, OnSample overrun stop, and the ultra-magnus / local-gpu unrecorded marks remains cheap.
- Soft (carry): prior mid-leg sampling / round-4–5 sampling-coverage Improvements remain **addressed**. Round 6 closes Sol/astra findings on the same card (see Notes); prior CoS soft items above still open.

## Notes

- Round 6 (`d3859b85` vs prior CoS head `6d60d7ce`): +938/−46 across 7 files (6 commits). Addresses Sol/astra round-6 findings:
  - **P3 production crash:** `run-ultramagnus-p3-validation.ps1` bare `(if ...)` argument → `$(if ...)` so PowerShell no longer executes a command named `if`.
  - **Partial collection refuses quiet (Sol BLOCKER / astra MAJOR):** snapshot records `unreadableProcessCount` + `processCpuSecondsById`; nonzero/absent unreadable on either bracket falls back to RAW; complete brackets use matched-by-PID deltas (exited PID cannot cancel a survivor). End-to-end verdict tests pin astra/sol exact 96% undercount repros as `exceeded`/`PROVISIONAL`, not quiet.
  - **Collection bounds (Sol/astra MAJOR):** `-ProcessEnumerationBudgetMs` (default 1500) caps the per-process TotalProcessorTime loop; overrun OnSample (>3× cadence) surfaces in failures and stops further scheduling (true non-returning OnSample preemption still disclosed residual). Spacing tolerance pinned at 1.5× with a 6400ms/4000ms (1.6×) test that fails if loosened to 1.7×.
  - **Consumer sweep omission (astra MAJOR):** `run-ultra-magnus-profile.ps1` and `run-local-gpu-capability.ps1` fps rows now emit `host_load_provisional=$true` (missing-json → `$null`); structural tests + ParseFile FILES 7→9.
  - **Mutation-sensitive enforcement:** P3-import / summarizer guards extracted via `_extract_block` and executed; census hardened against comment-only / literal-`$false` binding; ObservedMaxSampleGapMs call-site wiring pinned.
  - **Stdout/stderr drain sanitize (Sol MINOR):** `Get-GuiSmokeTaskText` records exception type only (same class as round-5 OnSample sanitize).
- Author notes full module 101 passed / 2 skipped; profiling pytest 39; coordination 18. Scope unchanged: exceeded/unknown ⇒ PROVISIONAL; default bar 75%; interior sampling default 4000ms.
- PR discloses unrelated `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap). Track separately.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged from prior tip; prior soft Improvements (cumulative top-consumer ranking, human glance) still open — **chat quiet** per later-push rule.
