schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 154
head: f7ace37c451a697cbf047f8c4c3a67819e266121
reviewed_utc: 2026-09-25T03:19:09Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip f7ace37c (+4797/−20, 11 files, 24 commits vs master): all tip checks IN_PROGRESS — Protected Check Route, Batch Compile, Repo Hygiene Python windows/ubuntu (Actions run 36089807613); Factory Bridge Regressions (36089807650); Analyze actions/python/c-cpp (36089804516). Windows Product Oracles / Windows GUI Pilot not yet listed at review time. Prior tip 36d4804e Tests run 36088544080 concluded cancelled with Repo Hygiene Python (ubuntu-latest) FAILURE (windows-latest + Product Oracles cancelled); this tip targets that red. mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on f7ace37c, especially Repo Hygiene Python ubuntu which failed on 36d4804e when before-bracket evidence threw before the syscall.)

## Improvements

- Soft: **0 GitHub reviews**. Diff is profiling/hygiene only and self-tests the gate; a short human glance at the before-bracket hold-evidence-failure-then-capture-then-rethrow path in `Get-HostLoadSnapshot` remains cheap.
- Soft (carry): prior CoS soft items on cumulative top-consumer decision input (round 7) and mid-leg sampling / round-4–10 Improvements remain addressed; no new soft carry from this tip beyond the review glance above.

## Notes

- Hub follow-up (`f7ace37c` vs prior CoS head `36d4804e`): +26/−1 across 2 files (1 commit). Addresses hosted-ubuntu red + fable HARDENING on the re-key after round-10 evidence-first BEFORE bracket:
  - **`Get-HostLoadSnapshot` before path:** evidence-first branch now `try { . $collectEvidence } catch { $evidenceFailure = $_ }`, then `. $captureSystemTimes`, then rethrow if held — so a throwing `Get-CimInstance` (always on non-Windows pwsh where the cmdlet does not exist) no longer aborts before syscall/subject capture. Outer catch records the same unknown/collected=false state as when syscall ran first; `subjectCpuSeconds` stays the definitional 0.0 on `-SubjectNotYetStarted`.
  - **Tests:** new `test_before_bracket_evidence_failure_does_not_lose_the_timing_capture` (synthetic throwing CIM; asserts SUBJECT=0, COLLECTED=False, and on nt SYSTEM_TIMES_COLLECTED=True). Prior tip failed `test_subject_not_yet_started_records_zero_not_unreadable` on ubuntu when evidence threw before capture.
- Scope unchanged: exceeded/unknown ⇒ PROVISIONAL; default bar 75%. PR discloses unrelated `CLOSEOUT-TEST-SUBPROCESS-TIMING-UNDER-LOAD-1` (zero file overlap).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged; prior soft Improvements not newly addressed this push — **chat quiet** per later-push rule (CI picture refreshed on bus).
