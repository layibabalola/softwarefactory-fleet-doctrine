schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 119
head: 064c8cedc97c3043a69b0d7196cbad1e35d55f30
reviewed_utc: 2026-09-15T18:03:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Hosted checks in progress after tip (CodeQL Analyze actions/c-cpp/python; Factory Bridge Regressions; Tests pending; CodeQL workflow completed neutral). No content blockers.

## Blockers

(none)

## Improvements

- `tests/console/test_playback_frame_range.cpp`: add a mid-clip collapse play-path case (e.g. `normalizeCutRange(30, 30, 60, true)` expects `30..60`) so the widen-to-end-of-clip contract is pinned away from the `cutIn==1` fixture.

## Notes

- Diff: `platform/qt/PlaybackFrameRange.h` (+13/−1) adds opt-in `repairCollapsedRangeForPlay` (default false); `MainWindow::normalizePlaybackCutRangeForLoadedClip` passes `true`; three other `normalizeCutRange` call sites unchanged. Tests add default-stays-collapsed, play-path repair `1,1→1,60`, and last-frame `60,60` left alone (+35).
- Intentional play-path behavior: pressing Play on a deliberate single-frame trim widens out to clip end; receipt/clip-load paths keep the trim. Red/green claimed on `PlaybackFrameRange.*` filter only; full `console_tests` skipped for known `PLAY-CLIPGOLDEN-PREEXISTING-1` ClipGolden noise; no interactive GUI smoke claimed.
- Head `064c8ced` is merge-master into `product/CLIPGOLDEN-CUTRANGE-COLLAPSE-1` after WIP fix tip `8dcf3193`. MERGEABLE once required hosted checks green.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.
