schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 123
head: c592af473c33478cb6ca2eb352ef49987f10ae22
reviewed_utc: 2026-09-16T08:54:23Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: All required/visible checks still pending on head c592af47 (CodeQL Analyze actions/c-cpp/python; Tests Protected Check Route / Batch Compile / Repo Hygiene win+ubuntu; Factory Bridge Regressions — runs 35076267100, 35076271386, 35076271327)

## Blockers

(none)

## Improvements

- **Post-merge board-local sync required** — dispatcher reads `.claude-state/coordination/dual-lane/prompts/v2/product-card-TEMPLATE.md` (board-local; not in the GitHub tree). Merging `docs/lane-prompts/v2/product-card-TEMPLATE.md` alone leaves production composition on the old template. After merge, copy the R2 section into that live file (CRLF-aware) before re-enabling `MLV-WorkstreamLoop`.
- Booked-separately defect stays open: ratified `PR_STEP` still uses multi-line `--body "..."`, which truncates to the first line. Do not paper over it with a prose `--body-file` rule in this template (would contradict the COMPOSER CONTRACT literal). Fix needs contract ratification, not a CoS-only note.

## Notes

- R2 push (`c592af47`) rewrites the new "Shell rules and known environment" section after same-family adversarial BLOCK on R1 (`d840ca66`). Scope remains +15 / −0 on `docs/lane-prompts/v2/product-card-TEMPLATE.md` only (vs master ahead 2 / behind 0).
- Removed (correctly, per commit evidence): ACCEPTANCE-only test rule (collided with existing Build-and-test block); STOP on nonexistent `ALLOWED_PATHS` entries (would STOP every currently dispatchable card; 6/13 tracked fields name absent paths / globs / prose).
- Corrected: Qt silent-exit vs `libgomp-1.dll` "not found"; remedy is PATH + `QT_PLUGIN_PATH` / `QT_QPA_PLATFORM_PLUGIN_PATH` as in `.github/workflows/tests.yml`, not "copy dll beside exe" for the CI case. Redirection rule now allows real redirects and warns that the command guard parses whole command text including data (write long text via Write tool + `-F` / file path).
- Still deliberately absent: any `gh` / `--body-file` instruction in the new section (avoids contradicting ratified `PR_STEP`).
- MERGEABLE; mergeStateStatus BLOCKED on pending required checks (strict status checks). Template is **not** on `CONTROL_HASHES_BASE` / operating-contract surfaces — Sol HIGH not process-required. Cross-family still stamped UNAVAILABLE (Codex usage limit; retry ~2026-09-21).
- PR claims COMPOSER CONTRACT byte-identical; 19 composer tests pass; no new placeholders / column-0 field labels. CoS did not re-run those tests.
- One of two blockers to re-enabling `MLV-WorkstreamLoop` per PR body; board-local sync above remains the operational half after this lands.
- CoS review via `gh`. No GitHub PR comment posted. Doctrine bus: refreshed `cos-feedback/mlv-app/pr-123.md` for R2 head.
