schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 123
head: d840ca668d117e365921490eda11a80bc05d3840
reviewed_utc: 2026-09-16T08:37:14Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route PASS; CodeQL Analyze (actions)/(python) PASS; Batch Compile / Windows GUI Pilot / Windows Product Oracles / Repo Hygiene (win+ubuntu) / Factory Bridge / Analyze (c-cpp) still pending on head d840ca66 (runs 35074650459, 35074650315, 35074647992)

## Blockers

(none)

## Improvements

- **Post-merge board-local sync required** — dispatcher reads `.claude-state/coordination/dual-lane/prompts/v2/product-card-TEMPLATE.md` (board-local; not in the GitHub tree). Merging `docs/lane-prompts/v2/product-card-TEMPLATE.md` alone leaves production composition on the old template. After merge, copy the new section into that live file (CRLF-aware) before re-enabling `MLV-WorkstreamLoop`.
- Booked-separately defect stays open: ratified `PR_STEP` still uses multi-line `--body "..."`, which truncates to the first line. Do not paper over it with a prose `--body-file` rule in this template (would contradict the COMPOSER CONTRACT literal). Fix needs contract ratification, not a CoS-only note.

## Notes

- Scope: +18 / −0 on `docs/lane-prompts/v2/product-card-TEMPLATE.md` only. Adds "Shell rules and known environment" before STOP conditions (foreground builds/tests, ACCEPTANCE-only tests, no redirection/tee/heredoc/Out-File, prefer Read/Grep, Qt plugins + `libgomp-1.dll` + `tests.yml` recipe, STOP on packet self-contradiction).
- Tip is ahead 1 / behind 0 vs `master` (`d840ca66`). MERGEABLE; mergeStateStatus BLOCKED on pending required checks (strict status checks).
- PR claims COMPOSER CONTRACT comment left byte-identical; 19 composer/template tests pass; composition resolves `{{BASE_SHA}}` with zero unresolved placeholders. CoS did not re-run those tests.
- Template is **not** on `CONTROL_HASHES_BASE` / operating-contract surfaces — Sol HIGH not process-required for this path (unlike #122). Cross-family stamped UNAVAILABLE (Codex usage limit; retry ~2026-09-21). Same-family adversarial optional when key returns.
- One of two blockers to re-enabling `MLV-WorkstreamLoop` per PR body; the board-local sync above is the operational half after this lands.
- CoS review via `gh`. No GitHub PR comment posted. Doctrine bus: created `cos-feedback/mlv-app/pr-123.md`.