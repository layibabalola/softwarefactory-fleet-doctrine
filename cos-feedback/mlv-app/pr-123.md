schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 123
head: b5980f95d3868e482e606b54aba7c68776cebdb4
reviewed_utc: 2026-09-16T09:04:05Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Protected Check Route PASS; CodeQL Analyze (actions) + (python) PASS; Analyze (c-cpp), Factory Bridge, Batch Compile, Repo Hygiene win+ubuntu, Windows Product Oracles, Windows GUI Pilot still pending on head b5980f95 (runs 35077155020, 35077159397, 35077159443)

## Blockers

(none)

## Improvements

- **Post-merge board-local sync required** — dispatcher reads `.claude-state/coordination/dual-lane/prompts/v2/product-card-TEMPLATE.md` (board-local; not in the GitHub tree). Merging `docs/lane-prompts/v2/product-card-TEMPLATE.md` alone leaves production composition on the old template. After merge, copy the R3 section into that live file (CRLF-aware) before re-enabling `MLV-WorkstreamLoop`.
- Booked-separately defect stays open: ratified `PR_STEP` still uses multi-line `--body "..."`, which truncates to the first line. Do not paper over it with a prose `--body-file` rule in this template (would contradict the COMPOSER CONTRACT literal). Fix needs contract ratification, not a CoS-only note.

## Notes

- R3 push (`b5980f95`) after same-family CHANGES_REQUESTED on R2 (`c592af47`). Scope remains single-file: `docs/lane-prompts/v2/product-card-TEMPLATE.md` only (vs master ahead 3 / behind 0; net +10 lines).
- Removed (not reworded): the whole command-guard bullet. R2 claimed `>` to the run directory was allowed; RUNDIR sits under `.claude-state/fleet-runs` (protected carve-out), so a truncating redirect there is refused NA-2, and the template already steers lanes to seal receipts there. "Do not embed long text in a shell command" also collided with ratified `PR_STEP` (inline `--body`). Two rounds of guard prose were wrong in detail; guard behaviour belongs where a test can pin it to the hook, not in this template.
- Environment bullet now **points at** `.github/workflows/tests.yml` as oracle (console :248, pipeline :264, GUI env :614-636: `QT_QPA_PLATFORM`, `QT_OPENGL`, `QT_QPA_PLATFORM_PLUGIN_PATH`, `QT_PLUGIN_PATH` plus PATH) instead of restating a partial variable list. CoS spot-checked those line anchors on this head.
- Kept: foreground builds/tests (no Monitor / end-turn-on-notification); prefer Read and Grep over shell `grep`/`cat`. Still deliberately absent: any `gh` / `--body-file` instruction.
- MERGEABLE; mergeStateStatus BLOCKED on pending required checks. Template is **not** on `CONTROL_HASHES_BASE` / operating-contract surfaces — Sol HIGH not process-required. Cross-family still stamped UNAVAILABLE (Codex usage limit; retry ~2026-09-21). No GitHub PR review comments present.
- PR claims COMPOSER CONTRACT byte-identical and 19 composer tests pass; CoS did not re-run those tests.
- One of two blockers to re-enabling `MLV-WorkstreamLoop` per PR body; board-local sync above remains the operational half after this lands.
- CoS review via `gh`. No GitHub PR comment posted. Doctrine bus: refreshed `cos-feedback/mlv-app/pr-123.md` for R3 head.