schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 163
head: 7f02435c6e8f18b06b896e3e7981cd44d27d8c08
reviewed_utc: 2026-09-25T14:09:33Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 7f02435c (+1408/−35, 6 files, 7 commits; ahead 7 / behind 0): Protected Check Route SUCCESS; CodeQL Analyze (actions/c-cpp/python) + Factory Bridge Regressions + Tests (Batch Compile / Repo Hygiene ubuntu+windows / Windows GUI Pilot / Windows Product Oracles) pending/in progress (Tests run 36145447503, CodeQL 36145444235, Factory Bridge 36145447399). MERGEABLE; mergeStateStatus BLOCKED (protection waiting on required checks). 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- Soft: when both bracket endpoints admit the same `presentedCount`, headline stays on `earliest` even if `displayedCount`/`displayedFps` would differ. A secondary key on displayed count (or on `rowsDifferingInWindowMembership` when non-zero) would make the "more inclusive" rule cover the display-rate path as well as the present path. Not blocking — ties are already deterministic and both ends are reported in `clockBracket`.

## Notes

- Supersedes open #160 (HARNESS-2), which superseded open #158 (HARNESS-1). Tip is HARNESS-1→2→3 plus two master merges (`e94ab927`, `1d8687d4`). Prefer closing or parking #158 and #160 once this lands so the HARNESS stack is not triple-open. (Separate from the IDENTITY GPU-window telemetry stack #159/#161/#162.)
- Addresses #160 contract BLOCKER (sol) and the two hardenings called out in the PR body:
  - **Real intervals only:** job filters `$pmIntervalRows` to positive `msBetweenDisplayChange` before `Get-Stats`; `manifest.presentMon.positiveSamples` counts that same population. NA/`MsUntilDisplayed`-only first present stays displayed but no longer coerces to a 0 ms sample. Pinned by `IntervalStatsFilterFixtureTests` (executes the job template's own filter + Get-Stats source).
  - **Dual-endpoint clock window:** `Get-AttrCudaPresentMonDisplayReport` takes `-EarliestCaptureStartUtc`/`-LatestCaptureStartUtc`, windows under both, reports per-end counts/rates + `rowsDifferingInWindowMembership`, headlines the more inclusive presented set (ties → earliest). Corrects the inverted HARNESS-2 direction comment. Pinned by `BracketWindowingFixtureTests`.
  - **Wait-failure evidence parity:** PresentMon wait catch now publishes `presentmon.csv` (if present) + `presentmon-capture.json` before typed exit 23, matching the parse-failure path.
- Required PresentMon columns match the real 2.5.1 28-column header (`MsUntilDisplayed`, no fictional `DisplayedTime`); MLVApp preview is PID-summed across swap chains; smoke artifacts publish before PresentMon wait/parse (HARNESS-1/2 carry-forward).
- Author local evidence: PLAYBACK-ATTR-3-CUDA suites + tools/profiling 351 passed + 288 subtests (hub); four PresentMon-related suites cited as 308/288 in the tip commit. Contract review deferred until hosted checks are green (contract v2.1). CoS did not re-run (Windows factory; review via gh only).
- Scoped-out follow-ups (author): low-sample flag; live-process test for `SMOKE_LOG_UNAVAILABLE` PresentMon stop; tighter post-spawn stamp; empirical anchor validation.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound; only tip required checks / branch protection blocking. Repo Hygiene (esp. windows PresentMon unit suites) is the first signal for this harness; GUI Pilot / Product Oracles are the usual long poles.