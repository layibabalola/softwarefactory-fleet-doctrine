schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 156
head: 7bfa31b3ab53c1e7b5a04eeff64b886d8a049e70
reviewed_utc: 2026-09-25T06:35:47Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 7bfa31b3 (hub: prove pre-dual-ISO fix pass with completed flag; +4 files / +12/−1 vs prior CoS 0e5323a5; PR vs master now +40/−9, 7 files, 4 commits): tip CI just queued — Tests 36103511287 (Protected Check Route, Batch Compile, Repo Hygiene windows/ubuntu IN_PROGRESS), Factory Bridge 36103511283 IN_PROGRESS, PR Analyze 36103509948 (actions/c-cpp/python IN_PROGRESS); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master f820d5a9; not draft. Windows Product Oracles not yet in tip rollup (likely post–Batch Compile matrix).

## Blockers

(none in the reviewed tip — wait for required checks on 7bfa31b3.)

## Improvements

- Soft residual (unchanged): confirm hosted **Windows Product Oracles** goes green on this tip (the flake this PR targets reddened that check on #133 / #154). If the matrix job is not yet queued when Checks settle, re-check the Tests run for `Product Oracles` / windows job names before merge.
- Soft residual (unchanged): optional follow-up — document in factory/test doctrine that `Milliseconds() > 0` / `elapsed_ms > 0` is never valid run-proof for OpenMP-timed stages (duration-as-proof-1/2 pattern, now covering SH blur *and* pre-dual-ISO fix), so future playback-preview asserts do not reintroduce the same trap.
- Soft residual (unchanged): **0 GitHub reviews**; delta grew (+40/−9, 7 files) — a 60-second human glance on the dual-ISO flag wiring plus the three SH test asserts is cheap before merge.

## Notes

- Round vs prior CoS head 0e5323a5: sole new commit `7bfa31b3` is `PROD-TELEMETRY-DURATION-AS-PROOF-2 hub: prove the pre-dual-ISO fix pass ran with a completed flag, not elapsed ms`. Real product delta (not a merge-only tip).
- New product scope this tip: `llrawproc_object.h` / `llrawproc.c` / `video_mlv.c` add `playback_pre_dualiso_fix_completed` (init 0, set 1 at stop_before_dual_iso return, reset with the ms sibling on render entry); `test_dual_iso_pipeline.cpp` Phase4B defaults-on test asserts the completed flag (+ keeps `ms >= 0.0`). Prior SH fullres-completed flag + five ProcessingFilters assert swaps unchanged.
- Pattern check: flag set only on the successful stop_before_dual_iso path; reset paired with `playback_pre_dualiso_fix_ms` at both reset sites in `mlv_render_scaled_rgb16_from_raw` — consistent with SH halfres/quarterres/fullres completed-flag design. No filter behaviour / threshold / non-test logic change claimed.
- Soft CoS Improvements from earlier passes not addressed by this tip (Windows Oracles confirmation still pending CI; doctrine note; human review). Lane/tests not re-run here; tip CI settling.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
