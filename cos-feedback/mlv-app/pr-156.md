schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 156
head: 58de41f148c92999839e88e72f91bbf33511c44f
reviewed_utc: 2026-09-25T05:51:34Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 58de41f1 just opened; tip CI IN_PROGRESS — Tests 36100280502 (Protected Check Route, Batch Compile, Repo Hygiene ubuntu/windows), Factory Bridge 36100280513 (Regressions), CodeQL/PR Analyze 36100278838 (actions/c-cpp/python); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master a6f2acba; not draft. Windows Product Oracles not yet in rollup (likely post–Batch Compile matrix).

## Blockers

(none in the reviewed tip — wait for required checks on 58de41f1.)

## Improvements

- Soft: confirm hosted **Windows Product Oracles** goes green on this tip (the flake this PR targets reddened that check on #133 / #154). If the matrix job is not yet queued when Checks settle, re-check the Tests run for `Product Oracles` / windows job names before merge.
- Soft: optional follow-up — document in factory/test doctrine that `Milliseconds() > 0` is never valid run-proof for OpenMP-timed SH stages (duration-as-proof-1/2 pattern), so future playback-preview asserts do not reintroduce the same trap.
- Soft: **0 GitHub reviews**; delta is small (+28/−8, 3 files) but a 30-second human glance on the three test asserts is cheap before merge.

## Notes

- Scope: `src/processing/raw_processing.{c,h}` add `g_processing_last_shadows_highlights_fullres_completed` (set at end of both fullres branches, reset with siblings, `*FullresCompletedForTesting()` accessor); `tests/pipeline/test_processing_filters.cpp` replaces five `Milliseconds() > 0.0` duration-as-proof asserts in three playback-preview SH tests with completed-flag / run-count telemetry.
- Surviving `== 0.0` duration asserts (same file + `test_dual_iso_pipeline.cpp`) prove a mutually exclusive branch did **not** run by construction (reset-then-only-taken-branch-writes); left unchanged — correct, not the flake class.
- PR claims lane+hub `ProcessingFilters.*` 324/0 and mutation table falsifying each new assert; CoS did not re-run the Windows hosted matrix here.
- Fixes cross-PR flake noise that previously reddened unrelated PRs (#133; #154 twice on 2026-09-25, cleared only by rerun).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
