schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 127
head: 84ad327b9dc6e6991f304a88fad64d8560b10ff8
reviewed_utc: 2026-09-16T14:29:25Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Protected Check Route + Analyze (actions, python) green; CodeQL aggregate skipping; Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu+windows), Windows Product Oracles, Windows GUI Pilot, Analyze (c-cpp) still pending. mergeable=MERGEABLE BLOCKED.

## Blockers

(none)

## Improvements

- `tools/coordination/Invoke-Workstream.ps1` (both launch sites ~1062 and ~1398): the `reserved` write passes `-LaneAllowEdits`, but the `finally` `charged`/`refunded` write still omits it, so terminal rows land `allowEdits: null` while the reservation row has a bool. Harmless to rate/coverage today (only `reserved` counts); pass `-LaneAllowEdits ([bool]$AllowEdits)` on the finally call for ledger consistency.
- After merge, every checkout still on pre-PR `Invoke-Lane.ps1` will produce unreserved receipts and keep coverage `PARTIAL` (`COVERAGE_RECEIPT_UNRESERVED`) until those venues upgrade — call that out in the merge/rollout note so operators do not treat early `PARTIAL` as a guard bug. Docs already state the unregistered-worktree limit; keep the same honesty in the merge note.

## Notes

- Round-2 push (`ae00d731` + master merge → head `84ad327b9dc6e6991f304a88fad64d8560b10ff8`) addresses Sol CHANGES_REQUESTED on the prior tip (`7e41d5b8`): (1) seven-day clock starts only at the first versioned row at or after the landed ledger writer on the source ref (unmerged-candidate rows cannot seed it); (2) receipt scan covers board `.claude-state/fleet-runs` plus every registered worktree; (3) a `linked` row is honoured only once, and only when it names an earlier `invoke-workstream` `reserved` row — otherwise it counts as a launch + `COVERAGE_LINK_UNMATCHED`; (4) mtime prefilter removed (judge by `startedUtc`); (5) `Invoke-Lane` default `RunDir` is the board tree; (6) ledger append uses `FileShare.ReadWrite` so concurrent `Add-Content` is not refused. New RED tests cover each undercount path.
- Scope unchanged: version-enforced all-venue dispatch accounting for plan 0.6 / TOOL-GUARD-COVERAGE-ARM-UNSATISFIABLE-1. No unfreeze shortcut; GREEN still does not thaw `frozen-factory-20260906`; execution-control chain remains uncertified (called out in docs). Rate numerator still counts all reserved launches (follow-up TOOL-GUARD-RATE-NUMERATOR-REVIEW-1).
- Prior CoS Improvements (allowEdits on finally; rollout note) still open — not addressed in this push.
- Review via `gh` diff + checks (no Cloud Agent). No GitHub review comment posted by CoS.
