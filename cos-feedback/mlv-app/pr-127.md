schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 127
head: 7e41d5b87ffb8a070dc3400c19e48e1eb51f634d
reviewed_utc: 2026-09-16T13:31:27Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route + CodeQL Analyze (actions, python) green; Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu+windows), Windows Product Oracles, Windows GUI Pilot, Analyze (c-cpp) still pending; CodeQL aggregate skipping. mergeable=MERGEABLE BLOCKED. Local author claim: 320 coordination + 270 repo_hygiene passed on Windows.

## Blockers

(none)

## Improvements

- `tools/coordination/Invoke-Workstream.ps1`: the `reserved` write passes `-LaneAllowEdits`, but the `finally` `charged`/`refunded` write omits it, so terminal rows land `allowEdits: null` while the reservation row has a bool. Harmless to rate/coverage today (only `reserved` counts); align the finally call for ledger consistency if readers ever key on `allowEdits`.
- After merge, every checkout still on pre-PR `Invoke-Lane.ps1` will produce unreserved receipts and keep coverage `PARTIAL` (`COVERAGE_RECEIPT_UNRESERVED`) until those venues upgrade — call that out in the merge/rollout note so operators do not treat early `PARTIAL` as a guard bug.

## Notes

- Scope: version-enforced all-venue dispatch accounting for plan 0.6 / TOOL-GUARD-COVERAGE-ARM-UNSATISFIABLE-1. Six files (+527/−24): `Invoke-Lane.ps1` (schemaVersion 2 ledger row after receipt slot, before provider; `reserved` vs `linked` via `MLV_DISPATCH_RESERVATION_ID`; refuse on write failure), `Invoke-Workstream.ps1` (versioned rows + env handoff at both launch sites), `Test-ProductRatioGuard.ps1` (COMPLETE only when window/enforcement/receipt bidirectional checks pass; specific `COVERAGE_*` reasons), docs, and tests (8 real-reader coverage cases + 3 containment ledger cases).
- Design matches the PR body: no unfreeze shortcut; GREEN still does not thaw `frozen-factory-20260906`; COMPLETE cannot land for ≥7 days after first versioned row; rate numerator still counts all reserved launches (follow-up booked as TOOL-GUARD-RATE-NUMERATOR-REVIEW-1).
- Live board reading cited on branch: share 4/35, observed 17, rate 4.25, `COVERAGE_NOT_ENFORCED` — expected until this code ships and the window fills.
- Review via `gh` diff + checks (no Cloud Agent). No GitHub review comment posted by CoS.