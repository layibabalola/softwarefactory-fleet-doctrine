schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 127
head: b6c78faccd17167a2cb610bc5cd0724089d2f7be
reviewed_utc: 2026-09-16T16:19:36Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Protected Check Route + Analyze (actions) PASS; CodeQL aggregate skipping; Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu+windows), Windows Product Oracles, Windows GUI Pilot, Analyze (c-cpp, python) still pending/in progress on tip. mergeable=MERGEABLE BLOCKED.

## Blockers

(none)

## Improvements

- `tools/coordination/Invoke-Workstream.ps1` (both launch sites ~1074 and ~1410): the `reserved` write passes `-LaneAllowEdits`, but the `finally` `charged`/`refunded` write still omits it, so terminal rows land `allowEdits: null` while the reservation row has a bool. Harmless to rate/coverage today (only `reserved` counts); pass `-LaneAllowEdits ([bool]$AllowEdits)` on the finally call for ledger consistency.
- After merge, every checkout still on pre-PR `Invoke-Lane.ps1` will produce unreserved receipts and keep coverage `PARTIAL` (`COVERAGE_RECEIPT_UNRESERVED`) until those venues upgrade — call that out in the merge/rollout note so operators do not treat early `PARTIAL` as a guard bug. Docs already state the unregistered-worktree limit; keep the same honesty in the merge note.

## Notes

- Tip `b6c78fac` (ahead 7 / behind 0 vs `master`) continues TOOL-GUARD-COVERAGE-ARM-UNSATISFIABLE-1 after prior CoS head `84ad327b`. Net vs master: +834/−28 across 7 files (coordination writers, guard, docs, tests).
- Round-2 Sol push (`55956b60`): machine-wide mutex `Global\MLV-App-DispatchLedger` serializes both `Add-DispatchLedgerRow` and `Write-DispatchReservation` (FileMode.Append EOF-per-stream overwrite under contention); coverage refuses `COMPLETE` while any registered checkout lacks the writer (`COVERAGE_STALE_RUNNER_PRESENT`) or a writer file was replaced inside the window (`COVERAGE_RUNNER_UPDATED_IN_WINDOW`). Contention test claimed: 4 processes × 40 rows intact.
- Round-3 Sol push (`b6c78fac`): stale-runner detection keys on token `MLV-DISPATCH-LEDGER-WRITER-V3-SERIALIZED` in **both** `Invoke-Lane.ps1` and `Invoke-Workstream.ps1` (not the function name alone — `84ad327b` already had `Add-DispatchLedgerRow` without the mutex). Enforcement landing bound to the same token. Regression fixtures include the `84ad327b` shape.
- `Invoke-WorkstreamLoop.ps1`: skips `venue: invoke-lane` ledger rows when computing daily dispatch budget so hub reviews/swarms do not spend the unattended loop's budget.
- Scope unchanged: version-enforced all-venue dispatch accounting for plan 0.6. No unfreeze shortcut; GREEN still does not thaw `frozen-factory-20260906`; execution-control chain remains uncertified. Rate numerator still counts all reserved launches (follow-up TOOL-GUARD-RATE-NUMERATOR-REVIEW-1).
- No GitHub formal reviews on tip; Sol CHANGES_REQUESTED rounds 1–3 addressed in commit messages / board-local review ids only. Prior CoS Improvements (allowEdits on finally; rollout note) still open.
- Review via `gh` diff + checks (no Cloud Agent). No GitHub review comment posted by CoS.