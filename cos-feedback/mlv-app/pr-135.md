schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 135
head: 5b9a5a8ca27b7fb83c19d68a26abbf689e6c42ac
reviewed_utc: 2026-09-17T07:25:05Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip is master-merge (ahead 3, behind 0); 8 checks IN_PROGRESS on 5b9a5a8c (Protected Check Route, Repo Hygiene ubuntu+windows, Factory Bridge Regressions, Batch Compile, Analyze actions/c-cpp/python); prior tip 28e16c52 all SUCCESS (11/11 including Windows GUI Pilot + Product Oracles + CodeQL); mergeable MERGEABLE; mergeStateStatus BLOCKED pending required checks; 0 reviews; +350/-7 across 3 files.

## Blockers

(none)

## Improvements

- `Invoke-UmRunDrop` re-hashes side-file `*.sidepart` copies from the share before rename, but the job `*.job.tmp` is copied and renamed without a share-side hash check. Mirror the side-file verify on the job temp (or document why the job path is trusted) so a faulty/observing Copier cannot drop a different script body.
- Optional: assert in `test_um_run_sidefiles.py` that Repo Hygiene windows-latest discovery still collects `UmRunDropModuleTests` / `UmRunEndToEndTests` (prior 28e16c52 windows Repo Hygiene was SUCCESS, so likely fine — keep an eye if the suite ever goes silent).

## Notes

- Head vs prior bus file: was 2609fc6e (r1); r2 `28e16c52` moved placement into `tools/profiling/UmRunDrop.psm1`, added normalised plain-name + allowlisted-extension + device-name + trailing-dot alias refusal, per-submission GUID temps, no-Force renames, concurrent same-bytes accept for side-files only, and observing/corrupting/racing tests. Tip `5b9a5a8c` is merge of master into `tooling/UM-RUN-SIDEFILES-1` (no further code delta beyond the merge).
- Prior CoS Improvements addressed: header/comment mismatch fixed (docs now point at UmRunDrop invariants); outbox `*.result.json` JobId collision covered by `test_an_existing_result_for_the_job_id_is_refused`.
- Sol r1 blocker (trailing-dot `evil.job.ps1.` alias) and majors (unique temps / non-overwrite / observable tests) look addressed in r2.
- Purpose unchanged: `-SideFile` / `-JobId` so generated host jobs (#133) can place verified inbox inputs without a hooked share write (NA-7). Tooling only; no footage.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).