schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 135
head: 2609fc6e427bc30b71a002b3ce4baee23e31d733
reviewed_utc: 2026-09-17T05:40:00Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route success; 9 checks still in_progress (Repo Hygiene ubuntu+windows, Factory Bridge Regressions, Batch Compile, Windows GUI Pilot, Windows Product Oracles, Analyze actions/c-cpp/python); mergeable true; mergeStateStatus BLOCKED pending required checks; 0 reviews; +160/-2 across 2 files.

## Blockers

(none)

## Improvements

- Header comment on `tools/profiling/um-run.ps1` says an existing same-name side-file is "replaced only if its hash already matches"; the code path `continue`s (idempotent accept, no rewrite). Align the comment with the skip-when-matching behavior.
- `tools/repo_hygiene/test_um_run_sidefiles.py` does not cover the outbox `*.result.json` JobId collision refuse path; add one fake-outbox case beside the pending-inbox test.
- Tests are correctly Windows-only (`os.name == "nt"` + `pwsh`); confirm Repo Hygiene windows-latest actually discovers/runs this module so the new coverage is not silently skipped on CI.

## Notes

- Adds `-SideFile` (`;`-separated for `pwsh -File`) and optional `-JobId` to the tracked submitter so generated host jobs (#133) can place verified inbox inputs without a hooked share write (NA-7).
- Placement order is sound: copy to `*.sidepart`, re-hash from the share, rename to final name, then drop `*.job.ps1`. Refuses different-content overwrite, job-named basenames, reparse points/dirs, pending same-id job, and reused result id.
- Tooling only; no footage. Branch `tooling/UM-RUN-SIDEFILES-1` → master. Unblocks #133 build/stage submit path.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
