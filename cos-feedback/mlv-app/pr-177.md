schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 177
head: 44f0d335f062c6038b1b6613bb3cc8a3494d53f0
reviewed_utc: 2026-09-26T05:02:22Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 44f0d335 (CUDA-PLAYBACK-CONTACT-SHEET-1; +2860/−13, 13 files, 13 commits; ahead 13 / behind 0). Protected Check Route SUCCESS; CodeQL Analyze (actions) SUCCESS; CodeQL app check NEUTRAL; Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, CodeQL Analyze (c-cpp/python), Windows GUI Pilot, Windows Product Oracles IN_PROGRESS (Tests 36219533151, Factory Bridge 36219533153, CodeQL 36219530539). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- After tip CI greens: run Ultra-Magnus and bachelor with `-ContactSheet` once each so the first real labelled sheets land (owner ask). Venue Python still needs Pillow+numpy for real compose; without them the job correctly falls through to `CONTACT_SHEET_COMPOSE_UNAVAILABLE`. Follow-up card already noted in tip commit: pin numpy+Pillow with hashes across hosted images, then drop the `test_make_contact_sheet.py` / colour-metrics ignores in `factory-bridge.yml`.
- `tools/repo_hygiene/test_playback_attr_3_cuda_split_route.py`: the artifact-index ↔ summary.json proximity window is now 10000 chars after three widenings for the compose step. Prefer asserting a unique compose marker (e.g. `CONTACT_SHEET_COMPOSE_UNAVAILABLE` / `--host`) sits between those two rather than growing a byte window again.

## Notes

- Push `44f0d335` (r1f): skip Windows-only job-generator tests on non-Windows runners. Prior tip `94f70d27` failed Repo Hygiene Python (ubuntu-latest) — `ContactSheetSwitchTests` / `ContactSheetDefaultOffPreCardCommitTests` executed the bachelor generator; `Join-Path` over a drive-letter AgentRoot raised `Cannot find drive C`. Fix: `@unittest.skipIf(os.name != "nt", ...)` on both classes, matching sibling ATTR-3 generator suites (Windows CI still covers them; local Windows 14/14). Diff vs prior CoS tip: +2/−0 in `tools/repo_hygiene/test_playback_attr_3_cuda_contact_sheet.py` only.
- Prior CoS Improvements (post-green `-ContactSheet` venue runs; compose-marker proximity assert) not addressed by this push — still open.
- Owner request: contact sheets of what CUDA playback actually presented while Look Assist looks improve on Ultra-Magnus and bachelor. Round-1 is the measured-path capture + labelled composer + opt-in bachelor `-ContactSheet` (default off; off path byte-identical).
- Hub/sol round-1 blockers remain addressed on this tip (r1b–r1e): un-timed second playback pass; disarm on seek; fail closed on grab/serial mismatch; quoted host/GPU/scale into composer; measured-session marker; relative sidecar paths; lazy `_write_frame` + factory-bridge ignore for numpy/Pillow.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green** unchanged: wait for hosted Suite before merge.
