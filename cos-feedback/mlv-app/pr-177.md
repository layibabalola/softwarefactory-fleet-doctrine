schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 177
head: 94f70d27c0186785914d8477e83b09b5c5576b30
reviewed_utc: 2026-09-26T04:28:01Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 94f70d27 (CUDA-PLAYBACK-CONTACT-SHEET-1; +2858/−13, 13 files, 12 commits; ahead 12 / behind 0). Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, CodeQL Analyze (actions/c-cpp/python), Windows GUI Pilot, Windows Product Oracles IN_PROGRESS (Tests 36217871121, Factory Bridge 36217871550, CodeQL 36217868417). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- After tip CI greens: run Ultra-Magnus and bachelor with `-ContactSheet` once each so the first real labelled sheets land (owner ask). Venue Python still needs Pillow+numpy for real compose; without them the job correctly falls through to `CONTACT_SHEET_COMPOSE_UNAVAILABLE`. Follow-up card already noted in tip commit: pin numpy+Pillow with hashes across hosted images, then drop the `test_make_contact_sheet.py` / colour-metrics ignores in `factory-bridge.yml`.
- `tools/repo_hygiene/test_playback_attr_3_cuda_split_route.py`: the artifact-index ↔ summary.json proximity window is now 10000 chars after three widenings for the compose step. Prefer asserting a unique compose marker (e.g. `CONTACT_SHEET_COMPOSE_UNAVAILABLE` / `--host`) sits between those two rather than growing a byte window again.

## Notes

- Push `94f70d27` (r1e): keep hosted CI green without numpy/Pillow. Prior tip failed Repo Hygiene (module-level import of `_write_frame` from `test_make_contact_sheet`) and Factory Bridge tools/profiling (`test_make_contact_sheet.py` numpy at import). Fix: lazy-import inside the one real-interpreter compose test (already skipped via `_HAS_REAL_PYTHON_DEPS`); add `--ignore=tools\\profiling\\test_make_contact_sheet.py` next to the existing colour-metrics ignore in `.github/workflows/factory-bridge.yml`.
- Owner request: contact sheets of what CUDA playback actually presented while Look Assist looks improve on Ultra-Magnus and bachelor. Round-1 is the measured-path capture + labelled composer + opt-in bachelor `-ContactSheet` (default off; off path byte-identical).
- Hub/sol round-1 blockers remain addressed on this tip (r1b–r1d): un-timed second playback pass; disarm on seek; fail closed on grab/serial mismatch; quoted host/GPU/scale into composer; measured-session marker; relative sidecar paths.
- Diff vs prior CoS tip: only the two CI-green files above (+6/−1). Full PR still touches `platform/qt/MainWindow.{h,cpp}`, `main.cpp`, console wiring test, composer + tests, bachelor job + AttrCudaArtifacts, repo_hygiene contact-sheet + behaviour/split-route.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green** unchanged: wait for hosted Suite before merge.
