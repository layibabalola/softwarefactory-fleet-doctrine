schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 177
head: 4e46a38efc3c947cdc79eb601ed8784ebab50e56
reviewed_utc: 2026-09-26T03:50:00Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 4e46a38e (CUDA-PLAYBACK-CONTACT-SHEET-1; +2853/−13, 12 files, 11 commits; ahead 11 / behind 0). Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS; Windows GUI Pilot + Windows Product Oracles QUEUED (Tests 36215957646, Factory Bridge 36215957657, CodeQL 36215956759). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- After tip CI greens: run Ultra-Magnus and bachelor with `-ContactSheet` once each so the first real labelled sheets land (owner ask). Confirm venue Python has Pillow+numpy so compose does not always fall through to `CONTACT_SHEET_COMPOSE_UNAVAILABLE`.
- `tools/repo_hygiene/test_playback_attr_3_cuda_split_route.py`: the artifact-index ↔ summary.json proximity window is now 10000 chars after three widenings for the compose step. Prefer asserting a unique compose marker (e.g. `CONTACT_SHEET_COMPOSE_UNAVAILABLE` / `--host`) sits between those two rather than growing a byte window again.

## Notes

- Owner request: contact sheets of what CUDA playback actually presented while Look Assist looks improve on Ultra-Magnus and bachelor. Round-1 is the measured-path capture + labelled composer + opt-in bachelor `-ContactSheet` (default off; off path byte-identical).
- Hub pre-review: seek-based capture was a blocker — fixed in r1b (`399be566`) with a genuine un-timed second playback pass via `noteContactSheetPresentedFrame` at `finishPresentedFrame`; seek kept only as labelled `--contact-sheet-seek-mode` (`playback_path=false`).
- Sol pre-review #1: 4 BLOCKERs fixed in r1c (`c448d152`/`28a79aef`) — disarm capture on seek frames; stop re-arming smoke telemetry; fail closed on GPU-window grab / serial mismatch; Python probe quoting; composer blob resolved only when `-ContactSheet` on; session-select job parsers.
- Sol pre-review #2: 1 BLOCKER + 4 HARDENING fixed in r1d (`9cfed18b`) — job passes quoted `--host`/`--gpu`/`--scale` into composer; explicit `playback_smoke.measured_session id=N` marker; clear smoke telemetry flags at session close; relative sidecar paths / frames_dir resolution; orphaned probe children recorded. Relative path hardening also in `bef61391`.
- Fork master merged at tip (`4e46a38e`); duration-as-proof inventory gate claimed 14/14 in PR body. Diff: `platform/qt/MainWindow.{h,cpp}`, `main.cpp`; `tests/console/test_contact_sheet_capture_wiring.cpp`; `tools/profiling/make-contact-sheet.py` + tests; bachelor `playback-attr-3-cuda-job.ps1` + `AttrCudaArtifacts.psm1`; repo_hygiene contact-sheet + behaviour/split-route touch-ups.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: sol/hub round-1 blockers addressed on this tip; wait for hosted Suite (Repo Hygiene behaviour + contact-sheet suites, Batch Compile, GUI Pilot, Product Oracles, CodeQL, Factory Bridge) before merge.
