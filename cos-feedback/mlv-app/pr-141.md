schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 141
head: b43f274971af6bd4158ca593b8c8a51bd5e5c121
reviewed_utc: 2026-09-21T03:58:03Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Fresh open on tip b43f2749 — Protected Check Route PASS; remaining IN_PROGRESS/pending (Repo Hygiene ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python, Windows GUI Pilot, Windows Product Oracles). mergeable MERGEABLE. Await green required checks on this tip before merge.

## Blockers

(none)

## Improvements

(none)

## Notes

- Scope: +4/−0 across four vendored `librtprocess` files — one-line `#include <new>` each in `src/librtprocess/src/include/jaggedarray.h`, `src/librtprocess/src/demosaic/ahd.cc`, `src/librtprocess/src/demosaic/hphd.cc`, `src/librtprocess/src/preprocess/CA_correct.cc`. One commit on tip (`b43f2749`). Branch `fix/macos-arm64-nothrow-include` → master.
- Defect: master `6235e22f` failed macOS Arm64 Release with `jaggedarray.h:40:43: error: no member named 'nothrow' in namespace 'std'` while Windows/Linux passed the same commit. Header had only `<cstring>` + `<limits>`; `std::nothrow` lives in `<new>`. Other platforms passed via transitive include order.
- Fix quality: self-contained header plus explicit includes at the three TUs that use `std::nothrow` directly. In `hphd.cc` the include is at top level, **not** inside the `_OPENMP` guard — correct (a guarded include would re-break non-OpenMP builds). Diff matches the claim.
- Local proof claimed in PR: g++ 13.1 `-fsyntax-only -std=c++11` on a TU that includes only `jaggedarray.h` — master rc=1 (`nothrow` not in `std`), this branch rc=0. CoS did not re-run that check (Linux box; claim is plausible and matches the header change).
- Verification limits (do not treat as this PR's blockers): hosted macOS Arm64 Release still stops at the vendored x86_64-only ffmpeg payload gate before compile; macOS Intel Release hangs in `brew install llvm` (2h job limit — runs 35545172748 / 35545174037). Owner redistribution approval needed for Arm64 payload; Intel bottle/LLVM install is a separate infra issue. PR check suite (Tests / Factory Bridge / CodeQL) is the merge gate for this tip.
- CoS via `gh` (four-line include fix; no Cloud Agent). No GitHub PR comment posted. Doctrine bus: `cos-feedback/mlv-app/pr-141.md` on master.