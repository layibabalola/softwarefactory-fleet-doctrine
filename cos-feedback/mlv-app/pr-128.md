schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 128
head: 0f54f117ac756c689b39c34fe10b9f6e6f3e9f98
reviewed_utc: 2026-09-16T18:12:50Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip re-queued all hosted checks after update-branch (Tests 35132895652, CodeQL 35132890587, Factory Bridge 35132895569). Required Batch Compile, Repo Hygiene (ubuntu/windows), Windows GUI Pilot, and Windows Product Oracles not settled yet on `0f54f117`. Prior tip had Batch Compile + ubuntu Repo Hygiene + Windows GUI Pilot PASS with windows Repo Hygiene + Product Oracles still open.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (ubuntu-latest + windows-latest), Windows GUI Pilot, and Windows Product Oracles on tip `0f54f117`. If Repo Hygiene (windows) red, first suspect the new source-scan assert in `test_gpu_job_result_provenance.py` (here-string backtick vs `$dllSha256` needle). Master merge also brought GUI/golden/agent-doc churn (`GpuDisplayViewport.*`, `tests/gui/test_gui_smoke.cpp`, `tests/fixtures/golden/gui_hashes.json`) that can affect Windows GUI Pilot / Product Oracles independently of this PR's three files.

## Improvements

- Optional: lowercase at the `Get-FileHash … .Hash` artifact site in both Ultramagnus evidence scripts so `artifacts[].sha256` is also lowercase for any future consumer, not only `provenance.dllSha256`.
- The producer-binding test uses `str.index` on escaped here-string text (`$dllSha256` vs `` `$dllSha256 ``). Prefer an AST/token-free but less brittle needle (e.g. require `ToLowerInvariant()` between the resolve assignment and the first `provenance.dllSha256` / `Write-ProvenanceFile` write) if this flakes under reformatting.

## Notes

- Tip `0f54f117` is another update-branch merge of `master` (`e60a1384`) onto prior tip `dda1ec08` on `factory/GPU-PROVENANCE-SELF-BINDING-1`. All three reviewed files are blob-identical at `3202f797`, `be10a20e`, `dda1ec08`, and `0f54f117`; net vs master still +25/−0 across those three paths only. MERGEABLE; `mergeStateStatus` BLOCKED (checks). 0 GitHub reviews.
- Content verdict unchanged: producer-only `ToLowerInvariant()` after resolving `igpu_recon_cuda.dll` sha256 in both Ultramagnus evidence scripts; validator unchanged; uppercase-reject + producer-binding tests added.
- Prior CoS Improvements not addressed on this push (expected for update-branch).
- Part 2 of GPU-PROVENANCE-SELF-BINDING-1 (derive `pendingSymbolPresence` from the hashed DLL) remains out of scope.
- CoS review via `gh` only; no GitHub PR comment. Bus refresh for new head (`cos-feedback/mlv-app/pr-128.md`).
