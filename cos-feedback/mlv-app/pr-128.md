schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 128
head: be10a20e1ad986404847a4b94e3f61853a7b2da0
reviewed_utc: 2026-09-16T17:13:55Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Required ubuntu Repo Hygiene + Windows GUI Pilot PASS; required Batch Compile, Repo Hygiene (windows-latest), and Windows Product Oracles still pending on tip (Tests 35126404640). Non-required CodeQL Analyze (actions/python/c-cpp) + Factory Bridge Regressions + Protected Check Route PASS.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (windows-latest), and Windows Product Oracles on tip `be10a20e`. If Repo Hygiene (windows) red, first suspect the new source-scan assert in `test_gpu_job_result_provenance.py` (here-string backtick vs `$dllSha256` needle).

## Improvements

- Optional: lowercase at the `Get-FileHash … .Hash` artifact site in both Ultramagnus evidence scripts so `artifacts[].sha256` is also lowercase for any future consumer, not only `provenance.dllSha256`.
- The producer-binding test uses `str.index` on escaped here-string text (`$dllSha256` vs `` `$dllSha256 ``). Prefer an AST/token-free but less brittle needle (e.g. require `ToLowerInvariant()` between the resolve assignment and the first `provenance.dllSha256` / `Write-ProvenanceFile` write) if this flakes under reformatting.

## Notes

- Tip `be10a20e` is update-branch merge of `master` (`b051a3b4`) onto prior tip `3202f797` on `factory/GPU-PROVENANCE-SELF-BINDING-1`. All three reviewed files are blob-identical at `3202f797` and `be10a20e`; net vs master still +25/−0 across those three paths only. MERGEABLE; `mergeStateStatus` BLOCKED (checks). 0 GitHub reviews.
- Content verdict unchanged from first CoS pass: producer-only `ToLowerInvariant()` after resolving `igpu_recon_cuda.dll` sha256 in both Ultramagnus evidence scripts; validator unchanged; uppercase-reject + producer-binding tests added.
- Prior CoS Improvements not addressed on this push (expected for update-branch).
- Part 2 of GPU-PROVENANCE-SELF-BINDING-1 (derive `pendingSymbolPresence` from the hashed DLL) remains out of scope.
- CoS review via `gh` only; no GitHub PR comment. Bus refresh for new head (`cos-feedback/mlv-app/pr-128.md`).
