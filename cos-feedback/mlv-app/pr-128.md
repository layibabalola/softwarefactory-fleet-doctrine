schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 128
head: dda1ec0881a1d623d841cc3d62d5d775bdbc86b6
reviewed_utc: 2026-09-16T17:44:11Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Required Batch Compile + ubuntu Repo Hygiene + Windows GUI Pilot PASS; required Repo Hygiene (windows-latest) and Windows Product Oracles still pending on tip (Tests 35129012939). Non-required CodeQL Analyze (actions/python/c-cpp) + Factory Bridge Regressions + Protected Check Route PASS.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Repo Hygiene Python (windows-latest) and Windows Product Oracles on tip `dda1ec08`. If Repo Hygiene (windows) red, first suspect the new source-scan assert in `test_gpu_job_result_provenance.py` (here-string backtick vs `$dllSha256` needle).

## Improvements

- Optional: lowercase at the `Get-FileHash … .Hash` artifact site in both Ultramagnus evidence scripts so `artifacts[].sha256` is also lowercase for any future consumer, not only `provenance.dllSha256`.
- The producer-binding test uses `str.index` on escaped here-string text (`$dllSha256` vs `` `$dllSha256 ``). Prefer an AST/token-free but less brittle needle (e.g. require `ToLowerInvariant()` between the resolve assignment and the first `provenance.dllSha256` / `Write-ProvenanceFile` write) if this flakes under reformatting.

## Notes

- Tip `dda1ec08` is another update-branch merge of `master` (`797b61e6`) onto prior tip `be10a20e` on `factory/GPU-PROVENANCE-SELF-BINDING-1`. All three reviewed files are blob-identical at `3202f797`, `be10a20e`, and `dda1ec08`; net vs master still +25/−0 across those three paths only. MERGEABLE; `mergeStateStatus` BLOCKED (checks). 0 GitHub reviews.
- Content verdict unchanged: producer-only `ToLowerInvariant()` after resolving `igpu_recon_cuda.dll` sha256 in both Ultramagnus evidence scripts; validator unchanged; uppercase-reject + producer-binding tests added.
- Prior CoS Improvements not addressed on this push (expected for update-branch). Batch Compile flipped PASS vs prior bus row; two required Windows checks remain open.
- Part 2 of GPU-PROVENANCE-SELF-BINDING-1 (derive `pendingSymbolPresence` from the hashed DLL) remains out of scope.
- CoS review via `gh` only; no GitHub PR comment. Bus refresh for new head (`cos-feedback/mlv-app/pr-128.md`).