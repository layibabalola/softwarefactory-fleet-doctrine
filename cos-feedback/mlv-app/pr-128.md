schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 128
head: 3202f797a900d0cafa39122806eb75680b49a2ce
reviewed_utc: 2026-09-16T16:37:34Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route + CodeQL Analyze (actions/python) PASS; Analyze (c-cpp), Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles still pending on tip (Tests 35122904452 / Factory Bridge 35122904415 / CodeQL 35122901403)

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Analyze (c-cpp), Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, and Windows Product Oracles on tip `3202f797`. If Repo Hygiene red, first suspect the new source-scan assert in `test_gpu_job_result_provenance.py` (here-string backtick vs `$dllSha256` needle).

## Improvements

- Optional: lowercase at the `Get-FileHash … .Hash` artifact site in both Ultramagnus evidence scripts so `artifacts[].sha256` is also lowercase for any future consumer, not only `provenance.dllSha256`.
- The producer-binding test uses `str.index` on escaped here-string text (`$dllSha256` vs `` `$dllSha256 ``). Prefer an AST/token-free but less brittle needle (e.g. require `ToLowerInvariant()` between the resolve assignment and the first `provenance.dllSha256` / `Write-ProvenanceFile` write) if this flakes under reformatting.

## Notes

- Tip `3202f797` on `factory/GPU-PROVENANCE-SELF-BINDING-1` → `master`. Net +25/−0 across 3 files. MERGEABLE; `mergeStateStatus` BLOCKED (checks). 0 GitHub reviews.
- Root cause: `Get-FileHash` returns UPPERCASE; PR #67 validator `gpu_job_result_provenance.py` requires `^[0-9a-f]{64}$`. Both remote bachelor producers copied the raw digest into `provenance.dllSha256`, so real bachelor manifests failed validation and blocked PLAY-COUNTERS-GPU / PLAY-C2-SUBMIT-2-ACCEPT.
- Fix is producer-only: `ToLowerInvariant()` immediately after resolving `igpu_recon_cuda.dll` sha256 in `invoke-ultramagnus-cdng-export-evidence.ps1` and `invoke-ultramagnus-p3-evidence.ps1`. Validator unchanged (correct). Card correction noted: `build-backend-dll.ps1` is not the dllSha256 source.
- Tests: uppercase digest still rejected; both producers must lowercase before any record (`provenance.dllSha256` / `Write-ProvenanceFile` must not appear between resolve and lowercase). Removing either lowercase line fails the binding test.
- Part 2 of GPU-PROVENANCE-SELF-BINDING-1 (derive `pendingSymbolPresence` from the hashed DLL) is explicitly out of scope on this PR.
- CoS review via `gh` only; no GitHub PR comment. First bus publish for this PR (`cos-feedback/mlv-app/pr-128.md`).