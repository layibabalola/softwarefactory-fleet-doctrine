schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 124
head: e48e2ea7958fbe5feb017c6ff9537b00008766b6
reviewed_utc: 2026-09-16T09:58:45Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: All checks still pending/in progress on head e48e2ea7 (CodeQL actions/c-cpp/python + Factory Bridge Regressions starting on runs 35082338996, 35082342376); prior head b467c9a4 checks superseded

## Blockers

(none)

## Improvements

- Optional follow-up (not blocking): put `status` into the schema `required` array if every future ledger must declare freshness/staleness. Right now `status` is optional in JSON Schema and only pinned by `test_ledger_declares_stale_status`, so a regenerator could drop the key and still schema-validate.
- When Repo Hygiene Python (ubuntu + windows) finish on this head, spot-check that `test_closeout_evidence_ratchet.py` is still collected and that the three remaining ledger/schema tests are green (local red-run + green-run documented in PR body for round 2).

## Notes

- Round 2 correctly removes the tracked `.closeout-evidence` dir-count ratchet. Seat 2 blocked round 1: `closeout.config.json` has `evidenceRepair` with `requiredFor` including `final_push`, so ordinary closeout creates new `.closeout-evidence/<workBlockId>/` dirs; `RATCHET = 617` in a required check would red-board every PR on the 618th. No in-scope defuse (raising constant forbidden; untracking Phase-3-gated). Real fix is a closeout evidence policy change — ratchet re-booked on a separate card blocked on that policy.
- What remains: `CLOSEOUT-CAPABILITY-LEDGER.json` top-level `"status": "STALE-2026-05-08"`, matching optional `status` property on the schema (`additionalProperties: false`), and three tests in `tools/repo_hygiene/test_closeout_evidence_ratchet.py` (validate, declare stale, reject unknown top-level keys). Unknown-key test tightened to require an `additionalProperties` error that names `unexpectedTopLevelKey` (was: any non-empty error list).
- Diff vs master: +62/−0 across the same three paths; file still named `test_closeout_evidence_ratchet.py` though ratchet logic is gone (rename optional hygiene).
- Prior CoS note about collecting the ratchet growth test is obsolete; growth enforcement correctly dropped for this card.
- MERGEABLE; no GitHub review comment posted by CoS. Review via `gh` only.