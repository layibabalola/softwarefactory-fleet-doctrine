schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 124
head: b467c9a4779d6be5b12f0fe620e625110e2f819a
reviewed_utc: 2026-09-16T09:50:45Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route PASS; CodeQL Analyze (actions) + (python) PASS; Analyze (c-cpp), Factory Bridge, Batch Compile, Repo Hygiene win+ubuntu, Windows Product Oracles, Windows GUI Pilot still pending on head b467c9a4 (runs 35081449514, 35081450984, 35081451048)

## Blockers

(none)

## Improvements

- Optional follow-up (not blocking): put `status` into the schema `required` array if the intent is that every future ledger must declare freshness/staleness. Right now `status` is optional in JSON Schema and only pinned by `test_ledger_declares_stale_status`, so a regenerator could drop the key and still schema-validate.
- When Repo Hygiene Python (ubuntu + windows) finish, spot-check that `test_closeout_evidence_ratchet.py` is collected and green there — that is the primary acceptance surface for this card (local suite claimed 4/4 new tests OK; 16 pre-existing `test_brokered_closeout.py` path-length failures unrelated).

## Notes

- Scope is tight and correct for HYG-EVIDENCE-RATCHET-1: +79/−0 across three files only — new `tools/repo_hygiene/test_closeout_evidence_ratchet.py` (`RATCHET = 617`), `CLOSEOUT-CAPABILITY-LEDGER.json` top-level `"status": "STALE-2026-05-08"`, and matching `status` property on the schema (needed because root `additionalProperties: false`).
- Verify-first discrepancy handled well: card said 615; `git ls-files` / cut-to-workBlockId at pinned base `b697b411` counted **617**. Ratcheting at 615 would have been red on arrival; constant uses derived current count. Red-run proof at 616 documented in PR body.
- Ratchet semantics match NA-2: archive-only tree; test fails on growth; docstring correctly says lowering RATCHET on cleanup is allowed, raising is not. Path split normalizes `\\` → `/` for Windows.
- No capability rows re-scored; ledger truthfulness is a status flag only — appropriate Phase-3 gate on closeout machinery itself.
- MERGEABLE; no labels; no GitHub review comments. CoS review via `gh` only (no CloudAgent). No GitHub PR comment posted.