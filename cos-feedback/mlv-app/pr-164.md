schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 164
head: 6e3af11b6b1f5337bbe70d93442d41a44908f0fe
reviewed_utc: 2026-09-25T14:55:42Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 6e3af11b (+376/−0, 6 files, 1 commit; ahead 1 / behind 0): Protected Check Route SUCCESS; CodeQL Analyze (actions) SUCCESS; Analyze (c-cpp/python) + Factory Bridge Regressions + Tests (Batch Compile / Repo Hygiene ubuntu+windows / Windows GUI Pilot / Windows Product Oracles) pending/in progress (Tests run 36150615955, CodeQL 36150609232, Factory Bridge 36150615817). MERGEABLE; mergeStateStatus BLOCKED (protection waiting on required checks). 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- Soft: if `gui_smoke.foreground_request … verified=0`, measurement still proceeds. A typed non-zero exit (or a gate verdict field) when verification fails would keep a refused SetForegroundWindow from silently poisoning PresentMon fps the same way occlusion did. Not blocking — the log line already exists for post-hoc filter, and author flags live bachelor confirmation (`verified=1`, `foreground_lost_count=0`, PresentMon displayed vs app fps) as the next run.

## Notes

- Scope: `--gui-smoke-playback` only. `forcePlaybackSmokeWindowForeground()` has a single call site immediately before the measured `ui->actionPlay->trigger()` in `runGuiPlaybackSmoke()`; wiring tests pin absence from `on_actionPlay_toggled` and from normal GUI startup in `main.cpp`.
- Windows path: `AllowSetForegroundWindow(ASFW_ANY)` + optional `AttachThreadInput` (always detached) + brief `HWND_TOPMOST`→`HWND_NOTOPMOST` + `SetForegroundWindow`/`BringWindowToTop`, verified via `GetForegroundWindow`. Embedded `GpuDisplayWindow` gets `requestActivate()` via new `GpuDisplayWindow::activeWindow()` (no independent OS HWND foreground — container owns that). Non-Windows falls back to Qt `isActiveWindow` / `activateWindow`.
- Telemetry reuses `MLVAPP_PLAYBACK_SMOKE_TELEMETRY` (construction-time `applicationStateChanged` connect only when on). Sibling `playback_smoke.foreground` line at gate: `foreground_at_begin` / `foreground_at_gate` / `foreground_lost_count` (event-driven via `ApplicationInactive`/`Hidden`, session-gated). Because force runs before Play toggle, `beginPlaybackSmokeTelemetry` samples post-force begin — intentional for "was foreground when measurement started."
- Author local evidence: app + tests/gui build; `console_tests --check-golden` 350 passed / 0 failed (hub); 12 wiring tests mutation-checked by hand. Live PresentMon confirmation still pending on bachelor.
- Related CUDA-PERF stack: Identity/HARNESS work on #159–#163 (display harness). This PR is the occlusion/foreground hypothesis for the ~3–7 displayed vs ~30 submitted fps gap.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound and smoke-scoped; only tip required checks / branch protection blocking. Repo Hygiene (windows) + GUI Pilot / Product Oracles are the usual long poles for this surface.
