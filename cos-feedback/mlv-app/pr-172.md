schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 172
head: 7c7279fcef46e0da41b520c97f6f7b00adce05df
reviewed_utc: 2026-09-25T23:55:48Z
event: pr-opened
verdict: not-merge-ready
ci_summary: Tip 7c7279fc (+1589/−8, 7 files, 4 commits; ahead 4 / behind 0): Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS (Tests 36202820331, Factory Bridge 36202820387, CodeQL 36202818089). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft. Hub live bachelor leg PENDING (required before merge; bachelor offline).

## Blockers

- **Live bachelor leg still required and PENDING** (hub merge condition, stated in PR body). Bachelor is currently offline. Prior tip `5f0ab8e9` held the display awake for a full leg (30 keep-alive nudges, screensaver never started) but ended `PRESENTMON_UNAVAILABLE`; a retry was lost to the claim-time gap that tip `553feb78` fixes. Do not merge until a clean live bachelor pass lands for this tip (or hub explicitly waives). Local attr-3 / console wiring tests are not a substitute for the hub-required live leg.

## Improvements

- After bachelor is back: treat `PRESENTMON_UNAVAILABLE` as a typed harness/smoke failure path with clear evidence (same class starved the `5f0ab8e9` live proof) so display-wake success is not masked by PresentMon sidecar gaps.
- Keep #167 CLOSED (CLOSED at open time of #172, not merged); do not reopen the WAKE-1 umbrella — this PR owns the superseding stack plus claim-time / secure-screensaver fixes.

## Notes

- Supersedes parked #167 (contract v2 R2). Carries #167 `100a807b` (WAKE-1), plus `5f0ab8e9` (screensaver keep-alive / SC_SCREENSAVE refusal — sol's #167 BLOCKER), `553feb78` (claim-time wake, SCREENSAVER_SECURE_OWNER_ONLY exit 25, OpenInputDesktop+SetThreadDesktop dismiss for non-secure already-running saver), then merge of fork master `c2f9d377` as tip `7c7279fc`.
- App: `SetThreadExecutionState(ES_CONTINUOUS|ES_DISPLAY_REQUIRED)` on play/stop/close; Windows-only `nativeEvent` refuses `WM_SYSCOMMAND` `SC_SCREENSAVE`/`SC_MONITORPOWER` while armed; reports acquire outcome + `screensaver_blocked_count`. Job: claim-time wake before footage/package/quiescence; 15s 1-px keep-alive Runspace; read-only `SPI_GET*` only (never `SPI_SET*`); secure saver → typed early exit, no bypass.
- Diff: `platform/qt/MainWindow.{h,cpp}`; new `tests/console/test_playback_display_wake_wiring.cpp` + `console_tests.pro`; `tools/profiling/bachelor/AttrCudaArtifacts.psm1` + `playback-attr-3-cuda-job.ps1`; `tools/repo_hygiene/test_playback_attr_3_cuda_behaviour.py`.
- Author claims: attr-3 python behaviour/ordering suites + console wiring; mutation-checked; lane caught PowerShell-scriptblock-on-new-thread crash before bachelor. Local: console 362/362 `--check-golden`; attr-3 behaviour 203/203; app+console_tests clean on Qt 6.10.2 + MinGW 13.1. CoS did not re-run Windows console on this box.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **not-merge-ready**: content + wiring look sound for sol's screensaver BLOCKER and the claim-time gap, but hub-required live bachelor is still open and tip CI is still settling.
