schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 140
head: 644429c0348bfdf23a719055caf69e1d88b39b92
reviewed_utc: 2026-09-18T07:19:30Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Fresh open on tip 644429c0 — all checks IN_PROGRESS/pending (Protected Check Route, Repo Hygiene ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeStateStatus BLOCKED (required checks); 0 GitHub reviews.

## Blockers

(none)

## Improvements

- Land Sol HIGH then fable as the PR body asks (admission predicate for files placed on an unattended host). GitHub still shows 0 reviews on tip `644429c0`.
- Confirm `Repo Hygiene Python (windows-latest)` green on this head: `UmRunFixtureContentPinTests` is `@unittest.skipUnless(os.name == "nt")` (and needs pwsh+git); ubuntu will not exercise the five new arms (`identical` admit, `altered` refuse, untracked, orphan-non-repo, no-git).
- Optional clarity: when `Test-UmRunFixtureContentPin` throws, `Test-UmRunTrackedFixtureSource` only `Write-Verbose`s then returns `$false`, so `Assert-UmRunSideFileName` may surface a generic extension-allowlist refusal instead of `ATTR3_FIXTURE_WORKING_TREE_DIRTY` / `UMRUN_FIXTURE_CONTENT_PIN_UNAVAILABLE`. Consider bubbling a distinguishable token into the side-file name assert path for operator logs.

## Notes

- Scope: +149/−6 across `tools/profiling/UmRunDrop.psm1` and `tools/repo_hygiene/test_um_run_sidefiles.py`. Closes fable key on #137: prior admit was name + `git ls-files --error-unmatch` only, so a working-tree overwrite with foreign bytes still staged onto the measurement host under the fixture name.
- Mechanism: new `Test-UmRunFixtureContentPin` reuses `Assert-AttrCudaFixtureCommittedBytes` from `tools/profiling/bachelor/AttrCudaArtifacts.psm1` (ATTR3-FIXTURE-STAGE-1 twin from #139) — `git hash-object` vs `git rev-parse HEAD:<rel>` — imported on demand **without** `-Force` so a caller that already bound that module keeps its binding. Fail-closed tokens: `UMRUN_FIXTURE_CONTENT_PIN_UNAVAILABLE`, `ATTR3_FIXTURE_GIT_UNAVAILABLE`, `ATTR3_FIXTURE_NOT_IN_A_REPO`, `ATTR3_FIXTURE_NOT_COMMITTED`, `ATTR3_FIXTURE_WORKING_TREE_DIRTY`.
- Evidence claimed in PR: producer red (`test_altered_working_tree_bytes_are_refused` failed against old admit) then green `174 passed, 249 subtests`; non-author run on HEAD `644429c0` same count under `.claude-state/fleet-runs/review-subject4-20260918T0715Z/`. CoS did not re-run Windows suite (Linux box).
- Branch `product/ATTR3-ADMIT-CONTENT-PIN-1` → master; not draft. Related: #137 (fixture rehearsal, landed), #139 (fixture stage / committed-bytes assert, landed), #134 (owner-consented footage, still open).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).