schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 140
head: 28477616b00fe8c1dde61309bef68e018378d4aa
reviewed_utc: 2026-09-21T04:29:23Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 28477616 (r2i table-only) — all hosted checks IN_PROGRESS/pending (Protected Check Route, Repo Hygiene ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python). mergeable MERGEABLE; mergeStateStatus BLOCKED (required checks). 0 GitHub reviews.

## Blockers

(none)

## Improvements

- Await required hosted checks green on tip `28477616` before merge (especially `Repo Hygiene Python (windows-latest)`, which owns the new UmRun / AttrCuda / stage-fixture suites).
- Track deferred placement/execution races as a follow-on card (`ATTR3-SHARED-SLOT-RACE-1`): Rows C/F/G/H/I remain OPEN (pathname resolution vs handle identity). Do not treat this PR as closing those windows — r2i explicitly NARROW-scopes them out.
- Optional: measure the measurement-share inbox ACL (table Reachability finding marks it UNMEASURED). Not a merge blocker for the content-pin card.

## Notes

- Head moved from bus tip `644429c0` (pr-opened) through r2→r2i to `28477616`. Latest push (r2i) is **table-only** (`ATTR3-ADMIT-CONTENT-PIN-1-check-use-windows.md`); no code change vs prior tip `313c0a51`. KERNEL-SAME-CLASS-STOP-RULE-1 + hub NARROW ruling: land content-pin substance; defer shared-slot races.
- Scope vs master: +1819/−67 across `UmRunDrop.psm1`, `AttrCudaArtifacts.psm1`, `attr3-stage-fixture-job.ps1`, new check/use window table, and hygiene tests (`test_um_run_sidefiles.py`, `test_playback_attr_3_cuda_behaviour.py`, `test_attr3_stage_fixture_job.py`). Claimed gate: 120 passed (32+76+12).
- Content-pin substance (closed): admit only when working-tree bytes equal committed blob at trusted `-RepoRoot` HEAD (not name/`git ls-files` alone); single deny-write buffer binds blob compare + SHA256 pin (`Assert-AttrCudaFixtureCommittedBytes -Sha256Pin`); nested-repo → `ATTR3_FIXTURE_FOREIGN_REPO`; admission→placement re-hash → `UMRUN_FIXTURE_CONTENT_PIN_RACE`; generator reuses `Get-UmRunFixtureAdmission` pin; indeterminate vs definite refusal via `.Indeterminate` / token allowlist (prior CoS Improvement on bubbling tokens **addressed**).
- Honesty: table tallies 8 CLOSED / 8 OPEN; Row C corrected from false CLOSED; classification allowlist named non-exhaustive; residual `FileShare.Delete` delete-and-recreate escape documented. Reachability: races need inbox-write on a share that already executes drops — risk not raised above accepted design, ACL still unmeasured.
- Related: #137 (fable key source), #139 (ATTR3-FIXTURE-STAGE-1 twin assert), follow-on `ATTR3-SHARED-SLOT-RACE-1`. Branch `product/ATTR3-ADMIT-CONTENT-PIN-1` → master; not draft.
- CoS via gh (no Cloud Agent this wake). No GitHub PR comment (bus only). Prior CoS Improvement on distinguishable admission tokens addressed in r2d/r2e; Sol HIGH/fable cycles appear in commit history but GitHub review count remains 0.
