schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 134
head: 4c50b39d313ba77f4f4a8df1176ab577ab57d7bd
reviewed_utc: 2026-09-17T05:34:13Z
event: pr-opened
verdict: not-merge-ready
ci_summary: Protected Check Route PASS; Analyze (actions/c-cpp/python), Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, Batch Compile, Windows GUI Pilot, Windows Product Oracles pending/in_progress on tip 4c50b39d; CodeQL skipping; mergeable MERGEABLE, mergeStateStatus BLOCKED (required checks); ahead 2 / behind 0 vs master.

## Blockers

- **Sol HIGH review missing** — PR cites ruling §1.3 two-key re-ratification (sol + Opus) and a sol HIGH review bound to head `4c50b39d`. Tip has 0 GitHub reviews. Unblock: land sol HIGH on this head (and Opus key) before merge; owner-veto window stays open with no owner action required.

## Improvements

- Job-side content verify still out of scope (stated LIMIT): before opening any admitted path, the job must check each part's length and sha256 against the same `fork/master:tools/gates/output-budget.json` object. Hook compares text only.
- Purpose binding is recorded not enforced (`#72b delta-baseline gate`, `PLAYBACK-ATTR-3-CUDA` rely on card id in an agent-written prompt). Tighten later if a card/prompt contract can assert purpose without widening NA-4.
- After merge, re-pin board receipts that still quote old hook/test hashes (`3408f089…` / `6cae429a…`): `.claude-state/coordination/dual-lane/receipts/0.05-hook-enforced.json` (`hookSha256`, `hookTestSha256`), `execution-control-0.7.json` hashes + O172 `fixedSetEqualityProof`, and `orchestrator-resume-CURRENT.md`. Until then Invoke-Lane refuses `-AllowEdits` (`hook-not-enforced`). New tip hashes claimed in PR body: hook `42e9bad6…`, test `f096222d…` — recompute on board after merge.

## Notes

- Scope: 3 files, +485/−13, branch `factory/NA4-OWNER-CONSENTED-FOOTAGE-1` → master. Two commits: (1) NA-10 guards `tools/gates/output-budget.json` as fourth protected tail (na10 rows 6→7); (2) NA-4 exception (3) owner-consented footage.
- Mechanism reviewed: frozen `OWNER_CONSENTED_FOOTAGE` `MappingProxyType` (exactly six ids M16-1243, M16-1327, M16-1347, M17-1207, M15-1320, M16-1210 — **not** M02-1344; per-part length+sha256; no paths); admit only when token equals `norm()` of a consented id's `clips[].path`/`parts[].path` from git object `fork/master:tools/gates/output-budget.json` via `git -C <board> show` (board must hold `.git`), with top-level and every part matching the table; any git/parse failure admits nothing; read at most once per invocation and only for a footage token not otherwise allowed. Register `mlv-app/never-authorized/v27` act + authority clause match.
- CoS hash pin check vs master `tools/gates/output-budget.json`: all six consented ids PRESENT with matching part lengths/sha256 and top-level; M02-1344 PRESENT in spec but correctly absent from the frozen table.
- Tests: `OwnerConsentedFootageTests` (ALLOW listed paths; DENY M02-1344 / wrong dir / digest|length mismatch / worktree-only or unpublished HEAD / unlisted cache / git failure / duplicate id; single-read; real hook process; table pin + register text). Author reports `py -3 -m unittest tools.repo_hygiene.test_mlv_never_authorized -v` → 281 OK, plus registration/containment/output-budget/line-endings suites and `check_pinned_tokens.py` PASS; red-run falsifier (admit line `if False`) failed 3 as expected.
- Related open work: #107 edits the same three files and is CONFLICTING vs master — whichever of #107/#134 lands second needs a rebase. This tip is clean ahead of master (not BEHIND).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
