schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 158
head: 030b713e1ef427816ae3070b625f25b4a25addd0
reviewed_utc: 2026-09-25T10:46:53Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 030b713e (+669/−26, 4 files, 3 commits; ahead 3 / behind 0): Tests run 36125692406 Protected Check Route SUCCESS; Batch Compile, Repo Hygiene ubuntu/windows, Windows GUI Pilot, Windows Product Oracles IN_PROGRESS; Factory Bridge 36125692541 IN_PROGRESS; CodeQL run 36125690194 Analyze (actions) SUCCESS, Analyze (python)/(c-cpp) IN_PROGRESS, suite CodeQL NEUTRAL. MERGEABLE; mergeStateStatus BLOCKED (protection waiting on required checks). 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

(none in this PR's owned scope — harness/job/tests only; companion app-side swap telemetry join is explicitly deferred.)

## Notes

- Scope: job script + AttrCudaArtifacts module + two new repo_hygiene tests only; no MainWindow / app / `.ui` code. Closes three bachelor baseline failures named in the PR body: (1) missing `presentmon.csv` after a full smoke left nothing published (3 of 8 attempts); (2) uncaught "no positive MsBetweenDisplayChange samples" throw after a passed smoke; (3) PresentMon `--timed 55` vs smoke `--seconds 40` idle contamination without window restriction.
- New `Get-AttrCudaPresentMonDisplayReport` (AttrCudaArtifacts.psm1, exported + embedded into the job): PID-scoped, window-restricted to `[process.startedAtUtc, process.endedAtUtc]` via TimeInMs relative to `$presentMonCaptureStartUtc`; groups by `(ProcessID, SwapChainAddress)`; selects MLVApp chain by `result.json` `process.id` (busiest same-PID chain on ties); typed `PRESENTMON_UNAVAILABLE` / `DISPLAY_ASLEEP` returns — never a throw. Keeps MsBetweenDisplayChange==0 rows in presentedCount.
- Job wiring (`playback-attr-3-cuda-job.ps1`): smoke artifacts (result.json, stdout/stderr, probe-timeline.csv, logs\smoke-run.log) published before any PresentMon parse; raw presentmon.csv copied if present; `presentmon-capture.json` + `presentMonCaptureStartUtc` on summary/manifest before report; non-OK exits 23 (UNAVAILABLE) or 24 (ASLEEP) with typed summary.json. Old bare Import-Csv loop and "no positive samples" throw removed.
- Tests: `test_playback_attr_3_cuda_presentmon_display_report.py` (10 fixture cases via pwsh against real module — missing/empty/columnless csv, bad identity, all-zero displayed, out-of-window, two-chain selection, secondary same-PID preference); `test_playback_attr_3_cuda_presentmon_publish_ordering.py` (7 static template/module tripwires — publish-before-parse, Test-Path guard, throw gone, exit-code routing, embed list, capture-anchor persist). Author local: 292 passed + 288 subtests (+1 anchor). CoS did not re-run (Windows factory; review via gh only).
- Commits: `090370e9` report function + display-report tests; `2f5d8c2a` hub publish ordering + typed exits + publish-ordering tests; `030b713e` persist presentmon-capture.json before parse. Diff limited to the 4 owned paths above.
- Companion app-side swap telemetry (join against PresentMon TimeInMs) is out of this PR; bachelor joined run deferred until that lands. CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound; only tip required checks / branch protection blocking.