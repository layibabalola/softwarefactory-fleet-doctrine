schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 120
head: 0a71bb05779b913cf465cea22edb2b4987f2314f
reviewed_utc: 2026-09-15T23:45:44Z
event: pr-opened
verdict: not-merge-ready
ci_summary: All hosted checks IN_PROGRESS on head 0a71bb05 (CodeQL Analyze actions/c-cpp/python; Factory Bridge Regressions; Tests: Protected Check Route, Repo Hygiene ubuntu/windows, Batch Compile). No conclusions yet.

## Blockers

- **CI: required hosted checks** — still IN_PROGRESS on head `0a71bb05779b913cf465cea22edb2b4987f2314f` (Actions runs 35037014629 CodeQL, 35037017128 Factory Bridge, 35037017124 Tests). Wait for green before merge; if red, fix and re-push.
- **Process: Sol HIGH cross-family review (GUARD-PATH)** — `Invoke-Lane.ps1` is in `mlv-never-authorized.py`'s `EXECUTION_CONTROL_HASH_TABLE`; PR body correctly requires Sol at HIGH effort to decide merge, not the implementer. 0 GitHub reviews yet. Land that review on this exact head before merge.

## Improvements

- `agents/orchestration-tiering.md` judgement/Fable row still says effort is "low by default; high only for a Tier 1 decision", while this packet sets `fable` table effort to `high` in `Invoke-Lane.ps1` (and pins that in tests) per the 2026-09-08 owner ruling. Update the Fable table cell in the same doc pass that already refreshed the Astra row so the binding default and the launcher agree.
- Optional follow-up (out of this packet's owner-grant scope): when `astra` is intentionally added to any workstream/loop ValidateSet later, keep the engine-keyed edit refusal and timer-absence tests as the contract that must stay green — do not reintroduce name lists.

## Notes

- Diff vs `master`: 3 files, +181/−8. `tools/coordination/Invoke-Lane.ps1` (+14/−7): ValidateSet +`astra`; `$LANES` adds `astra` (codex / `gpt-6-astra` / `xhigh` / judgement-design-arbiter) and sets `fable` effort `high`; edit refusal rekeyed from `$Lane -eq 'sol' -or $Lane -eq 'luna'` to `$LANES[$Lane].engine -eq 'codex'`; Claude child `CLAUDE_CODE_EFFORT_LEVEL` gated on resolved `$cfg.effort` instead of raw `-ReasoningEffort`. `test_coordination_guardrails.py` (+166): engine-keyed refusal (astra/sol/sonnet control), refusal helper stays engine-keyed, effort gate + receipt alignment, fable high default, astra row/ValidateSet/codex argv reuse, `xhigh` never on claude rows / override ValidateSet, astra absent from `Invoke-Workstream.ps1` and `Invoke-WorkstreamLoop.ps1`. Docs: Astra reachability + placement (codex-cli 0.154.0, `xhigh` probed 2026-09-15T23:16Z).
- Content review: the engine rekey closes a real hole for any future codex row; astra correctly reuses the generic codex argv branch (read-only + `xhigh`) with no lane-name fork; timer surfaces left untouched so judgement stays off heartbeats. Author claims local suite `213 passed in 153.96s` with red/green evidence for the two guards. Product ratio guard reported RED this session but is untouched and out of grant scope — not a merge blocker for this packet.
- Head `0a71bb05` is tip of five ASTRA-LANE-1 commits on `claude/astra-lane-1-20260915`. MERGEABLE once Sol HIGH review lands and required hosted checks are green.
- CoS review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (plan). Doctrine bus: created on `master` at `cos-feedback/mlv-app/pr-120.md`.