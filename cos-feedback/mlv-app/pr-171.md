schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 171
head: 42f92878e5cbc2759740fd6dffe7605128fa78e6
reviewed_utc: 2026-09-26T02:50:08Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Tip 42f92878 (+948/−17 vs master, 6 files, 7 commits; ahead 7 / behind 0): merge of master (#175 PROD-TELEMETRY-DURATION-AS-PROOF-4B) onto prior tip 5b121e15; no product-code change to the fullscreen latch stack. Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS/QUEUED (Tests 36212885123, Factory Bridge 36212884912, CodeQL 36212883223). MERGEABLE; mergeStateStatus BLOCKED (hub). 0 GitHub reviews; not draft. Hub live bachelor leg still PENDING (required before merge).

## Blockers

- **Live bachelor leg still required and PENDING** (hub merge condition, stated in PR body). Package for latch tip `ea58168e` was built/staged earlier; six live attempts failed on harness issues unrelated to this change (budget timeout; `VENUE_NOT_QUIESCENT`; 2× unguarded PresentMon sidecar "no positive MsBetweenDisplayChange samples" crash; others), then bachelor went offline. This push is a master sync only — it does not clear the bachelor gate. Do not merge until a clean live bachelor pass lands for this tip (or hub explicitly waives). #168's entry-path live proof (`verified=1` at 2560×1600, `played_ms` 40056, exit 0) covers only the unchanged entry path, not the mid-session loss latch.

## Improvements

- After bachelor is back: harden the PresentMon sidecar path so missing `MsBetweenDisplayChange` samples fail closed with a typed smoke reason instead of crashing the harness — that class blocked two of the six live attempts and will keep starving latch proof. Related work may already be in flight as #174 (PRESENTMON-HARNESS-ROBUSTNESS-1); keep this Improvement until live proof for #171 actually lands.
- Keep #168 CLOSED; do not re-open the UI-1 umbrella — this PR owns the superseding stack plus the latch fix.

## Notes

- Push event: tip moved `5b121e15` → `42f92878` by merging fork/master after #175 landed (`1475fe5c`). Diff vs master still the CUDA-PLAYBACK-FULLSCREEN-UI-2 stack only (hygiene files from #175 are already on base).
- Supersedes parked #168 (contract v2 R2). Stack: `5ed0a354`, `d93af141`, `c42ae94c`, `f0e3f219`, latch `ea58168e`, prior master merge `5b121e15`, this master merge `42f92878`.
- Latch mechanism unchanged since prior CoS pass: `MainWindow::changeEvent(WindowStateChange)` → `m_playbackSmokeFullscreenLostCount`, armed after fullscreen verified / before `actionPlay->trigger()`, disarmed at loop exit before guard teardown. Gate: `!isFullScreen() || lostCount > 0` → exit 13 `reason=fullscreen_lost_mid_session lost_count=N`. Telemetry `playback_smoke.foreground` carries `fullscreen_lost_count`.
- Diff vs master: `platform/qt/MainWindow.{h,cpp}`; new `tests/console/test_playback_smoke_fullscreen_wiring.cpp` + `test_playback_fullscreen_ui_wiring.cpp`; small `test_playback_smoke_foreground_wiring.cpp` + `console_tests.pro`.
- Author claims (unchanged): 9 new wiring tests + 1 updated; mutation-proved; local console 382/4282/0 failed on Qt 6.10.2 + MinGW 13.1. CoS did not re-run Windows console on this box.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict still **not-merge-ready**: content/wiring unchanged and still look sound; hub-required live bachelor remains open; tip CI re-settling after the master merge.
