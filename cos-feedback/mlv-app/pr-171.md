schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 171
head: e92cdb1f1204a3b0d2e46b1793ee6af0690630cc
reviewed_utc: 2026-09-26T03:22:08Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Tip e92cdb1f (+948/−17 vs master, 6 files, 8 commits; ahead 8 / behind 0): test-only rename in test_playback_smoke_fullscreen_wiring.cpp so #175 duration-as-proof inventory does not treat a QString::indexOf position as a duration. Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, Windows GUI Pilot (IN_PROGRESS), Windows Product Oracles (QUEUED), CodeQL Analyze (actions/c-cpp/python) settling (Tests 36214533356, Factory Bridge 36214533466, CodeQL/PR 36214531320). MERGEABLE; mergeStateStatus BLOCKED (hub). 0 GitHub reviews; not draft. Hub live bachelor leg still PENDING (required before merge).

## Blockers

- **Live bachelor leg still required and PENDING** (hub merge condition, stated in PR body). Package for latch tip `ea58168e` was built/staged earlier; six live attempts failed on harness issues unrelated to this change (budget timeout; `VENUE_NOT_QUIESCENT`; 2× unguarded PresentMon sidecar "no positive MsBetweenDisplayChange samples" crash; others), then bachelor went offline. This push is a test-string rename only — it does not clear the bachelor gate. Do not merge until a clean live bachelor pass lands for this tip (or hub explicitly waives). #168's entry-path live proof (`verified=1` at 2560×1600, `played_ms` 40056, exit 0) covers only the unchanged entry path, not the mid-session loss latch.

## Improvements

- After bachelor is back: harden the PresentMon sidecar path so missing `MsBetweenDisplayChange` samples fail closed with a typed smoke reason instead of crashing the harness — that class blocked two of the six live attempts and will keep starving latch proof. Related work may already be in flight as #174 (PRESENTMON-HARNESS-ROBUSTNESS-1); keep this Improvement until live proof for #171 actually lands.
- Keep #168 CLOSED; do not re-open the UI-1 umbrella — this PR owns the superseding stack plus the latch fix.

## Notes

- Push event: tip moved `42f92878` → `e92cdb1f`. Commit message: rename wiring-test local `durationAt` → `requestedFieldAt` (3 occurrences in `DoneLineReportsPreambleAndPlayedMsNextToRequestedDuration`) so master's duration-as-proof inventory gate (#175) does not flag `ASSERT_TRUE(durationAt >= 0)` as a duration value. Product/latch code unchanged.
- Diff vs master still the CUDA-PLAYBACK-FULLSCREEN-UI-2 stack only (+948/−17, 6 files).
- Supersedes parked #168 (contract v2 R2). Stack: `5ed0a354`, `d93af141`, `c42ae94c`, `f0e3f219`, latch `ea58168e`, master merges `5b121e15`/`42f92878`, this rename `e92cdb1f`.
- Latch mechanism unchanged: `MainWindow::changeEvent(WindowStateChange)` → `m_playbackSmokeFullscreenLostCount`, armed after fullscreen verified / before `actionPlay->trigger()`, disarmed at loop exit before guard teardown. Gate: `!isFullScreen() || lostCount > 0` → exit 13 `reason=fullscreen_lost_mid_session lost_count=N`. Telemetry `playback_smoke.foreground` carries `fullscreen_lost_count`.
- Author claims (unchanged): 9 new wiring tests + 1 updated; mutation-proved; local console 382/4282/0 failed on Qt 6.10.2 + MinGW 13.1. CoS did not re-run Windows console on this box.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict still **not-merge-ready**: content/wiring still look sound; hub-required live bachelor remains open; tip CI re-settling after the rename push.