schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 171
head: 5b121e154c266e9b0f65fbab05a10fb67e103ffb
reviewed_utc: 2026-09-25T23:37:00Z
event: pr-opened
verdict: not-merge-ready
ci_summary: Tip 5b121e15 (+948/−17, 6 files, 6 commits; ahead 6 / behind 0): Protected Check Route SUCCESS; CodeQL overall check NEUTRAL; Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS (Tests 36201641383, Factory Bridge 36201641159, CodeQL 36201639701). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft. Hub live bachelor leg PENDING (required before merge).

## Blockers

- **Live bachelor leg still required and PENDING** (hub merge condition, stated in PR body). Package for tip content `ea58168e` is built/staged, but six live attempts failed on harness issues unrelated to this change (budget timeout; `VENUE_NOT_QUIESCENT`; 2× unguarded PresentMon sidecar "no positive MsBetweenDisplayChange samples" crash; others), then bachelor went offline. Do not merge until a clean live bachelor pass lands for this tip (or hub explicitly waives). #168's entry-path live proof (`verified=1` at 2560×1600, `played_ms` 40056, exit 0) covers only the unchanged entry path, not the new mid-session loss latch.

## Improvements

- After bachelor is back: harden the PresentMon sidecar path so missing `MsBetweenDisplayChange` samples fail closed with a typed smoke reason instead of crashing the harness — that class blocked two of the six live attempts and will keep starving latch proof.
- Keep #168 CLOSED (already CLOSED draft at open time of #171); do not re-open the UI-1 umbrella — this PR owns the superseding stack plus the latch fix.

## Notes

- Supersedes parked #168 (contract v2 R2). Carries #168 commits `5ed0a354`, `d93af141`, `c42ae94c`, `f0e3f219`, plus latch fix `ea58168e`, then merge of fork master `c2f9d377` (#170) as tip `5b121e15`.
- Round-1 same-item split addressed: end-of-loop `isFullScreen()` was a point sample (Escape→wait→F11/Ctrl+F still passed). Fix is `MainWindow::changeEvent(WindowStateChange)` latch `m_playbackSmokeFullscreenLostCount`, armed only after fullscreen verified and immediately before `actionPlay->trigger()`, disarmed at loop exit before guard teardown. Gate: `!isFullScreen() || lostCount > 0` → exit 13 `reason=fullscreen_lost_mid_session lost_count=N`. Telemetry `playback_smoke.foreground` adds `fullscreen_lost_count`.
- Diff: `platform/qt/MainWindow.{h,cpp}` (changeEvent latch, enter/leave smoke fullscreen, fail-closed gates, clock moved after preamble, screen-local geometry); new `tests/console/test_playback_smoke_fullscreen_wiring.cpp` + `test_playback_fullscreen_ui_wiring.cpp`; small `test_playback_smoke_foreground_wiring.cpp` + `console_tests.pro` updates.
- Author claims: 9 new wiring tests + 1 updated; mutation-proved (revert gate → 3 red; remove disarm → 1 red); local console 382 tests / 4282 assertions / 0 failed (33 pre-existing profile-gated skips) on Qt 6.10.2 + MinGW 13.1. CoS did not re-run Windows console on this box.
- Also from #168 stack: menu fullscreen unhidden; F11 (Win) + Esc exit-only; maximized restore; fail-closed `fullscreen_not_verified` before play; `preamble_ms`/`played_ms` on DONE line.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **not-merge-ready**: content + wiring look sound for the latch defect, but hub-required live bachelor is still open and tip CI is still settling.
