schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 180
head: b3aa3fb96d872eda43c0a0c774e4a108d7568ed3
reviewed_utc: 2026-09-26T06:42:42Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip b3aa3fb9 (CUDA-PLAYBACK-CONTACT-SHEET-2 supersedes #177; +3415/−13, 13 files, 15 commits; ahead 15 / behind 0 vs master). Protected Check Route, CodeQL Analyze (actions/c-cpp/python), Factory Bridge Regressions, Repo Hygiene Python (ubuntu+windows), Batch Compile IN_PROGRESS (Tests 36224498739, Factory Bridge 36224498729, CodeQL 36224497956). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- `CONTACT-SHEET-LOOP-WRAP-QUALIFY-1` (already scoped out on the PR): wrap detection is any presented `display_frame` lower than the previous one during the measured session. Prefer requiring active Loop plus a cutOut→cutIn transition, and an executable span-normalization test, so an external backward seek cannot force the cutIn..cutOut override.
- `CI-PIN-NUMPY-PILLOW-1` (already scoped out): Factory Bridge still `--ignore`s `tools/profiling/test_make_contact_sheet.py` (and the contact-sheet behaviour suite lazy-imports numpy). Pin hashed numpy+Pillow across hosted CI, then drop the ignores so the composer suite runs on every PR.
- After tip CI greens: bachelor PresentMon / contact-sheet capture still needs a Windows host with CUDA playback to prove the un-timed second-pass grabs and early-refusal raw publish end-to-end (console wiring + repo_hygiene pin structure; hosted Suite does not exercise GPU present).

## Notes

- Supersedes parked #177 (contract v2 BLOCKER → PARK/SPLIT). Carries all of #177 (opt-in `--contact-sheet-dir`/`--contact-sheet-frames`, genuine un-timed playback capture vs labelled `--contact-sheet-seek-mode`, labelled header with all Look Assist values + majority render_path, in-job compose, relative sidecar paths, CommandLineToArgvW quoting, measured-session bind, default-off byte-identical base `$cmd`, numpy/Pillow CI skips, Windows-only generator skips) plus two tip commits:
  1. `09b7acba96ab` — wrapped measured run derives contact-sheet span from Loop cutIn/cutOut (0-based, `spinBox` − 1); `span_start`/`span_end`/`span_wrapped` on every sidecar and on `gui_smoke.contact_sheet`; `playback_smoke.measured_session` logged at the measured Play trigger (not first play-stop); `m_contactSheetOptionsPresent` gates `finishPresentedFrame` so options-off pays zero contact-sheet calls on the hot path.
  2. `b3aa3fb96d87` — `Publish-AttrCudaContactSheetRawCaptures` runs on `BACKEND_NOT_AVAILABLE` / `GPU_RECON_FRAMES_ZERO` early exits as well as main publish; `ConvertTo-AttrCudaQuotedProcessArgument` rewritten to CommandLineToArgvW backslash-before-quote rules with round-trip oracle tests.
- Hub procedure v2.2: sol pre-review APPROVED (span 0-based, no off-by-one; #177 findings fixed). Duration-as-proof inventory gate claimed 14/14. Fork master merged (`4e46a38efc3c`); tip ahead 15 / behind 0.
- Files: `platform/qt/MainWindow.{cpp,h}`, `platform/qt/main.cpp`, `tests/console/test_contact_sheet_capture_wiring.cpp` + `console_tests.pro`, `tools/profiling/make-contact-sheet.py` + `test_make_contact_sheet.py`, `tools/profiling/bachelor/{AttrCudaArtifacts.psm1,playback-attr-3-cuda-job.ps1}`, `tools/repo_hygiene/test_playback_attr_3_cuda_{contact_sheet,behaviour,split_route}.py`, `.github/workflows/factory-bridge.yml`.
- CoS via gh + cursor-github MCP (no Cloud Agent; not on plan). No GitHub PR review comment (bus only).