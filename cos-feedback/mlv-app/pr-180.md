schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 180
head: c7e3eae7b5ef6f953ad4537374d3b986ab75faea
reviewed_utc: 2026-09-26T13:09:51Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip c7e3eae7 (CUDA-PLAYBACK-CONTACT-SHEET-2 round 2; +3805/−29, 15 files, 18 commits; ahead 18 / behind 0 vs master). Protected Check Route, CodeQL Analyze (actions/c-cpp/python), Factory Bridge Regressions, Repo Hygiene Python (ubuntu+windows), Batch Compile all IN_PROGRESS (Tests 36244191639, Factory Bridge 36244191631, CodeQL 36244189373). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- `CI-PIN-NUMPY-PILLOW-1` (still scoped out on the PR): Factory Bridge still `--ignore`s `tools/profiling/test_make_contact_sheet.py` (and the contact-sheet behaviour suite lazy-imports numpy). Pin hashed numpy+Pillow across hosted CI, then drop the ignores so the composer suite runs on every PR.
- After tip CI greens: bachelor PresentMon / contact-sheet capture still needs a Windows host with CUDA playback to prove the un-timed second-pass grabs, drop-frame-off restore, early-refusal raw publish, and module-spliced `Publish-AttrCudaContactSheetRawCaptures` end-to-end (console wiring + repo_hygiene pin structure; hosted Suite does not exercise GPU present).

## Notes

- Round-2 tip vs prior CoS head `b3aa3fb9`: three commits —
  1. `aa1740e0a041` — **addresses prior CoS Improvement `CONTACT-SHEET-LOOP-WRAP-QUALIFY-1`**: wrap detection now goes through `playback_frame_range::isContactSheetLoopWrapTransition` (Loop must be on; jump must land near cutIn and leave near cutOut). Contact-sheet capture pass temporarily clears Drop Frame Mode so every target including a wrapped span's last frame is presented, then restores the prior setting. Drop-frame tick + target-frame math extracted to `PlaybackFrameRange.h` with executable tests (`test_playback_frame_range.cpp`) proving looped drop-frame never presents cutOut−1.
  2. `07edb70e6d6a` — merge fork/master (lands PRESENTMON-HARNESS-ROBUSTNESS-1..3 / #179).
  3. `c7e3eae7b5ef` — move `Publish-AttrCudaContactSheetRawCaptures` from inline job-template into `AttrCudaArtifacts.psm1` + splice list so PRESENTMON-HARNESS-ROBUSTNESS-2 `test_every_called_attrcuda_command_is_defined_in_the_real_embedded_text` passes; hygiene tests updated to Import-Module the real export.
- Prior round-1 content unchanged in substance: supersedes #177; whole-loop span from cutIn/cutOut; `measured_session` at Play trigger; default-off zero-cost hot path; raw publish on early refusal; CommandLineToArgvW quoter. Hub sol pre-review still APPROVED for round-1 span math.
- Files touched this tip: `platform/qt/MainWindow.cpp`, `platform/qt/PlaybackFrameRange.h`, `tests/console/test_{contact_sheet_capture_wiring,playback_frame_range}.cpp`, `tools/profiling/bachelor/{AttrCudaArtifacts.psm1,playback-attr-3-cuda-job.ps1}`, `tools/repo_hygiene/test_playback_attr_3_cuda_contact_sheet.py` (+ master-merge PresentMon surface).
- CoS via gh + cursor-github MCP (no Cloud Agent; not on plan). No GitHub PR review comment (bus only).
