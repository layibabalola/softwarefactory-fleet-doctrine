schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 144
head: 002818441b84108c928554d330fdab65d6de0d76
reviewed_utc: 2026-09-21T11:25:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 00281844 (3 commits on product/ATTR3-SMOKE-RUNNER-DEPS-1: original closure staging + r2 sol/fable fixes + merge fork/master 8d5cb28a; +1478/−309 across AttrCudaArtifacts.psm1, attr3-stage-smoke-runner-job.ps1, playback-attr-3-cuda-job.ps1, test_playback_attr_3_cuda_behaviour.py, test_playback_attr_3_cuda_split_route.py; ahead 3 / behind 0): all listed checks QUEUED or IN_PROGRESS (Analyze actions/c-cpp/python, Batch Compile, Factory Bridge Regressions, Protected Check Route, Repo Hygiene Python ubuntu+windows); mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which runs the new AST fail-closed / exact-set / reparse / launch-exception suites)

## Improvements

- Soft: attribution pre-launch pin still walks only the expected closure names. Stage job's `Test-AttrCudaClosureDirectoryMatches` now refuses extras + reparse; the attribution gate refuses reparse and mismatch but does not yet refuse an unexpected sibling leaf under `smoke-runner-<digest16>`. Mirror the exact leaf-set check before hash reads so a polluted cache directory cannot launch.
- Soft: `ConvertTo-AttrCudaGeneratorPsLiteral` remains duplicated in both generators — prefer one export from `AttrCudaArtifacts.psm1`.
- Soft: still **0 GitHub reviews** on Bachelor staging / venue diagnosis path; no bachelor proof yet (PR acknowledges). Short human glance before merge is cheap.

## Notes

- Prior CoS Improvements (pr-opened on 9cd13b4c) **addressed on this head** via r2 `a66c44d4` (sol three majors + fable four minors), then tip merged master:
  1. **Fail-closed scan**: `Get-AttrCudaScriptFileLiteralReferences` (AST) + `$AttrCudaClosureScanExclusions` next to the scanner; `Resolve-AttrCudaSmokeRunnerClosure` throws `ATTRCUDA_UNCLASSIFIED_SCRIPT_REFERENCE` for anything neither staged nor excluded. Tests cover the real runner's double-quoted `Join-Path $root "…/detect-playback-artifacts.ps1"` miss, exclusion path+literal keying, and assert ATTR-3 never passes `-DetectPlaybackArtifacts`.
  2. **Exact staged set + reparse refuse**: `Test-AttrCudaClosureDirectoryMatches` requires count equality, no unexpected names, no containers, no reparse at dir or members, then hashes. Attribution refuses reparse at dir/members before `Get-Sha`. Behavioural tests for extra file and reparse-as-ALREADY.
  3. **Launch exception → SMOKE_RUN_FAILED**: nested pwsh start is try/caught; exception maps to capped `CategoryInfo.Reason` (R4-safe, never `.GetType()`); `Stop-PresentMonCapture` records kill/wait errors and re-reads `confirmedExited`. `SmokeRunFailedPresentMonCleanupTests` drives a real PresentMon stop on launch exception; split-route asserts order + artifact fields.
  4. Digest sort now `[Array]::Sort(..., Ordinal)` (fable culture-aware Sort-Object).
- Intent unchanged from #142 follow-on: content-addressed four-file `$PSScriptRoot` closure + check smoke outcome before PresentMon so Bachelor reports `SMOKE_RUN_FAILED` / `ATTRCUDA_SMOKE_RUNNER_STALE` instead of `PRESENTMON_TIMEOUT`.
- Hub-claimed targeted suites 197 OK + `test_mlv_never_authorized` 283 OK on r2 — CoS did not re-run. Scope stays bachelor tooling / hygiene tests.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).