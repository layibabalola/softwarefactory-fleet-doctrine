schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 144
head: 9cd13b4cdebbc29483b5a1767d601e40384a387d
reviewed_utc: 2026-09-21T10:33:37Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 9cd13b4c (+841/−314 across AttrCudaArtifacts.psm1, attr3-stage-smoke-runner-job.ps1, playback-attr-3-cuda-job.ps1, test_playback_attr_3_cuda_behaviour.py, test_playback_attr_3_cuda_split_route.py; ahead 1 / behind 0): Protected Check Route PASS; Analyze (actions/c-cpp/python), Batch Compile, Factory Bridge Regressions, Repo Hygiene Python (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles PENDING; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which executes the new pwsh closure / stage / pin suites in `test_playback_attr_3_cuda_behaviour.py`)

## Improvements

- **`SMOKE_RUN_FAILED` coverage is string-order only.** `SmokeRunFailedBeforePresentMonTests` asserts source order (`$smokeRc` → check → `Stop-PresentMonCapture` → `Wait-PresentMonCapture`) and token presence, but never runs a pwsh probe that forces a non-zero smoke exit (or missing `result.json`) and asserts exit 18 + `RESULT=SMOKE_RUN_FAILED` without hitting `PRESENTMON_TIMEOUT`. A thin behavioural row would lock the diagnosis fix the same way the stale-pin probes lock the pre-flight refuse.
- **Closure matcher ignores extra files.** `Test-AttrCudaClosureDirectoryMatches` (stage job) returns true when every pinned name hashes, even if the directory also holds unexpected siblings. Prefer also asserting the directory's leaf set equals the closure name set (or refuse extras) so an interrupted partial + rename race cannot be treated as `ALREADY=1`.
- **Scanner shape is narrow by design.** `Get-AttrCudaScriptRootDependencies` only matches `^\s*(?:\.|Import-Module)\s+\(Join-Path\s+\$PSScriptRoot\s+'…'\)`. Production `run-release-gui-smoke.ps1:91–93` matches; double-quoted names, splatted paths, or `& (Join-Path …)` would be silently missed. Soft: add a negative test that a near-miss form is NOT scanned, and a comment/assert in the stage job that the live runner's three loads are present in the resolved closure (name-set check against known production basenames).
- Soft: `ConvertTo-AttrCudaGeneratorPsLiteral` is duplicated in both generators — prefer one export from `AttrCudaArtifacts.psm1`.
- Soft: still **0 GitHub reviews** on Bachelor staging / venue diagnosis path; short human glance before merge is cheap. No bachelor proof yet (PR acknowledges).

## Notes

- Closes the real #142 follow-on defect: stage job published only `run-release-gui-smoke.ps1`, so Bachelor died at the first `$PSScriptRoot` sibling load; PresentMon (`--timed 55 --terminate_after_timed`) never saw a target; waiting on PresentMon before checking smoke exit mis-labeled the failure `PRESENTMON_TIMEOUT`.
- Fix shape (aligned with PR body + diff):
  1. **Mechanical closure** via `Resolve-AttrCudaSmokeRunnerClosure` / `Get-AttrCudaScriptRootDependencies` / `Get-AttrCudaClosureDigestHex` in `AttrCudaArtifacts.psm1` (BFS over committed text; root first).
  2. **Content-addressed directory** `smoke-runner-<digest16>` published with `Publish-AttrCudaDirectoryMoveNonOverwriting`; every file base64-inlined and hash-checked before disk write; schema bump `mlvapp.attr3-stage-smoke-runner-deps.v1`.
  3. **Attribution pre-flight** loops every closure entry under that directory and refuses with `ATTRCUDA_SMOKE_RUNNER_STALE` naming the missing/mismatched file — before PresentMon / app launch.
  4. **Diagnosis order**: on smoke non-zero or missing `result.json`, `Stop-PresentMonCapture` then `RESULT=SMOKE_RUN_FAILED` exit 18; `PRESENTMON_TIMEOUT` reserved for smoke-success + PresentMon hang.
- Production runner loads (verified on tip): lines 91–93 load exactly `gui-smoke-screenshot-provenance.ps1`, `gui-smoke-process-boundary.psm1`, `provenance-stamp.ps1`. Those three siblings have **no** further `$PSScriptRoot` loads on tip — production closure is four files. Fixture repo adds a fifth recursive support file for scan tests only.
- Tests: independent Python re-scan vs staged set; staged-directory launch proves siblings resolve; missing-one-file pin names the dependency; digest order-independence; split-route static checks for `SMOKE_RUN_FAILED` ordering. Hub-claimed 177 + 283 OK — CoS did not re-run.
- Scope stays bachelor tooling / hygiene tests; no product playback / CUDA open. Prerequisite stage path supersedes #142 single-file pin.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
