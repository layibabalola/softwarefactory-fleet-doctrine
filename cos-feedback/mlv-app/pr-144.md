schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 144
head: 2d994e824f44ecd5596fa9c8db4629c81de18754
reviewed_utc: 2026-09-21T12:36:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 2d994e82 (4 commits on product/ATTR3-SMOKE-RUNNER-DEPS-1: original + r2 sol/fable + merge master + round 3 pinned-manifest redesign; +2168/−339 across AttrCudaArtifacts.psm1, attr3-stage-smoke-runner-job.ps1, playback-attr-3-cuda-job.ps1, test_playback_attr_3_cuda_behaviour.py, test_playback_attr_3_cuda_split_route.py; ahead 4 / behind 0): all listed checks IN_PROGRESS (Analyze actions/c-cpp/python, Batch Compile, Factory Bridge Regressions, Protected Check Route, Repo Hygiene Python ubuntu+windows); mergeable_state BLOCKED; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which exercises the new pinned-manifest census / shared exact-set / PresentMon kill-error suites)

## Improvements

- Soft: `ConvertTo-AttrCudaGeneratorPsLiteral` remains duplicated in both generators (`attr3-stage-smoke-runner-job.ps1` and `playback-attr-3-cuda-job.ps1`) — prefer one export from `AttrCudaArtifacts.psm1`.
- Soft: still **0 GitHub reviews** on Bachelor staging / venue diagnosis path; no bachelor proof yet (PR acknowledges). Short human glance before merge is cheap.

## Notes

- Prior CoS Improvement (attribution exact leaf-set) **addressed on this head** via round 3 `2d994e82`: both emitted jobs embed byte-identical `Get-AttrCudaClosureDirectoryMismatch` (exact count, no unexpected entries, no subdirs, no reparse, then sha256); attribution throws `ATTRCUDA_SMOKE_RUNNER_STALE` with the mismatch reason before PresentMon/app launch. Stage "already staged" uses the same function.
- Round 3 NARROW BY REDESIGN: discovery scanners removed from the trust path. Closure is pinned `$script:AttrCudaSmokeRunnerClosureManifest` (four repo-relative paths). `Get-AttrCudaScriptLoadSites` is fail-closed AST census; `Assert-AttrCudaClosureComplete` classifies every site as Join-Path `$PSScriptRoot`→manifest (full path, never basename), scriptblock-only `&`/`.`, Add-Type without -Path, or pinned exclusion — else `ATTRCUDA_UNCLASSIFIED_SCRIPT_REFERENCE`. Completeness is bidirectional (never-loaded manifest entry fails too).
- Negative census fixtures cover basename collision, extension-less load, bareword Import-Module, parse error, dynamic target, never-loaded manifest entry; `normalize_pwsh_message_text` strips ANSI/`|` gutters so console-wrapped throw text does not flake asserts.
- `Wait-PresentMonCapture` / stop path now surface `killError` + `confirmedExited` instead of swallowing Kill failures.
- Intent unchanged from #142 follow-on: content-addressed four-file `$PSScriptRoot` closure + check smoke outcome before PresentMon (`SMOKE_RUN_FAILED` / `ATTRCUDA_SMOKE_RUNNER_STALE` vs `PRESENTMON_TIMEOUT`).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
