schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 144
head: e4abf79f092832deb73d9d4c72fe7027ad4b9cf2
reviewed_utc: 2026-09-21T13:27:27Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip e4abf79f (5 commits on product/ATTR3-SMOKE-RUNNER-DEPS-1: original + r2 sol/fable + merge master + round 3 pinned-manifest + round 4 census-gap close; +2466/−340 across 6 files vs master; ahead 5 / behind 0): Analyze (actions) + Protected Check Route PASS; CodeQL skipping; Batch Compile, Factory Bridge Regressions, Repo Hygiene Python ubuntu+windows, Windows GUI Pilot, Windows Product Oracles, Analyze c-cpp/python IN_PROGRESS; mergeable_state BLOCKED; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which exercises the new round-4 census / Guid-partial / PresentMon wait-after-kill suites)

## Improvements

- Soft: `ConvertTo-AttrCudaGeneratorPsLiteral` remains duplicated in both generators (`attr3-stage-smoke-runner-job.ps1` ~:110 and `playback-attr-3-cuda-job.ps1` ~:256) — prefer one export from `AttrCudaArtifacts.psm1` (still absent there).
- Soft: still **0 GitHub reviews** on Bachelor staging / venue diagnosis path; no bachelor proof yet (PR acknowledges). Short human glance before merge is cheap.

## Notes

- Round 4 `e4abf79f` closes confirmed sol/fable census gaps and states the tripwire contract honestly (not CoS Improvements from prior bus file — those remain open above).
- Sol major 1: `Get-AttrCudaScriptLoadSites` matches loader names on the segment after the last `\`, so module-qualified `Microsoft.PowerShell.Core\Import-Module` is visible to the census.
- Sol major 2: `Set-Alias`/`sal`/`New-Alias`/`nal` (qualified or not) are always unclassified — alias targets are undecidable statically.
- Sol major 3: `Test-AttrCudaCommandHasPathParameter` treats abbreviated/aliased Add-Type path params (`-Pat`, `-Lit`, `-PSPath`, `-LP`) as pathful, so they cannot pass as pathless class-(c).
- Fable minor: static `[System.Diagnostics.Process]::Start` / `[Diagnostics.Process]::Start` is a census site; the real pinned exclusion is exact-site-text keyed (different arg expression refuses).
- Sol minor: emitted stage jobs use runtime `[Guid]::NewGuid()` partial-dir suffix so concurrent same-job invocations do not share one deterministic `.partial` directory; `Guid::NewGuid` allowlisted in `attr3_publish_write_scan.ps1`.
- Fable minor: `Wait-PresentMonCapture` timeout path now waits bounded after `Kill()` before sampling `HasExited`, and reports `killError` / `waitError` / `confirmedExited` (mirrors `Stop-PresentMonCapture`).
- Contract honesty: module header now states Assert-AttrCudaClosureComplete is a regression tripwire over the four pinned files, not an exhaustive proof; runtime `SMOKE_RUN_FAILED` remains the safety property for forms the census cannot see.
- Prior CoS Improvements (ConvertTo dup; 0 GH reviews / no bachelor) **not** addressed on this head.
- Intent unchanged: content-addressed four-file `$PSScriptRoot` closure + check smoke outcome before PresentMon.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).