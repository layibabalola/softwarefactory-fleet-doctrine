schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 144
head: 002818441b84108c928554d330fdab65d6de0d76
reviewed_utc: 2026-09-21T11:25:05Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 002818441b (+1478/−309 across AttrCudaArtifacts.psm1, attr3-stage-smoke-runner-job.ps1, playback-attr-3-cuda-job.ps1, test_playback_attr_3_cuda_behaviour.py, test_playback_attr_3_cuda_split_route.py; ahead 3 / behind 0 after merge of #143): Analyze (actions/c-cpp/python), Batch Compile, Factory Bridge Regressions, Protected Check Route, Repo Hygiene Python (ubuntu+windows) all PENDING/queued; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python on windows-latest which executes the new pwsh fail-closed / reparse / SMOKE_RUN_FAILED behavioural suites)

## Improvements

- Soft: `ConvertTo-AttrCudaGeneratorPsLiteral` (playback generator) and `ConvertTo-PsSingleQuoted` (same file, smoke launch line) remain near-duplicate single-quote escapers — prefer one export from `AttrCudaArtifacts.psm1`.
- Soft: still **0 GitHub reviews** on Bachelor staging / venue diagnosis path; short human glance before merge is cheap. No bachelor proof yet (PR acknowledges).

## Notes

- **Prior CoS Improvements addressed on r2 (`a66c44d4ad99`):**
  1. **`SMOKE_RUN_FAILED` behavioural coverage** — `SmokeRunFailedPresentMonCleanupTests` runs a real PresentMon stand-in process and asserts exit 18 + `RESULT=SMOKE_RUN_FAILED` + `presentMonConfirmedExited` for both ordinary nonzero smoke exit and launch `CommandNotFoundException` (sol major 3).
  2. **Exact closure leaf set** — `Test-AttrCudaClosureDirectoryMatches` now requires count + name-set equality (no extras), refuses containers, and refuses reparse points at the directory and each member; tests cover extra-file and junction-as-already-staged cases (sol major 2 + prior CoS).
  3. **Scanner narrowness** — AST scanner `Get-AttrCudaScriptFileLiteralReferences` + fail-closed `ATTRCUDA_UNCLASSIFIED_SCRIPT_REFERENCE` + reasoned exclusion for `detect-playback-artifacts.ps1` (keyed on exact `(repoRelativePath, literal)`); production closure still resolves to the four expected files (sol major 1).
- Also on r2: `Stop-PresentMonCapture` returns `{confirmedExited, killError, waitError}` (no empty catch); smoke launch wrapped in try/catch so every path hits PresentMon cleanup; ordinal `[StringComparer]::Ordinal` digest sort; `Test-AttrCudaPathIsReparsePoint` embedded in both generators.
- Tip `002818441b` is r2 + merge of fork/master (`8d5cb28a`, #143 ATTR3-FOOTAGE-BIND-1) for branch protection — PR diff vs master remains the five smoke-runner-deps files only.
- Scope stays bachelor tooling / hygiene tests; no product playback / CUDA open. Prerequisite stage path supersedes #142 single-file pin.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).