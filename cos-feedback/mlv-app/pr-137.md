schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 137
head: e469cea16962617f6b8594cbd8369d47c95308ac
reviewed_utc: 2026-09-17T18:25:09Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: All tip checks still pending/in progress on head e469cea1 (CodeQL Analyze actions/c-cpp/python; Factory Bridge Regressions; Tests: Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile — runs 35258576589 / 35258578267 / 35258578226). MERGEABLE; mergeStateStatus blocked on required checks. 0 GitHub reviews.

## Blockers

(none for content — fixture-rehearsal path is source-gated, literal ClipId allowlist, and flags fixture runs in artifacts)

## Improvements

- **Success-path labeling.** Early-exit venues write `summary.json` with `fixtureRehearsal`; success writes only `evidence-manifest.json` (pre-existing shape) plus `RESULT=MEASUREMENT_CAPTURED`. Consider either a success `summary.json` that also carries the flag, or a distinct `RESULT=FIXTURE_REHEARSAL_CAPTURED` when `$FixtureRehearsal`, so a skim of stdout cannot be read as a real attribution capture.
- **`consentReceipt` on fixture runs.** Generator still substitutes the default `owner-footage-consent-20260916.json` into the evidence manifest for fixture ClipIds. Prefer empty/null (or omit) when `$isFixtureRehearsal` so a fixture artifact never cites an owner consent file it did not use.
- **Docs vs success artifact.** Section 4b / header comment say the flag is in every `summary.json` and `evidence-manifest.json`; success path has no `summary.json`. Tighten the wording to “every early-exit `summary.json` plus `evidence-manifest.json` on success,” or add the success summary.

## Notes

- Scope: ATTR3-FIXTURE-REHEARSAL-1 (kernel SUBJECT 2). Diff vs master `8dabd9f3`: +208/−7 across 5 files — `UmRunDrop.psm1` (`Test-UmRunTrackedFixtureSource`), `playback-attr-3-cuda-job.ps1` (literal `tiny_dual_iso`/`large_dual_iso` ClipId + baked `fixtureRehearsal`), repo-hygiene tests, runbook §4b. No footage named or resolved.
- Design call is sound: first draft’s composed `('.' + 'mlv')` allowlist bypass of NA-4 was rejected; admission is by SOURCE under `tests/fixtures/clips` (extension allowlist untouched). Same basename from any other directory still refused. Fixtures `tiny_dual_iso.mlv` / `large_dual_iso.mlv` already tracked under `tests/fixtures/clips`.
- Unchanged gates still bind fixture runs: ClipPath must resolve into agent cache with BaseName -ceq ClipId; BuildManifestSha256 auth; eligibility exit 15 unless cuda_backend_available=1 and r16_available=1. Owner-clip ValidatePattern still matches.
- Tests claim 77 pass locally; CI Repo Hygiene (ubuntu+windows) will re-prove the new cases. Owner-footage leg remains on #134; this PR only unblocks the no-footage rehearsal.
- Branch `product/ATTR3-FIXTURE-REHEARSAL-1` → master; tip e469cea1; not draft; mergeable=true; mergeable_state=blocked. No GitHub PR comment posted by CoS. Review via `gh` (Cursor Cloud Agents not used for this pass).
