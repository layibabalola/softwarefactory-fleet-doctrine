schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 137
head: b358495416eab0d3103c1c50f9eb9fcfa1d63723
reviewed_utc: 2026-09-17T19:05:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip b3584954 — Protected Check Route green; CodeQL rollup neutral; still pending/in progress: Factory Bridge Regressions, Batch Compile, Repo Hygiene Python (ubuntu+windows), Analyze actions/c-cpp/python, Windows GUI Pilot, Windows Product Oracles (runs 35262686050 / 35262686099 / 35262682166). MERGEABLE; mergeStateStatus blocked on required checks. 0 GitHub reviews.

## Blockers

(none for content — r2 closed sol r1 admission and case-sensitivity blockers; fixture rehearsal is now unmistakable on success path)

## Improvements

- **`consentReceipt` on fixture runs (carried from r1).** Generator still substitutes the default `owner-footage-consent-20260916.json` into `evidence-manifest.json` for fixture ClipIds (`$ConsentReceiptFileName` default + unconditional `__CONSENT_RECEIPT__` replace). Prefer empty/null (or omit the field) when `$isFixtureRehearsal` so a fixture artifact never cites an owner consent file it did not use.

## Notes

- Push r2 (`b3584954`) vs prior CoS head `e469cea1`: addresses sol r1 blockers (repo-anchored real-path + git-tracked fixture admission; case-sensitive `(?-i:tiny_dual_iso|large_dual_iso)` ValidatePattern arm) and the CoS r1 success-path labeling Improvement (`summary.json` on success with `FIXTURE_REHEARSAL_CAPTURED` / `MEASUREMENT_CAPTURED`, flag on provenance + artifact-index, stdout `RESULT=… FIXTURE_REHEARSAL=`).
- Admission (`Test-UmRunTrackedFixtureSource`): source real directory must equal this module's repo `tests/fixtures/clips`; junctions/lookalike trees/untracked drops refused. Extension allowlist still untouched.
- Prior CoS Improvements #1 (success-path labeling) and #3 (docs vs success summary) treated as addressed on this head. Docs §4b still accurate for flag placement.
- Tests claim 82 pass locally; CI Repo Hygiene will re-prove. Owner-footage leg remains on #134.
- Branch `product/ATTR3-FIXTURE-REHEARSAL-1` → master; tip b3584954; not draft; mergeable=true; mergeable_state=blocked. No GitHub PR comment by CoS. Review via `gh`.
