schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 137
head: 82aa386e6906499bd1041a8ee40d92259887b96c
reviewed_utc: 2026-09-17T19:42:16Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Tip 82aa386e — Protected Check Route PASS; CodeQL rollup skipping/pending; still pending: Factory Bridge Regressions, Batch Compile, Repo Hygiene Python (ubuntu+windows), Analyze actions/c-cpp/python, Windows GUI Pilot, Windows Product Oracles (runs 35266315426 / 35266319042 / 35266319328). MERGEABLE; mergeStateStatus blocked on required checks. 0 GitHub reviews. Expected: AttributionJobFixtureRehearsalTests setUp will fail on Repo Hygiene once those jobs reach the new pattern.

## Blockers

- **Python tests cannot compile the new `\z` ClipId ValidatePattern.** `tools/repo_hygiene/test_playback_attr_3_cuda_split_route.py` `AttributionJobFixtureRehearsalTests.setUp` does `re.compile` on the literal PowerShell `[ValidatePattern('…')] ` string. .NET needs `\z` (r3: `$` also matches before a terminal LF), but CPython `re` rejects `\z` (`PatternError: bad escape \z`) — verified on this review host. That class’s setUp will error on Repo Hygiene (ubuntu + windows) before any fixture-id assertion runs. Fix: stop compiling the raw PowerShell pattern in Python — either (a) assert the generator source contains `\z` / `(?-i:` as strings and exercise acceptance/refusal via a small `pwsh` probe, or (b) if keeping a Python approximation, translate `\z`→`\Z` (and keep `(?-i:` only on Python ≥3.11) in a dedicated helper, never feed the raw attribute string to `re.compile`. Re-run `test_playback_attr_3_cuda_split_route` under the same Python CI uses before pushing r4.

## Improvements

(none new — prior CoS Improvement on `consentReceipt` for fixture runs is addressed on this head)

## Notes

- Push r3 (`82aa386e`) vs prior CoS head `b3584954`: closes sol r2 blocker (admission limited to clip stems `tiny_dual_iso` / `large_dual_iso`, not every tracked file under `tests/fixtures/clips` — README refused; tests select by stem); closes sol r2 major (`ValidatePattern` end-anchor `\z`); addresses CoS Improvement (fixture `evidence-manifest` sets `consentReceipt` to null). Claims 83 tests pass locally — the Python `\z` compile break is the gap between that claim and CI.
- Admission still repo-anchored (real path + git-tracked + stem allowlist); extension allowlist untouched; rehearsal still unmistakable (`FIXTURE_REHEARSAL_CAPTURED`, flag on summary/provenance/manifest/artifact-index/stdout).
- Owner-footage leg remains on #134. Docs §4b unchanged and still accurate.
- Branch `product/ATTR3-FIXTURE-REHEARSAL-1` → master; tip 82aa386e; not draft; mergeable=true; mergeable_state=blocked. No GitHub PR comment by CoS. Review via `gh`.
