schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 137
head: 7c02a4a9be0c34b68e57d167b8c4668cd84b3988
reviewed_utc: 2026-09-17T20:12:44Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 7c02a4a9 — PASS: Protected Check Route, Factory Bridge Regressions, Windows GUI Pilot, Repo Hygiene Python (ubuntu-latest), Analyze actions/c-cpp/python, CodeQL. Still pending: Batch Compile, Repo Hygiene Python (windows-latest), Windows Product Oracles (runs 35268966180 / 35268966265 / 35268962070). MERGEABLE; mergeStateStatus blocked on required checks. 0 GitHub reviews. Prior CoS blocker (Python re.compile of .NET \z ValidatePattern) cleared on this head — AttributionJobFixtureRehearsalTests all ok on ubuntu Repo Hygiene.

## Blockers

(none)

## Improvements

- Optional hardening: `match_in_powershell` skips when `pwsh` is absent. Ubuntu currently has pwsh and ran the probes ok; if a future matrix image drops it, those three acceptance tests would skip silently. Prefer failing (or installing pwsh in the ubuntu Repo Hygiene leg) over skip if the probes are load-bearing for merge confidence.

## Notes

- Push r4 (`7c02a4a9`) vs prior CoS head `82aa386e`: closes CoS r3 blocker. `AttributionJobFixtureRehearsalTests` no longer `re.compile`s the raw PowerShell `ValidatePattern`; it extracts the pattern string and evaluates accept/refuse candidates via `pwsh -cmatch`. Refusal cases now include `TINY_DUAL_ISO`, `Tiny_Dual_Iso`, and a trailing newline (sol r1/r2). Diff is test-only (`tools/repo_hygiene/test_playback_attr_3_cuda_split_route.py`).
- Product code unchanged from r3: stem-only fixture admission, `(?-i:...)` + `\z` ClipId pattern, `consentReceipt = $null` on fixture runs, unmistakable `FIXTURE_REHEARSAL_CAPTURED` / flag on summary/provenance/manifest/artifact-index/stdout.
- Owner-footage leg remains on #134. Docs §4b still accurate.
- Branch `product/ATTR3-FIXTURE-REHEARSAL-1` → master; tip 7c02a4a9; not draft; mergeable=true; mergeable_state=blocked. No GitHub PR comment by CoS. Review via `gh`.
