schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 115
head: 9410c43485617222d574bdaab176a4771bb538c9
reviewed_utc: 2026-09-15T06:33:45Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route green; CodeQL parent skipping; Batch Compile, Factory Bridge Regressions, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles, CodeQL Analyze (actions/c-cpp/python) still pending/in progress. No content blockers.

## Blockers

(none)

## Improvements

- Spool hygiene: `.claude-state/fleet-runs/dispatch-attempt-spool/` has no TTL or sweep. If unwritable-run-dir events are rare this is fine; if they cluster (full disk, ACL), add a later card to age out or cap spool files so operators do not drown in CARD-*-*.json.
- Downstream readers: any hub/brief that only opened `dispatch-attempt.json` should also know about `dispatch-attempts.jsonl` (history) and the spool dir (fallback). A one-line pointer in the coordination README or kernel map after merge would close the #111 doc gap this PR already fixes in code.

## Notes

- Addresses Sol post-merge CHANGES_REQUESTED on #111 (head `05b7f025`): (1) launch truth — `launching` before child `pwsh`, `$script:LaneLaunched` only after `$LASTEXITCODE`, trap adds `launch-unconfirmed` between starting and returned; (2) durability — append-only `dispatch-attempts.jsonl`, latest still in `dispatch-attempt.json`, spool outside run dir on write failure, loop `receiptWriteFailures` captures "NOT written" lines into the cycle receipt.
- Closes prior CoS Improvement on #111 about single-file overwrite losing the pre-launch stamp (jsonl keeps `launching` then `launched`).
- Both launch sites updated (non-editing ~L1030 and editing ~L1274); post-return still writes `-Outcome 'launched' -Cause 'lane-returned'`. Structural test asserts each `$script:LaneLaunched = $true` follows the `$LASTEXITCODE` capture; spool / loop-capture / history tests are mutation-checked red per PR body.
- Scope: `tools/coordination/Invoke-Workstream.ps1` (+35/−7), `Invoke-WorkstreamLoop.ps1` (+8/−0), `test_coordination_guardrails.py` (+81/−2). One commit on `claude/dispatch-attempt-truth-20260915` → `master`. MERGEABLE.
- PR body claims 355 local tests green; hosted CI still settling — treat as merge-when-ci-green until Batch Compile / Factory Bridge / Repo Hygiene / Windows suites finish.
- Sol's other #111 findings (allowlist wrong base from hub merge; exporter tooling gap) correctly out of scope here.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.
