schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 165
head: 333c1021b5004a948c66595dfb7558749775056a
reviewed_utc: 2026-09-25T15:10:49Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 333c1021 (+1455/−35, 6 files, 8 commits; ahead 8 / behind 0): Protected Check Route SUCCESS; CodeQL Analyze (actions/c-cpp/python) + Factory Bridge Regressions + Tests (Batch Compile / Repo Hygiene ubuntu+windows / Windows GUI Pilot / Windows Product Oracles) pending/in progress (Tests run 36152353484, CodeQL 36152351063, Factory Bridge 36152353327). MERGEABLE; mergeStateStatus BLOCKED (protection waiting on required checks). 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

(none — HARNESS-4 closes the #163 CoS soft note: bracket headline now ranks by displayed MLVApp rows first, then presented, ties to earliest.)

## Notes

- Supersedes parked #163 (HARNESS-3), which superseded #160 ← #158. Tip is HARNESS-1→2→3→4 plus two master merges (`e94ab927`, `1d8687d4`). Prefer closing/parking #158/#160/#163 once this lands so the HARNESS stack is not quadruple-open. (Separate from IDENTITY GPU-window telemetry #159/#161/#162 and foreground #164.)
- **Delta vs #163 (one commit `333c1021` on top of `7f02435c`):** `Get-AttrCudaPresentMonDisplayReport` headline selection ranked endpoints by `presentedCount` alone, so an endpoint that admitted only an undisplayed tail could head the report and yield a false `DISPLAY_ASLEEP` while the other endpoint admitted a displayed front-edge row. Endpoints now rank by `displayedCount` first, then `presentedCount`, ties → earliest; headline `displayedCount` is therefore the max over both ends, and `DISPLAY_ASLEEP` fires only when neither endpoint admits a displayed MLVApp row. `headlineReason` text updated to cite both counts. Pinned by `test_sol_repro_displayed_front_edge_row_beats_an_undisplayed_tail_row` and `test_fable_repro_more_presented_but_undisplayed_endpoint_does_not_head` (both fail under presented-only ranking).
- Carry-forward from #163 (unchanged in this tip's delta, still in the PR): real PresentMon 2.5.1 columns (no fictional `DisplayedTime`); publish-before-wait + wait-failure evidence parity; real-interval-only `Get-Stats` filter; dual-endpoint clock window with `rowsDifferingInWindowMembership`; PID-summed swap chains; typed exits 23/24.
- Author local evidence: PLAYBACK-ATTR-3-CUDA suites + tools/profiling pass locally; contract review deferred until hosted checks green (contract v2.1). CoS did not re-run (Windows factory; review via gh only).
- Scoped-out follow-ups (author, card DISPLAY-IDENTITY-HARNESS-HARDENING-1): low-sample flag; live-process test for `SMOKE_LOG_UNAVAILABLE` PresentMon stop; tighter post-spawn stamp; empirical anchor validation.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound and closes the prior CoS soft note; only tip required checks / branch protection blocking. Repo Hygiene (esp. windows PresentMon unit suites) is the first signal; GUI Pilot / Product Oracles are the usual long poles.