schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 165
head: d33eae95f291ab1d8f319d9068086cc5655c8659
reviewed_utc: 2026-09-25T15:54:34Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip d33eae95 (+1454/−35, 6 files, 10 commits; ahead 10 / behind 0): all tip checks IN_PROGRESS or QUEUED — Protected Check Route, Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, CodeQL Analyze (actions/c-cpp/python); Windows GUI Pilot + Windows Product Oracles QUEUED (Tests run 36157226591, CodeQL 36157224607, Factory Bridge 36157226606). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft. Prior tip 333c1021 run 36152353484 had Protected Check Route SUCCESS before this re-head.

## Blockers

(none)

## Improvements

(none — HARNESS-4 still closes the #163 CoS soft note; this push is docs/comment sync + absorb master #164, no ranking-logic change.)

## Notes

- Round 2 vs prior CoS head `333c1021`: (1) merge master `42a59a1d` / #164 FOREGROUND into the HARNESS-4 branch (`bab7a344`); (2) tip `d33eae95` — docs and comments only (`docs/playback-attr-3-cuda.md`, `AttrCudaArtifacts.psm1` function comment, `playback-attr-3-cuda-job.ps1` comment) so runbook text matches the displayed-first endpoint ranking already in code. Author: "Text only; no code change." Ranking logic and the two pinned repro tests unchanged.
- Supersedes parked #163 (HARNESS-3 ← #160 ← #158). Prefer closing/parking #158/#160/#163 once this lands. (Separate from IDENTITY GPU-window telemetry #159/#161/#162; #164 FOREGROUND is now on master and absorbed here.)
- Carry-forward content (unchanged by this tip's delta): displayed-first then presented ranking with ties → earliest; `DISPLAY_ASLEEP` only when neither endpoint admits a displayed MLVApp row; real PresentMon 2.5.1 columns; publish-before-wait + wait-failure evidence parity; real-interval-only `Get-Stats`; dual-endpoint clock window; PID-summed swap chains; typed exits 23/24.
- Author local evidence: PLAYBACK-ATTR-3-CUDA suites + tools/profiling pass locally; contract review deferred until hosted checks green (contract v2.1). CoS did not re-run (Windows factory; review via gh only).
- Scoped-out follow-ups (author, card DISPLAY-IDENTITY-HARNESS-HARDENING-1): low-sample flag; live-process test for `SMOKE_LOG_UNAVAILABLE` PresentMon stop; tighter post-spawn stamp; empirical anchor validation.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green** UNCHANGED: content sound; only tip required checks / branch protection blocking. Repo Hygiene (esp. windows PresentMon unit suites) is the first signal; GUI Pilot / Product Oracles are the usual long poles.
