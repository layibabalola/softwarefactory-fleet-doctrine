schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 149
head: 0b526560ae670cf17c02efe07450ffa5a700ecf5
reviewed_utc: 2026-09-22T03:36:09Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 0b526560 (merge of #147 round 6 / 024cc2de onto prior CoS head d8301b02; PR diff vs base still +628/−217, 9 files): Protected Check Route PASS; other tip checks IN_PROGRESS/pending (Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, Windows Product Oracles, Windows GUI Pilot); mergeable_state unstable; 0 GitHub reviews.

## Blockers

(none in the reviewed tip — wait for required checks on 0b526560, especially Repo Hygiene Python for `tools/repo_hygiene/test_gpu_window_recon_shader_static.py`, Batch Compile for shared `GpuPreviewProcessing` + window/MainWindow surface, and Windows GUI Pilot for recon refusal / LUT-readiness / post-WB-undo green-cast tests plus the inherited `MLV_SKIP_OR_FAIL_IF_READBACK_FAILED` contract from #147 round 6; mergeable_state unstable until checks settle. Still stacked on open #147 — do not merge to master until #147 lands and this PR is retargeted.)

## Improvements

- Soft: `GpuDisplayWindow.h` still `#include`s `GpuDisplayViewport.h` solely for `PresentationOptions` (unchanged vs prior CoS). If more shared presentation types appear, extract them (and LUT set types already in `GpuPreviewProcessing.h`) into a thin shared header so the window route does not depend on the viewport widget type. Not a merge blocker.
- Soft: **0 GitHub reviews** on a display-path / shader-sharing change that closes a visible dark-green regression; a short human glance at `GpuDisplayWindow::paintGL` `activeProgram` selection and the fail-closed LUT gate is still cheap before merge. Not cleared by this push.

## Notes

- pr-pushed after prior CoS on d8301b02 (pr-opened, merge-when-ci-green): tip advanced via merge commit 0b526560 picking up #147 round 6 (024cc2de) — CI contract / fail-closed GL readback (no pixmap fallthrough), `QMutexLocker` on `grabPresentedFramebufferIfActive`, and `MLV_SKIP_OR_FAIL_IF_READBACK_FAILED` under `MLV_REQUIRE_GL_TESTS=1`. Net PR diff vs base `product/ATTR3-VISUAL-QUALITY-EVIDENCE-1` remains the round-1c preview-processing share (+628/−217, 9 files); this push is stack hygiene, not a new TEXNR fix.
- Prior CoS soft Improvements (PresentationOptions include; human glance) not addressed by this merge.
- Round 1c substance unchanged: shared `GpuPreviewProcessing` helpers for viewport+window recon textures; fail-closed missing options/LUTs; MainWindow passes `gpuPresentationOptions`; static + GUI coverage. Board before/after captures still board-side; `present_content` provenance limitation still owned by #147.
- #147 remains OPEN (head 024cc2de, mergeStateStatus CLEAN at review time). Base still ATTR3 branch; author plans retarget to master after #147 merges.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped: verdict unchanged and prior Improvements not cleared.
