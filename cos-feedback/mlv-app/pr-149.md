schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 149
head: 0ab2d409828150d689a24d265d2a106a7650e28f
reviewed_utc: 2026-09-22T07:37:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 0ab2d409 (round 4 on prior CoS head 99dedb2c; compare +1 commit / +5 files — shared gpuPreviewProcessingReconTexturePresentationRefused for both paintGL paths, raw-Bayer16 submit LUT gate, bounded glGetError drain + GL_CONTEXT_LOST fail-closed, context-loss recovery asserts green not red; PR vs master +1194/−227, 9 files): all tip checks pending (Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED; 0 GitHub reviews (API); base master.

## Blockers

(none in the reviewed tip — wait for required checks on 0ab2d409, especially Repo Hygiene Python for `tools/repo_hygiene/test_gpu_window_recon_shader_static.py`, Batch Compile for shared `GpuPreviewProcessing` + window/viewport surface, and Windows GUI Pilot / product oracles for live-GL fail-closed + context-loss recovery tests and recon refusal gates; mergeable_state BLOCKED until checks settle.)

## Improvements

- Soft: `GpuDisplayWindow.h` still `#include`s `GpuDisplayViewport.h` solely for `PresentationOptions` / `SamplingMode` (unchanged vs prior CoS; confirmed on tip — round 4 does not touch the include). If more shared presentation types appear, extract them (and LUT set types already in `GpuPreviewProcessing.h`) into a thin shared header so the window route does not depend on the viewport widget type. Not a merge blocker.
- Soft: **0 formal GitHub reviews** remain on the PR API. Round 4 claims it addresses further sol minor / fable minor findings (shared refusal production function, raw-Bayer16 submit gate, bounded GL drain, contradictory assertion). Those are reflected in the tip, but there is still no submitted GitHub review thread to close. A short human glance at `gpuPreviewProcessingReconTexturePresentationRefused`, the raw-Bayer16 submit gate in `GpuDisplayViewport::setPresentedGpuPlaybackReconTexture`, and the bounded `glGetError` drain is still cheap before merge. Not a merge blocker.

## Notes

- pr-pushed after prior CoS on 99dedb2c (pr-pushed, merge-when-ci-green). Round 4 (0ab2d409) is real product delta vs that head:
  - Both `GpuDisplayWindow::paintGL` and `GpuDisplayViewport::paintGL` call shared `gpuPreviewProcessingReconTexturePresentationRefused()` instead of re-encoding the formula locally; the GPU-free formula test now exercises that production function (plus missing-pointer and disabled-config cases).
  - `GpuDisplayViewport::setPresentedGpuPlaybackReconTexture` (raw-Bayer16) now re-checks LUT readiness at submit time, matching the AMaZE route, so MainWindow fallback runs instead of a false-success present that paintGL later clears to black.
  - `gpuPreviewProcessingUpdateLutTextureSet` bounds the pre-upload `glGetError` drain at 16 iterations and treats `GL_CONTEXT_LOST` (0x0507) as immediate destroy/not-ready.
  - Context-loss recovery gui_smoke assertion fixed: submitted green frame now expects green-dominant center (was wrongly asserting red).
- Prior CoS soft Improvement on PresentationOptions include: **not** cleared.
- Prior CoS soft Improvement on human glance / GH review record: partially reflected (named sol/fable findings fixed in tip) but **not** cleared as a GitHub review record (reviews=[]).
- Round 1c substance vs master unchanged in intent: shared `GpuPreviewProcessing` helpers; MainWindow passes `gpuPresentationOptions`; static check that window `paintGL` is not unconditional passthrough. Board before/after captures still board-side; `present_content` provenance limitation from merged #147 part 1 remains disclosed.
- #147 MERGED; base is `master`. Ready to merge when required CI is green.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
