schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 149
head: 99dedb2cd7d6fca74ede02d1325f24eb92ca36d3
reviewed_utc: 2026-09-22T06:10:57Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 99dedb2c (round 3 on prior CoS head 92cf6934; compare +1 commit / +5 files — viewport recon fail-closed parity at submit+paintGL, glGetError LUT upload verification before signatureValid, window cleanupGLResources makeCurrent + m_textureDirty re-arm, gui_smoke refusal formula + live-GL refuse/recovery; PR vs master +1136/−227, 9 files): all tip checks IN_PROGRESS/pending (Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED; 0 GitHub reviews (API); base master.

## Blockers

(none in the reviewed tip — wait for required checks on 99dedb2c, especially Repo Hygiene Python for `tools/repo_hygiene/test_gpu_window_recon_shader_static.py`, Batch Compile for shared `GpuPreviewProcessing` + window/viewport surface, and Windows GUI Pilot / product oracles for the new live-GL fail-closed + context-loss recovery tests and recon refusal gates; mergeable_state BLOCKED until checks settle.)

## Improvements

- Soft: `GpuDisplayWindow.h` still `#include`s `GpuDisplayViewport.h` solely for `PresentationOptions` / `SamplingMode` (unchanged vs prior CoS; confirmed on tip — round 3 only adds `friend class GuiSmokeTest`). If more shared presentation types appear, extract them (and LUT set types already in `GpuPreviewProcessing.h`) into a thin shared header so the window route does not depend on the viewport widget type. Not a merge blocker.
- Soft: **0 formal GitHub reviews** remain on the PR API. Round 3 claims it addresses further sol major / fable minor findings (viewport draw-time recon refuse, LUT upload glGetError, cleanup makeCurrent + dirty re-arm) — those are reflected in the tip, but there is still no submitted GitHub review thread to close. A short human glance at `GpuDisplayViewport::paintGL` reconRefused and `gpuPreviewProcessingUpdateLutTextureSet` upload-error path is still cheap before merge. Not a merge blocker.

## Notes

- pr-pushed after prior CoS on 92cf6934 (pr-pushed, merge-when-ci-green). Round 3 (99dedb2c) is real product delta vs that head:
  - `GpuDisplayViewport::setPresentedGpuPlaybackReconAmazePostWbTexture` re-checks shared LUT readiness after `updateProcessingTexturesIfNeeded` (submit-time gate); `paintGL()` re-checks again and refuses rather than falling through to `previewProcessingEnabled=0`.
  - `gpuPreviewProcessingUpdateLutTextureSet` drains/checks `glGetError` around the five LUT uploads; only stamps `signatureValid` on a clean upload.
  - `GpuDisplayWindow::cleanupGLResources` `makeCurrent()`s before deleting GL wrappers (viewport parity) and re-arms `m_textureDirty` when a QImage is still retained.
  - GUI smoke: GPU-free shared refusal formula for both presenters; live-GL viewport draw-time refuse; live-GL real `cleanupGLResources` teardown/rebuild recovery (`GuiSmokeTest` friend on window).
- Prior CoS soft Improvement on PresentationOptions include: **not** cleared.
- Prior CoS soft Improvement on human glance / GH review record: partially reflected (named sol/fable findings fixed in tip) but **not** cleared as a GitHub review record (reviews=[]).
- Round 1c substance vs master unchanged in intent: shared `GpuPreviewProcessing` helpers; MainWindow passes `gpuPresentationOptions`; static check that window `paintGL` is not unconditional passthrough. Board before/after captures still board-side; `present_content` provenance limitation from merged #147 part 1 remains disclosed.
- #147 MERGED; base is `master`. Ready to merge when required CI is green.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).