schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 149
head: 92cf6934c34c3878a0e120791b3918e749c265d2
reviewed_utc: 2026-09-22T05:03:51Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 92cf6934 (round 2 on prior CoS head b5764cbd; compare +1 commit / +5 files — Viewport gate, Window cleanupGL+sampling, PreviewProcessing signatureValid fail-closed, gui_smoke readiness rewrite; PR vs master +816/−224, 9 files): all tip checks IN_PROGRESS/QUEUED (Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED; 0 GitHub reviews (API); base master.

## Blockers

(none in the reviewed tip — wait for required checks on 92cf6934, especially Repo Hygiene Python for `tools/repo_hygiene/test_gpu_window_recon_shader_static.py`, Batch Compile for shared `GpuPreviewProcessing` + window/viewport surface, and Windows GUI Pilot / product oracles for the rewritten LUT-readiness + live-GL fail-closed tests and recon refusal gates; mergeable_state BLOCKED until checks settle.)

## Improvements

- Soft: `GpuDisplayWindow.h` still `#include`s `GpuDisplayViewport.h` solely for `PresentationOptions` / `SamplingMode` (unchanged vs prior CoS; confirmed on tip — round 2 even threads `GpuDisplayViewport::SamplingMode` into `applySamplingMode`). If more shared presentation types appear, extract them (and LUT set types already in `GpuPreviewProcessing.h`) into a thin shared header so the window route does not depend on the viewport widget type. Not a merge blocker.
- Soft: **0 formal GitHub reviews** remain on the PR API. Round 2 claims it addresses sol round-1 CHANGES_REQUESTED and fable round-1 minor (LUT signatureValid, context-loss cleanup, sampling parity, viewport gate symmetry) — those findings are reflected in the tip, but there is still no submitted GitHub review thread to close. A short human glance at `GpuDisplayWindow::paintGL` `activeProgram` selection and the fail-closed LUT gate is still cheap before merge. Not a merge blocker.

## Notes

- pr-pushed after prior CoS on b5764cbd (pr-pushed, merge-when-ci-green, stack-retarget only). Round 2 (92cf6934) is real product delta vs that head:
  - `gpuPreviewProcessingLutTextureSetReady` now requires `signatureValid` (set only after every LUT texture is confirmed GL-created); allocation failure destroys the whole set instead of half-built ready.
  - `GpuDisplayWindow` gains `cleanupGLResources()` wired to `QOpenGLContext::aboutToBeDestroyed` (parity with viewport) so context recreation cannot leave stale non-null GL wrappers.
  - `applySamplingMode` honours `PresentationOptions::samplingMode` (nearest/bicubic vs linear) instead of always forcing linear.
  - `GpuDisplayViewport` recon submit path gets the same fail-closed options/LUT gate as the window (latent asymmetry closed; not reachable today via MainWindow).
  - GUI smoke: tautological LUT-readiness test replaced with GPU-free signature/pointer table + live-GL upload/missing-upload/context-loss test (`MLV_SKIP_OR_FAIL_IF_OFFSCREEN` / `MLV_SKIP_OR_FAIL_IF_READBACK_FAILED`).
- Prior CoS soft Improvement on PresentationOptions include: **not** cleared (still depends on viewport header; SamplingMode coupling increased slightly).
- Prior CoS soft Improvement on human glance: partially reflected (named sol/fable findings fixed in tip) but **not** cleared as a GitHub review record (reviews=[]).
- Round 1c substance vs master unchanged in intent: shared `GpuPreviewProcessing` helpers; MainWindow passes `gpuPresentationOptions`; static check that window `paintGL` is not unconditional passthrough. Board before/after captures still board-side; `present_content` provenance limitation from merged #147 part 1 remains disclosed.
- #147 MERGED; base is `master`. Ready to merge when required CI is green.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
