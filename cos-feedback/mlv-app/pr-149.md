schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 149
head: b5764cbd079a9da7fa6fcff66a0ad6d4ef873d1d
reviewed_utc: 2026-09-22T04:12:40Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip b5764cbd (merge of fork/master 9d85770f after #147 merged onto prior CoS head 0b526560; compare vs 0b526560 is +2 commits / 0 files — stack retarget only; PR diff vs master still +628/−217, 9 files): all tip checks PENDING/QUEUED (Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED; 0 GitHub reviews; base now master.

## Blockers

(none in the reviewed tip — wait for required checks on b5764cbd, especially Repo Hygiene Python for `tools/repo_hygiene/test_gpu_window_recon_shader_static.py`, Batch Compile for shared `GpuPreviewProcessing` + window/MainWindow surface, and Windows GUI Pilot / product oracles for recon refusal / LUT-readiness / post-WB-undo green-cast coverage plus the inherited `MLV_SKIP_OR_FAIL_IF_READBACK_FAILED` contract from merged #147; mergeable_state BLOCKED until checks settle. Stacking on #147 is cleared — #147 merged at 2026-09-22T03:51:03Z (merge commit 9d85770f) and this PR base is now `master`.)

## Improvements

- Soft: `GpuDisplayWindow.h` still `#include`s `GpuDisplayViewport.h` solely for `PresentationOptions` (unchanged vs prior CoS; confirmed on tip). If more shared presentation types appear, extract them (and LUT set types already in `GpuPreviewProcessing.h`) into a thin shared header so the window route does not depend on the viewport widget type. Not a merge blocker.
- Soft: **0 GitHub reviews** on a display-path / shader-sharing change that closes a visible dark-green regression; a short human glance at `GpuDisplayWindow::paintGL` `activeProgram` selection and the fail-closed LUT gate is still cheap before merge. Not cleared by this push.

## Notes

- pr-pushed after prior CoS on 0b526560 (pr-pushed, merge-when-ci-green): tip advanced via merge commit b5764cbd pulling master 9d85770f (merged #147 ATTR3-VISUAL-QUALITY-EVIDENCE-1 part 1) so branch protection sees an up-to-date base. No product-file delta vs prior CoS head (compare file_count 0).
- Prior CoS soft Improvements (PresentationOptions include; human glance) not addressed by this merge.
- Round 1c substance unchanged vs master: shared `GpuPreviewProcessing` helpers for viewport+window recon textures; fail-closed missing options/LUTs; MainWindow passes `gpuPresentationOptions`; static + GUI coverage. Board before/after captures still board-side; `present_content` provenance limitation from #147 part 1 remains disclosed, not newly introduced here.
- #147 is MERGED. Base retargeted from `product/ATTR3-VISUAL-QUALITY-EVIDENCE-1` to `master`. Ready to merge when required CI is green.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped: verdict unchanged and prior Improvements not cleared.
