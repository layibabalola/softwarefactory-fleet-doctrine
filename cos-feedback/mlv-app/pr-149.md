schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 149
head: d8301b026447da8c943fee687361bb2cd1bfc68d
reviewed_utc: 2026-09-22T02:28:25Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip d8301b02 (+628/−217, 9 files vs base product/ATTR3-VISUAL-QUALITY-EVIDENCE-1 / #147): Protected Check Route PASS; other tip checks IN_PROGRESS or queued (Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, Windows Product Oracles, Windows GUI Pilot); mergeable_state unstable; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip d8301b02, especially Repo Hygiene Python covering new `tools/repo_hygiene/test_gpu_window_recon_shader_static.py`, Batch Compile for shared `GpuPreviewProcessing` helpers + `GpuDisplayWindow`/`MainWindow` signature change, and Windows GUI Pilot for `gpuDisplayWindowRefusesReconTextureWithoutProcessingOptions` / LUT-readiness / post-WB-undo green-cast CPU-reference tests; mergeable_state unstable until checks settle. Stacked on open #147 — do not merge to master until #147 lands and this PR is retargeted.)

## Improvements

- Soft: `GpuDisplayWindow.h` now `#include`s `GpuDisplayViewport.h` solely for `PresentationOptions`. If more shared presentation types appear, extract them (and the LUT set types already in `GpuPreviewProcessing.h`) into a thin shared header so the window route does not depend on the viewport widget type. Not a merge blocker.
- Soft: **0 GitHub reviews** on a display-path / shader-sharing change that closes a visible dark-green regression; a short human glance at `GpuDisplayWindow::paintGL` `activeProgram` selection and the fail-closed LUT gate is cheap before merge.

## Notes

- Round 1c (d8301b02) first CoS pass on open: GL-window GPU scale-1 recon texture drew post-WB-undo linear camera RGB through the passthrough shader (no levels/WB/matrix/gamma), producing flat dark grey-green on RTX 3060; viewport route was already correct. Fix moves display-shader ensure + LUT create/update/bind/destroy into shared `GpuPreviewProcessing.{h,cpp}` helpers used by both `GpuDisplayViewport` and `GpuDisplayWindow`; `MainWindow::presentPlaybackPreparedFrame` now passes `task.gpuPresentationOptions` into the window recon-present API.
- Fail-closed: `setPresentedGpuPlaybackReconAmazePostWbTexture` refuses when preview-processing options/LUTs are unusable (`gpu_window_recon_missing_processing_options` / `gpu_window_recon_lut_upload_failed`); `paintGL` re-checks LUT readiness and draws recon textures only through `m_previewProcessingProgram` via `activeProgram` (passthrough reserved for display-referred QImage). Static hygiene test locks that code shape; GUI tests cover refusal, shared LUT readiness, and CPU-reference correction of the post-WB-undo green cast (`MLV_SKIP_OR_FAIL_IF_OFFSCREEN`).
- Board evidence (before/after GL framebuffer captures) kept board-side, not committed. Author discloses texture route still fails fresh-render provenance (`present_content`) — #147 part-1 limitation, not introduced here.
- Base is `product/ATTR3-VISUAL-QUALITY-EVIDENCE-1` (stacked on open #147); author plans retarget to master after #147 merges. Diff vs that base is the right review surface for this tip.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).