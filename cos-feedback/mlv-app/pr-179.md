schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 179
head: d37342a469c2a5b45ce3f7628037e7743008fdc5
reviewed_utc: 2026-09-26T06:12:54Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip d37342a4 (PRESENTMON-HARNESS-ROBUSTNESS-3; +1728/−34, 9 files, 12 commits; ahead 12 / behind 0 vs master). Protected Check Route SUCCESS; CodeQL Analyze (actions/c-cpp/python), Factory Bridge Regressions, Repo Hygiene Python (ubuntu+windows), Batch Compile, Windows GUI Pilot, Windows Product Oracles IN_PROGRESS (Tests 36223014166, Factory Bridge 36223014227, CodeQL 36223013305). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft. Supersedes #178.

## Blockers

(none)

## Improvements

- After tip CI greens: one bachelor PresentMon leg is still the only live proof the three-arm gate and embedded helpers clear `CommandNotFoundException` on a no-checkout host (fixtures cover arms + AST embed contract; hosted Suite does not run PresentMon).
- Coverage arm fallback `playback_smoke.gate` `frames_presented` is coarser than `gpu_window_swaps` (submissions vs confirmed on-screen swaps). Prefer keeping swap telemetry enabled on measured legs so the preferred arm is what clears `ok`.
- Without `--artifacts-dir`, an explicit `--presentmon-status ok` can still authorize any caller-supplied CSV/log pair (by design for hand re-checks). Callers who have a leg artifact root should prefer `--artifacts-dir` so status + data stay bound.

## Notes

- Supersedes parked #178 (contract v2 BLOCKER → PARK/SPLIT). Carries all of #178 (ROBUSTNESS-1/-2: typed PresentMon outcomes, app-side evidence kept, three-arm sufficiency gate with independent coverage, `verified_zero_displayed`, sanitized RESULT reasons, embedded helpers + AST contract test) plus three hub commits on tip:
  1. `162626b736dd` — with `--artifacts-dir`, the leg's published `presentMonStatus` is authoritative; explicit `--presentmon-status` may only agree; contradiction → exit 1, no output; report always carries `presentMonStatus` / `presentMonStatusReason`.
  2. `69da7b8d2c5c` — with `--artifacts-dir`, explicit `--presentmon-csv` / `--frame-log` must `realpath`+`commonpath` resolve inside that directory (same-leg binding); `build_report()` itself refuses `presentmon_status=None` (closes prior CoS Improvement on library path).
  3. `d37342a469c2` — cross-drive `commonpath` `ValueError` → typed refusal; per-binding parametrized outside-path tests + cross-drive case; stale comment fixed. In-lane claim: `test_refresh_period_histogram.py` 47 passed; mutants red their own tests.
- Three-arm `presentMonStatus=ok` gate unchanged from #178 tip (count ≥30; app-swap coverage ≥0.5 via `Get-AttrCudaAppSwapTelemetry`; temporal max gap ≤5000ms via `Get-AttrCudaTemporalCoverage`); helpers embedded; histogram refuses non-`ok`.
- Files touched beyond #178 tip `fbda7e29`: `docs/playback-attr-3-cuda.md`, `tools/profiling/refresh_period_histogram.py`, `tools/profiling/test_refresh_period_histogram.py` only (+178/−4 / +351/−8 / docs).
- Sol #178 BLOCKER and pre-review #1/#2 hardening claimed fixed on tip — CoS confirms authoritative status, same-leg path binding, `build_report` fail-closed, and typed cross-drive refusal in the reviewed head.
- Windows-only PresentMon/bachelor assumptions unchanged. CoS via gh + cursor-github MCP (no Cloud Agent). No GitHub PR review comment (bus only).
