schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: a7f10d26a13b336f615c377159d98ee947f99348
reviewed_utc: 2026-09-22T21:14:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip a7f10d26 (LANE-NO-BACKGROUND-END-TURN-1 round 12: self-test every discoverable shell candidate for a kind, not just the precedence winner; receipt authority.backgroundGateShellValidatedCandidates; deny-reason literal cross-referenced across hook/launcher/test helper; +1 commit / 4 files vs prior CoS 297097fa): tip checks IN_PROGRESS (Analyze actions/c-cpp/python, Factory Bridge Regressions, Protected Check Route, Repo Hygiene Python windows/ubuntu, Batch Compile); mergeable MERGEABLE; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 16 behind_by 0; PR vs master +2353/−35, 4 files (docs/lane-containment.md, tests/coordination/test_lane_containment.py, tools/coordination/Invoke-Lane.ps1, tools/coordination/lane-no-background.py).

## Blockers

(none in the reviewed tip — wait for required checks on a7f10d26, especially Factory Bridge Regressions / Repo Hygiene Python which exercise `tests/coordination` including the new all-candidates self-test loop, kind-disqualification-on-sibling-fail, and deny-reason cross-reference tests; mergeable_state BLOCKED until those settle.)

## Improvements

- Soft (clearly advanced at a7f10d26): round 12 closes the round-11 sub-case where this launcher's self-test proved only the precedence-preferred shell executable while Claude Code's own detection (independent; `shell` pins a KIND not a path; documented pin `env.CLAUDE_CODE_GIT_BASH_PATH` is NA-3-forbidden) could land on a different discoverable sibling. `Resolve-LaneExecutableAllCandidates` gathers every known-location hit plus PATH (override stays exclusive); the launch trusts a kind only when ALL candidates pass both self-test halves; one failing sibling disqualifies the whole kind and falls back (or refuses). Selection of which path is registered still follows override → known-location → PATH. New receipt field `authority.backgroundGateShellValidatedCandidates`. Tests: PATH candidate fails while known-location would pass → kind disqualified / PowerShell fallback; deny-reason substring must match across `lane-no-background.py` DENY_REASON, `$backgroundGateExpectedDenySubstring`, and `_bash_candidate_runs_the_hook`. Residual honesty (documented): still cannot prove Claude Code picks one of the discovered candidates — only that no discoverable-but-broken sibling remains among them. Not a merge blocker if tip CI goes green.
- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse + runtime-resolution + all-candidates self-test change; a short human glance at `Resolve-LaneExecutableAllCandidates`, the kind-gate vs selection split, PowerShell command form (`&` + `exit $LASTEXITCODE`), settings `shell` KIND pin, and the deliberate non-tail of `Invoke-Lane.ps1` is still cheap before merge. Not blocking.
- Soft residual (documented honestly): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.
- Soft residual (still open after round 7b revert; docs still say known gap / card NA10-LANE-NO-BACKGROUND-TAIL-1): `tools/coordination/lane-no-background.py` is **not** an NA-10 guarded tail. A lane that rewrites the worktree source copy can change what a FUTURE launch copies. Pre-launch self-test still catches a broken/non-denying hook for *this* launch. Board-venue follow-up remains open. Not a merge blocker for this PR's stated deny-list + receipt-truthfulness scope.
- Soft residual (round 8 honesty, not newly introduced): post-run tamper check compares the run-dir hook copy at exactly two points (launch write and provider exit). A mid-run rewrite restored before exit leaves hashes matching and the receipt still claims `denied-by-settings-hook`. Round 8 tests modify/delete-at-exit shapes. Not a merge blocker.

## Notes

- This tip vs prior CoS head `297097fa`: one product commit (round 12). Compare 297097fa...a7f10d26 touches `Invoke-Lane.ps1` (+168/−48), `test_lane_containment.py` (+154/−7), `docs/lane-containment.md` (+84/−0), `lane-no-background.py` (+9/−0 comment on DENY_REASON sync). Ahead 16 / behind 0 (was 15 / 0). PR surface +2353/−35 (was +1993/−35).
- Round 12 advances the shell-selection / self-test axis (all discoverable candidates per kind) rather than clearing a separately named prior open Improvement bullet; verdict unchanged: merge-when-ci-green.
- Core PR surface otherwise unchanged: disallowedTools + PreToolUse `lane-no-background.py` + dirty-no-commit → `ended-incomplete`. NA-10 fifth-tail still absent after deliberate 7b revert.
- Producer-claimed local: 167/167 `tests/coordination` (was 164; +2 regression + deny-reason cross-ref). CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped (later-push rule: verdict unchanged; no prior CoS Improvement clearly addressed as a discrete open residual — round 12 is additive product work on the already-advanced shell-selection axis).
