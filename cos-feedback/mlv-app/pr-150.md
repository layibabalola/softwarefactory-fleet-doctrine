schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: d737ebc7e56f6de4f169aba678db070e7b6a8d0b
reviewed_utc: 2026-09-22T14:25:28Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip d737ebc7 (round 6 vs prior CoS 17995c65: +2 commits / 4 files incl. new lane-no-background.py; PR vs master +984/−30, 4 files, ahead_by 6): all tip checks IN_PROGRESS/QUEUED at review (Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); prior tip 17995c65 Tests + Factory Bridge SUCCESS; mergeable MERGEABLE; mergeable_state BLOCKED; 0 GitHub reviews; base master.

## Blockers

(none in the reviewed tip — wait for required checks on d737ebc7, especially Repo Hygiene Python and Factory Bridge Regressions which exercise `tests/coordination` / lane containment; mergeable_state BLOCKED until checks settle.)

## Improvements

- Soft: dirty-no-commit still requires `$BaseSha` (`Invoke-Lane.ps1` pre-launch snapshot + post-exit gate). Confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD (or equivalent); if any path leaves `$BaseSha` null, the false-`complete` hole remains for that path. Not a merge blocker for paths that already pass it.
- Soft: hook interpreter is pinned to absolute `C:/Users/obabalola/AppData/Local/Python/bin/python.exe` (same pin pattern as `.claude/settings.json` / `mlv-never-authorized.py`). Fine on this machine; a missing pin fails open silently for the PreToolUse path. Not a merge blocker given the documented fail-open tradeoff and dirty-no-commit backstop.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse hook change; a short human glance at `lane-no-background.py` (stdout JSON deny protocol, not exit-2) and the round-5 fail-closed `Invoke-GitCaptureUtf8` / `hash-object` identity is still cheap before merge. Not blocking.

## Notes

- Round 6 vs prior CoS head `17995c65` (+2 commits: round 5 `3f420cb9` then round 6 `d737ebc7`): **prior soft Improvement on Bash `run_in_background` CLEARLY ADDRESSED.**
  - Round 5: `Invoke-GitCaptureUtf8` returns `{ok, stdout, exitCode, error}` (never bare string that collides with genuine empty success); capture/hash failure records `dirtyCheck=unavailable` + reason and leaves `workEvidence` untouched. Per-path identity is now `git hash-object` of working-tree bytes (DELETED marker when absent), not a hash of rendered `git diff` text (binary diffs were previously collapsed to identical text).
  - Round 6 (swarm ruling): NA-3 unamended; no `CLAUDE_CODE_*` / `ANTHROPIC_*` / `OPENAI_*` env assignment. New tracked PreToolUse hook `tools/coordination/lane-no-background.py` wired via per-lane `--settings` for every Claude-engine lane (read-only and editing); denies Bash with `run_in_background:true` via `hookSpecificOutput.permissionDecision=deny`; allows other inputs silently. Receipts record `authority.backgroundBash=denied-by-settings-hook`. Codex lanes unaffected. Tests cover settings wiring (RO + editing), deny/allow script cases, and receipt field.
- Prior soft Improvements: BaseSha path and 0 reviews **not cleared**. Bash `run_in_background` soft **cleared** (hard deny via settings hook; dirty-no-commit remains after-the-fact backstop).
- Tip CI not settled at review; round-4 tip CI was green. CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).