schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 76571db71d041e1d19369b3dbf61051f22c647a3
reviewed_utc: 2026-09-22T18:01:52Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 76571db7 (LANE-NO-BACKGROUND-END-TURN-1 round 9: runtime Resolve-LaneExecutable for $PYTHON_EXE / $GIT_BASH_EXE instead of absolute pins; +1 commit / 3 files vs prior CoS 0a1db8b1): tip checks IN_PROGRESS (Factory Bridge Regressions, Protected Check Route, Repo Hygiene Python windows/ubuntu, Batch Compile, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 13 behind_by 0; PR vs master +1635/−35, 4 files (docs/lane-containment.md, tests/coordination/test_lane_containment.py, tools/coordination/Invoke-Lane.ps1, tools/coordination/lane-no-background.py).

## Blockers

(none in the reviewed tip — wait for required checks on 76571db7, especially Factory Bridge Regressions / Repo Hygiene Python which exercise `tests/coordination` and the new resolver/refusal paths; mergeable_state BLOCKED until those settle. Prior hosted-CI failure on pinned `$PYTHON_EXE` is exactly what this tip claims to fix.)

## Improvements

- Soft (clearly advanced at 76571db7): round 9 replaces absolute machine-local pins for `$PYTHON_EXE` and `$GIT_BASH_EXE` with `Resolve-LaneExecutable` (override env `MLV_LANE_PYTHON_EXE` / `MLV_GIT_BASH` authoritative, then known install locations, then PATH last — known-before-PATH to avoid the WSL `System32\bash.exe` stub). Unresolvable either variable refuses launch before a hook command is built (`background-gate-interpreter-not-found` / `background-gate-shell-not-found`); receipt records resolved path + source tier. Four host-layout-independent override tests added. Residual: the Python known-locations list still includes the previous per-user path as a curated candidate (not a hard pin); PATH/override carry hosted CI. Not a merge blocker if tip CI goes green.
- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse + runtime-resolution change; a short human glance at exit-2 protocol, run-dir hook copy + Git-Bash self-test/tamper, resolver order, and the deliberate non-tail of `Invoke-Lane.ps1` is still cheap before merge. Not blocking.
- Soft residual (documented honestly): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.
- Soft residual (still open after round 7b revert; docs still say known gap / card NA10-LANE-NO-BACKGROUND-TAIL-1): `tools/coordination/lane-no-background.py` is **not** an NA-10 guarded tail. A lane that rewrites the worktree source copy can change what a FUTURE launch copies. Pre-launch self-test still catches a broken/non-denying hook for *this* launch. Board-venue follow-up remains open. Not a merge blocker for this PR's stated deny-list + receipt-truthfulness scope.
- Soft residual (round 8 honesty, not newly introduced): post-run tamper check compares the run-dir hook copy at exactly two points (launch write and provider exit). A mid-run rewrite restored before exit leaves hashes matching and the receipt still claims `denied-by-settings-hook`. Round 8 tests modify/delete-at-exit shapes. Not a merge blocker.

## Notes

- This tip vs prior CoS head `0a1db8b1`: one product commit (round 9). Compare 0a1db8b1...76571db7 touches `Invoke-Lane.ps1` (+99/−16), `test_lane_containment.py` (+88/−5), `docs/lane-containment.md` (+27/−9). `lane-no-background.py` unchanged this tip. Ahead 13 / behind 0 (was 12 / 0).
- Prior CoS Improvement on machine-local `$PYTHON_EXE` / `$GIT_BASH_EXE` pins **clearly addressed** (runtime resolve + fail-closed + receipt source tiers + override tests). Verdict unchanged: merge-when-ci-green.
- Core PR surface otherwise unchanged: disallowedTools + PreToolUse `lane-no-background.py` + dirty-no-commit → `ended-incomplete`. NA-10 fifth-tail still absent after deliberate 7b revert.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat reported (later-push rule: prior Improvement clearly addressed).
