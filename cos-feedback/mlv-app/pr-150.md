schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 0a1db8b1801579a06f627da73e38c118363f34cb
reviewed_utc: 2026-09-22T17:06:10Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 0a1db8b1 (merge fork/master into product/LANE-NO-BACKGROUND-END-TURN-1; parents 65184851 + 6942b019 = merged #148 ATTR3-FOOTAGE-STAGE-1; no new product commits on the lane-containment surface vs prior CoS 65184851): tip checks IN_PROGRESS (Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 12 behind_by 0; PR vs master +1450/−34, 4 files unchanged (docs/lane-containment.md, tests/coordination/test_lane_containment.py, tools/coordination/Invoke-Lane.ps1, tools/coordination/lane-no-background.py).

## Blockers

(none in the reviewed tip — wait for required checks on 0a1db8b1, especially Factory Bridge Regressions / Repo Hygiene Python which exercise `tests/coordination` and lane containment; mergeable_state BLOCKED until those settle.)

## Improvements

- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft (partially addressed at 65184851; unchanged this tip): round 8 self-test runs the exact registered `$hookCommand` through pinned `$GIT_BASH_EXE` (`C:\Program Files\Git\bin\bash.exe`), matching Claude Code's Windows shell-form hook path, with a regression that breaks only the interpreter half of the registered string (expects exit 127 via bash while a direct interpreter call still denies with exit 2). Residual: both `$PYTHON_EXE` and `$GIT_BASH_EXE` remain absolute machine-local pins (same class as `.claude/settings.json` / `mlv-never-authorized.py`). Not a merge blocker.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse change; a short human glance at exit-2 protocol, run-dir hook copy + Git-Bash self-test/tamper, and the deliberate non-tail of `Invoke-Lane.ps1` is still cheap before merge. Not blocking.
- Soft residual (documented honestly): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.
- Soft residual (still open after round 7b revert; docs still say known gap / card NA10-LANE-NO-BACKGROUND-TAIL-1): `tools/coordination/lane-no-background.py` is **not** an NA-10 guarded tail. A lane that rewrites the worktree source copy can change what a FUTURE launch copies. Pre-launch self-test still catches a broken/non-denying hook for *this* launch. Board-venue follow-up remains open. Not a merge blocker for this PR's stated deny-list + receipt-truthfulness scope.
- Soft residual (round 8 honesty, not newly introduced): post-run tamper check compares the run-dir hook copy at exactly two points (launch write and provider exit). A mid-run rewrite restored before exit leaves hashes matching and the receipt still claims `denied-by-settings-hook`. Round 8 tests modify/delete-at-exit shapes. Not a merge blocker.

## Notes

- This tip vs prior CoS head `65184851`: merge-only sync. Parents `65184851` (round 8) + `6942b019` (master after #148 merge). Compare 65184851...0a1db8b1 brings in ATTR3 footage-stage files from master; PR vs master product file set is unchanged (same four lane-containment files, +1450/−34). Ahead 12 / behind 0 (was 11 / 0).
- No prior CoS Improvement cleared by this push. Verdict unchanged: merge-when-ci-green.
- Core PR surface unchanged: disallowedTools + PreToolUse `lane-no-background.py` + dirty-no-commit → `ended-incomplete`. NA-10 fifth-tail still absent after deliberate 7b revert.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped (later-push rule: verdict unchanged; no Improvement clearly addressed).
