schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: a29c7cf148a5a1e54074bb08878f815c091fbebb
reviewed_utc: 2026-09-22T22:20:40Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip a29c7cf1 (LANE-NO-BACKGROUND-END-TURN-1 round 13: register PreToolUse background gate in EXEC FORM — command=resolved python + args=[hook copy]; delete rounds 8–12 shell-candidate machinery; receipt authority.backgroundGateForm=exec + backgroundGateHookArgs; docs/tests rewritten; +1 commit / 3 files vs prior CoS a7f10d26): tip checks IN_PROGRESS (Analyze actions/c-cpp/python, Factory Bridge Regressions, Protected Check Route, Repo Hygiene Python windows/ubuntu, Batch Compile; runs under Actions 35791754335 / 35791760059 / 35791760063); mergeable MERGEABLE; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 17 behind_by 0; PR vs master +1799/−35, 4 files (docs/lane-containment.md, tests/coordination/test_lane_containment.py, tools/coordination/Invoke-Lane.ps1, tools/coordination/lane-no-background.py).

## Blockers

(none in the reviewed tip — wait for required checks on a29c7cf1, especially Factory Bridge Regressions / Repo Hygiene Python which exercise `tests/coordination` after the shell-candidate test suite was deleted and replaced with exec-form registration/self-test coverage; mergeable_state BLOCKED until those settle.)

## Improvements

- Soft (clearly addressed at a29c7cf1 vs prior CoS a7f10d26): round 13 closes the shell-selection / candidate-proof axis that rounds 8–12 kept widening. Vendor hooks docs name exec form (`args` present → Claude Code resolves `command` as an executable and spawns it with `args`; no shell; `shell` ignored). Registration is now `{"type":"command","command":$PYTHON_EXE,"args":@($hookCopyPath)}`. Deleted: `Resolve-LaneExecutableAllCandidates`, Git Bash/PowerShell resolvers for the gate, per-kind self-test loop, `$hookCommand` string builders, `background-gate-shell-not-found`, and receipt fields `backgroundGateShellKind`/`Path`/`Source`/`ValidatedCandidates`. Kept: single-winner `Resolve-LaneExecutable` for `$PYTHON_EXE` only (still argv[0]); self-test invokes the same exec form via PowerShell call operator + arg array, still requiring deny-reason substring + positive allow control. New receipt: `authority.backgroundGateForm: 'exec'` and `backgroundGateHookArgs`. Tests: −9 dead shell-candidate tests / helpers; +6 exec-form tests (registration shape, inert overrides, wrong-interpreter discrimination, hook-copy missing/corrupted, not-a-valid-Win32-executable). Residual honesty (documented): `Start-Job`, trailing `&`, `nohup … &` inside an otherwise-foreground call still invisible to the hook. Not a merge blocker if tip CI goes green.
- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse + exec-form registration change; a short human glance at the settings hook entry shape (`command` + `args`, no `shell`), the self-test call-operator form matching registration, interpreter-not-found refusal, and the deliberate non-tail of `Invoke-Lane.ps1` is still cheap before merge. Not blocking.
- Soft residual (documented honestly; unchanged by round 13): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.
- Soft residual (still open after round 7b revert; docs still say known gap / card NA10-LANE-NO-BACKGROUND-TAIL-1): `tools/coordination/lane-no-background.py` is **not** an NA-10 guarded tail. A lane that rewrites the worktree source copy can change what a FUTURE launch copies. Pre-launch self-test still catches a broken/non-denying hook for *this* launch. Board-venue follow-up remains open. Not a merge blocker for this PR's stated deny-list + receipt-truthfulness scope.
- Soft residual (round 8 honesty, not newly introduced): post-run tamper check compares the run-dir hook copy at exactly two points (launch write and provider exit). A mid-run rewrite restored before exit leaves hashes matching and the receipt still claims `denied-by-settings-hook`. Round 8 tests modify/delete-at-exit shapes. Not a merge blocker.

## Notes

- This tip vs prior CoS head `a7f10d26`: one product commit (round 13). Compare a7f10d26...a29c7cf1 touches `Invoke-Lane.ps1` (+84/−315), `test_lane_containment.py` (+180/−361), `docs/lane-containment.md` (+111/−253); `lane-no-background.py` unchanged in this tip. Ahead 17 / behind 0 (was 16 / 0). PR surface +1799/−35 (was +2353/−35 — net shrink from deleting shell-candidate machinery).
- Round 13 clearly addresses the prior CoS Improvement on the shell-selection / all-candidates self-test axis by replacing that approach wholesale with vendor-documented exec form rather than proving another shell-candidate hole. Verdict unchanged: merge-when-ci-green (wait tip CI).
- Core PR surface otherwise unchanged: disallowedTools + PreToolUse `lane-no-background.py` + dirty-no-commit → `ended-incomplete`. NA-10 fifth-tail still absent after deliberate 7b revert.
- Producer-claimed local: 164/164 `tests/coordination` (was 167; −9 shell-candidate +6 exec-form). Producer also claims end-to-end proof through the registered settings file from a real lane launch: background Bash and background PowerShell both deny (exit 2 + deny reason); ordinary foreground allows (exit 0). CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
