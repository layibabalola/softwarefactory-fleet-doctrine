schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 297097fa334a0abb37319957c88b39aebd0646ae
reviewed_utc: 2026-09-22T19:49:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 297097fa (LANE-NO-BACKGROUND-END-TURN-1 round 11: classify shells by self-test not existence; fall back through every resolved candidate; require deny-reason text plus positive-control ALLOW before trusting a self-test; +1 commit / 3 files vs prior CoS 560d5407): tip checks IN_PROGRESS / mixed (Protected Check Route pass; Factory Bridge Regressions, Repo Hygiene Python windows/ubuntu, Batch Compile, Analyze actions/c-cpp/python, Windows GUI Pilot, Windows Product Oracles pending); mergeable MERGEABLE; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 15 behind_by 0; PR vs master +1993/−35, 4 files (docs/lane-containment.md, tests/coordination/test_lane_containment.py, tools/coordination/Invoke-Lane.ps1, tools/coordination/lane-no-background.py).

## Blockers

(none in the reviewed tip — wait for required checks on 297097fa, especially Factory Bridge Regressions / Repo Hygiene Python which exercise `tests/coordination` including the new WSL-stub fallback and missing-hook-copy-path self-test refusal tests; mergeable_state BLOCKED until those settle.)

## Improvements

- Soft (clearly advanced at 297097fa): round 11 closes the round-10 sub-case where Git Bash *resolved* by file existence (e.g. System32 WSL `bash.exe` stub) but then failed its self-test, and the launch was refused without trying PowerShell. Selection is now a preference-ordered loop over every resolved candidate (bash, then powershell); the self-test *is* the classification; refusal fires only when every candidate fails. Soft (also clearly advanced): exit 2 alone is no longer sufficient — self-test requires the hook deny-reason substring (`headless lane`) in output and a positive control (same registered command, non-background payload, exit 0) before trusting a candidate. Tests: fallback when resolved-but-unusable bash decoy (cmd.exe copy) succeeds on PowerShell; refuse when registered hook-copy path is missing (Python exit 2 without deny text); sibling bad-interpreter test now also forces PowerShell unresolvable so bash breakage decides. Test helper `_find_real_git_bash` validates candidates the same way. Docs addendum in `docs/lane-containment.md` (file already over the 12 KB cap, flagged in commit). Not a merge blocker if tip CI goes green.
- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse + runtime-resolution + PowerShell-fallback + self-test-classification change; a short human glance at the candidate loop, deny-reason + positive-control protocol, PowerShell command form (`&` + `exit $LASTEXITCODE`), settings `shell` pin, and the deliberate non-tail of `Invoke-Lane.ps1` is still cheap before merge. Not blocking.
- Soft residual (documented honestly): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.
- Soft residual (still open after round 7b revert; docs still say known gap / card NA10-LANE-NO-BACKGROUND-TAIL-1): `tools/coordination/lane-no-background.py` is **not** an NA-10 guarded tail. A lane that rewrites the worktree source copy can change what a FUTURE launch copies. Pre-launch self-test still catches a broken/non-denying hook for *this* launch. Board-venue follow-up remains open. Not a merge blocker for this PR's stated deny-list + receipt-truthfulness scope.
- Soft residual (round 8 honesty, not newly introduced): post-run tamper check compares the run-dir hook copy at exactly two points (launch write and provider exit). A mid-run rewrite restored before exit leaves hashes matching and the receipt still claims `denied-by-settings-hook`. Round 8 tests modify/delete-at-exit shapes. Not a merge blocker.

## Notes

- This tip vs prior CoS head `560d5407`: one product commit (round 11). Compare 560d5407...297097fa touches `Invoke-Lane.ps1` (+105/−48), `test_lane_containment.py` (+91/−2), `docs/lane-containment.md` (+25/−0). `lane-no-background.py` unchanged this tip. Ahead 15 / behind 0 (was 14 / 0). PR surface +1993/−35 (was +1822/−35).
- Round 11 advances the round-10 PowerShell-fallback / self-test story (usable-not-merely-resolved classification + deny-reason + positive control) rather than clearing a separately named prior open Improvement bullet; verdict unchanged: merge-when-ci-green.
- Core PR surface otherwise unchanged: disallowedTools + PreToolUse `lane-no-background.py` + dirty-no-commit → `ended-incomplete`. NA-10 fifth-tail still absent after deliberate 7b revert.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped (later-push rule: verdict unchanged; no prior CoS Improvement clearly addressed as a discrete open residual — round 11 is additive product work on the already-advanced shell-selection axis).