schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 595b582a28f047e2f385fc44e297e8a7dff49b53
reviewed_utc: 2026-09-22T06:02:55Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 595b582a (round 3 vs prior CoS 48a1cfa: +1 commit / 3 files; PR vs master +393/−25, 3 files, ahead_by 3): Protected Check Route pass; remaining tip checks IN_PROGRESS/QUEUED (Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python, Windows GUI Pilot, Windows Product Oracles); mergeable MERGEABLE; mergeable_state BLOCKED; 0 GitHub reviews; base master.

## Blockers

(none in the reviewed tip — wait for required checks on 595b582a, especially Repo Hygiene Python and Factory Bridge Regressions which exercise `tests/coordination` / lane containment; mergeable_state BLOCKED until checks settle.)

## Improvements

- Soft: dirty-no-commit still requires `$BaseSha` (`Invoke-Lane.ps1` pre-launch snapshot + post-exit gate). Confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD (or equivalent); if any path leaves `$BaseSha` null, the false-`complete` hole remains for that path. Not a merge blocker for paths that already pass it.
- Soft: **`Bash` `run_in_background` is not technically blocked** (documented honestly in `docs/lane-containment.md`; round 3 also states dirty-no-commit never sees background work that touches no tracked file). Containment for that path is after-the-fact dirty-no-commit + brief prohibition, not a hard deny. Acceptable given editing lanes need Bash; worth remembering if background shells reappear in receipts.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness change; a short human glance at the content-identity dirty map and the expanded nine-tool deny list is still cheap before merge. Not blocking.

## Notes

- Round 3 vs prior CoS head `48a1cfa`: three substantive tightenings (sol/fable minors), not a no-op.
  - Deny list grows to nine tools: `Workflow` and `TaskCreate` join Agent/Task/Monitor/ScheduleWakeup/CronCreate/CronDelete/RemoteTrigger. Same single `$DENIED_TOOLS_DISPLAY` still feeds pre-reservation rejection, `--disallowedTools`, and receipt authority; rejection tests parametrized over the new names.
  - Dirty-no-commit upgrades from porcelain **line-text** compare to per-path **content identity** (`Get-TrackedDirtyContentIdentity`: sha256 of `git diff HEAD -- <path>`). Catches a further edit to an already-dirty tracked file whose status line stays ` M path`. New fixture `MLV_FIXTURE_FURTHER_EDIT_TRACKED_PATH` + test pins the case. Still Claude `-AllowEdits` only; still appends `workEvidence.reason`; still ignores untracked-only / clean / pre-existing-unchanged / HEAD-moved / Codex / read-only.
  - Docs NA-3 rationale corrected: this PR does not ADD any `ANTHROPIC_`/`OPENAI_`/`CLAUDE_CODE_` assignment; pre-existing `CLAUDE_CODE_EFFORT_LEVEL` (8ad69168) is out of scope and tracked under card NA3-CHILD-ENV-SCOPE-1. Honest disclosure of what dirty-no-commit does **not** catch (background work with no tracked-file delta).
- Prior soft Improvements: BaseSha path, Bash `run_in_background`, and 0 reviews **not cleared**. Env-flag note remains cleared from round 2 (flag removed).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).