schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 65184851da81a315b546018d0125f632475ab79f
reviewed_utc: 2026-09-22T16:41:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 65184851 (round 8: self-test exact registered $hookCommand through Git Bash; tamper regression tests; two-point-in-time tamper honesty; +1 commit / 3 files vs prior CoS c3cbd96f): tip checks IN_PROGRESS (Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 11 behind_by 0; PR vs master +1450/−34, 4 files (docs/lane-containment.md, tests/coordination/test_lane_containment.py, tools/coordination/Invoke-Lane.ps1, tools/coordination/lane-no-background.py).

## Blockers

(none in the reviewed tip — wait for required checks on 65184851, especially Factory Bridge Regressions / Repo Hygiene Python which exercise `tests/coordination` and lane containment; mergeable_state BLOCKED until those settle.)

## Improvements

- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft (partially addressed this tip): round 8 self-test now runs the exact registered `$hookCommand` through pinned `$GIT_BASH_EXE` (`C:\Program Files\Git\bin\bash.exe`), matching Claude Code's Windows shell-form hook path, and a regression breaks only the interpreter half of the registered string (expects exit 127 via bash while a direct interpreter call still denies with exit 2). Residual: both `$PYTHON_EXE` and `$GIT_BASH_EXE` remain absolute machine-local pins (same class as `.claude/settings.json` / `mlv-never-authorized.py`). Not a merge blocker.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse change; a short human glance at exit-2 protocol, run-dir hook copy + Git-Bash self-test/tamper, and the deliberate non-tail of `Invoke-Lane.ps1` is still cheap before merge. Not blocking.
- Soft residual (documented honestly): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.
- Soft residual (still open after round 7b revert; docs still say known gap / card NA10-LANE-NO-BACKGROUND-TAIL-1): `tools/coordination/lane-no-background.py` is **not** an NA-10 guarded tail. A lane that rewrites the worktree source copy can change what a FUTURE launch copies. Pre-launch self-test still catches a broken/non-denying hook for *this* launch. Board-venue follow-up remains open. Not a merge blocker for this PR's stated deny-list + receipt-truthfulness scope.
- Soft residual (round 8 honesty, not newly introduced): post-run tamper check compares the run-dir hook copy at exactly two points (launch write and provider exit). A mid-run rewrite restored before exit leaves hashes matching and the receipt still claims `denied-by-settings-hook`. Round 8 now tests modify/delete-at-exit shapes. Not a merge blocker.

## Notes

- Round 8 vs prior CoS head `c3cbd96f`: `Invoke-Lane.ps1` builds `$hookCommand` once for both `--settings` and self-test; self-test is `& $GIT_BASH_EXE -c $hookCommand` instead of round 7's `& $PYTHON_EXE $hookCopyPath`. Tests add registered-command bad-interpreter refusal plus post-run tamper modify/delete coverage. Docs state two-point-in-time tamper limits and keep the NA-10 fifth-tail gap open.
- Author claim: `py -3 -m pytest tests/coordination -q -p no:cacheprovider` → 157 passed. Tip CI still settling at review time.
- Core PR surface unchanged in intent: disallowedTools + PreToolUse `lane-no-background.py` + dirty-no-commit → `ended-incomplete`. NA-10 fifth-tail still absent after deliberate 7b revert.
- Verdict unchanged: merge-when-ci-green. Soft Improvement on self-test fidelity clearly advanced (registered-string × Git Bash); pin locality and NA-10 gap remain. CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
