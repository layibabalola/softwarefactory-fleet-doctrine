schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 48a1cfa64c95149ca95d5f2a256c8083865bbdbc
reviewed_utc: 2026-09-22T05:41:11Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 48a1cfa (+299/−19, 3 files vs master; +1 commit / round 2 vs prior CoS 13fbf8dc): all tip checks IN_PROGRESS/QUEUED (Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED; 0 GitHub reviews; base master.

## Blockers

(none in the reviewed tip — wait for required checks on 48a1cfa, especially Repo Hygiene Python and Factory Bridge Regressions which exercise `tests/coordination` / lane containment; mergeable_state BLOCKED until checks settle.)

## Improvements

- Soft: dirty-no-commit still requires `$BaseSha` (`Invoke-Lane.ps1` pre-launch snapshot + post-exit gate). Confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD (or equivalent); if any path leaves `$BaseSha` null, the false-`complete` hole remains for that path. Not a merge blocker for paths that already pass it.
- Soft: **`Bash` `run_in_background` is not technically blocked** (documented honestly in `docs/lane-containment.md`). Containment for that path is after-the-fact dirty-no-commit + brief prohibition, not a hard deny. Acceptable given editing lanes need Bash; worth remembering if background shells reappear in receipts.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness change; a short human glance at the porcelain delta filter and the `-AllowEdits`/Claude-only gate is still cheap before merge. Not blocking.

## Notes

- Round 2 vs prior CoS head `13fbf8dc`: hub NA-3 (`docs/never-authorized.json`) forbids any `CLAUDE_CODE_*` assignment, so `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS` and receipt `authority.backgroundTasks` were **removed** (addresses prior CoS soft Improvement on the unverified env flag). Containment now rests on `--disallowedTools`, dirty-no-commit receipt, and brief-level prohibition.
- Single `$DENIED_TOOLS` / `$DENIED_TOOLS_DISPLAY` constant now feeds pre-reservation allowlist rejection, `--disallowedTools` argv, and receipt authority (closes round-1 drift where rejection checked only Agent/Task while deny list had seven tools). Parametrized tests cover all seven tools individually.
- Dirty-no-commit (sol major 1 / fable minor 3): gated to `-AllowEdits` Claude lanes only; compares pre-launch vs post-exit tracked porcelain; pre-existing dirt ignored; appends to existing `workEvidence.reason` rather than overwriting. Tests pin: lane-introduced dirt → ended-incomplete; untracked-only / clean / pre-existing dirt / HEAD-moved-with-leftover-dirt / dirty Codex / dirty read-only Claude → stay complete.
- Hub claim: `tests/coordination/test_lane_containment.py` 43 passed; `tools/coordination/test_coordination_guardrails.py` 240 passed (not re-run here).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior soft Improvements: env-flag note **cleared** (removed); BaseSha path + 0 reviews **not cleared**.