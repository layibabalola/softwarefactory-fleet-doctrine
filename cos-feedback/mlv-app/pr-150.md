schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 13fbf8dc8584d12bd9bc60ed10d42573489119d2
reviewed_utc: 2026-09-22T05:02:45Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 13fbf8dc (+130/−14, 3 files: Invoke-Lane.ps1, test_lane_containment.py, docs/lane-containment.md): Protected Check Route PASS; remaining tip checks PENDING (Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED; 0 GitHub reviews; base master.

## Blockers

(none in the reviewed tip — wait for required checks on 13fbf8dc, especially Repo Hygiene Python and Factory Bridge Regressions which exercise `tests/coordination` / lane containment; mergeable_state BLOCKED until checks settle.)

## Improvements

- Soft: dirty-worktree override only runs when `$BaseSha` is set (`Invoke-Lane.ps1` after child exit). Confirm every headless Claude implementer launch path that should get this honesty still passes `-BaseSha` (or equivalent); if any path omits it, the false-`complete` hole remains. Not a merge blocker for the paths that already pass it.
- Soft: `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS` is verified by binary string scan / installed CLI, not `--help`. If the installed Claude Code build ever drops or renames that env, the deny-list token is still the primary guard — worth a one-line note in `docs/lane-containment.md` that the env is belt-and-braces and the deny list is authoritative. Doc already covers the deny list; optional sharpen only.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness change; a short human glance at the porcelain tracked-dirty filter and the `ended-incomplete` state mapping is still cheap before merge. Not blocking.

## Notes

- Addresses four consecutive 2026-09-22 headless `claude -p` implementer lanes that ended on "I'll resume when the background job completes" with uncommitted work and false `complete` receipts (GPU-TEXNR-S1 r1/r1b, ATTR3-VISUAL-QUALITY r6, ATTR3-FOOTAGE-STAGE r6 per PR body).
- Claude-only: extends `--disallowedTools` to `Agent,Task,Monitor,ScheduleWakeup,CronCreate,CronDelete,RemoteTrigger` (still one comma-separated token), sets `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1`, records both in receipt `authority`; Codex paths unchanged (env absent).
- Receipt honesty: HEAD still at lane `baseSha` + tracked porcelain dirt forces `workEvidence.reason: dirty-worktree-no-commit` → `ended-incomplete` / `complete=false`; untracked-only dirt ignored. Tests cover tracked dirt, untracked-only, and clean tree; argv/authority assertions updated; hub claim 347 pytest coordination + 1270 unittest OK at 13fbf8dc (not re-run here).
- Docs updated in `docs/lane-containment.md` to match.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).