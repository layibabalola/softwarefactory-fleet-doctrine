schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 17995c65d814eefd737ba52c22cbdff3c13c69ec
reviewed_utc: 2026-09-22T06:49:35Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 17995c65 (round 4 vs prior CoS 595b582a: +1 commit / 3 files; PR vs master +538/−26, 3 files, ahead_by 4): all tip checks IN_PROGRESS at review (Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BLOCKED; 0 GitHub reviews; base master.

## Blockers

(none in the reviewed tip — wait for required checks on 17995c65, especially Repo Hygiene Python and Factory Bridge Regressions which exercise `tests/coordination` / lane containment; mergeable_state BLOCKED until checks settle.)

## Improvements

- Soft: dirty-no-commit still requires `$BaseSha` (`Invoke-Lane.ps1` pre-launch snapshot + post-exit gate). Confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD (or equivalent); if any path leaves `$BaseSha` null, the false-`complete` hole remains for that path. Not a merge blocker for paths that already pass it.
- Soft: **`Bash` `run_in_background` is not technically blocked** (documented honestly in `docs/lane-containment.md`; dirty-no-commit never sees background work that touches no tracked file). Containment for that path is after-the-fact dirty-no-commit + brief prohibition, not a hard deny. Acceptable given editing lanes need Bash; worth remembering if background shells reappear in receipts.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness change; a short human glance at the content-identity dirty map (now `-z` + UTF-8 capture) and the Claude-gated nine-tool deny preflight is still cheap before merge. Not blocking.

## Notes

- Round 4 vs prior CoS head `595b582a`: three substantive tightenings (sol major + minors), not a no-op.
  - Denied-tool preflight (`nested-agent-tool-forbidden`) now explicitly gated on `$LANES[$Lane].engine -eq 'claude'`, not merely `-AllowEdits`. Previously unreachable for codex only by accident of the earlier `codex-lane-never-edits` throw order. New test neutralises that earlier throw and proves a codex allowlist naming a denied tool is not rejected by this Claude-only check.
  - Docs token drift fixed: `docs/lane-containment.md` now quotes the real nine-tool `--disallowedTools` token (`…,Workflow,TaskCreate`) and says "nine denied tools"; new test derives both the token and the English count word from `$DENIED_TOOLS_DISPLAY` in `Invoke-Lane.ps1` (not from the test file's hand-copy alone).
  - `Get-TrackedDirtyContentIdentity` now reads `git status --porcelain=v1 -z` (NUL-separated, quoting off; rename/copy consumes the extra orig-path field) and captures git stdout via `Invoke-GitCaptureUtf8` (`ProcessStartInfo` + explicit UTF-8 `StandardOutputEncoding`) instead of `& git` / console encoding. New fixture path with embedded space + non-ASCII (`trac ked éè.txt`) proves further-edit dirty-no-commit still fires.
- Prior soft Improvements: BaseSha path, Bash `run_in_background`, and 0 reviews **not cleared**. Env-flag note remains cleared from round 2 (flag removed).
- Author claims 53/53 `tests/coordination/test_lane_containment.py` green locally; tip CI not settled at review.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).