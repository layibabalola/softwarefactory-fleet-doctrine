schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: cf20afeb0763f7ffd03884b8f99b2f3bae417f15
reviewed_utc: 2026-09-22T23:33:09Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip cf20afeb (LANE-NO-BACKGROUND-END-TURN-1 round 14: hash + post-run tamper-check the exec-form gate interpreter, not only the hook copy; StrictMode init/Contains fixes; DENY_REASON comment drift fix; docs trimmed; +1 commit / 4 files vs prior CoS a29c7cf1): tip checks mostly IN_PROGRESS (Analyze actions/c-cpp/python, Factory Bridge Regressions, Repo Hygiene Python windows/ubuntu, Batch Compile, Windows Product Oracles, Windows GUI Pilot; Protected Check Route SUCCESS; runs under Actions 35797877405 / 35797880105 / 35797880146); mergeable MERGEABLE; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 18 behind_by 0; PR vs master +1960/−49, 4 files (docs/lane-containment.md, tests/coordination/test_lane_containment.py, tools/coordination/Invoke-Lane.ps1, tools/coordination/lane-no-background.py).

## Blockers

(none in the reviewed tip — wait for required checks on cf20afeb, especially Factory Bridge Regressions / Repo Hygiene Python which exercise `tests/coordination` after +3 interpreter-tamper fixtures and the DENY_REASON comment assertion; mergeable_state BLOCKED until those settle.)

## Improvements

- Soft (clearly addressed at cf20afeb vs prior CoS a29c7cf1): round 14 closes sol's r13 major on the exec-form `command` interpreter. Before this tip only the run-dir hook copy had launch-hash / post-run-rehash; an editing lane's foreground Bash/PowerShell grant could replace or delete the resolved interpreter mid-run after self-test, and vendor hook docs treat a failed hook spawn as non-blocking — so `run_in_background: true` could proceed while the receipt still claimed `denied-by-settings-hook`. Now: hash `$PYTHON_EXE` before self-test into `$backgroundInterpreterLaunchSha256` / receipt `authority.backgroundGateInterpreterSha256`; post-run rehash or missing file overwrites `authority.backgroundGate` to `background-gate-tampered` (same literal as hook-copy tamper, never a softer third state); `backgroundGateTamperDetail` appends when both fire (StrictMode-safe `.Contains('backgroundGateTamperDetail')`). Pre-try init of `$backgroundInterpreterLaunchSha256` mirrors the hook-copy variable so an early throw cannot leave post-run reading an unset var. Tests: fixture `MLV_FIXTURE_TAMPER_INTERPRETER` modify/delete; disposable interpreter byte-copy + `PYTHONHOME` (never corrupts real `sys.executable`); replace / delete / both-with-hook-copy cases. fable minor 2: `lane-no-background.py` DENY_REASON comment no longer names deleted `_bash_candidate_runs_the_hook` / renamed test; consistency test asserts current sites only. Docs: interpreter added to residuals; two-points-in-time limit stated for both hook copy and interpreter; doc size 16889 vs round-8 17066. Not a merge blocker if tip CI goes green.
- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse + exec-form + interpreter-tamper change; a short human glance at interpreter hash timing (before self-test), post-run append-not-overwrite with hook-copy, and StrictMode Contains gating is still cheap before merge. Not blocking.
- Soft residual (documented honestly; round 14 extends the same honesty to the interpreter): tamper checks are still exactly two points in time (launch write / self-test hash, and provider exit). A mid-run rewrite of either the hook copy or the interpreter that is restored before exit leaves both hashes matching and the receipt can still claim `denied-by-settings-hook`. Round 8/14 tests cover modify/delete-at-exit shapes, not restore-before-exit. Not a merge blocker.
- Soft residual (documented honestly; unchanged by round 14): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.
- Soft residual (still open after round 7b revert; docs still say known gap / card NA10-LANE-NO-BACKGROUND-TAIL-1): `tools/coordination/lane-no-background.py` (and `Invoke-Lane.ps1` / the interpreter install location before resolve+hash) are **not** NA-10 guarded tails. A lane that rewrites the worktree source copy can change what a FUTURE launch copies. Pre-launch self-test still catches a broken/non-denying hook for *this* launch. Board-venue follow-up remains open. Not a merge blocker for this PR's stated deny-list + receipt-truthfulness scope.

## Notes

- This tip vs prior CoS head `a29c7cf1`: one product commit (round 14). Compare a29c7cf1...cf20afeb touches `Invoke-Lane.ps1` (+51/−0), `test_lane_containment.py` (+101/−0), `docs/lane-containment.md` (+189/−197), `lane-no-background.py` (+12/−9). Ahead 18 / behind 0 (was 17 / 0). PR surface +1960/−49 (was +1799/−35).
- Round 14 clearly addresses the prior CoS soft residual that post-run tamper covered only the hook copy under exec form by giving the interpreter the same treatment (and documenting the shared two-points-in-time limit). Verdict unchanged: merge-when-ci-green (wait tip CI).
- Core PR surface otherwise unchanged: disallowedTools + PreToolUse `lane-no-background.py` in EXEC FORM + dirty-no-commit → `ended-incomplete`. NA-10 fifth-tail still absent after deliberate 7b revert.
- Producer-claimed local: 167/167 `tests/coordination` (was 164; +3 interpreter-tamper / both-tamper cases); `check_pinned_tokens.py` PASS; real read-only sonnet lane registered `backgroundGateInterpreterSha256` and the registered command+args pair denied background Bash and PowerShell, allowed ordinary foreground. CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).