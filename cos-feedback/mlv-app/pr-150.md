schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 9ffd12b98f4268ea1a243b4ae7ffac902f1ce402
reviewed_utc: 2026-09-22T15:52:25Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 9ffd12b9 (round 7+7b vs prior CoS d737ebc7: +2 commits / 6 files; PR vs master +1370/−39, 6 files, ahead_by 8 behind_by 12): all tip checks IN_PROGRESS (Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable MERGEABLE; mergeable_state BEHIND; 0 GitHub reviews; base master.

## Blockers

(none in the reviewed tip — wait for required checks on 9ffd12b9, especially Repo Hygiene Python and Factory Bridge Regressions which exercise `tests/coordination` / lane containment and the new NA-10 fifth-tail rows; mergeable_state BEHIND until rebased/merged with master and checks settle.)

## Improvements

- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 now distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft: hook interpreter remains pinned to absolute `C:/Users/obabalola/AppData/Local/Python/bin/python.exe` (same pin as `.claude/settings.json` / `mlv-never-authorized.py`). Round 7 adds a pre-launch self-test of that exact interpreter×copy pair (expects exit 2), so a missing/wrong pin fails closed at launch rather than open at first Bash. Residual: pin still machine-local. Not a merge blocker.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse + NA-10 fifth-tail change; a short human glance at exit-2 protocol, run-dir hook copy + self-test/tamper, and deliberate non-tail of `Invoke-Lane.ps1` is still cheap before merge. Not blocking.
- Soft residual (documented honestly in `docs/lane-containment.md`): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.

## Notes

- Round 7 (`861104de`) + 7b (`9ffd12b9`) vs prior CoS head `d737ebc7` (+2 commits / 6 files): **prior soft Improvements CLEARLY ADDRESSED.**
  - Exit-2 / stderr deny protocol (same as `mlv-never-authorized.py`) replaces round-6 stdout-JSON `permissionDecision`; fail-closed on bad/missing stdin; script check is tool-name-agnostic; matcher widened to `Bash|PowerShell`.
  - Per-run copy of hook into board-rooted run slot (not worktree `$PSScriptRoot`); pre-launch self-test; post-run tamper re-hash → `background-gate-tampered`; receipt fields renamed `backgroundGate` + `backgroundHookSha256`.
  - Pre/post `git rev-parse HEAD` routed through `Invoke-GitCaptureUtf8`; failed rev-parse → `unavailable` with which-call reason (clears prior BaseSha misclassification note partially).
  - Round 7b: `tools/coordination/lane-no-background.py` is NA-10's fifth guarded tail (DENY at lane venue / ALLOW at board); `Invoke-Lane.ps1` deliberately NOT a tail (ALLOW pinned); `na10` table count 7→10. Closes the "filed and not yet landed" honesty gap from round-7 docs.
- Soft Improvements still open: BaseSha path coverage for all editing Claude launchers, absolute Python pin locality, 0 GH reviews, residual non-parameter backgrounding routes.
- Tip CI not settled at review; PR behind master by 12. CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
