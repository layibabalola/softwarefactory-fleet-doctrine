schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: c3cbd96f9a4d4ea502747437a29b7b866bd5b52e
reviewed_utc: 2026-09-22T16:24:40Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip c3cbd96f (revert 9ffd12b9 NA-10 fifth-tail for lane-no-background.py; parent 4043bbb2): tip checks mostly IN_PROGRESS (Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python; CodeQL umbrella neutral); mergeable MERGEABLE; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 10 behind_by 0; PR vs master +1320/−34, 4 files (docs/lane-containment.md, tests/coordination/test_lane_containment.py, tools/coordination/Invoke-Lane.ps1, tools/coordination/lane-no-background.py).

## Blockers

(none in the reviewed tip — wait for required checks on c3cbd96f, especially Factory Bridge Regressions / Repo Hygiene Python which exercise `tests/coordination` and lane containment; mergeable_state BLOCKED until those settle.)

## Improvements

- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft: hook interpreter remains pinned to absolute `C:/Users/obabalola/AppData/Local/Python/bin/python.exe` (same pin as `.claude/settings.json` / `mlv-never-authorized.py`). Round 7 pre-launch self-test of that exact interpreter×copy pair (expects exit 2) fails closed at launch if pin is wrong. Residual: pin still machine-local. Not a merge blocker.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse change; a short human glance at exit-2 protocol, run-dir hook copy + self-test/tamper, and the deliberate non-tail of `Invoke-Lane.ps1` is still cheap before merge. Not blocking.
- Soft residual (documented honestly in `docs/lane-containment.md` after this revert): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.
- Soft residual (re-opened by this revert, documented as known gap): `tools/coordination/lane-no-background.py` is **not** an NA-10 guarded tail again. A lane that rewrites the worktree source copy can change what a FUTURE launch copies. Pre-launch self-test still catches a broken/non-denying hook for *this* launch. Board-venue follow-up to re-land the fifth tail (or equivalent) remains open. Not a merge blocker for this PR's stated deny-list + receipt-truthfulness scope.

## Notes

- This push is a deliberate revert of round 7b (`9ffd12b9`): removes `lane-no-background.py` from `NA10_GUARDED_TAILS`, drops the matching `test_mlv_never_authorized.py` rows, and restores docs to "Known gap, not yet closed (round 7)" attributing the NA-10 edit to a board-rooted actor. Core PR surface (disallowedTools + CLI background flag + PreToolUse hook + dirty-no-commit → `ended-incomplete`) is unchanged vs the pre-7b product commits.
- Compare `master...c3cbd96f` lists four LANE-NO-BACKGROUND files only (NA-10 / hygiene test diffs no longer on the PR tip after revert).
- Prior soft Improvements NOT cleared. Soft Improvements still open: BaseSha path coverage, absolute Python pin locality, 0 GH reviews, residual non-parameter backgrounding routes, plus the re-opened NA-10 fifth-tail gap.
- Verdict unchanged: merge-when-ci-green. CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skip: later-push rule (verdict unchanged; no prior Improvement clearly addressed — fifth-tail was re-opened, not closed).