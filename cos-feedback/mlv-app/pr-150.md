schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 150
head: 560d54072c66c5edcac7aa9f451b4d0655077bec
reviewed_utc: 2026-09-22T18:51:10Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 560d5407 (LANE-NO-BACKGROUND-END-TURN-1 round 10: PowerShell fallback when Git Bash absent; pin settings shell field; fixture pins MLV_LANE_PYTHON_EXE; +1 commit / 3 files vs prior CoS 76571db7): tip checks IN_PROGRESS / mixed (Protected Check Route pass; Analyze (actions) pass; CodeQL skipping; Factory Bridge Regressions, Repo Hygiene Python windows/ubuntu, Batch Compile, Analyze c-cpp/python, Windows GUI Pilot, Windows Product Oracles pending); mergeable MERGEABLE; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 14 behind_by 0; PR vs master +1822/−35, 4 files (docs/lane-containment.md, tests/coordination/test_lane_containment.py, tools/coordination/Invoke-Lane.ps1, tools/coordination/lane-no-background.py).

## Blockers

(none in the reviewed tip — wait for required checks on 560d5407, especially Factory Bridge Regressions / Repo Hygiene Python which exercise `tests/coordination` including the new PowerShell-fallback and known-location-before-PATH tests; mergeable_state BLOCKED until those settle.)

## Improvements

- Soft (clearly advanced at 560d5407): round 10 replaces round 9's Git-Bash-or-refuse policy with a registered, self-tested PowerShell fallback. Git Bash stays preferred; when it does not resolve, `Resolve-LaneExecutable` for `MLV_LANE_POWERSHELL_EXE` (System32 `powershell.exe` known location, then PATH) supplies the shell. The hook entry now sets `shell` to `bash` or `powershell` so Claude Code auto-detection cannot disagree with the self-test. PowerShell command form is structurally different (`& "<python>" "<hook>"; exit $LASTEXITCODE`) — measured: bare quoted string is a parse error, and without `; exit $LASTEXITCODE` exit 2 collapses to exit 1 (non-blocking PreToolUse). Receipt records `backgroundGateShellKind` + path/source. Tests cover bash-override success, PowerShell fallback, neither-shell refuse, and known-location-before-PATH with a PATH decoy. Fixture `prepare()` pins `MLV_LANE_PYTHON_EXE=sys.executable` for suite determinism (deliberately does **not** pin Git Bash, so the fallback path stays exercised). Residual: Python known-locations list still includes the previous per-user path as a curated candidate (not a hard pin); PATH/override/fixture pin carry hosted CI. Not a merge blocker if tip CI goes green.
- Soft: dirty-no-commit still requires a successful pre-launch `$BaseSha` path for editing Claude lanes. Round 7 distinguishes failed rev-parse (`dirtyCheck=unavailable` + reason) from a genuine empty base (`not-applicable`); confirm every headless Claude `-AllowEdits` launch path that should get this honesty still resolves a worktree HEAD. Not a merge blocker for paths that already pass it.
- Soft: **0 GitHub reviews** on a lane-containment / receipt-truthfulness + PreToolUse + runtime-resolution + PowerShell-fallback change; a short human glance at the PowerShell command form (`&` + `exit $LASTEXITCODE`), settings `shell` pin, exit-2 protocol, and the deliberate non-tail of `Invoke-Lane.ps1` is still cheap before merge. Not blocking.
- Soft residual (documented honestly): shell built-ins that background without the `run_in_background` parameter (`Start-Job`, trailing `&`, `nohup … &`) remain invisible to the hook; job-object still closes descendants. Not a merge blocker for the stated scope.
- Soft residual (still open after round 7b revert; docs still say known gap / card NA10-LANE-NO-BACKGROUND-TAIL-1): `tools/coordination/lane-no-background.py` is **not** an NA-10 guarded tail. A lane that rewrites the worktree source copy can change what a FUTURE launch copies. Pre-launch self-test still catches a broken/non-denying hook for *this* launch. Board-venue follow-up remains open. Not a merge blocker for this PR's stated deny-list + receipt-truthfulness scope.
- Soft residual (round 8 honesty, not newly introduced): post-run tamper check compares the run-dir hook copy at exactly two points (launch write and provider exit). A mid-run rewrite restored before exit leaves hashes matching and the receipt still claims `denied-by-settings-hook`. Round 8 tests modify/delete-at-exit shapes. Not a merge blocker.

## Notes

- This tip vs prior CoS head `76571db7`: one product commit (round 10). Compare 76571db7...560d5407 touches `Invoke-Lane.ps1` (+86/−25), `test_lane_containment.py` (+99/−11), `docs/lane-containment.md` (+58/−20). `lane-no-background.py` unchanged this tip. Ahead 14 / behind 0 (was 13 / 0). PR surface +1822/−35 (was +1635/−35).
- Round 10 advances the round-9 resolver story (PowerShell fallback + `shell` field + fixture Python pin + precedence test) rather than clearing a separately named prior Improvement bullet; verdict unchanged: merge-when-ci-green.
- Core PR surface otherwise unchanged: disallowedTools + PreToolUse `lane-no-background.py` + dirty-no-commit → `ended-incomplete`. NA-10 fifth-tail still absent after deliberate 7b revert.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped (later-push rule: verdict unchanged; no prior CoS Improvement clearly addressed as a discrete open item — round 10 is additive product work).
