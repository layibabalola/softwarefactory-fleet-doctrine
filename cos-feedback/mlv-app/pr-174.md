schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 174
head: 4d0ed7e2b2000420faef08911ff4371c07a2dd4a
reviewed_utc: 2026-09-26T00:05:05Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 4d0ed7e2 (+465/−7, 5 files, 3 commits; ahead 3 / behind 0): Protected Check Route, Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, CodeQL Analyze (actions/c-cpp/python) all IN_PROGRESS or QUEUED (Tests 36203437921, Factory Bridge 36203438013, CodeQL 36203435753). Required contexts also include Windows GUI Pilot + Windows Product Oracles (not yet reported). MERGEABLE; mergeStateStatus BLOCKED. Combined status pending. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- After bachelor is back: run one live full-screen leg so `presentModes` (and thin-sample `degraded` status) populate against real sample loss — author notes this is still open and bachelor is offline; fixtures cover the shapes but cannot substitute a live PresentMode mix.
- Track the disclosed `Get-FrameRows` / `Get-LastGpuSummary` uncaught-throw gap as a follow-up (author correctly scoped it out of this PresentMon round).

## Notes

- Round-1 defect class for PresentMon: spawn throws (rc=6 ETW / pre-existing output file) used to crash the job with no publish; now typed `PRESENTMON_UNAVAILABLE` / exit 23 with `presentMonStatus=unavailable`.
- Display/wait refusal branches keep app-side evidence (diagnostics, GPU summary/totals, region stats, frame rows on display refusal; frame rows on wait refusal) so a gate-passed measurement is not discarded when PresentMon cannot verify display.
- Success path adds `presentMonStatus` (`ok`|`degraded`|`unavailable`) + reason: `degraded` when display is confirmed but no positive `MsBetweenDisplayChange` intervals (thin NA-first / full-screen sample-loss shape); carried on RESULT line, `summary.json`, and evidence-manifest `presentMon` block.
- `Get-AttrCudaPresentMonDisplayReport` records per-chain and selectedChain `presentModes` (`presentMode` + count) for full-screen sample-loss diagnosis without a live repro.
- Diff: `tools/profiling/bachelor/AttrCudaArtifacts.psm1`, `playback-attr-3-cuda-job.ps1`; tests in `test_playback_attr_3_cuda_behaviour.py`, `…_presentmon_display_report.py`, `…_presentmon_publish_ordering.py`. Author claims four attr-3 suites 328 passed / 293 subtests. CoS did not re-run Windows attr-3 on this box.
- Addresses the #172 CoS Improvement about treating `PRESENTMON_UNAVAILABLE` as a typed harness path with clear evidence. Stale "no positive MsBetweenDisplayChange samples" throw already removed on master by HARNESS-1 (`090370e9`).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content + tests look sound for the three PresentMon defect classes; wait for required checks (incl. Windows GUI Pilot / Product Oracles) before merge. Live bachelor is validation for `presentModes`, not a stated hub merge gate on this PR.
