schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 122
head: 4de11eff26f9bac0ae85d67aee8c7adf9f812f29
reviewed_utc: 2026-09-16T06:47:21Z
event: pr-opened
verdict: not-merge-ready
ci_summary: Protected Check Route PASS on head 4de11eff; CodeQL Analyze (actions/c-cpp/python), Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu/windows), Windows GUI Pilot, Windows Product Oracles, CodeQL workflow still pending (runs 35065233479 Tests, 35065233257 Factory Bridge, 35065231032 CodeQL).

## Blockers

- **CI: required hosted checks** — most still pending on head `4de11eff26f9bac0ae85d67aee8c7adf9f812f29` (runs 35065233479, 35065233257, 35065231032). Wait for green before merge; if red, fix and re-push.
- **Process: Sol HIGH cross-family review (GUARD-PATH)** — `tools/coordination/Invoke-Workstream.ps1` is in `CONTROL_HASHES_BASE` in `tools/hooks/mlv-never-authorized.py`; PR body correctly requires Sol at HIGH effort (same standard as #120). 0 GitHub reviews on this tip. Land that review on head `4de11eff` before merge.

## Improvements

- Worktree reuse on the continuation path sets `$laneWorkDir` to the existing tree but does not assert the tree is clean or checked out at `$ContinueFromSha`. A dirty stage-1 tree (or a worktree briefly detached) would hand stage 2 unexpected state — the PR body already flags this; a `git status --porcelain` / `rev-parse HEAD` fail-closed check before reuse would close it.
- `$script:IsContinuation` / `$script:ContinuationTip` are set on the continuation arm but unused later in `Invoke-Workstream.ps1` (dispatch receipt / attempt row still use the generic path). Either wire them into the dispatch-attempt detail (so ratio-guard forensics can see `continuation`) or drop the dead script vars in a follow-up.
- Optional: one test that reuses an existing worktree (not only a fresh `worktree add`) so the reuse branch is load-bearing under CI, not only the tip-survival assertion.

## Notes

- Scoped review of tip `4de11eff26f9bac0ae85d67aee8c7adf9f812f29` vs `master`: 2 files, +172/−12 (`Invoke-Workstream.ps1`, `test_coordination_guardrails.py`).
- Design matches the card: opt-in `-ContinueFromSha` (40-hex or empty; default preserves `existing-branch-has-work` exit 6); content-addressed tip match; `merge-base --is-ancestor` refuse for divergent branches; `continuation-branch-absent` when the named tip has no branch. `git branch -f` stays only on the no-unique-commits arm — continuation does not rewind.
- Four new tests cover happy path tip survival, sha-mismatch, diverged-from-base, and branch-absent. Author claims 205 pass locally with RED/GREEN evidence (full revert reds all four; neutering only ancestry reds exactly the divergence test). Hosted Factory Bridge / Repo Hygiene not yet settled — that is the automated gate beside Sol.
- Stated against interest in PR body: hub-authored (ratio guard red blocks factory lane); Claude produce / Sol review split preserved; no owner-grant override claimed.
- MERGEABLE tip on `factory/TOOL-DISPATCHER-NO-CONTINUATION-PATH-1` → `master`, mergeStateStatus BLOCKED (checks + process). Verdict **not-merge-ready** until Sol HIGH lands and required CI is green.
- CoS review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (plan / analysis budget). Doctrine bus: published on `master` at `cos-feedback/mlv-app/pr-122.md`.