schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 108
head: b826532c45d2bfa9919bd495a84a3cfca7dd2137
reviewed_utc: 2026-09-14T06:31:14Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: red — Protected Check Route, Factory Bridge Regressions, Repo Hygiene (ubuntu+windows), Batch Compile, Windows GUI Pilot, Windows Product Oracles all fail at checkout (gitlink); CodeQL skipping/neutral; Analyze (actions/c-cpp/python) still in progress; mergeStateStatus BLOCKED

## Blockers

- **Accidental gitlink still on tip:** path `mlvtmptest-worktree-add-20260910T180000` remains mode `160000` → `2f63d24ff4e3ac3bfdaacfc5c152767947a6ce63`. Checkout fails with `fatal: No url found for submodule path 'mlvtmptest-worktree-add-20260910T180000' in .gitmodules` (seen on Protected Check Route, Factory Bridge, hygiene, compile, Windows jobs). `git rm` the gitlink and re-push; do not leave a nested checkout. Latest commit `b826532c` adopted Fable findings but did not drop this path.
- **CI red until gitlink gone:** required checks cannot run past `actions/checkout` while the gitlink remains. Re-check after a clean tip.

## Improvements

- **Scope vs title (unchanged):** tip is now 10 commits / +570 ahead of `master`. Title/body still focus on SAFE worktree retirement + lane scratch, while the same tip also lands MaxTurns 40→65, `astra` lane + Codex mapping, Codex→Claude quota failover, factory-bridged routing (`Invoke-FactoryWorkOrder.ps1`), and branch-exists `worktree add` fix. Prefer a thin hygiene PR, or expand the description.
- **Hardcoded factory root (unchanged):** `Invoke-Workstream.ps1` (~line 613) and `Invoke-FactoryWorkOrder.ps1` (`-ExternalFactoryRoot` default) still hardcode `C:\!Layi Wkspc\Adobe Document Cloud Ingester`. Prefer param / env / board config (default can stay local).
- **Repo-wide stash gate (partially improved):** new falsifier `test_non_empty_stash_keeps_worktree` pins the keep reason; still no doc note that a leftover stash elsewhere will accumulate linked worktrees. Optional follow-up: document or scope stash inspection if git allows without weakening the gate.
- **Cosmetic (unchanged):** `Invoke-Lane.ps1` receipt line `complete     =($null -eq ...` still missing the space after `=` (~line 900).

## Notes

- Push `b826532c` ("adopt PR #108 review findings") correctly addresses Fable notes: `$scratchDir = $null` before try so finally cannot resolve a parent-scope path; quarantine dir is `<leaf>-<utc stamp>` so same-day same-name retirements do not nest; DryRun message no longer claims removal when the SAFE gate keeps the worktree; tests add nested-worktree + stash cases and pin the no-`QuarantineRoot` reason text. Local claim: coordination + line-endings 104 passed.
- SAFE gate design still looks sound: fail closed on probe errors, never `--force`, quarantine ignored non-debris before `git clean -fdX`, never delete branch refs, record `worktreeDisposition`. Opt-in `-RetireWorktree` on `Invoke-Lane.ps1`; workstream always uses the gate with `MergeTarget fork/master`.
- Behaviour change still stands: committed-but-unpushed work keeps the worktree (safe); looping dispatchers that never push will accumulate worktrees via receipts.
- DiskGuard flip still correctly out of scope.
- CoS review via `gh` (no Cloud Agent on plan). No GitHub PR comment posted. Prior CoS head was `b075c114`; verdict unchanged (`not-merge-ready`) — CI picture sharpened from pending to confirmed checkout-fail on the gitlink.