schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 108
head: b075c114ff5ee14472baddcc192a851587955300
reviewed_utc: 2026-09-14T06:21:54Z
event: pr-opened
verdict: not-merge-ready
ci_summary: all required checks still in progress / pending (CodeQL x3, Factory Bridge Regressions, Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile); mergeStateStatus BLOCKED

## Blockers

- **Accidental gitlink / submodule in tree:** path `mlvtmptest-worktree-add-20260910T180000` is mode `160000` pointing at `2f63d24ff4e3ac3bfdaacfc5c152767947a6ce63` (looks like a worktree path committed as a subproject). Remove it from the branch (`git rm` the gitlink; do not leave a nested checkout). Re-push so the tip is hygiene-only content plus intentional coordination changes.
- **CI not green yet:** no check has completed on this head; do not merge until Protected Check Route / Factory Bridge / hygiene / compile settle. Re-check after the submodule drop (new head will re-run them).

## Improvements

- **Scope vs title:** PR is 9 commits / +542 ahead of `master`. Title and body focus on SAFE worktree retirement + lane scratch, but the same tip also lands MaxTurns 40→65, `astra` lane + Codex mapping, Codex→Claude quota failover, factory-bridged card routing (`Invoke-FactoryWorkOrder.ps1`), and branch-exists `worktree add` fix. Prefer a thin hygiene PR, or rename/expand the description so reviewers know those ship together.
- **Hardcoded factory root:** `Invoke-Workstream.ps1` and `Invoke-FactoryWorkOrder.ps1` hardcode `C:\!Layi Wkspc\Adobe Document Cloud Ingester`. Prefer a param / env / board config so other machines and CI do not depend on that path (default can stay local).
- **Repo-wide stash gate:** `Invoke-RetireLaneWorktree` keeps when *any* stash exists on the main repo (`stash list` on `$main`). Conservative and safe, but a leftover stash elsewhere will accumulate linked worktrees. Consider documenting that, or scoping stash inspection if git allows a worktree-local read without weakening the gate.
- **Cosmetic:** `Invoke-Lane.ps1` receipt line `complete     =($null -eq ...` lost the space after `=`; restore for readability (non-functional).

## Notes

- SAFE gate design looks sound for the stated failure mode: fail closed on probe errors, never `--force`, quarantine ignored non-debris before `git clean -fdX`, never delete branch refs, record `worktreeDisposition` on lane receipt and workstream dispatch. Opt-in `-RetireWorktree` on `Invoke-Lane.ps1`; workstream `Remove-LaneWorktreeIfClean` always uses the gate with `MergeTarget fork/master`.
- New falsifiers in `tests/coordination/test_retire_lane_worktree.py` (8 cases under StrictMode) cover main-skip, clean retire, dirty, unpushed, quarantine, missing QuarantineRoot, ProtectPath, bad MergeTarget — good shape; CI will confirm on Windows/ubuntu hygiene jobs.
- Behaviour change called out in the PR body is real: committed-but-unpushed work used to lose the worktree and now keeps it — safe direction; looping dispatchers that never push will accumulate worktrees visibly via receipts.
- DiskGuard flip correctly left out of scope per PR body.
- CoS review via `gh` (no Cloud Agent on plan). No GitHub PR comment posted.