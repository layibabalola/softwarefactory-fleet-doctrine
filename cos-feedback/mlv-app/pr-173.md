schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 173
head: d9f23ab0795a2f16e6b52010de28250728397d6e
reviewed_utc: 2026-09-26T00:05:07Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip d9f23ab0 (+1918/−0, 3 files, 1 commit; ahead 1 / behind 0): Protected Check Route, Batch Compile, Repo Hygiene Python (windows in_progress / ubuntu queued), Factory Bridge Regressions, CodeQL Analyze (actions/c-cpp/python) not settled. MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft. Hygiene-only PR (no app behaviour change); live bachelor not required for this tip.

## Blockers

(none)

## Improvements

- Confirm hosted **Repo Hygiene** (ubuntu + windows) goes green on this tip. Author reports gate unit tests 11/11 and pinned-token pass locally, but a full local `repo_hygiene` run also showed 23 failures all in `test_brokered_closeout.py` (untouched by this diff). Hosted Suite is the arbitrator; do not treat local closeout noise as this PR's failure if those jobs stay green.
- Treat scanner coverage as the declared lower bound: `SCAN_GLOBS` are `src/*`, `platform/qt/*`, `tests/*` pathspecs. Inventory already pins nested sites (`src/dng/dng.c`, `tests/console/`, `tests/pipeline/`, etc.) that this Git's `ls-files` returns under those globs. If a future host Git is stricter about `*` vs `/`, coverage would shrink silently — prefer an explicit recursive pathspec (`**`) or a short comment in the scanner docstring that documents the required Git pathspec behaviour.
- Keep `PROD-TELEMETRY-DURATION-AS-PROOF-5` as the sole behaviour-fix successor for the 13 open `run_proof` rows (every #169 blocker site is already pointed there). Do not fold behaviour fixes into a -4 follow-up push.

## Notes

- Successor to parked #169 (ruling PARK+SPLIT). Round 1 only: mechanical CI-enforced inventory; no duration-as-proof behaviour rewrites.
- Adds `tools/repo_hygiene/duration_as_proof_scan.py` (four shapes: ident_compare, milliseconds_getter, json_ms_key, assert_macro; site key = path + normalized anchor text, never line number), `duration_as_proof_inventory.json` (146 classified rows; classes division_guard 7 / value_fallback_select 24 / display_or_format 2 / run_proof 14 / not_run_proof_structural 99; 13 open run_proof → -5, 1 resolved by existing flag), and `test_duration_as_proof_inventory.py` (fails on unlisted live site, stale row, bad class/clock, run_proof without flag or open successor, empty justification; seeded positive/negative/json+macro controls; anti-leak of seed markers into live inventory).
- Author claims gate proven RED on unlisted site, JSON-key form, deleted entry, and stale anchor, then green after restore. CoS did not re-run the gate on this Linux box (Windows factory floor).
- Diff is tools/repo_hygiene only; no `src/` / `platform/qt/` product edits in the patch itself (inventory references those trees).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content and gate design look sound for the PARK+SPLIT round-1 intent; wait for tip CI (especially Repo Hygiene) before merge.
