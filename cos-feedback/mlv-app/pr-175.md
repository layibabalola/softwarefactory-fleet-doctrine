schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 175
head: 60317bc51ebb90837b5040cd1d74f7fdba45bbf6
reviewed_utc: 2026-09-26T01:56:35Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 60317bc5 (+2416/−0, 3 files, 4 commits; ahead 4 / behind 0): Protected Check Route, Factory Bridge Regressions, CodeQL (actions/c-cpp/python Analyze) SUCCESS; Repo Hygiene Python (ubuntu+windows), Batch Compile, Windows GUI Pilot, Windows Product Oracles still IN_PROGRESS. MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft. Hygiene-only PR (no app behaviour change); live bachelor not required for this tip.

## Blockers

(none)

## Improvements

- Confirm hosted **Repo Hygiene** (ubuntu + windows) goes green on this tip — that is the suite that must exercise the new token-based scanner + inventory gate. Hosted Suite is the arbitrator.
- Track the HARDENING leftovers called out in the PR body / sol pre-review (raw-string strip, parenthesized operands, five GUI-latency debt rows, comment-edit identity) as follow-on debt; do not block this tip on them if the gate’s declared controls hold.
- Keep `PROD-TELEMETRY-DURATION-AS-PROOF-5` as the sole behaviour-fix successor for open `run_proof` rows. Do not fold behaviour fixes into a -4B follow-up push.

## Notes

- Supersedes parked #173 (PARK+SPLIT continuation). Carries #173’s CI-enforced duration-as-proof inventory gate plus round-1 review fixes: token-based scanner grammar (camelCase/snake word-split against unit vocabulary; toDouble/toInt JSON reads; ASSERT_*/EXPECT_* DOUBLE_EQ/FLOAT_EQ/NEAR; string-literal-aware comment stripper); site identity `(path, anchor, occurrence count)` so copied anchors must be reclassified; exclusive positive controls per matcher; token-aware anchor normalization; 141 rows re-normalized 1:1 (no class changes) + 21 new rows classified; fable justification fix; merged fork master.
- Hub: sol non-counting pre-review APPROVED; HARDENING items filed (see Improvements). Behaviour fixes for open `run_proof` stay PROD-TELEMETRY-DURATION-AS-PROOF-5.
- Diff is tools/repo_hygiene only (scanner + inventory + gate). CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
- Verdict **merge-when-ci-green**: same posture as #173; content and token-based grammar look sound for the superseding tip; wait for tip CI (especially Repo Hygiene) before merge.
