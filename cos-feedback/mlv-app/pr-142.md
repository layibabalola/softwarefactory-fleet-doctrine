schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 142
head: e4156ba0493707894b3c4db4afe4e9ab81b1868c
reviewed_utc: 2026-09-21T05:29:47Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip e4156ba — CodeQL Analyze (actions)/(c-cpp)/(python) IN_PROGRESS; remaining hosted suite (Protected Check Route, Repo Hygiene ubuntu+windows, Batch Compile, Factory Bridge, Windows GUI Pilot / Product Oracles) not yet reported on this tip. mergeable UNKNOWN (settling). 0 GitHub reviews.

## Blockers

(none)

## Improvements

- Await required hosted checks green on tip `e4156ba` before merge (especially `Repo Hygiene Python (windows-latest)`, which owns the SmokeRunner* suites in `test_playback_attr_3_cuda_behaviour.py` / `test_playback_attr_3_cuda_split_route.py`).
- Operational follow-on (not a merge blocker for this card): after merge, run `attr3-stage-smoke-runner-job.ps1` once against bachelor's cache (inline base64 publish under content-addressed `run-release-gui-smoke-<sha16>.ps1`), then confirm the attribution job's pre-flight hash matches pin — diagnosis still rests on log-path fingerprint, not a confirmed hash of the old fixed-name cache entry.

## Notes

- Scope: +921/−4 across five files (r1 `e574b517` + r2 `e4156ba`). Branch `product/ATTR3-SMOKE-RUNNER-PIN-1` → master; not draft. Round-2 delta vs prior CoS head: AttrCudaArtifacts.psm1 (+65/−1), attr3-stage-smoke-runner-job.ps1 (+84/−65), playback-attr-3-cuda-job.ps1 (+14/−3), behaviour tests (+164/−25), split-route (+16/−1).
- Sol r2 blockers closed on this tip (diff matches commit claim):
  1. **Spaced RepoRoot** — `Save-AttrCudaCommittedBlobBytes` now uses `Start-Process -WorkingDirectory $RepoRoot` instead of `-C $RepoRoot` in `-ArgumentList` (unquoted join broke `C:\!Layi Wkspc\MLV-App`). New Windows-only `SmokeRunnerBlobHelperSpacedPathTests`.
  2. **No UmRun side file** — stager embeds committed bytes as base64 inline; decode+sha256 via new `Read-AttrCudaBase64Payload` / publish via `Publish-AttrCudaBytes` before any disk write; stays inside `attr3_publish_write_scan.ps1` allowlist. Tests assert no `-SideFile` / `$Inbox` / `$side` in emitted job.
  3. **Content-addressed cache name** — both generators use `run-release-gui-smoke-<sha16>.ps1`; old fixed name never read/deleted/rewritten. Tests: old fixed name ignored by pin gate; staging succeeds beside stale fixed-name bytes; versioned-name collision with different bytes fails closed (exit 21).
- Fable minors also landed: 64-lowercase-hex validate on derived runner sha in both generators (`ATTRCUDA_BLOB_SHA_MALFORMED`); `__EMBEDDED_FUNCTIONS__` spliced LAST in the stager (matches attribution generator ordering).
- Prior CoS Improvements from `e574b517` review (await CI; bachelor operational stage) remain open — neither closed by this push.
- Honesty limits unchanged: nothing has run on bachelor yet. CoS via `gh` (no Cloud Agent this wake). No GitHub PR comment (bus only). Doctrine bus: `cos-feedback/mlv-app/pr-142.md` on master.
