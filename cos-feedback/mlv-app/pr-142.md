schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 142
head: e574b517d66e86a69f1565cbd330a4a398e5f8fa
reviewed_utc: 2026-09-21T04:59:50Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Fresh open on tip e574b517 — CodeQL Analyze (actions) PASS; Analyze (c-cpp)/(python) IN_PROGRESS; remaining hosted suite (Protected Check Route, Repo Hygiene ubuntu+windows, Batch Compile, Factory Bridge, Windows GUI Pilot / Product Oracles) not yet reported on this tip. mergeable UNKNOWN (settling). 0 GitHub reviews.

## Blockers

(none)

## Improvements

- Await required hosted checks green on tip `e574b517` before merge (especially `Repo Hygiene Python (windows-latest)`, which owns the new SmokeRunner* suites in `test_playback_attr_3_cuda_behaviour.py` / `test_playback_attr_3_cuda_split_route.py`).
- Operational follow-on (not a merge blocker for this card): run the new `attr3-stage-smoke-runner-job.ps1` once against bachelor's cache before the next ATTR-3 CUDA attribution, then confirm the read-only cache hash matches pin `da170033…a87a4a` (PR notes diagnosis not yet confirmed by hashing bachelor's cached runner).

## Notes

- Scope: +673/−4 across five files — `AttrCudaArtifacts.psm1` (+73: `Resolve-AttrCudaCommittedBlobId`, `Save-AttrCudaCommittedBlobBytes`), new `attr3-stage-smoke-runner-job.ps1` (+305), `playback-attr-3-cuda-job.ps1` (+40/−4 pin + pre-flight), and hygiene tests (+203 / +52). One commit on tip (`e574b517`). Branch `product/ATTR3-SMOKE-RUNNER-PIN-1` → master; not draft.
- Defect closed: attribution job previously only checked that `cache/run-release-gui-smoke.ps1` existed; no tracked stage/assemble job ever published current bytes, so bachelor ran a pre-`f401bf9a` runner (old log layout, no `evidence.runLogSnapshot`) and spent a quiet window before `Resolve-AttrCudaSmokeRunLog` correctly refused with exit 16 `ATTRCUDA_SMOKE_LOG_UNBOUND`. Resolver unchanged (correct).
- Fix quality (diff matches claim):
  1. **Pin** — generators resolve `tools/profiling/run-release-gui-smoke.ps1` as committed blob at `-SourceCommit` via `git rev-parse` + `git cat-file blob` streamed with `Start-Process -RedirectStandardOutput` (byte-exact; avoids PowerShell text pipeline / CRLF rewrite). Both attribution and stage generators derive independently from the same `-SourceCommit`.
  2. **Refuse before launch** — template throws `ATTRCUDA_SMOKE_RUNNER_STALE` on missing or sha256 mismatch at cache path, before deploy (`Publish-AttrCudaFileCopy` ~555) and before `Start-PresentMonCapture` (~633). PresentMon-style uppercase compare (`Get-Sha` vs `$SmokeRunnerSha256.ToUpperInvariant()`).
  3. **Ship separately** — stage job modelled on `attr3-stage-fixture-job.ps1` (partial → verify → non-overwriting rename; `-VerifyOnly`); correctly NOT folded into `playback-attr-3-cuda-stage-job.ps1` (that job's `build.json` binds exactly three compiled artifacts).
- Tests: new `SmokeRunnerStaleRefusalTests` (mismatch / missing / match), `SmokeRunnerStageJobTests` (committed-blob stage + RESULT=SMOKE_RUNNER_STAGE_OK), `SmokeRunnerPinTests` in split-route (baked pin, refusal ordering vs PresentMon/deploy). Generator list updated to include the new stage script. PR claims 152 tests OK at tip with clean tree; CoS did not re-run pwsh suites on this Linux box.
- Honesty limits: nothing has run on bachelor yet; diagnosis rests on log-path fingerprint, not a confirmed hash of the cached runner. That probe is the next operational step and needs no quiet window.
- Related: ATTR-3 CUDA route / #133 log-binding work; sibling stage pattern from fixture stage (#139 / content-pin #140). CoS via `gh` (no Cloud Agent this wake). No GitHub PR comment (bus only). Doctrine bus: `cos-feedback/mlv-app/pr-142.md` on master.
