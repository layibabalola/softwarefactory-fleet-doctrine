schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 168
head: f0e3f21999fd3f5d55b18e9b460c1a1b5fcc1e9c
reviewed_utc: 2026-09-25T20:00:49Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip f0e3f219 (+750/−17, 6 files, 4 commits; ahead 4 / behind 0): Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles, Factory Bridge Regressions, CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS or pending (Tests 36183068357, Factory Bridge 36183068315, CodeQL 36183066326). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- `MainWindow.h` declarations for `enterPlaybackSmokeFullscreen` / `leavePlaybackSmokeFullscreen`: comment still says "existing (menu-hidden) actionFullscreen" — stale after `d93af141` unhid the View entry; update to match normal-use reachability (menu + Ctrl+F / F11).
- Author-scoped p2 (agree, non-blocking): `leavePlaybackSmokeFullscreen` keys on `actionFullscreen->isChecked()` rather than `isFullScreen()`; if action and window state ever diverge, the RAII leave could no-op while the window stays full screen.

## Notes

- Supersedes parked #166 (CLOSED unmerged). Carries #166 tip `5ed0a354` plus three round-1 commits that close Sol/fable review items and the prior CoS soft note on fail-open verification.
- Round 1 vs #166: (A) fail-closed — `enterPlaybackSmokeFullscreen` returns bool; unverified → `[GUI-SMOKE] FAIL reason=fullscreen_not_verified …` exit **13** before Play; mid-session loss → `fullscreen_lost_mid_session` after the duration loop (shared `logFullscreenSmokeFailure` lambda). No windowed escape hatch. Exit 13 unused; consumers already fail on any nonzero (`run-release-gui-smoke.ps1`, `capture-reference-frame.ps1`, `playback-attr-3-cuda-job.ps1` → `SMOKE_RUN_FAILED`). (B) `playbackClock` starts after preamble + fail-closed gate, immediately before `actionPlay->trigger()`; DONE adds `preamble_ms` / `played_ms` beside `duration_ms`.
- Normal use (`d93af141`): removes 2018 `setVisible(false)`; F11 (Windows) + Ctrl+F; Esc exits only when checked; maximized bit captured/restored; geometry verification and `computeDisplaySceneGeometry` size from `this->screen()` (fallback primary).
- Wiring: `test_playback_smoke_fullscreen_wiring.cpp` (+ fail-closed / clock / DONE pins), `test_playback_fullscreen_ui_wiring.cpp` (menu/F11/Esc/maximized/own-screen). Foreground call-count pin updated 1→2. Author: console 370/0; bachelor live proof `verified=1` 2560x1600, `played_ms≈40056`, `RESULT=MEASUREMENT_CAPTURED`.
- Scoped-out (author): leave-guard isChecked vs isFullScreen (p2); validate-visible-playback WINDOWED header / explicit windowed mode → CUDA-PERF-DISPLAY-MODE-AB-1; typed harness parse of fullscreen fields (p2); PresentMon 1-row on fullscreen leg (harness).
- Files: `platform/qt/MainWindow.cpp`, `MainWindow.h`, `tests/console/console_tests.pro`, `test_playback_smoke_foreground_wiring.cpp`, new `test_playback_smoke_fullscreen_wiring.cpp` + `test_playback_fullscreen_ui_wiring.cpp`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound; #166 CoS Improvement (fail smoke on unverified fullscreen) addressed. Tip required checks / branch protection still settling. Repo Hygiene + Batch Compile first signal; GUI Pilot / Product Oracles usual long poles.
