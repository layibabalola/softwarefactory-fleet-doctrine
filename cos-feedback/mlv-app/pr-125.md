schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 125
head: f19dbf4aba10ac2068a56c5c630fce0015c28fea
reviewed_utc: 2026-09-16T13:09:47Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Checks re-queued on head f19dbf4 (CodeQL Analyze actions/c-cpp/python + Factory Bridge Regressions in progress; CodeQL run 35100057211, Factory Bridge run 35100063605). Other required workflows (Protected Check Route, Batch Compile, Repo Hygiene) not yet visible on this tip.

## Blockers

(none)

## Improvements

- `tests/console/test_async_h2d_counter_contract.cpp`: add a dedicated `DuplicateFrameId` case (verdict still exists in `AsyncH2dCounterValidator::evaluate` but is uncovered; Empty and MalformedBuildIdentity are now covered on this tip). Keeps red/green discipline for the remaining first-violation path.
- PR description still claims `QT += core` for `platform/qt/AsyncH2dCounterContract.h`, but the header remains pure STL (`cstdint`/`set`/`string`/`vector`); drop the Qt claim or note why `.pro` lists it under HEADERS without Qt types (harmless doc drift).

## Notes

- r2 push `f19dbf4` addresses hub-verified review findings: `frameId` and `fires` are now `std::uint64_t` (closes signed `fires=-1,bytes=0` hole; removes `NegativeFrameId` sentinel path); `buildIdentity` documented as lowercase hex SHA-256 matching PR #67 `dllSha256`, with shape check + `MalformedBuildIdentity` verdict before mismatch; tests added for Empty and MalformedBuildIdentity.
- Prior CoS Improvements: Empty coverage addressed; NegativeFrameId obsolete by design; DuplicateFrameId still open; QT claim in PR body still present.
- Hosted leg only as scoped: header-only `AsyncH2dCounterRecord` + pure `AsyncH2dCounterValidator` + injectable `AsyncH2dCounterBackend`; FAKE backend in tests; bachelor/real-CUDA receipt submission correctly deferred under `EXTERNAL_CAPABILITY_UNAVAILABLE(gpu-runner)`.
- Diff vs master: +221/−2 across `platform/qt/AsyncH2dCounterContract.h`, `tests/console/test_async_h2d_counter_contract.cpp`, `tests/console/console_tests.pro`. Validator order is fixed (Empty → DuplicateFrameId → FiresZeroWithBytesPositive → MalformedBuildIdentity → BuildIdentityMismatch).
- MERGEABLE; mergeStateStatus BLOCKED on pending required checks. No GitHub review comment posted by CoS. Review via `gh` only.
