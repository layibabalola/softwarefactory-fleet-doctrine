schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 112
head: 240c828e039320001e1975928b018dc1a28e4028
reviewed_utc: 2026-09-14T22:17:18Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: All required checks still in progress at review (Repo Hygiene ubuntu+windows, Protected Check Route, Batch Compile, Factory Bridge, CodeQL Analyze actions/python/c-cpp). No content blockers.

## Blockers

(none)

## Improvements

- K4 row already records the measured `complete:true` on exit-1 / `max_turns` (card PLAY-COUNTERS-CPU). Cross-link the in-flight fix on MLV-App #111 (receipt `processEnded` vs `complete` split) so the map does not go stale the moment #111 lands — either a one-line "fix in #111" note now, or a follow-up map tweak after #111 merges.
- K12 cites bus `adjudications/factory-kernel/mlv-app.md` with harvest pending. After this PR merges, confirm the adjudication filing's harvest status still matches; if harvest advances, refresh the Gap cell in a tiny follow-up rather than leaving "harvest pending" forever.
- Optional: add the bus commit or path pin for `specs/fleet-factory-kernel.md` / `bootstrap/PROMPT-K-dogfood-kernel.md` (short SHA or dated sync tip) so a later kernel r2 bump is obvious from this file alone.

## Notes

- Scope: docs only. 1 commit / +30−0 over `master`. Files: `agents/factory-kernel-instance.md` (new), `CLAUDE.md` (+3 Document Map lines). No code, tooling, gate, or config change.
- Status correctly framed as DOGFOOD, not ADOPT, per bus PROMPT-K. Doctrine-is-data line present; NONE entries treated as findings, not papered gaps.
- Notable NONE / Gap findings: K5 (no pre-work profile declaration; thermal not typed), K7 (no typed accepted-but-undelivered / `CLOSURE_INCOMPLETE`), K11 (no tracked R9 `posture:` emitter). K4 Gap is the false-positive `complete:true` receipt (separate fix track).
- Author claims every mechanism path exists at `fork/master` `9c63151e` via `git cat-file -e`; CLAUDE.md 10,668 B under 12 KB hard cap; pinned-token check claimed green locally — confirm via Repo Hygiene once it settles.
- Profile note: `code@r1` primary with proposed `measured-objective@r1` sub-instance for render/export parity — fine for dogfood; not an adoption claim.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.