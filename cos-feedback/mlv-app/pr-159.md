schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 159
head: 89bc7d2006aba51bf4ba15b8a6627a2c72279787
reviewed_utc: 2026-09-25T10:46:21Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 89bc7d20 (+390/−2, 7 files, 1 commit; ahead 1 / behind 0): Tests run 36125695927 Batch Compile + Repo Hygiene ubuntu/windows IN_PROGRESS, Protected Check Route QUEUED (Windows GUI Pilot / Product Oracles not yet listed); Factory Bridge 36125695790 Factory Bridge Regressions IN_PROGRESS; CodeQL run 36125693561 Analyze (actions)/(c-cpp)/(python) all IN_PROGRESS. MERGEABLE; mergeStateStatus BLOCKED (protection waiting on required checks). 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- `platform/qt/GpuDisplayWindow.h` / `.cpp`: the 2048-entry `m_swapTelemetryRing` is written in `noteRealSwap` but never read (snapshot only returns aggregates from `m_swapTelemetryCounters`). Either expose a bounded recent-ring slice on `GpuWindowSwapTelemetrySnapshot` for PresentMon join, or drop the ring until a consumer exists (keeps the ~80 KB footprint and the write path honest).

## Notes

- Solves the measurement gap named in the PR: `frames_presented` counts submissions; this records real buffer swaps on both paths — Qt automatic `frameSwapped` after `paintGL`, and the explicit `glContext->swapBuffers(win)` in `grabPresentedFramebufferIfActive` (which does not emit `frameSwapped`). Gate line `playback_smoke.gpu_window_swaps` reports swaps, swap fps, max inter-swap gap location, and `swaps_minus_frames_presented`.
- Reuses existing `MLVAPP_PLAYBACK_SMOKE_TELEMETRY` (same “set and not literally 0” semantics as MainWindow frame telemetry); zero cost when unset. Session reset wired in `beginPlaybackSmokeTelemetry`; snapshot at `finishPlaybackSmokeTelemetry`.
- Pure summariser `GpuWindowSwapTelemetry.h` + 5 unit tests (zero/single/zero-span/even-cadence/passthrough) and 5 source-census wiring tests pinning both emit sites, env gate, and MainWindow reset/read. Author local evidence: `console_tests --check-golden` 273/0; both mutation checks red; no bachelor run this round. CoS did not re-run (Windows factory; review via gh only).
- Diff scoped to 7 owned paths: `GpuDisplayWindow.{h,cpp}`, new `GpuWindowSwapTelemetry.h`, `MainWindow.cpp` (reset + gate line), `console_tests.pro`, `test_gpu_window_swap_telemetry.cpp`, `test_gpu_window_swap_wiring.cpp`.
- Soft: per-swap `qInfo` under the smoke flag will be chatty on long legs; consistent with existing per-frame smoke telemetry volume — accept unless log volume becomes a gate problem.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound; only tip required checks / branch protection blocking.
