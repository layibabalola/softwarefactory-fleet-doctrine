schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 178
head: fbda7e29654c063110f39d4401d00bd6684c4a1c
reviewed_utc: 2026-09-26T05:03:10Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip fbda7e29 (PRESENTMON-HARNESS-ROBUSTNESS-2; +1591/−26, 9 files, 9 commits; ahead 9 / behind 0). Protected Check Route SUCCESS; CodeQL Analyze (actions) SUCCESS; CodeQL rollup NEUTRAL; Factory Bridge Regressions, CodeQL Analyze (c-cpp/python), Repo Hygiene Python (ubuntu+windows), Batch Compile, Windows GUI Pilot, Windows Product Oracles IN_PROGRESS (Tests 36219563290, Factory Bridge 36219563284, CodeQL 36219562348). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- After tip CI greens: one bachelor PresentMon leg is still the only live proof the three-arm gate and embedded helpers clear `CommandNotFoundException` on a no-checkout host (fixtures cover arms + AST embed contract; hosted Suite does not run PresentMon).
- `refresh_period_histogram.build_report()` still accepts `presentmon_status=None` for library/unit callers — intentional, but any future non-CLI entrypoint that writes a histogram artifact should go through the same fail-closed path as `main()` (require status or `--artifacts-dir`).
- Coverage arm fallback `playback_smoke.gate` `frames_presented` is coarser than `gpu_window_swaps` (submissions vs confirmed on-screen swaps). Prefer keeping swap telemetry enabled on measured legs so the preferred arm is what clears `ok`.

## Notes

- Supersedes parked #174 (contract v2 R2). Carries #174 commits `e5d80c09`, `8520a960`, `4d0ed7e2` plus round-1 sufficiency-gate fixes (`eaee4cf0`…`d0750d1a`) and merge of fork master (`fbda7e29`).
- Three-arm `presentMonStatus=ok` gate is real and non-circular in tip:
  1. count: ≥30 positive `MsBetweenDisplayChange` rows;
  2. app-swap coverage: `positiveSamples / appSwapCount ≥ 0.5` from `Get-AttrCudaAppSwapTelemetry` (log `playback_smoke.gpu_window_swaps`, else gate `frames_presented`; neither → coverage unavailable → arm fails — never divides by PresentMon `presentedCount`);
  3. temporal: `Get-AttrCudaTemporalCoverage` max head/internal/tail gap ≤5000ms against `windowStartMs`/`windowEndMs`.
  Failed arms are named in `presentMonStatusReason`; rule + verdict published on `evidence-manifest.json` `presentMon.sufficiency` and `summary.json`.
- Helpers embedded: `Get-AttrCudaAppSwapTelemetry` and `Get-AttrCudaTemporalCoverage` are in the generator's `Get-AttrCudaEmbeddedFunctionSource -Name @(...)` list; `EmbeddedFunctionCoverageTests` AST-asserts every `*-AttrCuda*` CommandAst in the spliced job is defined in that output (mutation-checked against the sol PRE-REVIEW #2 `CommandNotFoundException` gap).
- Histogram consumer: `build_report` refuses any status ≠ `ok`; CLI `main()` hard-requires `--presentmon-status` or `--artifacts-dir` (derives status from summary/manifest; malformed JSON → typed `RefreshHistogramError`). Docs command updated to `--artifacts-dir`.
- Sol pre-review blockers claimed fixed on tip (CLI skip; circular coverage; helpers not embedded) — CoS confirms the three are addressed in the reviewed head. `$presentMonPresentedCount` remains published for diagnostics only.
- Windows-only PresentMon/bachelor assumptions unchanged (expected for this harness). In-lane claim: four PLAYBACK-ATTR-3-CUDA suites + `test_refresh_period_histogram.py` green.
- CoS via gh + cursor-github MCP (no Cloud Agent). No GitHub PR review comment (bus only).
