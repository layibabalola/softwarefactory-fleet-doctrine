schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 131
head: ea6c5bf8cb541dc737d288d59772287a020561a7
reviewed_utc: 2026-09-16T18:29:10Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip ea6c5bf8 re-queued hosted checks after rename push (Tests 35134506522, CodeQL 35134503556, Factory Bridge 35134506542). Protected Check Route + Analyze (actions) PASS; CodeQL NEUTRAL/skipping. Required Batch Compile, Repo Hygiene (ubuntu/windows), Windows GUI Pilot, Windows Product Oracles, Factory Bridge, and Analyze (c-cpp/python) still in progress / pending.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (ubuntu-latest + windows-latest), Windows GUI Pilot, Windows Product Oracles, and Factory Bridge Regressions on tip `ea6c5bf8`. Prior tip failed ubuntu Repo Hygiene on tracked ignored `playback-attr-3-cuda-build-job.ps1`; this tip renames that generator to `playback-attr-3-cuda-compile-job.ps1` (no `build` substring in the path) and updates docs + attribution-job references. Confirm Repo Hygiene green on both OS before merging.

## Improvements

- Optional: when Bachelor first runs the compile job, confirm the INFERRED qmake path / configure step and windeployqt flags (called out in generator comments vs Ultramagnus / `tools/build-release.ps1`) against a live `result.json` step list before treating stage-1 tooling as proven for stage-2 product work.
- Internal job ids / schemas still use the `playback-attr-3-cuda-build-*` / `mlvapp.playback-attr-3-cuda-build.v1` strings inside the compile generator. Harmless for gitignore (path no longer matches `*build*`); rename those identifiers only if you want naming consistency with the new filename.

## Notes

- Prior CoS Blocker addressed: renamed `tools/profiling/bachelor/playback-attr-3-cuda-build-job.ps1` → `playback-attr-3-cuda-compile-job.ps1` and updated runbook + attribution-job references. No remaining tracked path with the old name on tip. Verdict moves from `not-merge-ready` → `merge-when-ci-green`.
- Scope unchanged: tools/ + docs/ only (+1412/−0 across 5 paths vs master). Measurement tooling for PLAYBACK-ATTR-3-CUDA stage 1; no product landing. Stage 2 remains the targeted CUDA-path fix.
- Clip handling unchanged: `Resolve-ClipById` matches cache files by `BaseName -ceq $ClipId` and discriminates the primary part via extension shape without concatenating a literal extension onto the id. Consent cited by receipt filename only. Still read as sanctioned id-addressed Bachelor cache route, not NA-4 hook evasion.
- Attribution job still lowercases `dllSha256` via `ToLowerInvariant()` for `provenance.json`.
- `mergeable` MERGEABLE; `mergeStateStatus` BEHIND (master has moved). Expect an update-branch before merge once checks settle, per MLV-App factory practice.
- Local claim: 16 pytest cases for `refresh_period_histogram.py`; generators emit parseable jobs. CoS did not re-run pytest on this Linux box.
- 0 GitHub reviews. CoS via `gh` only; no PR comment. Bus file: `cos-feedback/mlv-app/pr-131.md`.