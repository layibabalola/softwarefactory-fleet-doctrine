schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 131
head: 6b57fe0a73bffe0244c6fac6beb8e74b7482d64d
reviewed_utc: 2026-09-16T20:07:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 6b57fe0a after r3 (non-finite refresh-period-ms + attribution-job TEMP boundary); all required hosted checks still IN_PROGRESS/pending on fresh Actions runs 35144522482 / 35144522581 / 35144516675 (Batch Compile, Repo Hygiene ubuntu/windows, Protected Check Route, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python). Windows GUI Pilot / Product Oracles not yet listed on this tip rollup.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (ubuntu-latest + windows-latest), Protected Check Route, Factory Bridge Regressions, and any required Windows GUI Pilot / Product Oracles on tip `6b57fe0a`. Content looks mergeable once those settle green.

## Improvements

- Optional: when Bachelor first runs the compile job, confirm the INFERRED qmake path / configure step and windeployqt flags (still called out in generator comments vs Ultramagnus / `tools/build-release.ps1`) against a live `result.json` step list before treating stage-1 tooling as proven for stage-2 product work.
- Internal job ids / schemas still use the `playback-attr-3-cuda-build-*` / `mlvapp.playback-attr-3-cuda-build.v1` strings inside the compile generator. Harmless for gitignore (path is `*-compile-job.ps1`); rename those identifiers only if you want naming consistency with the filename.
- Cache publish still uses three sequential `Move-Item -Force` renames then a manifest write — better than the prior non-transactional copy, but not a single filesystem transaction. If a crash mid-rename is a real Bachelor failure mode, consider staging the whole cache triple under one temp dir and swapping via a single directory rename, or documenting the manifest-as-completion-token contract for operators.
- PresentMon sidecar TEMP: attribution job correctly documents that `schtasks.exe /Run` cannot inherit process TEMP/TMP (choice b). If operators ever need PresentMon writes also under `C:\mlvtmp`, that requires a sidecar-runner change outside this repo (passing scratch via `pm-request.json`).

## Notes

- Head moved `7178f39` → `6b57fe0a` (2 r3 commits). Verdict unchanged: `merge-when-ci-green`. Prior CoS Improvements (INFERRED build confirmation; internal `build-*` id rename; transactional cache publish) not clearly addressed; bus refreshed because head/CI picture changed. No chat ping per later-push rule.
- r3 content reviewed as sound for tooling-only stage 1:
  - **Non-finite `--refresh-period-ms`:** `build_report` now rejects `nan`/`inf` before the positivity check (`nan <= 0` / `inf <= 0` are False and would otherwise corrupt bucket multiples). Tests cover nan, ±inf, 0, and negative.
  - **Attribution-job TEMP boundary:** mirrors compile-job pattern — Assert-UnderMlvTmp for Root/Work/Pub/PresentMon paths; creates `$Work/.job-tmp` and sets TEMP/TMP before child processes; stops recreating `$Work` mid-job (would delete live scratch). Records `environmentBoundary` in evidence manifest including PresentMon sidecar note.
- Scope vs master: still tools/ + docs/ only (5 paths). Measurement tooling; no product landing. Stage 2 remains the targeted CUDA-path fix.
- `mergeable` MERGEABLE; `mergeStateStatus` BLOCKED on unsettled required checks. Tip is post-master-merge from r2; re-check BEHIND before merge if master moves again.
- Local claim: histogram pytest expanded (non-finite cases). CoS did not re-run pytest on this Linux box.
- 0 GitHub reviews. CoS via `gh` only; no PR comment. Bus file: `cos-feedback/mlv-app/pr-131.md`.
