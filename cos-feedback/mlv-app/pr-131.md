schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 131
head: 7178f39aaa0d9b79153496464a8798770dbea809
reviewed_utc: 2026-09-16T19:25:18Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 7178f39 after r2 remediation stack + master merge; Protected Check Route PASS; CodeQL NEUTRAL/skipping. Required Batch Compile, Repo Hygiene (ubuntu/windows), Windows GUI Pilot, Windows Product Oracles, Factory Bridge Regressions, and Analyze (actions/c-cpp/python) still in progress / pending on Actions runs 35140260929 / 35140261071 / 35140257006.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (ubuntu-latest + windows-latest), Windows GUI Pilot, Windows Product Oracles, and Factory Bridge Regressions on tip `7178f39`. Content looks mergeable once those settle green.

## Improvements

- Optional: when Bachelor first runs the compile job, confirm the INFERRED qmake path / configure step and windeployqt flags (still called out in generator comments vs Ultramagnus / `tools/build-release.ps1`) against a live `result.json` step list before treating stage-1 tooling as proven for stage-2 product work.
- Internal job ids / schemas still use the `playback-attr-3-cuda-build-*` / `mlvapp.playback-attr-3-cuda-build.v1` strings inside the compile generator. Harmless for gitignore (path is `*-compile-job.ps1`); rename those identifiers only if you want naming consistency with the filename.
- Cache publish still uses three sequential `Move-Item -Force` renames then a manifest write — better than the prior non-transactional copy, but not a single filesystem transaction. If a crash mid-rename is a real Bachelor failure mode, consider staging the whole cache triple under one temp dir and swapping via a single directory rename, or documenting the manifest-as-completion-token contract for operators.

## Notes

- Head moved `ea6c5bf8` → `7178f39` (r2 remediation commits + merge of `fork/master`). Verdict unchanged: `merge-when-ci-green`. Prior CoS Improvements (INFERRED build confirmation; internal `build-*` id rename) not clearly addressed; bus refreshed because head/CI picture changed.
- r2 content fixes reviewed as sound for tooling-only stage 1:
  - **TEMP boundary:** job-owned `.job-tmp` under `C:\mlvtmp\mlv-agent\...` assigned to `TEMP`/`TMP` before nvcc/qmake/make/windeployqt.
  - **Build identity:** injects `build_buildinfo.h` with pinned `-SourceCommit` before qmake (archive has no `.git`).
  - **Self-contained package:** explicit MinGW runtime DLL set + required `cudart64_*.dll` (fail closed if missing).
  - **Non-transactional publish:** `.partial` → rename for exe/DLL/pkg; cache manifest written last; attribution job requires manifest and verifies all three sha256s.
  - **CUDA gate:** `gpuFramesTotal` excludes `gpu_preview_frames` (recon/texture frames only).
  - **Clip resolver:** derives primary extension from continuation-part letter +`LV` (no id+extension concat); fails closed without parts. Still read as sanctioned id-addressed Bachelor cache route, not NA-4 hook evasion — though the `+ 'LV'` literal embeds MLV naming knowledge (acceptable for this family).
  - **Histogram ambiguity:** `MIN_OUTSIDE_CLUSTER_SHARE` fail-closed without `--refresh-period-ms`; tests cover all-2-refresh; runbook updated.
- Scope vs master: still tools/ + docs/ only (5 paths; compile/attribution generators, histogram + tests, runbook). Measurement tooling; no product landing. Stage 2 remains the targeted CUDA-path fix.
- `mergeable` MERGEABLE; `mergeStateStatus` BLOCKED on unsettled required checks. Tip includes a master merge, so BEHIND risk from prior tip is reduced for this head — re-check before merge if master moves again.
- Local claim: pytest for histogram expanded (ambiguity cases). CoS did not re-run pytest on this Linux box.
- 0 GitHub reviews. CoS via `gh` only; no PR comment. Bus file: `cos-feedback/mlv-app/pr-131.md`.