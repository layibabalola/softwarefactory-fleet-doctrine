schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 131
head: 3ae7da65460af0224e5f75e528dbb5aaf10e4601
reviewed_utc: 2026-09-16T20:43:52Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 3ae7da6 after r5 (PresentMon direct child + runbook -ClipPath); Protected Check Route SUCCESS; Batch Compile / Repo Hygiene (ubuntu+windows) / Factory Bridge Regressions / CodeQL Analyze (actions/c-cpp/python) / Windows GUI Pilot / Windows Product Oracles still IN_PROGRESS or queued on tip.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (ubuntu-latest + windows-latest), Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, and CodeQL Analyze jobs on tip `3ae7da6`. Content looks mergeable once those settle green. Protected Check Route already SUCCESS.

## Improvements

- Optional gen-time guard: the generator embeds `-ClipPath` without checking parent-dir/BaseName/`Test-Path` locally; only the emitted Bachelor job fails closed. A pre-emit check would catch typos before inbox drop.
- Optional: when Bachelor first runs the compile job, confirm the INFERRED qmake path / configure step and windeployqt flags (still called out in generator comments vs Ultramagnus / `tools/build-release.ps1`) against a live `result.json` step list before treating stage-1 tooling as proven for stage-2 product work.
- Internal job ids / schemas still use the `playback-attr-3-cuda-build-*` / `mlvapp.playback-attr-3-cuda-build.v1` strings inside the compile generator. Harmless for gitignore (path is `*-compile-job.ps1`); rename those identifiers only if you want naming consistency with the filename.
- Cache publish still uses three sequential `Move-Item -Force` renames then a manifest write — better than the prior non-transactional copy, but not a single filesystem transaction. If a crash mid-rename is a real Bachelor failure mode, consider staging the whole cache triple under one temp dir and swapping via a single directory rename, or documenting the manifest-as-completion-token contract for operators.

## Notes

- Head moved `7360af7` → `3ae7da6` (1 r5 commit). Verdict unchanged: `merge-when-ci-green`. **Prior CoS Improvements clearly addressed in r5:** (1) runbook now documents owner-typed `CLIP_OR_NONE` + mandatory `-ClipPath` (was contradicting the generator); (2) PresentMon launches as a direct child inheriting job-owned TEMP/TMP (no elevated scheduled task / `schtasks` path; fails closed `PRESENTMON_FAILED rc=6` if ETW rights missing). Chat report required by later-push rule.
- **r5 PresentMon (Sol r3-aligned):** removed `MLV\PresentMonSidecar` / `pm-request.json` / `pm-request.done.json` path. `Start-PresentMonCapture` uses `Start-Process` on cached PresentMon with `--process_name` / `--output_file` / `--timed` / `--terminate_after_timed` / `--stop_existing_session` / `--no_console_stats`. Manifest records `launch=direct-child-inherits-job-temp` and `allChildrenInheritJobTemp=true`.
- Scope vs master: still tools/ + docs/ only (5 paths). Measurement tooling; no product landing. Stage 2 remains the targeted CUDA-path fix.
- `mergeable` MERGEABLE; `mergeStateStatus` BLOCKED on unsettled required checks.
- Local claim: histogram pytest (21 cases, prior tip). CoS did not re-run pytest on this Linux box for r5 (no Python test changes in the r5 commit).
- 0 GitHub reviews / review comments on the PR itself (Sol findings cited in commit messages / fleet-runs). CoS via `gh` only; no PR comment. Bus file: `cos-feedback/mlv-app/pr-131.md`.
