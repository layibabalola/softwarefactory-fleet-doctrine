schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 131
head: 7360af7fa0ce56fa95fe9a655d3dfc30b25c2074
reviewed_utc: 2026-09-16T20:08:54Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 7360af7 after r4 (exact -ClipPath replaces Resolve-ClipById); required hosted checks still IN_PROGRESS/pending on Actions (Batch Compile / Repo Hygiene / Protected Check Route not yet on tip rollup; Factory Bridge Regressions + CodeQL Analyze actions/c-cpp/python IN_PROGRESS; CodeQL parent NEUTRAL/skipping). Windows GUI Pilot / Product Oracles not yet listed.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (ubuntu-latest + windows-latest), Protected Check Route, Factory Bridge Regressions, and any required Windows GUI Pilot / Product Oracles on tip `7360af7`. Content looks mergeable once those settle green.

## Improvements

- **Docs lag r4:** `docs/playback-attr-3-cuda.md` still says clips are addressed "by id only — never by a host-local path" and the §3 generator invocation omits `-ClipPath`. That contradicts the Sol-aligned generator (mandatory `-ClipPath` = lane `CLIP_OR_NONE` line). Update the runbook before operators treat it as authoritative.
- Optional gen-time guard: the generator embeds `-ClipPath` without checking parent-dir/BaseName/`Test-Path` locally; only the emitted Bachelor job fails closed. A pre-emit check would catch typos before inbox drop.
- Optional: when Bachelor first runs the compile job, confirm the INFERRED qmake path / configure step and windeployqt flags (still called out in generator comments vs Ultramagnus / `tools/build-release.ps1`) against a live `result.json` step list before treating stage-1 tooling as proven for stage-2 product work.
- Internal job ids / schemas still use the `playback-attr-3-cuda-build-*` / `mlvapp.playback-attr-3-cuda-build.v1` strings inside the compile generator. Harmless for gitignore (path is `*-compile-job.ps1`); rename those identifiers only if you want naming consistency with the filename.
- Cache publish still uses three sequential `Move-Item -Force` renames then a manifest write — better than the prior non-transactional copy, but not a single filesystem transaction. If a crash mid-rename is a real Bachelor failure mode, consider staging the whole cache triple under one temp dir and swapping via a single directory rename, or documenting the manifest-as-completion-token contract for operators.
- PresentMon sidecar TEMP: attribution job correctly documents that `schtasks.exe /Run` cannot inherit process TEMP/TMP (choice b). If operators ever need PresentMon writes also under `C:\mlvtmp`, that requires a sidecar-runner change outside this repo (passing scratch via `pm-request.json`).

## Notes

- Head moved `6b57fe0a` → `7360af7` (1 r4 commit). Verdict unchanged: `merge-when-ci-green`. Prior CoS Improvements (INFERRED build confirmation; internal `build-*` id rename; transactional cache publish; PresentMon sidecar) not clearly addressed; bus refreshed because head/CI picture changed. Chat skip per later-push rule (verdict same; CoS Improvements not addressed). Sol NA-4 clip finding was addressed in code (outside prior CoS Improvements list).
- **r4 NA-4 clip route (Sol-aligned):** removed `Resolve-ClipById` (ruled lexical-detector evasion). Generator now requires `-ClipPath` (exact owner-typed `CLIP_OR_NONE` path). Emitted job opens only `$AuthorizedClipPath` and fails closed unless (1) parent is the agent cache, (2) BaseName equals `-ClipId`, (3) leaf exists. No id-to-file lookup, directory scan, or extension construction remains. Single-quote escaping on embed (`.Replace(\"'\", \"''\")`).
- Scope vs master: still tools/ + docs/ only (5 paths). Measurement tooling; no product landing. Stage 2 remains the targeted CUDA-path fix.
- `mergeable` MERGEABLE; `mergeStateStatus` BLOCKED on unsettled required checks.
- Local claim: histogram pytest (prior tip). CoS did not re-run pytest on this Linux box for r4 (no Python test changes in the r4 commit).
- 0 GitHub reviews / review comments on the PR itself (Sol finding cited in commit message / fleet-runs). CoS via `gh` only; no PR comment. Bus file: `cos-feedback/mlv-app/pr-131.md`.
