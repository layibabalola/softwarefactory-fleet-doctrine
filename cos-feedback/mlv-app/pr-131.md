schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 131
head: 82725fd8d8ecd32e01409ded874a13964668fa82
reviewed_utc: 2026-09-16T21:19:04Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 82725fd8 after r6 (nested -Command ClipPath injection closed); Protected Check Route SUCCESS; CodeQL Analyze (actions) SUCCESS; Batch Compile / Repo Hygiene Python (ubuntu+windows) / Factory Bridge Regressions / Analyze (c-cpp, python) / Windows GUI Pilot / Windows Product Oracles still pending on tip; CodeQL suite row may show skipping while language jobs settle.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (ubuntu-latest + windows-latest), Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, and remaining CodeQL Analyze jobs on tip `82725fd8`. Content looks mergeable once those settle green. Protected Check Route and Analyze (actions) already SUCCESS.

## Improvements

- Optional gen-time existence/parent check: r6 added `[ValidatePattern]` on generator `-ClipPath` (character allowlist), which closes the apostrophe-injection class at emit time, but still no local `Test-Path` / parent-dir / BaseName check before writing the job. A pre-emit existence check would catch typos before inbox drop.
- Residual nested-command surface: `$envList` for `-ExtraEnvironment` is still built with ` "'" + ($envs -join "','") + "'" `. Paths like `QT_PLUGIN_PATH=$pkgDir` and `MLVAPP_GPU_PLAYBACK_RECON_DLL=$reconDll` are not run through `ConvertTo-PsSingleQuoted`. Under current cache naming this is low risk; apply the same helper if package/DLL names can ever carry `'`.
- Optional: when Bachelor first runs the compile job, confirm the INFERRED qmake path / configure step and windeployqt flags (still called out in generator comments vs Ultramagnus / `tools/build-release.ps1`) against a live `result.json` step list before treating stage-1 tooling as proven for stage-2 product work.
- Internal job ids / schemas still use the `playback-attr-3-cuda-build-*` / `mlvapp.playback-attr-3-cuda-build.v1` strings inside the compile generator. Harmless for gitignore (path is `*-compile-job.ps1`); rename those identifiers only if you want naming consistency with the filename.
- Cache publish still uses three sequential `Move-Item -Force` renames then a manifest write — better than the prior non-transactional copy, but not a single filesystem transaction. If a crash mid-rename is a real Bachelor failure mode, consider staging the whole cache triple under one temp dir and swapping via a single directory rename, or documenting the manifest-as-completion-token contract for operators.

## Notes

- Head moved `3ae7da6` → `82725fd8` (1 r6 commit). Verdict unchanged: `merge-when-ci-green`. **Prior CoS Improvements not clearly closed by r6** (character allowlist only partially overlaps the optional gen-time guard). **No chat report** under later-push rule (verdict same; CoS Improvements not clearly addressed).
- **r6 Sol r4-aligned injection fix** in `tools/profiling/bachelor/playback-attr-3-cuda-job.ps1` only (+5/−1): (1) generator `[ValidatePattern('^[A-Za-z]:\\[A-Za-z0-9 _.\\-]+$')]` on `-ClipPath`; (2) same allowlist re-checked on Bachelor before any use; (3) nested `pwsh -Command` path slots go through `ConvertTo-PsSingleQuoted` (`'` → `''`). Generator embed still doubles single quotes when substituting `__CLIP_PATH__`. Claimed checked against Sol repro (malicious path refused; escaped command parses as one statement). CoS did not re-execute that PowerShell repro on this Linux box.
- Scope vs master: still tools/ + docs/ only (5 paths). Measurement tooling; no product landing. Stage 2 remains the targeted CUDA-path fix.
- `mergeable` MERGEABLE; `mergeStateStatus` BLOCKED on unsettled required checks.
- Local claim: histogram pytest (21 cases) unchanged by r6 (no Python edits). CoS did not re-run pytest (pytest not installed on this Linux box).
- 0 GitHub reviews / review comments on the PR itself (Sol findings cited in commit messages / fleet-runs). CoS via `gh` only; no PR comment. Bus file: `cos-feedback/mlv-app/pr-131.md`.