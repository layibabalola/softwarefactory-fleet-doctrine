schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 131
head: 4b9b51c3535e9f324916d6a088b96dbda4e12d6b
reviewed_utc: 2026-09-16T18:08:14Z
event: pr-opened
verdict: not-merge-ready
ci_summary: Repo Hygiene Python (ubuntu-latest) FAIL on tip — tracked ignored file tools/profiling/bachelor/playback-attr-3-cuda-build-job.ps1 not allowlisted (*build* gitignore). Batch Compile + Factory Bridge + CodeQL + Windows GUI Pilot + Protected Check Route PASS; Repo Hygiene (windows-latest) and Windows Product Oracles still pending (Tests 35131557823).

## Blockers

- **CI / hygiene: tracked ignored `*build*` path** — `test_policy_verifier_passes_for_repo_config_docs_and_tests` failed with `tracked ignored file is not allowlisted: tools/profiling/bachelor/playback-attr-3-cuda-build-job.ps1`. Root cause: root `.gitignore` has a global `*build*` rule; the filename contains `build`. Fix: add a negation next to the existing build-tooling carve-outs (same block as `!tools/build-release.ps1` / `!tools/gen-buildinfo.ps1`), e.g. `!tools/profiling/bachelor/playback-attr-3-cuda-build-job.ps1`, then re-run Repo Hygiene. The sibling `playback-attr-3-cuda-job.ps1` is unaffected (no `build` substring).

## Improvements

- Optional: prefer a stable pattern under that carve-out (e.g. `!tools/profiling/bachelor/*-build-job.ps1`) if more Bachelor build-job generators are expected, so the next `*-build-job.ps1` does not trip the same check.
- Generator comments already flag INFERRED qmake + windeployqt steps vs Ultramagnus precedent; when Bachelor first runs the build job, confirm those flags against a live `result.json` step list before treating stage-1 tooling as proven for stage-2 product work.

## Notes

- Scope: tools/ + docs/ only (+1412/−0 across 5 paths). Measurement tooling for PLAYBACK-ATTR-3-CUDA stage 1; no product landing (product-ratio-guard kind). Stage 2 remains the targeted CUDA-path fix.
- Clip handling: `Resolve-ClipById` matches cache files by `BaseName -ceq $ClipId` and discriminates the primary part via extension length/shape (`Extension.Length -eq 4`, second char `M`, non-`^\d{2}$` tail) without concatenating a literal extension onto the id. Consent is cited by receipt filename only. CoS reads this as the sanctioned id-addressed Bachelor cache route (historical invoke-stage pattern), not an NA-4 hook evasion.
- Attribution job lowercases `dllSha256` via `ToLowerInvariant()` for `provenance.json` (aligns with `gpu_job_result_provenance.py`); general `Get-Sha` remains uppercase for other artifact fields.
- Local claim: 16 pytest cases for `refresh_period_histogram.py`; generators emit parseable jobs. CoS did not re-run pytest on this Linux box.
- MERGEABLE; `mergeStateStatus` BLOCKED (checks). 0 GitHub reviews. CoS via `gh` only; no PR comment. Bus file: `cos-feedback/mlv-app/pr-131.md`.