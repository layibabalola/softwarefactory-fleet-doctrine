schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 121
head: 3224429636d7d1f523ac0c13a1dd3aa7c704d679
reviewed_utc: 2026-09-16T06:46:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: All required hosted checks IN_PROGRESS on head 32244296 (CodeQL Analyze actions/c-cpp/python; Factory Bridge Regressions; Tests Protected Check Route, Batch Compile, Repo Hygiene ubuntu/windows — runs 35065191360 CodeQL, 35065192582 Factory Bridge, 35065192352 Tests).

## Blockers

(none)

## Improvements

- Follow-up family (out of this packet, booked in the PR body): three sibling tests in `tests/pipeline/test_processing_filters.cpp` still treat Milliseconds() as execution proof — `SharpOddHeightPlaybackPreviewKeepsFullresShadowsHighlights` (~686), `AggressiveOddHeightPlaybackPreviewUsesHalfresShadowsHighlights` (~729–731), `AggressiveX8PlaybackPreviewUsesQuarterresShadowsHighlights` (~769–773). Same duration-as-proof hazard; convert them to counters/completed-flags on a dedicated card rather than expanding this PR.
- Optional: expose halfres completed getters next to the existing quarterres ones in any future probe/docs surface so both lanes share one mental model (already consistent in `raw_processing.h` for this tip).

## Notes

- Push `3224429636d7d1f523ac0c13a1dd3aa7c704d679` on tip after Sol HIGH review: restores the six `ASSERT_EQ(0.0, …)` halfres/quarterres duration assertions that prove probe timing is opt-in and off by default (`MLVAPP_SHADOWS_HIGHLIGHTS_PROBE` unset → probe mode -1 → per-substage `omp_get_wtime()` writes skipped). Prior tip had dropped them while keeping assertion count via the new counters/flags alone — wrong: counters prove stages ran; durations-at-zero prove detailed probe telemetry was never recorded. Comment on the restores names that contract so they are not "fixed" back into run-proof duty.
- Author claims RED (force `shadows_highlights_probe_enabled=1` → restored asserts see non-zero durations) then GREEN on the ProcessingFilters suite. Hosted CI not yet settled — that remains the merge gate.
- Scoped tip vs `master`: still 3 files (`raw_processing.c`/`.h`, `test_processing_filters.cpp`); net +82/−2. Counter/flag execution proof from first commit unchanged; second commit is test-only (+18).
- CoS Improvements from prior review (sibling duration-as-proof family; optional probe/docs surface) not addressed by this push — still open.
- CoS review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (plan). Doctrine bus: rewritten on `master` at `cos-feedback/mlv-app/pr-121.md`.