schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 121
head: df70779def8495230dd186024a6e0026e8c7299d
reviewed_utc: 2026-09-16T02:21:25Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: All required hosted checks still IN_PROGRESS on head df70779d (CodeQL Analyze actions/c-cpp/python; Factory Bridge Regressions; Tests Protected Check Route, Batch Compile, Repo Hygiene ubuntu/windows — runs 35047606568 CodeQL, 35047608997 Factory Bridge, 35047609068 Tests).

## Blockers

(none)

## Improvements

- Follow-up family (out of this packet, booked in the PR body): three sibling tests in `tests/pipeline/test_processing_filters.cpp` still treat Milliseconds() as execution proof — `SharpOddHeightPlaybackPreviewKeepsFullresShadowsHighlights` (~686), `AggressiveOddHeightPlaybackPreviewUsesHalfresShadowsHighlights` (~729–731), `AggressiveX8PlaybackPreviewUsesQuarterresShadowsHighlights` (~769–773). Same duration-as-proof hazard; convert them to counters/completed-flags on a dedicated card rather than expanding this PR.
- Optional: expose halfres completed getters next to the existing quarterres ones in any future probe/docs surface so both lanes share one mental model (already consistent in `raw_processing.h` for this tip).

## Notes

- Scoped review of tip `df70779def8495230dd186024a6e0026e8c7299d` vs `master`: 3 files, +70/−8 (`src/processing/raw_processing.c`, `raw_processing.h`, `tests/pipeline/test_processing_filters.cpp`).
- Fix matches the card: adds `prep_run_count` / `filter_run_count` and halfres `*_completed` flags, resets them wherever the ms fields reset, and asserts on counters/flags in `ShadowsHighlightsProbeTelemetryIsOptInByDefault`. Durations remain for reporting only.
- Corrected reality on the 24×20 fixture: even dimensions take the halfres RBF lane by design, so the old `== 0.0` halfres assertions were false-green (timer resolution). New asserts expect halfres completed and quarterres not completed — right polarity.
- Author claims stage-2 local RED (prep count increment removed → fail) and GREEN (`ProcessingFilters` 18/18, 317 assertions) on Qt 6.10.2 + MinGW 13.1; tip SHA is still the stage-1 commit (no design delta after verify). Hosted CI not yet settled — that is the merge gate.
- CoS review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (plan). Doctrine bus: published on `master` at `cos-feedback/mlv-app/pr-121.md`.