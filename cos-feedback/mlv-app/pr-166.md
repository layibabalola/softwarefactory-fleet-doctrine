schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 166
head: 5ed0a3542668892c41c24b16675fecbcd10e9462
reviewed_utc: 2026-09-25T17:24:51Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 5ed0a354 (+410/−10, 5 files, 1 commit; ahead 1 / behind 0): Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles, Factory Bridge Regressions, CodeQL Analyze (actions/c-cpp/python) all IN_PROGRESS (Tests 36166868129, Factory Bridge 36166868117, CodeQL 36166864500). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- `MainWindow::enterPlaybackSmokeFullscreen`: on `verified=0` after the settle loop, consider failing the smoke (typed non-zero exit) instead of only logging and continuing into play. A windowed measurement logged under fullscreen telemetry would quietly poison CUDA-PERF comparison; fail-closed matches the PR thesis that windowed ≠ full-screen present path.

## Notes

- Follows #164 FOREGROUND (now on master): measured `--gui-smoke-playback` now also enters full screen via existing menu-hidden `actionFullscreen` after foreground and before play; RAII `PlaybackSmokeFullscreenGuard` restores on every return path; second `forcePlaybackSmokeWindowForeground()` after the switch is fullscreen-safe (`wasFullScreen` skips `showNormal()` / `SW_SHOWNORMAL`).
- Files: `platform/qt/MainWindow.cpp`, `MainWindow.h`, `tests/console/console_tests.pro`, `test_playback_smoke_foreground_wiring.cpp` (call-count 1→2), new `test_playback_smoke_fullscreen_wiring.cpp` (pins smoke-only use, order, verify+log, guard restore, menu stays hidden, telemetry gate reuse). Author: wiring mutation-checked; local gui_tests 41 passed / console_tests golden 362 passed; live `verified=1` deferred to next bachelor run.
- Menu entry remains hidden this round (wiring test asserts `setVisible(false)` and no `setVisible(true)`). 2018 disable comment treated as untested, not confirmed broken — scoped correctly to smoke path only.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound; tip required checks / branch protection still settling. Repo Hygiene + Batch Compile first signal; GUI Pilot / Product Oracles usual long poles; bachelor `verified=1` is post-merge evidence, not a merge blocker.
