schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 162
head: 0629be9d59b67b53f96f9b5ee4087627f1b3695f
reviewed_utc: 2026-09-25T12:23:39Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 0629be9d (+692/−0, 8 files, 4 commits; ahead 4 / behind 0): Protected Check Route + Analyze (actions) SUCCESS; CodeQL NEUTRAL; Tests (Batch Compile / Repo Hygiene ubuntu+windows / Windows GUI Pilot / Windows Product Oracles) IN_PROGRESS (run 36134535276); Factory Bridge Regressions IN_PROGRESS (36134535275); CodeQL Analyze c-cpp/python IN_PROGRESS (36134532938). MERGEABLE; mergeStateStatus BLOCKED (protection waiting on required checks). 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- `platform/qt/GpuDisplayWindow.h` / `.cpp`: the 2048-entry `m_swapTelemetryRing` is still written in `noteRealSwap` but never read (snapshot/summary only use `m_swapTelemetryCounters`). Carry-forward from CoS on #159/#161: either expose a bounded recent-ring slice on `GpuWindowSwapTelemetrySnapshot` for PresentMon join, or drop the ring until a consumer exists (keeps the ~80 KB footprint and the write path honest).

## Notes

- Supersedes open #161 (IDENTITY-2), which superseded open #159 (IDENTITY-1). Tip continues from #161 head `4dcea7e3` + IDENTITY-3 hub commit `0629be9d`. Same stack plus `tests/gui/gui_tests.pro`; +692/−0 vs master (net of master merge + round-2/3 fixes). Prefer closing or parking #159 and #161 once this lands so the IDENTITY stack is not triple-open.
- Addresses #161 contract BLOCKER (sol) and the hosted Windows GUI Pilot link miss CoS also missed on #159/#161:
  - **No work when telemetry is off:** `resetSwapTelemetry` / `swapTelemetrySnapshot` return before any QPC sample or state change when `!swapTelemetryEnabled()`; screenshot-path swap in `grabPresentedFramebufferIfActive` calls `noteRealSwap` only when telemetry is on (constructor already gated `frameSwapped` connect; `noteRealSwap` still early-returns on the gate).
  - **gui_tests links libgomp:** `tests/gui/gui_tests.pro` adds `win32: LIBS += -llibgomp-1` (matches `MLVApp.pro`), fixing `undefined reference to omp_get_wtime` via `mlv_stage_timing_now()` when `GpuDisplayWindow.o` is linked into gui_tests.
- Four new wiring tests pin each IDENTITY-3 gate (session begin/end no-work, capture-path gate, gui_tests.pro libgomp line); author says mutation-checked. Prior IDENTITY-1/2 wiring and pure summariser tests retained.
- Soft: per-swap `qInfo` under the smoke flag remains chatty on long legs — accept unless log volume becomes a gate problem.
- Author local evidence: app, gui_tests, and `console_tests --check-golden` (338/0) build and pass. Contract review deferred until hosted checks are green (contract v2.1). CoS did not re-run (Windows factory; review via gh only).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound; only tip required checks / branch protection blocking. Watch Windows GUI Pilot specifically — that is the check that failed to link on #159/#161 and this PR's explicit fix target.
