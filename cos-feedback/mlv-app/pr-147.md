schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 147
head: b48f5529449e1630b49bf74aae9dc34fdeafc82b
reviewed_utc: 2026-09-21T23:05:49Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip b48f5529449e (3 commits on product/ATTR3-VISUAL-QUALITY-EVIDENCE-1; +896/−291 across 14 files vs master): Protected Check Route SUCCESS; Analyze actions/c-cpp/python + Factory Bridge + Repo Hygiene Python (win/ubuntu) + Batch Compile + Windows Product Oracles + Windows GUI Pilot IN_PROGRESS/QUEUED; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip b48f5529449e, especially Windows GUI Pilot for `gpuDisplayWindowGrabsPresentedFramebufferReadback` / GL readback path, Repo Hygiene Python for five-file AttrCuda closure fixtures, and the new Tests step `Test color-artifact scan capture-invalid contract` that runs `test-gui-smoke-color-artifact-scan.ps1`)

## Improvements

- Soft: when `GpuDisplayWindow::isActive()` but `grabPresentedFramebufferIfActive` fails, `MainWindow::runGuiPlaybackSmoke` still falls through to the QGraphicsView viewport grab — the same hidden/undersized path this lane exists to replace. Prefer fail-closed (or skip color-scan) rather than a known-blank capture that only `capture-invalid` / `capture-too-dark` can catch after the fact (`platform/qt/MainWindow.cpp` screenshot branch). Unchanged on round 3.
- Soft: still **0 GitHub reviews** on a GL-window capture + smoke-runner closure + scanner-threshold change; a short human glance at `grabPresentedFramebufferIfActive` (re-`paintGL` + `glReadPixels`) and the `capture-too-dark` peak/skew rule is cheap.

## Notes

- Round 3 (`b48f5529449e`) vs prior CoS head `fee13393dcfe`: adds `capture-too-dark` failing verdict (black-pedestal = peak channel <96 AND channel skew >1.3; AND-combined so dark-but-correct / receipt-B shapes pass); wires `test-gui-smoke-color-artifact-scan.ps1` into `.github/workflows/tests.yml` next to the provenance test; documents known part-1 limitations (GPU-recon texture path never satisfies `-RequireFreshScreenshotRender`; `paintGL`/`updateTextureIfNeeded` can capture one frame ahead of logged provenance — fail-closed via provenance, not false-clean); stale closure comments now say explicitly pinned five-file manifest (never mechanically derived).
- Part 1 of ATTR3-VISUAL-QUALITY-EVIDENCE-1: GL-window screenshots were blank; fix is `GpuDisplayWindow::grabPresentedFramebufferIfActive` + provenance method `gl_window_framebuffer_readback`; color scan extracted to `gui-smoke-color-artifact-scan.ps1` with `capture-invalid` for tiny/uniform. Round 2 pinned that script into AttrCuda smoke-runner closure (four→five files).
- Documented scanner gap (pinned in unit test): dark low-peak but channel-balanced captures still pass — intentional conservative AND rule.
- Owner laptop evidence (PR body): real 3312×2059 captures at scale 4/1; separate picture defects deferred to part 2. No screen-region capture.
- Prior soft Improvements not addressed on this push. CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).