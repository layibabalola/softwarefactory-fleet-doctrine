schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 147
head: 024cc2ded0170e24b8d241f42cba484ea678b0d6
reviewed_utc: 2026-09-22T02:59:33Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 024cc2ded017 (6 commits on product/ATTR3-VISUAL-QUALITY-EVIDENCE-1; +1577/−319 across 18 files vs master): all tip checks IN_PROGRESS/pending at review (Protected Check Route, Factory Bridge, Repo Hygiene Python win/ubuntu, Batch Compile, CodeQL actions/c-cpp/python); mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 024cc2ded017, especially Protected Check Route / Repo Hygiene Python for the restored contiguous `frame=%7 serial=%8 generation=%9` log substring + provenance `gl-window-active-non-gl-capture-method` contract, Windows GUI Pilot for fail-closed GL readback + MLV_SKIP_OR_FAIL_IF_READBACK_FAILED, and Batch Compile for MainWindow/GpuDisplayWindow)

## Improvements

- Soft: still **0 GitHub reviews** on a GL-window capture redesign + smoke-runner closure + scanner-threshold change; a short human glance at capture-by-real-present (`paintGL` + in-call `glReadPixels` + `swapBuffers`) and the `capture-too-dark` peak/skew rule remains cheap.
- Soft (optional): round 6 closes the known-blank fallback, but there is still no positive GUI test that asserts `runGuiPlaybackSmoke` returns 12 / `gl_window_readback_required_but_failed` when the GL window is active and readback fails (only provenance refuses the bad method after the fact). Cheap if a injectable failure hook already exists; skip if it would force a fake GL path.

## Notes

- Round 6 (`024cc2ded017`) vs prior CoS head `691423aa4470` (round 5): addresses **both** prior soft Improvements that lived in code — (1) when `GpuDisplayWindow::isActive()` and `grabPresentedFramebufferIfActive` fails, `MainWindow::runGuiPlaybackSmoke` now fails closed (exit 12, token `gl_window_readback_required_but_failed`) instead of falling through to pixmap/viewport grab; (2) stale ~9310 comment rewritten for capture-by-real-present (may promote pending one paint early). Also: CI regression restore of contiguous `frame=%7 serial=%8 generation=%9` in the screenshot log line (hosted `test_process_hygiene_contract.py`); provenance V1+V2 add `gl-window-active-non-gl-capture-method`; three GL coherence tests use `MLV_SKIP_OR_FAIL_IF_READBACK_FAILED` under `MLV_REQUIRE_GL_TESTS=1`; `grabPresentedFramebufferIfActive` loads `g_activeWindow` under `g_activeMutex`; closure docs in `attr3-stage-smoke-runner-job.ps1` / `test_playback_attr_3_cuda_behaviour.py` aligned to pinned five-file manifest.
- Prior soft Improvement (fail-closed MainWindow fallback) **addressed**. Prior soft Improvement (stale promote-never comment) **addressed**. Soft human-review Improvement still open.
- Part 1 of ATTR3-VISUAL-QUALITY-EVIDENCE-1 unchanged in scope: `grabPresentedFramebufferIfActive` + method `gl_window_framebuffer_readback`; color scan in `gui-smoke-color-artifact-scan.ps1` with `capture-invalid` / `capture-too-dark`; AttrCuda smoke-runner closure five files. Documented scanner gaps (balanced dark still passes; tinted dark can false-positive) unchanged. Owner laptop evidence in PR body; no screen-region capture. Part 2 still deferred.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
