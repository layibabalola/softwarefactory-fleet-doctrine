schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 147
head: fee13393dcfebdaadcca84324ee7cd9f5b173b63
reviewed_utc: 2026-09-21T22:36:37Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip fee13393dcfe (2 commits on product/ATTR3-VISUAL-QUALITY-EVIDENCE-1; +703/−271 across 10 files vs master): Protected Check Route SUCCESS; Analyze actions/c-cpp/python + Factory Bridge + Repo Hygiene Python (win/ubuntu) + Batch Compile + Windows Product Oracles + Windows GUI Pilot IN_PROGRESS; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip fee13393dcfe, especially Windows GUI Pilot for `gpuDisplayWindowGrabsPresentedFramebufferReadback` / GL readback path and Repo Hygiene Python for the five-file AttrCuda smoke-runner closure fixtures)

## Improvements

- Soft: when `GpuDisplayWindow::isActive()` but `grabPresentedFramebufferIfActive` fails, `MainWindow::runGuiPlaybackSmoke` still falls through to the QGraphicsView viewport grab — the same hidden/undersized path this lane exists to replace. Prefer fail-closed (or skip color-scan) rather than a known-blank capture that only the new `capture-invalid` guard can catch after the fact (`platform/qt/MainWindow.cpp` screenshot branch).
- Soft: still **0 GitHub reviews** on a GL-window capture + smoke-runner closure change that already needed a round-2 pin after the AttrCuda AST census refused the unstaged dot-source; a short human glance at `grabPresentedFramebufferIfActive` (re-`paintGL` + `glReadPixels`) and the five-file closure manifest is cheap.

## Notes

- Part 1 of ATTR3-VISUAL-QUALITY-EVIDENCE-1: GL-window mode screenshots were blank (150×45 internal placeholder / empty `QWidget::grab` viewport that cannot see `GpuDisplayWindow`). Fix: `GpuDisplayWindow::grabPresentedFramebufferIfActive` re-renders retained presentation state then `glReadPixels` (avoids post-swap undefined back-buffer that `grabFramebuffer()` measured as solid black). Frame screenshot uses that readback; window screenshot composites it onto the main-window grab. Provenance method `gl_window_framebuffer_readback`.
- Color-artifact scan extracted to `tools/profiling/gui-smoke-color-artifact-scan.ps1` with unit tests (`test-gui-smoke-color-artifact-scan.ps1`): tiny or uniform images now verdict `capture-invalid` (failing) instead of false `clear-heuristic`. Runner accepts the new GL-window method alongside `app_internal_gl_viewport_grab`.
- Round 2 (`fee13393dcfe`): pin `gui-smoke-color-artifact-scan.ps1` into `Get-AttrCudaSmokeRunnerClosureManifest` (four → five files) after round 1’s unstaged dot-source tripped `ATTRCUDA_UNCLASSIFIED_SCRIPT_REFERENCE` / `test_the_real_current_repo_at_head_classifies_cleanly` — tripwire working as designed. Fixtures + census tests updated throughout `test_playback_attr_3_cuda_behaviour.py`.
- Owner laptop evidence (PR body): tracked 16-frame fixture yields real 3312×2059 captures with provenance at scale 4 and 1; also surfaced separate picture defects (GPU texture near-black at scale 1; magenta cast / cyan speckles) deferred to part 2 (reference-render judge + attribution wiring). No screen-region capture anywhere.
- GUI unit coverage: fail-without-active-window + present-and-readback (QSKIP offscreen / unavailable GL). CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
