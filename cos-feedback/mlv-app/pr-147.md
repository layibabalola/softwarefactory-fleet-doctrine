schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 147
head: 691423aa447037b5727e3667ef7a5143498e7356
reviewed_utc: 2026-09-22T00:41:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 691423aa4470 (5 commits on product/ATTR3-VISUAL-QUALITY-EVIDENCE-1; +1480/−309 across 18 files vs master): Protected Check Route PASS; Analyze (actions) PASS; CodeQL skipping; Analyze c-cpp/python + Factory Bridge + Repo Hygiene Python (win/ubuntu) + Batch Compile + Windows Product Oracles + Windows GUI Pilot pending/IN_PROGRESS; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 691423aa4470, especially Windows GUI Pilot for capture-by-real-present + serial-threaded GPU-recon path, Repo Hygiene Python for AttrCuda five-file closure fixtures, and Tests step `Test color-artifact scan capture-invalid contract` / provenance V2 serial cross-check)

## Improvements

- Soft: when `GpuDisplayWindow::isActive()` but `grabPresentedFramebufferIfActive` fails, `MainWindow::runGuiPlaybackSmoke` still falls through to `ui->graphicsView->viewport()->grab()` / pixmap (`app_internal_gl_viewport_grab` / `app_internal_viewport_grab`) — the same hidden/undersized path this lane exists to replace. Prefer fail-closed (refuse screenshot / skip color-scan) rather than a known-blank capture that only `capture-invalid` / `capture-too-dark` can catch after the fact (`platform/qt/MainWindow.cpp` screenshot branch ~9289–9330). Still open after rounds 4–5.
- Soft: still **0 GitHub reviews** on a GL-window capture redesign + smoke-runner closure + scanner-threshold change; a short human glance at capture-by-real-present (`paintGL` + in-call `glReadPixels` + `swapBuffers`) and the `capture-too-dark` peak/skew rule is cheap.
- Soft: stale comment in `MainWindow::runGuiPlaybackSmoke` (~9310) still says the captured serial is "never a frame promoted just for this capture"; round 5 capture-by-real-present intentionally promotes pending state on the capture paint (owner-visible: one paint early). Align the comment with `GpuDisplayWindow.h` / grab header docs.

## Notes

- Round 5 (`691423aa4470`) vs prior CoS head `b48f5529449e` (round 3): round 4 added presentationSerial / exact-swap identity, shared `Get-ColorArtifactFailingVerdicts`, loud `-FailOnColorArtifact` without `-CaptureScreenshot`, and GUI race tests; round 5 replaces capture-only re-render (`m_captureRenderInProgress`) with capture-by-real-present (`m_captureReadbackRequested` inside `paintGL`, then `swapBuffers`), threads `presentationSerial` into the GPU-recon texture route (`MainWindow` passes `task.requestSerial`), promotes pending→presented serial uniformly in `paintGL` for both routes, and makes `gui-smoke-screenshot-provenance.ps1` V2 fail-closed on `gl_window_presented_serial` mismatch / `serial_valid=0`. New GUI tests: capture presents pending as real paint; failed recon submit does not corrupt prior identity; `MLV_REQUIRE_GL_TESTS=1` hard-fails offscreen skip (not set in offscreen CI).
- Prior soft Improvement (fail-closed MainWindow fallback) **not** addressed. Related Notes-era race (GPU-recon / one-frame-ahead) **is** redesigned via capture-by-real-present + serial on both routes (tradeoff documented: capture can present one paint early).
- Part 1 of ATTR3-VISUAL-QUALITY-EVIDENCE-1: GL-window screenshots were blank; fix remains `grabPresentedFramebufferIfActive` + method `gl_window_framebuffer_readback`; color scan extracted to `gui-smoke-color-artifact-scan.ps1` with `capture-invalid` / `capture-too-dark`. Round 2 pinned that script into AttrCuda smoke-runner closure (four→five files).
- Documented scanner gap (dark low-peak but channel-balanced still passes) unchanged; dark strongly tinted scenes can false-positive on `capture-too-dark` (documented beside thresholds).
- Owner laptop evidence (PR body): real 3312×2059 captures at scale 4/1; separate picture defects deferred to part 2. No screen-region capture.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).