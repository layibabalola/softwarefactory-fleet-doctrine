schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 113
head: 5a8a9989686dff36d6a96b4115cd2de0889b7b0f
reviewed_utc: 2026-09-15T04:17:24Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Fresh push re-ran checks; CodeQL Analyze (actions/python/c-cpp), Factory Bridge, Protected Check Route, Repo Hygiene (ubuntu+windows), Batch Compile all in progress/queued at review time. No content blockers.

## Blockers

(none)

## Improvements

- Both new fields files still lack a trailing newline (`fields-PLAY-COUNTERS-CPU-A.md`, `fields-PLAY-COUNTERS-CPU-B.md`). Add `\n` at EOF before merge or in a tiny follow-up so editors/CI newline-at-EOF rules stay quiet.
- PR body says `queue.json` is reconciled locally after merge. After this lands, confirm the hub actually rewrites the PLAY-COUNTERS-CPU → -A/-B queue rows (or deletes the old id) so dispatch does not keep pointing at the deleted `fields-PLAY-COUNTERS-CPU.md`.
- Optional cross-link in Notes/roadmap: card B's VERIFY_FIRST requires `PlaybackGatePolicy.h` present (CPU-A merged). If the hub ever dispatches B before A, it STOPs with DEPENDENCY-UNMET — worth a one-line conductor note next to DEPENDS_ON so operators do not treat both priority-7 rows as parallel.

## Notes

- This push is another merge of `fork/master` into `docs/play-counters-cpu-split-20260915` (brought #111 typed dispatch-attempt receipts / coordination guardrails onto the branch). The A/B fields / roadmap / parity-test delta vs master is unchanged from the prior CoS review at `f8ee912ad38d42acd28e80f35c41109cdf7cd428` / original tip `44cf57af22384c9d027b5f27cb8446832096b72f`.
- Scope: docs / lane-prompt fields. Files: delete `fields-PLAY-COUNTERS-CPU.md`; add `-A` (policy) and `-B` (cumulative wiring); roadmap 15→16 rows; parity test expected count 15→16.
- Motivation matches prior STOP evidence: old card's decode input was live queue depth (`m_decodeRequests.size()`), parity was a function-local static, and ALLOWED_PATHS could not cover MainWindow.h / RenderFrameThread.h. Split is the right shape.
- Fable `RATIFY_WITH_EDITS` / `choice=split` edits look applied: B snapshots decode counter (no reset) into `m_playbackSmokeStartDecodeRequestsIssued`, evaluate once in `finishPlaybackSmokeTelemetry` with frames-expected, A adds no call site, B DEPENDS_ON A.
- A wiring checker is deliberately falsified on a fixture (no real MainWindow call yet); B un-skips the real-file assertion. Gate must FAIL when parity evidence is missing — good.
- #111 has now merged on master (visible via this merge-in). PR body still says the loop stays disabled until #111 merges and one supervised CPU-A run pushes — the #111 gate is cleared; do not treat merge of #113 alone as a green light to unattended-run A/B without that supervised push.
- Prior CoS Improvements not addressed by this merge-in push (trailing newlines still missing).
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.
