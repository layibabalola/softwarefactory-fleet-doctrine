schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 113
head: 44cf57af22384c9d027b5f27cb8446832096b72f
reviewed_utc: 2026-09-15T00:47:00Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: All checks still in progress at review (Repo Hygiene ubuntu+windows, Protected Check Route, Batch Compile, Factory Bridge, CodeQL Analyze actions/python/c-cpp). No content blockers.

## Blockers

(none)

## Improvements

- Both new fields files lack a trailing newline (`fields-PLAY-COUNTERS-CPU-A.md`, `fields-PLAY-COUNTERS-CPU-B.md`). Add `\n` at EOF before merge or in a tiny follow-up so editors/CI newline-at-EOF rules stay quiet.
- PR body says `queue.json` is reconciled locally after merge. After this lands, confirm the hub actually rewrites the PLAY-COUNTERS-CPU → -A/-B queue rows (or deletes the old id) so dispatch does not keep pointing at the deleted `fields-PLAY-COUNTERS-CPU.md`.
- Optional cross-link in Notes/roadmap: card B's VERIFY_FIRST requires `PlaybackGatePolicy.h` present (CPU-A merged). If the hub ever dispatches B before A, it STOPs with DEPENDENCY-UNMET — worth a one-line conductor note next to DEPENDS_ON so operators do not treat both priority-7 rows as parallel.

## Notes

- Scope: docs / lane-prompt fields only. 1 commit / +71−33 over `master`. Files: delete `fields-PLAY-COUNTERS-CPU.md`; add `-A` (policy) and `-B` (cumulative wiring); roadmap 15→16 rows; parity test expected count 15→16.
- Motivation matches prior STOP evidence: old card's decode input was live queue depth (`m_decodeRequests.size()`), parity was a function-local static, and ALLOWED_PATHS could not cover MainWindow.h / RenderFrameThread.h. Split is the right shape.
- Fable `RATIFY_WITH_EDITS` / `choice=split` edits look applied: B snapshots decode counter (no reset) into `m_playbackSmokeStartDecodeRequestsIssued`, evaluate once in `finishPlaybackSmokeTelemetry` with frames-expected, A adds no call site, B DEPENDS_ON A.
- A wiring checker is deliberately falsified on a fixture (no real MainWindow call yet); B un-skips the real-file assertion. Gate must FAIL when parity evidence is missing — good.
- Not in scope: dispatching either card. Loop stays disabled until MLV-App #111 merges and one supervised CPU-A run pushes — do not treat merge of #113 as a green light to unattended-run A/B.
- Author claims local composer + `test_roadmap_queue_parity` (15 tests) + `check_pinned_tokens.py` green — confirm via Repo Hygiene once it settles.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.
