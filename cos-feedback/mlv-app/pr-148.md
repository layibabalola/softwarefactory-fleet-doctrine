schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 148
head: 1d07da09b3209070efd5bf24997a8342b9b12d5c
reviewed_utc: 2026-09-22T00:30:01Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 1d07da09 (+2491/−14, 8 files vs master): all observed tip checks IN_PROGRESS (Protected Check Route, Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, Analyze actions+c-cpp+python); mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 1d07da09, especially Repo Hygiene Python windows-latest covering round-4 authority/param-set, exclusive CreateNew publish integrity, ConvertTo-Attr3FootageStageSafeOutput path-free rows, per-part resume via Get-Attr3FootagePresentPartIndexes, Remove-AttrCudaOwnerFootageStagingResidue, and presence TARGET_PATH_UNSAFE / link-check rows; mergeable_state blocked until checks settle)

## Improvements

- Soft: `Resolve-Attr3StagePython` in `attr3-footage-stage.ps1` still mirrors the presence-job / board-health-sweep Python-3 probe; optional shared-helper note unchanged. Not a merge blocker.
- Soft: still **0 GitHub reviews** on the bachelor owner-footage / NA-4 surface; short human glance before merge is cheap. Round 4 closed further external sol/astra findings in-commit, but that is not a GitHub review stamp.

## Notes

- Round 4 (1d07da09) vs prior Cos head a7e78cb4: closes sol/astra round-3 hub-swarm findings. Authority: `-AgentShare` / `-AgentRootOnHost` removed from the public param set (fixed constants `\\bachelor\\mlv-agent` and `C:\\mlvtmp\\mlv-agent`); CLI is exactly `-ClipId`, `-TimeoutSec`. Publish integrity: share-side and target-volume partials use exclusive `[IO.FileMode]::CreateNew` (refuse pre-existing partial; never `Copy-Item -Force`); post-rename re-hash mismatch removes the target this attempt published (`PLACED_VERIFY_*`) rather than leaving corrupt bytes under a PLACED-shaped status. Path-free: `ConvertTo-Attr3FootageStageSafeOutput` re-emits only fixed-shape `PART=`/`RESULT=` lines; remaining unwrapped Test-Path/File.Move sites wrapped to fixed tokens. Per-part resume: presence preflight via `Get-Attr3FootagePresentPartIndexes` transfers only missing parts; any failure (and success path after submit) calls `Remove-AttrCudaOwnerFootageStagingResidue` on this attempt's `footage-stage/<jobId>` under the share root. Link checks: stage job refuses a staged leaf that is a reparse point before hashing; presence job applies the same target-chain check (`TARGET_PATH_UNSAFE` in the uncertain bucket). Author notes 663 unittest OK (1 skipped needs elevation).
- Prior CoS soft Improvement on orphan share residue after failed/timeout submit: **addressed** by `Remove-AttrCudaOwnerFootageStagingResidue` on refuse and staged-success paths.
- Scope unchanged: id-only stage CLI → resolver → Send-AttrCudaOwnerFootagePartToStaging (does **not** widen UmRunDrop allowlist) → stage job place/clean; reports index/status only.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged: merge-when-ci-green.
