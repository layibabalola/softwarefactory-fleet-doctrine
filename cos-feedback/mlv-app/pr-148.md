schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 148
head: 5340e725e0996992c04cbe40130f573dcfeb605d
reviewed_utc: 2026-09-21T22:43:03Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 5340e725 (+1040/−0, 5 files vs master): all tip checks IN_PROGRESS (Analyze actions+c-cpp+python, Factory Bridge Regressions, Protected Check Route, Batch Compile, Repo Hygiene Python ubuntu+windows); mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 5340e725, especially Repo Hygiene Python windows-latest covering SendPartToStagingTests, FootageStageJobTests outcome matrix, 8.3 AgentRoot short-name admission, and the sentinel no-path-in-output rows; mergeable_state blocked until checks settle)

## Improvements

- Soft: `Resolve-Attr3StagePython` in `attr3-footage-stage.ps1` still mirrors the presence-job / board-health-sweep Python-3 probe; same optional shared-helper note as prior ATTR3 CoS (`Resolve-AttrCudaOwnerPython` / presence-job copies). Not a merge blocker.
- Soft: if `um-run.ps1` submit fails or times out after share transfer, `footage-stage/<jobId>/part-*` may remain on the agent share (job cleanup only runs when the bachelor job actually executes). Consider a CLI `finally` that removes only this script's `shareStageDir` on non-zero submit, or document orphan cleanup for operators.
- Soft: still **0 GitHub reviews** on the bachelor owner-footage / NA-4 surface; short human glance before merge is cheap. Local synth tests are not a substitute for a GitHub review stamp.

## Notes

- Scope: ATTR3-FOOTAGE-STAGE-1 — id-only `attr3-footage-stage.ps1` resolves via `resolve_consented_clip.py --emit-json`, verifies every source part (`Test-AttrCudaFootagePart`), transfers via new `Send-AttrCudaOwnerFootagePartToStaging` (neutral `part-<n>`, `.partial` + share-side re-hash + non-overwriting rename; does **not** widen UmRunDrop side-file allowlist), then emits/submits `Attr3FootageStageJob.psm1` job that re-verifies, places non-overwriting at resolver spec path, cleans only its own staged slots; reports index/status only (`FOOTAGE_STAGED` / `FOOTAGE_STAGE_REFUSED`).
- Round 2 (5340e725): admits `~` in `-AgentRoot` / `-AgentRootOnHost` ValidatePattern for Windows 8.3 TEMP segments (OBABAL~1); dedicated short-name AgentRoot test so suite is not host-TEMP-dependent.
- Tests (`test_attr3_footage_stage.py`): synthetic parts + fake share; transfer idempotency/conflict/tamper; job outcome matrix (placed, already-present, conflict, corrupt/missing staged); base64-only path embedding; real-resolver refusal for non-existent clip id; pins `Attr3FootageStageJob.psm1` in `_CLAIM_CARRIERS_PINNED` (`test_mlv_never_authorized.py` +1).
- Path hygiene looks intentional and consistent with ATTR3-FOOTAGE-BIND / presence-job precedents (base64 path bake, no caller-typed path param, TOKEN sentinel). CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).