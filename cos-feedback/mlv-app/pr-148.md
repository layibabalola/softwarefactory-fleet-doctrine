schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 148
head: 90960d51ad96d29def832c0d7d1af088b2e6ced0
reviewed_utc: 2026-09-22T03:00:23Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 90960d51 (+3618/−14, 8 files vs master; +386/−29 vs prior CoS head 8fae501e): Protected Check Route PASS; other tip checks IN_PROGRESS (Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, Analyze actions+c-cpp+python, Windows Product Oracles, Windows GUI Pilot); mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 90960d51, especially Repo Hygiene Python windows-latest covering round-6 residue-marker identity binding, RESIDUE_MARKER_REMOVAL_FAILED, stale-sweep floors / ValidateRange, and TimeoutSec binder-leak tests; mergeable_state blocked until checks settle)

## Improvements

- Soft: `Resolve-Attr3StagePython` in `attr3-footage-stage.ps1` still mirrors the presence-job / board-health-sweep Python-3 probe; optional shared-helper note unchanged. Not a merge blocker.
- Soft: still **0 GitHub reviews** on the bachelor owner-footage / NA-4 surface; short human glance before merge is cheap. Round 6 closed further hub-swarm findings in-commit, but that is not a GitHub review stamp.

## Notes

- Round 6 (90960d51) vs prior CoS head 8fae501e: closes hub-reproduced round-5 swarm findings (sol/astra). Residue identity binding: `.attr3-footage-stage-verify-failed` marker now records length/sha256/last-write-time/jobId of the retained bytes at write time; a later run re-derives and compares all fields before deleting, refusing `TARGET_CONFLICT` (both files untouched) on any mismatch instead of trusting the marker's mere presence. Marker removal after successful recovery is verified (`RESIDUE_MARKER_REMOVAL_FAILED` if the marker cannot be removed). Stale sweeps: target-volume partial sweep proves the target directory chain is link-free before enumerate, matches only this tool's generated partial-name grammar, and floors staleness at `$MinStaleResidueFloorSec` (300) independently of caller threshold; share-side `Remove-AttrCudaOwnerFootageStaleAttempts` and job `-StaleResidueAfterSec` both get `[ValidateRange(30, …)]` plus an independent floor. Binder leak: `-TimeoutSec` is now `[string]` with body-level `TryParse` / min-30 check emitting fixed `ATTR3_FOOTAGE_STAGE_TIMEOUT_SEC_INVALID` (never echoes the caller value). New tests cover owner-replaced target beside stale marker, marker-removal-failure token, ValidateRange rejections, path-shaped TimeoutSec never reaching output, and zero/negative timeout fixed token.
- Prior CoS soft Improvements (shared Python helper; 0 GitHub reviews): **not addressed**.
- Scope unchanged: id-only stage CLI → resolver → Send-AttrCudaOwnerFootagePartToStaging (does **not** widen UmRunDrop allowlist) → stage job place/clean; reports index/status only.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged: merge-when-ci-green.
