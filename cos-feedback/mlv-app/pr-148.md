schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 148
head: 8fae501e9193b4dc84f8ee0c9e011b07c5cf1816
reviewed_utc: 2026-09-22T01:34:49Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 8fae501e (+3261/−14, 8 files vs master; +797/−27 vs prior CoS head 1d07da09): Protected Check Route PASS; other tip checks IN_PROGRESS (Repo Hygiene Python ubuntu+windows, Batch Compile, Factory Bridge Regressions, Analyze actions+c-cpp+python, Windows Product Oracles, Windows GUI Pilot); mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 8fae501e, especially Repo Hygiene Python windows-latest covering round-5 presence-index return-shape, publish-recovery / residue-marker, cleanup-ownership hooks, source link check, stale-attempt sweep, and output-channel / ClipId-token tests; mergeable_state blocked until checks settle)

## Improvements

- Soft: `Resolve-Attr3StagePython` in `attr3-footage-stage.ps1` still mirrors the presence-job / board-health-sweep Python-3 probe; optional shared-helper note unchanged. Not a merge blocker.
- Soft: still **0 GitHub reviews** on the bachelor owner-footage / NA-4 surface; short human glance before merge is cheap. Round 5 closed further hub-swarm findings in-commit, but that is not a GitHub review stamp.

## Notes

- Round 5 (8fae501e) vs prior CoS head 1d07da09: closes hub-reproduced round-4 swarm findings. Presence-index crash: `Get-Attr3FootagePresentPartIndexes` now returns a plain `@(...)` array (never `,@(...)`); caller `@(...)` wrapping no longer double-wraps into a 1-element Object[] that broke `[int]$presentIndex` for zero/one/many PASS indexes. Publish recovery: post-rename verify-fail removal is verified; failure reports `PLACED_VERIFY_FAILED_TARGET_RETAINED` and leaves `.attr3-footage-stage-verify-failed` residue marker so a later run self-heals instead of perpetual `TARGET_CONFLICT`. Output channels: um-run / UmRunDrop `Write-Host` discarded with `6>$null` at both submit sites; resolver refusal is a fixed token (no raw JSON/`UNKNOWN_ID` fold-in); `-ClipId` ValidatePattern moved to body check with fixed `ATTR3_FOOTAGE_STAGE_CLIP_ID_INVALID` (binder error echoed the value). Source link check: `Assert-AttrCudaNoLinkBelowRoot` on every resolver-designated source before hash/copy (`SOURCE_PATH_UNSAFE`). Cleanup ownership: target-volume local partial deleted only when this attempt's exclusive CreateNew succeeded (not on share-side open failure); `TestHookForceLocalSourceOpenFailurePartIndex` proves it. Interrupted-attempt residue: `Remove-AttrCudaOwnerFootageStaleAttempts` sweeps share-side stale jobId dirs at CLI start; stage job sweeps stale target-volume partials older than `-StaleResidueAfterSec` (= CLI `-TimeoutSec`). New tests cover real-CLI end-to-end presence paths, removal-failure residue, later-run self-heal, and cleanup-ownership.
- Prior CoS soft Improvements (shared Python helper; 0 GitHub reviews): **not addressed**.
- Scope unchanged: id-only stage CLI → resolver → Send-AttrCudaOwnerFootagePartToStaging (does **not** widen UmRunDrop allowlist) → stage job place/clean; reports index/status only.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict unchanged: merge-when-ci-green.