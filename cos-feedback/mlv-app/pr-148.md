schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 148
head: a7e78cb47556b4b3808f1bb91e907fabb545276a
reviewed_utc: 2026-09-21T23:39:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip a7e78cb4 (+1733/−5, 8 files vs master): Protected Check Route PASS; remaining tip checks IN_PROGRESS/pending (Analyze actions+c-cpp+python, Factory Bridge Regressions, Batch Compile, Repo Hygiene Python ubuntu+windows, Windows GUI Pilot, Windows Product Oracles); mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip a7e78cb4, especially Repo Hygiene Python windows-latest covering the round-3 import-order suite, presence/stage job-id uniqueness, same-volume placement, and no-path-in-output rows; mergeable_state blocked until checks settle)

## Improvements

- Soft: `Resolve-Attr3StagePython` in `attr3-footage-stage.ps1` still mirrors the presence-job / board-health-sweep Python-3 probe; same optional shared-helper note as prior ATTR3 CoS. Not a merge blocker.
- Soft: if `um-run.ps1` submit fails or times out after share transfer, `footage-stage/<jobId>/part-*` may remain on the agent share (job cleanup only runs when the bachelor job actually executes; CLI has no `finally` that removes only this script's `shareStageDir` on non-zero submit). Consider that cleanup or document orphan cleanup for operators.
- Soft: still **0 GitHub reviews** on the bachelor owner-footage / NA-4 surface; short human glance before merge is cheap. Round 3 closed external sol/astra findings in-commit, but that is not a GitHub review stamp.

## Notes

- Round 3 (a7e78cb4) vs prior Cos head 5340e725: fixes nested `Import-Module AttrCudaArtifacts.psm1 -Force` strip in `AttrCudaOwnerFootage.psm1`, `Attr3FootageStageJob.psm1`, and `Attr3FootagePresenceJob.psm1` (conditional import + `-Global`; import-order regression tests added — author notes 121 failures on 5340e725). Removes caller `-RepoRoot` (resolver/submitter from tracked checkout only); `-AgentShare` ValidatePattern fixed to `\host\share` shape.
- Placement: emitted stage job now uses owned per-attempt partial on the target volume + same-volume non-overwriting rename + post-placement re-hash before `PLACED` (closes cross-volume `File.Move` copy-then-delete gap). Reparse-point-free proof on staging chain before touch. Presence preflight short-circuits to `ALREADY_PRESENT`. Fresh per-attempt job ids (presence + stage) avoid `UMRUN_JOBID_IN_USE` on retry. Path hygiene tightened (JOB= opaque id; um-run failure → fixed token).
- Scope unchanged: id-only `attr3-footage-stage.ps1` → `resolve_consented_clip.py --emit-json` → `Send-AttrCudaOwnerFootagePartToStaging` (does **not** widen UmRunDrop allowlist) → stage job place/clean; reports index/status only.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior CoS soft Improvements not clearly addressed; verdict unchanged from merge-when-ci-green.