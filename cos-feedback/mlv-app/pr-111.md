schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 111
head: 601fb46859a9c0378f810e30a6a2af9a02718b64
reviewed_utc: 2026-09-14T22:07:28Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route pass; CodeQL skipping; Factory Bridge, Repo Hygiene (ubuntu+windows), Batch Compile, Windows Product Oracles, Windows GUI Pilot still in progress. No content blockers.

## Blockers

(none)

## Improvements

- `Invoke-Workstream.ps1` `Write-DispatchAttempt`: `launched`/`lane-starting` is overwritten by `lane-returned` in the same `dispatch-attempt.json`. That is fine for "final outcome," but operators who want both timestamps lose the pre-launch stamp. Optional: append an `attempts[]` array (schema bump) or keep `startedUtc` when rewriting to `lane-returned`.
- `Get-LaneWorkEvidence` for `codex` treats exit 0 alone as work completed (no envelope). Document that asymmetry next to the claude rules so a future codex envelope does not silently change the contract.
- Structural test `test_every_workstream_exit_after_the_run_dir_is_named_writes_an_attempt_receipt` only inspects `lines[i-1]`. A blank line between `Write-DispatchAttempt` and `exit` would false-fail; consider scanning a small lookback window.

## Notes

- Scope: 1 commit / +245−19 over `master`. Files: `Invoke-Lane.ps1`, `Invoke-Workstream.ps1`, `lane-provider-refusal.ps1`, `test_coordination_guardrails.py`, `tests/coordination/test_lane_containment.py`.
- (a) Pre-launch refusals now leave `dispatch-attempt.json` (`mlv-app/workstream-dispatch-attempt/v1`) with `outcome=refused-before-launch` and a typed `cause` (worktree-add-failed, existing-branch-has-work, kill-switch, ratio guard, procedure drift, etc.). Addresses the measured ~90 `PLAY-COUNTERS-CPU` run dirs that held only `lane-prompt.md`.
- (b) Lane receipt: `processEnded` vs `complete` split; `complete` / `state=complete` now require exit 0 plus, for claude, a result envelope with `is_error=false`, `subtype=success`, and `terminal_reason` absent or `completed`. New `state=ended-incomplete`. Falsifier covers the real 2026-09-14 `error_max_turns` envelope at exit 1 and exit 0.
- Timeout containment test correctly updated: timed-out run is `ended-incomplete` with `processEnded=true`, `complete=false` (no longer falsely `complete`).
- Integration test `test_a_real_refused_dispatch_leaves_a_typed_attempt_receipt` exercises non-dry-run refuse of an existing branch with work.
- Downstream readers of fleet receipts must stop treating `complete=true` / `state=complete` as "child exited"; old max_turns receipts were false positives.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.