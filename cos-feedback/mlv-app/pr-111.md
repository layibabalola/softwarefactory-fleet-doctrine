schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 111
head: b514026e48ec393a3580fc1e911f78862cda86d8
reviewed_utc: 2026-09-14T22:52:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Protected Check Route pass; Analyze (actions) pass; CodeQL skipping; Factory Bridge, Repo Hygiene (ubuntu+windows), Batch Compile, Windows Product Oracles, Windows GUI Pilot, Analyze (c-cpp/python) still in progress. No content blockers.

## Blockers

(none)

## Improvements

- `Invoke-Workstream.ps1` `Write-DispatchAttempt`: `launched`/`lane-starting` is still overwritten by `lane-returned` in the same `dispatch-attempt.json`. Operators who want both timestamps lose the pre-launch stamp. Optional: append an `attempts[]` array (schema bump) or keep `startedUtc` when rewriting to `lane-returned`.
- `Get-LaneWorkEvidence` for `codex` still treats exit 0 alone as work completed (no envelope). Document that asymmetry next to the claude rules so a future codex envelope does not silently change the contract.
- Structural test `test_every_workstream_exit_after_the_run_dir_is_named_writes_an_attempt_receipt` still only inspects `lines[i-1]`. A blank line between `Write-DispatchAttempt` and `exit` would false-fail; consider scanning a small lookback window.

## Notes

- R2 (head b514026e) addresses Sol R1 CHANGES_REQUESTED: DryRun exits now write `outcome=dry-run-not-launched` (early `if ($DryRun) { return }` removed from `Write-DispatchAttempt`); guarded script `trap` writes `cause=unhandled-error` and re-throws; receipt write failures also go to stderr; behavioural test injects a Get-FileHash throw via a directory procedure path; `hub-procedure.md` waits only on `reserved`/`running` (terminal states including `ended-incomplete` no longer block the hub forever); PR body truncation fixed out of band.
- Scope vs master: 2 commits / +292−20. Files: `Invoke-Lane.ps1`, `Invoke-Workstream.ps1`, `lane-provider-refusal.ps1`, `test_coordination_guardrails.py`, `tests/coordination/test_lane_containment.py`, `docs/lane-prompts/v2/hub-procedure.md`.
- (a) Pre-launch refusals leave `dispatch-attempt.json` (`mlv-app/workstream-dispatch-attempt/v1`) with typed `cause`. Addresses the measured ~90 `PLAY-COUNTERS-CPU` run dirs that held only `lane-prompt.md`.
- (b) Lane receipt: `processEnded` vs `complete` split; `complete` / `state=complete` require exit 0 plus, for claude, a result envelope with `is_error=false`, `subtype=success`, and `terminal_reason` absent or `completed`. New `state=ended-incomplete`. Falsifier covers the real 2026-09-14 `error_max_turns` envelope at exit 1 and exit 0.
- Timeout containment test correctly expects `ended-incomplete` with `processEnded=true`, `complete=false`.
- Prior CoS Improvements (timestamp overwrite, codex asymmetry docs, structural lookback) are unchanged on this head.
- Downstream readers must stop treating `complete=true` / `state=complete` as "child exited"; old max_turns receipts were false positives.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.
