schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 111
head: 05b7f025567695587bb3bc743a19ddb023d0926a
reviewed_utc: 2026-09-15T03:50:24Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Fresh push (docs comment/docstring + merge fork/master); Factory Bridge, Protected Check Route, Repo Hygiene (ubuntu+windows), Batch Compile, CodeQL Analyze (actions/c-cpp/python) all queued or in progress. No content blockers on review.

## Blockers

(none)

## Improvements

- `Invoke-Workstream.ps1` `Write-DispatchAttempt`: `launched`/`lane-starting` is still overwritten by `lane-returned` in the same `dispatch-attempt.json`. Operators who want both timestamps lose the pre-launch stamp. Optional: append an `attempts[]` array (schema bump) or keep `startedUtc` when rewriting to `lane-returned`.
- `Get-LaneWorkEvidence` for `codex` still treats exit 0 alone as work completed (no envelope). Document that asymmetry next to the claude rules so a future codex envelope does not silently change the contract.

## Notes

- R4 (head 05b7f025): comment/docstring-only fix on the PR tip (`a06da280`) plus merge of `fork/master` (`05b7f025`). Corrects the receipt-before-cleanup comment to admit a cleanup throw still overwrites the single-file receipt (append-only deferred to TOOL-DISPATCH-ATTEMPT-WRITE-FAILURE-1); structural audit docstring now describes the 3-line lookback window. No behavior change.
- Master merge brought `agents/factory-kernel-instance.md` (DOGFOOD kernel map). Its K4 row still documents the pre-fix `complete:true` on exit-1 `max_turns` and missing pre-launch receipts — this PR is the fix for both; after merge, that row should be refreshed so the map does not restate a closed gap.
- Prior CoS Improvements still open: timestamp overwrite on `lane-returned`; codex asymmetry documentation. Not addressed by this docs-only push.
- Scope vs prior reviewed tip 5e55bfc1: coordination files +2/−0 lines of comment/docstring; merge also adds kernel instance map (+27) and CLAUDE.md pointer (+3).
- (a) Pre-launch refusals leave `dispatch-attempt.json` (`mlv-app/workstream-dispatch-attempt/v1`) with typed `cause`. Addresses ~90 `PLAY-COUNTERS-CPU` run dirs that held only `lane-prompt.md`.
- (b) Lane receipt: `processEnded` vs `complete` split; `complete` / `state=complete` require exit 0 plus, for claude, a result envelope with `is_error=false`, `subtype=success`, and `terminal_reason` absent or `completed`. New `state=ended-incomplete`. Falsifiers cover the real 2026-09-14 `error_max_turns` envelope and each success conjunct.
- Downstream readers must stop treating `complete=true` / `state=complete` as "child exited"; old max_turns receipts were false positives.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.
