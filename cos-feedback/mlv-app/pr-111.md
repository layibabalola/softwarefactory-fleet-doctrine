schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 111
head: 5e55bfc1e50622bcedaef0ae79ed9f663e756fb4
reviewed_utc: 2026-09-15T00:44:43Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Fresh push; Factory Bridge, Protected Check Route, Repo Hygiene (ubuntu+windows), Batch Compile, CodeQL Analyze (actions/c-cpp/python) all queued or in progress. No content blockers on review.

## Blockers

(none)

## Improvements

- `Invoke-Workstream.ps1` `Write-DispatchAttempt`: `launched`/`lane-starting` is still overwritten by `lane-returned` in the same `dispatch-attempt.json`. Operators who want both timestamps lose the pre-launch stamp. Optional: append an `attempts[]` array (schema bump) or keep `startedUtc` when rewriting to `lane-returned`.
- `Get-LaneWorkEvidence` for `codex` still treats exit 0 alone as work completed (no envelope). Document that asymmetry next to the claude rules so a future codex envelope does not silently change the contract.

## Notes

- R3 (head 5e55bfc1) addresses degraded-mode review findings at b514026e: one falsifier envelope per remaining work-evidence conjunct (`subtype`, `terminal_reason`) so deleting either guard turns the suite red; absent `is_error` now reasons `envelope-is-error-absent` (not `envelope-is-error`); dry-run / kill-switch / ratio exits write the attempt receipt before worktree cleanup so a retire-gate throw cannot replace the cause; trap `launched` receipt now carries `laneExitCode`; structural exit audit starts at the `$runDir` assignment.
- Prior CoS Improvement on structural lookback: **addressed** — `test_every_workstream_exit_after_the_run_dir_is_named_writes_an_attempt_receipt` now scans a 3-line lookback window (and allows cleanup between receipt and exit).
- Prior CoS Improvements still open: timestamp overwrite on `lane-returned`; codex asymmetry documentation.
- Scope vs master: 3 commits / +310−20. Files: `Invoke-Lane.ps1`, `Invoke-Workstream.ps1`, `lane-provider-refusal.ps1`, `test_coordination_guardrails.py`, `tests/coordination/test_lane_containment.py`, `docs/lane-prompts/v2/hub-procedure.md`.
- (a) Pre-launch refusals leave `dispatch-attempt.json` (`mlv-app/workstream-dispatch-attempt/v1`) with typed `cause`. Addresses ~90 `PLAY-COUNTERS-CPU` run dirs that held only `lane-prompt.md`.
- (b) Lane receipt: `processEnded` vs `complete` split; `complete` / `state=complete` require exit 0 plus, for claude, a result envelope with `is_error=false`, `subtype=success`, and `terminal_reason` absent or `completed`. New `state=ended-incomplete`. Falsifiers cover the real 2026-09-14 `error_max_turns` envelope and each success conjunct.
- Timeout containment test expects `ended-incomplete` with `processEnded=true`, `complete=false`.
- Downstream readers must stop treating `complete=true` / `state=complete` as "child exited"; old max_turns receipts were false positives.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.
