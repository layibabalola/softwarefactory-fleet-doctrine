schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 133
head: 259bd1bc050a212f9cfd5df587084de6153ef641
reviewed_utc: 2026-09-17T00:06:19Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Protected Check Route / Windows GUI Pilot / CodeQL Analyze (actions/c-cpp/python) PASS; Repo Hygiene Python (ubuntu-latest) FAIL (16 failures in test_playback_attr_3_cuda_behaviour.py); Factory Bridge Regressions FAIL (1 agent-bridge reconnect counter assert); Repo Hygiene Windows / Batch Compile / Windows Product Oracles still pending; mergeStateStatus BEHIND (~44 commits incl. #132 upstream sync).

## Blockers

- **Repo Hygiene Python (ubuntu-latest) FAIL** — run 35164642135 job 105022890049: 16 failures in tools/repo_hygiene/test_playback_attr_3_cuda_behaviour.py (text-level test_playback_attr_3_cuda_split_route.py stayed green). Root cause cluster:
  - Windows-only AgentRoot ValidatePattern (`^[A-Za-z]:\\[A-Za-z0-9 _.~\\-]+$`) rejects Linux temp roots like `/tmp/attr3-…/agent`. Staging / DLL-pair generator setUps fail before any job body runs (InterruptedStagingTests, StagingNameSafetyTests generate path, SourceArchiveBindingTests._generate).
  - Smoke log / eligibility — Resolve-AttrCudaSmokeRunLog throws ATTRCUDA_SMOKE_LOG_OUTSIDE_ROOT on the happy path; Get-AttrCudaEligibilityVerdict cases expect `admitted=False exitCode=15` but get empty stdout (Linux path containment vs Windows-shaped Join-Path).
  - Assert-AttrCudaDirectChild happy path — test_the_shared_guard_requires_a_direct_child never prints the accepted `/tmp/…/ok.exe` path (only THREW lines for deeper/`..` cases).
  - Unblock: either (a) skip / gate the pwsh behavioural suites that invent AgentRoot on non-Windows (file already claims CI Windows has both), or (b) give ubuntu a Windows-drive-shaped agent root the ValidatePattern accepts and fix path-equality in Assert-AttrCudaDirectChild / smoke-log containment for `/` vs `\\`. Prefer (a) plus Windows-only behavioural so ubuntu stays green without pretending Bachelor runs on Linux. Confirm green on Repo Hygiene Python (windows-latest) once it settles — that is the intended host for these tests.

- **Factory Bridge Regressions FAIL** — run 35164642214: tools/agent-bridge/test_agent_bridge.py::AgentBridgeTests::test_bridge_process_status_marks_wrapper_reconnected_after_host_scoped_tool_proof — inner_restart_count_today got 0 expected 1 (impact_class / mcp_host_likely_reconnected already OK). This tip does not touch tools/agent-bridge/; prior tip d4bdd335 had Factory Bridge SUCCESS. Likely day-boundary / counter flake around UTC midnight (job ~00:02Z). Unblock: re-run the workflow on this tip; if it keeps failing, fix the today window in agent-bridge (out of scope for stage 1c content).

- **Branch BEHIND master** — tip is ~44 commits behind after #132 (sync/upstream-877dea2) landed. Unblock: update/rebase product/PLAYBACK-ATTR-3-CUDA-S1C onto current master, then re-run required checks (factory gap: update-branch after each master push).

## Improvements

- Live end-to-end still unproven on this tip: contract + behavioural VerifyOnly tests are not a full Ultra-Magnus to assemble to Bachelor stage to attribution receipt. Keep one owner-granted CLIP_OR_NONE run with dll-pair / build.json / stage / attribution artifacts.
- Parity harness default still non-fatal (-RequireParityHarness). Occasional sm_86+harness receipt still worth having.
- Hardcoded board-host Qt/MinGW paths in playback-attr-3-cuda-assemble.ps1 (C:\\Qt\\6.10.2\\mingw_64\\bin, C:\\Qt\\Tools\\mingw1310_64\\bin) unchanged.
- Stamped cache still names only the recon DLL; AMaZE + cudart stay inside the package zip — document or pin package-contents in text tests if anything loads from the stamped cache DLL alone.

## Notes

- Delta since prior CoS head d4bdd335 (pr-opened): six r2 commits addressing Sol blockers — source-archive sha+EOCD commit bind (exit 8), smoke log from result.json log.path + sha (exit 16), mandatory -BuildManifestSha256 before ConvertFrom-Json, staging names derived via Get-AttrCudaArtifactNames + Assert-AttrCudaSafeArtifactName/Assert-AttrCudaDirectChild (exit 6), -VerifyOnly / PubReady, -Confirm:false partial cleanup, plus test_playback_attr_3_cuda_behaviour.py (31 cases) and expanded split_route text pins. Content direction is sound; hosted CI on ubuntu is what flipped the verdict.
- Scope still tools/ + docs only (+3487/-111 across 9 paths). No footage tokens. Branch product/PLAYBACK-ATTR-3-CUDA-S1C to master.
- Attribution still fails closed exit 15 BACKEND_NOT_AVAILABLE unless cuda_backend_available=1 and r16_available=1. Compile-on-Bachelor remains retired.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
