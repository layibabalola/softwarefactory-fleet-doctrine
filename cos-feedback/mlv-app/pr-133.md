schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 133
head: 33d9597b9802442a875e0ae903c902a3f3929e80
reviewed_utc: 2026-09-17T01:36:17Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Protected Check Route PASS on tip; Analyze (actions/c-cpp/python), Batch Compile, Factory Bridge, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles in_progress; mergeStateStatus BEHIND (~44 commits after #132).

## Blockers

- **Branch BEHIND master** — tip still ~44 commits behind after #132 (sync/upstream-877dea2). Unblock: update/rebase `product/PLAYBACK-ATTR-3-CUDA-S1C` onto current master, then re-run required checks (factory gap: update-branch after each master push).

## Improvements

- Live end-to-end still unproven on this tip: contract + behavioural VerifyOnly / junction tests are not a full Ultra-Magnus → assemble → Bachelor stage → attribution receipt. Keep one owner-granted CLIP_OR_NONE run with dll-pair / build.json / stage / attribution artifacts.
- Parity harness default still non-fatal (`-RequireParityHarness`). Occasional sm_86+harness receipt still worth having.
- Hardcoded board-host Qt/MinGW paths in `playback-attr-3-cuda-assemble.ps1` (`C:\Qt\6.10.2\mingw_64\bin`, `C:\Qt\Tools\mingw1310_64\bin`) unchanged.
- Stamped cache still names only the recon DLL; AMaZE + cudart stay inside the package zip — document or pin package-contents in text tests if anything loads from the stamped cache DLL alone.

## Notes

- Delta since prior CoS head `f291e674`: one commit `33d9597b` (attr3 s1c r4) — never write through a link on publish; embed verifiers before first use (addresses sol r3 blockers, not prior CoS Improvements).
  - New `Assert-AttrCudaWritableFileSlot`: parent must exist and not be a reparse point; slot must be absent or a plain file (cleared); else `ATTRCUDA_SLOT_OCCUPIED` / `ATTRCUDA_SLOT_PARENT_IS_LINK` / `ATTRCUDA_SLOT_PARENT_MISSING` and nothing is written. Wired before every publish Copy/Move/Set-Content in DLL job, stage job, and assembler.
  - Attribution job template moves `__EMBEDDED_FUNCTIONS__` above first call (fixes command-not-found at job start).
  - Behavioural tests: junction `.partial` stage writes nothing outside; slot refusal (junction slot + linked parent); plain-file slot clear; static ordering that every embedded AttrCuda function is defined before its first call in each job template.
- Prior CoS Improvements (live e2e receipt, parity harness, hardcoded Qt paths, stamped-cache naming) not addressed by this tip.
- Scope still tools/ + docs only (+3795/-112 across 9 paths). No footage tokens. Branch `product/PLAYBACK-ATTR-3-CUDA-S1C` → master. mergeable MERGEABLE but BEHIND.
- Attribution still fails closed exit 15 `BACKEND_NOT_AVAILABLE` unless `cuda_backend_available=1` and `r16_available=1`. Compile-on-Bachelor remains retired.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skip: verdict unchanged; CoS Improvements not addressed.