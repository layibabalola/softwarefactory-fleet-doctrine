schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 133
head: d4bdd335f793f8c9da914b75fb01671373d6ae27
reviewed_utc: 2026-09-16T22:33:01Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route PASS on tip d4bdd335; Batch Compile, Repo Hygiene Python (ubuntu/windows), Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, CodeQL Analyze (actions/c-cpp/python) still IN_PROGRESS; mergeStateStatus BLOCKED (required checks unsettled).

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (ubuntu-latest + windows-latest), Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, and remaining CodeQL Analyze jobs on tip `d4bdd335`. Content looks mergeable once those settle green. Protected Check Route already SUCCESS.

## Improvements

- **Live end-to-end still unproven on this tip:** contract tests are text-level only (correct for generators that run on Ultra-Magnus / board host / Bachelor). Before treating stage 1c as operational, run one full split route and keep receipts: Ultra-Magnus `dll-pair-manifest.json` with `exitCode` 0 and `sm_86` proved via `cuobjdump`, assembler `playback-attr-3-cuda-<sha12>-build.json`, Bachelor stage `result.json`, then attribution under an owner-granted `CLIP_OR_NONE` lane.
- **Parity harness default is non-fatal:** DLL-pair job records `reconBackendScript.exitCode` / `parityHarnessPassed` but only fails on `-RequireParityHarness`. Runbook already explains sm_86-only SASS cannot LoadLibrary on Ultra-Magnus sm_89. Prefer an occasional `-CudaArchitectures sm_86,compute_86 -RequireParityHarness` receipt so the harness is exercised somewhere, not only documented.
- **Hardcoded board-host Qt/MinGW paths** in `playback-attr-3-cuda-assemble.ps1` (`C:\Qt\6.10.2\mingw_64\bin`, `C:\Qt\Tools\mingw1310_64\bin`). Fine if the board host is pinned; if paths drift, discovery (or a probe receipt) would fail earlier than qmake/make exits 12–15.
- **Stamped cache still names only the recon DLL** (`AttrCudaArtifacts.psm1` / staging triple). AMaZE + cudart ride inside the package zip (assemble requires both in the pair manifest and copies them into the release dir). Acceptable if attribution always unpacks the pkg; if anything ever loads from the stamped cache DLL alone, AMaZE would be missing — document that contract next to the naming module or add a package-contents pin in the text tests.

## Notes

- Scope: PLAYBACK-ATTR-3-CUDA stage 1c split build after Bachelor compile job exit 5 (`msvcDiscovery`). +1865/−100 across 8 paths (tools/ + docs only; no `src/` / product landing). Branch `product/PLAYBACK-ATTR-3-CUDA-S1C` → `master`.
- Route: Ultra-Magnus DLL pair (`playback-attr-3-cuda-dll-job.ps1`, both DLLs, sm_86 + cuobjdump proof, `pendingSymbolPresence` via dumpbin) → board-host assemble (`playback-attr-3-cuda-assemble.ps1` + `AttrCudaArtifacts.psm1`) → Bachelor staging (`playback-attr-3-cuda-stage-job.ps1`, hash-baked, build.json LAST) → attribution reads `pendingSymbolPresence` from manifest and fails closed exit 15 `BACKEND_NOT_AVAILABLE` unless `cuda_backend_available=1` and `r16_available=1`.
- `playback-attr-3-cuda-compile-job.ps1` retired: throws before emit; historical body kept unrun. Runbook rewrite includes trap "a precedent script is not a precedent result."
- Tests: `tools/repo_hygiene/test_playback_attr_3_cuda_split_route.py` (46 TestCase methods; named `split_route` so `.gitignore` `*build*` does not swallow it). Claims cover sm_86, both DLLs, no dumpbin/vswhere in attribution, BACKEND gate order, manifest-last publish, retired compile refusal, no footage tokens in new scripts.
- `mergeable` MERGEABLE; `mergeStateStatus` BLOCKED on unsettled required checks. No footage named in new tooling. CoS via `gh` (no Cloud Agent). No GitHub PR review comment (bus only).