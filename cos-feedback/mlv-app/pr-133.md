schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 133
head: 597f553c1815a2e73ec27625cef60673e7f18138
reviewed_utc: 2026-09-17T03:42:14Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Analyze (actions/c-cpp/python), Protected Check Route, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions in_progress; Batch Compile queued; Windows GUI Pilot / Product Oracles not yet listed; mergeStateStatus BEHIND (diverged: 44 behind master, 20 ahead).

## Blockers

- **Branch BEHIND master** — tip diverged: 44 commits behind current master (`80df58c0`, #132 sync) and 20 ahead. Unblock: update/rebase `product/PLAYBACK-ATTR-3-CUDA-S1C` onto current master, then re-run required checks (factory gap: update-branch after each master push).

## Improvements

- Live end-to-end still unproven on this tip: contract + behavioural VerifyOnly / junction / slot-check / AST publish-write tests are not a full Ultra-Magnus → assemble → Bachelor stage → attribution receipt. Keep one owner-granted CLIP_OR_NONE run with dll-pair / build.json / stage / attribution artifacts.
- Parity harness default still non-fatal (`-RequireParityHarness`). Occasional sm_86+harness receipt still worth having.
- Hardcoded board-host Qt/MinGW paths in `playback-attr-3-cuda-assemble.ps1` (`C:\Qt\6.10.2\mingw_64\bin`, `C:\Qt\Tools\mingw1310_64\bin`) unchanged.
- Stamped cache still names only the recon DLL; AMaZE + cudart stay inside the package zip — document or pin package-contents in text tests if anything loads from the stamped cache DLL alone.

## Notes

- Delta since prior CoS head `95430a64`: one commit `597f553c` (attr3 s1c r7b) — scanner rejects member-name ForEach-Object and link-type New-Item (sol r7 follow-up; not prior CoS Improvements).
  - `attr3_publish_write_scan.ps1` R2: ForEach-Object may take only script blocks (rejects `ForEach-Object Delete` / `-MemberName`); New-Item must name `-ItemType 'Directory'` or `'File'` literally (no Junction/SymbolicLink/HardLink under `$Work`). Documents `reg` and Start-Process/`&` child processes as accepted non-filesystem / out-of-scan mutations.
  - Behavioural fixtures added: `foreach_member_name`, `foreach_member_name_param`, `new_item_junction_under_work`, `new_item_missing_type` (all expected flagged).
- Prior CoS Improvements (live e2e receipt, parity harness, hardcoded Qt paths, stamped-cache naming) not addressed by this tip.
- Scope still tools/ + docs only (+4505/-123 across 10 paths). No footage tokens. Branch `product/PLAYBACK-ATTR-3-CUDA-S1C` → master. mergeable MERGEABLE but BEHIND.
- Attribution still fails closed exit 15 `BACKEND_NOT_AVAILABLE` unless `cuda_backend_available=1` and `r16_available=1`. Compile-on-Bachelor remains retired.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skip: verdict unchanged; CoS Improvements not addressed.
