schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 133
head: 59a125872c5c14e577f30fa93c54ac9feb78d6a1
reviewed_utc: 2026-09-17T02:54:30Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Protected Check Route PASS on tip; CodeQL pending; Analyze (actions/c-cpp/python), Batch Compile, Factory Bridge, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles pending; mergeStateStatus BEHIND (diverged: 44 commits behind master, 18 ahead).

## Blockers

- **Branch BEHIND master** — tip diverged: 44 commits behind current master and 18 ahead. Unblock: update/rebase `product/PLAYBACK-ATTR-3-CUDA-S1C` onto current master, then re-run required checks (factory gap: update-branch after each master push).

## Improvements

- Live end-to-end still unproven on this tip: contract + behavioural VerifyOnly / junction / slot-check / AST publish-write tests are not a full Ultra-Magnus → assemble → Bachelor stage → attribution receipt. Keep one owner-granted CLIP_OR_NONE run with dll-pair / build.json / stage / attribution artifacts.
- Parity harness default still non-fatal (`-RequireParityHarness`). Occasional sm_86+harness receipt still worth having.
- Hardcoded board-host Qt/MinGW paths in `playback-attr-3-cuda-assemble.ps1` (`C:\\Qt\\6.10.2\\mingw_64\\bin`, `C:\\Qt\\Tools\\mingw1310_64\\bin`) unchanged.
- Stamped cache still names only the recon DLL; AMaZE + cudart stay inside the package zip — document or pin package-contents in text tests if anything loads from the stamped cache DLL alone.

## Notes

- Delta since prior CoS head `19516730`: one commit `59a12587` (attr3 s1c r6) — structural publish-write invariant with an AST proof (addresses sol r5 blocker that the line scan was unsound; not prior CoS Improvements).
  - `Publish-AttrCudaText` / `Publish-AttrCudaFileCopy` / `Publish-AttrCudaFileMove` / `New-AttrCudaDirectory` in `AttrCudaArtifacts.psm1` perform the slot check and the write in ONE call; DLL / staging / attribution emitted jobs use them for outbox and cache writes; inbox side-file removal uses the plain-file remover.
  - New `tools/repo_hygiene/attr3_publish_write_scan.ps1` parses each template with the PowerShell AST and flags every raw write primitive (write cmdlets/aliases, redirections, `[IO.File]`/`[IO.Directory]` mutators, instance CopyTo/MoveTo/Delete, dynamic code) whose destination is not provably under the job work tree; positional or unproven destinations are violations.
  - Behavioural tests: real templates scan clean (commit notes 31 violations at `19516730`); 21 bypass fixtures including every evasion sol named are each flagged; 5 controls are not. Outbox created explicitly so its parent is link-checked.
- Prior CoS Improvements (live e2e receipt, parity harness, hardcoded Qt paths, stamped-cache naming) not addressed by this tip.
- Scope still tools/ + docs only (+4227/-123 across 10 paths). No footage tokens. Branch `product/PLAYBACK-ATTR-3-CUDA-S1C` → master. mergeable MERGEABLE but BEHIND.
- Attribution still fails closed exit 15 `BACKEND_NOT_AVAILABLE` unless `cuda_backend_available=1` and `r16_available=1`. Compile-on-Bachelor remains retired.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skip: verdict unchanged; CoS Improvements not addressed.
