schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 133
head: f291e6741d03a44b05b381488fc5555542e7c2f9
reviewed_utc: 2026-09-17T00:59:40Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Protected Check Route / Analyze (actions) PASS on tip; Batch Compile / Factory Bridge / Repo Hygiene (ubuntu+windows) / Analyze (c-cpp+python) / Windows GUI Pilot / Windows Product Oracles still pending or queued; CodeQL skipping; mergeStateStatus BEHIND (~44 commits after #132).

## Blockers

- **Branch BEHIND master** — tip still ~44 commits behind after #132 (sync/upstream-877dea2). Unblock: update/rebase `product/PLAYBACK-ATTR-3-CUDA-S1C` onto current master, then re-run required checks (factory gap: update-branch after each master push).

## Improvements

- Live end-to-end still unproven on this tip: contract + behavioural VerifyOnly / junction tests are not a full Ultra-Magnus → assemble → Bachelor stage → attribution receipt. Keep one owner-granted CLIP_OR_NONE run with dll-pair / build.json / stage / attribution artifacts.
- Parity harness default still non-fatal (`-RequireParityHarness`). Occasional sm_86+harness receipt still worth having.
- Hardcoded board-host Qt/MinGW paths in `playback-attr-3-cuda-assemble.ps1` (`C:\Qt\6.10.2\mingw_64\bin`, `C:\Qt\Tools\mingw1310_64\bin`) unchanged.
- Stamped cache still names only the recon DLL; AMaZE + cudart stay inside the package zip — document or pin package-contents in text tests if anything loads from the stamped cache DLL alone.

## Notes

- Delta since prior CoS head `bf1f717c`: one commit `f291e674` (attr3 s1c r3) — link-safe cleanup + snapshot length binding (addresses sol r2).
  - `Remove-AttrCudaPartialFile`: deletes a `.partial` only when it is a plain file; directory or reparse point left in place (avoids `Remove-Item -Recurse` following an NTFS junction out of the job root; PowerShell#26913).
  - `Remove-AttrCudaTree`: refuses any tree containing a reparse point before recursive delete; wired into assemble / attribution / stage / Ultra-Magnus job pre-cleans. Job scripts show zero remaining `Remove-Item -Recurse` hits.
  - `Resolve-AttrCudaSmokeRunLog` now requires integer `evidence.runLogSnapshot.length` and checks it (`ATTRCUDA_SMOKE_LOG_LENGTH_UNBOUND` / `ATTRCUDA_SMOKE_LOG_LENGTH_MISMATCH`).
  - Behavioural suite adds `LinkSafeCleanupTests` (real junctions; sentinel outside survives) plus length mismatch/unbound cases.
- Prior CoS Improvements (live e2e receipt, parity harness, hardcoded Qt paths, stamped-cache naming) not addressed by this tip.
- Scope still tools/ + docs only (+3677/-112 across 9 paths). No footage tokens. Branch `product/PLAYBACK-ATTR-3-CUDA-S1C` → master. mergeable MERGEABLE but BEHIND.
- Attribution still fails closed exit 15 `BACKEND_NOT_AVAILABLE` unless `cuda_backend_available=1` and `r16_available=1`. Compile-on-Bachelor remains retired.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
