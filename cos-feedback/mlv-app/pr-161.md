schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 161
head: 4dcea7e30189f2ef088263e3b215d44d2f233460
reviewed_utc: 2026-09-25T11:52:10Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 4dcea7e3 (+627/−0, 7 files, 3 commits; ahead 3 / behind 0): Protected Check Route SUCCESS; Tests (Batch Compile / Repo Hygiene ubuntu+windows / Windows GUI Pilot / Windows Product Oracles) IN_PROGRESS (run 36131614501); Factory Bridge Regressions IN_PROGRESS (36131614408); CodeQL Analyze actions/c-cpp/python IN_PROGRESS (36131611757). MERGEABLE; mergeStateStatus BLOCKED (protection waiting on required checks). 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- `platform/qt/GpuDisplayWindow.h` / `.cpp`: the 2048-entry `m_swapTelemetryRing` is still written in `noteRealSwap` but never read (snapshot/summary only use `m_swapTelemetryCounters`). Carry-forward from CoS on superseded #159: either expose a bounded recent-ring slice on `GpuWindowSwapTelemetrySnapshot` for PresentMon join, or drop the ring until a consumer exists (keeps the ~80 KB footprint and the write path honest).

## Notes

- Supersedes open #159 (IDENTITY-1). Branch continues from #159 head `89bc7d20` + merge of master `6b3e4f26` + IDENTITY-2 tip `4dcea7e3`. Same 7 owned paths; +627/−0 vs master (net of master merge + round-2 fixes).
- Addresses both #159 contract BLOCKERs in tip:
  - **True opt-in (BLOCKER 2):** constructor connects `frameSwapped`→`noteRealSwap` only when `swapTelemetryEnabled()`; `noteRealSwap` still early-returns on the gate; `playback_smoke.gpu_window_swaps` emits only under `swapSnapshot.telemetryEnabled`. Reuses `MLVAPP_PLAYBACK_SMOKE_TELEMETRY` ("set and not literally 0").
  - **Closed at the gate (BLOCKER 1):** `swapTelemetrySnapshot()` sets `g_swapTelemetrySessionActive = false` before returning; `noteRealSwap` refuses closed/no-session swaps so queued/screenshot swaps after the gate are not attributed. Snapshot still runs unconditionally in `finishPlaybackSmokeTelemetry` so the session always closes even when the summary line is skipped.
- Hardening: session id / active / begin-QPC live at file scope (survive inactive or recreated window); summary adds `head_gap_ms` / `tail_gap_ms` + first/last swap UTC for stall visibility beyond inter-swap `max_gap_ms`.
- Both real swap paths still covered: Qt automatic `frameSwapped` after `paintGL`, and explicit `glContext->swapBuffers(win)` in `grabPresentedFramebufferIfActive` (which does not emit `frameSwapped`).
- Tests: pure summariser tests (incl. head/tail gap) + expanded source-census wiring tests pinning gated connect, summary gate-after-snapshot, session close, and session-id-before-window-lookup. Author local evidence: app build OK; `console_tests --check-golden` 334/0; master repo-hygiene suites reading MainWindow/GpuDisplayWindow 391 OK; no bachelor run. CoS did not re-run (Windows factory; review via gh only).
- Soft: per-swap `qInfo` under the smoke flag remains chatty on long legs — accept unless log volume becomes a gate problem.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound; only tip required checks / branch protection blocking. Prefer closing or parking #159 once this lands so the IDENTITY-1 stack is not double-open.
