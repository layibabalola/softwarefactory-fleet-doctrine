schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 110
head: 8ec10472e32765c097179a00c02f1f15c10c7cb7
reviewed_utc: 2026-09-14T14:19:14Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route FAILED (artifact FinalizeArtifact 403 Forbidden after router success); Windows Product Oracles + Windows GUI Pilot fail-closed on unavailable route. Repo Hygiene ubuntu+windows, Batch Compile, Factory Bridge, CodeQL still in progress. Content review clean; re-run Tests workflow for the 403.

## Blockers

- **CI: `Protected Check Route`** — job derived the route successfully (`route.reason=UNKNOWN_OR_PRODUCT_PATH_RUN_REAL_ORACLES`, product+GUI required) then failed on `actions/upload-artifact` FinalizeArtifact HTTP 403. Not a content defect. Re-run the Tests workflow on head `8ec10472e32765c097179a00c02f1f15c10c7cb7` (or wait for a clean re-fire); do not change the PR to "fix" the 403.
- **CI cascade: `Windows Product Oracles` / `Windows GUI Pilot`** — both failed closed on unavailable product/GUI route after Protected Check Route failed. Same unblock: green Protected Check Route receipt, then re-run dependents.

## Improvements

- `Invoke-Lane.ps1` comment above `MaxTurns` still says "so 40 is a runaway guard" while the default is now 65. Tighten the prose so the measured baseline and the current backstop match.
- Protected-check router classified `tools/coordination/**` as `UNKNOWN_OR_PRODUCT_PATH_RUN_REAL_ORACLES` (forces real product + GUI oracles). Optional: add an explicit non-product credit path for coordination-only diffs so loop-tool PRs do not pay the full Windows oracle tax every time (policy call, not a merge gate).
- `Invoke-Workstream.ps1` reuse path: when `branch -f` fails because the branch is checked out elsewhere, exit 3 is correct; optional one-line note in the loop receipt synopsis so operators distinguish "busy elsewhere" from other CANNOT-DETERMINE cases.

## Notes

- Scope: 2 commits / +95−2 over `master`. Files: `Invoke-Lane.ps1` (MaxTurns 40→65), `Invoke-Workstream.ps1` (safe stale-branch reuse), `test_coordination_guardrails.py` (reuse / refuse / new-branch).
- Stale-branch logic: existing ref with no commits beyond `baseSha` → `git branch -f` then `worktree add`; commits beyond base → exit 6 `REFUSED existing-branch-has-work` (branch untouched); probe failures → exit 3. Correctly avoids the local-master pitfall that reused a stale tip and the `90b5c0e6` gitlink.
- PR body correctly archives (does not land) astra ValidateSet-without-$LANES, quota failover comparing process exit to HTTP codes, and Adobe-ingester cross-project loop→factory commits; preserved at `refs/preserve/master-direct-20260911`.
- Author claims 371 coordination/line-ending tests passed locally + falsifier against fork/master's old worktree-add path. Await Repo Hygiene coordination step on CI for remote confirmation.
- No mode-160000 gitlink in this tip (explicitly excluded). Live effect: next `Invoke-WorkstreamLoop` cycle on `fork/master` picks up MaxTurns 65 and the reuse path.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.