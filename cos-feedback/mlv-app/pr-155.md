schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: 9582cbde8d0836229b67ee6049dac666aa2b636e
reviewed_utc: 2026-09-23T20:35:26Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 9582cbde (+2847/−57, 27 files, 12 commits vs master): Protected Check Route SUCCESS; Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python IN_PROGRESS; Windows GUI Pilot + Windows Product Oracles QUEUED (Actions runs 35916878781 / 35916878868 / 35916873882); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 9582cbde, especially Batch Compile for MainWindow / PlaybackPresentedFrameIdentityTracker / PlaybackLookaheadLoopPositionPolicy / PlaybackFramePopulationPolicy wiring, Repo Hygiene Python for profiling script touch, and Factory Bridge for smoke helper regression coverage.)

## Improvements

- Soft: **0 GitHub reviews**. Round 6 inverts round 5's identity key (loop-epoch pair + request-side identity sets + presented union). A short human glance at `PlaybackLookaheadLoopPositionPolicy::wrap`, the telemetry-gated tracker mutations, and the rewritten console tests (3-lap healthy loop, cross-origin overlap, duplicate-request, dual-origin never-presented) would still help.
- Soft: `playback_smoke.timing_validity` still *reads* `playback_prep_region_clock_resolution_ns` / `playback_prep_region_clock_monotonic` from stage timing telemetry (unchanged across rounds 2–6). Confirm the existing MainWindow probe always writes those keys on every presented frame; if a path skips the probe, consumers see 0/false. One-line smoke assert later is enough if already always present.

## Notes

- Round 6 vs prior CoS head `c5795c63` (+945/−356 across 10 files, 1 commit): sol+astra BLOCKER on round 5's inverted identity key — not feature sprawl.
  - Round 5 keyed identity by `displayFrame` alone; healthy 3-lap loops of a 24-frame span read ~66.7% source-frame loss because `offeredSourceFrames` counts every lap while the numerator could not. Tip keys `(loop epoch, displayFrame)`; `PresentationRequestContext::playbackSmokeLoopEpoch` captured at request-issue from `m_playbackSmokeLoopWrapCount`; lookaheads past cut-out get epoch via new unit-tested `PlaybackLookaheadLoopPositionPolicy::wrap`.
  - Tracker now records REQUESTED occurrences too; bucket membership is QSet difference on a shared identity basis (closes duplicate request-event → false skip). `presentedFrames` is the union (not sum) of per-origin presented sets.
  - Round 4's `m_playbackSmokeReusedLookaheadTargetFrames` back-out counter removed: reuse structurally never reaches `noteRequestedFrame`. Tracker mutations gated on `m_playbackSmokeFrameTelemetry` (not merely `m_playbackSmokeActive`) so ordinary interactive playback never touches the QSets.
  - Last unpropagated non-authoritative label path closed: `run-release-cuda-playback-ab.ps1` `New-PlaybackCompare` now carries `timelineFpsAuthoritative` (AND of both legs; fail-closed).
  - Console tests rewritten to drive the real tracker call sequence; new `test_playback_lookahead_loop_position_policy.cpp` covers single/double-lap wrap, boundary, non-zero cut-in, and degenerate loopSpan.
- Round 2–5 content still in tip: route-truthful `achieved_scale`, sync quality/phase3, four-state texture-present, target-vs-lookahead population, NaN/+inf deadline reject, source-frame offered accumulator + four-bucket partition, log-line `session=` anchor, authoritative timeline_fps label sweep, identity-based presentation.
- Scope unchanged: instrument fix demanded by astra arbiter before any CUDA optimisation lane. Blocks optimisation work until merge. Pairs with #154 (host-load gate).
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior CoS soft Improvements (0 reviews; clock-probe keys) not addressed this push; verdict unchanged → chat silent per later-push rule.
