schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: 3df5eaf4602ac866d25391aaec7fbc4f5950b5ea
reviewed_utc: 2026-09-25T09:28:36Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Tip 3df5eaf (+6097/−107, 41 files, 29 commits vs master; +1 commit / +199/−3 across 6 files vs prior CoS 8ed449d): Analyze (actions) SUCCESS; Analyze (c-cpp)/(python) IN_PROGRESS; CodeQL neutral/skipping; required Repo Hygiene / Batch Compile / Windows GUI Pilot / Windows Product Oracles not yet reported on tip (Actions run 36118511649). mergeable false; mergeable_state dirty (diverged: tip ahead 29 / behind 49 vs master; protection strict=true). 0 GitHub reviews; not draft.

## Blockers

- **Conflicts with master (dirty).** Tip diverged from master (`c5cda379`): behind by 49 commits including #154 PLAYBACK-MEASURE-HOST-LOAD-GATE-1 and ATTR3-FOOTAGE-STAGE-SUBMIT-RETRY-1 work that touches overlapping profiling / repo_hygiene surfaces. Branch protection is `strict: true`, so required checks alone cannot clear merge until the head is rebased or merged onto current master and conflicts are resolved. Concrete unblock: rebase `product/CUDA-ATTRIBUTION-BASELINE-1` onto `master` (or merge master in), re-run tip CI, then re-evaluate.
- Tip required-status checks not yet green on `3df5eaf` (Repo Hygiene Python windows/ubuntu, Batch Compile, Windows GUI Pilot, Windows Product Oracles). Wait for green after the rebase; prior tip `8ed449d` had been on a mergeable/BLOCKED path, not dirty.

## Improvements

- Soft: **0 GitHub reviews** (unchanged across rounds 2–13). Round 13 is a real sol r12 BLOCKER fix in population accounting; a short human glance at the new `startOccurrenceOffered` fencepost (drop-frame credit vs normal-mode no-credit) and the three console cases in `tests/console/test_playback_frame_population_policy.cpp` would still help before merge.
- Soft: after rebase, confirm Repo Hygiene Python **windows** still executes the expanded PS1 CI wrapper family to completion (findall + `EXPECTED_SUITE_NAMES` from round 12) and that the new 10th-arg wiring census pins in `tools/repo_hygiene/test_playback_identity_mainwindow_wiring.py` stay green on both ubuntu and windows.

## Notes

- Round 13 vs prior CoS head `8ed449d` (+199/−3 across 6 files, 1 commit `3df5eaf4`): hub fix for sol r12 BLOCKER — drop-frame start-occurrence fencepost.
  - `offered_source_frames` is displacement (session delta of `m_playbackTimelineSourceFramesOffered`); identity unions count every occurrence touched, including the start position. Drop-frame's first tick advances < 1 frame so start frame P is requested/presented while `round(E)` is one short; normal mode advances +1.0 before first `drawFrame()` and never requests P.
  - Fix: `computeSourceFramePopulation()` gains trailing `startOccurrenceOffered` (0/1) applied after +0.5 rounding when telemetry measured. `MainWindow::drawFrame()` sets `m_playbackSmokeStartOccurrenceOffered` on the first offered occurrence (`!hasOfferedCeiling()`), before `noteOfferedFrame()`, iff session delta < 1.0. Reset in `beginPlaybackSmokeTelemetry()`; passed through from `finishPlaybackSmokeTelemetry()`. Not a blanket +1 (would fabricate never-requested frames in normal mode).
  - Files: `platform/qt/MainWindow.cpp` / `.h`, `PlaybackFramePopulationPolicy.h`, `PlaybackPresentedFrameIdentityTracker.h` (`hasOfferedCeiling()`), `tests/console/test_playback_frame_population_policy.cpp` (drop-frame credited → sound; uncredited negative control → unsound; normal-mode no credit → sound), `tools/repo_hygiene/test_playback_identity_mainwindow_wiring.py` (10th-arg live member + reset + guarded set pins, mutation-proven).
- Bachelor repro cited at prior tip `8ed449d1` (M16-1243, CUDA): drop-OFF offered==requested; drop-ON under-reported never_requested by one when skips absorbed the deficit. Validator unchanged; accounting fixed upstream.
- Round 2–12 content still in tip (route-truthful scale, population gate, UNMEASURED third state, structural census, PS1 SUMMARY findall, etc.). Scope unchanged: instrument fix demanded by astra arbiter before any CUDA optimisation lane. Pairs with #154 (host-load gate) — #154 is among the master commits this branch is behind, so rebase will also integrate that pairing.
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **changed** from prior `merge-when-ci-green` → `not-merge-ready` solely because tip is now dirty vs master (product fix itself looks sound pending post-rebase CI). Chat report per later-push rule.
