schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: c5795c63f47da84f722d1870d0c71bed91817ef3
reviewed_utc: 2026-09-23T18:25:13Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip c5795c63 (+2256/−55, 25 files, 11 commits vs master): Protected Check Route SUCCESS; CodeQL neutral; Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, Analyze actions/c-cpp/python IN_PROGRESS (Actions runs 35902159611 / 35902159586 / 35902155155); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on c5795c63, especially Batch Compile for MainWindow / PlaybackPresentedFrameIdentityTracker / PlaybackFramePopulationPolicy wiring, Repo Hygiene Python for the new `playback-smoke-log-parsing.ps1` + authoritative-label tests, and Factory Bridge for smoke helper regression coverage.)

## Improvements

- Soft: **0 GitHub reviews**. Rounds 4–5 are the densest attribution-correctness push yet (log-line selection, lookahead-reuse accounting, identity-based presentation). A short human glance at `Select-PlaybackSmokeRecordLine` (`session=` anchor), the reuse back-out in `computeSourceFramePopulation`, and `PlaybackPresentedFrameIdentityTracker` remaining cheap.
- Soft: `playback_smoke.timing_validity` still *reads* `playback_prep_region_clock_resolution_ns` / `playback_prep_region_clock_monotonic` from stage timing telemetry (unchanged across rounds 2–5). Confirm the existing MainWindow probe always writes those keys on every presented frame; if a path skips the probe, consumers see 0/false. One-line smoke assert later is enough if already always present.

## Notes

- Round 4–5 vs prior CoS head `1529cf27` (+675/−30 across 14 files, 4 commits): three astra/sol correctness fixes, not feature sprawl.
  - Round 4 log-anchor: bare `*playback_smoke.frame_population*` wildcard matched prose inside `source_frame_population`'s `population_basis` and `Select-Object -Last 1` could drop the real record (reviving SMOKE-VALIDATOR-LOOP-WRAP-FALSE-FAIL-1). New `tools/profiling/playback-smoke-log-parsing.ps1` anchors on ` session=`; `test-playback-smoke-log-parsing.ps1` reproduces the two-record collision.
  - Round 4 reuse: `m_nextTargetRenderRequestSerial` advances before lookahead-reuse early return, so reuse inflated `requestedThenSkippedTargetFrames` / broke `partitionSound`. Tip tracks `m_playbackSmokeReusedLookaheadTargetFrames`, backs it out before skip derivation, folds reuseAccountingSound into `partitionSound`; six console tests cover astra's offered=60 / offered=20 shapes plus reuse>requests fail-closed.
  - Round 4 label sweep: non-authoritative timeline_fps label now propagates through compare-ab, cuda-playback-ab, ultramagnus-p3, and local-cuda-dng-smoke (fail-closed when source label absent); `test-playback-smoke-ab-authoritative-label.ps1` guards dropouts.
  - Round 5 identity (sol blocker): presentation-EVENT counts certified 0% loss on 60 events / 20 distinct displayFrames. New `PlaybackPresentedFrameIdentityTracker` (QSet per origin) feeds distinct counts into `computeSourceFramePopulation`; field semantics of `presented_via_*_frames` change to distinct source frames (consumers enumerated; cadence still on `m_playbackSmokePresentedFrames`). Standing-check test encodes sol's 60/60/20 false-pass.
- Round 2–3 content still in tip: route-truthful `achieved_scale`, sync quality/phase3, four-state texture-present, target-vs-lookahead population, NaN/+inf deadline reject, source-frame offered accumulator + four-bucket partition.
- Scope unchanged: instrument fix demanded by astra arbiter before any CUDA optimisation lane. Blocks optimisation work until merge. Pairs with #154 (host-load gate).
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect.
- Author notes console_tests 288/3805 with 1 pre-existing unrelated AvxParity failure; platform/qt release build clean. CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior CoS soft Improvements (0 reviews; clock-probe keys) not addressed this push; verdict unchanged → chat silent per later-push rule.
