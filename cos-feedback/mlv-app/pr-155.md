schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: 8e1ed00abaaca2f0adb4fa273d91633a6ca2ff29
reviewed_utc: 2026-09-25T00:35:01Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 8e1ed00 (+5154/−107, 40 files, 26 commits vs master): all tip checks IN_PROGRESS at review (Protected Check Route, Repo Hygiene Python windows/ubuntu, Batch Compile, Factory Bridge Regressions, Analyze actions/python/c-cpp — Actions runs 36078124121 / 36078124085 / 36078122239); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft. Prior CoS tip 15557aca (round 10).

## Blockers

- (none in round-11 product/telemetry content — wait for required tip checks on `8e1ed00`, especially Repo Hygiene Python **windows** for `test_profiling_ps1_suites_are_ci_enforced.py` + the new wiring modules, Repo Hygiene Python ubuntu for the pure-Python analyzer/wiring tests, Batch Compile if any C++ touched this round — round 11 is tools-only so Batch Compile should be a no-op vs product sources.)
- Prior tip Factory Bridge FAILURE on `15557aca` (trampoline `run.call_args` None) is superseded by a fresh tip run now IN_PROGRESS; treat as cleared only after green on `8e1ed00`. Factory Bridge is still **not** in master required_status_checks.

## Improvements

- Soft: **0 GitHub reviews** (unchanged across rounds 2–11). Round 11 is the largest census/CI-enforcement closure yet; a short human glance at `test_playback_manifest_timing_writers_wiring.py`'s clock-probe + scale/quality capture pins and at `analyze-frame-telemetry.py`'s absent-field exclude path would still help.
- Soft residual on the addressed PS1-CI Improvement: `test_profiling_ps1_suites_are_ci_enforced.py` **skips** (never passes) off Windows / without `pwsh`. Confirm Repo Hygiene Python **windows-latest** actually executes it to completion (not just collects) on tip `8e1ed00`, and that all seven `tools/profiling/test-*.ps1` suites stay at failed=0. Ubuntu hygiene correctly skips — do not treat that skip as green coverage of the PS1 family.

## Notes

- Round 11 vs prior CoS head `15557aca` (+789/−31 across 6 files, 4 commits): sol/fable follow-ups — CI enforcement + census gap closure + consumer honesty; not feature sprawl.
  - 18df4557: add `tools/repo_hygiene/test_profiling_ps1_suites_are_ci_enforced.py` — globs `tools/profiling/test-*.ps1`, runs each via `pwsh`, fails on nonzero exit or `[SUMMARY] failed=N` with N>0. **Addresses prior CoS Improvement** that asked to confirm CI executes `test-playback-smoke-validation-block-wiring.ps1` (and the rest of the family). Windows-only skip is explicit, not silent pass.
  - 3bffe1af: add `tools/repo_hygiene/test_playback_manifest_timing_writers_wiring.py` — pins clock-probe resolution sample write + stageTimingTelemetry / timing_validity re-emit; requestContext scale/quality capture and render_manifest fields; recon/amaze three-state writers on both presentation paths; CPU-debayer skip marker; achieved-scale production call; phase3_mode reset/replace. Also pins `refresh_period_histogram.py` CLI `target_fps=args.target_fps` via new `main(argv)` tests. **Addresses prior CoS soft Improvement** on confirming the MainWindow clock probe always writes the keys `timing_validity` re-reads (now census-pinned at write + both emission sites).
  - e6d41095: close fable suffix-insertion gap on offered-accumulator feed — nothing but whitespace between `offeredAdvance(...)`'s closing paren and the terminating `;`.
  - 8e1ed00a: fix `analyze-frame-telemetry.py` silent zero-fill of absent FIELDS (`row.get(f, 0.0)` → present-only stats + `n`/`unavail`); join companion `playback_smoke.timing_validity` so derived threading-overhead fields print `(derived)`; guard CLI under `if __name__ == "__main__"`. New `tools/profiling/test_analyze_frame_telemetry.py` mutation-verified.
- Round 2–10 content still in tip (route-truthful scale, population gate, UNMEASURED third state, fail-closed missing-record helper, validation-block wiring pin, etc.).
- Scope unchanged: instrument fix demanded by astra arbiter before any CUDA optimisation lane. Blocks optimisation work until merge. Pairs with #154 (host-load gate).
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior CoS Improvements (PS1 CI enforcement; clock-probe write/emit pins) **clearly addressed** this push → chat report per later-push rule; verdict unchanged (`merge-when-ci-green`).
