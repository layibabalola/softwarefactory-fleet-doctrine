schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: f685125b139f4c321e0257a924e5537c49257468
reviewed_utc: 2026-09-24T21:18:54Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip f685125b (+3801/−75, 35 files, 17 commits vs master): Protected Check Route SUCCESS; Analyze (actions) SUCCESS; Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, Analyze c-cpp/python IN_PROGRESS or pending (Actions runs 36060508987 / 36060509003 / 36060503926); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft. Prior CoS tip 9582cbde Tests failed Repo Hygiene Python (ubuntu-latest) — round 8b targets that ATTRCUDA_UNCLASSIFIED_SCRIPT_REFERENCE gap.

## Blockers

(none in the reviewed tip — wait for required checks on f685125b, especially Repo Hygiene Python ubuntu/windows for the new MainWindow wiring census + seven-file smoke-runner closure pin, Batch Compile for PlaybackDropFrameAdvancePolicy / identity-tracker ceiling wiring, and Factory Bridge for smoke-helper regression coverage.)

## Improvements

- Soft: **0 GitHub reviews** (unchanged across rounds 2–8b). Rounds 7–8 add EOF overshoot caps, offered-ceiling filtering, UNMEASURED attribution state, and a source-level MainWindow wiring census. A short human glance at `PlaybackDropFrameAdvancePolicy::offeredAdvance`, `noteOfferedFrame` ceiling membership, and `test_playback_identity_mainwindow_wiring.py`'s six live-argument sites would still help.
- Soft: `playback_smoke.timing_validity` still *reads* `playback_prep_region_clock_resolution_ns` / `playback_prep_region_clock_monotonic` from stage timing telemetry (unchanged across rounds 2–8b). Confirm the existing MainWindow probe always writes those keys on every presented frame; if a path skips the probe, consumers see 0/false. One-line smoke assert later is enough if already always present.

## Notes

- Round 7–8b vs prior CoS head `9582cbde` (+954/−35 across 15 files, 5 commits): astra majors on round 6 tip + CI closure fix — not feature sprawl.
  - Round 7 (ee7eb6b1 / 92d795fe / 0a4dc5cc): cap non-looping EOF overshoot out of `m_playbackTimelineSourceFramesOffered` via new `PlaybackDropFrameAdvancePolicy`; exclude lookahead identities beyond the offered ceiling in `PlaybackPresentedFrameIdentityTracker`; emit UNMEASURED third state (`source_frame_loss_ratio=unmeasured`, `partitionSound=false`) when `MLVAPP_PLAYBACK_SMOKE_TELEMETRY` is off instead of a confident 100% loss.
  - Round 8 (e65b583e): `tools/repo_hygiene/test_playback_identity_mainwindow_wiring.py` — source-level census of six MainWindow call sites (same pattern as `test_playback_gate_wiring.py`); closes astra's RULED gap that console tests alone cannot catch a deleted production wiring call.
  - Round 8b (f685125b): stage `playback-smoke-log-parsing.ps1` into `AttrCudaSmokeRunnerClosureManifest` (six→seven pinned files) + fixture/behaviour test updates. Same fix shape as ATTR3-VISUAL-QUALITY-EVIDENCE-1 r2 and CUDA-S4-TEXTURE-ROUTE-CLAMP-1 r2. Addresses hosted `ATTRCUDA_UNCLASSIFIED_SCRIPT_REFERENCE` from round 4's unstaged dot-source (prior tip 9582cbde Repo Hygiene ubuntu failure).
- Round 2–6 content still in tip: route-truthful `achieved_scale`, sync quality/phase3, four-state texture-present, target-vs-lookahead population, NaN/+inf deadline reject, source-frame offered accumulator + four-bucket partition, log-line `session=` anchor, authoritative timeline_fps label sweep, `(loop epoch, displayFrame)` identity key.
- Scope unchanged: instrument fix demanded by astra arbiter before any CUDA optimisation lane. Blocks optimisation work until merge. Pairs with #154 (host-load gate).
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior CoS soft Improvements (0 reviews; clock-probe keys) not addressed this push; verdict unchanged → chat silent per later-push rule.
