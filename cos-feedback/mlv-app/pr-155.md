schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: 1529cf276cb9f5a568d08ff9572bd287832bf462
reviewed_utc: 2026-09-23T16:33:08Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 1529cf27 (+1581/−25, 17 files, 7 commits vs master): Protected Check Route SUCCESS; Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, Analyze actions/c-cpp/python IN_PROGRESS (Actions runs 35889461372 / 35889461370 / 35889455655); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 1529cf27, especially Batch Compile for `PlaybackFramePopulationPolicy` / MainWindow accumulator wiring and Repo Hygiene Python / Factory Bridge for `run-release-gui-smoke.ps1` source-frame-loss gate.)

## Improvements

- Soft: **0 GitHub reviews**. Round 3 adds the source-frame loss instrument (+519 vs prior CoS head f33d25c5 across MainWindow, PlaybackFramePopulationPolicy, console tests, smoke gate); a short human glance at `computeSourceFramePopulation` partitionSound / never-requested arithmetic and the drop-frame wrap double-count guard remains cheap.
- Soft: `playback_smoke.timing_validity` still *reads* `playback_prep_region_clock_resolution_ns` / `playback_prep_region_clock_monotonic` from stage timing telemetry (unchanged across rounds 2–3). Confirm the existing MainWindow probe always writes those keys on every presented frame; if a path skips the probe, consumers see 0/false. One-line smoke assert later is enough if already always present.

## Notes

- Round 3 vs prior CoS head `f33d25c5` (astra round-2 major finding 4 NOT_RESOLVED): loss gate previously only saw frames that became target requests, so drop-frame catch-up that never issued a request was invisible (astra repro: 60 offered / 20 presented → 0% by target-serial, 66.7% true). Tip adds `m_playbackTimelineSourceFramesOffered` (monotonic wrap-immune accumulator fed from raw pre-wrap deltas in `playbackHandling`), `PlaybackFramePopulationPolicy::computeSourceFramePopulation` (four named buckets + `partitionSound` fail-closed), presented-via-target vs lookahead split, and smoke preference for `source_frame_loss_ratio`. Legacy timeline-position surfaces finished as non-authoritative (smokeTimelineFps, log.summary/raw.summary, fieldLog.timeline_fps). Console tests include the astra repro asserting never-requested=40 and lossRatio>0.5.
- Round 2 content still in tip (unchanged this push): route-truthful `achieved_scale`, executed `phase3_mode` + sync `quality_mode`, four-state texture-present basis, target-vs-lookahead request population, NaN/+inf deadline reject, three policy headers + console tests.
- Scope unchanged: instrument fix demanded by astra arbiter before any CUDA optimisation lane. Blocks optimisation work until merge. Pairs with #154 (host-load gate).
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior CoS soft Improvements (0 reviews; clock-probe keys) not addressed this push; verdict unchanged → chat silent per later-push rule.