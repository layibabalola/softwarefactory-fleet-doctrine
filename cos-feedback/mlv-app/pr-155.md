schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: 2bc8cc0aa5a17d0efd1fcc6ad8db3f4ff423fd36
reviewed_utc: 2026-09-23T11:08:02Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 2bc8cc0a (+401/−10, 8 files, 1 commit vs master): all tip checks IN_PROGRESS — Protected Check Route, Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python (Actions runs 35852662517 / 35852662452 / 35852660860); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 2bc8cc0a, especially Batch Compile for the C++/header struct changes and Repo Hygiene Python for `test_refresh_period_histogram.py` deadline tests.)

## Improvements

- Soft: `playback_smoke.render_manifest` captures requested/effective/achieved scale from `requestContext` / `readyFrame` (per-request), but `quality_mode` / `phase3_mode` still read live `m_playbackQualityMode` at present time. If quality can change mid-smoke, those two fields can disagree with the scale triple for the same session+index. Prefer stashing quality/phase3 on the request context the same way the clamp-pre scale is stashed, then emit from that.
- Soft: `playback_smoke.timing_validity` *reads* `playback_prep_region_clock_resolution_ns` / `playback_prep_region_clock_monotonic` from stage timing telemetry. Confirm those keys are already written by the existing MainWindow probe on every presented frame in this tip (PR body claims the wire-up); if a frame path skips the probe, consumers will see 0 / false and may mis-attribute clock quality. If already always present, no change needed — just worth a one-line assert in a smoke validator later.
- Soft: **0 GitHub reviews**. Diff is instrumentation + smoke-gate hygiene; a short human glance at the `llrawproc` struct field adds (ABI/layout for any out-of-tree consumers of `GpuPlaybackReconAmazePostWbTiming`) and the new `frame_population` gate preference in `run-release-gui-smoke.ps1` remains cheap.

## Notes

- Scope: instrument fix demanded by astra arbiter (`fleet-runs/arbiter-cuda-astra-20260923T0900Z`) before any CUDA optimisation lane. Makes a playback leg attributable: scale triple + clamp flag, wrap-immune frame population, three-state timing validity, deadlines-not-buckets in `refresh_period_histogram.py`.
- Files: `GpuDisplayWindow.cpp`, `MainWindow.cpp`/`.h`, `RenderFrameThread.h`, `llrawproc.h`, `run-release-gui-smoke.ps1`, `refresh_period_histogram.py`, `test_refresh_period_histogram.py`.
- Gate behaviour: smoke prefers serial-based skip ratio when `playback_smoke.frame_population` is present; `first==last` presented-frame stuck fail is suppressed when `loop_wrap_count > 0` (missing wrap count still fails closed). Legacy timeline skip figures kept for older consumers.
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect; no stage-median summing in `tools/profiling`.
- Blocks CUDA optimisation work until merge. Post-merge lever ranking per PR: full-res pixel work first, then queue pacing; readback removal low.
- Pairs with #154 (host-load gate for fps comparisons) as another attribution precondition.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).