schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: 15557aca603e8e96513dd39f5b3e7802597c7f80
reviewed_utc: 2026-09-24T23:53:51Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 15557aca (+4365/−76, 36 files, 22 commits vs master): Protected Check Route SUCCESS; Analyze (actions)/(python) SUCCESS; CodeQL neutral; Factory Bridge Regressions FAILURE (run 36074762829 — agent-bridge trampoline mock, not in PR diff); remaining tip checks IN_PROGRESS — Repo Hygiene Python windows/ubuntu, Batch Compile, Windows GUI Pilot, Windows Product Oracles, Analyze (c-cpp) (Actions runs 36074762817 / 36074762829 / 36074758074); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft. Prior CoS tip d1581eec.

## Blockers

- **Factory Bridge Regressions** red on tip `15557aca` (job 107883380158 / run 36074762829): `tools.agent-bridge.test_server_wrapper_phase2.ServerWrapperPhase2Tests.test_trampoline_host_env_queries_prefer_pwsh_for_cim` errored with `AttributeError: 'NoneType' object has no attribute 'args'` at `tools/agent-bridge/test_server_wrapper_phase2.py:853` (`run.call_args` was None). Round 10 delta does not touch `tools/agent-bridge/**`; prior tip `d1581eec` Factory Bridge run 36068783937 was SUCCESS. Unblock: re-run Factory Bridge on `15557aca`; if it reproduces, assert `run` was called before reading `.args` (or fix the trampoline host-env mock path). Note: Factory Bridge is **not** in master required_status_checks (those are Repo Hygiene Python windows/ubuntu, Windows GUI Pilot, Windows Product Oracles, Batch Compile) — still red on the tip and worth clearing before relying on the suite.
- (none in round-10 product/telemetry content — wait for required tip checks on `15557aca`, especially Repo Hygiene Python ubuntu/windows for the census/wiring strengthenings + new `test-playback-smoke-validation-block-wiring.ps1` surface if invoked, Batch Compile for MainWindow.cpp comment/prose edits, Windows GUI Pilot / Product Oracles.)

## Improvements

- Soft: **0 GitHub reviews** (unchanged across rounds 2–10). Round 10 pins the fail-closed helper's production call site and closes three census insertion-blindness gaps; a short human glance at `run-release-gui-smoke.ps1`'s `Get-SourceFrameAttributionValidationFailures` call site and the tightened accumulator feed would still help.
- Soft: `playback_smoke.timing_validity` still *reads* `playback_prep_region_clock_resolution_ns` / `playback_prep_region_clock_monotonic` from stage timing telemetry (unchanged across rounds 2–10). Confirm the existing MainWindow probe always writes those keys on every presented frame; if a path skips the probe, consumers see 0/false. One-line smoke assert later is enough if already always present.
- Soft: confirm CI actually executes the new `tools/profiling/test-playback-smoke-validation-block-wiring.ps1` (Factory Bridge's tools/profiling steps were **skipped** because the earlier agent-bridge launcher step failed). After a green Factory Bridge re-run, verify the profiling step lists this script; if it is only local-manual, wire it into the same runner that already runs `test-playback-smoke-log-parsing.ps1`.

## Notes

- Round 10 vs prior CoS head `d1581eec` (+318/−16 across 3 files, 3 commits): sol/fable follow-ups on round-9 mechanisms — not feature sprawl.
  - 1da7eeb5: add `tools/profiling/test-playback-smoke-validation-block-wiring.ps1` — extracts the live `if (-not $LaunchOnlyProbe) { ... }` validation block from `run-release-gui-smoke.ps1` and executes it for a FrameTelemetry-requested leg with a missing `source_frame_population` record; proves three call-site mutations (hardcoded `-FrameTelemetryRequested $false`, deleted `$validationFailures +=`, result discarded into `$ignored`) would restore round-9 fail-open while helper-only tests stay green.
  - 7dd244fb: close three census insertion-blindness gaps in `test_playback_identity_mainwindow_wiring.py` — require only whitespace between `m_playbackTimelineSourceFramesOffered +=` and `offeredAdvance(...)`; pin `computeSourceFramePopulation` args 0–1 to the live accumulator pair; add exact-RHS check for `dropFrameSourceFramesAdvanced = getFramerate() * (double)timeDiff / 1000.0`.
  - 15557aca: reword emitted `playback_smoke.frame_population` `population_basis` prose + two MainWindow.cpp comments so target-serial figures are wrap-immune but **not** authoritative; `source_frame_loss_ratio` is the gate (aligns with round-3 machine-facing `*Authoritative=false` contract). Prose/comment only; no consumer parses `population_basis` content.
- Round 2–9 content still in tip: route-truthful `achieved_scale`, sync quality/phase3, four-state texture-present, target-vs-lookahead population, NaN/+inf deadline reject, source-frame offered accumulator + four-bucket partition, log-line `session=` anchor, authoritative timeline_fps label sweep, `(loop epoch, displayFrame)` identity key, EOF overshoot cap, offered-ceiling filter, UNMEASURED third state, MainWindow value-source census, missing-record fail-closed helper, seven-file AttrCuda smoke-runner closure.
- Scope unchanged: instrument fix demanded by astra arbiter before any CUDA optimisation lane. Blocks optimisation work until merge. Pairs with #154 (host-load gate).
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior CoS soft Improvements (0 reviews; clock-probe keys) not addressed this push; verdict unchanged → chat silent per later-push rule (bus refresh only).
