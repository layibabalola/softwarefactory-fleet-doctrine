schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: f33d25c5f76028095e463fa4d175f74f55cd5083
reviewed_utc: 2026-09-23T14:52:45Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip f33d25c5 (+1069/−23, 17 files, 6 commits vs master): Protected Check Route SUCCESS; Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, Analyze actions/c-cpp/python still pending/queued (Actions runs 35877313844 / 35877313770 / 35877325640); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on f33d25c5, especially Batch Compile for the C++/policy-header refactor and Repo Hygiene Python for `test_refresh_period_histogram.py` NaN/+inf deadline guard.)

## Improvements

- Soft: **0 GitHub reviews**. Round 2 is a large attribution-instrumentation delta (+1069/−23); a short human glance at `PlaybackAchievedScalePolicy` / `GpuTexturePresentAvailabilityPolicy` / `PlaybackFramePopulationPolicy` plus the `run-release-gui-smoke.ps1` gate preference for target-serial skip ratio remains cheap.
- Soft: `playback_smoke.timing_validity` still *reads* `playback_prep_region_clock_resolution_ns` / `playback_prep_region_clock_monotonic` from stage timing telemetry (prior CoS note). Confirm the existing MainWindow probe always writes those keys on every presented frame; if a path skips the probe, consumers see 0/false. One-line smoke assert later is enough if already always present.

## Notes

- Round 2 vs prior CoS head `2bc8cc0a` (astra majors + prior CoS Improvement): (1) `achieved_scale` is route-truthful — Debayered16 defaults to 1 via `PlaybackAchievedScalePolicy` (no longer claims requested scale on a non-resizing route). (2) `phase3_mode` comes from `ReadyFrame::phase3Mode` (executed mode); live config kept as `phase3_mode_configured`. (3) `quality_mode` captured on `PresentationContext::playbackQualityMode` at drawFrame request-build (addresses prior CoS soft). (4) texture-present basis is four-state (`not_executed` / `unavailable` / `partial` / `measured`) via `GpuTexturePresentAvailabilityPolicy`; viewport writer now populates recon/amaze component flags. (5) frame population splits target-only vs all-requests+lookahead (`m_nextTargetRenderRequestSerial`); smoke gate prefers target-serial ratio. (6) deadline tolerance rejects NaN/+inf. (7) three pure policy headers + console unit tests (13 new assertions) matching `PlaybackGatePolicy` pattern.
- Scope unchanged: instrument fix demanded by astra arbiter before any CUDA optimisation lane. Blocks optimisation work until merge. Pairs with #154 (host-load gate).
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
