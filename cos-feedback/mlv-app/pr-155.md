schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 155
head: d1581eec70d4d8f17055053a1f9f2e2628346432
reviewed_utc: 2026-09-24T22:41:53Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip d1581eec (+4063/−76, 35 files, 19 commits vs master): tip checks IN_PROGRESS — Protected Check Route, Repo Hygiene Python windows/ubuntu, Batch Compile, Factory Bridge Regressions, Analyze actions/c-cpp/python (Actions runs 36068783991 / 36068783937 / 36068780976); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft. Prior CoS tip f685125b.

## Blockers

(none in the reviewed tip — wait for required checks on d1581eec, especially Repo Hygiene Python ubuntu/windows for the strengthened MainWindow value-source census + Get-SourceFrameAttributionValidationFailures coverage, Batch Compile for unchanged C++ surface, and Factory Bridge for smoke-helper regression coverage.)

## Improvements

- Soft: **0 GitHub reviews** (unchanged across rounds 2–9). Round 9 tightens the wiring census to exact value sources and closes the missing-record fail-open hole; a short human glance at `PlaybackDropFrameAdvancePolicy::offeredAdvance` args, epoch producer statements, and the missing-record validation path would still help.
- Soft: `playback_smoke.timing_validity` still *reads* `playback_prep_region_clock_resolution_ns` / `playback_prep_region_clock_monotonic` from stage timing telemetry (unchanged across rounds 2–9). Confirm the existing MainWindow probe always writes those keys on every presented frame; if a path skips the probe, consumers see 0/false. One-line smoke assert later is enough if already always present.

## Notes

- Round 9 vs prior CoS head `f685125b` (+270/−9 across 4 files, 2 commits): astra/sol majors on round 8 tip — not feature sprawl.
  - e4146ead: pin value sources feeding loop epoch and EOF-policy args in `test_playback_identity_mainwindow_wiring.py` (exact statements for wrap-counter→requestContext epoch, lookahead seed/accumulate/copy chain, two guarded wrap increments, wrap-count reset, and exact `offeredAdvance` four-arg list including `ui->actionLoop->isChecked()`). Closes sol+astra MAJOR that round-8 only proved call presence, not producer correctness (constant `true` for loop arg previously passed).
  - d1581eec: extract `Get-SourceFrameAttributionValidationFailures` into `playback-smoke-log-parsing.ps1`; missing `source_frame_population` record now fails closed when `-FrameTelemetry` was requested (third state beside present-and-unsound / present-and-unmeasured). Six-state matrix covered in `test-playback-smoke-log-parsing.ps1` (tests 9→16). Addresses astra round-8 MAJOR fail-open fallback to weaker request-based ratio.
- Round 2–8b content still in tip: route-truthful `achieved_scale`, sync quality/phase3, four-state texture-present, target-vs-lookahead population, NaN/+inf deadline reject, source-frame offered accumulator + four-bucket partition, log-line `session=` anchor, authoritative timeline_fps label sweep, `(loop epoch, displayFrame)` identity key, EOF overshoot cap, offered-ceiling filter, UNMEASURED third state, MainWindow call-presence census, seven-file AttrCuda smoke-runner closure.
- Scope unchanged: instrument fix demanded by astra arbiter before any CUDA optimisation lane. Blocks optimisation work until merge. Pairs with #154 (host-load gate).
- Explicitly out of scope (correct): matched non-overlapping span reconciliation — methodological, not a source defect.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Prior CoS soft Improvements (0 reviews; clock-probe keys) not addressed this push; verdict unchanged → chat silent per later-push rule.
