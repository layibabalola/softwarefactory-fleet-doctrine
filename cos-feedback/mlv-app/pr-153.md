schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: 4daa99437752891cccbbefefa0bec1819fb5ec87
reviewed_utc: 2026-09-23T08:19:38Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 4daa9943 (ATTR3-FOOTAGE-STAGE-SUBMIT-RETRY-1: carry caller's -TimeoutSec to agent via inbox\<id>.meta.json before job visible; client poll gains proportional grace max 180s / floor 5s; +132/−10, 3 files, 1 commit): tip checks all IN_PROGRESS (Batch Compile, Repo Hygiene Python windows/ubuntu, Protected Check Route, Factory Bridge Regressions, Analyze actions/c-cpp/python; Actions runs 35836412038 / 35836411908 / 35836409703); mergeable true; mergeable_state BLOCKED (pending required checks); 0 GitHub reviews; base master; ahead_by 1 behind_by 0; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 4daa9943, especially Repo Hygiene Python windows/ubuntu which run `tools/repo_hygiene/test_um_run_sidefiles.py` after +6 budget/order/range/concurrency/e2e cases; mergeable_state BLOCKED until those settle.)

## Improvements

- Soft: commit message body says grace is "min 180 s"; the code and PR description correctly use `[math]::Min(180, …)` as a **max** of 180 s (floor 5 s). Fix the commit message wording on a follow-up or squash so operators do not misread the bound. Not a merge blocker.
- Soft: `um-run.ps1` now always forwards `-TimeoutSec` as `-JobTimeoutSec`, so every existing caller (including short playback-smoke probes) writes agent metadata and waits `TimeoutSec + grace`. That is the intended fix for ATTR3, and e2e asserts the 1 s + 5 s grace message — just confirm no caller relied on a client-only short poll while the agent kept a longer 1800 s default. Not a merge blocker given the PR's stated scope and measured 30-min kills.
- Soft: **0 GitHub reviews** on a host-agent budget / drop-ordering change; a short human glance at meta-before-job rename order and the concurrent `UMRUN_JOBID_IN_USE` path is still cheap. Not blocking.
- Soft residual: `Invoke-UmRunDrop -JobTimeoutSec 0` (default) still leaves the agent on its 1800 s default; only the `um-run.ps1` path auto-wires the caller's timeout. Direct module callers that need a non-default agent budget must pass `-JobTimeoutSec` themselves. Documented by the 0-means-no-metadata contract; not a merge blocker.

## Notes

- Closes a measured ATTR3 footage-stage failure: multi-GB placement given an hour was killed at 30 min (`exitCode 124`, "killed process tree") twice on 2026-09-22 because nothing wrote `inbox\<id>.meta.json` and the agent fell back to 1800 s while the client alone honoured `-TimeoutSec`.
- Design matches the existing side-file ordering contract: metadata via nonce temp + rename (no `-Force`) **before** the job temp is written; observing-copier test proves `meta-present-at-job-copy=True`; out-of-range and concurrent-meta paths place neither job nor overwrite.
- Client grace is proportional (`TimeoutSec * 0.1`, clamped 5..180) so equal deadlines do not race the agent's receipt. Timeout error text now names both deadlines.
- Producer-claimed local: 23 pass in `test_um_run_sidefiles.py`. CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Hub-authored; Claude CLI login expired mid-session so lanes were not dispatched.