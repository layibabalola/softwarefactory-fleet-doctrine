schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: 8c8a8f6ba598e06a1faa7785f3961a9dbdfc3cca
reviewed_utc: 2026-09-25T00:32:51Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 8c8a8f6 (round 11: +2 commits / +922/−410 across 7 files vs prior CoS a9039bbf; PR vs master +2354/−98, 10 files, 20 commits): Protected Check Route SUCCESS; pending — Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, Analyze actions/c-cpp/python (Actions runs 36077933090 / 36077933120 / 36077930083); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 8c8a8f6.)

## Improvements

- Soft residual (unchanged): commit 4daa9943 body still says grace is "min 180 s"; code no longer uses that formula (round 10+ heartbeat/UNRESOLVED path). Fix on squash/follow-up. Not a merge blocker.
- Soft (narrowed): um-run's default `-MaxQueueWaitSec 86400` and new default `-MaxClaimedWaitSec 86400` still apply to callers that omit them; attr3 sites now pin both to the job budget. Worth documenting for non-attr3 callers. Not a merge blocker.
- Soft (unchanged): **0 GitHub reviews** still. Round 11 is a production-path redesign (never-fail-while-agent-may-own + no age reclaim); a short human glance remains cheap before merge.

## Notes

- Round 11 vs prior CoS head a9039bbf (2 commits b015aca1 + 8c8a8f6): closes sol/fable round-10 blockers/majors per hub ruling that a client cutoff must never declare failure while the agent may still own the job.
- `UmRunDrop.psm1`: age-based claim reclamation (`-OrphanMetaGraceSec`) removed entirely — existing metadata at any age refused with "choose a new -JobId"; post-claim rollback deletes metadata only when nonce still matches this call; `Get-UmRunHeartbeatJobTag` / `Get-UmRunAgentLiveness` moved here with torn-read double-confirm before job-mismatch; emits `UMRUN_NONCE=` for caller cleanup.
- `um-run.ps1`: queue-ceiling never-claimed path atomically renames job out of inbox then throws `RETRACTED:` (or falls through if race); claimed-phase past budget requires started-marker absence plus liveness loss before stop, and every non-receipt outcome is `UNRESOLVED:` (never generic failure); deadlines are `[DateTimeOffset]` throughout; absolute outer ceiling still fires.
- `attr3-footage-stage.ps1`: classifies `RETRACTED`/`UNRESOLVED` by message prefix; UNRESOLVED → `RESULT=FOOTAGE_STAGE_UNRESOLVED` exit 2 (not retryable as ordinary submit failure); both um-run call sites pass caller-derived `-MaxClaimedWaitSec` (mirroring `-MaxQueueWaitSec`). Docs replace OrphanMetaGraceSec guidance with choose-new-JobId / manual meta clear.
- Prior CoS Improvement addressed: round-10 orphan-reclaim residual (live-but-slow submitter displaceable past grace) closed by removing age reclaim; nonce-checked rollback closes the displaced-owner destroy-new-claim residual. Lane/tests not re-run here; tip CI still settling.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
