schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: 3adfbb8cea96103b224ce5bf4f5207388e0b7eed
reviewed_utc: 2026-09-23T20:35:23Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 3adfbb8c (round 7: +1 commit / +187/−23 across 3 files vs prior CoS d2c0d662; PR vs master +1154/−44, 9 files, 7 commits): all tip checks IN_PROGRESS — Protected Check Route, Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python (Actions runs 35916886598 / 35916886765 / 35916880494); Windows GUI Pilot / Product Oracles not yet listed (likely gated on Batch Compile); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 3adfbb8c.)

## Improvements

- Soft residual (unchanged): commit 4daa9943 body still says grace is "min 180 s"; code uses `[math]::Min(180, …)` as a **max** of 180 s (floor 5 s) in `tools/profiling/um-run.ps1`. Round 7 did not amend that wording. Fix on squash/follow-up. Not a merge blocker.
- Soft (narrowed, unchanged): um-run's default `-MaxQueueWaitSec 86400` still applies to callers that omit it; attr3 sites already pin it. Round 6's claim marker + round 7's client-clock claim deadline make equal TimeoutSec/MaxQueueWaitSec safer, but the long default is still worth documenting for non-attr3 callers. Not a merge blocker.
- Soft (unchanged): **0 GitHub reviews** still. Round 7 is substantive (client-clock claimedAt + queue-deadline claim recheck + two new mechanism-locking tests); a short human glance remains cheap.

## Notes

- Round 7 vs prior CoS head d2c0d662: (1) claimed-phase deadline no longer anchors to the agent-authored `startedUtc` in `running\<id>.started.json` — `$claimedAt` is the client's own `Get-Date` when it first observes the marker (errs toward more patience; closes cross-host clock skew that could expire the deadline before the agent receipt lands). (2) At the queue deadline, the loop now rechecks the claim marker (and fires optional `-TestHookAtQueueDeadline` for deterministic tests) before diagnosing "never claimed", then switches onto the job's execution deadline if a late claim appeared. (3) New regression coverage that fails on revert for round-6 rollback-deletion folded error (`-TestHookBeforeJobVisible` exclusive lock) and share-clock probe (`-TestHookAfterShareClockProbe`), plus clock-skew claim and claim-at-queue-deadline tests in `tools/repo_hygiene/test_um_run_sidefiles.py`. Files: `UmRunDrop.psm1` +8/−1, `um-run.ps1` +42/−21, `test_um_run_sidefiles.py` +137/−1.
- Prior soft Improvements closed earlier (started.json claim-marker parity in round 6). Still open: min-vs-max 180 s wording, MaxQueueWaitSec docs, 0 reviews. Round 7 did not address those soft items; it closed sol blockers on clock domain and late-claim diagnosis.
- Commit message notes `test_a_full_budget_job_is_not_misdiagnosed_as_queued_against_the_tracked_agent` can flake under load on the author machine (reproduced on unmodified round-6 baseline) — pre-existing agent-side kill/report timing margin, unrelated to this round's client-side clock fix. Watch tip CI for that flake.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped this push: verdict unchanged and prior CoS Improvements not addressed.