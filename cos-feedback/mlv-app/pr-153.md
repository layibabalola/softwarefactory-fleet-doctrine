schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: d2c0d66254a9a6e9d68bb10d1823fcedb1c6fa0d
reviewed_utc: 2026-09-23T18:07:10Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip d2c0d662 (round 6: +1 commit / +173/−28 across 8 files vs prior CoS 978e942a; PR vs master +989/−43, 9 files, 6 commits): Protected Check Route SUCCESS; Batch Compile, Windows GUI Pilot, Windows Product Oracles, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python all IN_PROGRESS (Actions runs 35900119969 / 35900119802 / 35900114493); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on d2c0d662.)

## Improvements

- Soft residual (unchanged): commit 4daa9943 body still says grace is "min 180 s"; code uses `[math]::Min(180, …)` as a **max** of 180 s (floor 5 s) in `tools/profiling/um-run.ps1`. Round 6 did not amend that wording. Fix on squash/follow-up. Not a merge blocker.
- Soft (narrowed): um-run's default `-MaxQueueWaitSec 86400` still applies to callers that omit it; attr3 sites already pin it. Round 6's claim marker makes `TimeoutSec == MaxQueueWaitSec` safe with the tracked agent, so the long default is less risky than before but still worth documenting for non-attr3 callers. Not a merge blocker.
- Soft (unchanged): **0 GitHub reviews** still. Round 6 is substantive (claim marker + pre-transfer TimeoutSec ceiling + share-clock orphan age); a short human glance remains cheap.

## Notes

- Round 6 vs prior CoS head 978e942a addresses the prior soft Improvement that tracked `ultra-magnus-agent.ps1` did not write `running\<id>.started.json`: it now writes and retires the claim marker (proved with TimeoutSec==MaxQueueWaitSec full-budget E2E). Also: CLI rejects TimeoutSec > 86400 before any transfer; orphan meta age uses a share-clock probe; metadata rollback delete failure is folded into the throw; copy-before-metadata test observes live meta state; honour-path E2E gets the same 5 s archival settle as round 5; I/O inventory line numbers re-pinned.
- Prior soft Improvements closed by this push: started.json claim-marker parity. Still open: min-vs-max 180 s wording, 0 reviews.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).