schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: f7c287963d9813660ffec34759e0597b3b2e2625
reviewed_utc: 2026-09-24T19:26:26Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip f7c28796 (round 9: +5 commits / +246/−1 across 3 files vs prior CoS 5e9c48e7; PR vs master +1517/−44, 9 files, 13 commits): Protected Check Route SUCCESS; IN_PROGRESS — Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, Analyze actions/c-cpp/python (Actions runs 36048044959 / 36048044960 / 36048039777); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on f7c28796.)

## Improvements

- Soft residual (unchanged): commit 4daa9943 body still says grace is "min 180 s"; code uses `[math]::Min(180, …)` as a **max** of 180 s (floor 20 s after round 8) in `tools/profiling/um-run.ps1`. Round 8–9 comments correctly say "180 s cap" but did not amend the initial commit message. Fix on squash/follow-up. Not a merge blocker.
- Soft (narrowed, unchanged): um-run's default `-MaxQueueWaitSec 86400` still applies to callers that omit it; attr3 sites already pin it. Round 6–9 claim/deadline work makes equal TimeoutSec/MaxQueueWaitSec safer, but the long default is still worth documenting for non-attr3 callers. Not a merge blocker.
- Soft (unchanged): **0 GitHub reviews** still. Round 9 is test-strength + client honesty (no production path redesign); a short human glance remains cheap before merge.

## Notes

- Round 9 vs prior CoS head 5e9c48e7 (5 commits, mostly tests): (1) same-JobId hub ruling premise pinned in `test_attr3_footage_stage.py` — generator is the only tracked non-test caller that builds a path to `um-run.ps1`, plus executable proof that `New-Attr3FootageStageJob` / `New-Attr3FootagePresenceJob` mint distinct GUID-shaped attempt nonces on identical inputs (hardening residual stays open as a card, not closed here). (2) rollback-fold test now asserts the distinct original job-rename-race remainder (`inbox\demo.job.ps1 appeared concurrently…`), not only static fold suffixes. (3) orphan-age test with nonzero grace + far-future share probe proves aging uses `$shareNowUtc` at the subtraction, not merely that the probe is read. (4) second `claimedAt = Get-Date` at the queue-deadline recheck covered against a skewed marker (`-TestHookAtQueueDeadline` + `-MaxQueueWaitSec 0`). (5) claimed-branch timeout throw wording: "this client is giving up, but the agent still owns $jobId and its receipt may still land…" (`um-run.ps1` +5/−1). Files: `test_attr3_footage_stage.py` +129, `test_um_run_sidefiles.py` +112, `um-run.ps1` +5/−1.
- Prior soft Improvements still open: min-vs-max wording in 4daa9943, MaxQueueWaitSec docs, 0 reviews. Round 9 did not address those; it strengthened tests around already-merged round 6–8 behaviour and clarified client deadline honesty.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped this push: verdict unchanged and prior CoS Improvements not addressed.
