schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: baa9e20ca642ef3b40587d7b2336fa7a71c57a35
reviewed_utc: 2026-09-23T09:38:18Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip baa9e20c (round 2: +1 commit vs prior CoS 4daa9943; PR vs master +567/−24, 7 files, 2 commits): Protected Check Route SUCCESS; Analyze (actions) SUCCESS; CodeQL NEUTRAL; Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze c-cpp/python, Windows Product Oracles, Windows GUI Pilot IN_PROGRESS/QUEUED (Actions runs 35844023028 / 35844022952 / 35844019452); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on baa9e20c, especially Repo Hygiene Python windows/ubuntu which exercise the new rollback / claim-queue / agent-metadata suites, and Windows Product Oracles / GUI Pilot once queued behind Protected Check Route.)

## Improvements

- Soft cleared-in-part: public `um-run.ps1` now refuses `-TimeoutSec 0` (`UMRUN_TIMEOUT_SEC_INVALID`) before submit, closing the prior CoS note where a zero budget under-armed client patience while the agent kept its own default. Module-level `-JobTimeoutSec 0` remains the intentional "write no metadata" sentinel for direct callers/tests.
- Soft residual: commit 4daa9943 body still says grace is "min 180 s"; code uses `[math]::Min(180, …)` as a **max** of 180 s (floor 5 s). Round 2 did not amend that wording. Fix on squash/follow-up. Not a merge blocker.
- Soft: tracked `ultra-magnus-agent.ps1` still does not write `running\<id>.started.json`. With default `-MaxQueueWaitSec 86400`, a host running only the tracked agent will sit in the QUEUED phase for up to a day before the "never claimed" throw. Callers on the tracked double should pass a tighter queue ceiling (tests use 2–30 s). Documented in the agent header; not a merge blocker for bachelor's deployed agent.
- Soft: **0 GitHub reviews** still. Round 2 is the right shape for fable/sol majors (meta rollback, agent honour, claim-vs-queue), but a short human glance at the nested `try`/`catch` around job rename + `$metaPlacedHere` rollback and the claim-marker timezone parse path is still cheap.

## Notes

- Round 2 vs prior CoS head 4daa9943: addresses fable/sol round-1 majors/minors — (1) failed job placement rolls back metadata this call placed so deterministic JobIds can retry; (2) tracked agent reads `inbox\<id>.meta.json` timeoutSec 1..86400 with fallback, uses it for kill/result, moves meta to `processed\`; (3) client waits for claim marker up to `-MaxQueueWaitSec` then switches to claim-time + budget + grace, with distinct queued vs claimed timeout messages and a final receipt re-check; (4) budget validated before side-file loop; (5) meta write verified before rename; (6) ATTR3 presence preflight capped at 300 s independent of placement budget.
- Deployed vs tracked agent divergence is now explicit in the tracked agent header (no queue-age expiry, no claim marker) — correct Law-1 documentation, not a silent equivalence claim.
- New coverage: `test_um_run_sidefiles.py` rollback / pre-copy reject / zero-reject / never-claimed / late-claim / late-receipt; `test_ultra_magnus_agent_metadata.py` (Windows+pwsh) missing/unparseable meta fallback; `test-ultra-magnus-agent-timeout.ps1` asserts metadata budget 20 s over agent default 4 s and meta moved to processed.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped: verdict unchanged from prior CoS; prior soft Improvements not fully cleared.
