schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: 5e9c48e7dbe0f82f41b1837d4653ba27d77e24ce
reviewed_utc: 2026-09-24T18:36:38Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 5e9c48e7 (round 8: +1 commit / +156/−38 across 3 files vs prior CoS 3adfbb8c; PR vs master +1272/−44, 9 files, 8 commits): Protected Check Route SUCCESS; IN_PROGRESS — Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, Analyze actions/c-cpp/python (Actions runs 36042272723 / 36042272649 / 36042266096); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 5e9c48e7.)

## Improvements

- Soft residual (unchanged): commit 4daa9943 body still says grace is "min 180 s"; code uses `[math]::Min(180, …)` as a **max** of 180 s (floor now 20 s after round 8) in `tools/profiling/um-run.ps1`. Round 8 comments correctly say "180 s cap" but did not amend the initial commit message. Fix on squash/follow-up. Not a merge blocker.
- Soft (narrowed, unchanged): um-run's default `-MaxQueueWaitSec 86400` still applies to callers that omit it; attr3 sites already pin it. Round 6–8 claim/deadline work makes equal TimeoutSec/MaxQueueWaitSec safer, but the long default is still worth documenting for non-attr3 callers. Not a merge blocker.
- Soft (unchanged): **0 GitHub reviews** still. Round 8 is substantive (cross-JobId metadata governance recheck + grace floor derived from agent overhead + narrower-revert test locks); a short human glance remains cheap.

## Notes

- Round 8 vs prior CoS head 3adfbb8c: (1) sol BLOCKER — before writing inbox metadata, re-check that `$final` (the claimable job) is still absent, so a racing same-JobId submitter with no budget cannot become visible during a long job copy and then inherit this call's `timeoutSec` ("metadata never governs a job it wasn't written for"). New test `test_metadata_is_never_attempted_once_a_racing_job_is_already_visible` asserts metadata is never attempted, not merely rolled back. (2) sol BLOCKER / fable minor — client grace floor raised 5 s → 20 s, derived from bounded agent overhead (setup before countdown, hard 5 s poll lag, process-tree stop, receipt publish) documented in `um-run.ps1`; formula is now `[math]::Min(180, [math]::Max(20, [int]($TimeoutSec * 0.1)))`. (3) New `-TestHookAfterShareProbeWritten` stamps the share clock probe with a far-future LastWriteTimeUtc so the share-clock test fails under a Get-Date substitute. (4) Queue-deadline late-claim test rewritten to require a post-`continue` poll that actually reads a late receipt (narrower-revert lock). Files: `UmRunDrop.psm1` +28/−1, `um-run.ps1` +25/−2, `test_um_run_sidefiles.py` +103/−35. Author verify: 39 pass in `test_um_run_sidefiles.py`; unrelated brokered-closeout flake called out in commit body.
- Prior soft Improvements still open: min-vs-max wording in 4daa9943, MaxQueueWaitSec docs, 0 reviews. Round 8 closed sol blockers on metadata/job governance and grace floor, not those soft items.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped this push: verdict unchanged and prior CoS Improvements not addressed.