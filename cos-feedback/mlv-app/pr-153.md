schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: a9039bbf59d4f498828a7165f9aae5f6807c894e
reviewed_utc: 2026-09-24T22:56:19Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip a9039bbf (round 10: +5 commits / +597/−326 across 6 files vs prior CoS f7c28796; PR vs master +1832/−88, 10 files, 18 commits): Protected Check Route SUCCESS; IN_PROGRESS — Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, Analyze actions/c-cpp/python (Actions runs 36070025655 / 36070025644 / 36070020364); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft. Prior tip f7c28796 Tests+Factory Bridge SUCCESS.

## Blockers

(none in the reviewed tip — wait for required checks on a9039bbf.)

## Improvements

- Soft residual (unchanged): commit 4daa9943 body still says grace is "min 180 s"; code uses `[math]::Min(180, …)` as a *max* of 180 s (floor 20 s after round 8) in `tools/profiling/um-run.ps1`. Round 10 replaced the fixed grace-floor wait with heartbeat liveness but did not amend the initial commit message. Fix on squash/follow-up. Not a merge blocker.
- Soft (narrowed): um-run's default `-MaxQueueWaitSec 86400` still applies to callers that omit it for the queued phase; attr3 sites already pin it. Round 10's claimed-phase redesign (heartbeat liveness + `-MaxClaimedWaitSec`) does not remove the long queued-phase default for non-attr3 callers. Worth documenting. Not a merge blocker.
- Soft (unchanged): **0 GitHub reviews** still. Round 10 is a production-path redesign (claim-first ownership + client liveness wait); a short human glance remains cheap before merge.
- Soft (new, documented residual): astra round-8 orphan-reclaim risk remains open by design — a live-but-slow submitter whose placement exceeds `-OrphanMetaGraceSec` can still be reclaimed. Module header + docs now treat the param as the caller's worst-case claim-to-exposure promise (default 60s; ATTR3-FIXTURE-REHEARSAL-1 docs show raising it for large side-files). Not solved with a submitter heartbeat. Not a merge blocker for automated attr3 callers (no `-SideFile`).

## Notes

- Round 10 vs prior CoS head f7c28796 (5 commits): (1) `UmRunDrop.psm1` claim-first — every submission atomically claims `inbox\<id>.meta.json` (with `nonce`) before any side-file/job byte; no-budget claims omit `timeoutSec`; reverses round-4 job-bytes-before-metadata; exports `Get-UmRunShareNowUtc`. (2) `um-run.ps1` replaces fixed budget+grace cutoff with claim-time + heartbeat liveness (`Get-UmRunAgentLiveness`: share-clock freshness + optional `job=<id>` tag match; outcomes receipt / liveness-lost / outer `-MaxClaimedWaitSec` ceiling); forwards `-OrphanMetaGraceSec`. (3) Docs + tests: `docs/playback-attr-3-cuda.md` documents `-OrphanMetaGraceSec` for manual fixture workflow; round-9 same-JobId ruling docstring corrected as superseded; agent missing-metadata test rewritten race-free; sidefiles tests cover claim-before-copy, no-budget claim omitting timeoutSec, racing dual claims, job-mismatch liveness, OrphanMetaGraceSec CLI passthrough, heartbeat torn-read note. Lane-reported: test_um_run_sidefiles 44 passed; attr3/agent/inventory unittest 79 OK.
- Round 9 same-JobId "unreachable from production" ruling reversed (sol): manual operator-chosen JobIds in docs + presence-preflight budgets falsified both premises; claim-first closes the race by construction instead of pinning a false premise.
- Prior soft Improvements still open: 4daa9943 min-vs-max wording, MaxQueueWaitSec docs, 0 reviews. Round 10 did not target those; it redesigns ownership + claimed-phase wait.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat skipped this push: verdict unchanged and prior CoS Improvements not clearly addressed (new residual noted on bus).
