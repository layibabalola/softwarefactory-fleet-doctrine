schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: 346e4c9a0bf2ba8348042a3b11c8aeddfd4b8c75
reviewed_utc: 2026-09-23T14:02:02Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 346e4c9a (round 4: +1 commit / +303/−58 across UmRunDrop.psm1, attr3-footage-stage.ps1, inventory, test_attr3_footage_stage.py, test_um_run_sidefiles.py vs prior CoS d658f947; PR vs master +828/−40, 9 files, 4 commits): tip checks all IN_PROGRESS — Protected Check Route, Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Analyze actions/c-cpp/python (Actions runs 35871134814 / 35871134749 / 35871129204); mergeable true; mergeable_state BLOCKED; 0 GitHub reviews; base master; not draft.

## Blockers

(none in the reviewed tip — wait for required checks on 346e4c9a, especially Repo Hygiene Python windows/ubuntu which assert MaxQueueWaitSec call-site pins, orphan self-heal, and the dead-agent bounded queue e2e.)

## Improvements

- Soft residual (unchanged): commit 4daa9943 body still says grace is "min 180 s"; code uses `[math]::Min(180, …)` as a **max** of 180 s (floor 5 s). Round 4 did not amend that wording. Fix on squash/follow-up. Not a merge blocker.
- Soft (unchanged): tracked `ultra-magnus-agent.ps1` still does not write `running\<id>.started.json`. Callers that omit `-MaxQueueWaitSec` still inherit um-run's 86400 s queue default. Round 4 fixed the attr3 call sites; other um-run callers remain on the long default. Documented in the agent header; not a merge blocker for bachelor's deployed agent.
- Soft (unchanged): **0 GitHub reviews** still. Round 4 shape is right for fable/sol majors; a short human glance at orphan self-heal (`-OrphanMetaGraceSec`), the job-bytes-before-metadata reorder, and `Get-Attr3FootageUmRunFailureClass` substring matches remains cheap.

## Notes

- Round 4 vs prior CoS head d658f947 addresses the prior soft Improvement on day-long QUEUED hangs: both attr3 um-run call sites now pass `-MaxQueueWaitSec` derived from this run's own `-TimeoutSec` (presence + placement); submission/presence failures carry `CLASS=QUEUED|CLAIMED|UNKNOWN` without echoing share paths.
- Sol major: `Invoke-UmRunDrop` copies job bytes to the nonce temp first, then publishes metadata, then renames into view — orphan window is one rename. Stale orphan meta (no job, older than grace) is self-healed on retry; removal failure is surfaced. New tests cover reorder, meta-before-visible hook, racing rename rollback, torn write verify, orphan heal both sides of grace, cleanup-failure surfacing, structural MaxQueueWaitSec pins, and dead-agent e2e (`PRESENCE PREFLIGHT=INCONCLUSIVE CLASS=QUEUED` under 90 s).
- Inventory re-pin only (line moves after the MaxQueueWaitSec / CLASS insert).
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat reported: prior MaxQueueWaitSec Improvement clearly addressed; verdict unchanged.
