schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 153
head: ab033d68fe6d33c9d70eca852e6eb27e052dfdd7
reviewed_utc: 2026-09-25T05:15:17Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip ab033d68 (round 12 + hub terminal-normalization: +2 commits / +525/−31 across 6 files vs prior CoS 8c8a8f6; PR vs master +2848/−98, 10 files, 22 commits): Protected Check Route SUCCESS; pending — Batch Compile, Repo Hygiene Python windows/ubuntu, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, Analyze actions/c-cpp/python (Actions runs 36097710083 / 36097710094 / 36097706177); mergeable true; mergeable_state BEHIND; 0 GitHub reviews; base master; not draft. Prior tip 8c8a8f6 Tests/Factory Bridge/PR Analyze all SUCCESS.

## Blockers

(none in the reviewed tip — wait for required checks on ab033d68; branch is behind master so expect a rebase/update before merge.)

## Improvements

- Soft residual (unchanged): commit 4daa9943 body still says grace is "min 180 s"; code no longer uses that formula (round 10+ heartbeat/UNRESOLVED path). Fix on squash/follow-up. Not a merge blocker.
- Soft (unchanged): um-run's default `-MaxQueueWaitSec 86400` and `-MaxClaimedWaitSec 86400` still apply to callers that omit them; attr3 sites pin both to the job budget. Worth documenting for non-attr3 callers. Not a merge blocker.
- Soft (unchanged): **0 GitHub reviews** still. Round 12 is another production-path honesty pass (RETRACTED wording, residue retain, structural catch); a short human glance remains cheap before merge.
- Soft (new, optional): on RETRACTED meta cleanup, a failed nonce read still attempts `Remove-Item` (defaults `$ownsClaim = $true` then falls into the delete branch). Mirrors the module-rollback "narrow window" choice and is disclosed in the throw note when delete fails — not a blocker, but a comment tying the default-`true` to that intentional bias would help the next reader.

## Notes

- Round 12 vs prior CoS head 8c8a8f6 (2 commits 961c2fc6 + ab033d68): closes sol/fable round-11 CHANGES_REQUESTED (sol 3 blockers / 1 major / 1 minor; fable 1 major / 3 minors).
- `um-run.ps1`: RETRACTED message no longer overclaims "nothing was submitted" — discloses that a stale per-poll inbox enumeration can still write a late claim marker + honest launch-failure receipt after rename-out (job script content cannot execute). Structural try/catch around the wait loop remaps unexpected post-publish exceptions to UNRESOLVED after a guarded receipt recheck (exhaustive four-outcome contract). Retraction cleanup (job temp + nonce-gated meta) now surfaces removal failures instead of SilentlyContinue. New test hooks: `-TestHookAfterRetractionJobRenamed`, `-TestHookAtLoopTop`.
- `attr3-footage-stage.ps1`: UNRESOLVED path no longer calls `Remove-Attr3FootageStageAttemptResidue`; emits `RESIDUE=RETAINED` on `RESULT=FOOTAGE_STAGE_UNRESOLVED` (exit 2) so staged share-side parts survive while the agent may still read them.
- Hub follow-up ab033d68: phrase assertions strip ANSI escapes (and keep whitespace/`|` normalization) so colour-terminal `TERM=xterm-256color` wraps no longer flake e2e message tests; test-only; claimed 55 pass with and without colour.
- Tests added/expanded: stale-enumeration late launch-failure after retract; UNRESOLVED classifier + submit-catch harness proving exit 2 and residue retention; structural-catch UNRESOLVED remapping; retraction cleanup failure surfacing; DateTimeOffset host-timezone-independent mutation; ANSI-normalized phrase asserts.
- Soft CoS Improvements from prior rounds not addressed by this tip (wording / default docs / human review). Lane/tests not re-run here; tip CI still settling. Branch behind master.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).
