schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 129
head: 5e236a600675cd3eae5c13a927f57b9a2e47abc7
reviewed_utc: 2026-09-16T16:40:45Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: CodeQL Analyze (actions/python) + Protected Check Route PASS; required Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles still pending; Factory Bridge Regressions + CodeQL Analyze (c-cpp) also pending (Tests 35123166166 / Factory Bridge 35123166021 / CodeQL 35123162470)

## Blockers

(none)

## Improvements

- After required CI greens, attach or link the claimed Sol HIGH review evidence for the two golden keys on this tip (or re-request Sol on #129) so the review trail is visible on the split PR, not only on parent #72 / adjudication receipts.
- Optional: in `GpuDisplayViewport::eventFilter`, assert `watched == this` before the paint short-circuit in a debug build only — already guarded, but a `Q_ASSERT` would catch a future accidental filter install on a different object.

## Notes

- Tip `5e236a60`: two cherry-picks (`3167e765` paint routing from `d43ba415`; `5e236a60` continuous geometry + exact-pixel asserts from `8fcda7e1`) onto master; ahead 2 / behind 0. Net +261/−39 across 6 files. No `src/`, `tools/`, `RenderFrameThread`, `MainWindow`, or async-H2D scheduling commits — those remain on open #72 (`d4e21972`).
- Scope matches PR claim: visible-playback correctness only. Paint ownership via self `eventFilter` while `hasPendingFrame()` (calls `QOpenGLWidget::event`, not bare `paintGL`); continuous `viewportTransform().mapRect` fixes 4×4→5×5 inclusive-edge inflation. Goldens `gpu.viewport.rgb888.pattern_nearest` and `gpu.viewport.rgb16.pattern_nearest` → `817ca20e…`; Sep 8 hub adjudication recorded in `agents/release-and-regression.md`.
- Real-footage A/B gate from #72 does **not** apply here: this PR intentionally changes output (black→visible; geometry). Measurement owed is native GUI suite + Windows GUI Pilot / Product Oracles on this tip without the async commits.
- MERGEABLE tip → `master`; `mergeStateStatus` BLOCKED (checks). 0 GitHub reviews on tip. CoS via `gh` only; no GitHub PR comment. First bus publish for this PR (`cos-feedback/mlv-app/pr-129.md`). Continuity: parent #72 feedback remains `not-merge-ready` at `cos-feedback/mlv-app/pr-72.md` (real-footage + Sol + CI).
