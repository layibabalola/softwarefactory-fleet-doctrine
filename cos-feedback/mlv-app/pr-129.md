schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 129
head: 54ea092872ff389c65b09e776bedadc0bdab86ef
reviewed_utc: 2026-09-16T16:50:31Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Protected Check Route PASS; CodeQL Analyze (actions/python) PASS; required Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles still pending (Tests 35124166286); Factory Bridge Regressions FAIL on tip (run 35124166348) — not required; prior tip 5e236a60 PASS and master b051a3b4 PASS same suite; CodeQL Analyze (c-cpp) pending

## Blockers

(none)

## Improvements

- After required CI greens, attach or link the claimed Sol HIGH review evidence for the two golden keys on this tip (or re-request Sol on #129) so the review trail is visible on the split PR, not only on parent #72 / adjudication receipts.
- Optional: in `GpuDisplayViewport::eventFilter`, assert `watched == this` before the paint short-circuit in a debug build only — already guarded, but a `Q_ASSERT` would catch a future accidental filter install on a different object.
- Re-run Factory Bridge Regressions on `54ea0928` (or ignore if not gating): failed step `test_server_wrapper_process_entry_prefers_pwsh_for_cim` with `AttributeError: 'NoneType' object has no attribute 'args'` at `tools/agent-bridge/test_server_wrapper_phase2.py:872` (`run.call_args` was None). Unrelated to this PR's GPU paint/geometry commits; prior tip and master #127 bridge green — treat as flake unless it repeats.

## Notes

- Push was merge `master` (`b051a3b4`, #127 coordination/ratio-guard) into `product/PR72A-GPU-VISIBILITY`. Product tip content unchanged vs prior review: cherry-picks `3167e765` (paint routing from `d43ba415`) and `5e236a60` (continuous geometry + exact-pixel asserts from `8fcda7e1`). Net merge delta is coordination/tools only (`docs/product-ratio-guard.md`, `tools/coordination/*`, `tests/coordination/*`) — no `src/`, no async-H2D, no GPU viewport further edits.
- Scope still matches PR claim: visible-playback correctness only. Real-footage A/B gate from #72 does **not** apply. Measurement owed remains native GUI suite + Windows GUI Pilot / Product Oracles on this tip without the async commits.
- MERGEABLE tip → `master`; `mergeStateStatus` BLOCKED (checks). 0 GitHub reviews on tip. CoS via `gh` only; no GitHub PR comment. Bus rewrite for new head (`cos-feedback/mlv-app/pr-129.md`). Continuity: parent #72 feedback remains `not-merge-ready` at `cos-feedback/mlv-app/pr-72.md`.
