schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 129
head: 70b7bf7169c75bf083c27ed12f7c870124d41ead
reviewed_utc: 2026-09-16T17:46:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Protected Check Route + CodeQL Analyze (actions/python) PASS on tip; required Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles pending (Tests 35130062525). Non-required Factory Bridge Regressions + CodeQL Analyze (c-cpp) pending. Prior tip 54ea0928 had all five required checks + Factory Bridge PASS.

## Blockers

(none for content)

- **CI: required hosted checks** — wait for Batch Compile, Repo Hygiene Python (ubuntu-latest + windows-latest), Windows GUI Pilot, and Windows Product Oracles on tip `70b7bf71` (Tests run 35130062525). Prior tip already proved the product suite green without the async-H2D commits.

## Improvements

- After required CI greens, attach or link the claimed Sol HIGH review evidence for the two golden keys on this tip (or re-request Sol on #129) so the review trail is visible on the split PR, not only on parent #72 / adjudication receipts.
- Optional: in `GpuDisplayViewport::eventFilter`, assert `watched == this` before the paint short-circuit in a debug build only — already guarded, but a `Q_ASSERT` would catch a future accidental filter install on a different object.

## Notes

- Tip `70b7bf71` is update-branch merge of `master` (`797b61e6`, #130 KIND-PATH booking rule) onto prior tip `54ea0928`. Net vs prior tip is docs-only (`docs/product-ratio-guard.md` +25/−0). All six product paths blob-identical vs `54ea0928` (`GpuDisplayViewport.cpp/.h`, `test_gui_smoke.cpp`, `gui_hashes.json`, both agents/*.md).
- Product content unchanged: cherry-picks `3167e765` (paint routing from `d43ba415`) and `5e236a60` (continuous geometry + exact-pixel asserts from `8fcda7e1`). Goldens still `817ca20e…` for both `gpu.viewport.rgb888.pattern_nearest` and `gpu.viewport.rgb16.pattern_nearest`.
- Prior tip `54ea0928` closed green on every required check plus Factory Bridge — the owed measurement (native GUI / Windows GUI Pilot / Product Oracles without async commits) already passed once; this tip only needs the same suite to re-green after the docs merge.
- Scope still matches PR claim: visible-playback correctness only. Real-footage A/B gate from #72 does **not** apply. No `src/`, no async-H2D, no RenderFrameThread/MainWindow/GpuDisplayWindow.
- MERGEABLE tip → `master`; `mergeStateStatus` BLOCKED (checks; branch protection strict). 0 GitHub reviews on tip. Prior CoS Improvements not addressed on this push (expected for update-branch). Factory Bridge flake note from prior bus row retired — prior tip FB passed.
- CoS review via `gh` only; no GitHub PR comment. Bus rewrite for new head (`cos-feedback/mlv-app/pr-129.md`). Continuity: parent #72 feedback remains `not-merge-ready` at `cos-feedback/mlv-app/pr-72.md`.