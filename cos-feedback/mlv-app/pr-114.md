schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 114
head: ecdbc367b86169b6df705da41ce521156d2367f3
reviewed_utc: 2026-09-15T05:21:27Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: All required checks in progress at review (CodeQL Analyze actions/python/c-cpp, Factory Bridge Regressions, Protected Check Route, Repo Hygiene ubuntu+windows, Batch Compile). No content blockers.

## Blockers

(none)

## Improvements

- Vacuous Pass on all-zero counters: `framesExpected == 0` (and zeros elsewhere) returns `Pass`. If smoke sessions always have a positive expected frame count, add an early `framesExpected <= 0` fail (or assert) so a forgotten expected-count never green-lights an empty session.
- `tools/repo_hygiene/test_playback_gate_wiring.py` `_extract_balanced`: no end-of-string guard — truncated fixture text can `IndexError`. Optional bounds check would turn malformed input into a clearer assertion failure (low priority; fixtures and real MainWindow are controlled).
- Header comment already bans wall-clock fps as a gate input; when PLAY-COUNTERS-CPU-B wires the call site, keep fps diagnostics beside the verdict only (do not extend `PlaybackGateCounters`).

## Notes

- Scope: policy-only card A. Files: `platform/qt/PlaybackGatePolicy.h` (new), `MLVApp.pro` HEADERS +1, `tests/console/test_playback_gate_policy.cpp` (5 cases), `console_tests.pro` SOURCES/HEADERS, `tools/repo_hygiene/test_playback_gate_wiring.py` (checker + fixtures; real MainWindow assert skipped naming card B). +276/−2; 1 commit on `product/PLAY-COUNTERS-CPU-A` → `master`.
- Policy is pure and session-level: evaluate once on cumulative totals; order is presented-vs-expected, decode-vs-expected, parity-vs-presented. L-CHEAT case correctly proves constant decodeRequestCount cannot mask parity failure. Matches PR intent to avoid the archived WIP defects (per-frame eval, function-local static parity, unfalsifiable wiring test, queue-depth input).
- Wiring checker design is sound for this card: fixture tests call `check_evaluate_call_uses_live_arguments` (not a bare regex on the literal); real-file skip is explicit until B adds the call site.
- Local red/green claims in PR body are credible given the inverted-comparison and stubbed-checker narratives; hosted CI still settling — confirm console_tests + Repo Hygiene Python pick up the new files once green.
- Depends-on chain: B must not land before A; #113 docs split already documents DEPENDS_ON. No MainWindow / RenderFrameThread edits here (correct for A).
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents unavailable on plan.
