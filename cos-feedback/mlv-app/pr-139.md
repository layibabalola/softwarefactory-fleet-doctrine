schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 139
head: 460f7d34b04d82351a86b82b3874177413e0bff4
reviewed_utc: 2026-09-18T05:57:48Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: All listed checks pending/IN_PROGRESS on head 460f7d34 (Protected Check Route, Repo Hygiene windows+ubuntu, Factory Bridge Regressions, Batch Compile, CodeQL actions/c-cpp/python); mergeable MERGEABLE; mergeStateStatus BEHIND; 0 GitHub reviews (Sol r1 CHANGES_REQUESTED addressed in this push).

## Blockers

(none)

## Improvements

- Update the branch (mergeStateStatus BEHIND vs `master`) before merge so this head does not freeze behind a later master push; MLV-App has no native merge queue.
- Re-request / land Sol HIGH (cross-family) after r3: Sol r1 MAJOR+MINOR findings are addressed in code, but GitHub still shows 0 reviews — treat as not human-acked until Sol re-approves.
- Confirm `Repo Hygiene Python (windows-latest)` green on this head: stage-job suite and new race/junction tests remain `@unittest.skipUnless(os.name == "nt")`; ubuntu will not cover those arms.
- Follow-up (out of card scope, noted in commit): `playback-attr-3-cuda-stage-job.ps1` still uses shared `Publish-AttrCudaFileMove` and retains the same overwrite race Sol flagged for the fixture stager.

## Notes

- Push `460f7d34` (r3) vs prior CoS head `26d25d7c` (r2): addresses Sol PR #139 r1 CHANGES_REQUESTED. (1) MAJOR — fixture stager now calls new `Publish-AttrCudaFileMoveNonOverwriting` / `Assert-AttrCudaNonOverwritingFileSlot` (atomic `[IO.File]::Move(...,$false)`; on occupied dest re-hash → identical = `Complete-AlreadyStaged`, different = exit 21). Shared helper unchanged. (2) MAJOR — runbook 4b quotes path placeholders and splits `<stageJobId>` / `<attrJobId>` (avoids spaces breakage and `UMRUN_JOBID_IN_USE`). (3) MINOR — already-staged and normal paths honor inbox cleanup via `Complete-AlreadyStaged` / explicit bool; refusal fails closed exit 22 `STEP=inboxCleanup`. Tests: RED/GREEN race helpers, junction cleanup refusal, already-staged cleans inbox, runbook text pins.
- Scope still ATTR3-FIXTURE-STAGE-1 (unblocks #137 acceptance item 5): stage generator, committed-bytes assert, optional `-ClipPath` + required `-FixtureSha256` (exit 17), consentReceipt pin, TEMP-portable fixture-content slice from r2.
- Contract read via gh (no Cloud Agent). No GitHub PR review comment (bus only).