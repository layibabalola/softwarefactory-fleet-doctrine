schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 139
head: 12c77774d895bda3f9d536e0cba0dd19349cdeb6
reviewed_utc: 2026-09-18T05:03:40Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Protected Check Route SUCCESS; remaining checks IN_PROGRESS (Repo Hygiene windows+ubuntu, Factory Bridge Regressions, Batch Compile, Windows Product Oracles, Windows GUI Pilot, CodeQL actions/c-cpp/python); mergeable MERGEABLE; mergeStateStatus BEHIND; 0 reviews.

## Blockers

(none)

## Improvements

- Update the branch (mergeStateStatus BEHIND vs `master`) before merge so this head does not freeze behind a later master push; MLV-App has no native merge queue.
- Land the Sol HIGH (cross-family) review the PR body requests before treating the NA-4 / fixture-hash contract as human-acked (0 GitHub reviews at CoS pass).
- Confirm `Repo Hygiene Python (windows-latest)` green on this head: `test_attr3_stage_fixture_job.py` is `@unittest.skipUnless(os.name == "nt")` and exercises the emitted stage job for real; ubuntu will not cover that arm.

## Notes

- Scope: ATTR3-FIXTURE-STAGE-1 unblocks SUBJECT 2 / #137 acceptance item 5. Adds `attr3-stage-fixture-job.ps1` (inbox→cache guarded publish of one tracked fixture with baked sha256), `Assert-AttrCudaFixtureCommittedBytes` (refuse dirty working-tree bytes vs HEAD blob), and extends `playback-attr-3-cuda-job.ps1` so fixture ids may omit `-ClipPath` (derived under `$AgentRoot\cache`) but must pass `-FixtureSha256` (fail-closed exit 17 `FIXTURE_CONTENT_MISMATCH` before package deploy). Owner-clip path rules unchanged; `-FixtureSha256` refused for owner ids.
- Docs: `docs/playback-attr-3-cuda.md` section 4b rewritten to the real four-step sequence (stage gen → um-run with `-AgentShare \\bachelor\mlv-agent` → attribution without `-ClipPath` → um-run); removes the false claim that `um-run -SideFile` lands in the cache.
- Tests: new stage-job behavioural suite (outside fixture / stem mismatch / VerifyOnly / publish / tamper exit 4 / missing exit 3 / restage no-op / different-bytes exit 21); attribution generator pins for ClipPath/FixtureSha256 rules, end-to-end mismatch, committed-bytes assert, and fixture `consentReceipt = null` (ATTR3-CONSENT-RECEIPT-TEST-1). Local claim: 142 passed / 233 subtests; hub dry-run under board hook in `.claude-state/fleet-runs/attr3-fixture-rehearsal-20260918T0432Z/dryrun-12c77774/`.
- Contract read via gh (no Cloud Agent). No GitHub PR review comment (bus only).