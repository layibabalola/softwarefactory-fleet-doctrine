schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 118
head: 6a1a093ffdf58fc546d0e0fc833246eea2523c98
reviewed_utc: 2026-09-15T16:43:44Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Hosted checks in progress (CodeQL Analyze actions pass / python pass / c-cpp pending; Protected Check Route pass; Batch Compile, Repo Hygiene Python ubuntu/windows, Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles pending). No content blockers.

## Blockers

(none)

## Improvements

- `platform/qt/MainWindow.cpp` `finishPlaybackSmokeTelemetry`: log `PlaybackGateVerdict` as a stable string name (Pass / FramesPresentedBelowExpected / DecodeRequestsBelowExpected / ParityMismatch), not `static_cast<int>(gateVerdict)`, so smoke logs are readable without the enum ordinal map.
- Gate result is evaluated and logged only; if PLAY-COUNTERS-CPU-B is meant to fail the GUI smoke process on non-Pass (alongside the existing exact presented-frame target exit 11), wire the verdict into the smoke exit path in a follow-up — do not treat the log line alone as harness authority.
- Lane commit is explicitly WIP (max_turns, not reviewed/built). Let Batch Compile and Repo Hygiene Python (new `test_playback_gate_wiring.py` cases) go green before merge; do not rely on the exhausted-lane local claim.

## Notes

- Scope (+140/−0, 6 files): wire cumulative session counters into existing `PlaybackGatePolicy::evaluate` (already on master). Adds `m_playbackSmokeParityMatchCount` + decode-requests delta around `begin`/`finishPlaybackSmokeTelemetry`, increments parity on `parity.checked && parity.match` in `presentPlaybackPreparedFrame`, exposes `RenderFrameThread::decodeRequestsIssuedCount()` with `m_decodeRequestsIssuedCount.fetch_add` at `queueDecodeRequestLocked`, documents the gate in `docs/playback-improvement-plan-round2.md`, and extends `tools/repo_hygiene/test_playback_gate_wiring.py` (live-args, finish-not-note call site, no queue-depth symbol, decode counter increment-only).
- Policy contract matches docs: fps not gated; inactive parity probe fails closed (`parityMatchCount` 0 vs `framesPresented`). MERGEABLE tip `6a1a093f` on `product/PLAY-COUNTERS-CPU-B` → `master`.
- CoS review via `gh`. No GitHub PR comment posted. Cloud Agents not used (review via gh per standing factory guidance).