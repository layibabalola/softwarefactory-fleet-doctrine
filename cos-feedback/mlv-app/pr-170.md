schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 170
head: 62c04eaabcfce978044f2e5358542080c09ec9e5
reviewed_utc: 2026-09-25T21:43:10Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 62c04eaa (+28/−8, 3 files, 2 commits; ahead 2 / behind 0): Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS (Tests 36192953086, Factory Bridge 36192953027, CodeQL 36192950178). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- Keep #169 parked (currently OPEN draft after PARK+SPLIT): do not re-activate the duration-as-proof-3 umbrella until CI inventory for PROD-TELEMETRY-DURATION-AS-PROOF-4 is ready; this PR owns only the three SH playback-preview flake sites.
- After this tip is green and merged, confirm #156 stays CLOSED (already closed; supersedes claim in body) so the defect class is single-tracked through #170 then #4.

## Notes

- Split out of parked #169 (ruling PARK+SPLIT, 3 of 3 adjudicators). Hub-authored. Cherry-picked `-x` from #156/#169: `f25b1e4c` (fullres-completed flag, thread-local, reset on entry) + tip `62c04eaa` (three SH playback-preview tests prove ran via completed flags / FilterRunCount, not Milliseconds() > 0.0).
- Defect: hosted Windows omp_get_wtime tick is 1 ms; tiny fixtures can read 0.0 even when the path ran — the flake that reddened Product Oracles on #154 at `test_processing_filters.cpp:747/787`.
- Diff: `src/processing/raw_processing.{c,h}` add `g_processing_last_shadows_highlights_fullres_completed` + getter; reset in `processingResetLastTimingTelemetry` and `processingRefreshShadowsHighlightsBlurFromRgb16`; set in both fullres branches of `processing_compute_shadows_highlights_blur`. Tests: `SharpOddHeight…` → FullresCompleted; `AggressiveOddHeight…` → HalfresRbfCompleted + FilterRunCount; `AggressiveX8…` → Quarterres*Completed + FilterRunCount.
- Out of scope (carried by PROD-TELEMETRY-DURATION-AS-PROOF-4): production telemetry, render-entry resets / processed8 cache-hit replay, pre-dual-ISO fix, `test_dual_iso_pipeline.cpp` ~10114, `tests/console`.
- Hub verification claimed: `ProcessingFilters.*` 324/0; mutation suppressing both fullres_completed writes turns SharpOddHeight red at :708; neither file provenance-pinned.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: narrow, previously-adjudicated defect-class fix; tip required checks still settling. Product Oracles is the usual long pole for this flake class.
