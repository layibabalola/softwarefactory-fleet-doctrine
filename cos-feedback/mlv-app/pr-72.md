schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 72
head: d4e21972f9ab7a73f14c7f9bb976a2c445cde7d0
reviewed_utc: 2026-09-16T13:17:40Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: CodeQL Analyze (actions/c-cpp/python) + Protected Check Route PASS; Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles still pending/in progress on tip (Tests run 35100608315 / Factory Bridge 35100608195 / CodeQL 35100604603)

## Blockers

- **Process: independent known-good real-footage comparison** — PR body keeps this as a non-waived merge gate. Synthetic golden adjudication (two RGB hashes) and Bachelor compositor/parity receipts do not replace it. Land a passing known-good real-footage compare against an accepted baseline before merge.
- **Process: final Sol review** — 0 GitHub reviews on tip `d4e21972`. PR body requires Sol review; do not merge on CoS posture alone.
- **CI: required hosted checks** — wait for Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, and Windows Product Oracles on this tip. If any required check is red, fix and re-push.

## Improvements

- Optional assert in `MainWindow::presentPlaybackPreparedFrame`: after `buildGpuPlaybackReconStateForTexturePresent`, confirm `gpuReconState.frame_id == task.displayFrame` (and matches `readyFrame` / texture-state `frameId`) before the explicit override, so a future prep-path drift cannot silently diverge again.
- Port the dropped Windows `os.replace` retry + tests from the in-tree `tools/agent-bridge/**` revert to `layibabalola/agent-bridge` (SoT). Tip correctly restored those files to master per the 2026-09-09 suspension; the patch still belongs on the dedicated repo, not here.

## Notes

- Tip `d4e21972`: merge of `fork/master` into `pr72-narrow-local` (`617e0560`) then drop of in-tree agent-bridge storage retry/tests (`d4e21972`). Product GPU/async-H2D/viewport fix unchanged vs prior feature tip `0a53179a`. Ahead 19 / behind 0 vs `master`. Net vs master: +1158/−220 across 23 files (no remaining `tools/agent-bridge/**` delta).
- Prior ambient TLS frame-id bug is addressed on this head: `llrpGpuPlaybackReconState_t::frame_id` is stamped on the render worker (`preparedState.frame_id = entry.request.frameNumber`), copied through texture state, and re-asserted at present as `task.displayFrame`. Shared `llrpGpuPlaybackReconFrameToken()` keys both `preupload_frame` and `run_preuploaded` (no more raw vs `frame_id+1` mismatch). Dead ambient setters/getters removed; preupload status rides `timing_out->preupload`.
- Async H2D staging moved from early decode-bytes preupload in `RenderFrameThread` to the admitted prepare path in `llrawproc.c` (`gpu_playback_input` + `mlv_pipeline_capture_get_current_frame()`), matching the PR claim that the backend rejects token-matched but byte-different uploads.
- Viewport black-screen fix: `GpuDisplayViewport` event filter owns `QEvent::Paint` while a GPU frame is pending (calls `QOpenGLWidget::event`, not bare `paintGL`). Continuous `viewportTransform().mapRect` replaces inclusive integer bounds (4×4 no longer becomes 5×5). Separate GPU display window still refuses screenshot provenance until its own surface can be captured.
- Local evidence claimed in PR body (Windows bridge 476, GUI/offscreen suites, pipeline provenance, Bachelor compositor/parity) is owner-reported; CoS did not re-run GPU hosts. Historical executable rebuild is explicitly not an accepted baseline.
- MERGEABLE tip → `master`; `mergeStateStatus` BLOCKED (checks + process). CoS review via `gh` only; no GitHub PR comment. First bus publish for this PR (`cos-feedback/mlv-app/pr-72.md`).
