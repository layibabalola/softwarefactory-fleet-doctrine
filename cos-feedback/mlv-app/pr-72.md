schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 72
head: a22a5bc7c514acfb0bdc1e88497bb911b7e2c879
reviewed_utc: 2026-09-16T22:44:09Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Protected Check Route + CodeQL Analyze (actions) PASS; Analyze (c-cpp/python), Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles pending/in progress on tip (Tests run 35158990941 / Factory Bridge 35158990934 / CodeQL 35158986630); one CodeQL matrix entry skipping

## Blockers

- **Process: independent known-good real-footage comparison** — PR body keeps this as a non-waived merge gate. Synthetic golden adjudication (two RGB hashes) and Bachelor compositor/parity receipts do not replace it. Land a passing known-good real-footage compare against an accepted baseline before merge.
- **Process: final Sol review** — 0 GitHub reviews on tip `a22a5bc7`. PR body requires Sol review; do not merge on CoS posture alone.
- **CI: required hosted checks** — wait for Factory Bridge Regressions, Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles, and remaining CodeQL Analyze jobs on this tip. If any required check is red, fix and re-push.

## Improvements

- Optional assert in `MainWindow::presentPlaybackPreparedFrame`: after `buildGpuPlaybackReconStateForTexturePresent`, confirm `gpuReconState.frame_id == task.displayFrame` (and matches `readyFrame` / texture-state `frameId`) before the explicit override, so a future prep-path drift cannot silently diverge again. Still absent on this tip (override remains unconditional).
- Port the dropped Windows `os.replace` retry + tests from the in-tree `tools/agent-bridge/**` revert to `layibabalola/agent-bridge` (SoT). Tip correctly keeps those files at master per the 2026-09-09 suspension; the patch still belongs on the dedicated repo, not here.

## Notes

- Tip `a22a5bc7`: clean merge of `master` `80df58c0` (PR #132 upstream sync; also carries #128/#129 72a split/#130/#131) into prior #72b tip `d4e21972`. Product delta vs master unchanged in scope: 18 files, +897/−181, ahead 20 / behind 0. No new product commits on this push — sync only.
- #129 (PR72A GPU visibility) already on master; remaining #72b still owns async-H2D explicit `frame_id`, prepare-path staging, viewport paint ownership / continuous geometry, screenshot provenance binding, output-budget receipt selection, and related tests/docs listed in the PR file set.
- Prior ambient TLS frame-id bug remains addressed: `llrpGpuPlaybackReconState_t::frame_id` stamped on the render worker, copied through texture state, re-asserted at present as `task.displayFrame`. Shared `llrpGpuPlaybackReconFrameToken()` keys both `preupload_frame` and `run_preuploaded`. Dead ambient setters/getters removed; preupload status rides `timing_out->preupload`.
- Local evidence claimed in PR body (Windows bridge 476, GUI/offscreen suites, pipeline provenance, Bachelor compositor/parity) is owner-reported; CoS did not re-run GPU hosts. Historical executable rebuild is explicitly not an accepted baseline.
- MERGEABLE tip → `master`; `mergeStateStatus` BLOCKED (checks + process). CoS review via `gh` only; no GitHub PR comment. Bus rewrite for new head only (`cos-feedback/mlv-app/pr-72.md`).
