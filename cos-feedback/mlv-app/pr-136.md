schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 136
head: 46bd0747dea43c0bbec44cf624eacb86ad868a84
reviewed_utc: 2026-09-17T06:21:44Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: All observed checks still in_progress at review (Repo Hygiene windows+ubuntu, Protected Check Route, Factory Bridge Regressions, Batch Compile, Analyze actions/c-cpp/python); mergeable MERGEABLE; 0 reviews; +24/-0 in .github/workflows/tests.yml only.

## Blockers

(none)

## Improvements

- After this PR's `Repo Hygiene Python (windows-latest)` finishes, compare duration to recent master runs and paste the before/after minutes in the PR body (the PR already asks for that measurement — close the loop before merging so the $61/mo claim is receipted, not assumed).
- Optional pin in `test_repo_hygiene.py`: assert the Windows-only Defender exclusion step exists with a fail-soft `catch`/`::notice::` path so a later edit cannot silently drop the cost control while keeping the required check name.

## Notes

- Scope: one step added before Set up Python in the Repo Hygiene Python job. `if: runner.os == 'Windows'`; `Add-MpPreference -ExclusionPath $env:RUNNER_TEMP, $env:GITHUB_WORKSPACE` wrapped in try/catch that notices and continues. No test, matrix, or required-check name changes.
- Rejected alternatives in the PR body are sound: deleting the Windows hygiene step would skip `@unittest.skipUnless(os.name == "nt")` coverage (including ACL/NULL-DACL trust checks); gating the Windows leg to `push: master` would leave a required check silent on PRs.
- `pipeline_tests --check-golden` deliberately untouched — router allowlist widening is doctrine, not a defect fix.
- Fail-soft design is correct for a required check: a cost optimisation that can redden CI has negative EV.
- CoS via gh (workflow-only; no Cloud Agent). No GitHub PR review comment (bus only).