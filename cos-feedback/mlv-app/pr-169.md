schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 169
head: ef9f6fc3332176f0de474e68b64efe085848586a
reviewed_utc: 2026-09-25T20:44:21Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip ef9f6fc3 (+125/−27, 11 files, 6 commits; ahead 6 / behind 0): Protected Check Route, Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, CodeQL Analyze (actions/c-cpp/python) all IN_PROGRESS/pending (Tests 36187485095, Factory Bridge 36187485136, CodeQL 36187482795). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft. GUI Pilot / Product Oracles not yet listed on rollup.

## Blockers

(none)

## Improvements

- Close or mark superseded: #156 is still OPEN (title PROD-TELEMETRY-DURATION-AS-PROOF-2) while this PR claims to be its successor; once #169 is green and merged, close #156 so the defect-class work is not double-tracked.
- Hub note on lane-local console `--check-golden` (10 failures, `dual_iso_mode_effective` 1 vs 2): treat Hosted Windows Product Oracles as arbiter before any review keys; if Product Oracles fails on the same delta, do not attribute to "environment" without a master-vs-tip bisect on the hosted runner.

## Notes

- Defect class (round-1): nowhere in tests/pipeline, tests/console, or production playback telemetry is "a stage ran" inferred from elapsed-ms > 0; each site uses an explicit completed flag (or existing structural proof), reset on every live render entry.
- Reused from #156: `21e61f30` / `913cba0a` / `36339c1f` — SH fullres-completed flag + preview test rewrites; pre-dual-ISO completed flag on shared llrawproc object for Phase4B test.
- New on this tip: `42834bcb` — production `RenderFrameThread` reads thread-local `llrpGetLastPreDualIsoFixCompleted` / `Milliseconds` (not racy shared struct); `llrpResetLastPreDualIsoFixTelemetry` at `mlv_render_scaled_rgb16_v2` entry (was missing) and at `_from_raw` reset sites. Shared-field reset at `_v2` deliberately avoided (prefetch race; caught by DualIsoPipeline.Phase4B…).
- `188a850e` — seven `test_clip_golden.cpp` TinyDualIso duration asserts `> 0.0` → `>= 0.0` with earlier structural proofs (`measured_frames`, `dual_iso_mode_effective`/`preview_runtime_active`, `fast_path_active`+`measurement_active`, `play_to_first_frame_measured`).
- `ef9f6fc3` — regenerates `pipeline_hashes.provenance.json` for reused #156 source change (was #156 hosted Repo Hygiene source[3] sha256 mismatch).
- Author lane: `ProcessingFilters.*:DualIsoPipeline.*` 226/0; `test_pipeline_golden_provenance` 29 passed; pinned-token check pass.
- Files: `platform/qt/RenderFrameThread.cpp`, `src/mlv/llrawproc/llrawproc.{c,h}`, `llrawproc_object.h`, `src/mlv/video_mlv.c`, `src/processing/raw_processing.{c,h}`, `tests/console/test_clip_golden.cpp`, `tests/pipeline/test_{dual_iso_pipeline,processing_filters}.cpp`, `tests/fixtures/golden/pipeline_hashes.provenance.json`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound for the defect class; tip required checks / branch protection still settling. Repo Hygiene + Batch Compile first signal; Product Oracles usual long pole given the hub golden note.