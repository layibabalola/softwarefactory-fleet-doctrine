schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 176
head: d6e4c9c7f3a34bdf064f57d802498e8529227532
reviewed_utc: 2026-09-26T12:49:44Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Tip d6e4c9c7 (round 2 after live Ultra-Magnus ACCESS_DENIED legs; +274/−38 on AttrCudaArtifacts.psm1 / playback-attr-3-cuda-job.ps1 / test_playback_attr_3_cuda_behaviour.py vs prior tip ea87c7d0; full PR +2803/−33, 7 files, 13 commits vs merge-base). mergeable CONFLICTING; mergeStateStatus DIRTY — branch diverged from master (ahead 13 / behind 13; master tip 58457761 = #179 PRESENTMON-HARNESS-ROBUSTNESS-3). CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS run 36243079610; hosted Suite / Batch / Factory Bridge / Windows GUI / Product Oracles not yet reported on this tip. Hub live leg still PENDING (required before merge).

## Blockers

- **Merge conflicts with master** — `mergeable=CONFLICTING` / `mergeStateStatus=DIRTY`. Master moved with #179 (PresentMon harness robustness) after this branch's last master merge (`ea87c7d0`). Rebase or merge `master` into `product/CUDA-PERF-DISPLAY-WAKE-3` and resolve before any merge decision; wake files and PresentMon job paths likely overlap in `tools/profiling/bachelor/`.
- **Live leg still required and PENDING** (hub merge condition, stated in PR body). Round 2 was driven by two Ultra-Magnus legs on integration sha `56e14896` that recorded `screensaverRunningBefore=true`, `screensaverRunningAfter=TRUE`, and every `SendInput` denied with `lastError=5` despite successful `OpenInputDesktop`/`SetThreadDesktop`. Do not merge until a clean live pass lands on this tip (or hub explicitly waives). Hosted Suite / local attr-3 wiring tests are not a substitute.
- Tip CI re-settling after the push — wait for hosted Suite (especially Repo Hygiene python behaviour covering the new mask / per-tick desktop-switch / `dismissFailed` tests) plus Batch Compile / GUI Pilot / Product Oracles / CodeQL before any merge decision.

## Improvements

- After the next live venue attempt: confirm the third keep-alive checkpoint (`after_measured_interval`) actually fires on a mid-measurement nudge failure in a real leg, not only via extracted-source mutation tests — that was the gap that could publish `MEASUREMENT_CAPTURED` over a dead wake.
- On the next live leg, pin evidence that `DESKTOP_JOURNALPLAYBACK` cleared the `lastError=5` inject denials and that keep-alive ticks report successful `InputDesktopNudge.Run` (not direct `SendInput` on the Runspace thread). If inject still fails with the new bit, treat as UIPI / higher-integrity desktop and fail closed — do not widen the mask toward `GENERIC_ALL` (test `InputDesktopNudgeMaskTests` pins the four-bit OR).
- Keep #172 CLOSED (closed at open time of #176, not merged); do not reopen the WAKE-2 umbrella.

## Notes

- Round 2 tip commits: `8a0c488f` (DESKTOP_JOURNALPLAYBACK on dismiss mask; keep-alive loop follows input desktop every tick via `InputDesktopNudge.Run`; `dismissFailed` + typed `DISPLAY_WAKE_DISMISS_FAILED` exit 27 before keep-alive starts) and `d6e4c9c7` (tests: `InputDesktopNudgeMaskTests`, `DisplayWakeKeepAlivePerTickDesktopSwitchTests`, `dismissFailed` coverage including live-evidence shape, exit-27 ordering; summary write-site count 18→19).
- Prior CoS Improvements on tip `ea87c7d0` were not the focus of this push; this push responds to live Ultra-Magnus ACCESS_DENIED evidence instead. Live-leg confirmation of the third checkpoint remains open.
- Supersedes parked #172. Stack still carries WAKE-1/2 + claim-time / SCREENSAVER_SECURE_OWNER_ONLY + round-1 sol FAIL-CLOSED fixes + hub v2.2 (unknown running → owner-only; third keep-alive checkpoint; native-unavailable health; outer try/finally).
- Diff vs master still spans `platform/qt/MainWindow.{h,cpp}`, `tests/console/test_playback_display_wake_wiring.cpp`, `AttrCudaArtifacts.psm1`, `playback-attr-3-cuda-job.ps1`, `test_playback_attr_3_cuda_behaviour.py`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **not-merge-ready**: conflicts + pending live leg + tip CI unsettled, even though round 2 correctly fail-closes the live ACCESS_DENIED defect class.