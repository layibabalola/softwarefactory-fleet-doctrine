schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 176
head: 564707323079837d202ea7b2b29743168ab152c3
reviewed_utc: 2026-09-26T13:48:48Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Tip 56470732 (round 3 after live Ultra-Magnus dismiss-ignore legs; +310/−33 on AttrCudaArtifacts.psm1 / playback-attr-3-cuda-job.ps1 / test_playback_attr_3_cuda_behaviour.py vs prior tip d6e4c9c7; full PR +3080/−33, 7 files, 14 commits vs merge-base). mergeable CONFLICTING; mergeStateStatus DIRTY — still diverged from master (ahead 14 / behind 13; master tip 1475fe5c includes #179 PRESENTMON-HARNESS-ROBUSTNESS-3). CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS run 36246340303; hosted Suite / Batch / Factory Bridge / Windows GUI / Product Oracles not yet reported on this tip. Hub live leg still PENDING (required before merge).

## Blockers

- **Merge conflicts with master** — `mergeable=CONFLICTING` / `mergeStateStatus=DIRTY`. Still ahead 14 / behind 13 vs master after this tip. Rebase or merge `master` into `product/CUDA-PERF-DISPLAY-WAKE-3` and resolve before any merge decision; wake files and PresentMon job paths likely overlap in `tools/profiling/bachelor/`.
- **Live leg still required and PENDING** (hub merge condition, stated in PR body). Round 3 responds to three Ultra-Magnus legs where every `InputDesktopNudge` step succeeded (OpenInputDesktop/SetThreadDesktop/SendInput, thread joined) yet `screensaverRunningAfter` stayed true — sub-threshold 1px move ignored by classic scrnsave + immediate after-read. Do not merge until a clean live pass lands on this tip (or hub explicitly waives). Hosted Suite / local attr-3 wiring tests are not a substitute.
- Tip CI re-settling after the push — wait for hosted Suite (especially Repo Hygiene python behaviour covering the new Wait-AttrCudaScreensaverDismissed / VK_F15 nudge / dismissWait evidence) plus Batch Compile / GUI Pilot / Product Oracles / CodeQL before any merge decision.

## Improvements

- On the next live venue attempt: pin evidence that the 8px+VK_F15 dismiss plus `Wait-AttrCudaScreensaverDismissed` (≤5s / 250ms) actually clears `screensaverRunningAfter` to confirmed false, and that `.dismissWait.pollCount` / `.elapsedMilliseconds` are present on the summary. If the screen saver still ignores F15+8px, fail closed (exit 27) — do not escalate to SPI_SET* or Sticky-Keys-triggering keys.
- After a clean live dismiss: confirm the third keep-alive checkpoint (`after_measured_interval`) still fires on a mid-measurement nudge failure in a real leg, not only via extracted-source mutation tests.
- Keep #172 CLOSED (closed at open time of #176, not merged); do not reopen the WAKE-2 umbrella.

## Notes

- Round 3 tip commit `56470732`: `InputDesktopNudge.Run` now sends 8px round-tripped mouse move + single VK_F15 down/up (inert F13–F24; not Shift); new `Wait-AttrCudaScreensaverDismissed` polls running-state up to 5s at 250ms when a dismiss was attempted; `.dismissWait` evidence on start wake; embedding list gains the wait helper; tests cover nudge composition (mouse/keyboard union arms), wait stop-on-false / timeout / never-coerce-null, and embedding contract.
- Prior CoS Improvement on tip `d6e4c9c7` about pinning that `DESKTOP_JOURNALPLAYBACK` cleared `lastError=5` inject denials is addressed by the live UM evidence driving this push (inject steps all clean; defect moved to ignore-threshold / immediate after-read). Live confirmation of round-3 dismiss success remains open.
- Supersedes parked #172. Stack still carries WAKE-1/2 + claim-time / SCREENSAVER_SECURE_OWNER_ONLY + round-1 sol FAIL-CLOSED + hub v2.2 + round-2 JOURNALPLAYBACK / per-tick InputDesktopNudge / exit-27 dismissFailed.
- Diff vs master still spans `platform/qt/MainWindow.{h,cpp}`, `tests/console/test_playback_display_wake_wiring.cpp`, `AttrCudaArtifacts.psm1`, `playback-attr-3-cuda-job.ps1`, `test_playback_attr_3_cuda_behaviour.py`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **not-merge-ready**: conflicts + pending live leg + tip CI unsettled, even though round 3 correctly targets the live dismiss-ignore defect class.
