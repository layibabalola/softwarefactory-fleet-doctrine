schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 176
head: ea87c7d0d5862248f4a3b981164fa434ddb25472
reviewed_utc: 2026-09-26T02:49:50Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Tip ea87c7d0 (merge master into product/CUDA-PERF-DISPLAY-WAKE-3; +2567/−33, 7 files vs master, 11 commits; ahead 11 / behind 0). Merge brought #175 PROD-TELEMETRY-DURATION-AS-PROOF-4B onto the tip; wake stack tip content still from 655936c6. Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Factory Bridge Regressions, Windows GUI Pilot, Windows Product Oracles, CodeQL Analyze (actions/c-cpp/python) IN_PROGRESS (Tests 36212858200, Factory Bridge 36212858228, CodeQL 36212864700). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft. Hub live leg still PENDING (required before merge; bachelor offline, Ultra-Magnus venue in progress).

## Blockers

- **Live leg still required and PENDING** (hub merge condition, stated in PR body). Runs on Ultra-Magnus or bachelor; bachelor is offline and the Ultra-Magnus venue is in progress. Do not merge until a clean live pass lands for this tip (or hub explicitly waives). Hosted Suite / local attr-3 wiring tests are not a substitute for the hub-required live leg.
- Tip CI re-settling after master merge — wait for hosted Suite (especially Repo Hygiene python behaviour suite covering the fail-closed / keep-alive health tests) plus Batch Compile / GUI Pilot / Product Oracles / CodeQL before any merge decision. PCR already SUCCESS on this tip.

## Improvements

- After the live venue is back: confirm the third keep-alive checkpoint (`after_measured_interval`) actually fires on a mid-measurement nudge failure in a real leg, not only via the extracted-source mutation tests — that was the gap that could publish MEASUREMENT_CAPTURED over a dead wake.
- Keep #172 CLOSED (closed at open time of #176, not merged); do not reopen the WAKE-2 umbrella — this PR owns the superseding stack plus round-1 / hub v2.2 fail-closed fixes.

## Notes

- Push `ea87c7d0` is merge of master (`1475fe5c`, #175 duration-as-proof inventory gate) into `product/CUDA-PERF-DISPLAY-WAKE-3`. No new wake-stack commits beyond prior tip `655936c6`. Prior CoS Improvements not addressed by this push (master sync only).
- Supersedes parked #172 (contract v2 BLOCKER → PARK/SPLIT). Carries #172 (`100a807b` WAKE-1, `5f0ab8e9` WAKE-2 keep-alive / SC_SCREENSAVE refusal, `553feb78` claim-time + SCREENSAVER_SECURE_OWNER_ONLY) plus round-1 sol BLOCKER fixes (`7a65e6a3`/`93ba5c70`/`2284e585`: fail-closed on unknown secure state; every SendInput outcome recorded; KEEPALIVE_FAILED exit 26; CloseDesktop result recorded; keep-alive setup try/catch; `nativeEvent` override; minimal DESKTOP_* mask) and hub procedure v2.2 pre-review fixes (`afdf53c9`/`d2418546`/`655936c6`: unknown running state always owner-only; third keep-alive checkpoint after measured interval; native-unavailable → typed setupError + RequireSuccessSoFar at first checkpoint; one outer try/finally over whole wake lifetime). Sol's second pre-review APPROVED.
- App: `SetThreadExecutionState(ES_CONTINUOUS|ES_DISPLAY_REQUIRED)` on play/stop/close; Windows-only `nativeEvent` refuses `WM_SYSCOMMAND` `SC_SCREENSAVE`/`SC_MONITORPOWER` while armed. Job: claim-time wake; 15s keep-alive Runspace; read-only `SPI_GET*` only (never `SPI_SET*`); unknown/secure saver → typed early exit no input; three KEEPALIVE_FAILED health checkpoints.
- Diff vs master: `platform/qt/MainWindow.{h,cpp}`; `tests/console/test_playback_display_wake_wiring.cpp` + `console_tests.pro`; `tools/profiling/bachelor/AttrCudaArtifacts.psm1` + `playback-attr-3-cuda-job.ps1`; `tools/repo_hygiene/test_playback_attr_3_cuda_behaviour.py`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **not-merge-ready**: content still addresses #172 sol blockers and hub v2.2 findings, but hub-required live leg remains open and tip CI is re-settling after the master merge.
