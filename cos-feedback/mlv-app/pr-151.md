schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 151
head: ace707a46eada437a4e616c5bab90bd904f767bf
reviewed_utc: 2026-09-22T14:34:27Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip ace707a4 (+585/−21, 12 files vs master; 4 commits on branch incl. master merge): Analyze (actions) PASS; CodeQL NEUTRAL; remaining IN_PROGRESS/queued (Protected Check Route, Factory Bridge Regressions, Analyze c-cpp+python, Repo Hygiene Python ubuntu+windows, Batch Compile; Windows Product Oracles / GUI Pilot not yet listed on tip); mergeable MERGEABLE, mergeStateStatus BLOCKED; ahead 4 / behind 0 vs master; 0 GitHub reviews.

## Blockers

(none)

## Improvements

- Soft: `m_playbackScaleClampedForGpuTextureRouteActive` / `…RequestedScale` remain `mutable` on a `const` method for smoke-summary side effects — still worth a one-line comment at the member decls (carried from prior CoS).
- Soft: once-per-session `loggedScaleClampOnce` static still will not re-log if eligibility flips mid-session; fine for smoke, prefer per-clamp or rate-limited log when diagnosing route flips.
- Soft: PR body still says **merge AFTER #149** — #149 is now MERGED (`db4e5a66`, 2026-09-22T14:32:04Z). Refresh the description so consumers do not treat the order note as current.
- Soft: body promised bachelor re-measure of colour + fps after taking master with #149; this tip is the merge + round 3 validation only — no new bachelor receipt on `ace707a4`. Optional before merge: one texture-route scale-4 colour+fps check on bachelor.
- Soft: still **0 GitHub reviews**; short human glance on the policy table + validation contract is cheap.

## Notes

- **Verdict change:** prior CoS (`3de0f84a`) was `not-merge-ready` for merge-order (#149 open). #149 MERGED; this tip merged `fork/master` (`ace707a4`) so texture-route colour fix is on the base. Content posture → `merge-when-ci-green`.
- Round 3 (`456f47e`): smoke validation fails on ANY non-`gpu_texture_no_readback` counter when texture route requested (`cpu_frames` / `gpu_preview_frames` / `gpu_recon_readback_frames` / `gpu_texture_readback_frames`); `Test-GuiSmokeEnvironmentPairTruthy` trims like C++; route-requested detection uses effective child env (launch override > clear > ambient).
- Intent unchanged: clamp effective playback scale to 1 when GPU recon texture-present route is eligible so GL sampler downscales; user-facing scale unchanged; avoids silent full-res `gpu_recon_readback` at scale 2/4 (~3.3–3.5 → ~19 fps claimed on prior bachelor receipt at `3de0f84a`).
- Round 1–2 still on tip: pure clamp + eligibility table; `m_gpuPlaybackReconPolicyLibraryReady` startup gate; sixth smoke-runner closure file pin.
- Files vs master (12): `MainWindow.cpp`/`.h`, `MainWindowGpuPreviewPolicy.h`, `test_gui_smoke.cpp`, AttrCudaArtifacts + attr3/playback jobs, validation + test scripts, `run-release-gui-smoke.ps1`, two repo_hygiene Python tests.
- Known disclosed limitation (texture-route fresh-render provenance) remains #147 territory.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).