schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 151
head: 3de0f84ab542e33c9aab466c781740af897d0262
reviewed_utc: 2026-09-22T07:20:34Z
event: pr-opened
verdict: not-merge-ready
ci_summary: Tip 3de0f84 (+439/−21, 12 files vs master; 2 commits): Protected Check Route PASS; Analyze (actions) PASS; CodeQL NEUTRAL; remaining IN_PROGRESS (Factory Bridge Regressions, Analyze c-cpp+python, Repo Hygiene Python ubuntu+windows, Batch Compile, Windows Product Oracles, Windows GUI Pilot); mergeable MERGEABLE, mergeStateStatus BLOCKED; ahead 2 / behind 0 vs master; 0 GitHub reviews.

## Blockers

- **Merge order: land #149 first.** PR body and measured path require `GPU-TEXNR-S1-DARK-GREEN-1` (#149, still OPEN at 99dedb2c) before this tip. On master the texture route still shows the dark grey-green picture #149 fixes; with this clamp, scale 2/4 also take that route, so merging #151 first would regress scale-4 colour. After #149 merges, retarget/rebase this branch on master and re-measure colour + fps on bachelor before merge.
- Wait for required tip checks still IN_PROGRESS (especially Batch Compile / Windows GUI Pilot / Windows Product Oracles and Repo Hygiene Python covering the six-file smoke-runner closure pin).

## Improvements

- Soft: `m_playbackScaleClampedForGpuTextureRouteActive` / `…RequestedScale` are `mutable` on a `const` method so telemetry can update during `effectivePlaybackScaleFactorForRequest()` — intentional, but a future reader may want a short comment at the member decls tying mutability to smoke-summary side effects only.
- Soft: once-per-session `loggedScaleClampOnce` static means a later eligibility flip (scopes on/off, caching, env) will not re-log; fine for smoke, but if diagnosing mid-session route changes, prefer a per-clamp or rate-limited log.
- Soft: still **0 GitHub reviews**; short human glance on the policy table + bachelor merge-order note is cheap before merge.

## Notes

- Intent: GPU recon texture-present route is wired only for `playbackScaleFactor == 1`. Requesting scale 2/4/8 while that route would arm previously fell through to full-res `gpu_recon_readback` (~3.3–3.5 fps at scale 4). Clamp effective scale to 1 at the single policy source so GL sampler downscales; user-facing scale setting unchanged.
- Round 1 (`72bd923a`): `mainWindowClampPlaybackScaleForGpuTextureRoute` + `effectivePlaybackScaleFactorForRequest` / `playbackScaleFactorPolicyDecision` split; smoke fields `gpu_texture_route_scale_clamp_*`; validation `gpu_texture_route_readback_regression` when texture route requested but any `gpu_recon_readback` frame appears.
- Round 2 (`3de0f84a`): startup crash fix — eligibility touched `m_pProcessingObject` during `initGui()` before `initLib()` (indeterminate non-null pointer). Gate on `m_gpuPlaybackReconPolicyLibraryReady`; pure `mainWindowGpuPlaybackReconTextureRouteEligibleAtScaleOne`; pin `gui-smoke-gpu-texture-route-validation.ps1` as sixth smoke-runner closure file (five→six across AttrCudaArtifacts / jobs / hygiene tests).
- Bachelor receipt (claimed in body, tracked fixture 3de0f84a): GPU scale 4 **19.0 fps** (609/609 texture, 0 readback) vs prior 3.3–3.5; scale 2 **17.5**; scale 1 ~17.9. CoS did not re-run bachelor.
- Tests: C++ decision-table unit tests in `tests/gui/test_gui_smoke.cpp`; PowerShell `tools/profiling/test-gui-smoke-gpu-texture-route-validation.ps1`; hygiene tests updated for six-file closure.
- Known disclosed limitation (texture-route fresh-render provenance) remains #147 territory; not introduced here.
- Files: `MainWindow.cpp` / `.h`, `MainWindowGpuPreviewPolicy.h`, `test_gui_smoke.cpp`, AttrCudaArtifacts + attr3/playback jobs, new validation + test scripts, `run-release-gui-smoke.ps1`, two repo_hygiene Python tests.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).