schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 146
head: 2c4bd8baf3b8e22c7023f9f666b2a8b5afb9724e
reviewed_utc: 2026-09-21T15:44:45Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 2c4bd8baf (2 commits on product/CUDA-SCALE4-ZERO-PRESENT-1; +269/−3 across 6 files vs master): all checks QUEUED/pending at open (Protected Check Route, Batch Compile, Repo Hygiene Python ubuntu+windows, Factory Bridge Regressions, CodeQL Analyze actions/c-cpp/python); mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python which exercises the new present-nothing tripwires, and GUI/Protected Check Route for the policy tests in `tests/gui/test_gui_smoke.cpp`)

## Improvements

- Soft: `TRACE_LOOKBACK_LINES = 20` in `tools/repo_hygiene/test_present_playback_prepared_frame_traces_drops.py` is a proximity heuristic — a future bare `return;` with a distant or unrelated `logInteractionEvent` in the lookback window could still pass; consider requiring the event name to include a drop/present_nothing token, or anchoring to the immediately preceding non-blank statement.
- Soft: still **0 GitHub reviews** on a presentation-policy gate that already needed a round-2 narrow after a scale-1 window regression; a short human glance at `mainWindowAllowsGpuAmazeTexturePresentation` (widget OR tex-nr) before merge is cheap.

## Notes

- Root cause matches the PR narrative: `gpuViewportInstalled` was true whenever `GpuDisplayWindow::isActive()`, so scale-4 GPU+processing took the AMaZE-texture branch whose present calls only hit the QOpenGLWidget viewport; `presentPlaybackPreparedFrame` then released the slot and returned with no trace.
- Round 1 (`07c68d2cef`): adds `gpuWidgetViewportInstalled`, gates `mainWindowAllowsGpuAmazeTexturePresentation` on it, populates the flag at every policy-builder site from `GpuDisplayViewport::isInstalledOn`, and instruments the silent path with `draw_frame_ready.present_nothing` + `m_presentNothingDropCount` / `present_nothing_drops` in `playback_smoke.summary`.
- Round 2 (`2c4bd8baf`): narrows the gate to `gpuWidgetViewportInstalled || mainWindowAllowsGpuPlaybackReconTexturePresentation(state)` so scale-1 tex-nr window routing stays eligible (byte-for-byte pre-round-1 when tex-nr is eligible); scale 4 still requires the widget viewport. GUI policy test covers both cases.
- Tripwires: every bare `return;` in `presentPlaybackPreparedFrame` must be preceded by `logInteractionEvent` within 20 lines; summary format must declare `present_nothing_drops=%N` with a `PresentNothingDrops` arg sourced from `m_presentNothingDropCount`.
- Owner laptop evidence (diagnostic, not measurement): scale 4 GPU 0→69/70 presented; scale 1 gap ~40→0; `present_nothing_drops` 0 after fix.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).