schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 146
head: 6ddbd332dfae90a177ab85ceda8ab06c1664f35f
reviewed_utc: 2026-09-21T16:15:02Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 6ddbd332dfae (3 commits on product/CUDA-SCALE4-ZERO-PRESENT-1; +399/−3 across 6 files vs master): Protected Check Route PASS; Analyze actions/c-cpp/python, Factory Bridge Regressions, Batch Compile, Repo Hygiene Python ubuntu+windows, Windows GUI Pilot, Windows Product Oracles IN_PROGRESS/pending; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python for the strengthened present-nothing tripwires and GUI/Protected Check Route for the unconditional widget-viewport policy tests)

## Improvements

- Soft: still **0 GitHub reviews** on a presentation-policy gate that already needed a round-2 false start and a round-3 honesty pass; a short human glance at `mainWindowAllowsGpuAmazeTexturePresentation` (now unconditional widget) and the `drawFrame` evaluation order (`:6258` before `:6260` compatible compute) before merge is cheap.
- Soft: tripwire still accepts any `logInteractionEvent(` in the enclosing block (not a drop/present_nothing-named event). Brace scoping closed the prior lookback hole; naming the event remains optional hardening.

## Notes

- Round 3 (`6ddbd332dfae` vs prior CoS head `2c4bd8baf`): removes the dead tex-nr carve-out from `mainWindowAllowsGpuAmazeTexturePresentation`. Reviewers confirmed the round-2 disjunct was dead at the only production feed site — `drawFrame` evaluates the gate while `gpuPlaybackReconTexturePresentationCompatible` is still the hard `false` set at `:6212` (real value computed after at `:6260-6266`). Bachelor A/B therefore ran round-1's unconditional widget gate the whole time; round 2's "byte-for-byte" scale-1 restoration never shipped. Gate is now `usesAmazeDebayer && envRequested && gpuWidgetViewportInstalled` only.
- Round 3 also: refreshes `gpuWidgetViewportInstalled` symmetrically with `gpuViewportInstalled` at the `drawFrameReady` copy site; tripwire scoped to nearest enclosing brace block and matches inline `if(x) return;`; summary placeholder test proves `%N` position maps to the PresentNothingDrops `.arg()` call (contiguous 1..N placeholders).
- Prior CoS Improvement (TRACE_LOOKBACK_LINES=20 proximity heuristic) **addressed** by round-3 brace-block scoping. Prior soft (0 GitHub reviews) **not** addressed.
- Round 1/2 substance retained: `gpuWidgetViewportInstalled` field, populate at every policy-builder from `GpuDisplayViewport::isInstalledOn`, `draw_frame_ready.present_nothing` + `m_presentNothingDropCount` / `present_nothing_drops=%64` in `playback_smoke.summary`.
- Owner laptop evidence (diagnostic): scale 4 GPU 0→70/72 presented; scale 1 begin==end; `present_nothing_drops` 0 after fix. Scale-1 window AMaZE texture now correctly refuses without widget (offscreen/presentImage path); tex-nr recon route remains separate via `mainWindowAllowsGpuPlaybackReconTexturePresentation`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).