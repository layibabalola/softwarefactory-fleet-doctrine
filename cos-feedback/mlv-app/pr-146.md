schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 146
head: 434d5cb5dcef81093d9a8525c23c77568ca976e1
reviewed_utc: 2026-09-21T17:39:22Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 434d5cb5dcef (7 commits on product/CUDA-SCALE4-ZERO-PRESENT-1; +1176/−3 across 6 files vs master; round 7 test-only +270/−12 on tripwire lexer/strictness): Analyze actions/c-cpp/python + Factory Bridge Regressions IN_PROGRESS on tip; other tip checks not yet reported; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 434d5cb5dcef, especially Repo Hygiene Python for the round-7 fail-closed `_lex(strict=True)` suite and GUI/Protected Check Route for the unconditional widget-viewport policy tests)

## Improvements

- Soft: still **0 GitHub reviews** on a presentation-policy gate that already needed a round-2 false start and a round-3 honesty pass; a short human glance at `mainWindowAllowsGpuAmazeTexturePresentation` (unconditional widget) and the `drawFrame` evaluation order (gate before compatible compute) before merge is cheap.

## Notes

- Round 7 (`434d5cb5dcef` vs prior CoS head `533f95cfae49`): **test-only**. Closes the lexing question for this tripwire per hub ruling: model the standard cases exactly and fail closed on anything outside that model. (1) sol r6: `//` line comment + backslash-newline continues onto the next physical line in both views (backslash blanked as comment; state stays `_LINE_COMMENT`; newline unblanked for line numbers). (2) fable r6 minor 1: `_find_returns_with_context` matches `RETURN_PATTERN` against the CODE-ONLY view so a `return;` only inside a string cannot register as a phantom return. (3) `_lex(..., strict=True)` raises `UnsupportedConstructError` (construct + line) on backslash-newline outside comment/string, digit separator, encoding-prefixed raw string (`LR"`/`uR"`/`UR"`/`u8R"`), trigraph, or preprocessor directive at code depth — scoped to the already-extracted function body only; whole-source brace find stays non-strict. Twelve new tests (refused + adjacent not-refused + comment-continuation lexer case + string-return phantom + real-body has-no-unsupported). Author: 307 tests green locally. No product code touched.
- Prior CoS Improvement (0 GitHub reviews) **not** addressed. Round-4 depth-exact + counter lockstep, round-5 code-only matching, and round-6 single-pass `_lex` Improvements remain satisfied; round 7 closes residual phase-2 comment splicing, string-phantom returns, and silent-mislex classes.
- Round 6 substance retained: one `_lex` emits both `comment_masked` and `code_only`; raw-string support; unmatched-quote-in-comment false-accept mutation.
- Round 5 substance retained: code-only token matching for `traced`/`counted`; `is_present_nothing` still matches the comment-masked view so the marker's own string literal stays visible.
- Round 4 substance retained: return tripwire tracks only text at each return's own brace depth; present_nothing exit requires `fetch_add(` lockstep; in-memory mutation tests for sibling-block / missing counter / untraced return / string-only tokens.
- Round 3 substance retained: dead tex-nr carve-out removed from `mainWindowAllowsGpuAmazeTexturePresentation` (gate is `usesAmazeDebayer && envRequested && gpuWidgetViewportInstalled` only); `gpuWidgetViewportInstalled` refreshed symmetrically at `drawFrameReady` copy site; summary placeholder test proves `%64` maps to PresentNothingDrops `.arg()`.
- Round 1/2 substance retained: `gpuWidgetViewportInstalled` field populated at every policy-builder from `GpuDisplayViewport::isInstalledOn`; formerly silent drop emits `draw_frame_ready.present_nothing` + counter exported as `present_nothing_drops=%64` in `playback_smoke.summary`.
- Owner laptop evidence (diagnostic, earlier rounds): scale 4 GPU 0→70/72 presented; scale 1 begin==end; `present_nothing_drops` 0 after fix. Scale-1 window AMaZE texture correctly refuses without widget (offscreen/presentImage path); tex-nr recon remains separate via `mainWindowAllowsGpuPlaybackReconTexturePresentation`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat silent on this push: verdict unchanged and prior CoS Improvements not clearly addressed.