schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 146
head: 8e49a996ae901b6d6d179a4acfd0660f1e1ab610
reviewed_utc: 2026-09-21T18:55:45Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 8e49a996ae90 (10 commits on product/CUDA-SCALE4-ZERO-PRESENT-1; +282/−3 across 5 files vs master; round 10 deletes the 1083-line static tripwire module, keeps product fix + GUI policy + summary-format tests): Analyze actions/c-cpp/python + Factory Bridge Regressions IN_PROGRESS/pending on tip; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 8e49a996ae90, especially GUI/Protected Check Route for the unconditional widget-viewport policy tests and Repo Hygiene Python for `test_playback_smoke_summary_present_nothing_drops.py`)

## Improvements

- Soft: still **0 GitHub reviews** on a presentation-policy gate that already needed a round-2 false start and a round-3 honesty pass; a short human glance at `mainWindowAllowsGpuAmazeTexturePresentation` (unconditional widget) and the `drawFrame` evaluation order (gate before compatible compute) before merge is cheap.

## Notes

- Round 10 (`8e49a996ae90` vs prior CoS head `65cd489322f2`): **test-only scope cut**. Hub ruling (three-brief swarm): rounds 3–9 findings were all on the hand-written C++ lexer in `tools/repo_hygiene/test_present_playback_prepared_frame_traces_drops.py`, while product code had no finding since round 3 and is proven on hardware. Owner priority is CUDA playback; this PR blocks the CUDA measurement chain. Static tripwire split out to card **STATIC-PRESENT-TRIPWIRE-1** (reference implementation retained in this branch history at `65cd4893`). PR keeps: product fix, runtime `draw_frame_ready.present_nothing` + `present_nothing_drops`, GUI policy tests, `test_playback_smoke_summary_present_nothing_drops.py` (docstring now points at the card). Deleted: entire `test_present_playback_prepared_frame_traces_drops.py` (−1083). Net vs master now +282/−3 / 5 files (was +1363/−3 / 6 with the lexer).
- Prior CoS Improvement (0 GitHub reviews) **not** addressed. Product surface unchanged since round 3; only the tripwire ballast left.
- Product substance retained (rounds 1–3): `gpuWidgetViewportInstalled` gates `mainWindowAllowsGpuAmazeTexturePresentation` unconditionally; populated at every policy-builder + symmetric refresh with `gpuViewportInstalled` at `drawFrameReady` copy; dead round-2 tex-nr carve-out removed; silent drop → `present_nothing` trace + counter in `playback_smoke.summary`.
- Owner laptop evidence (diagnostic, earlier rounds): scale 4 GPU 0→70/72 presented; scale 1 begin==end; `present_nothing_drops` 0 after fix. Scale-1 window AMaZE texture correctly refuses without widget (offscreen/presentImage path); tex-nr recon remains separate via `mainWindowAllowsGpuPlaybackReconTexturePresentation`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat silent on this push: verdict unchanged and prior CoS Improvements not clearly addressed.
