schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 146
head: 7f2f4b254f0183766606c165a23d42e4de6c70f0
reviewed_utc: 2026-09-21T18:04:50Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 7f2f4b254f01 (8 commits on product/CUDA-SCALE4-ZERO-PRESENT-1; +1266/−3 across 6 files vs master; round 8 test-only +153/−63 on tripwire lexer class-refusal): Analyze actions/c-cpp/python + Factory Bridge Regressions + CodeQL IN_PROGRESS/pending on tip; other tip checks not yet reported; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 7f2f4b254f01, especially Repo Hygiene Python for the round-8 fail-closed class-refusal suite and GUI/Protected Check Route for the unconditional widget-viewport policy tests)

## Improvements

- Soft: still **0 GitHub reviews** on a presentation-policy gate that already needed a round-2 false start and a round-3 honesty pass; a short human glance at `mainWindowAllowsGpuAmazeTexturePresentation` (unconditional widget) and the `drawFrame` evaluation order (gate before compatible compute) before merge is cheap.

## Notes

- Round 8 (`7f2f4b254f01` vs prior CoS head `434d5cb5dcef`): **test-only**. Hub ruling: refuse whole construct CLASSES instead of modelling more corners. Closes two round-7 lexical gaps that shared one root cause (modelling individual "fine here" spellings). (1) sol r7: block-comment terminator formed by backslash-newline splicing (`*` `\` newline `/`) was accepted but mis-lexed (round 7 only modeled splicing inside `//`, not block comments). (2) fable r7: preprocessor whose `#` is preceded on its line only by a comment (e.g. `/* x */ #if 0`) missed round 7's `_is_first_nonspace_on_line` check. Round 8 responses: ANY backslash immediately followed by newline (optional CRLF) ANYWHERE in scanned text refused via whole-text regex pre-scan in strict mode — removes round-7 line-comment continuation modelling and the string-escape carve-out (line splicing is pre-tokenization). ANY `#` in the CODE view refused regardless of column — `_is_first_nonspace_on_line` removed. Digit separators / encoding-prefixed raw strings / trigraphs unchanged. New/rewritten refuse tests (block-comment splice terminator; `//` trailing backslash; backslash-newline in string; comment-then-`#`; mid-line `#` in code) plus not-refused for `#` inside comment/string; real function still passes. No product code touched.
- Prior CoS Improvement (0 GitHub reviews) **not** addressed. Rounds 4–7 Improvements remain satisfied; round 8 closes residual sol/fable r7 class gaps without widening product surface.
- Round 7 substance retained then superseded where noted: standard-case modeling + fail-closed on unsupported constructs; `RETURN_PATTERN` on code-only view; `_lex(strict=True)` raises `UnsupportedConstructError`. Round 8 replaces the round-7 "modeled as continuing" `//` backslash-newline and first-nonspace `#` checks with class refusal.
- Round 6 substance retained: one `_lex` emits both `comment_masked` and `code_only`; raw-string support; unmatched-quote-in-comment false-accept mutation.
- Round 5 substance retained: code-only token matching for `traced`/`counted`; `is_present_nothing` still matches the comment-masked view so the marker's own string literal stays visible.
- Round 4 substance retained: return tripwire tracks only text at each return's own brace depth; present_nothing exit requires `fetch_add(` lockstep; in-memory mutation tests for sibling-block / missing counter / untraced return / string-only tokens.
- Round 3 substance retained: dead tex-nr carve-out removed from `mainWindowAllowsGpuAmazeTexturePresentation` (gate is `usesAmazeDebayer && envRequested && gpuWidgetViewportInstalled` only); `gpuWidgetViewportInstalled` refreshed symmetrically at `drawFrameReady` copy site; summary placeholder test proves `%64` maps to PresentNothingDrops `.arg()`.
- Round 1/2 substance retained: `gpuWidgetViewportInstalled` field populated at every policy-builder from `GpuDisplayViewport::isInstalledOn`; formerly silent drop emits `draw_frame_ready.present_nothing` + counter exported as `present_nothing_drops=%64` in `playback_smoke.summary`.
- Owner laptop evidence (diagnostic, earlier rounds): scale 4 GPU 0→70/72 presented; scale 1 begin==end; `present_nothing_drops` 0 after fix. Scale-1 window AMaZE texture correctly refuses without widget (offscreen/presentImage path); tex-nr recon remains separate via `mainWindowAllowsGpuPlaybackReconTexturePresentation`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat silent on this push: verdict unchanged and prior CoS Improvements not clearly addressed.
