schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 146
head: 65cd489322f2447364ba56298428c01da4aa4938
reviewed_utc: 2026-09-21T18:30:37Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip 65cd489322f2 (9 commits on product/CUDA-SCALE4-ZERO-PRESENT-1; +1363/−3 across 6 files vs master; round 9 test-only +211/−114 on tripwire lexer): Analyze actions/c-cpp/python + Factory Bridge Regressions + CodeQL IN_PROGRESS/pending on tip; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip 65cd489322f2, especially Repo Hygiene Python for the round-9 unconditional raw-string refusal suite and GUI/Protected Check Route for the unconditional widget-viewport policy tests)

## Improvements

- Soft: still **0 GitHub reviews** on a presentation-policy gate that already needed a round-2 false start and a round-3 honesty pass; a short human glance at `mainWindowAllowsGpuAmazeTexturePresentation` (unconditional widget) and the `drawFrame` evaluation order (gate before compatible compute) before merge is cheap.

## Notes

- Round 9 (`65cd489322f2` vs prior CoS head `7f2f4b254f01`): **test-only**. Hub ruling: refuse the whole raw-string CLASS everywhere (delete modelling). Closes sol r8: unprefixed raw string with brace in delimiter (e.g. `R"}(payload)}"`) was accepted but the delimiter text was scanned in `_CODE` before `_RAW_STRING` and never blanked, so a `}` leaked into the comment-masked brace-count view and could end `_extract_function_body` early before a later untraced `return;` while the tripwire still reported success. Round 9 removes `_RAW_STRING` state machine; `_lex` raises `UnsupportedConstructError` on any `R"` / `LR"`/`uR"`/`UR"`/`u8R"` opener (not preceded by an identifier char) **unconditionally** (both strict and non-strict), including the full-~20k-line non-strict extraction pass. Encoding-prefixed and unprefixed forms share one `_RAW_STRING_OPEN` regex. New tests: brace-delimiter extraction fail-closed end-to-end; full-source has no raw string during extraction; unprefixed/prefixed refused in both strict modes; identifier-ending-in-R not mistaken for raw; former `test_unprefixed_raw_string_is_not_refused` inverted. Deliberate cost: an unrelated raw string anywhere in `MainWindow.cpp` would now break this tripwire (hub accepts fail-closed). No product code touched.
- Prior CoS Improvement (0 GitHub reviews) **not** addressed. Rounds 4–8 Improvements remain satisfied; round 9 closes sol r8 raw-string model hazard without widening product surface.
- Round 8 substance retained then superseded where noted: class refusal for backslash-newline anywhere + `#` in CODE view; round 9 replaces encoding-prefixed-only raw refusal (strict) and unprefixed raw modelling with unconditional all-raw refusal.
- Round 7 substance retained then superseded where noted: standard-case modeling + fail-closed; `RETURN_PATTERN` on code-only view; `_lex(strict=True)` raises `UnsupportedConstructError`.
- Round 6 substance retained then superseded: one `_lex` emits both views; raw-string *modelling* deleted in round 9 (refusal replaces it).
- Round 5–1 substance retained: code-only token matching; return tripwire at own brace depth + `fetch_add` lockstep; dead tex-nr carve-out removed; `gpuWidgetViewportInstalled` at every policy-builder + symmetric refresh; `present_nothing` / `present_nothing_drops`.
- Owner laptop evidence (diagnostic, earlier rounds): scale 4 GPU 0→70/72 presented; scale 1 begin==end; `present_nothing_drops` 0 after fix. Scale-1 window AMaZE texture correctly refuses without widget (offscreen/presentImage path); tex-nr recon remains separate via `mainWindowAllowsGpuPlaybackReconTexturePresentation`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat silent on this push: verdict unchanged and prior CoS Improvements not clearly addressed.
