schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 146
head: ccda4fb75f3434954540cb182e82d6abd9ee338c
reviewed_utc: 2026-09-21T16:31:01Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip ccda4fb75f34 (4 commits on product/CUDA-SCALE4-ZERO-PRESENT-1; +636/−3 across 6 files vs master; round 4 test-only +267/−30 on tripwire): Analyze actions/c-cpp/python + Factory Bridge Regressions IN_PROGRESS; other required checks not yet reported on this tip; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks, especially Repo Hygiene Python for the round-4 tripwire mutation suite and GUI/Protected Check Route for the unconditional widget-viewport policy tests)

## Improvements

- Soft: still **0 GitHub reviews** on a presentation-policy gate that already needed a round-2 false start and a round-3 honesty pass; a short human glance at `mainWindowAllowsGpuAmazeTexturePresentation` (unconditional widget) and the `drawFrame` evaluation order (`:6258` gate before `:6260` compatible compute) before merge is cheap.

## Notes

- Round 4 (`ccda4fb75f34` vs prior CoS head `6ddbd332dfae`): **test-only**. Rewrites `test_present_playback_prepared_frame_traces_drops.py` so the return tripwire tracks only text written directly at each return's own brace depth (nested closed sibling blocks excluded), masks comments/in-string braces before depth tracking, and requires `m_presentNothingDropCount.fetch_add(` alongside the trace for the `draw_frame_ready.present_nothing` exit. In-memory mutation tests prove: sibling-block false trace rejected, missing counter rejected, inserted untraced `return;` rejected, real-shaped sample still passes. No product code touched.
- Prior CoS Improvement (tripwire accepts any `logInteractionEvent(` via sibling-block false positive) **addressed** by round-4 depth-exact scan + present_nothing counter lockstep. Prior soft (0 GitHub reviews) **not** addressed.
- Round 3 substance retained: dead tex-nr carve-out removed from `mainWindowAllowsGpuAmazeTexturePresentation` (gate is `usesAmazeDebayer && envRequested && gpuWidgetViewportInstalled` only); `gpuWidgetViewportInstalled` refreshed symmetrically at `drawFrameReady` copy site; summary placeholder test proves `%64` maps to PresentNothingDrops `.arg()`.
- Round 1/2 substance retained: `gpuWidgetViewportInstalled` field populated at every policy-builder from `GpuDisplayViewport::isInstalledOn`; formerly silent drop emits `draw_frame_ready.present_nothing` + counter exported as `present_nothing_drops=%64` in `playback_smoke.summary`.
- Owner laptop evidence (diagnostic, earlier rounds): scale 4 GPU 0→70/72 presented; scale 1 begin==end; `present_nothing_drops` 0 after fix. Scale-1 window AMaZE texture correctly refuses without widget (offscreen/presentImage path); tex-nr recon remains separate via `mainWindowAllowsGpuPlaybackReconTexturePresentation`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only).