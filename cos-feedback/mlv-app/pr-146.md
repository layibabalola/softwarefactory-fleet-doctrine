schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 146
head: fea673de2e2f698a6356ddc46ca110403d3d2228
reviewed_utc: 2026-09-21T16:55:50Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Tip fea673de2e2f (5 commits on product/CUDA-SCALE4-ZERO-PRESENT-1; +736/−3 across 6 files vs master; round 5 test-only +126/−26 on tripwire): Analyze actions/c-cpp/python + Factory Bridge Regressions IN_PROGRESS on tip; Tests workflow pending/queued; prior tip ccda4fb75f34 had Protected Check Route / Batch Compile / Repo Hygiene Python ubuntu / Windows GUI Pilot PASS with Repo Hygiene Python windows still IN_PROGRESS and Windows Product Oracles cancelled; mergeable_state blocked; 0 GitHub reviews.

## Blockers

(none in the reviewed diff — wait for required checks on tip fea673de2e2f, especially Repo Hygiene Python for the round-5 code-only tripwire suite and GUI/Protected Check Route for the unconditional widget-viewport policy tests)

## Improvements

- Soft: still **0 GitHub reviews** on a presentation-policy gate that already needed a round-2 false start and a round-3 honesty pass; a short human glance at `mainWindowAllowsGpuAmazeTexturePresentation` (unconditional widget) and the `drawFrame` evaluation order (gate before compatible compute) before merge is cheap.

## Notes

- Round 5 (`fea673de2e2f` vs prior CoS head `ccda4fb75f34`): **test-only**. Tripwire now builds a code-only view (`_mask_all_literals`: comments blanked plus string/char contents blanked) for `traced`/`counted` matches, so a `logInteractionEvent(` that exists only in a comment or a `m_presentNothingDropCount.fetch_add(` that exists only inside a string literal no longer satisfies the return check. `is_present_nothing` still matches the comment-only-masked view so the marker's own string literal stays visible. No product code touched.
- Prior CoS Improvement (0 GitHub reviews) **not** addressed. Round-4 depth-exact + counter lockstep Improvement remains satisfied; round 5 closes the residual comment/string false-positive class that round 4 left open.
- Round 4 substance retained: return tripwire tracks only text at each return's own brace depth; present_nothing exit requires `fetch_add(` lockstep; in-memory mutation tests for sibling-block / missing counter / untraced return.
- Round 3 substance retained: dead tex-nr carve-out removed from `mainWindowAllowsGpuAmazeTexturePresentation` (gate is `usesAmazeDebayer && envRequested && gpuWidgetViewportInstalled` only); `gpuWidgetViewportInstalled` refreshed symmetrically at `drawFrameReady` copy site; summary placeholder test proves `%64` maps to PresentNothingDrops `.arg()`.
- Round 1/2 substance retained: `gpuWidgetViewportInstalled` field populated at every policy-builder from `GpuDisplayViewport::isInstalledOn`; formerly silent drop emits `draw_frame_ready.present_nothing` + counter exported as `present_nothing_drops=%64` in `playback_smoke.summary`.
- Owner laptop evidence (diagnostic, earlier rounds): scale 4 GPU 0→70/72 presented; scale 1 begin==end; `present_nothing_drops` 0 after fix. Scale-1 window AMaZE texture correctly refuses without widget (offscreen/presentImage path); tex-nr recon remains separate via `mainWindowAllowsGpuPlaybackReconTexturePresentation`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Chat silent on this push: verdict unchanged and prior CoS Improvements not clearly addressed.