schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 167
head: 100a807b6a966efbd6115cc06c07760594669942
reviewed_utc: 2026-09-25T18:24:10Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip 100a807b (+544/−2, 6 files, 1 commit; ahead 1 / behind 0): Protected Check Route SUCCESS; Batch Compile, Repo Hygiene (ubuntu+windows), Windows GUI Pilot, Windows Product Oracles, Factory Bridge Regressions, CodeQL Analyze (actions/c-cpp/python) all IN_PROGRESS/QUEUED (Tests 36173088645, Factory Bridge 36173088597, CodeQL 36173086451). MERGEABLE; mergeStateStatus BLOCKED. 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

- `Start-AttrCudaDisplayWake` (`AttrCudaArtifacts.psm1`): after the SendInput nudge, consider a short bounded settle (e.g. 200–500 ms) before `Get-AttrCudaScreensaverRunning` for `screensaverRunningAfter`. Screensaver teardown can lag the input event; without a settle, evidence can still show `before=true, after=true` on a successful wake and muddy DISPLAY_ASLEEP diagnosis.

## Notes

- Owner ask (2026-09-25): wake blank screensaver so bachelor measured legs stop ending `DISPLAY_ASLEEP` (PresentMon presented but displayed 0).
- Job side: `Start-AttrCudaDisplayWake` (1-pixel SendInput nudge + `SetThreadExecutionState(ES_CONTINUOUS|ES_SYSTEM_REQUIRED|ES_DISPLAY_REQUIRED)`) before package deploy / MLVApp launch; `Stop-AttrCudaDisplayWake` in leg `finally`; `displayWake` evidence on all 10 summary/evidence write sites including DISPLAY_ASLEEP. Win32 calls non-throwing (errors recorded). No persistent screensaver/power setting changes.
- App side: `setPlaybackDisplayRequiredExecutionState` once on play start, release on stop and `closeEvent` — ordinary media-player behaviour, not gated on telemetry; `playback_smoke.display_required` logged once per session. Job holds SYSTEM+DISPLAY; app holds DISPLAY only (roles match).
- Tests: 8 Python behaviour/ordering (+ native-failure recorded not thrown; `_PwshCase` Windows-skips so ubuntu Repo Hygiene stays clean); 6 console wiring tests pin 3 call sites, order vs smoke telemetry, never-per-frame, Windows-only helper. Mutation-checked. Author: attr-3 318/0; console 356 with 1 pre-existing unrelated AvxParity env failure. No bachelor legs this round.
- Files: `platform/qt/MainWindow.cpp`, `tests/console/console_tests.pro`, `test_playback_display_wake_wiring.cpp`, `tools/profiling/bachelor/AttrCudaArtifacts.psm1`, `playback-attr-3-cuda-job.ps1`, `tools/repo_hygiene/test_playback_attr_3_cuda_behaviour.py`.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound; tip required checks / branch protection still settling. Bachelor confirmation that DISPLAY_ASLEEP clears is post-merge evidence, not a merge blocker.
