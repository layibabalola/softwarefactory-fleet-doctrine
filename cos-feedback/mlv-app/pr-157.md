schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 157
head: fae8d2ee5f899409abacc02018790be577c9dc90
reviewed_utc: 2026-09-25T10:19:46Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Tip fae8d2ee (+2077/−2, 12 files, 2 commits; ahead 2 / behind 0): Tests run 36123267804 Protected Check Route SUCCESS; Batch Compile, Repo Hygiene ubuntu/windows, Windows GUI Pilot, Windows Product Oracles IN_PROGRESS; Factory Bridge 36123267824 IN_PROGRESS; CodeQL run 36123266457 Analyze (python) SUCCESS, Analyze (actions)/(c-cpp) IN_PROGRESS, suite CodeQL NEUTRAL. MERGEABLE; mergeStateStatus BLOCKED (protection waiting on required checks). 0 GitHub reviews; not draft.

## Blockers

(none)

## Improvements

(none in this PR's owned scope — Soft Improvements from parked #155 rounds that touched absent files, e.g. ConvertTo-AttrCudaGeneratorPsLiteral dup, are deferred to their owning surfaces / CUDA-ATTRIBUTION-WIRING-1.)

## Notes

- Split from parked #155 per hub ruling SPLIT-SEVERITY-AND-NEXT-20260925. Pure policy-only half: six self-contained headers taken verbatim from #155 tip `3e72f190` (`PlaybackFramePopulationPolicy.h`, `PlaybackPresentedFrameIdentityTracker.h`, `PlaybackDropFrameAdvancePolicy.h`, `PlaybackAchievedScalePolicy.h`, `PlaybackLookaheadLoopPositionPolicy.h`, `GpuTexturePresentAvailabilityPolicy.h`) plus matching `tests/console/test_*.cpp` and `console_tests.pro` entries. No MainWindow / app / `.ui` wiring (MainWindow appears only in doc comments); wiring is later CUDA-ATTRIBUTION-WIRING-1.
- Behaviour fix both #155 reviewers named: `computeSourceFramePopulation` now sets `offeredSourceFrames = floor(rawOffered + 1e-6) + startOccurrenceOffered` (credit after floor). Replaces round-half-up `cast(rawOffered + 0.5)` which double-counted against the drop-frame start-occurrence credit when frac(E) >= 0.5, fabricating a phantom never-requested frame. Spot-check: fable repro `fencepostPopulation(10, 34, 24.6, 1)` → offered 25 == touched span, never_requested 0, partitionSound true.
- New fencepost tests in `test_playback_frame_population_policy.cpp`: `CUDA_POLICY1_FableRepro_FloorNotRoundHalfUpMatchesTouchedSpan`, `CUDA_POLICY1_FracBelowHalfDropFrameStaysSound` (non-regression control), `CUDA_POLICY1_NormalModeIntegralOfferedUnaffectedByFix`, `CUDA_POLICY1_FloatDriftBelowIntegralOfferedIsNotUndercounted` (guards bare floor without epsilon). Prior SolR12_* fencepost suite retained.
- Author local evidence (PR body): `console_tests --check-golden` 314/0 fail; `PlaybackFramePopulationPolicy.*` 32/32. CoS did not re-run (Windows factory; review via gh only).
- Commits: `6e6a6ff9` take six headers + tests from parked tip; `fae8d2ee` floor fix + POLICY1 tests. Diff limited to the 12 owned paths above.
- CoS via gh (no Cloud Agent). No GitHub PR review comment (bus only). Verdict **merge-when-ci-green**: content sound; only tip required checks / branch protection blocking.
