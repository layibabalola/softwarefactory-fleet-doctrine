schema: cos-feedback.v1
project: mlv-app
repo: layibabalola/MLV-App
pr: 107
head: ff57cc8c42bbef7d1f99868c3dd2b92ec4d90e30
reviewed_utc: 2026-09-10T17:24:31Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: ubuntu Repo Hygiene + Factory Bridge + Windows GUI Pilot + CodeQL pass on head ff57cc8; Windows Product Oracles, Batch Compile, Repo Hygiene (windows) still pending (Tests run 34507587209)

## Blockers

- **CI still settling** — wait for `Repo Hygiene Python (windows-latest)`, `Batch Compile`, and `Windows Product Oracles` on Actions run 34507587209. Do not merge while any required check is pending/red; if red, fix the failing suite and re-push.
- **Process: Sol review** — 0 GitHub reviews on this head. PR body requires Sol review of the hashed composer + hook controls before the merge sequence.
- **Process: non-shrinking execution-control receipt rewrite** — PR body makes this non-negotiable before merge (hashed composer + hook paths). Land the receipt rewrite after Sol, not as a drive-by.

## Improvements

- `tools/coordination/get_doctrine_brief.py` (cos-feedback load): the outer `except Exception: cos_files = []` is intentionally fail-soft, but it also swallows unexpected bugs (e.g. TypeError in fixture mode). Prefer catching the same `RuntimeError` / API failures you already use for list/fetch, and let programmer errors surface in tests.
- `list_cos_feedback_names` / fixture list: good digit-only `pr-*.md` filter and newest-first sort. Consider a unit that asserts non-`pr-` files and `pr-abc.md` are ignored (README already covered by the present-path test).
- Optional: one hosted/live or mocked test that Contents API 404 on `cos-feedback/mlv-app` omits the section (mirror of fixture-absent). Fixture coverage already pins the contract for offline.

## Notes

- This push (`ff57cc8` on `feat/doctrine-consumer-20260909`) adds fail-soft `cos-feedback/mlv-app/pr-*.md` into `Get-DoctrineBrief` / `get_doctrine_brief.py`: missing dir/files omit the section (do not `REFUSED`); labels **data only / zero authority**; caps 8 files × 6000 chars. Docs: `agents/doctrine-consumer.md` + CLAUDE Document Map line. Tests: present + absent fixture paths.
- Prior commit `71d1581` still carries the rest of the PR: fail-closed doctrine brief (RULINGS / MLV candidates / specs), composer `{{DOCTRINE_BRIEF}}` inject, NA-11 deny on `tools/agent-bridge/**` Write/Edit/shell, SoT pointer `agents/agent-bridge-source-of-truth.md` (companion #106 still open). Does not modify agent-bridge product code.
- Doctrine #58 (`cos-feedback/` surface) is **merged** to bus master — this file lands under `cos-feedback/mlv-app/` on master. Doctrine #56 remains **candidate** (ZERO_AUTHORITY); landing #107 does not ADOPT it.
- Law 1 check on this head: brief labels CoS feedback as data / zero authority; hubs surface Blockers/Improvements; lanes must not execute. Circular include of this `pr-107.md` into future briefs is expected once published — truncation + data-only label keep it bounded.
- Full PR: 12 files, +916/−10 vs master. MERGEABLE but mergeStateStatus BLOCKED (checks + process). No GitHub PR comment posted by CoS. Cloud agent not used (review-only via `gh`; Grok meter is analysis budget, not factory floor).