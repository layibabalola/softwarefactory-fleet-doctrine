schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 65
head: 6ed2e00d06f113e92618952957d30673cd86cbea
reviewed_utc: 2026-09-15T01:17:00Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift pass; factory contracts and typecheck/lint/build/test still pending on head 6ed2e00d (run 34916589139); author reports local pwsh scripts/ci.ps1 GREEN exit 0 in 775s on this tip

## Blockers

(none for content) — scope matches staging-only claim. Remaining hosted checks are the merge gate; land when they go green (or post a matching local gate receipt if CI flakes).

## Improvements

- At transport time, re-check every quote and REPLACES anchor against current bus master blobs (filing pins kernel `8504a6dc`, profile `420f71b9`, and `bus_master` `6b5759fd`).
- Keep `verify-kernel-filing.mjs` runnable for row VERIFY (script lives in the PR body details block; it is not tracked in the tree).
- After land, transport still needs a separately reviewed high-risk point-9 amendment (after #64 / GUARD-DOCTRINE-PORT) admitting `adjudications/factory-kernel/adversarialllm.md` — correctly out of scope here.

## Notes

- Diff vs `master`: +123/−0 on three paths only — `factory/doctrine/adjudications/factory-kernel/adversarialllm.md` (K1–K12, §4/§6, eight `P:code` lines, Instance map, Untested), `factory/doctrine/kernel-block-2026-09-14.md` (`KERNEL: DOGFOOD-PENDING`), and one `factory/queue.jsonl` append (`KERNEL-DOGFOOD`, order 246, family claude, no dependsOn).
- Not high-risk under point 8: no process-authority document, guard, CI, hook, or state-module change; no bus byte written.
- Spot-checks: all twelve K-lines present once; FRICTION/BREAK lines carry REPLACES; kernel blob `8504a6dc` resolves on the bus; queue order 246 present on tip.
- Filing health `assurance=UNSATISFIED operability=PRESSURED`. Subject findings (#47 missing re-readable Claude key / base-mismatched codex receipt; #60 Claude reissue; K7 delivery-without-acceptance on #42) are dogfood evidence, not blockers for this staging PR.
- Related open work: #64 still owns point-9 text; this PR deliberately does not amend it.
- No GitHub PR comment posted. Review via `gh` only (Cloud Agents not on plan).
