schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 93
head: 42f9e72dc2e831f695657bc3014b6000a25cc5fc
reviewed_utc: 2026-09-17T00:48:08Z
event: pr-opened
verdict: not-merge-ready
ci_summary: docs drift (delta gate) PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head 42f9e72d (Actions run 35167896423); local claim 77 focused schedule/native/transaction tests + Windows companion go test ./... green; full hosted CI pending

## Blockers

- **Regression: ferry ingest drops non-scheduled campaigns** — `adversarialllm/src/entrypoints/background.ts` `handleFerriedRunResult` now returns early when `campaign.scheduledRun` is absent (`FERRY_INGEST_REJECTED`). Prior P1 everyday ferry path applied `RUN_RESULT` to any matching local campaign. If dedicated/unobtrusive non-scheduled runs still arrive here, results are silently dropped. Fix: keep `settleFerriedTerminal` / publish-after-commit only for scheduled occurrences; restore a non-scheduled apply path (or shared settle without occurrence identity) when `scheduledRun` is missing. Add a unit/integration case that a campaign without `scheduledRun` still ingests a valid ferry frame.
- **CI: `factory contracts`** — IN_PROGRESS on head `42f9e72dc2e831f695657bc3014b6000a25cc5fc` (Actions run 35167896423). Wait for green; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule.

## Improvements

- `mayLaunch` in `background.ts` treats `alertOnFinish: true` as `unsupported` (alongside battery/quiet-hours/skipUnauthed guards). Confirm that is intentional for this packet (no host notification/preflight yet) vs. a local-chrome notification that should not block launch; if the latter, allow launch and keep notification gating in `shouldNotifyFerriedCampaign` only.
- `settleFerriedTerminal` conflict list is append-only with no bound — repeated distinct conflicting redeliveries can grow `ferryTerminal.conflicts` without limit. Consider a capped ring or count+last-N if adversarial replay is in threat model.
- Land dual-family adversarial D2 on tip `42f9e72d` before merge (0 GitHub reviews yet). Author is layibabalola / implementer Codex per PR body.
- PR body says focused suites 77 passed; evidence text also cites 78 across six suites — align the count in the PR description or evidence packet so controllers do not chase a phantom missing test.

## Notes

- Scope: ph-P2-R offline scheduling delivery + immutable ferry terminal repair. +1286/−37 across 26 files; 16 commits ahead of master, behind_by 0. Branch `codex/ph-P2-R`. MERGEABLE / UNSTABLE while checks run.
- Core fix looks sound: `settleFerriedTerminal` does read/compare/conditional-write in one strict IndexedDB transaction; first terminal wins; identical redelivery is no-write `identical-duplicate`; conflicts append audit without changing authoritative `reportModel`/status; publish only on `applied` via `settleAndPublishScheduledFerry`. Covered by `db-ferry-terminal.test.ts` (ordering, concurrent identical/conflict, reopen equality, reject unknown/mismatch) plus large `schedule-journey.test.ts`.
- Scheduling remains default-off (`SCHEDULE_EXECUTION_SETTING_KEY`, sf-flags `scheduleEditor`, paired substrate). Separate schedules DB; at-most-once claims (missed run after crash is acknowledged). Not a live-provider or signed-deploy claim.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/adversarialllm/pr-93.md`.