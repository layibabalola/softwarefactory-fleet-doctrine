schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 93
head: e086d81e656ec1ac8101599507c248b959a748ce
reviewed_utc: 2026-09-17T01:57:39Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: docs drift (delta gate) PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head e086d81e (Actions run 35172472143); local claim 77 focused schedule/native/transaction tests + Windows companion go test ./... green; full hosted CI pending

## Blockers

- **Regression: ferry ingest drops non-scheduled campaigns** — still present on tip `e086d81e`. `adversarialllm/src/entrypoints/background.ts` `handleFerriedRunResult` returns early when `campaign.scheduledRun` is absent (`FERRY_INGEST_REJECTED`). Prior P1 everyday ferry path applied `RUN_RESULT` to any matching local campaign. Fix: keep `settleFerriedTerminal` / publish-after-commit only for scheduled occurrences; restore a non-scheduled apply path (or shared settle without occurrence identity) when `scheduledRun` is missing. Add a unit/integration case that a campaign without `scheduledRun` still ingests a valid ferry frame.
- **CI: `factory contracts`** — IN_PROGRESS on head `e086d81e656ec1ac8101599507c248b959a748ce` (Actions run 35172472143). Wait for green; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule.

## Improvements

- `mayLaunch` in `background.ts` still treats `alertOnFinish: true` as `unsupported` (alongside battery/quiet-hours/skipUnauthed guards). Confirm intentional for this packet vs. allow launch and keep notification gating in `shouldNotifyFerriedCampaign` only. **Unaddressed** vs prior CoS note.
- Land dual-family adversarial D2 on tip `e086d81e` before merge (0 GitHub reviews yet). Author is layibabalola / implementer Codex per PR body.
- PR body says focused suites 77 passed; earlier evidence cited 78 across six suites — align the count if still divergent.

## Notes

- Push since prior CoS (`42f9e72d` → `e086d81e`): `fix(scheduling): reject malformed ferry terminals` + `test(scheduling): preserve native wire compatibility`. Delta touches `db.ts`, `types.ts`, `db-ferry-terminal.test.ts`, and docs/28 inventory.
- **Prior Improvement addressed:** `settleFerriedTerminal` now caps durable conflicts at `MAX_FERRY_TERMINAL_CONFLICTS` (16) and increments `droppedConflicts` past the cap; covered by new bound + malformed-input + already-terminal + failed-put abort tests in `db-ferry-terminal.test.ts`. `validTerminalInput` rejects bad hash / nonterminal outcome / incomplete report / identity mismatch / invalid timestamp without writing.
- Core immutable-terminal design unchanged and still sound: first terminal wins in one strict IndexedDB transaction; identical redelivery is no-write; conflicts audit without changing authority.
- Scheduling remains default-off. Behind_by 0 vs master; MERGEABLE / UNSTABLE while checks run. Branch `codex/ph-P2-R`. +1355/−37 across 26 files; 18 commits.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: refreshed on `master` at `cos-feedback/adversarialllm/pr-93.md`.
