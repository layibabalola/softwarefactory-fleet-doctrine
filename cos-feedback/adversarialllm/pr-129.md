schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 129
head: 580d8ff7b1b0c81f73cc77906f1927c42cdf7197
reviewed_utc: 2026-09-25T00:38:15Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36078382340 — all three required jobs IN_PROGRESS (docs drift, factory contracts, typecheck/lint/build/test); mergeable true / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `580d8ff7`.** Actions run 36078382340 still has docs drift (delta gate), factory contracts, and typecheck/lint/build/test IN_PROGRESS. Do not merge on an incomplete check set.
- **Independent second-family review still pending.** PR body flags exact-head/base reviews as not yet delivered. Diff is test-only mock path budget (+42/−5, 2 files) so risk is bounded, but CoS does not substitute for that adjudication if the row class requires it.

## Notes

- **What changed:** New `assertMeasuredFixtureProfilePathBudget` in `adversarialllm/tests/mock/fixture-page.ts` walks the real profile tree after the first browser context closes and fails if any file path length exceeds the modelled IndexedDB longest-file budget (`INDEXED_DB_DIRECTORY_SUFFIX` + `INDEXED_DB_LONGEST_FILE`). Wired into the offline campaign test after `assertPersistedIndexedDb`. Predicted preflight error text now says "maximum supported checkout root for the modelled IndexedDB path". New focused test covers bounded pass + over-budget reject. Symlinks skipped during walk.
- **Scope:** vs master `b2636f43`, +42/−5, 2 files, 1 commit on `feat/CI-PREFLIGHT-MEASURED`. Ahead 1 / behind 0. Row token `CI-PREFLIGHT-MEASURED`. No production/runtime path changes.
- **Author claims (not re-run here):** focused path-budget tests + scoped ESLint/diff green; full root CI GREEN on 54-char and 108-char Windows checkouts at this exact head; first long-root attempt failed an existing cancellation-readiness 5s poll (not the new measured assertion), then bounded full-gate rerun passed unchanged.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.