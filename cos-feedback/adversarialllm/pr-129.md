schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 129
head: 695cafa8014785bc110c313852dd338c0f686cac
reviewed_utc: 2026-09-25T08:30:19Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 36113171463 — docs drift QUEUED, typecheck/lint/build/test QUEUED, factory contracts IN_PROGRESS; prior reviewed tip cfc55733 run 36084695457 SUCCESS; mergeable true / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `695cafa8`.** Actions run 36113171463 still has docs drift (delta gate) and typecheck/lint/build/test QUEUED; factory contracts IN_PROGRESS. Do not merge on an incomplete check set. Prior reviewed tip `cfc55733` already cleared the same three required jobs (run 36084695457 SUCCESS).
- **Independent second-family review still pending.** PR body still requires current-head hosted checks plus two validated independent round-two reviews before D2 merge. Diff remains test-only mock path budget (+53/−5, 2 files vs master) so risk is bounded, but CoS does not substitute for that adjudication if the row class requires it.

## Notes

- **Round 2 product delta since prior CoS `cfc55733`:** commit `7ff9c0fd` ("prove directory links stay outside measurement") adds focused test `measured profile budget does not traverse directory links outside the profile` in `adversarialllm/tests/mock/two-provider-two-round.test.ts` (+12/−1 vs prior tip). Tip `695cafa8` is merge of remote master `2074c90b` (#137 CI-SELF-HOSTED-LINUX) onto that; owned mock path-budget code unchanged by the merge.
- **What the head does:** `assertMeasuredFixtureProfilePathBudget` in `adversarialllm/tests/mock/fixture-page.ts` walks the real profile tree (skips `isSymbolicLink` entries), fails when any file path length exceeds the modelled IndexedDB longest-file budget, and is wired into the offline campaign test after `assertPersistedIndexedDb`. Predicted preflight error text qualifies "maximum supported checkout root for the modelled IndexedDB path". Focused tests cover bounded pass, over-budget reject, and dir-link/junction non-traversal.
- **Scope:** vs master `2074c90b`, +53/−5, 2 files, 4 commits on `feat/CI-PREFLIGHT-MEASURED`. Ahead after merge (merge commits + owned). Row token `CI-PREFLIGHT-MEASURED`. No production/runtime path changes.
- **Author claims (not re-run here; claimed for tip `695cafa8`):** full 54-char short-root and 108-char long-root CI GREEN (supervisor exit 0); evidence under `.factory-local` as `ci-short-695cafa8-a1.*` / `ci-long-695cafa8-a1.*`. Earlier long-root stalls adjudicated as Playwright route-registration, not the new measured assertion.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted. Chat quiet per later-push rule (verdict unchanged; Improvements not addressed).
