schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 82
head: 583be134e34192204612457a30e34e06ca7a5c35
reviewed_utc: 2026-09-16T07:24:18Z
event: pr-opened
verdict: not-merge-ready
ci_summary: tip 583be134e341 — docs drift (delta gate) PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on Actions run 35068294678; mergeable=true; 0 GitHub reviews on tip

## Blockers

- **CI: hosted jobs not settled green on this tip** — `docs drift (delta gate)` already PASS on head `583be134e34192204612457a30e34e06ca7a5c35`. Wait for `factory contracts` and `typecheck / lint / build / test` on the same Actions run `35068294678`. Do not merge while either is pending or red; if red, fix and re-push. Author claims local `pwsh scripts/ci.ps1 -BaseSha e1b12b8a13bd8df86a3ff1522ffc6c1cdce85052` GREEN (18/18, 769s) on byte-identical blobs — hosted must still settle.
- **Process: independent dual-family D2 on this exact head** — 0 GitHub PR reviews on tip `583be134`. After hosted CI settles green, land fresh Sol/Codex + Claude/Sonnet on this tip. Non-author controller turns the key. Reviewers should verify the inventory row correction against `round-prompt-map.ts:230` (plan presence), not against prior docs/28 attribution.

## Improvements

- `adversarialllm/scripts/inventory.mjs` `flag()` helper still accepts a hand-authored pairing without checking that the anchor file references the flag symbol. That is the measured root of this false attribution (and of the prior #52 docstring false model). A follow-up that fails closed when `flag(...)` is supplied but the anchor path has zero references to that constant would prevent the next silent publish of a false derived fact. Out of scope for this docs/inventory row fix; worth a tracked inventory hardening card.
- Abandoned sibling branch `feat/INV-FLAG-FIX` carries identical bytes with no PR (D11-R blocks non-fast-forward rewrite). After this PR merges, delete or clearly mark that branch inert so a later session does not open a duplicate/CONFLICT row from it.

## Notes

- Scoped review of tip `583be134e34192204612457a30e34e06ca7a5c35` vs `master`: 2 files, +7/−2 (`adversarialllm/scripts/inventory.mjs`, `adversarialllm/docs/28_IMPLEMENTATION_PROGRESS.md`). Docs/inventory description only — no product control-flow change.
- **Attribution claim (CoS spot-checked):** `round-prompt-map.ts` imports no `M1_ROUND_ENGINE_*`; branches at `:230` on `!plan || plan.rounds.length === 0` (legacy) else plan/engine path. `M1_ROUND_ENGINE_ENABLED` JSDoc in `constants.ts` is the durable round-feed writer rollout gate; orchestrator uses it for feed R/W, abort seam, and resume/checkpoint — matching why adjacent `saveRoundFeed` / `getRoundFeed` inventory rows keep the flag and this routing row must not. Diff changes exactly one inventory Flag cell (`M1_ROUND_ENGINE_ENABLED=false` → `none`) plus gap text; docs/28 block regenerated from inventory.mjs, not hand-edited.
- Directly repairs the false model that poisoned #52's earlier docstring and review framing (flag=false misread as "legacy iteration path"). Keep adjacent feed rows flagged; only the `buildRoundPromptMap` row was wrong.
- MERGEABLE tip on `feat/P5-FLAG-ATTRIBUTION` → `master`. Verdict **not-merge-ready** until hosted CI is green and D2 lands on this tip.
- CoS review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (small inventory/docs tip; analysis budget). Doctrine bus: published on `master` at `cos-feedback/adversarialllm/pr-82.md`.