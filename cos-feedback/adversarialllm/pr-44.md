schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 44
head: 800954770794dd291b38234501040a410d8249eb
reviewed_utc: 2026-09-10T17:21:58Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; typecheck/lint/build/test + factory contracts still pending on run 34507580537 (head 8009547)

## Blockers

- **CI: `typecheck / lint / build / test`** — still pending on head `800954770794dd291b38234501040a410d8249eb` (Actions run 34507580537). Wait for green before merge; if red, fix the failing suite and re-push.
- **CI: `factory contracts`** — still pending on the same run. Same wait/fix rule.
- **Process: independent D2 review** — 0 GitHub reviews yet; PR body requires Sol/dual-family review on the current head before merge. Land those reviews (or an explicit owner waiver outside this file) after hosted CI settles.

## Improvements

- `factory/get-doctrine-brief.mjs`: no offline fixture test landed with this PR despite `ADVLLM_DOCTRINE_FIXTURE_ROOT` / `--fixture-root` support. Add a small node test (or extend `factory/ci-contract.test.mjs`) that builds a mini bus tree (RULINGS.md, specs/adversarialllm.md, ruling-candidates/, optional cos-feedback/adversarialllm/pr-1.md) and asserts exit 0, Law-1 labels, fail-soft omit when cos dir missing, and exit 2 / `REFUSED:` when sealed files are absent.
- `factory/get-doctrine-brief.mjs` (`listCosFeedbackNames` / load loop): Contents list is uncapped before `slice(0, COS_FEEDBACK_MAX_FILES)`. Fine at current board size; if the dir grows, prefer server-side `per_page` + stop after max, or filter open-PR tips only so stale closed `pr-*.md` do not crowd the brief.
- `agents/doctrine-consumer.md` adoption checklist still open on controller brief injection and hub `doctrine-sync check` — expected follow-ups; track as separate rows so this PR stays thin.

## Notes

- Later-push delta vs prior tip `4a6d897`: include `cos-feedback/adversarialllm/pr-*.md` via Contents API (fail soft if missing), label CoS feedback as data only / zero authority, update consumer checklist + AGENTS/CLAUDE pointers. +91/−0 in `get-doctrine-brief.mjs` plus doc nits.
- Full PR scope (6 files, +394/−0): fail-closed read-only brief (`factory/get-doctrine-brief.mjs`), `agents/doctrine-consumer.md`, conductor/implementer prompt injection laws, CLAUDE/AGENTS pointers. Modeled on MLV-App #107 / doctrine #57. Does not re-add fleet-bus write roots; does not ratify the fleet candidate.
- Safety posture on review: sealed surfaces (RULINGS, project spec, candidate list) remain fail-closed (`REFUSED:` / exit 2); CoS feedback is correctly fail-soft (omit section / skip bad files). `execFileSync('gh', ['api', …])` avoids shell interpolation. Brief text labels candidates and CoS notes as zero authority / Law 1 data.
- `docs drift (delta gate)` already PASS on this tip.
- Cursor Cloud Agents not used for this pass (review-only CoS via `gh`; Grok meter is analysis, not factory floor). No GitHub PR comment posted.
