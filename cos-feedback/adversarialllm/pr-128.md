schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 128
head: 25552d57db706e3d28d1a12ed02ce148b3fde8e5
reviewed_utc: 2026-09-24T19:14:58Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 36046693117 — all three required jobs IN_PROGRESS (factory contracts, docs drift, typecheck/lint/build/test); prior tip 7bd76662 run 36044513438 failed on factory contracts / CI script contract tests; MERGEABLE/UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `25552d57`.** Actions run 36046693117 still has factory contracts, docs drift, and typecheck/lint/build/test IN_PROGRESS. Do not merge on an incomplete check set. Prior tip `7bd76662` failed factory contracts at step "CI script contract tests" (run 36044513438); this push is the CRLF-boundary repair.
- **Two-leg / D2 still open.** Row `CI-SELF-HOSTED-WIN` is `family: claude` and touches `.github/workflows/ci.yml` (high-risk class). Needs an independent second-family leg plus a literal APPROVE from a non-author before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.
- **Local gate not recorded on this head.** PR body says full `pwsh scripts/ci.ps1` was not run because `Global\AdvLLM-ci-run` was held by PR #126's gate on the implementer host. Controller should run the root local gate on `25552d57` before merge (body already flags this).
- **Open interaction with #107 (`CI-COST-1R`).** That row's acceptance still pins bare `windows-latest` on factory contracts. If #128 merges first, #107's VERIFY needs a one-line amendment to the `CI_RUNS_ON` expression.

## Notes

- **Round 2 vs prior CoS head `7bd76662` (+20/−8, 1 file):** `factory/ci-contract.test.mjs` only. `assertFactoryRunsOn` now takes the full workflow YAML, CRLF-normalizes to LF, then cuts the factory-contracts job on the `\n\n  ci:` boundary (Windows `core.autocrlf=true` otherwise has no `\n\n  ci:` match). Call sites pass full `yaml` instead of a pre-sliced job. Negative-test helper builds a mini workflow with optional CRLF EOL and adds CRLF + missing-trailing-`ci` cases. Intent: fix the prior tip's factory-contracts contract-test failure on hosted Windows.
- **PR vs master still 3 files, +45/−3, 2 commits:** `.github/workflows/ci.yml` — only `factory-contracts` `runs-on` moves from bare `windows-latest` to `${{ vars.CI_RUNS_ON != '' && fromJSON(vars.CI_RUNS_ON) || 'windows-latest' }}` (Cloudvore/Conjugal pattern). `factory/ci-contract.test.mjs` — exact-byte `CI_RUNS_ON` pin + negatives; `ci`/`docs-drift` stay `ubuntu-latest`. `factory/queue.jsonl` — self-filed row `CI-SELF-HOSTED-WIN` order 320 family claude.
- **Inert until owner action.** Unset/empty `CI_RUNS_ON` keeps hosted `windows-latest`. Setting the repo variable and registering one `ultra-magnus` runner instance are owner-only post-merge (not in this PR). Body correctly warns: one runner instance per host for this repo because of `Global\AdvLLM-ci-gate` / `Global\AdvLLM-ci-run` mutexes; cancel-in-progress can leave an abandoned mutex once on a persistent host.
- **Governance / filing risk.** Outside-conductor Claude session; body asks controller to apply `impl:claude` from body/`Co-Authored-By`. Self-files under operative amendment point 1 as a process change. If controller applies the narrow `CONDUCTOR-PREFLIGHT-TIMEOUT-FILE` reading, split into a queue-only FILE PR first (workflow/test bytes unchanged). Linux `ci` / `docs-drift` stay hosted by design.
- **Diff review:** Round-2 CRLF cut is the right fix for the prior Windows contract failure mode; expression and pins otherwise unchanged vs prior CoS note. Mutation-control and line-ending claims in the body were not re-executed here. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted. Verdict unchanged (`merge-when-ci-green`); chat skip per later-push rule (no verdict change; dual-leg / local-gate Improvements still open).
