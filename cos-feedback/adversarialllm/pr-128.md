schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 128
head: e2ae9226e35f6133885b51b8de02b96b010faf3c
reviewed_utc: 2026-09-25T00:52:06Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 36079424735 — all three required jobs IN_PROGRESS (factory contracts, docs drift, typecheck/lint/build/test); prior tip 25552d57 run 36046693117 SUCCESS; MERGEABLE/UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `e2ae9226`.** Actions run 36079424735 still has factory contracts, docs drift, and typecheck/lint/build/test IN_PROGRESS. Do not merge on an incomplete check set. Prior tip `25552d57` (run 36046693117) was SUCCESS for all three required jobs; this push is a re-head merge onto master `24bc6f16` with no workflow/test byte change.
- **Two-leg / D2 still open.** Row `CI-SELF-HOSTED-WIN` is `family: claude` and touches `.github/workflows/ci.yml` (high-risk class). Needs an independent second-family leg plus a literal APPROVE from a non-author before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.
- **Local gate not recorded on this head.** PR body (and prior CoS notes) say full `pwsh scripts/ci.ps1` was not run on `25552d57` because `Global\AdvLLM-ci-run` was held by PR #126's gate on the implementer host. Re-head body does not claim a new local-gate run on `e2ae9226`. Controller should run the root local gate on this tip before merge.
- **Open interaction with #107 (`CI-COST-1R`).** That row's acceptance still pins bare `windows-latest` on factory contracts. If #128 merges first, #107's VERIFY needs a one-line amendment to the `CI_RUNS_ON` expression.

## Notes

- **Round 3 vs prior CoS head `25552d57` (queue-only vs tip):** merge commit `e2ae9226` parents `25552d57` + master `24bc6f16` (`git merge --no-ff`, never rebase). Compare `25552d57...e2ae9226`: only `factory/queue.jsonl` (+11/−1) — absorbs master's interim queue rows (274–275 CI-PREFLIGHT-MEASURED/FILE, 301–303/305 EA-CUT, 311–314 GATE-FIX-FILE pack including GATE-ATTEMPT-SUPERSEDE `supersededBy`) then keeps this PR's `CI-SELF-HOSTED-WIN` order 320 as the sole tip append. `git diff 25552d57 HEAD -- .github/workflows/ci.yml factory/ci-contract.test.mjs` empty per body; PR vs master still 3 files +45/−3 (ci.yml 1/1, ci-contract.test.mjs 43/2, queue.jsonl 1/0 net vs master tip).
- **PR vs master still the CI_RUNS_ON routing change:** `.github/workflows/ci.yml` — only `factory-contracts` `runs-on` moves from bare `windows-latest` to `${{ vars.CI_RUNS_ON != '' && fromJSON(vars.CI_RUNS_ON) || 'windows-latest' }}` (Cloudvore/Conjugal pattern). `factory/ci-contract.test.mjs` — CRLF-normalize + `\n\n  ci:` job cut in `assertFactoryRunsOn`; exact-byte pin + negatives; `ci`/`docs-drift` stay `ubuntu-latest`. Self-filed row order 320 family claude.
- **Inert until owner action.** Unset/empty `CI_RUNS_ON` keeps hosted `windows-latest`. Setting the repo variable and registering one `ultra-magnus` runner instance remain owner-only post-merge. Body correctly warns: one runner instance per host for this repo because of `Global\AdvLLM-ci-gate` / `Global\AdvLLM-ci-run` mutexes; cancel-in-progress can leave an abandoned mutex once on a persistent host.
- **Governance / filing risk.** Outside-conductor Claude session; body asks controller to apply `impl:claude` from body/`Co-Authored-By`. Self-files under operative amendment point 1 as a process change. If controller applies the narrow `CONDUCTOR-PREFLIGHT-TIMEOUT-FILE` reading, split into a queue-only FILE PR first (workflow/test bytes unchanged). Linux `ci` / `docs-drift` stay hosted by design.
- **Diff review:** Re-head is queue hygiene only; protected workflow/test bytes unchanged from prior CoS-green tip. Order 320 still unique vs master max 314. Mutation-control / line-ending claims from earlier body not re-executed here. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted. Verdict unchanged (`merge-when-ci-green`); chat skip per later-push rule (no verdict change; prior Improvements wait-CI / D2 / local-gate / #107 still open).
