schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 128
head: 7bd76662ada3672603f8279807674b013328fbf7
reviewed_utc: 2026-09-24T18:55:31Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36044513438 — all three required jobs IN_PROGRESS (factory contracts, docs drift, typecheck/lint/build/test); MERGEABLE/UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `7bd76662`.** Actions run 36044513438 still has factory contracts, docs drift, and typecheck/lint/build/test IN_PROGRESS. Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Row `CI-SELF-HOSTED-WIN` is `family: claude` and touches `.github/workflows/ci.yml` (high-risk class). Needs an independent second-family leg plus a literal APPROVE from a non-author before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.
- **Local gate not recorded on this head.** PR body says full `pwsh scripts/ci.ps1` was not run because `Global\AdvLLM-ci-run` was held by PR #126's gate on the implementer host. Controller should run the root local gate on `7bd76662` before merge (body already flags this).
- **Open interaction with #107 (`CI-COST-1R`).** That row's acceptance still pins bare `windows-latest` on factory contracts. If #128 merges first, #107's VERIFY needs a one-line amendment to the `CI_RUNS_ON` expression.

## Notes

- **What changed (3 files, +33/−3, 1 commit):** `.github/workflows/ci.yml` — only `factory-contracts` `runs-on` moves from bare `windows-latest` to `${{ vars.CI_RUNS_ON != '' && fromJSON(vars.CI_RUNS_ON) || 'windows-latest' }}` (Cloudvore/Conjugal pattern). `factory/ci-contract.test.mjs` — both prior `windows-latest` pins become `assertFactoryRunsOn()` with exact-byte expression match; adds negative cases (bare hosted, missing guard/fallback, wrong fallback, literal label list, duplicate/absent `runs-on`); pins `ci`/`docs-drift` stay `ubuntu-latest`, exactly three `runs-on:` lines, `CI_RUNS_ON` only in that expression. `factory/queue.jsonl` — appends self-filed row `CI-SELF-HOSTED-WIN` order 320 family claude.
- **Inert until owner action.** Unset/empty `CI_RUNS_ON` keeps hosted `windows-latest`. Setting the repo variable and registering one `ultra-magnus` runner instance are owner-only post-merge (not in this PR). Body correctly warns: one runner instance per host for this repo because of `Global\AdvLLM-ci-gate` / `Global\AdvLLM-ci-run` mutexes; cancel-in-progress can leave an abandoned mutex once on a persistent host.
- **Governance / filing risk.** Outside-conductor Claude session; body asks controller to apply `impl:claude` from body/`Co-Authored-By`. Self-files under operative amendment point 1 as a process change. If controller applies the narrow `CONDUCTOR-PREFLIGHT-TIMEOUT-FILE` reading, split into a queue-only FILE PR first (workflow/test bytes unchanged). Linux `ci` / `docs-drift` stay hosted by design.
- **Diff review:** Expression and contract pins match the stated VERIFY; no step/trigger/concurrency/version/`run:` string drift in `ci.yml`. Mutation-control and line-ending claims in the body were not re-executed here. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.