schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 106
head: ae01d7c9810cc579dfe442242a36efa829fadde0
reviewed_utc: 2026-09-18T21:23:40Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on run 35396438163; mergeable MERGEABLE; 0 GitHub reviews

## Blockers

(none for content — proposal-only ordinary class: task contract + adjudication + queue row; no process-authority or harness code changed on this tip)

## Improvements

- **Wait for remaining CI** — confirm `factory contracts` and `typecheck / lint / build / test` PASS on head `ae01d7c9` before merge (docs drift already green on run 35396438163).
- **Order claim precision** — body says open PRs take orders 248–252; live open queue tips include `PROC-ROW-TOKEN` 246 (#105), `TIER-ASTRA` 251 (#97), `CODEX-DISPATCH-PROBE` 252 (#99), `PERM-DENY-EDIT` 260 (#104). Order **253** for `REVIEW-ROUTE` does not collide; optional body tweak only.
- **Implementing row sequencing** — proposal correctly notes rebase onto whichever of #97 (TIER-ASTRA / point 8 / §7) or #69 (D2-BREACH-0911; currently CONFLICTING) lands first. Keep that as a hard gate on the implementing PR, not this proposal.
- **Dark-family wait is intentional** — implementing row is high-risk and needs a claude worker-host key under the rule it writes; while CLI is `AUTHENTICATION`-dark it must wait (point 8). Do not treat an in-session advisory receipt as that key.
- **Author merge posture** — body states author does not merge this head; D2 for this ordinary proposal still applies per project policy. CoS verdict is not a review APPROVE.

## Notes

- Gap: `evaluateReview` / `evaluateMerge` ignore launch mechanism; PR #101 r2 in-session Agent-tool leg validated `REVIEWED` after `claude -p` AUTHENTICATION. Proposal binds evidence in `factory/adjudications/REVIEW-ROUTE-20260918.md` and contracts the fix in `factory/proposals/REVIEW-ROUTE.md`.
- Rule (to land later): claude D2 key = `worker-host.ps1` subprocess on §7 `reviewer-claude`; any in-session subagent is advisory whoever spawns it; dark = route `AUTHENTICATION`/`CAPACITY`; receipts require `attempt.launch` (`ADVISORY` / `INVALID_LAUNCH`); `evaluateLegacyMerge` unchanged.
- Adjudication: three Opus legs split; Fable tie-break chose subprocess-only; Leg A dissent recorded (revisit if harness emits signed subagent launch records).
- Diff: +142/−0 on 3 paths — `factory/proposals/REVIEW-ROUTE.md`, `factory/adjudications/REVIEW-ROUTE-20260918.md`, `factory/queue.jsonl` (row `REVIEW-ROUTE` order 253, family claude).
- Local claims in body: `pwsh scripts/ci.ps1 -BaseSha 6b504b25…` GREEN (re-run after host CI lease); `validateRows` OK 52 rows; `queue-state.test.mjs` 16/16; numstats match with `--ignore-cr-at-eol`.
- Branch `feat/REVIEW-ROUTE` → `master`; tip `ae01d7c9`; author layibabalola (co-authored Claude Opus 5); 0 reviews.
- Cursor Cloud Agents not used (proposal-only; reviewed via gh). No GitHub PR comment posted.
- Doctrine bus: create `cos-feedback/adversarialllm/pr-106.md` on master for first review of this head.