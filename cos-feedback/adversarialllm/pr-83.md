schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 83
head: d6d3afc019139b8059ca4f32cba66329a0a54dbd
reviewed_utc: 2026-09-16T07:43:18Z
event: pr-opened
verdict: not-merge-ready
ci_summary: tip d6d3afc01913 — docs drift (delta gate) PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on Actions run 35069788373; mergeable=true (unstable); 0 GitHub reviews on tip

## Blockers

- **CI: hosted jobs not settled green on this tip** — `docs drift (delta gate)` already PASS on head `d6d3afc019139b8059ca4f32cba66329a0a54dbd`. Wait for `factory contracts` and `typecheck / lint / build / test` on the same Actions run `35069788373`. Do not merge while either is pending or red; if red, fix and re-push. Author claims local inventory (7) + queue-state (15) + typecheck GREEN with ordinary/CR-ignored numstats identical — hosted must still settle.
- **Process: independent dual-family D2 on this exact head** — 0 GitHub PR reviews on tip `d6d3afc0`. After hosted CI settles green, land fresh Sol/Codex + Claude/Sonnet on this tip. Non-author controller turns the key. Reviewers should verify (1) inventory row correction against `round-prompt-map.ts:230` (plan presence, not `M1_ROUND_ENGINE_ENABLED`), (2) adjudication honesty vs #52 round-cap facts, (3) `ph-M2` → `ph-M2-R` `supersededBy` queue contract, and (4) that this PR does **not** implement the recipe journey (proposal + evidence fix only).
- **Process: open sibling #82 overlaps the same inventory/docs bytes** — #82 (`P5-FLAG: stop attributing M1_ROUND_ENGINE_ENABLED to prompt routing`, tip `583be134`) is still OPEN and touches the same `inventory.mjs` + `docs/28` flag cell. Merging both without rebasing will CONFLICT. Close #82 as superseded by this tip's inventory portion (or merge #82 first and rebase #83) before treating either as land-ready.

## Improvements

- `adversarialllm/scripts/inventory.mjs` `flag()` helper still accepts a hand-authored pairing without checking that the anchor file references the flag symbol. That false pairing is the measured root of the #52 review poison and of #82/#83. A follow-up that fails closed when `flag(...)` is supplied but the anchor path has zero references to that constant would prevent the next silent publish of a false derived fact. Out of scope for this amendment; worth a tracked inventory hardening card.
- `factory/proposals/ph-M2-R.md` acceptance item 3 restates the inventory flag contract — good. Consider also naming the exact inventory test assertions (`promptMap.flag === 'none'` + gap regex) so the replacement implementer does not re-introduce the false pairing during recipe work.
- Adjudication `factory/adjudications/ph-M2-pr52-round-cap.md` is narrative Markdown (not a hashed `ADJ-*.json`). Confirm controller/closeout tooling treats this filename as the durable round-cap record for #52, or add a one-line pointer from any existing #52 closeout artifact so later sessions do not miss it.

## Notes

- Scoped review of tip `d6d3afc019139b8059ca4f32cba66329a0a54dbd` vs `master` (`e1b12b8a13bd`): 6 files, +51/−3. Contents: inventory flag correction + test lock; durable #52 round-cap adjudication; new `ph-M2-R` proposal; queue `ph-M2.supersededBy → ph-M2-R` + new row. **Does not** reopen #52, authorize a third round, implement RecipeEditor/journey, select a license, or activate live behavior. `ph-P3` correctly excluded.
- **Attribution claim (CoS spot-checked):** `round-prompt-map.ts` imports no `M1_ROUND_ENGINE_*` (only `FILE_UPLOAD_THRESHOLD` from constants); branches at `:230` on `!plan || plan.rounds.length === 0`. Adjacent `saveRoundFeed` / `getRoundFeed` rows correctly keep `M1_ROUND_ENGINE_ENABLED=false`. Diff changes exactly one inventory Flag cell plus gap text; docs/28 block regenerated; new unit asserts `promptMap.flag === 'none'`.
- **Queue contract (CoS spot-checked):** `queue-state.mjs` already knows `supersededBy` (KEYS set + `SUPERSEDED_PENDING` / `SUPERSEDED_DELIVERED`). Empty `supersededBy` fails validation; non-empty `["ph-M2-R"]` is the supported shape. `ph-M2-R` dependsOn `["P5","P3"]` matches original `ph-M2` deps. No other open queue parent appeared to depend solely on `ph-M2` in the diff.
- Proposal `ph-M2-R` preserves original owned files, two-round cap, default-off/live protections, and corrected acceptance that plan presence (not the flag) selects plan-path execution — aligns with measured `round-prompt-map.ts` behavior and with #52/#82 CoS notes.
- MERGEABLE tip on `codex/ph-M2-redesign-amendment` → `master`. Verdict **not-merge-ready** until hosted CI is green, D2 lands on this tip, and the #82 overlap is resolved.
- CoS review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (factory proposal + inventory tip; analysis budget). Doctrine bus: published on `master` at `cos-feedback/adversarialllm/pr-83.md`.