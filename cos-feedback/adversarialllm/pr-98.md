schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 98
head: a5b353bd262dcca193ffd6aad393942c477f2853
reviewed_utc: 2026-09-17T19:07:31Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS on head a5b353bd; factory contracts + typecheck/lint/build/test IN_PROGRESS (run 35262898401); 0 GitHub reviews on tip

## Blockers

(none for content — proposal-only ordinary-class staging of one queue row + proposal + evidence; no high-risk / point-8 path touched; implements nothing)

## Improvements

- **Wait for remaining CI** — factory contracts and typecheck / lint / build / test still IN_PROGRESS at review; confirm both PASS before merge (docs drift already green on this head). Author claims local `pwsh scripts/ci.ps1 -BaseSha 6b504b25…` GREEN (~1121s, 18/18 steps) and `node --test factory/queue-state.test.mjs` 16/16 — re-check against the GitHub jobs, not the claim alone.
- **Independent non-author review on tip** — ordinary class (queue + proposals/ + adjudications/ outside point 8), but the PR body still requires two validated current-head/current-base adversarial legs, zero MUST, no BLOCKER, a literal APPROVE from a non-author leg, and merge by a fresh non-author controller (authoring session will not merge). Land at least one independent non-author APPROVE on a5b353bd before merge (factory/HUB.md D2). One leg per family preferred.
- **Keep implementation out of this PR** — this head correctly implements nothing. The follow-on implementer must not touch scheduled tasks, root CLAUDE.md / AGENTS.md / HUB.md / RESET-PLAN.md / prompts/, or the machine-local checkpoint hook; archive-by-`git mv` only (never delete session-state / resume-state.config); no body may carry `row:F1.1`.

## Notes

- Scope: +512/−0 across exactly 3 paths vs base 6b504b25 — new factory/proposals/PROC-RESUME-READINESS.md, factory/adjudications/PROC-RESUME-READINESS-evidence-20260917.md, and one appended queue row PROC-RESUME-READINESS (order 250, family claude, spec points at the proposal). Matches stated intent (#83 / #88 / #91 proposal-only pattern).
- Queue JSON line parses; keys are id/title/spec/order/family (no dependsOn); order 250 avoids open #90's claimed 248–249 and tree max 247. All three new blobs are LF (0 CR).
- Proposal refuses delete-and-re-establish after three-leg adjudication: archive-and-replace stale resume docs; leave AdvLLM-ResumeState-Heartbeat disabled (D3); report-only on machine-local session-checkpoint starvation (999 worktrees vs 5s Stop timeout); add two-tiered RESUME.md dirty-worktree step with measured path-scoped predicate.
- Branch feat/PROC-RESUME-READINESS → master; mergeable=true; mergeable_state=unstable (checks pending); not draft; tip a5b353bd; base 6b504b25; author layibabalola (co-authored Claude Opus 5); 0 reviews.
- Cursor Cloud Agents not used; review via gh (factory queue + proposal + evidence). No GitHub PR comment posted.
- Doctrine bus: create cos-feedback/adversarialllm/pr-98.md on master (first CoS pass for this PR).