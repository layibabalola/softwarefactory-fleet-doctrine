schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 144
head: 88148f249d89f2b3ef03f916b6b9c8f5d41d4cf5
reviewed_utc: 2026-09-25T15:02:14Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36151384109 — typecheck/lint/build/test IN_PROGRESS; factory contracts + docs drift QUEUED; MERGEABLE/UNSTABLE; ahead 1 / behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `88148f24`.** Actions run 36151384109 just started; typecheck is in progress and the other two required jobs are queued. Re-check before merge.
- **High-risk gate change: two independent review legs still required (D2).** Author packet requires current-head/base Sol and Sonnet reviews plus guarded non-author merge. Conductor/controller must still dispatch non-author legs.
- **Confirm hosted CI surfaces attempt receipts when a classified retry fires.** Local packet claims full gate GREEN with zero retries (1333s) on this tip; a real infra flake should leave `.factory-local/ci-failures/*-*.attempts.json` with authorizing `signature` and `GREEN-AFTER-RETRY` (or `RED` if second attempt fails).

## Notes

- **What changed:** CI-FLAKE-CLASSIFY. `scripts/ci.ps1` adds `Get-StepInfrastructureSignature` and a one-retry loop in `Invoke-Step`. Fail-closed vetoes first (timeouts/abandoned/held-lease; typecheck/lint/harness-lint/build/p8-artifact-build; AssertionError/expected/not-ok/FAIL). Allowlist: pinned `observe-pwsh-null-status` (source SHA256 `8ea90597…` verified against tip `observe-runner-contract.test.ts`), spawn EAGAIN/ENOMEM/UNKNOWN, vitest-worker-start (unit only), mutex-acquisition, install-network EAI_AGAIN/ECONNRESET/ENETUNREACH/ENOTFOUND (install only, lifecycle/integrity codes excluded). Summary reports `GREEN-AFTER-RETRY` when any step recovered.
- **Owned scope:** +203/−9 across 2 files, 1 commit (`88148f24`). Files: `scripts/ci.ps1`, `factory/ci-contract.test.mjs`. No app/source/lockfile changes. Base `d3652596`.
- **Regression coverage:** classifier corpus covers recoveries (pool/null-spawn/mutex/install-network/spawn) and refusals (assertions, timeouts, wrong step, mixed failure, changed pinned source). Real-gate harness checks GREEN-AFTER-RETRY summary/order and second-attempt assertion stays RED (skips build).
- **Spot-check:** timeout veto precedes allowlist; null-status requires exact test identity + single AssertionError/FAIL + pinned hash; receipt written on failure or second attempt; clean first-pass leaves no receipt. Indentation inside the new `for` loop is uneven but semantics are clear.
- No secrets in diff. Review via `gh`. No GitHub PR comment posted.