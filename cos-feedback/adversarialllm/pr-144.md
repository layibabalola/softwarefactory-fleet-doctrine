schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 144
head: 152517190885228cc0d1e548f290ecbf65260af9
reviewed_utc: 2026-09-25T15:39:51Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 36155530557 — docs drift SUCCESS; typecheck/lint/build/test IN_PROGRESS; factory contracts QUEUED; prior tip 88148f24 run 36151384109 all SUCCESS; MERGEABLE/UNSTABLE; ahead 2 / behind 0; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `15251719`.** Actions run 36155530557 just started on the new head; docs drift already SUCCESS, typecheck in progress, factory contracts queued. Prior tip `88148f24` run 36151384109 completed all-SUCCESS — that wait is cleared. Re-check this tip before merge.
- **High-risk gate change: two independent review legs still required (D2).** Still 0 GitHub reviews. Author packet requires current-head/base Sol and Sonnet reviews plus guarded non-author merge. Conductor/controller must still dispatch non-author legs on `15251719`.
- **Confirm hosted CI surfaces attempt receipts when a classified retry fires.** Still open. Local packet claims full gate GREEN on this tip (1193s, exit 0, zero survivors) — likely zero retries again. A real infra flake should leave `.factory-local/ci-failures/*-*.attempts.json` with authorizing `signature` and `GREEN-AFTER-RETRY` (or `RED` if second attempt fails).

## Notes

- **What changed since prior CoS (`88148f24`):** one commit `15251719` fix(ci): reject mixed errors in null-status retry (+4/−0). `Get-StepInfrastructureSignature` now also requires exactly one `(?i)\b\w*Error\s*:` and one `(?i)\bexpected\b` match inside the pinned null-status path (closes mixed `Error:` / `TypeError:` smuggling past the single-AssertionError count). Contract test adds `mixed-error-assertion` and `mixed-type-error` refusal cases. Packet notes focused regression reproduced the mixed-error false positive before the fix and passes afterward.
- **Owned scope (full PR):** +207/−9 across 2 files, 2 commits (`88148f24` + `15251719`). Files: `scripts/ci.ps1` (+67/−6), `factory/ci-contract.test.mjs` (+140/−3). No app/source/lockfile changes. Base `d3652596`.
- **Classifier (unchanged core + tip tighten):** Fail-closed vetoes first (timeouts/abandoned/held-lease; typecheck/lint/harness-lint/build/p8-artifact-build; AssertionError/expected/not-ok/FAIL after the pinned exception). Allowlist: pinned `observe-pwsh-null-status` (source SHA256 `8ea90597…` + exact test identity + single AssertionError/Error:/expected/FAIL counts), spawn EAGAIN/ENOMEM/UNKNOWN, vitest-worker-start (unit only), mutex-acquisition, install-network EAI_AGAIN/ECONNRESET/ENETUNREACH/ENOTFOUND (install only; lifecycle/integrity codes excluded). One retry max; receipts on failure or second attempt; `GREEN-AFTER-RETRY` in summary when any step recovered.
- **Prior Improvements vs this push:** (1) wait-for-`88148f24`-CI — addressed (all SUCCESS), superseded by wait-for-`15251719`; (2) D2 legs — still open; (3) hosted attempt-receipt confirmation — still open.
- No secrets in diff. Review via `gh`. No GitHub PR comment posted.