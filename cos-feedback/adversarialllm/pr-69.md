schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 69
head: c29ab67b600a8fab18728d38e16b216790ef4e82
reviewed_utc: 2026-09-15T06:50:30Z
event: pr-opened
verdict: not-merge-ready
ci_summary: docs drift (delta gate) SUCCESS on c29ab67b (run 34938688740); factory contracts and typecheck/lint/build/test still pending/in progress. PR claims local pwsh scripts/ci.ps1 GREEN (915 s, 14 stages) on this tree before commit.

## Blockers

- **Dual-family D2 + non-author APPROVE on this tip** — ordinary class (queue only) but still needs two validated current-head/base legs and a non-author APPROVE. Authoring session read the prior receipts/transcripts and is ineligible as a leg; that means a codex leg is required for the non-author APPROVE. CoS does not staff Sol/Claude D2 legs.
- **Hosted CI not settled on this tip** — wait for factory contracts and typecheck/lint/build/test green on `c29ab67b` (or a posted local `pwsh scripts/ci.ps1` receipt bound to this exact head) before merge.

## Improvements

- After merge, keep the controller hold on **CTRL-2d / PR #49** until REVIEW-D2-0911 and VERDICT-SCHEMA deliver (evaluator cannot express that hold via `dependsOn` because it checks open PR first). Surface the hold in the hub brief so a later lane does not merge #49 early.
- When REVIEW-D2-0911 runs, bind receipts to the stated squash subjects (`ab121fd7`/`fbdfda4f` for #47; `fbdfda4f`/`697c45e5` for #46) plus current `scripts/never-authorized-guard.mjs` on master (#64/#67 already moved it).

## Notes

- Diff vs base: `factory/queue.jsonl` only (+5/−1). CTRL-2 `supersededBy` gains `REVIEW-D2-0911` and `VERDICT-SCHEMA` (CTRL-2a-REOPEN precedent). New rows: D2-BREACH-0911 (63), REVIEW-D2-0911 (64), REVIEW-D2-0911-45 (65), VERDICT-SCHEMA (66, depends on 64).
- Records that #45–#47 merged 2026-09-11 from one Haiku chat whose labelled “cross-family” legs were same-model Agent subagents with no reviewer-host receipts / no `evaluateMerge`. No Claude-family receipt on those heads; high-risk #46/#47 lack non-author APPROVE. Nothing reverted here — REVIEW-D2-0911 decides.
- VERDICT-SCHEMA closes the #60 ADOPT/MUST + same-attempt `--resume` hole: REVIEWER items are open findings only; ADOPT and empty `declared_self_failure` sentinels become `INVALID_FINDING`; corrected verdicts need a new attempt id.
- Graph as claimed: CTRL-2 `SUPERSEDED_PENDING` waiting for CTRL-2d/e + the two new rows; REVIEW-D2-0911 and REVIEW-D2-0911-45 READY after this lands; VERDICT-SCHEMA waits on REVIEW-D2-0911.
- No GitHub PR comment posted by CoS. Review via `gh` only (Cloud Agents not on plan).