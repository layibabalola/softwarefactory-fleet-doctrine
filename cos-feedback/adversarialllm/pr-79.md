schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 79
head: 6f9b983ee95dfbb522bc3b7f228e7604be0cff11
reviewed_utc: 2026-09-16T04:56:28Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; typecheck/lint/build/test + factory contracts PENDING on head 6f9b983 (Actions run 35057505715); queue-row-only (+1/0); tip ahead of master by 1

## Blockers

- **CI: `typecheck / lint / build / test`** — PENDING on head `6f9b983ee95dfbb522bc3b7f228e7604be0cff11` (Actions run 35057505715). Wait for green; if red, fix and re-push.
- **CI: `factory contracts`** — PENDING on the same run. Same wait/fix rule.
- **Process: non-author APPROVE + fresh non-author controller** — Ordinary class (queue row only; no process-authority path), Claude-authored. PR body still requires non-author APPROVE and a fresh non-author controller. 0 GitHub reviews on this head.

## Improvements

- **Tighten VERIFY's prohibition grep before the successor implementation PR.** VERIFY (b) still greps `factory/KERNEL-ADOPT.md` for `K1`–`K12` and the words `conforms`, `gap`, `not met`, `none`, and `absent`. Soft words `none` / `absent` can false-fail legitimate provenance/status prose. Prefer phrase-level bans (`not met`, `conforms to K`, `gap:`) or exclude status/provenance lines. Unchanged from prior CoS note on `b06ef575`.
- Optional: when the replacement lands, cite the measured tree-OID counterexample paths in the bus filing (not in-repo record) so the false-absence lesson stays falsifiable without a clause table.

## Notes

- Round-2 push (`6f9b983`) addresses Claude round-1 MUSTs on the queue row itself: (1) ownership is now an explicit four-path set (`factory/KERNEL-ADOPT.md`, kernel bullet in root `CLAUDE.md`, kernel section of `factory/doctrine-entry.md`, this `queue.jsonl` row) with "no other path / no filing or transport"; multi-site defect is a measurable head property (no PR 75 finding condition at any changed path) rather than a review-method directive. (2) Filing pointer no longer requires claiming staged bytes; VERIFY requires stating actual status (no kernel filing under `factory/doctrine` at head; staging proposed in unmerged `#65`; `#65` scoped to kernel r2 while this adopts r4), naming that no transport row exists, and recording the D12 lowercase vs RESET-PLAN capitalised spelling gap as an open item for a later transport owner.
- Scope still single path `factory/queue.jsonl` (+1/−0). Row `KERNEL-ADOPT-B` order 248, family `claude`, no `dependsOn`. Ordinary-class claim holds.
- Queue hygiene spot-check PASS at this tip: 48 rows; unique `id` and `order`; no missing `dependsOn` targets. Replaces adj-closed `#75`; rewrite source `feat/KERNEL-ADOPT` @ `1ccea3bc`.
- Prior CoS soft-word Improvement not addressed on this head. Verdict unchanged: merge-when-ci-green.
- mergeable=MERGEABLE; mergeStateStatus=UNSTABLE while CI runs. Review via `gh`; no GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-79.md` for head `6f9b983`.
