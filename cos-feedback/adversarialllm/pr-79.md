schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 79
head: b06ef575abec2a8f3f5863f390f01d19684025a0
reviewed_utc: 2026-09-16T04:30:35Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; typecheck/lint/build/test + factory contracts PENDING on head b06ef575 (Actions run 35055564414); queue-row-only (+1/0); tip ahead of master by 1, behind by 0

## Blockers

- **CI: `typecheck / lint / build / test`** — PENDING on head `b06ef575abec2a8f3f5863f390f01d19684025a0` (Actions run 35055564414). Wait for green; if red, fix and re-push.
- **CI: `factory contracts`** — PENDING on the same run. Same wait/fix rule.
- **Process: non-author APPROVE + fresh non-author controller** — Ordinary class (queue row only; no process-authority path), but authored by a Claude session. PR body correctly requires a non-author APPROVE and a fresh non-author controller before merge. 0 GitHub reviews yet.

## Improvements

- **Tighten VERIFY's prohibition grep before the successor implementation PR.** The VERIFY bans clause claims in `factory/KERNEL-ADOPT.md` via grep for `K1`–`K12` and the words `conforms`, `gap`, `not met`, `none`, and `absent`. `none` / `absent` are soft: legitimate provenance/status prose (e.g. "none of the process-authority documents", "authority is absent from this record") can false-fail. Prefer phrase-level bans (`not met`, `conforms to K`, `gap:`) or exclude the status/provenance lines from the grep. Attack surface the PR body already flags.
- Optional: when the replacement lands, cite the measured tree-OID counterexample files in the bus filing (not in-repo record) — CoS spot-check on this head found `treeOid` / `candidate_tree_oid` in at least the three `adversarialllm/docs/decisions/utilization-shadow/*.template.json` files plus `factory/doctrine/adjudications/approach-a-design/AdversarialLLM.md` (and archive verify scripts). Spec's "six tracked files" narrative is directionally right; pin exact paths in the filing so the false-absence lesson stays falsifiable without reintroducing a clause table here.

## Notes

- Scope: single path `factory/queue.jsonl` (+1/−0). Adds row `KERNEL-ADOPT-B` (order 248, family `claude`, no `dependsOn`). No `CLAUDE.md` / `HUB.md` / `RESET-PLAN.md` touch — ordinary class claim holds.
- Queue hygiene spot-check PASS: 48 rows; `order` values unique; no missing `dependsOn` targets; `id` unique. Replaces adj-closed `#75` (HUB Retries bars automatic successor). Retained rewrite source: `feat/KERNEL-ADOPT` @ `1ccea3bc`.
- Design of the prescribed replacement is coherent with the stated defect class (false positive → false negative on mechanism claims): delete the clause-table surface; keep provenance pin, record-not-authority status, filing pointer, degradation statement; define "answer" locally as recording dispositions under `factory/adjudications/`. VERIFY obligations look mechanically checkable once the soft-word list is tightened.
- Carried-forward settled points (D12 case-insensitive dest; external-pin structural answer) are narrative only in this row — fine for a queue prescription; do not re-litigate on this PR.
- mergeable=MERGEABLE; mergeStateStatus=UNSTABLE while CI runs. Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/adversarialllm/pr-79.md`.