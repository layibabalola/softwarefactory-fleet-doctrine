schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 51
head: 8b8c7a0acd485aa4c06c44456947698b312a65d1
reviewed_utc: 2026-09-10T13:14:22Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift pass; typecheck/lint/build/test + factory contracts still pending on run 34481105441; author claims local exact-head CI green

## Blockers

- **CI: `typecheck / lint / build / test`** — still pending on head `8b8c7a0acd485aa4c06c44456947698b312a65d1` (Actions run 34481105441). Wait for green before merge; if red, fix the failing suite and re-push.
- **CI: `factory contracts`** — still pending on the same run. Same wait/fix rule.
- **Process: independent D2 review** — no GitHub reviews yet; PR body requires native Sol/Codex + independent Claude review on the current head before merge. Land those reviews (or an explicit owner waiver outside this file) after hosted CI settles.

## Improvements

- `adversarialllm/src/background/bundle/source-upload-options.ts` (~lines 364–369): base64 via `String.fromCharCode(...subarray)` can stress call-stack limits on very large artifacts. Prefer a typed-array-safe encoder (e.g. chunked `btoa` without spread, or a small helper) before enabling live large ZIP/PDF sources.
- `adversarialllm/src/background/bundle/campaign-source.ts` storage-cap path: cap is checked after `savePreparedSourceBundle` using global `getCampaignStorageStats()`. Fine for the single-operator extension, but concurrent starts could race the cap; if multi-start ever becomes real, fold the cap check into the same write transaction as the add.
- `adversarialllm/src/entrypoints/sidepanel/components/SourceBundleControls.tsx`: redaction `<select>` only offers `full` / `none` while preparation accepts `patterns`. Either expose patterns in the UI later or document that patterns mode is API-only until then (non-blocking while the flag is off).
- Consider a focused App-level test that `startDisabled` stays false when `BUNDLE_PACKAGER_ENABLED` is false and `sourceBundleInput` is undefined (today covered indirectly via `sourceInputProblem(undefined) === undefined`).

## Notes

- Scope: P4 product path only — ordered text sources → redaction/prep → content-addressed DB v4 `sourceBundles` → atomic campaign handles → verified rehydrate → existing uploader → routing receipt before prompt. Docs/evidence updated; companion auto-drive forced to foreground when a managed source is attached.
- `BUNDLE_PACKAGER_ENABLED` remains compile-time `false`. UI checkbox disabled; explicit `sourceBundleInput` throws `SOURCE_BUNDLE_DISABLED`; legacy starts omit the field. No live-provider proof claimed.
- Integrity posture looks careful: add-only store, verify-on-read, refuse unverified upload / tab change / routing-persist failure instead of prompt fallback, PDF Unicode refusal as `PDF_NON_ASCII`.
- Author receipt: local exact-head CI green (~785s) at this head vs base `2153e3af9f6d7a34dd22d9ff3efe053f03966930` — hosted jobs still the merge gate.
- Cursor Cloud Agents unavailable on current plan; CoS reviewed via `gh` (title/body/diff/checks + key modules/tests). No GitHub PR comment posted.
- Doctrine bus: `cos-feedback/` lands via open PR layibabalola/softwarefactory-fleet-doctrine#58 (branch `docs/cos-feedback-20260910`); this file is published there until #58 merges to master.