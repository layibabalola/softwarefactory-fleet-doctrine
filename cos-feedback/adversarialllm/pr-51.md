schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 51
head: 298f649f0b77daa5b67cdd2ee5caf5c3765c5dca
reviewed_utc: 2026-09-16T03:59:04Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35053762887; typecheck/lint/build/test + factory contracts IN_PROGRESS/pending; mergeStateStatus UNSTABLE; no GitHub reviews

## Blockers

- **CI: `typecheck / lint / build / test`** — still pending on head `298f649f0b77daa5b67cdd2ee5caf5c3765c5dca` (Actions run 35053762887). Wait for green before merge; if red, fix the failing suite and re-push.
- **CI: `factory contracts`** — still pending on the same run. Same wait/fix rule.
- **Process: independent D2 review** — no GitHub reviews yet; PR body requires native Sol/Codex + independent Claude review on the **current** head before merge. Master merge `da2ce41a6f06` (master `4dcbcbc3`) voids any prior-tip D2; land fresh dual-family reviews (or an explicit owner waiver outside this file) after hosted CI settles.

## Improvements

- `adversarialllm/src/background/bundle/source-upload-options.ts` (~line 48): base64 still builds via `String.fromCharCode(...subarray(..., 8192))` then `btoa`. Chunking helps vs full-buffer spread, but 8k-arg spreads can still stress some call stacks on large ZIP/PDF artifacts. Prefer a typed-array-safe encoder without spread before enabling live large sources.
- `adversarialllm/src/background/bundle/campaign-source.ts` storage-cap path: cap is checked after `savePreparedSourceBundle` using global `getCampaignStorageStats()`. Fine for the single-operator extension, but concurrent starts could race the cap; if multi-start ever becomes real, fold the cap check into the same write transaction as the add.
- `adversarialllm/src/entrypoints/sidepanel/components/SourceBundleControls.tsx`: redaction `<select>` only offers `full` / `none` while preparation accepts `patterns`. Either expose patterns in the UI later or document that patterns mode is API-only until then (non-blocking while the flag is off).
- Consider a focused App-level test that `startDisabled` stays false when `BUNDLE_PACKAGER_ENABLED` is false and `sourceBundleInput` is undefined (today covered indirectly via `sourceInputProblem(undefined) === undefined`).

## Notes

- Push since prior CoS head `8b8c7a0acd485aa4c06c44456947698b312a65d1`: merge current master (`4dcbcbc3` via `da2ce41a6f06`), then `80993817226c` (preserve source retry contract after P2.5 send-prompt extraction — source dispatches skip legacy upload-cache reuse; every source send must re-prove the routed artifact on the current tab), then `298f649f0b77` (preserve base line endings on `types.ts` / docs28 P4 blurb — CR-only delta hygiene).
- Scope unchanged: P4 product path — ordered text sources → redaction/prep → content-addressed DB v4 `sourceBundles` → atomic campaign handles → verified rehydrate → existing uploader → routing receipt before prompt. `BUNDLE_PACKAGER_ENABLED` remains compile-time `false`. No live-provider proof claimed.
- Prior CoS Improvements (base64 encoder, storage-cap race, patterns UI, App startDisabled test) not clearly addressed by this push; carried forward.
- Author receipt on older tip: local exact-head CI green (~785s) at `8b8c7a0…` — hosted jobs on the post-merge tip remain the merge gate.
- Cursor Cloud Agents unavailable on current plan; CoS reviewed via `gh` (title/body/diff/checks + post-merge P4 patches). No GitHub PR comment posted.
