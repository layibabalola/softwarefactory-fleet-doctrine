schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 51
head: f69171d44b89b7633d1e7c14b6d237e15343a9a1
reviewed_utc: 2026-09-16T04:30:57Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35055839130; typecheck/lint/build/test + factory contracts IN_PROGRESS/pending; mergeStateStatus UNSTABLE; no GitHub reviews

## Blockers

- **CI: `typecheck / lint / build / test`** — still pending on head `f69171d44b89b7633d1e7c14b6d237e15343a9a1` (Actions run 35055839130). Wait for green before merge; if red, fix the failing suite and re-push.
- **CI: `factory contracts`** — still pending on the same run. Same wait/fix rule.
- **Process: independent D2 review** — no GitHub reviews yet; PR body requires native Sol/Codex + independent Claude review on the **current** head before merge. Master merge `da2ce41a6f06` (master `4dcbcbc3`) and tip `f69171d4` void any prior-tip D2; land fresh dual-family reviews (or an explicit owner waiver outside this file) after hosted CI settles.

## Improvements

- `adversarialllm/src/background/bundle/source-upload-options.ts` (~line 48): base64 still builds via `String.fromCharCode(...subarray(..., 8192))` then `btoa`. Chunking helps vs full-buffer spread, but 8k-arg spreads can still stress some call stacks on large ZIP/PDF artifacts. Prefer a typed-array-safe encoder without spread before enabling live large sources.
- `adversarialllm/src/background/bundle/campaign-source.ts` storage-cap path: cap is checked after `savePreparedSourceBundle` using global `getCampaignStorageStats()`. Fine for the single-operator extension, but concurrent starts could race the cap; if multi-start ever becomes real, fold the cap check into the same write transaction as the add.
- `adversarialllm/src/entrypoints/sidepanel/components/SourceBundleControls.tsx`: redaction `<select>` only offers `full` / `none` while preparation accepts `patterns`. Either expose patterns in the UI later or document that patterns mode is API-only until then (non-blocking while the flag is off).
- Consider a focused App-level test that `startDisabled` stays false when `BUNDLE_PACKAGER_ENABLED` is false and `sourceBundleInput` is undefined (today covered indirectly via `sourceInputProblem(undefined) === undefined`).
- `adversarialllm/tests/unit/file-upload-recycle-invalidation.test.ts`: new `wrong-route` / `wrong-filename` / `noncanonical-base64` cases are kimi/PDF-only. Add one zip-route provider case (chatgpt/claude/grok) so ATTACHMENT_ROUTING mismatch is covered for both forms.

## Notes

- Push since prior CoS head `298f649f0b77daa5b67cdd2ee5caf5c3765c5dca`: `f69171d44b89` — `fix(P4): validate routed source upload forms`. `dispatch-one.ts` now checks form mime/filename against `ATTACHMENT_ROUTING` (`source-bundle.zip|pdf`) and requires canonical base64 (`btoa(decoded) === encoded`) before upload; invalid forms fail closed as `SOURCE_BUNDLE_FORM_INVALID` without prompt fallback. Tests add `wrong-route`, `wrong-filename`, `noncanonical-base64` modes.
- Scope unchanged: P4 product path — ordered text sources → redaction/prep → content-addressed DB v4 `sourceBundles` → atomic campaign handles → verified rehydrate → existing uploader → routing receipt before prompt. `BUNDLE_PACKAGER_ENABLED` remains compile-time `false`. No live-provider proof claimed.
- Prior CoS Improvements (base64 encoder, storage-cap race, patterns UI, App startDisabled test) not clearly addressed by this push; carried forward. New zip-route test coverage noted above.
- Author receipt on tip: local exact-head CI green (~672s) at `f69171d4` vs base `4dcbcbc3` — hosted jobs on this tip remain the merge gate.
- Cursor Cloud Agents unavailable on current plan; CoS reviewed via `gh` (title/body/diff/checks + tip patch). No GitHub PR comment posted.