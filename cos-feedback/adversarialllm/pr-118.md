schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 118
head: 84fa9238dce6b2ff4e02b015f4f6e5d531d1bc79
reviewed_utc: 2026-09-21T09:24:26Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS on run 35582807390 (cleared prior HIGH); factory contracts + typecheck/lint/build/test still IN_PROGRESS; mergeable MERGEABLE/unstable; 0 GitHub reviews

## Blockers

(none) — prior docs-drift HIGH on `docs/02` §17 Restore is cleared on this tip (hosted delta gate PASS). Remaining required checks unsettled at review time; wait for full green before merge.

## Improvements

- **Wait for remaining CI green on `84fa9238`.** `factory contracts` and `typecheck / lint / build / test` were still IN_PROGRESS on Actions run 35582807390. Do not merge on docs-drift alone.
- **Two-leg / D2 still open.** Round-2 commit provenance again cites `codex exec --sandbox workspace-write` (gpt-5.6-sol) with controller-only staging/commit + one-cell `docs/28` correction. Implementer family remains **codex** → D2 still needs a literal APPROVE from the **claude** leg. GitHub Reviews API still returns 0 formal review objects. CoS does not substitute for that adjudication.
- **Static `background.ts` string asserts** in `tests/unit/reach-delivery.test.ts` (routing / cold-resume source regex) remain brittle — same Improvement as prior #53 / round-1 CoS. Prefer an exported seam / injected finalize wrapper over source regex.
- **Receipt capacity 256** still refuses new keys (correct anti-replay). Operator signal when hit remains coarse (`reach_failed` / throw). Track retention/GC + clearer signal as a follow-up if real use grows.
- **Optional follow-up (out of ph-P3-R):** consider adding `reach-delivery.ts` (+ sync-writer / verdict-head) to `docs-source-map.json` if uncovered-changed-file noise returns under a stricter fail-on; round-2 cleared the HIGH without it.

## Notes

- **Round-2 head `84fa9238` vs prior CoS head `dff6b481`.** Commit message: "ph-P3-R round 2: fix the five product MUSTs round 1 found." Diff vs prior tip: `reach-delivery.ts` +242/−52, `reach-delivery.test.ts` +121/−4, `docs/02` +8 (Reach receipt reconciliation under §17 Restore), `docs/28` one-cell reach call-site correction (`writeVerdictHeads` at `reach-delivery.ts:159`, was falsely "none found" when the adapter was still untracked at round-1 gate time). Overall PR vs master: +976/−9 across 7 files.
- **Prior CoS Blocker addressed:** hosted `docs drift (delta gate)` now PASS (17s) on run 35582807390 — the §17 Restore HIGH that failed run 35576991108 is gone after documenting boot-time webhook receipt reconciliation (`reachBinding.initialize` → reconciler; `attempting` → terminal `ambiguous`/`interrupted`; no POST replay).
- **Prior CoS Improvement clearly addressed (DISABLE-01):** `reach.config` is no longer memoized for the whole SW lifetime. Binding observes `onChanged` on `reach.config`, `loadConfig` / `isEnabled` re-read before delivery and port ops, `AbortController` cancels pending sync work on disable, webhook `canPost` re-checks enabled + exact config snapshot before POST. New tests cover explicit-false / omitted opt-in flags, positive control POST, mid-flight disable, and cancel-of-retry.
- **Other round-1 product MUSTs (on read):** RETRY-01 — `ReachDeadline` with `REACH_PORT_TIMEOUT_MS=2000` / `REACH_DELIVERY_TIMEOUT_MS=12000` / webhook POST 10s; stuck-port test releases the serialized channel. SYNC-COLLISION-01 — `sameHead` requires exact attempted head retained or fail (no delivered receipt on collision). MV3-INTERRUPT-01 — boot reconciler + in-flight `attempting` → `ambiguous` without replay; covered by unit test. C1-BINDING-OPTIN-UNPINNED — binding-level tests for present config with explicit false flags + omitted flags + positive control.
- **Safety / minimal-shape posture still looks sound:** default disabled, exact `grantedOrigin`, HTTPS-only / HMAC `X-*` header gate, fetch redirect/credentials/cache hardening, one-POST max for webhook, sync retries capped at 3, `finalizeBeforeReach` keeps reach best-effort after durable finalize. Live cross-device still OUT OF SCOPE (`ph-P3-LIVE` / `R-REACH`).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
