schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 118
head: dff6b4818c2bc8f368d328ac73207e65814681f6
reviewed_utc: 2026-09-21T08:17:32Z
event: pr-opened
verdict: not-merge-ready
ci_summary: docs drift (delta gate) FAIL on run 35576991108 (HIGH: docs/02 §17 Restore driven by background.ts); factory contracts + typecheck/lint/build/test still IN_PROGRESS; mergeable MERGEABLE/unstable; 0 reviews

## Blockers

- **CI: `docs drift (delta gate)` FAILED** on head `dff6b481` (Actions run 35576991108 / job 106260944497). Hosted check: `node scripts/docs-drift-check.mjs --since b1bc0b55 --ignore-updated-docs --fail-on high` → severity high=1. Concrete HIGH hit: `docs/02_ARCHITECTURE_DIAGRAMS.md` §17 Restore, driven by `src/entrypoints/background.ts` (reason: SW restore/boot path mirrors background.ts). Commit attribution is this tip (`dff6b4818 ph-P3-R`). Also reported (non-failing under `--fail-on high`): 9 LOW hits + uncovered changed files `src/background/reach-delivery.ts`, `src/shared/sync/sync-writer.ts`, `src/shared/sync/verdict-head.ts` with no docs-source-map rule. Not flake/base-only noise — failure is tip-driven. Author body claims local `ci.ps1` GREEN including docs-drift on exact head; hosted required check disagrees. Fix: update §17 Restore (and map/source coverage for the new reach/sync files as needed), or otherwise clear the HIGH before re-push. Do not merge while this required check is red.
- **CI: remaining required checks still unsettled** — `factory contracts` and `typecheck / lint / build / test` IN_PROGRESS on the same run. Even after docs-drift is repaired, wait for full green on the new tip.

## Improvements

- **Clear the docs-drift HIGH on the next tip.** Prefer an honest §17 / boot-path note that `reachBinding.initialize()` runs at SW start and `finalizeWithReach` wraps both fresh and cold-resume finalize paths (`adversarialllm/src/entrypoints/background.ts` ~2252–2260 / ~2354 / defineBackground). Consider adding `reach-delivery.ts` (+ sync-writer / verdict-head) to `docs-source-map.json` so they are not left as uncovered changed files.
- **Reconcile local vs hosted docs-drift.** PR claims root `ci.ps1` docs-drift ok on `dff6b481`; hosted delta gate fails HIGH on the same bytes. Confirm local invocation matches CI (`--since` base, `--fail-on high`) before the next gate claim.
- **`reach.config` loaded once per SW lifetime** (`createReachBinding` initialize path in `reach-delivery.ts`). Acceptable for default-disabled opt-in offline half, but surface in settings UX / docs that a settings change may need SW restart; file a small follow-up if live reload is desired (out of ph-P3-R scope).
- **Static `background.ts` string asserts** in `tests/unit/reach-delivery.test.ts` ("routes fresh and cold-resume…") remain brittle to formatting/rename (same Improvement as prior #53 CoS). Prefer an exported seam / injected finalize wrapper over source regex.
- **Receipt capacity 256** refuses new keys (correct anti-replay). When hit, operator signal is still coarse (`reach_failed` / throw). Track retention/GC + clearer signal as a follow-up row if real use grows.
- **Two-leg gate still open on this head.** PR body: needs two independent legs; implementer family **codex** by producing-command evidence → D2 needs a literal APPROVE from the **claude** leg. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.

## Notes

- Scope matches ph-P3-R offline half: new `adversarialllm/src/background/reach-delivery.ts` (+283) + `tests/unit/reach-delivery.test.ts` (+303); wiring in `background.ts` (+28/−2), `sync-writer.ts` (+14/−5), `verdict-head.ts` (+31 `readVerdictHeads`); `docs/28` one regenerated safety-rails anchor `background.ts:4332` → `:4358` (line count claim 1159 unchanged — spot-check consistent with +28 lines above the call site). Live cross-device OUT OF SCOPE (`ph-P3-LIVE` / `R-REACH`) — correctly not claimed.
- **Safety / minimal-shape posture looks sound on read:** `readVerdictHeads` copies only documented fields and drops extras; tests strip `rawProviderText` / `secret` / `sourceBundles`; sync default `enabled=false` invokes no ports; webhook uses `DEFAULT_WEBHOOK_CONFIG.enabled=false`, exact `grantedOrigin`, HTTPS-only gate, `X-*` HMAC header restriction, fetch `redirect:'error'` / `credentials:'omit'` / `cache:'no-store'`; sync retries capped at 3 with durable attempting receipts; webhook one-POST max with ambiguous/no-auto-replay on interrupt/timeout; `finalizeBeforeReach` keeps reach best-effort after durable finalize so lease release is not stalled. Ported from approved-but-unmerged #53 head `2b8c40b8` onto master `b1bc0b55`.
- Provenance note (D2): content from `codex exec --sandbox workspace-write`; controller ran gate + staging/commit only (sandbox could not npm or linked-worktree index.lock). Treat as codex implementer; claude leg still required.
- No product-code blockers found beyond the docs-drift CI failure and unsettled remaining checks. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.