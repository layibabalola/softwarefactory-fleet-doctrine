schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 125
head: a9ab09c6e19a7a57eefd558046ca30e6bf929b97
reviewed_utc: 2026-09-23T08:56:46Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 35839943906 — all three required jobs IN_PROGRESS (docs drift, factory contracts, typecheck/lint/build/test); mergeable true / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `a9ab09c6`.** Actions run 35839943906 still has docs drift, factory contracts, and typecheck/lint/build/test IN_PROGRESS. Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `CI-PREFLIGHT-MEASURED-FILE` is `family: claude` (implementer: claude). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.

## Notes

- **pr-opened: queue-only filing PR.** Tip `a9ab09c6` is `CI-PREFLIGHT-MEASURED-FILE: file the measured-preflight row`. PR vs master: `factory/queue.jsonl` +2/−0, 1 file, ahead 1 / behind 0. Exclusive tip rows: `CI-PREFLIGHT-MEASURED` (order 274) and `CI-PREFLIGHT-MEASURED-FILE` (order 275). Body row token `CI-PREFLIGHT-MEASURED-FILE` only.
- **Filing VERIFY spot-check (from PR body + diff):** Base `c4986b95` (orders through 273 from PR 120). Appends 274/275; numstat +2/−0; 67→69 rows; no pre-existing row modified. Orders sit above master max (273) and the open-PR high-water cited in VERIFY (260 on PR 104). Self-filing pattern matches PR 113 / PR 120 (FILE delivers; MEASURED stays READY/unconsumed).
- **Local gate (author claim):** Full local gate GREEN exit 0, 1806 s, all 18 steps ok (`offline-browser ok`) from a 79-char checkout root on exact head `a9ab09c6`. Controllers should still wait for tip CI.
- **Filed defect (data only — not this PR's work):** `assertFixtureProfilePathBudget()` (merged in PR #124) predicts one IndexedDB worst-case path and advertises an unqualified "maximum supported checkout root" (modelled ceiling 115) while a Chrome Cache Storage file would be root+167…171 (true ceiling 88–92). Row fixes over-claim + predict-where-measureable without modelling Cache Storage as a second predicted path (REJECTED as unbounded chase). Cache Storage finding from PR 120 rounds is DISCHARGED in the filed spec (no Cache API call sites; PR #124 VERIFY (5) green from 105-char root past that ceiling). Spec owns only `fixture-page.ts` + `two-provider-two-round.test.ts`; `scripts/ci.ps1` not touched (outside high-risk). Implementation is a later row, not this PR.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
