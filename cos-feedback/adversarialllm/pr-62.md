schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 62
head: 00e207bbd193c691850368d646b1452c43e97b59
reviewed_utc: 2026-09-14T18:53:35Z
event: pr-opened
verdict: not-merge-ready
ci_summary: no checks reported on feat/guard-allow-doctrine-merge-bootstrap; mergeable=CONFLICTING (behind master by 4)

## Blockers

- **Merge conflict with `master`** — GitHub reports `mergeable: CONFLICTING` / `mergeStateStatus: DIRTY`. Compare: tip is ahead by 3 and behind by 4 vs `master`. Rebase or merge `master` into `feat/guard-allow-doctrine-merge-bootstrap`, resolve conflicts, and push before any merge.
- **Branch pollution / unrelated queue row** — first commit `2653c4c` and `factory/queue.jsonl` add `DOC-CLI-ROTATION-QS` (codex family, CLI credential rotation Quick Start). That is outside the stated guard/merge scope. Drop that commit/row from this PR or split to a separate PR so the bootstrap-guard fix stays reviewable alone.
- **Undeclared guard behavior in same hunk set** — `scripts/never-authorized-guard.mjs` also adds `isEvalFamilyDelivered()` and blocks Bash `npm run lint` / `eslint` / `vitest` / `tsc` until queue row `CTRL-3` is `DELIVERED`. PR title/body only describe adding `merge` to the bus allowlist. Either document and test that lint-gate change explicitly, or revert it from this PR.

## Improvements

- Stated allowlist fix looks sound: `foreignPathDeny` now permits `git -C <bus> merge` alongside `fetch|commit|push`, with a matching control in `scripts/never-authorized-guard.test.mjs` and denial copy updated to `fetch/commit/push/merge`. Keep `--ff-only` in the PROMPT A recipe so non-FF merges stay out of the happy path.
- Adjudication `factory/adjudications/PROMPT-A-bootstrap-guard-blocker.md` is useful fleet learning; after land, fold the trap text into bus `TRAPS.md` via a doctrine PR (do not treat this project adjudication as bus authority by itself).
- Once conflicts are cleared, ensure hosted CI (typecheck/lint/build/test + factory contracts if required) actually runs on the tip — currently `statusCheckRollup` is empty / `gh pr checks` reports none.

## Notes

- Scope claimed: unblock PROMPT A §1 bootstrap sync by allowing `git -C <bus> merge` (universal bootstrap blocker). Core allowlist + test delta is small (+15/−1 guard, +2/−1 test) plus adjudication (+112) and the unrelated queue line.
- Safety posture on the allowlist change: matches existing D12 bus precedent for `fetch|commit|push`; merge with `-C` to the bus is the intended bootstrap path. Guard remains defense-in-depth; pre-push / content validators stay the publish boundary.
- No GitHub PR comment posted. Cursor Cloud Agents not used (review-only CoS via `gh`).
