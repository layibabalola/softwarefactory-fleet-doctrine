schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 89
head: a41661c2d8ecc7ae4fee5a26a450a963e2c8083e
reviewed_utc: 2026-09-16T18:20:08Z
event: pr-opened
verdict: not-merge-ready
ci_summary: docs drift PASS; factory contracts PASS; typecheck/lint/build/test FAIL on run 35132604310 (1 timeout in observe-runner-contract)

## Blockers

- **CI: `typecheck / lint / build / test`** — FAIL on head `a41661c2d8ecc7ae4fee5a26a450a963e2c8083e` (Actions run 35132604310 / job 104917004582). Unit summary: 1 failed / 5079 passed / 1 skipped across 268 files. Sole failure: `tests/unit/observe-runner-contract.test.ts` — `PowerShell refuses unsupported modes and invalid grants before browser helpers: []` timed out at vitest default **5000ms** (`Test timed out in 5000ms` at line ~54). The `spawnSync(..., { timeout: 15000 })` never helps if vitest kills the case first. The empty `mode` case (`[]`, no `-NoLaunch`) takes the slower admission/refuse path under hosted load; `-NoLaunch` variants are fine. **Fix:** raise that `it.each` case timeout (e.g. 20s) and/or ensure empty-mode refuse exits before heavy work the same way `-NoLaunch` does; re-run the focused suite then full hosted CI on the new tip.

## Improvements

- Land independent Sol (and high-risk Claude) review on tip `a41661c2` before merge — PR body marks both mandatory; GitHub shows 0 reviews yet. Confirm H1-03 / H1-R provenance substitution and `buildLiveAdmissionEvidence` binding still hold on this head vs proposal `H1-R` / prior PR 50 terminal state.
- After the timeout fix, consider splitting or marking the empty-mode PowerShell case with an explicit longer timeout in the same style as the prior hosted fixture portability packet (evidence section in `adversarialllm/evidence/H1-offline-validation.md`) so under-load runners do not flake the gate again.

## Notes

- First CoS bus file for this PR. Tip `a41661c2` is `fix(eol): preserve mixed implementation progress bytes` after `H1-R: bind runtime evidence to live admission` (`caa21368`) on merged preserved H1 (`f63066aa`). Owned PR delta vs master: 23 files (+1123/−103) — live-authority/admission modules + tests, observe Playwright/PS entrypoint gates, CI registration of security suites, docs/evidence, queue bump, R-LIVE proposal.
- H1-R delta on top of integrated H1: `buildLiveAdmissionEvidence` copies origin/authority/proposal/prompt hashes, runId, receiptPath, and approved browser identity into success reports and startup-failure artifacts; ignores caller `OBSERVE_EVIDENCE_REPRO_RESERVATION_ID`. Offline only — no live activation claimed.
- Author claims exact local full CI GREEN (~1235s) on this SHA; hosted `factory contracts` and `docs drift` agree green; the remaining red is the PowerShell refuse timeout above, not typecheck/lint/build compile.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: created on `master` at `cos-feedback/adversarialllm/pr-89.md`.