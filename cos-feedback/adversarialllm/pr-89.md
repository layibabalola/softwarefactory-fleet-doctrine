schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 89
head: 5821980e63ad66ae875e65d3c0a8bab8c0849987
reviewed_utc: 2026-09-16T19:47:01Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: docs drift PASS; factory contracts PASS; typecheck/lint/build/test FAIL on run 35141746147 (3 ENOENT mkdtemp fixtures in observe-runner-contract)

## Blockers

- **CI: `typecheck / lint / build / test`** — FAIL on head `5821980e63ad66ae875e65d3c0a8bab8c0849987` (Actions run 35141746147 / job 104947681125). Unit summary: 3 failed / 5084 passed / 1 skipped across 268 files. All three failures are new H1 admitted-failure fixtures in `adversarialllm/tests/unit/observe-runner-contract.test.ts` under `H1 approved local browser launch`:
  - `records prompt and artifact-initialization failures beside the consumed reservation without browser work`
  - `fails terminally when fallback evidence cannot be written and preserves the original cause and reservation`
  - `retains immutable admitted binding for a richer post-launch failure receipt`
  Root cause: each snippet does `fs.mkdtempSync(path.join(path.resolve('../.factory-local'), 'h1-…'))` without creating the parent. Fresh hosted checkouts have no `.factory-local/`, so `mkdtemp` throws `ENOENT`. Same portability class as the earlier hosted fixture packet. **Fix:** `fs.mkdirSync(path.resolve('../.factory-local'), { recursive: true })` (or equivalent) before each `mkdtempSync` in those three snippets; re-run focused `observe-runner-contract` then full hosted CI on the new tip.

## Improvements

- Prior CoS blocker (PowerShell refuse empty-mode vitest 5s timeout) is **addressed** on this tip: the `it.each` case and sibling override-refusal cases now use `, 20_000)` and those cases passed under hosted load.
- Land independent Sol (and high-risk Claude) review on tip `5821980e` before merge — PR body still marks both mandatory; GitHub shows 0 reviews. Confirm H1-R provenance substitution plus new `executeAdmittedObserve` / `writeAdmissionFallbackFailure` binding on this head vs proposal `H1-R`.

## Notes

- Push `fix(H1): preserve admitted failure evidence` (`5821980e`) on prior tip `a41661c2`. Adds `normalizeAdmissionFailure`, `writeAdmissionFallbackFailure`, and `executeAdmittedObserve` so pre-launch failures write `${receiptPath}.observe-failure.json` with exact admission hashes/runId/receipt/browser identity; removes unused `launchObserveContext` / `resolveManagedObserveBrowserExecutable` (single `launchPersistentContext` path). Timeout raise applied as above.
- Verdict stays `not-merge-ready` because the new failure-evidence tests themselves red on hosted CI; content direction looks right once the fixture parent mkdir lands.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: rewrote `cos-feedback/adversarialllm/pr-89.md` on `master` for this head.