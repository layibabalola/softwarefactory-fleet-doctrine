schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 89
head: 9eb5ca0cd438041734ae8e88ef3d187f7a3c5766
reviewed_utc: 2026-09-16T20:16:40Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on run 35145446259 (head 9eb5ca0); prior tip 5821980 unit red (3 ENOENT) addressed by this tip

## Blockers

(none content-side). Wait for hosted `factory contracts` and `typecheck / lint / build / test` on tip `9eb5ca0` (Actions run 35145446259) before merge. If either reds, fix and re-push.

## Improvements

- Prior CoS blocker (3 hosted ENOENT `mkdtemp` fixtures under `H1 approved local browser launch`) is **addressed** on this tip: each of the three admitted-failure snippets now `mkdirSync(..., { recursive: true })` on a dedicated fixture parent under `.factory-local/` before `mkdtempSync`. Confirm those three cases pass on hosted run 35145446259.
- Prior CoS note (PowerShell refuse empty-mode vitest 5s timeout) remains addressed from tip `5821980` (20s timeouts).
- Land independent Sol (and high-risk Claude) review on tip `9eb5ca0` before merge — PR body still marks both mandatory; GitHub shows 0 reviews. Confirm H1-R provenance binding plus `executeAdmittedObserve` / `writeAdmissionFallbackFailure` / fixture-parent portability on this head vs proposal `H1-R`.

## Notes

- Push `test(H1): create clean-checkout fixture parent` (`9eb5ca0`) on prior tip `5821980`. Owned delta is only `adversarialllm/tests/unit/observe-runner-contract.test.ts` (+12/−3): three fixture parents (`h1-admitted-failure-fixtures`, `h1-evidence-write-fixtures`, `h1-post-launch-fixtures`) with rm+mkdir before mkdtemp. Same portability pattern as the earlier hosted launch-race packet.
- Verdict moves `not-merge-ready` → `merge-when-ci-green`: the content/CI fixture blocker from the prior bus file is fixed in source; remaining gate is settling hosted CI on this tip.
- Scope unchanged: H1-R offline evidence binding, no live provider activation. Author still claims exact full CI GREEN on earlier tip `a41661c2` (1,235s) with ownership Seal.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: rewrote `cos-feedback/adversarialllm/pr-89.md` on `master` for this head.