schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 143
head: a74253a881ed811e3791c9c8df19313d48911ad3
reviewed_utc: 2026-09-25T13:52:28Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36143647136 — typecheck/lint/build/test IN_PROGRESS; factory contracts + docs drift QUEUED (self-hosted); MERGEABLE/UNSTABLE; ahead 2 / behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `a74253a8`.** Actions run 36143647136 just started; typecheck/lint/build/test is in progress and the other two required jobs are queued. Re-check before merge.
- **High-risk gate change: two independent review legs still required (D2).** Author packet explicitly requires current-head/base Codex and Claude reviews before merge. Conductor/controller must still dispatch non-author legs.
- **Confirm hosted CI exercises the new `p8-artifact-build` step.** Local receipt claims full gate GREEN (855s, supervisor exit 0) on this tip with `.factory-local/ci-a74253a8-a3.receipt.json`; hosted path should show the fresh-output build + unit bind, not only the later `npm run build`.

## Notes

- **What changed:** CI-GATE-HERMETIC. P8 previously scanned leftover `output/` (or passed with nothing to scan). Gate now builds into a unique per-run dir under `.factory-local/p8-artifacts-<guid>` via new `Invoke-P8Unit`, sets `ADVLLM_P8_OUTPUT_DIR`, runs unit against that tree, and restores prior env in `finally`. Missing `chrome-mv3/manifest.json` fails closed. Normal `npm run build` for browser checks is preserved.
- **Owned scope:** +93/−12 across 2 files, 2 commits (`9d23c150` fix + `a74253a8` merge master `85d712c5`). Files: `scripts/ci.ps1`, `adversarialllm/tests/unit/p8-ship-posture.test.ts`. No source/extension/lockfile changes.
- **Regression coverage:** new describe extracts production `Invoke-P8Unit` through PowerShell AST and covers stale forbidden loose/ZIP, absent prior output, forbidden current content, build failure, and env restoration. Existing ship-posture scan now reads `ADVLLM_P8_OUTPUT_DIR || output`.
- **Spot-check:** try/finally restores `ADVLLM_P8_OUTPUT_DIR`; GUID dir collision throws; ZIP entries scanned with same forbidden regex as loose files; dual build (P8 wxt outDir then full npm build) matches stated intent.
- **Intentional cost:** gate now pays an extra WXT build before unit for hermetic P8 provenance. Author accepts that vs scanning stale artifacts.
- No secrets in diff. Review via `gh`. No GitHub PR comment posted.
