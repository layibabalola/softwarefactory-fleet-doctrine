schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 100
head: c70dedeffdef4d81104f3d893fc90c2cb655ba1b
reviewed_utc: 2026-09-18T04:11:56Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS; master tree proof correctly SKIPPED on pull_request; factory contracts + typecheck/lint/build/test still pending on run 35305875015; mergeable MERGEABLE; 0 GitHub reviews on tip

## Blockers

(none for content — push-only `master tree proof` job plus `needs`/`if` skip gates on the two heavy jobs look fail-open and PR-safe; required check names and pull_request path preserved)

## Improvements

- **Wait for remaining CI** — confirm `factory contracts` and `typecheck / lint / build / test` PASS on head `c70dedef` before merge (docs drift already green; proof job correctly skipped on this PR event).
- **Local gate before D2** — authoring session could not run `pwsh scripts/ci.ps1` (never-authorized guard). A controller or review leg should run it on this head and record the result before D2.
- **High-risk D2 (point 8)** — this edits `.github/workflows/ci.yml`. Needs one leg per family and a literal APPROVE from a non-authoring leg; author has not merged. Do not treat CoS verdict as that APPROVE.
- **Post-merge verify** — on the first master push after merge (when the PR head contained master): `master tree proof` should succeed, both heavy jobs should skip, and the workflow conclusion must stay `success` so tripwire / D2 VERIFY / resume probes keep working. If the merge was stale-base, expect a full suite instead.

## Notes

- Scope: +144/−0 across 3 paths — `.github/workflows/ci.yml` (+43), new `factory/adjudications/CI-COST-20260917.md` (+98), three queue rows `CI-COST-1` (order 248) plus deferred `CI-COST-2` / `CI-COST-3` (249–250).
- Design: on `push` only, prove `tree(master)==tree(merged PR head)` and newest `pull_request` CI run on that head concluded `success`; only then skip `factory contracts` and `typecheck / lint / build / test`. API errors / missing PR / tree mismatch leave `proven=false` and run the full suite. Replay claim: 22/25 last master SHAs proven, 3 stale-base full runs — matches stated tree analysis. Estimated ~$11–13/month; rejects removing `push: master` (tripwire + untested stale merges).
- Live wiring check on this PR: proof job skipped, heavy jobs still queued/running under `needs: master-proof` + `if: !cancelled() && needs.master-proof.outputs.proven != 'true'` — matches the intended PR path.
- Contract claim: `node --test factory/ci-contract.test.mjs` 23/23 before and after with test file unchanged (pinned job names, concurrency, cancel-in-progress, node 22×3, go-version, windows-latest, fetch-depth 0, run strings).
- Branch `feat/CI-COST-1` → `master`; tip `c70dedef`; author layibabalola (co-authored Claude Opus 5); 0 reviews.
- Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.
- Doctrine bus: create `cos-feedback/adversarialllm/pr-100.md` on master (first CoS pass for this PR).