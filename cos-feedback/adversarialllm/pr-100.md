schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 100
head: fb17a3667847b2d8429ae6117dc64cdba072e3e7
reviewed_utc: 2026-09-18T21:39:03Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS on tip; master tree proof correctly SKIPPED on pull_request; factory contracts + typecheck/lint/build/test IN_PROGRESS on run 35397771385; mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews on tip

## Blockers

(none for content — tip `fb17a366` is a CR-byte hygiene amend so the r2 contract-test insertion stays a pure +70/−0 delta; required check names and pull_request path preserved; fail-open toward full suite)

## Improvements

- **Wait for remaining CI** — confirm `factory contracts` and `typecheck / lint / build / test` PASS on head `fb17a366` before merge (docs drift already green; proof job correctly skipped on this PR event).
- **Local gate on this tip** — body claims `pwsh scripts/ci.ps1` GREEN on earlier tip `d4ee567c` (1092s). Re-run on `fb17a366` (or confirm the new contract test + workflow + CR restore were covered) before D2 APPROVE.
- **High-risk D2 (point 8)** — still edits `.github/workflows/ci.yml`. Needs one leg per family and a literal APPROVE from a non-authoring leg on **this** tip; author has not merged. Do not treat CoS verdict as that APPROVE. Round-2 already folded Codex r1 F-01 MUST (same-branch base bind) and Claude r1 F-01/F-02 SHOULD (shadowed-gh regression test); this tip only restores the two base CR bytes the line-ending delta gate requires.
- **Post-merge verify** — on the first master push after merge (when the PR head contained master): `master tree proof` should succeed, both heavy jobs should skip, and the workflow conclusion must stay `success` so tripwire / D2 VERIFY / resume probes keep working. If the merge was stale-base, expect a full suite instead.

## Notes

- Tip delta vs prior reviewed head `ce8e2b07`: only `factory/ci-contract.test.mjs` — restore CR on blank line 528 and on the file's final line so the new proof-step regression test is a pure insertion after line 528 (ordinary and CR-normalized numstat both 70 0). No workflow / adjudication / queue change on this push.
- Design unchanged: on `push` only, prove tree match + newest same-branch `pull_request` CI success + pre-merge tip ancestor of head; only then skip heavy jobs. API errors / missing PR / tree mismatch / non-ancestor / other-base leave `proven=false` and run the full suite. Estimated ~$11–13/month; rejects removing `push: master`.
- Replay claim unchanged: 22/25 proven, 3 full runs (`73fc4191` #66, `ab121fd7` #47, `fbdfda4f` #46).
- Live wiring check on this PR: proof job skipped; docs drift PASS (CR restore achieved its purpose); heavy jobs still running under `needs: master-proof` + proven≠true `if`.
- Branch `feat/CI-COST-1` → `master`; tip `fb17a366`; author layibabalola (co-authored Claude Opus 5); 0 reviews; +277/−0 across 4 paths on the full PR.
- Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.
- Doctrine bus: rewrite `cos-feedback/adversarialllm/pr-100.md` on master for new head (prior file was for `ce8e2b07`).
