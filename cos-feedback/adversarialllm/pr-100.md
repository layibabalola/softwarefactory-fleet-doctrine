schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 100
head: ce8e2b07ff2eb140460a19b60a430400e0023906
reviewed_utc: 2026-09-18T21:20:07Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS; master tree proof correctly SKIPPED on pull_request; factory contracts + typecheck/lint/build/test IN_PROGRESS on run 35396047968; mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews on tip

## Blockers

(none for content — round-2 same-branch base binding + regression test close Codex/Claude D2 r1 F-01; required check names and pull_request path preserved; fail-open toward full suite)

## Improvements

- **Wait for remaining CI** — confirm `factory contracts` and `typecheck / lint / build / test` PASS on head `ce8e2b07` before merge (docs drift already green; proof job correctly skipped on this PR event).
- **Local gate on this tip** — body claims `pwsh scripts/ci.ps1` GREEN on prior tip `d4ee567c` (1092s). Re-run on `ce8e2b07` (or confirm the new contract test + workflow delta were covered) before D2 APPROVE.
- **High-risk D2 (point 8)** — still edits `.github/workflows/ci.yml`. Needs one leg per family and a literal APPROVE from a non-authoring leg on **this** tip; author has not merged. Do not treat CoS verdict as that APPROVE. Round-2 already folded Codex r1 F-01 MUST (same-branch base bind) and Claude r1 F-01/F-02 SHOULD (shadowed-gh regression test).
- **Post-merge verify** — on the first master push after merge (when the PR head contained master): `master tree proof` should succeed, both heavy jobs should skip, and the workflow conclusion must stay `success` so tripwire / D2 VERIFY / resume probes keep working. If the merge was stale-base, expect a full suite instead.

## Notes

- Round-2 delta vs prior tip `d4ee567c`: workflow adds `BRANCH` env + `commits/$head/pulls` base.ref uniqueness check (`bases == BRANCH`); adjudication documents the binding (merged runs have empty `pull_requests`, so PR-number bind is impossible) and appends the verbatim 25-SHA replay transcript; `factory/ci-contract.test.mjs` gains one test that extracts the step script and proves positive / other-base / red-newest / stale-tree / non-ancestor compare / API-fail paths; queue `CI-COST-1` VERIFY now owns that append-only test. Full PR now +279/−2 across 4 paths.
- Design (unchanged except base bind): on `push` only, prove tree match + newest `pull_request` CI success + pre-merge tip ancestor of head + every PR on that head targets the pushed branch; only then skip heavy jobs. API errors / missing PR / tree mismatch / non-ancestor / other-base leave `proven=false` and run the full suite. Estimated ~$11–13/month; rejects removing `push: master`.
- Replay claim unchanged: 22/25 proven, 3 full runs (`73fc4191` #66, `ab121fd7` #47, `fbdfda4f` #46).
- Live wiring check on this PR: proof job skipped; heavy jobs still running under `needs: master-proof` + proven≠true `if` — matches intended PR path.
- Branch `feat/CI-COST-1` → `master`; tip `ce8e2b07`; author layibabalola (co-authored Claude Opus 5); 0 reviews.
- Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.
- Doctrine bus: rewrite `cos-feedback/adversarialllm/pr-100.md` on master for new head (prior file was for `d4ee567c`).