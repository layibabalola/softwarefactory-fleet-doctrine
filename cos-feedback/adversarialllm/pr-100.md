schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 100
head: d4ee567c558ba358b667882f1b5818641ab1c023
reviewed_utc: 2026-09-18T19:55:03Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS; master tree proof correctly SKIPPED on pull_request; factory contracts + typecheck/lint/build/test still pending on run 35388436935; mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews on tip

## Blockers

(none for content — round-1 ancestor guard on the push-only proof is fail-open and PR-safe; required check names and pull_request path preserved)

## Improvements

- **Wait for remaining CI** — confirm `factory contracts` and `typecheck / lint / build / test` PASS on head `d4ee567c` before merge (docs drift already green; proof job correctly skipped on this PR event).
- **Local gate before D2** — authoring session could not run `pwsh scripts/ci.ps1` (never-authorized guard). A controller or review leg should run it on this head and record the result before D2.
- **High-risk D2 (point 8)** — this edits `.github/workflows/ci.yml`. Needs one leg per family and a literal APPROVE from a non-authoring leg; author has not merged. Do not treat CoS verdict as that APPROVE.
- **Post-merge verify** — on the first master push after merge (when the PR head contained master): `master tree proof` should succeed, both heavy jobs should skip, and the workflow conclusion must stay `success` so tripwire / D2 VERIFY / resume probes keep working. If the merge was stale-base, expect a full suite instead.

## Notes

- Round-1 delta vs prior tip `c70dedef`: +10/−0 across `.github/workflows/ci.yml` (+5) and `factory/adjudications/CI-COST-20260917.md` (+5). Proof now also requires `compare(pre-merge-parent...PR-head)` status `ahead` or `identical`, so a green PR run that only tested `refs/pull/N/merge` against a later-reverted base cannot prove the head alone. Replay claim unchanged: 22/25 proven, 3 full runs (`73fc4191`, `ab121fd7`, `fbdfda4f`).
- Full PR scope still +154/−0 across 3 paths — workflow (+48), adjudication (+103), queue rows `CI-COST-1`/`CI-COST-2`/`CI-COST-3`.
- Design (unchanged except ancestor gate): on `push` only, prove tree match + newest `pull_request` CI success + pre-merge tip ancestor of head; only then skip heavy jobs. API errors / missing PR / tree mismatch / non-ancestor leave `proven=false` and run the full suite. Estimated ~$11–13/month; rejects removing `push: master`.
- Live wiring check on this PR: proof job skipped; heavy jobs still running under `needs: master-proof` + proven≠true `if` — matches intended PR path.
- Branch `feat/CI-COST-1` → `master`; tip `d4ee567c`; author layibabalola (co-authored Claude Opus 5); 0 reviews.
- Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.
- Doctrine bus: rewrite `cos-feedback/adversarialllm/pr-100.md` on master for new head (prior file was for `c70dedef`).