schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 73
head: 724a954919b7fa5b8c9ac29036329c6a862e559f
reviewed_utc: 2026-09-16T00:20:20Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35039463423; typecheck/lint/build/test + factory contracts IN_PROGRESS on head 724a9549; author claims exact-head local full CI GREEN exit0 792s vs base 5c23636e

## Blockers

- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on head `724a954919b7fa5b8c9ac29036329c6a862e559f` (Actions run 35039463423). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Process: independent dual-family D2 on this head** — 0 GitHub reviews yet; PR body requires fresh Sol/Codex and Sonnet/Claude reviews plus all three hosted checks on the *current* head/base (`724a9549` / `5c23636e`) before merge. Land those after hosted CI settles.

## Improvements

- `wait-activation.ts` (~348 lines) and `wait-failure.ts` (~187 lines) have no dedicated unit files while the other seven extracted phases do. Add focused tests for lease-skip / deferred retry / mid-wait heartbeat cancel on activation, and for the main failure-classification branches in `buildWaitFailure`.
- Circular type edge (same as P2.5a): all nine new modules import `DispatchOneDeps` from `dispatch-one.ts` while `dispatch-one.ts` imports their runners. Prefer a tiny shared deps type (or `Pick<>` in a neutral module) before the next extract grows the cycle further.
- `blocked-quota-recovery.ts`, `foreground-recovery.ts`, and `mid-sentence-recovery.ts` each landed ~550 lines with move-style indentation still visible in places (e.g. extra indent inside `runBlockedQuotaRecovery`). A follow-up pass to normalize formatting and optionally split the largest recovery loops would make later audits cheaper; not merge-blocking.

## Notes

- First CoS bus file for this PR. Tip `724a954919b7fa5b8c9ac29036329c6a862e559f` is `P2.5c: preserve original docs03 line ending` after the extract commit `94e45c88`. Owned delta vs base `5c23636e`: 21 files (+3518/−2855). `dispatch-one.ts` shrinks ~−2844 lines; nine new WAIT/recovery modules plus seven phase unit tests and scoped docs/architecture/adapters updates.
- Call order on the extract looks correct for the claimed contract: `startWaitActivations` → `runWaitRetry` → `extractWaitResponse` → `runForegroundRecovery` → `runMidSentenceRecovery` → `runTerminalTruncationRecovery` → `tryTimeoutPromotion` → `runBlockedQuotaRecovery` → `buildWaitFailure`. `dispatchToOne` keeps the 16-argument signature, outbox/send acceptance, generation ack, and later success-path quality/response build. No live provider activation claimed.
- New unit coverage pins wait-retry delays/tab rebound/abort fencing, wait-response decoding/hydration, foreground lease completion, mid-sentence / truncation recovery epochs, timeout partial preservation, and Kimi conversation-quota precheck. Author reports local `orchestrator-echo` + `content-handlers` oracles green (1304 passed, 1 skipped) and exact-head full CI GREEN; docs03 CRLF repair made the line-endings gate GREEN on this tip. Hosted `docs drift (delta gate)` already PASS (~25s).
- Offline source refactor only. Dual-family D2 still required per PR body. Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: created on `master` at `cos-feedback/adversarialllm/pr-73.md`.