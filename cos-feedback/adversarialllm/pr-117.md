schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 117
head: afd4db9370a3818f5df5d152fcbeb906a897e865
reviewed_utc: 2026-09-21T01:45:15Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS (CI run 35551867312)

## Blockers

(none)

## Improvements

- **Two-leg gate still open on this head.** PR body: fresh attempt with its own two-round cap; needs two independent legs. GitHub Reviews API returns zero formal review objects at review time — dispatch/record both families against `afd4db93` before merge.
- After legs land, confirm `queue-state` simulation still shows `ph-P3` SUPERSEDED_PENDING waiting on both `ph-P3-R` and `ph-P3-LIVE`, and `ph-P3-LIVE` WAITING_DEPENDENCY on `[ph-P3-R, R-LIVE, R-REACH]` (never READY before `R-REACH`).

## Notes

- Successor to adj-closed #116 (sustained MUST `LIVE-UNMODELED-AUTHORITY-DEPENDENCY`; `LIVE-DISTINCT-DEVICES` overruled). Cure follows H1→R-LIVE: new `ph-P3-LIVE-PROP` (262, claude) writes `factory/proposals/R-REACH.md`; `R-REACH` (263, codex) ratifies; `ph-P3-LIVE` (264) now `dependsOn: [ph-P3-R, R-LIVE, R-REACH]`.
- Compare `master...afd4db93`: ahead_by 1, `factory/queue.jsonl` +6/−1 (numstat confirmed). Spot-check: 61 rows; `PH-P3-FILE-R2` present (order 265); `ph-P3.supersededBy` = `[ph-P3-R, ph-P3-LIVE]`; no duplicate orders.
- Device clause carried forward intact (tier overruled as not-a-defect): DISTINCT NAMED DEVICES; VMs/containers/accounts/profiles on one physical host excluded; evidence contract `R-REACH-E1-v1` to carry the dual physical-host assertion.
- Filing VERIFY contract: base-to-head numstat via `git diff --numstat` against base SHA; body row token `PH-P3-FILE-R2` resolves at committed head.
- Scope remains queue-only; no `src`/tests/workflows on the tip. Cursor Cloud Agent not used; review via `gh`. No GitHub PR comment posted.