schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 136
head: a236b86090f83714ce9a3fdaf22cbe0a011902aa
reviewed_utc: 2026-09-25T11:59:39Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 36132325599 — docs drift IN_PROGRESS; factory contracts + typecheck/lint/build/test QUEUED (self-hosted / CI_RUNS_ON); prior tip 1e0de890 run 36120600249 SUCCESS (Improvement cleared); MERGEABLE/UNSTABLE; ahead 9 / behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `a236b860`.** Actions run 36132325599 just queued on the self-hosted runner after this remediation push. Prior tip `1e0de890` run 36120600249 finished SUCCESS — that wait is cleared — but do not merge until the new tip run succeeds.
- **Local root gate still not evidenced on this lineage.** VERIFY clause (6) wants `pwsh scripts/ci.ps1` green (or an equivalent-tree green). Controller should record it.
- **Two independent review legs still required.** Author notes D2 needs two legs and the author does not merge. Conductor/controller tasks remain Disabled per the handoff memory entry — a non-author controller session must dispatch legs.

## Notes

- **What changed vs prior CoS head `1e0de890`:** one new commit `a236b860` — round-2 remediation of `classify-a-red-gate-before-re-running.md` (+7/−4). Drops the job-split reading ("the GitHub-hosted jobs still start from a clean image") and states cleanliness per runner: hosted = fresh image; self-hosted = reused workspace; routing decided by `CI_RUNS_ON` + each job's `runs-on` in `.github/workflows/ci.yml`. Asserts no current job split. Other owned paths unchanged.
- **Prior Improvement addressed:** tip CI green on `1e0de890` (run 36120600249 SUCCESS). New tip reopens the wait.
- **Prior CoS Note corrected:** at `1e0de890` CoS said the CI_RUNS_ON premise phrasing "stays correct"; a round-1 Claude leg correctly found the "GitHub-hosted jobs still…" sentence still read as a current job split after #137 routed all three jobs. This tip is the fix.
- **Scope (PR vs master):** +128/−0 across 7 files — three memory entries, INDEX/MEMORY pointers, one appended `factory/queue.jsonl` row `MEM-R4` at `order: 322`. No code, test, gate, or workflow changes owned by this PR.
- **Queue hygiene:** master has zero `order: 322` / `MEM-R4`; only open PR 136 carries `order: 322`.
- **Related:** product-first ledger issue #130; theme continues #128/#137/#140. No secrets in diff. Review via `gh`. No GitHub PR comment posted.
