schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 136
head: d964adfff7c1b79708000ac0bd6ceda38347012f
reviewed_utc: 2026-09-25T05:21:07Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 36098131998 — all three jobs QUEUED (factory contracts, typecheck/lint/build/test, docs drift); MERGEABLE/UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `d964adff`.** Actions run 36098131998 has all three required jobs QUEUED. After #137/#138 landed, `CI_RUNS_ON` routes all three to the self-hosted Windows runner (ultra-magnus), so a long queue usually means the runner is busy or offline rather than a hosted backlog. Do not merge until all three succeed.
- **Local root gate not evidenced on this head.** PR verification shows `validateRows OK` and order-322 uniqueness, but does not record `pwsh scripts/ci.ps1` green on `d964adff`. Controller should run the root local gate (or confirm a prior green on an equivalent tree) before merge; VERIFY clause (6) requires it.
- **Two independent review legs still required.** Author notes D2 needs two legs and the author does not merge. Conductor/controller tasks remain Disabled per the handoff memory entry — a non-author controller session must dispatch legs.

## Notes

- **What changed vs prior CoS head `61e9cf0b`:** one new commit `d964adff` — merge master `2074c90b` (--no-ff) and move `MEM-R4` from `order: 321` to `order: 322` because #137 (`CI-SELF-HOSTED-LINUX`) merged first and took 321 on master. Memory entry bytes and ownership scope unchanged vs prior tip.
- **Scope (PR vs master):** +125/−0 across 7 files — three new memory entries, INDEX/MEMORY pointers, one appended `factory/queue.jsonl` row `MEM-R4` at `order: 322`. No code, test, gate, or workflow changes. Ahead of master by 6 commits, behind 0.
- **Queue hygiene:** CoS scan — master has zero `order: 322` / `MEM-R4`; open PR heads scanned (135, 129, 112, 107, 106, 104, 99, 98, 97, 90, 84, 77, 69, 49, 44) have zero `order: 322`. Tip has exactly one. Prior order-321 collision with open #137 is cleared (#137 merged).
- **CI_RUNS_ON premise:** memory entry phrasing stays correct (per-job: a self-hosted routed job does not start clean). PR body controller note correctly flags that the earlier claim "`ci`/`docs drift` still on ubuntu-latest" is stale after #137/#138; the entry itself does not need a further edit for that.
- **Related:** product-first ledger issue #130; theme continues #128/#137 self-hosted routing. No secrets in diff. Review via `gh`. No GitHub PR comment posted.