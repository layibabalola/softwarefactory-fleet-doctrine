schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 136
head: 61e9cf0b4273b624c45950f682cb02727b314755
reviewed_utc: 2026-09-25T02:18:04Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36085597268 — docs drift (delta gate) SUCCESS; typecheck / lint / build / test IN_PROGRESS; factory contracts queued (self-hosted Windows via CI_RUNS_ON); MERGEABLE/UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `61e9cf0b`.** Actions run 36085597268 still has typecheck/lint/build/test IN_PROGRESS and factory contracts queued on the self-hosted Windows runner (`CI_RUNS_ON` → ultra-magnus). Do not merge until all three required jobs succeed. If factory contracts stays queued, check that the self-hosted runner is online — a required job pinned there queues rather than fails while the runner is offline (the premise this PR documents).
- **Local root gate not evidenced on this head.** PR verification shows `validateRows OK` and order-321 uniqueness, but does not record `pwsh scripts/ci.ps1` green on `61e9cf0b`. Controller should run the root local gate (or confirm a prior green on an equivalent tree) before merge; VERIFY clause (6) requires it.
- **Two independent review legs still required.** Author notes D2 needs two legs and the author does not merge. Conductor/controller tasks remain Disabled per the new handoff memory entry — a non-author controller session must dispatch legs.

## Notes

- **Scope (PR vs master):** +125/−0 across 7 files — three new memory entries (`classify-a-red-gate-before-re-running.md`, `handoff-requires-a-live-mechanism.md`, `product-first-operating-practice-2026-09-25.md`), their INDEX.md + MEMORY.md pointers, and one appended `factory/queue.jsonl` row `MEM-R4` at `order: 321`. No code, test, gate, or workflow changes. Diff matches claimed ownership.
- **CI_RUNS_ON premise correction:** `classify-a-red-gate-before-re-running.md` "How to apply" now distinguishes GitHub-hosted clean-image jobs (`ci`, `docs drift`) from the self-hosted `factory contracts` job routed by repo var `CI_RUNS_ON` (landed with #128). File stays at 39 lines (cap 45). Correction is accurate vs post-#128 routing.
- **Queue hygiene:** CoS scan of open PR heads found `order: 321` only on #136; master has zero hits. Body carries exactly one row token `MEM-R4`. Frontmatter on all three entries looks valid (name, description, metadata.type).
- **Related:** product-first practice ledger is issue #130; theme continues #128 CI-SELF-HOSTED-WIN. Merge commit `28b9ef85` absorbed master `e7a3a563` (--no-ff). No secrets/credentials in diff. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
