schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 110
head: d06500f65ea98cd404d18975b82916249f458128
reviewed_utc: 2026-09-19T05:07:54Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts and typecheck/lint/build/test still pending on run 35423068153; mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews

## Blockers

(none for content — ph-P2-R3 delivers offline scheduling + schema-complete ferry terminal validation; fail-closed malformed/incomplete ReportModel and immutable first-writer settlement look sound on tip `d06500f6`)

## Improvements

- **Wait for CI** — confirm `factory contracts` and `typecheck / lint / build / test` PASS on head `d06500f65ea98cd404d18975b82916249f458128` before merge (body claims local focused 129 PASS, companion `go test ./...` PASS, and exact-base `scripts/ci.ps1` GREEN in 1174s on this tip; hosted still running).
- **Operator expectation** — `mayLaunch` returns `unsupported` whenever skipUnauthed / battery / quietHours / alertOnFinish are set until host-authoritative preflight exists. That is intentional fail-closed, not a missing feature in this packet; keep it in release notes so schedule authors do not expect those guards to arm.
- **Scope boundary** — treat this as candidate/offline proof only (matches docs + `evidence/ph-P2-scheduling.md`). Do not read merge as signed deployment, live provider auth, or OS-task scheduling.

## Notes

- Successor to capped PR #108 on separately authorized row `ph-P2-R3` (branch `codex/ph-P2-R3` → `master`; implementer Codex). Tip delta +1493/−37 across 26 files.
- Core surfaces: `settleFerriedTerminal` / `validReportModel` in `src/shared/db.ts` (reject before txn; agreement scores bounded to unit interval; empty/incomplete verdict and bad schemaVersion fail closed); `projectFerriedResult` identity/outcome match; `settleAndPublishScheduledFerry` publishes only after `applied`; schedule store/runner/editor default-off behind `scheduleEditor` + `scheduleExecutionEnabled` + dedicated autodrive + paired native; at-most-once transactional claims on separate `adversarialllm-schedules` IDB.
- Only `COMPLETE` maps campaign FSM to success; `PARTIAL`/`FAILED`/`BLOCKED`/`SKIPPED` map to ERROR while the report retains the precise outcome. Exact redelivery → `identical-duplicate`; differing redelivery → durable conflict (cap 16) without mutating authority.
- Tests added/extended: `db-ferry-terminal.test.ts`, `schedule-journey.test.ts`, `native-host-client-ferry.test.ts`, `p2-ui.test.ts`, `schedule-spec.test.ts`. Evidence: `adversarialllm/evidence/ph-P2-scheduling.md`.
- Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.