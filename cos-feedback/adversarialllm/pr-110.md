schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 110
head: 671b225d9dd47e070d96ee37fce9e7f0579a2497
reviewed_utc: 2026-09-19T06:03:13Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: all three required checks FAILED on run 35425492696 (~3–4s) because GitHub Actions refused to start jobs (account payments failed or spending limit); prior tip d06500f6 was fully GREEN on run 35423068153; mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews

## Blockers

- **GitHub Actions billing / spending limit** — run 35425492696 annotations: "The job was not started because recent account payments have failed or your spending limit needs to be increased." Affects `docs drift (delta gate)`, `factory contracts`, and `typecheck / lint / build / test` on head `671b225d9dd47e070d96ee37fce9e7f0579a2497`. Unblock under GitHub **Settings → Billing & plans** (fix payment or raise Actions spending limit), then re-run the failed workflow or push a no-op so required checks can actually execute on this tip. This is not a code failure.

## Improvements

- **Re-prove this tip after billing** — parent tip `d06500f65ea98cd404d18975b82916249f458128` was GREEN (docs + factory contracts + typecheck/lint/build/test). New commits since then are small (ingress validation + inventory anchors/line endings). After billing is fixed, confirm the same three checks PASS on `671b225d9dd47e070d96ee37fce9e7f0579a2497` before merge.
- **Operator expectation** — `mayLaunch` returns `unsupported` whenever skipUnauthed / battery / quietHours / alertOnFinish are set until host-authoritative preflight exists. Intentional fail-closed; keep in release notes so schedule authors do not expect those guards to arm.
- **Scope boundary** — candidate/offline proof only (docs + `evidence/ph-P2-scheduling.md`). Merge is not signed deployment, live provider auth, or OS-task scheduling.

## Notes

- Push delta vs prior CoS head `d06500f6`: three commits — `fix(scheduling): validate ferry reports before database access` (`d216698e`), inventory line-ending preserve (`fd51d5da`), inventory anchor refresh (`671b225d`). Files: `src/shared/db.ts` (+`loadFerriedCampaignAfterValidation` — `validReportModel` + campaignId/runOutcome match before any `getCampaign` load), `src/entrypoints/background.ts` (ferry ingest uses that helper; malformed → `FERRY_INGEST_REJECTED` with no DB read), `tests/unit/db-ferry-terminal.test.ts` (empty/incomplete verdict and missing/unsupported schemaVersion refuse before `load` is called), `docs/28_IMPLEMENTATION_PROGRESS.md` anchors.
- Content posture unchanged and slightly stronger: production ingress now rejects incomplete ReportModel before IndexedDB, matching the PR title/body claim. Prior CoS content blockers remain none.
- Full PR still +1524/−38 across 26 files on `codex/ph-P2-R3` → `master` (row `ph-P2-R3`, implementer Codex; successor to capped #108). Only `COMPLETE` maps campaign FSM to success; non-success terminals retain precise report outcome. Evidence: `adversarialllm/evidence/ph-P2-scheduling.md`.
- Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.