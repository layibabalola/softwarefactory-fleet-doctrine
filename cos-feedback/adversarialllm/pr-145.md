schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 145
head: 9067e47e8fcd6bb09a21bbb3c376b08f32658741
reviewed_utc: 2026-09-25T16:10:50Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36158981004 — docs drift IN_PROGRESS; factory contracts + typecheck/lint/build/test QUEUED (self-hosted); MERGEABLE/UNSTABLE; ahead 2 / behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `9067e47e`.** Actions run 36158981004 just started on this open. Re-check all three jobs (docs drift, factory contracts, typecheck/lint/build/test) before merge.
- **Local root gate still not evidenced on this head.** VERIFY wants `pwsh scripts/ci.ps1` green on the delivered tip; the evidence file correctly refuses to name that tip SHA (head/base binding). Controller should record the receipt in the PR body (as the EA-3-R VERIFY and EA-CUT.md §(d) already say).
- **Two independent review legs still required (D2).** Docs/evidence/queue-only row, but author notes do not waive D2. Still 0 GitHub reviews. Conductor/controller must still dispatch non-author legs on `9067e47e`.
- **PR body Outputs block is from the first commit.** Outputs show `git rev-parse HEAD` = `6eb1f4ca` and `+572/−1`; tip is `9067e47e` (+586/−1 after the pre-review-fixes commit). Cosmetic for reviewers — not a content defect — but refresh Outputs on the next push if the controller re-records gate receipts.

## Notes

- **What this PR is:** replacement row `EA-3-R` for `EA-3` after #142 hit the two-round cap (`NEEDS_REDESIGN`). Material defect in #142: `EA-CUT.md` dispositioned P8-R obligation-5 self-hits by absolute line number; a later 13-line "Base refresh" insertion shifted every citation. This PR restores `EA-INSTALL.md` / `PRIVACY.md` byte-for-byte from `af3df9dc` (blob SHAs `2707f8e3` / `9f7f8b7f` verified identical at tip) and rewrites `EA-CUT.md` fresh with self-hits dispositioned by quoted snippet + section heading.
- **Owned scope:** +586/−1 across 4 files, 2 commits (`6eb1f4ca` implement + `9067e47e` pre-review fixes). Paths: `adversarialllm/docs/EA-INSTALL.md` (+90), `PRIVACY.md` (+175), `evidence/EA-CUT.md` (+319), `factory/queue.jsonl` (+2/−1: new `EA-3-R` order 323 + `EA-3.supersededBy: ["EA-3-R"]`). Ahead 2 / behind 0. No source, build config, constants, or lockfile changes.
- **Material correction spot-check:** Self-hits tables use section heading + quoted snippet only; `rg` finds no `\b(above|below)\b` and no `line[s]? N` positional self-refs in tip `EA-CUT.md`. Pattern-1/2 final re-run counts stated as 6 / 13.
- **EA-2 constants at tip (unchanged):** `BUNDLE_PACKAGER_ENABLED` / `M1_ROUND_ENGINE_ENABLED` false at `constants.ts:1809/1812`; all `DEFAULT_SF_FLAGS` false; `DEFAULT_WEBHOOK_CONFIG.enabled` false.
- **Advisory pre-review:** PR body lists 10 findings (4 MUST / 6 SHOULD+NIT) all fixed on tip `9067e47e` — hit-count undercount, positional refs removed, hard-coded diff-stat example replaced, false no-tag claim corrected (113 archive tags exist; none `v*`; none at base), abridged vs verbatim labeling, path count 5→4, Node pin claim reworded to match `ci.yml` major-only, three dropped VERIFY clauses restored in queue spec.
- **Intentional scope:** describes only; no release tag, no distribution, no provider contact. Previous attempt: #142 (`adj-close`).
- No secrets in diff. Review via `gh`. No GitHub PR comment posted.