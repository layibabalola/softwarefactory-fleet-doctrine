schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 107
head: 2c1eb040b8ab6bd7e0ccb30a65cadae2f9b9169e
reviewed_utc: 2026-09-19T00:01:32Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: all three required checks IN_PROGRESS/pending on run 35407860287 (docs drift, factory contracts, typecheck/lint/build/test); mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews

## Blockers

(none for content — queue-only proposal + adjudication + three queue rows; no workflow, script, test, or source edits on this tip)

## Improvements

- **Wait for CI** — confirm `docs drift (delta gate)`, `factory contracts`, and `typecheck / lint / build / test` PASS on head `2c1eb040` before merge (body claims local `pwsh scripts/ci.ps1` GREEN and `validateRows` on 54 rows; hosted still running).
- **Scope reminder for implementers** — this PR only files the requirement. The high-risk `.github/workflows` change lands later under row `CI-COST-1R` and still needs one D2 leg per family plus a literal APPROVE from a non-authoring leg. Do not treat this proposal merge as that APPROVE.
- **Acceptance point 1 wiring** — when implementing, keep workflow `name: CI` unchanged so `scripts/tripwire.ps1` `workflowName == 'CI'` matching stays intact; prove that with a contract assertion, not assumption.

## Notes

- Replaces closed unmerged PR #100 (`CI-COST-1`, adj-close at two-round cap on Codex r2 F-01). Binding fix: workflow `run-name` records `pr=<n> base=<sha>` while pull_request context exists; proof selects the source PR's newest run and requires recorded base to be an ancestor of the merge parent; red-source/green-other same-head case must fail closed.
- Tip delta: +274/−0 across `factory/proposals/CI-COST-1R.md`, `factory/adjudications/CI-COST-20260917.md`, `factory/queue.jsonl` (adds `CI-COST-1R` order 248, plus measure-first follow-ups `CI-COST-2`/`CI-COST-3` depending on it). No `supersededBy` because `CI-COST-1` never reached master.
- Design carries forward lessons from #100: keep the two committed CR bytes in `factory/ci-contract.test.mjs`; run controller gate before review legs; keep review findings out of the PR body.
- Estimated ~$11–13/month; rejects removing `push: master` (tripwire + D2 VERIFY + resume probes need master runs).
- Branch `feat/CI-COST-1R` → `master`; author layibabalola (co-authored Claude Opus 5); Cursor Cloud Agents not used; review via gh. No GitHub PR comment posted.
