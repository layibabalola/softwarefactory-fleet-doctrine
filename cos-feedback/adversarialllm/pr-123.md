schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 123
head: 8c7afefcf59ebeb2b23351a224a032b4369bf3f3
reviewed_utc: 2026-09-22T06:11:14Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS on run 35693604333; factory contracts + typecheck/lint/build/test still IN_PROGRESS/pending; mergeable MERGEABLE/UNSTABLE; 0 GitHub reviews

## Blockers

(none) — content posture looks sound for offline ph-P3-R2 (default-off, disable abort, late-write settlement). Remaining required hosted checks unsettled at review time; wait for full green before merge.

## Improvements

- **Wait for remaining CI green on `8c7afefc`.** `factory contracts` and `typecheck / lint / build / test` were still IN_PROGRESS on Actions run 35693604333. Do not merge on docs-drift alone. Author claims local a2 full CI green (1022s) after inventory-anchor refresh; hosted must confirm.
- **Two-leg / D2 still open.** Implementer family is **codex** (fresh Codex-owned head replacing closed #118). D2 still needs a literal APPROVE from the **claude** leg. GitHub Reviews API returns 0 formal review objects. CoS does not substitute for that adjudication.
- **Static `background.ts` string asserts** in `tests/unit/reach-delivery.test.ts` (`readFileSync` + `toContain` for `finalizeWithReach` / `finalizeBeforeReach` wiring) remain brittle — same Improvement as prior #118 / #53. Prefer an exported seam / injected finalize wrapper over source regex.
- **Receipt capacity 256** still refuses new keys (correct anti-replay). Operator signal when hit remains coarse (`reach_failed` / throw). Track retention/GC + clearer signal as a follow-up if real use grows.
- Soft: settlement repair in `createReachBinding` fenced `syncStore.set` re-applies only `latestSyncWrite` after a stale pending completes; errors on that repair path are swallowed (`.catch(() => undefined)`). Acceptable for best-effort reach; if hosted flakes appear around late-write races, consider a single serialized repair queue + explicit report token.

## Notes

- **Fresh PR vs closed #118.** #118 (`ph-P3-R`, tip `84fa9238`) was closed unmerged 2026-09-21. #123 (`ph-P3-R2`) is a Codex-owned resubmit of the offline reach binding: +1120/−9 across 7 files on branch `codex/ph-P3-R2`, tip `8c7afefc` (2 commits: feature + inventory-anchor refresh). Same file set as #118 (`reach-delivery.ts` new, binding in `background.ts`, `sync-writer` / `verdict-head` projection, docs/02 + docs/28, 47 focused tests).
- **Claimed defect dispositions (on read):** DISABLE-01 — per-site durable recheck (S0–S8) + disable-bound cancellation. RETRY-LATE-WRITE-01 — monotonic `syncWriteSequence` / `latestSyncWrite` repair so a timed-out or disable-abandoned storage.sync write cannot overwrite a later delivered head (two deterministic tests). DISABLE-01-RECHECK-UNPINNED — pinned local `reach.config` observer + `AbortController` abort of in-flight webhook POST on disable (test covers abort → `ambiguous`). R2-BOUND-TEXT-01 retained as process guidance.
- **Safety / minimal-shape posture still looks sound:** default disabled, exact `grantedOrigin`, HTTPS-only / HMAC `X-*` header gate, fetch `redirect:error` / `credentials:omit` / `cache:no-store`, one-POST max for webhook, sync retries capped at 3, boot + send-path interrupted-webhook → terminal `ambiguous` without replay, `finalizeBeforeReach` keeps reach best-effort after durable finalize. Live cross-device still OUT OF SCOPE (`ph-P3-LIVE`); no flag activated here.
- Cursor Cloud Agents not used (plan gap); review via `gh`. No GitHub PR comment posted.