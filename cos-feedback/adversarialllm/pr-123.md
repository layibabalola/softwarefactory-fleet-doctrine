schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 123
head: 329ff6cae7c5bbd897b6aee0b4f7cac40f2642fb
reviewed_utc: 2026-09-22T07:15:59Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS on run 35698604416; factory contracts + typecheck/lint/build/test still IN_PROGRESS; mergeable MERGEABLE/UNSTABLE; 0 GitHub reviews

## Blockers

(none) — offline ph-P3-R2 posture still looks sound (default-off, disable abort, monotonic late-write settlement with recursive fence). Remaining required hosted checks unsettled at review time; wait for full green before merge.

## Improvements

- **Wait for remaining CI green on `329ff6ca`.** `factory contracts` and `typecheck / lint / build / test` still IN_PROGRESS on Actions run 35698604416. Do not merge on docs-drift alone. Delta since prior CoS head adds one focused fence-reentry test (48 reach tests claimed); hosted must confirm.
- **Two-leg / D2 still open.** Implementer family is **codex**. D2 still needs a literal APPROVE from the **claude** leg. GitHub Reviews API returns 0 formal review objects. CoS does not substitute for that adjudication.
- **Static `background.ts` string asserts** in `tests/unit/reach-delivery.test.ts` (`readFileSync` + `toContain` for `finalizeWithReach` / `finalizeBeforeReach` wiring) remain brittle — same Improvement as prior #118 / #53. Prefer an exported seam / injected finalize wrapper over source regex.
- **Receipt capacity 256** still refuses new keys (correct anti-replay). Operator signal when hit remains coarse (`reach_failed` / throw). Track retention/GC + clearer signal as a follow-up if real use grows.
- Soft: `settleSyncWrite` still swallows repair-path errors (`.catch(() => undefined)`). Recursive fence + `abandonWrite` on disable improved the race; if hosted flakes appear around late-write races, consider an explicit report token on swallowed repair failures.

## Notes

- **Delta vs prior CoS head `8c7afefc`:** +2 commits on `codex/ph-P3-R2` — `8116cf28` fence recursive sync reconciliation; `329ff6ca` refresh fenced reach inventory anchor. Tip now `329ff6ca` (+1175/−9, 7 files vs master).
- **Prior soft Improvement addressed:** settlement repair no longer one-shot re-apply. `createReachBinding` now uses recursive `settleSyncWrite` with `activePhysicalSyncWrites`, fenced `get` that prefers in-flight `latestSyncWrite`, and `abandonWrite` clearing `latestSyncWrite` on disable/error paths in `createReachSync`. New regression: corrective write that settles after a newer delivery still lands the newest head (test count 47→48). Cleared the prior soft note about single-shot repair; residual swallow on `.catch` kept as soft.
- **Claimed defect dispositions (unchanged on read):** DISABLE-01, RETRY-LATE-WRITE-01, DISABLE-01-RECHECK-UNPINNED fixed; R2-BOUND-TEXT-01 retained as process guidance.
- **Safety / minimal-shape posture still looks sound:** default disabled, exact `grantedOrigin`, HTTPS-only / HMAC `X-*` header gate, fetch `redirect:error` / `credentials:omit` / `cache:no-store`, one-POST max for webhook, sync retries capped at 3, boot + send-path interrupted-webhook → terminal `ambiguous` without replay, `finalizeBeforeReach` keeps reach best-effort after durable finalize. Live cross-device still OUT OF SCOPE (`ph-P3-LIVE`); no flag activated here.
- Cursor Cloud Agents not used (plan gap); review via `gh`. No GitHub PR comment posted.