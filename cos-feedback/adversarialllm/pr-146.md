schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 146
head: 12957f9b29036965d2e1e243f322c0e67f84b6bf
reviewed_utc: 2026-09-25T20:34:47Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36186551252 — docs drift IN_PROGRESS; factory contracts + typecheck/lint/build/test QUEUED (self-hosted); MERGEABLE/UNSTABLE; ahead 4 / behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `12957f9b`.** Actions run 36186551252 just started on open. Re-check all three required jobs (factory contracts, docs drift, typecheck/lint/build/test) before merge — especially the `ci` job unit step under the new self-hosted timeout expression.
- **Two independent review legs still required (D2).** High-risk class (`.github/workflows` + gate). Row `CI-VITEST-TIMEOUT-PARITY` is `family: claude`. Still 0 GitHub reviews. Conductor/controller must dispatch non-author legs on `12957f9b`.
- **Local root gate receipt on this head.** VERIFY clause (8) wants required checks and `pwsh scripts/ci.ps1` green on the delivered tip; body says the controller records the exact-head gate receipt on this PR. Confirm that receipt is present before merge (not re-verified here).

## Notes

- **Scope:** corrective for #137 / issue #138. After `CI-SELF-HOSTED-LINUX` routed the `ci` job onto the self-hosted Windows runner via `CI_RUNS_ON`, the unit step stayed at vitest's 5000 ms default while the local gate already used `--testTimeout=30000` for measured host-load false reds on virtual-ten. This PR mirrors 30000 ms onto self-hosted only; GitHub-hosted keeps 5000 ms.
- **Diff vs master (+78/−9, 4 files, 4 commits):** `.github/workflows/ci.yml` — one-line change on the `ci` job `Tests` step: `npx vitest run ${{ runner.environment == 'self-hosted' && '--testTimeout=30000' || '' }}` (numstat 1/1). `factory/ci-contract.test.mjs` — +63 pins that exact line scoped to the `ci` job slice and rejects unconditional timeout, missing conditional, wrong predicate keys, wrong values (60000/5000), and a pinned `Tests` step sitting in another job. `scripts/ci.ps1` — comment-only (body's non-comment grep is empty). `factory/queue.jsonl` — row `CI-VITEST-TIMEOUT-PARITY` order 324, `dependsOn: ["CI-SELF-HOSTED-LINUX"]`, family claude.
- **Honesty fixes from advisory pre-review:** measured host is named (virtual-ten); ultra-magnus slowdown is stated as unmeasured; pin job-scoped; no unproven "same machine" claim. Looks correct for the stated MUST.
- No secrets in diff. Review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (not on plan for this account).
