schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 126
head: 241303ebcf6768abda77c5f49453c6fc065ea7ac
reviewed_utc: 2026-09-23T10:35:15Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 35849605292 — all three required jobs IN_PROGRESS (docs drift, factory contracts, typecheck/lint/build/test); mergeable true / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `241303eb`.** Actions run 35849605292 still has docs drift, factory contracts, and typecheck/lint/build/test IN_PROGRESS. Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `EA-CUT-FILE` is `family: claude` (implementer: claude). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.
- **VERIFY open-PR high-water is stale.** Body claims highest order on any open PR branch is 260 (PR #104). Open PR #125 already holds orders 274/275 on `feat/CI-PREFLIGHT-MEASURED-FILE`. Orders 301–305 here still clear that high-water, so uniqueness holds, but the VERIFY citation should name the real open-PR max (275 on #125, or whichever is higher at re-check).

## Notes

- **pr-opened: queue-only filing PR.** Tip `241303eb` is `EA-CUT-FILE: file the early-access cut rows`. PR vs master: `factory/queue.jsonl` +5/−0, 1 file, ahead 1 / behind 0 (base `c4986b95`). Exclusive tip rows: `EA-1` (301, claude), `EA-2` (302, codex, dependsOn EA-1), `EA-3` (303, claude, dependsOn EA-2), `EA-4` (304, no family, dependsOn EA-3), `EA-CUT-FILE` (305, claude). Body row token `EA-CUT-FILE` only.
- **Filing VERIFY spot-check (from PR body + diff):** Appends five rows; numstat +5/−0; no pre-existing row modified. Self-filing pattern matches PR 113 / PR 117 / PR 125 (FILE delivers; EA-1 stays READY/unconsumed; EA-2/3/4 WAITING_DEPENDENCY). Orders sit above master max (273) and above open #125 (275).
- **Cut scope (data only):** Early-access cut keeps every kill switch at base values (M1_ROUND_ENGINE / BUNDLE_PACKAGER / DEFAULT_SF_FLAGS / webhook enabled false). Out of scope: store submission, signing, CRX, release tag. `EA-4` is attended owner Chrome only — no agent browser, no `observe-campaign.ps1`, no live-authority prerequisite; absent session → `EXTERNAL_CAPABILITY_UNAVAILABLE(attended-session)`.
- **Sibling open filing:** PR #125 (`CI-PREFLIGHT-MEASURED-FILE`, orders 274/275) shares the same master base. Merge either first; queue appends do not collide on order numbers.
- **Local gate (author claim):** Full local gate GREEN (2090 s, all 18 steps) on committed head; earlier reds attributed to stale `output/` artifacts and host contention, not this diff. Controllers should still wait for tip CI.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
