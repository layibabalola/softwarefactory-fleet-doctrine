schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 126
head: ceb19ea692142709ea25a7aa853e31a861e5b13e
reviewed_utc: 2026-09-23T11:01:08Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 35852010494 — docs drift SUCCESS; factory contracts and typecheck/lint/build/test IN_PROGRESS; mergeable true / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `ceb19ea6`.** Actions run 35852010494 still has factory contracts and typecheck/lint/build/test IN_PROGRESS (docs drift already SUCCESS). Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `EA-CUT-FILE` is `family: claude` (implementer: claude). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.

## Notes

- **pr-pushed: VERIFY high-water clause fixed.** Tip `ceb19ea6` (`EA-CUT-FILE: make the order clause verifiable at review time`) rewrites only the `EA-CUT-FILE` VERIFY text so the open-PR order ceiling is checked at review time rather than naming a fixed PR/order that goes stale. Addresses prior CoS Improvement on head `241303eb` (stale claim of 260 / PR #104 while #125 held 274/275). Orders 301–305 unchanged; base-to-head numstat still +5/−0 on `factory/queue.jsonl`; ahead 2 / behind 0.
- **Queue-only filing PR (unchanged substance).** Exclusive tip rows still: `EA-1` (301, claude), `EA-2` (302, codex, dependsOn EA-1), `EA-3` (303, claude, dependsOn EA-2), `EA-4` (304, no family, dependsOn EA-3), `EA-CUT-FILE` (305, claude). Body row token `EA-CUT-FILE` only. Self-filing pattern matches PR 113 / PR 117 / PR 125.
- **Cut scope (data only):** Early-access cut keeps every kill switch at base values. Out of scope: store submission, signing, CRX, release tag. `EA-4` is attended owner Chrome only — no agent browser, no live-authority prerequisite.
- **Sibling open filing:** PR #125 (`CI-PREFLIGHT-MEASURED-FILE`, orders 274/275) shares the same master base. Merge either first; queue appends do not collide on order numbers.
- **Author note on push:** Local root gate re-running on new head; summary pending. Controllers should still wait for tip CI.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.