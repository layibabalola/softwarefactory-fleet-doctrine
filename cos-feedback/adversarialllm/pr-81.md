schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 81
head: b10566fe887fbb75caec11f109d0a88c0abcf0e6
reviewed_utc: 2026-09-16T05:51:03Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test PENDING on head b10566fe (Actions run 35061034379); tip ahead of master by 1, behind by 0

## Blockers

- **CI: `factory contracts`** — PENDING on head `b10566fe887fbb75caec11f109d0a88c0abcf0e6` (Actions run 35061034379). Wait for green; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — PENDING on the same run. Same wait/fix rule.
- **Process: independent dual-family D2 on this head** — Label `impl:codex`; 0 GitHub reviews / review requests. Needs one live review leg per family (Codex + Claude), zero MUST/BLOCKER, and a literal non-author APPROVE. Deciding APPROVE must come from a Claude-family leg. Author neither reviews nor merges.

## Improvements

- **Collapse the single-member `ChartEngine` plumbing.** `ChartEngine` is now `'recharts'` only, `MetricsDashboard` hardcodes `const engine = 'recharts' as const`, and `ENGINE_CHART_SUPPORT` / `getEffectiveChartType(engine, …)` still take an engine argument. Drop the engine parameter (or replace with a const) so chart-type helpers are Recharts-only and the dead selector surface cannot grow back casually.
- **Pin release-tag discovery for CI/shallow clones.** `getReleaseVersion` uses `git describe --tags --match 'v[0-9]*' --abbrev=0`. Document (or CI-assert) that release builds fetch tags / are not shallow-without-tags, otherwise every CI/dev build silently falls back to package `0.0.0` and a real tag cut can look identical to an untagged build until someone notices. A one-line note in `P8-SHIP-READINESS.md` next to the clean-clone release validation bullet is enough.
- **Optional: trim residual engine comments in `App.tsx` metrics section** after the Recharts-only rewrite (already mostly done); keep dashboard copy aligned with the single renderer so future auditors do not hunt for removed engines.

## Notes

- Scope (16 files, +114/−1136): removes unused `@anthropic-ai/sdk` and Chart.js / lightweight-charts / react-chartjs-2 stacks; deletes `ChartJSChart.tsx`, `LightweightChart.tsx`, `SVGChart.tsx`; retains Recharts; adds `scripts/release-version.ts` + unit tests and wires `wxt.config.ts` manifest `version` through `getReleaseVersion()`; adds `SECURITY.md` and `evidence/P8-SHIP-READINESS.md`; corrects `native-host/client.ts` header (substrate `dev-companion-arm.ts` / `ferry-send.ts` do import `NativeHostClient` — verified on this tip).
- PR body correctly scopes out license selection, signing, publishing, and production activation. Native companion remains development-only per readiness record.
- Local author validation claimed: 38 focused unit tests, typecheck, production extension build (dev manifest `0.0.0`), native Go tests, docs constants, line-endings delta — all green. Hosted CI still settling.
- Content review via `gh` (no Cursor Cloud Agent). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/adversarialllm/pr-81.md` for head `b10566fe`.