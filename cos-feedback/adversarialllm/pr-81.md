schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 81
head: 62d470ab524c0afb8eb8afa9e26db31477258214
reviewed_utc: 2026-09-16T17:07:15Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test PENDING on head 62d470ab (Actions run 35125732407); tip ahead of master by 3, behind by 0; mergeable=true (unstable)

## Blockers

- **CI: `factory contracts`** — PENDING on head `62d470ab524c0afb8eb8afa9e26db31477258214` (Actions run 35125732407). Wait for green; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — PENDING on the same run. Same wait/fix rule.
- **Process: independent dual-family D2 on this exact round-2 head** — Round 1 (both families) was CHANGES_REQUESTED on `b10566fe`. Tip is now `62d470ab` (Claude Opus 5 round-2 + merge of `origin/master`). 0 GitHub reviews on this tip; label still `impl:codex`. Needs one live review leg per family on `62d470ab`, zero MUST/BLOCKER, and a literal non-author APPROVE. Author families on this tip include Codex (round 1) and Claude (round 2) — deciding APPROVE must come from a non-author controller/leg. This is the last evaluable round (HUB Retries). Author neither reviews nor merges.

## Improvements

- **Collapse the single-member `ChartEngine` plumbing (carried from round 1).** Still open: `ChartEngine` is `'recharts'` only, `MetricsDashboard` hardcodes `const engine = 'recharts' as const`, and `ENGINE_CHART_SUPPORT` / `getEffectiveChartType(engine, …)` / `getSupportedChartTypes(engine)` still take an engine argument. Drop the engine parameter (or replace with a const) so chart-type helpers are Recharts-only and the dead selector surface cannot grow back casually.
- **Document tag fetch for release builds (narrowed).** Round 2 fixed P8-02 with `git describe --tags --exact-match --match 'v[0-9]*' HEAD` (no longer inherits a nearest ancestor tag). Residual: a shallow/no-tags clone still fails describe and silently falls back to package `0.0.0`, so a tagged release cut can look identical to an untagged build until someone notices. One line in `evidence/P8-SHIP-READINESS.md` beside the clean-clone release-validation bullet (“fetch tags / not shallow-without-tags”) is enough.
- **Optional owner veto on `LICENSE` copyright-holder line.** Text names “the owner of the GitHub repository layibabalola/AdversarialLLM-ClaudeCode” deliberately (no invented legal entity). If commercial distribution needs an individual or company name, replace that line in a follow-up; not a merge blocker for P8 posture.

## Notes

- Round-2 delta vs prior CoS head `b10566fe`: merge `bcec7012` (origin/master `c9a08261`) + `62d470ab` (“P8: add proprietary license and bind release version to HEAD tag”). Owned tip vs master: 19 files, +203/−1159 (3 commits).
- **Prior CoS Improvements status:** (1) App.tsx metrics section copy now Recharts-only — addressed. (2) Release-tag discovery rewritten to `--exact-match` at HEAD — addresses the round-1 P8-02 / inheritance concern; shallow-clone note still open as above. (3) ChartEngine collapse — not addressed.
- Scope delivered for row P8: proprietary root `LICENSE` + `package.json` `"license": "UNLICENSED"` / `"private": true`; remove unused `@anthropic-ai/sdk` and Chart.js / lightweight-charts stacks; Recharts sole engine; `scripts/release-version.ts` + unit tests wired through `wxt.config.ts`; `SECURITY.md`; `evidence/P8-SHIP-READINESS.md`; developer-guide dependency/chart tables and `native-host/client.ts` header aligned.
- Out of scope (correct): signing, publish, production activation; native companion stays development-only. Declined follow-ups recorded in PR body: lint `scripts/`, THIRD_PARTY_NOTICES for distributed builds.
- Author validation claimed on round-2 head vs base `c9a08261`: `pwsh scripts/ci.ps1` GREEN exit 0 (~1433s). Hosted Actions still settling on run 35125732407.
- Content review via `gh` (no Cursor Cloud Agent). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-81.md` for head `62d470ab`.