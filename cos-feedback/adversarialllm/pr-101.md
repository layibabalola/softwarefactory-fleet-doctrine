schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 101
head: 4c2794f0e143ee302c3eb77cccf07f56721a3205
reviewed_utc: 2026-09-18T06:45:29Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head 4c2794f0 (Actions run 35316170773); prior tip 0235d25c was all-green; mergeable MERGEABLE; 0 GitHub reviews.

## Blockers

- **CI: `factory contracts`** — IN_PROGRESS on head `4c2794f0e143ee302c3eb77cccf07f56721a3205` (Actions run 35316170773). Wait for green before merge; if red, fix and re-push. Prior tip `0235d25c` had this check SUCCESS.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule. Prior tip was SUCCESS.
- **Dual-family D2 + non-author merge not landed.** PR body requires two independent adversarial legs (one claude, one codex), `factory/review-state.mjs` `evaluateMerge`, and a fresh non-author controller. Session that authored the head (Claude Opus 5) must not merge. 0 GitHub reviews yet.

## Improvements

- `factory/proposals/P2-HARNESS.md` Run block still uses `pwsh ../scripts/ci.ps1` — confirm cwd (repo-root vs `factory/`); prefer `pwsh scripts/ci.ps1` if root-relative is house style (same note as H1-R / #88 / #91). Unchanged on this tip.
- After CI green, land head-bound dual-family legs on tip `4c2794f0` before any merge attempt; do not treat queue-state local proof alone as merge authority.
- Queue hygiene: other open PRs that also edit `factory/queue.jsonl` can still race; if another queue PR lands first, refresh base so `P2.dependsOn` / new rows stay coherent. Rows already sit before `P2` (orders 111/112).

## Notes

- Push tip `4c2794f0` vs prior CoS head `a2f7a67e`: (1) `P2-HARNESS` never launders non-login readiness into unavailability — `unauthenticated` only when `ok: true`, `authenticated` not true, and `data.reason` is `needs_login`; `composer_missing` / `account_identity_missing` become `readiness-defect` → `defectProviders` → overall `STOP` (not `EXTERNAL_CAPABILITY_UNAVAILABLE`); `readiness-failed` remains for thrown/`ok` not true. Offline proof adds a `composer_missing` case. (2) Adjudication fact 6 corrected (STOP packet on first unauthenticated provider; no multi-provider EXTERNAL reason today). (3) Swarm ruling reframed as advisory local session data under HUB Autonomy, not a merge key. (4) P2-HARNESS now explicitly high-risk class (edits D10-authorized live script) with one-leg-per-family / no-merge-while-family-dark. Queue placement unchanged from `a2f7a67e`.
- Scope still `row:P2-AMEND` only: records why P2 cannot run and stages two bounded prerequisite proposals. **Implements nothing.** No `row:P2` token; does not depend on R-LIVE. Same four paths vs master `6b504b25` (+84/−1).
- Content read: adjudication rejects false DELIVERED / SUPERSEDED_DELIVERED / hand-started browser; P2-HARNESS offline-only with clearer sign-out vs defect split; P2-LAUNCH high-risk sweep-scoped launcher keeping H1-R refusals byte-identical. Not a re-audit of `a19-model-discovery-sweep.mjs` or `observe-campaign.ps1` beyond cited facts.
- Author claims on open still apply: `queue-state.mjs` → P2 WAITING_DEPENDENCY waitingFor `[P2-HARNESS, P2-LAUNCH]`; `queue-state.test.mjs` 16/16. No issue comments yet with local `pwsh scripts/ci.ps1` result.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: rewrite on `master` at `cos-feedback/adversarialllm/pr-101.md` for new head. Verdict unchanged (`merge-when-ci-green`); chat notify skipped per push rule (no verdict change; prior CoS Improvements not clearly closed — `../scripts/ci.ps1` cwd note and dual-family legs still open).
