schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 101
head: 0235d25c886ee55dd7bdae5153bd8e2a0ea3feb2
reviewed_utc: 2026-09-18T05:58:47Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head 0235d25c (Actions run 35312875943); mergeable MERGEABLE; mergeStateStatus UNSTABLE; 0 GitHub reviews.

## Blockers

- **CI: `factory contracts`** — IN_PROGRESS on head `0235d25c886ee55dd7bdae5153bd8e2a0ea3feb2` (Actions run 35312875943). Wait for green before merge; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Dual-family D2 + non-author merge not landed.** PR body requires two independent adversarial legs (one claude, one codex), `factory/review-state.mjs` `evaluateMerge`, and a fresh non-author controller. Session that authored the head (Claude Opus 5) must not merge. 0 GitHub reviews yet.

## Improvements

- `factory/proposals/P2-HARNESS.md` Run block uses `pwsh ../scripts/ci.ps1` — confirm cwd (repo-root vs `factory/`); prefer `pwsh scripts/ci.ps1` if root-relative is house style (same note as H1-R / #88 / #91).
- After CI green, land head-bound dual-family legs on tip `0235d25c` before any merge attempt; do not treat queue-state local proof alone as merge authority.
- Queue hygiene: confirm no open PR also edits `factory/queue.jsonl` before merge; if another queue PR lands first, refresh base so `P2.dependsOn` / new rows stay coherent.

## Notes

- First CoS bus file for this PR. Tip `0235d25c` vs master `6b504b25`: +71/−1 on exactly 4 factory paths — `factory/adjudications/P2-launcher-block-20260918.md`, `factory/proposals/P2-HARNESS.md`, `factory/proposals/P2-LAUNCH.md`, `factory/queue.jsonl` (`P2.dependsOn` → `[P5, P2-HARNESS, P2-LAUNCH]`; new `P2-HARNESS` order 111; new `P2-LAUNCH` order 112 dependsOn `[P5, H1-R]`).
- Scope is `row:P2-AMEND` only: records why P2 cannot run (D10 authorizes sweep; H1-R removed non-campaign launch; CDP 9222 refused; harness cannot emit EXTERNAL_CAPABILITY_UNAVAILABLE) and stages two bounded prerequisite proposals. **Implements nothing.** Body carries no `row:P2` token; does not depend on R-LIVE.
- Content read: adjudication rejects false DELIVERED / SUPERSEDED_DELIVERED / hand-started browser; P2-HARNESS is offline-only with clear acceptance (cdp-unreachable packet, per-provider readiness, STOP invariants); P2-LAUNCH is high-risk sweep-scoped launcher that keeps H1-R refusals byte-identical. Not a re-audit of a19-model-discovery-sweep.mjs or observe-campaign.ps1 beyond the cited facts.
- Author claims `queue-state.mjs` with live PR history → P2 WAITING_DEPENDENCY waitingFor `[P2-HARNESS, P2-LAUNCH]`; `queue-state.test.mjs` 16/16; local `pwsh scripts/ci.ps1 -BaseSha 6b504b25…` was running at open (result TBD as PR comment).
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: created on `master` at `cos-feedback/adversarialllm/pr-101.md`.