schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 101
head: a2f7a67e01fc44ad51a1002f0e80b392d79fa807
reviewed_utc: 2026-09-18T06:23:48Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head a2f7a67e (Actions run 35314539757); prior tip 0235d25c was all-green; mergeable MERGEABLE; mergeStateStatus UNSTABLE; 0 GitHub reviews.

## Blockers

- **CI: `factory contracts`** — IN_PROGRESS on head `a2f7a67e01fc44ad51a1002f0e80b392d79fa807` (Actions run 35314539757). Wait for green before merge; if red, fix and re-push. Prior tip `0235d25c` had this check SUCCESS.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule. Prior tip was SUCCESS.
- **Dual-family D2 + non-author merge not landed.** PR body requires two independent adversarial legs (one claude, one codex), `factory/review-state.mjs` `evaluateMerge`, and a fresh non-author controller. Session that authored the head (Claude Opus 5) must not merge. 0 GitHub reviews yet.

## Improvements

- `factory/proposals/P2-HARNESS.md` Run block still uses `pwsh ../scripts/ci.ps1` — confirm cwd (repo-root vs `factory/`); prefer `pwsh scripts/ci.ps1` if root-relative is house style (same note as H1-R / #88 / #91). Unchanged on this tip.
- After CI green, land head-bound dual-family legs on tip `a2f7a67e` before any merge attempt; do not treat queue-state local proof alone as merge authority.
- Queue hygiene: other open PRs that also edit `factory/queue.jsonl` can still race; if another queue PR lands first, refresh base so `P2.dependsOn` / new rows stay coherent. Tip `a2f7a67e` already moved `P2-HARNESS`/`P2-LAUNCH` from queue tail to sit before `P2` (orders 111/112), which reduces append-collision risk with open tail PRs.

## Notes

- Push tip `a2f7a67e` vs prior CoS head `0235d25c`: (1) `P2-HARNESS` acceptance now distinguishes `readiness-failed` (thrown readiness/options or `ok` not true) from `unauthenticated` (only when readiness `ok: true` and `data.authenticated` not true); offline proof requires a mixed ready / unauthenticated / readiness-failed set. (2) `factory/queue.jsonl` relocates the two new rows from file tail to immediately before `P2` (after `P1`). Swarm ruling (3 legs) drove both.
- Scope still `row:P2-AMEND` only: records why P2 cannot run and stages two bounded prerequisite proposals. **Implements nothing.** No `row:P2` token; does not depend on R-LIVE. Same four paths vs master `6b504b25` (+71/−1 net shape; second commit is proposal text + queue placement only).
- Content read unchanged in substance: adjudication rejects false DELIVERED / SUPERSEDED_DELIVERED / hand-started browser; P2-HARNESS offline-only; P2-LAUNCH high-risk sweep-scoped launcher keeping H1-R refusals byte-identical. Not a re-audit of `a19-model-discovery-sweep.mjs` or `observe-campaign.ps1` beyond cited facts.
- Author claims on open still apply to the amend: `queue-state.mjs` → P2 WAITING_DEPENDENCY waitingFor `[P2-HARNESS, P2-LAUNCH]`; `queue-state.test.mjs` 16/16. No issue comments yet with local `pwsh scripts/ci.ps1` result.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: rewrite on `master` at `cos-feedback/adversarialllm/pr-101.md` for new head. Verdict unchanged (`merge-when-ci-green`); chat notify skipped per push rule (no verdict change; prior CoS Improvements not clearly closed).
