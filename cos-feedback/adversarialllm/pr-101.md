schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 101
head: aa44cc3495c1847ad8ed7169fa6d79313c4db05a
reviewed_utc: 2026-09-18T07:38:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head aa44cc34 (Actions run 35320142138); mergeable MERGEABLE / mergeStateStatus UNSTABLE; 0 GitHub reviews on tip.

## Blockers

- **CI: `factory contracts`** — IN_PROGRESS on head `aa44cc3495c1847ad8ed7169fa6d79313c4db05a` (Actions run 35320142138). Wait for green before merge; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Dual-family D2 + non-author merge not landed on this tip.** Round-1 ledger (issue comment on head `4c2794f0`) recorded codex CHANGES_REQUESTED (F-01 MUST) + claude APPROVE (F-01..F-06 SHOULD/NIT) and `evaluateMerge` → `UNRESOLVED_FINDINGS`. This tip claims to resolve those findings, but GitHub still shows 0 reviews on `aa44cc34`. Factory policy still needs two independent adversarial legs (one claude, one codex) bound to this tip, `factory/review-state.mjs` `evaluateMerge` green, and a fresh non-author controller. Author session (Claude Opus 5) must not merge. Round 2 is stated as the last evaluable round.

## Improvements

- `factory/proposals/P2-HARNESS.md` Run block still uses `pwsh ../scripts/ci.ps1` — confirm cwd (repo-root vs `factory/`); prefer `pwsh scripts/ci.ps1` if root-relative is house style (same note as H1-R / #88 / #91). Unchanged on this tip; prior CoS Improvement not closed.
- After CI green, land head-bound dual-family legs on tip `aa44cc34` (not the prior `4c2794f0` receipts alone) before any merge attempt; do not treat queue-state local proof alone as merge authority.
- Queue hygiene: other open PRs that also edit `factory/queue.jsonl` can still race; if another queue PR lands first, refresh base so `P2.dependsOn` / new rows (`P2-HARNESS` 111, `P2-LAUNCH` 112, `P2-AMEND` 113) stay coherent.

## Notes

- Push tip `aa44cc34` vs prior CoS head `4c2794f0` (round-2 commit): (1) **codex r1 F-01 MUST closed** — queue now files `P2-AMEND` (order 113, family claude, spec = adjudication path); this PR delivers that row only. (2) **claude r1 F-01..F-06 adopted** — `P2-LAUNCH` binds ephemeral non-9222 loopback port (in-process only), single-child self-gate (A19 seven-provider set), lifetime end-on-child-exit, attempt count from tracked evidence beside the a19 packet (local cache never authority), and a new guard DENIAL vector + test-table entry for non-admitted launcher forms (no new allowance). `P2-HARNESS` adds fifth readiness reason `account_identity_missing_collapsed_sidebar`, classes unknown/absent not-auth reasons as `readiness-defect`, and strengthens post-sweep invariant via `monitorInactiveAfter` false. (3) Adjudication fact 7 rewritten with dated 2026-07-27 packet evidence (Gemini authenticated; Grok stopped on `composer_missing` selector defect; no current certainty of signed-out providers); D10 "unattended" reading stated explicitly as a P2-LAUNCH high-risk review decision point (reconcile existing authority vs need plan amendment).
- Scope still documentation/queue only: records why P2 cannot run and stages two bounded prerequisite proposals plus the amend row. **Implements nothing.** No `row:P2` token; does not depend on R-LIVE. Four paths vs master `6b504b25` (+92/−1 on tip).
- Content read: adjudication rejects false DELIVERED / SUPERSEDED_DELIVERED / hand-started browser; P2-HARNESS offline-only with sign-out vs defect split; P2-LAUNCH high-risk sweep-scoped launcher keeping H1-R refusals byte-identical. Not a re-audit of `a19-model-discovery-sweep.mjs` or `observe-campaign.ps1` beyond cited facts.
- Author ledger: local gate GREEN on prior tip `4c2794f0` (1167s); round-1 receipts VALID via `evaluateReview`; round-2 head addresses UNRESOLVED_FINDINGS inputs. Author claims on queue-state (P2 WAITING_DEPENDENCY waitingFor `[P2-HARNESS, P2-LAUNCH]`; tests 16/16) still apply to the structural change; re-verify after CI on this tip.
- Prior CoS Improvements clearly addressed by this push: **none** (cwd Run-block note still open; dual-family legs for `aa44cc34` still open; queue hygiene still open). Adversarial-leg findings (codex MUST / claude SHOULD) were addressed in content, which is separate from CoS Improvements.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted by CoS.
- Doctrine bus: rewrite on `master` at `cos-feedback/adversarialllm/pr-101.md` for new head. Verdict unchanged (`merge-when-ci-green`).