schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 101
head: 620d20efa511c476f78304a462c4b377e1adcd71
reviewed_utc: 2026-09-18T08:04:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head 620d20ef (Actions run 35322409432); prior tip aa44cc34 CI SUCCESS (run 35320142138); mergeable MERGEABLE / mergeStateStatus UNSTABLE; 0 GitHub reviews on tip.

## Blockers

- **CI: `factory contracts`** — IN_PROGRESS on head `620d20efa511c476f78304a462c4b377e1adcd71` (Actions run 35322409432). Wait for green before merge; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Dual-family D2 + non-author merge not landed on this tip.** Round-1 ledger (issue comment on head `4c2794f0`) recorded codex CHANGES_REQUESTED (F-01 MUST) + claude APPROVE (F-01..F-06 SHOULD/NIT) and `evaluateMerge` → `UNRESOLVED_FINDINGS`. Tips `aa44cc34` and now `620d20ef` claim to resolve those findings / internal consistency, but GitHub still shows 0 reviews on `620d20ef`. Factory policy still needs two independent adversarial legs (one claude, one codex) bound to this tip, `factory/review-state.mjs` `evaluateMerge` green, and a fresh non-author controller. Author session (Claude Opus 5) must not merge. Round 2 is stated as the last evaluable round; this push is labeled pre-round-2 consistency before legs run.

## Improvements

- `factory/proposals/P2-HARNESS.md` Run block still uses `pwsh ../scripts/ci.ps1` — confirm cwd (repo-root vs `factory/`); prefer `pwsh scripts/ci.ps1` if root-relative is house style (same note as H1-R / #88 / #91). Unchanged on this tip; prior CoS Improvement not closed.
- After CI green, land head-bound dual-family legs on tip `620d20ef` (not the prior `4c2794f0` receipts alone, and not `aa44cc34` alone) before any merge attempt; do not treat queue-state local proof alone as merge authority.
- Queue hygiene: other open PRs that also edit `factory/queue.jsonl` can still race; if another queue PR lands first, refresh base so `P2.dependsOn` / new rows (`P2-HARNESS` 111, `P2-LAUNCH` 112, `P2-AMEND` 113) stay coherent.

## Notes

- Push tip `620d20ef` vs prior CoS head `aa44cc34` (pre-round-2 consistency, +31/−11 across adjudication + P2-HARNESS + P2-LAUNCH; queue unchanged): (1) **P2-LAUNCH in-process** — imports sweep module / calls exported entry (owned by P2-HARNESS); ephemeral loopback port never 9222; no child process / no port across process boundary. (2) **Authority precondition** moved into `P2-LAUNCH.md` with narrow-reading bounded exit (`NEEDS_REDESIGN`, no auto successor; P2 waits for reviewed D10 plan change). Adjudication binds nothing by itself. (3) **Attempt accounting** — every attempt counts including STOP/crash; launcher writes no tracked file; P2 PR commits the attempt log. (4) **Guard** — single admitted invocation is an allowance reviewed under high-risk; all other forms DENIAL + test-table. (5) **P2-HARNESS** — total readiness classification order (`ok` / `authenticated` / `needs_login` / else defect); exports callable sweep entry for launcher + CLI + tests.
- Scope still documentation/queue only: records why P2 cannot run and stages two bounded prerequisite proposals plus the amend row. **Implements nothing.** No `row:P2` token; does not depend on R-LIVE. Four paths vs master `6b504b25` (+112/−1 on tip).
- Content read: adjudication rejects false DELIVERED / SUPERSEDED_DELIVERED / hand-started browser; P2-HARNESS offline-only with sign-out vs defect split; P2-LAUNCH high-risk sweep-scoped launcher keeping H1-R refusals byte-identical. Not a re-audit of `a19-model-discovery-sweep.mjs` or `observe-campaign.ps1` beyond cited facts.
- Author ledger: local gate GREEN on prior tip `4c2794f0` (1167s); round-1 receipts VALID via `evaluateReview`; round-2 head `aa44cc34` addressed UNRESOLVED_FINDINGS inputs; this tip tightens internal consistency before round-2 legs. Author claims on queue-state (P2 WAITING_DEPENDENCY waitingFor `[P2-HARNESS, P2-LAUNCH]`; tests 16/16) still apply to the structural change; re-verify after CI on this tip. Prior tip `aa44cc34` CI concluded SUCCESS.
- Prior CoS Improvements clearly addressed by this push: **none** (cwd Run-block note still open; dual-family legs for `620d20ef` still open; queue hygiene still open). Adversarial-leg findings (codex MUST / claude SHOULD) were addressed in prior tip content, which is separate from CoS Improvements.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted by CoS.
- Doctrine bus: rewrite on `master` at `cos-feedback/adversarialllm/pr-101.md` for new head. Verdict unchanged (`merge-when-ci-green`).
