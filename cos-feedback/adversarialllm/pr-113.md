schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 113
head: f144eda359df87c0f1d45c1bdca5d63ef015d86f
reviewed_utc: 2026-09-20T21:09:34Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: run 35537763707 on tip f144eda3 — docs drift PASS (23s); factory contracts and typecheck/lint/build/test still pending; mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for required checks** — run 35537763707 still pending on `factory contracts` and `typecheck / lint / build / test`. Content is queue-only; treat GitHub required checks as the merge gate.
- **Two-leg gate (process)** — PR body correctly requires two independent legs before merge; author does not merge. CoS does not substitute for that adjudication. Both new rows inherit a codex-leg need (`ph-P3-R` by family; `GATE-ATTEMPT-SUPERSEDE` is high-risk).

## Notes

- Scope: +3/−1 in one file (`factory/queue.jsonl`) on `feat/ph-P3-R` → `master`. No `src`, tests, workflows, hooks, or scripts touched.
- Diff: `ph-P3` gains `supersededBy: ["ph-P3-R"]`; adds `ph-P3-R` (`order: 249`, `family: codex`, `dependsOn: ["P5","P9"]`) and `GATE-ATTEMPT-SUPERSEDE` (`order: 250`, `family: claude`, high-risk class owning review-state + vector table). Both `row:` tokens present in PR body.
- Intent matches Retries rule: replace unfinished `ph-P3` (adj-closed on review TOPOLOGY after two same-family receipts, not on product defect) without quietly retiring the original requirement; gate defect filed as a separate high-risk row rather than silently changing merge selection in this PR.
- `ph-P3-R` VERIFY correctly carries the occurrence-count proof for the P3 narrative block (union-resolution duplicate invisible to head-to-head diff) and points dispatch constraint at `GATE-ATTEMPT-SUPERSEDE`.
- CoS did not re-run `validateRows` / `evaluateQueue` locally; PR body claims 57 rows and `ph-P3-R` READY against full PR history — confirm via factory contracts check.
- Cursor Cloud Agents not used (queue-only scope); review via gh. No GitHub PR comment posted.