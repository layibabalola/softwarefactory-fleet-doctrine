schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 113
head: 7c9109e592058b5eba36bc79b9146d90d2588124
reviewed_utc: 2026-09-20T21:27:37Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: run 35538701670 on tip 7c9109e5 — docs drift PASS (28s); factory contracts and typecheck/lint/build/test still pending; mergeable MERGABLE / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for required checks** — run 35538701670 still pending on `factory contracts` and `typecheck / lint / build / test`. Content is queue-only; treat GitHub required checks as the merge gate.
- **Two-leg gate (process)** — PR body requires two validated independent legs on this head before merge; author does not merge. CoS does not substitute for that adjudication. This PR only files the high-risk `GATE-ATTEMPT-SUPERSEDE` row (no gate-file implementation); the eventual implement PR will need one leg per family.

## Notes

- Round-2 head rewrite vs prior tip `f144eda3`. Scope now: **+2/0** in one file (`factory/queue.jsonl`) on `feat/ph-P3-R` → `master`. No `src`, tests, workflows, hooks, or scripts touched.
- Diff adds only `GATE-ATTEMPT-SUPERSEDE` (order 249, family claude, high-risk owning review-state + vector table) and filing row `GATE-SUPERSEDE-FILE` (order 250, family claude). Prior `ph-P3-R` / `ph-P3.supersededBy` removed entirely — `ph-P3` stays `NEEDS_REDESIGN`.
- Body carries **exactly one** `row:` token: `GATE-SUPERSEDE-FILE` (title also retitled). Addresses the token-consumption hazard at `factory/queue-state.mjs:83` (merged PR body names consume rows before `supersededBy` handling). Round-1 MUSTs (claude F-01/F-02, codex F-01) and SHOULDs recorded as resolved in the PR body.
- Simulated merge claim (body): filed row stays READY/unconsumed; filing row DELIVERED via #113; unresolved 10. CoS did not re-run `validateRows` / `evaluateQueue` locally — confirm via factory contracts check (body claims 56 rows / validateRows pass).
- Branch name still `feat/ph-P3-R` from original scope; content governs.
- Cursor Cloud Agents not used (queue-only scope); review via gh. No GitHub PR comment posted.