schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 96
head: d05fd6f7c126ee95eaf96d3d1ceba271350963eb
reviewed_utc: 2026-09-21T11:29:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS (run 35594233667); mergeable MERGEABLE / mergeStateStatus UNSTABLE

## Blockers
- None on the reviewed head. Do not merge until `factory contracts` and `typecheck / lint / build / test` conclude SUCCESS on `d05fd6f7c126ee95eaf96d3d1ceba271350963eb`.

## Improvements
- Refresh the PR body: it still says "Draft. No implementation yet" / zero-byte declaration, and still lists the LICENSE copyright-holder veto as open. Record that the owner resolved acceptance 3 to the individual holder `Olayiwola Babalola` (already on `LICENSE` and pinned in `p8-ship-posture.test.ts`), and that r2 landed real implementation after the K5 declaration.
- Add a repo-root `SECURITY.md` (or a one-line pointer to `adversarialllm/SECURITY.md`) so GitHub's Security tab / policy surface picks it up; today only `adversarialllm/SECURITY.md` exists (r2 correctly softened that file's prerelease reporting route, but the root surface is still missing).
- MetricsDashboard Suspense fallback still says "Loading chart engine…" — rename to "Loading chart…" to match the single-engine posture (copy residue only; not touched in r2).

## Notes
- Head `d05fd6f7c126ee95eaf96d3d1ceba271350963eb` is P8-R r2 (Codex-family round-2 after `8ed651ccd77d9aeb27bc92125e4da30d89e50732`). Delta vs prior reviewed head: 4 files (`SECURITY.md`, `docs/28_HARDENED_IMPLEMENTATION_ROADMAP.md`, `evidence/P8-SHIP-READINESS.md`, `tests/unit/p8-ship-posture.test.ts`). Overall PR +645/−1174 across 24 files.
- Prior CoS Improvement addressed: evidence now cites `scripts/install-dev-companion.ps1` (with line refs) instead of misnamed `install.ps1`.
- Round-1 MUSTs reflected on this head: one-engine rule scoped to the extension package + dependency set (CDN Chart.js in `metrics/*.html` called out as out-of-package tooling; new posture test scans `src/` / entrypoints / public / build output / ZIPs for forbidden runtime packages); signing contract inputs now list certificate, publisher identity, signing tooling, and timestamping as absent; installer path corrected as above.
- Acceptance posture unchanged in substance: proprietary root `LICENSE` (`Olayiwola Babalola`), `private`/`UNLICENSED`, tag-exact `release-version.ts` wired into `wxt.config.ts`, single shipped chart engine `recharts`, ship boundary unsigned / unpublished.
- Owner LICENSE veto remains closed on head; PR body text is still stale (see Improvements).
