schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 96
head: 8ed651ccd77d9aeb27bc92125e4da30d89e50732
reviewed_utc: 2026-09-21T10:58:37Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test still IN_PROGRESS (run 35591094813); mergeable MERGEABLE / mergeStateStatus UNSTABLE

## Blockers
- None on the reviewed head. Do not merge until `factory contracts` and `typecheck / lint / build / test` conclude SUCCESS on `8ed651ccd77d9aeb27bc92125e4da30d89e50732`.

## Improvements
- Refresh the PR body: it still says "Draft. No implementation yet" / zero-byte declaration, and still lists the LICENSE copyright-holder veto as open. Record that the owner resolved acceptance 3 to the individual holder `Olayiwola Babalola` (already on `LICENSE` and pinned in `p8-ship-posture.test.ts`).
- Align evidence wording with acceptance 1: `P8-SHIP-READINESS.md` cites companion `install.ps1`, while the proposal names `adversarialllm/scripts/install-dev-companion.ps1` as the path that writes host name / NativeMessagingHosts. Point the evidence section at the script that actually writes those paths (or quote both with roles).
- Add a repo-root `SECURITY.md` (or a one-line pointer to `adversarialllm/SECURITY.md`) so GitHub's Security tab / policy surface picks up the reporting route; today only `adversarialllm/SECURITY.md` exists.
- MetricsDashboard Suspense fallback still says "Loading chart engine…" — rename to "Loading chart…" to match the single-engine posture (copy residue only).

## Notes
- Head `8ed651ccd77d9aeb27bc92125e4da30d89e50732` is the first real Codex-family implementation after the K5 zero-byte declaration (attempt p8r-impl-a4). Stats +528/−1173 across 24 files.
- Acceptance posture on head: proprietary root `LICENSE` (no permission / no warranty) + `package.json`/`package-lock` root `private: true` / `license: UNLICENSED`; `scripts/release-version.ts` tag-exact `v*` → else package version, wired into `wxt.config.ts`; single chart engine `recharts` (deleted ChartJS/SVG/Lightweight modules + deps `@anthropic-ai/sdk`, `chart.js`, `lightweight-charts`, `react-chartjs-2`); docs trust levers rewritten off open-source / "link to source"; evidence at `adversarialllm/evidence/P8-SHIP-READINESS.md` with native unsigned SHA-256s and search dispositions.
- ph-P2-R protected-path touches are comment-only as authorized: `native-host/client.ts` header + `App.tsx` metrics comment. No executable token changes observed in those files.
- Owner LICENSE veto: closed on this head by owner-authored commit choosing individual `Olayiwola Babalola` (not a invented company name). PR body text remains stale (see Improvements).
- Ship boundary respected: unsigned / unpublished / no store or signing actions claimed. Manifest version stays `0.0.0` until an exact `v*` tag lands on HEAD — expected.