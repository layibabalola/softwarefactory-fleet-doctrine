schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 127
head: b3eea99fc03056c191b4baa13dc42aa69f5fad57
reviewed_utc: 2026-09-23T18:32:57Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 35903194276 — all three required jobs IN_PROGRESS (docs drift, factory contracts, typecheck/lint/build/test); mergeable true / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `b3eea99f`.** Actions run 35903194276 still has docs drift, factory contracts, and typecheck/lint/build/test IN_PROGRESS. Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `GATE-FIX-FILE` is `family: claude` (implementer: claude). Body correctly flags high-risk class for the rows it files (`review-state.mjs` and `ci.ps1`). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.

## Notes

- **pr-pushed vs prior CoS head `1afa14ad`:** Tip is now `b3eea99f`. Still queue-only on `factory/queue.jsonl`. Diff vs master is now +5/−1 (was +4/−1): rewrite `GATE-ATTEMPT-SUPERSEDE` (order 249) with `supersededBy: ["GATE-ATTEMPT-SUPERSEDE-R"]`; append `GATE-ATTEMPT-SUPERSEDE-R` (311, claude), `CI-GATE-HERMETIC` (312, codex), `GATE-FIX-FILE` (313, claude), and newly **`CI-FLAKE-CLASSIFY` (314, codex, dependsOn CI-GATE-HERMETIC)**. Body row token remains `GATE-FIX-FILE` only. Ahead 2 / behind 0 (2 commits).
- **What the new row adds:** `CI-FLAKE-CLASSIFY` owns `scripts/ci.ps1` + `factory/ci-contract.test.mjs`; serializes behind hermeticity so the two rows never contend on `ci.ps1`. VERIFY: one retry on named INFRASTRUCTURE signatures (pool/worker start fail, host mutex, network install); never retry assertion/typecheck/lint/build/TIMEOUT; double infra fail stays RED with both attempts named; GREEN-AFTER-RETRY distinct from plain GREEN; no assertion/timeout/step weakened. Filing VERIFY now expects numstat exactly 5/1 and post-merge `CI-FLAKE-CLASSIFY` WAITING_DEPENDENCY on `CI-GATE-HERMETIC`.
- **Filing VERIFY spot-check:** Numstat +5/−1 matches tip; orders 311–314 sit above master max (273) and above open-branch high-water cited in body. Self-filing pattern intact (FILE delivers; GATE-ATTEMPT-SUPERSEDE → SUPERSEDED_PENDING; GATE-ATTEMPT-SUPERSEDE-R + CI-GATE-HERMETIC READY; CI-FLAKE-CLASSIFY waiting).
- **Verdict unchanged** from prior CoS on `1afa14ad` (`merge-when-ci-green`). Prior Improvements (wait CI, D2) not addressed by this push.
- **Why the original three rows:** PR #114 tier ruling specified a corrected GATE-ATTEMPT-SUPERSEDE replacement that was never filed; CI-GATE-HERMETIC records unit-before-build hermeticity defect (`scripts/ci.ps1` / `p8-ship-posture.test.ts`).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
