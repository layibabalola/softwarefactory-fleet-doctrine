schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 127
head: 1dd4bbee56ce7c6c791d329a2a657595c3255c5c
reviewed_utc: 2026-09-23T18:59:47Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 35906213423 — all three required jobs IN_PROGRESS (docs drift, factory contracts, typecheck/lint/build/test); mergeable true / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `1dd4bbee`.** Actions run 35906213423 still has docs drift, factory contracts, and typecheck/lint/build/test IN_PROGRESS. Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `GATE-FIX-FILE` is `family: claude` (implementer: claude). Body correctly flags high-risk class for the rows it files (`review-state.mjs` and `ci.ps1`). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.

## Notes

- **pr-pushed vs prior CoS head `b3eea99f`:** Tip is now `1dd4bbee`. Still queue-only on `factory/queue.jsonl`. Diff vs master remains +5/−1 (1 file). Ahead 3 / behind 0 (3 commits). Body row token remains `GATE-FIX-FILE` only.
- **What this push changes:** Spec-only rewrite of `CI-FLAKE-CLASSIFY` (order 314). Evidence corpus expanded from three gate runs to four on base `c4986b95`. New run four: `tests/unit/observe-runner-contract.test.ts` failed `expected null to be 1` where null is a `spawnSync` exit status (helper never ran); same file later 43/43 green in isolation. VERIFY now names **null or signal-terminated exit status from a spawned helper** as an INFRASTRUCTURE signature (retry once), while a numeric status that merely differs from expected is not. Fixture corpus: pool-start must retry, null spawn must retry, forbidden-package must not, timeout must not. Orders 311–314 and GATE-ATTEMPT-SUPERSEDE `supersededBy` unchanged.
- **Filing VERIFY spot-check:** Numstat +5/−1 still matches tip; orders 311–314 sit above master max (273). Self-filing pattern intact (FILE delivers; GATE-ATTEMPT-SUPERSEDE → SUPERSEDED_PENDING; GATE-ATTEMPT-SUPERSEDE-R + CI-GATE-HERMETIC READY; CI-FLAKE-CLASSIFY waiting on hermetic).
- **Verdict unchanged** from prior CoS on `b3eea99f` (`merge-when-ci-green`). Prior Improvements (wait CI, D2) not addressed by this spec enrichment.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.