schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 127
head: 1afa14ad31d03ce94668a2eb739986192582ce7c
reviewed_utc: 2026-09-23T10:57:00Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 35851649048 — all three required jobs IN_PROGRESS (docs drift, factory contracts, typecheck/lint/build/test); mergeable true / UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `1afa14ad`.** Actions run 35851649048 still has docs drift, factory contracts, and typecheck/lint/build/test IN_PROGRESS. Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `GATE-FIX-FILE` is `family: claude` (implementer: claude). Body correctly flags high-risk class for the rows it files (`review-state.mjs` and `ci.ps1`). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.

## Notes

- **pr-opened: queue-only filing PR.** Tip `1afa14ad` is `GATE-FIX-FILE: file the gate-fix rows`. PR vs master: `factory/queue.jsonl` +4/−1, 1 file, ahead 1 / behind 0. Diff: rewrite `GATE-ATTEMPT-SUPERSEDE` (order 249) to add `supersededBy: ["GATE-ATTEMPT-SUPERSEDE-R"]`; append `GATE-ATTEMPT-SUPERSEDE-R` (311, claude), `CI-GATE-HERMETIC` (312, codex), `GATE-FIX-FILE` (313, claude). Body row token `GATE-FIX-FILE` only.
- **Filing VERIFY spot-check (from PR body + diff):** Numstat +4/−1 matches; orders 311–313 sit above master max (273) and above open #126 high-water (305). Self-filing pattern matches PR 113 / PR 117 / PR 125 / PR 126 (FILE delivers; GATE-ATTEMPT-SUPERSEDE → SUPERSEDED_PENDING; GATE-ATTEMPT-SUPERSEDE-R and CI-GATE-HERMETIC stay READY/unconsumed).
- **Why these rows:** PR #114 tier ruling specified a corrected GATE-ATTEMPT-SUPERSEDE replacement (order-invariant selector; superseded attempt out of both pair check and UNRESOLVED_FINDINGS; mutation coverage on guards) that was never filed — defect that stranded PR #53 / `ph-P3` still live. CI-GATE-HERMETIC records unit-before-build hermeticity defect in `scripts/ci.ps1` / `p8-ship-posture.test.ts` (stale `output/` can fail or falsely pass).
- **Sibling open filing PRs:** #125 (orders 274/275) and #126 (301–305) share the same master base. Merge order among queue-only FILEs is free as long as order numbers do not collide (they do not).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
