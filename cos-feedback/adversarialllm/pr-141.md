schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 141
head: 0e60d00b088bd3e6c8f8c9e2c74cdd02e8b9bedd
reviewed_utc: 2026-09-25T11:40:04Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36130575380 — factory contracts IN_PROGRESS, typecheck/lint/build/test QUEUED, docs drift QUEUED; MERGEABLE/UNSTABLE; ahead 2 / behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `0e60d00b`.** Actions run 36130575380 still has factory contracts IN_PROGRESS and the other two required jobs QUEUED on the self-hosted Windows runner. Author claims local focused fixtures 10/10 + full root CI green on this SHA; hosted checks are still the merge gate.
- **Soft: sidepanel imports a background webhook module.** `CapabilityPanel.tsx` pulls `DEFAULT_WEBHOOK_CONFIG` from `@/background/webhook/webhook-gate`. That file is pure today, but the sidepanel UI now depends on a background path. Prefer a shared constants export (or re-export from `@/shared`) so UI never imports under `background/`.
- **Soft: camelCase label fallback is awkward.** New SF keys without a `labels` entry render via `key.replace(/([a-z])([A-Z])/g, '$1 $2')` (test expects `future Feature`). Title-case or a small `humanizeKey` helper would read better when a flag lands before a display label is added.
- **Two independent review legs still required (D2).** Author notes this PR does not assert delivery or waive D2; prior PR140 controller remains quarantined. Conductor/controller must still dispatch non-author legs before merge.

## Notes

- **What changed:** EA-2 capability panel. New `CapabilityPanel.tsx` (+41) lists disabled build capabilities from `M1_ROUND_ENGINE_ENABLED`, `BUNDLE_PACKAGER_ENABLED`, every `DEFAULT_SF_FLAGS` field, and `DEFAULT_WEBHOOK_CONFIG.enabled`. Mounted in `App.tsx` beside `FirstRunCard`. Unit tests (+65) cover delivered defaults, flipped exports, and a newly added SF field without a labels entry. Docs inventory anchors for RecipeGallery/ReportReader bumped +2 lines after the App insertion (no flag/behavior change).
- **Scope:** +110/−2 across 4 files, 2 commits (`11c3d179` panel+tests; `0e60d00b` inventory regenerate). Ahead 2 / behind 0 vs master `cfd101cf`. No campaign behavior or flag-value changes.
- **Intentional scope limit:** panel reads **exported defaults**, not runtime `resolveSfFlags` / IndexedDB. Matches row EA-2 ("build capabilities"); user-toggled SF storage would not appear here.
- **Author validation (claimed, not re-run here):** capability fixtures 10/10; typecheck + scoped lint; full root CI green locally on `0e60d00b` base `cfd101cf`; evidence under `.factory-local/focused-a1.*`, `focused-a2.*`, `ci-0e60d00b-a3.*`. First gate hit stale inventory anchors (regenerated); second attempt abandoned host lease (receipt kept).
- No secrets in diff. Review via `gh`. No GitHub PR comment posted.