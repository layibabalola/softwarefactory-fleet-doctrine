schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 94
head: 95533171f1f6f3799c79b0804a5f7ed2aba435e3
reviewed_utc: 2026-09-17T22:50:25Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: all three required checks SUCCESS on head 95533171 (Actions run 35283087244): docs drift 28s, factory contracts 8m0s, typecheck/lint/build/test 10m15s

## Blockers

- **Dual-family D2** — 0 GitHub reviews on tip yet. Land independent Sol + Opus APPROVE on this head, then merge by a fresh non-author controller (same gate as other adversarial factory amends). Author is layibabalola / implementer Codex per PR body. CI is no longer blocking.

## Improvements

- **Carry CoS #93 ferry non-scheduled ingest into R2** — prior CoS on #93 (head `e086d81e`) blocked on `handleFerriedRunResult` rejecting campaigns without `scheduledRun` (`FERRY_INGEST_REJECTED`), regressing the P1 everyday ferry path. `factory/proposals/ph-P2-R2.md` strengthens schema-complete `ReportModel` validation (Sol round-2 MUST `PH-P2-R-MALFORMED-REPORT-SHAPE`) and says it preserves the full `ph-P2-R` contract, but still does not name that non-scheduled apply path. Prefer an explicit acceptance bullet + focused regression in R2 (or confirm out of R2 scope with rationale) before the replacement implementation PR opens. Unchanged vs prior CoS on this PR.
- Queue supersession shape confirmed accepted by `factory contracts` on this head: `ph-P2-R.supersededBy: ["ph-P2-R2"]`, new `ph-P2-R2` order 232 / `dependsOn: ["P5","F1.2-CI"]` / `family: codex`, and `ph-P2.supersededBy` remains `["ph-P2-R"]` only (chain via R→R2).

## Notes

- Push event: merge of `origin/master` into `codex/ph-P2-R2-amend` (commit `95533171`); factory amend content (+29/−1, 3 files) otherwise same as prior tip `dd0f34fa`. Prior CoS was `not-merge-ready` for all-red CI + missing D2; CI anomaly cleared — only D2 remains.
- Scope: factory amend only. Adds `factory/adjudications/ph-P2-pr93-round-cap.md`, `factory/proposals/ph-P2-R2.md`, and queue supersession for unfinished `ph-P2-R` after PR #93 closed `adj-close` at the two-round cap (not merged). Grants no live authority and does not deliver the replacement — matches #83/#88/#91 amend pattern.
- Relates to CoS on #93 (`not-merge-ready`): #93 tip `e086d81e` closed unmerged; adjudication records Sol round-2 CHANGES_REQUESTED on malformed report-shape admission and Opus APPROVE. This PR proposes the reviewed schema-complete successor row, not a third #93 round.
- Content posture of the proposal text still looks coherent for an amend: preserves `ph-P2-R` requirements, adds pre-transaction full `ReportModel` / nested optional validation, no-write `malformed-terminal` regressions, and restates no live/provider/spend authority.
- Branch `codex/ph-P2-R2-amend` → `master`; MERGEABLE; mergeStateStatus was UNSTABLE while checks ran, then CI green; not draft.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: rewrite on `master` at `cos-feedback/adversarialllm/pr-94.md` for new head.
