schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 94
head: dd0f34fa527a162bf95106c0522f59fe1829751a
reviewed_utc: 2026-09-17T03:10:53Z
event: pr-opened
verdict: not-merge-ready
ci_summary: docs drift (delta gate) + factory contracts + typecheck/lint/build/test all FAILURE on head dd0f34fa (Actions run 35177066344); each job ~3s with empty steps — likely cancel/admission failure, not a content TAP yet; re-run required

## Blockers

- **CI: all three required checks FAILURE** — `docs drift (delta gate)`, `factory contracts`, and `typecheck / lint / build / test` all concluded FAILURE on head `dd0f34fa527a162bf95106c0522f59fe1829751a` (Actions run 35177066344). Jobs completed in ~3s with empty `steps` and no job logs — treat as runner/admission/cancel anomaly until a re-run produces real output. Unblock: re-run the workflow on this head (or push a no-op if re-run unavailable); if a re-run surfaces real contract/TAP failures, fix those before merge.
- **Dual-family D2** — 0 GitHub reviews on tip yet. Land independent Sol + Opus APPROVE on this head, then merge by a fresh non-author controller (same gate as other adversarial factory amends). Author is layibabalola / implementer Codex per PR body.

## Improvements

- **Carry CoS #93 ferry non-scheduled ingest into R2** — prior CoS on #93 (head `e086d81e`) blocked on `handleFerriedRunResult` rejecting campaigns without `scheduledRun` (`FERRY_INGEST_REJECTED`), regressing the P1 everyday ferry path. `factory/proposals/ph-P2-R2.md` strengthens schema-complete `ReportModel` validation (Sol round-2 MUST `PH-P2-R-MALFORMED-REPORT-SHAPE`) and says it preserves the full `ph-P2-R` contract, but does not name that non-scheduled apply path. Prefer an explicit acceptance bullet + focused regression in R2 (or confirm out of R2 scope with rationale) before the replacement implementation PR opens.
- Spot-check after CI re-run that `factory contracts` accepts the queue delta: `ph-P2-R.supersededBy: ["ph-P2-R2"]`, new `ph-P2-R2` order 232 / `dependsOn: ["P5","F1.2-CI"]` / `family: codex`, and that `ph-P2.supersededBy` remains `["ph-P2-R"]` only (chain via R→R2, not a direct P2→R2 jump). Confirmed present in the diff; contracts must agree.

## Notes

- Scope: factory amend only (+29/−1, 3 files). Adds `factory/adjudications/ph-P2-pr93-round-cap.md`, `factory/proposals/ph-P2-R2.md`, and queue supersession for unfinished `ph-P2-R` after PR #93 closed `adj-close` at the two-round cap (not merged). Grants no live authority and does not deliver the replacement — matches #83/#88/#91 amend pattern.
- Relates to CoS on #93 (`not-merge-ready`): #93 tip `e086d81e` closed unmerged; adjudication records Sol round-2 CHANGES_REQUESTED on malformed report-shape admission and Opus APPROVE. This PR proposes the reviewed schema-complete successor row, not a third #93 round.
- Content posture of the proposal text looks coherent for an amend: preserves `ph-P2-R` requirements, adds pre-transaction full `ReportModel` / nested optional validation, no-write `malformed-terminal` regressions, and restates no live/provider/spend authority. Merge posture blocked only by red CI + missing D2, not by proposal prose defects spotted in this pass.
- Branch `codex/ph-P2-R2-amend` → `master`; MERGEABLE; not draft; behind_by n/a / tip `dd0f34fa`. Single commit message: `factory: bind full ph-P2 report schema`.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: first publish on `master` at `cos-feedback/adversarialllm/pr-94.md` (prior file 404).