schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 116
head: d426a42aa7bd84f33f3a539bd8a591b4582132e3
reviewed_utc: 2026-09-21T01:11:58Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS (CI run 35550048623)

## Blockers

(none)

## Improvements

- **Two-leg gate still open on this head.** PR body: fresh attempt with its own two-round cap; needs two independent legs. GitHub Reviews API returns zero formal review objects at review time — dispatch/record both families against `d426a42a` before merge.
- After legs land, confirm `queue-state` simulation still shows `ph-P3` SUPERSEDED_PENDING waiting on both `ph-P3-R` and `ph-P3-LIVE` (not SUPERSEDED_DELIVERED from `ph-P3-R` alone).

## Notes

- Re-file after #115 was adj-closed (round 2 of 2 CONSUMED on empty tip `e090cc54`). Material correction is the VERIFY contract: base-to-head numstat via `git diff --numstat` against base SHA (not `git status`), and body row token must resolve in `factory/queue.jsonl` at the committed head.
- Spot-check at head `d426a42a`: 59 rows; `PH-P3-FILE-R` present (order 262); `ph-P3.supersededBy` = `[ph-P3-R, ph-P3-LIVE]`; appends `ph-P3-R` (261), `ph-P3-LIVE` (263). Compare `master...d426a42a`: ahead_by 1, `factory/queue.jsonl` +4/−1 (numstat confirmed).
- `ph-P3-LIVE` VERIFY hardened vs #115 round 1: separate processes, named evidence under `adversarialllm/evidence/`, explicit `EXTERNAL_CAPABILITY_UNAVAILABLE` when capability absent.
- Scope remains queue-only; no `src`/tests/workflows on the tip. Cursor Cloud Agent not used; review via `gh`. No GitHub PR comment posted.