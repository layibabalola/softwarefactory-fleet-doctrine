schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 116
head: 0b96be6ee0ce5674e18465e7dcb9d7dc317ad7b9
reviewed_utc: 2026-09-21T01:25:13Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS (CI run 35550761322)

## Blockers

(none)

## Improvements

- **Two-leg gate still open on this head.** PR body: fresh attempt with its own two-round cap; needs two independent legs. GitHub Reviews API still returns zero formal review objects at review time — dispatch/record both families against `0b96be6e` before merge. Commit message claims a claude APPROVE with zero items on the amendment; that is not yet visible as a GitHub Review object.
- After legs land, confirm `queue-state` simulation still shows `ph-P3` SUPERSEDED_PENDING waiting on both `ph-P3-R` and `ph-P3-LIVE` (not SUPERSEDED_DELIVERED from `ph-P3-R` alone).

## Notes

- Round-1 amendment at `0b96be6e` (after open head `d426a42a`): rewrites `ph-P3-LIVE` VERIFY only. Compare `master...0b96be6e`: ahead_by 2, `factory/queue.jsonl` +4/−1 (numstat confirmed). Spot-check: 59 rows; `PH-P3-FILE-R` present (order 262); `ph-P3.supersededBy` = `[ph-P3-R, ph-P3-LIVE]`.
- **LIVE-DISTINCT-DEVICES:** VERIFY now requires two clients on DISTINCT NAMED DEVICES with independently verifiable device identity AND process identity; two processes on one device do not satisfy; missing second device → `EXTERNAL_CAPABILITY_UNAVAILABLE` naming the device (no local-process substitute).
- **LIVE-AUTHORITY-MISMATCH:** `R-LIVE` restated as NECESSARY BUT NOT SUFFICIENT (D10 gate only; L1–L6 / L3 are single-host campaign authority, opposite topology). Delivery additionally requires reviewed authority enumerating reach transport, endpoints, traffic bounds, data shape, and evidence contract; absent that, report `EXTERNAL_CAPABILITY_UNAVAILABLE` and never deliver by citing `R-LIVE` alone. `dependsOn` still `[ph-P3-R, R-LIVE]`.
- Filing VERIFY contract from open review still holds: base-to-head numstat via `git diff --numstat` against base SHA; body row token must resolve in `factory/queue.jsonl` at committed head.
- Scope remains queue-only; no `src`/tests/workflows on the tip. Cursor Cloud Agent not used; review via `gh`. No GitHub PR comment posted.