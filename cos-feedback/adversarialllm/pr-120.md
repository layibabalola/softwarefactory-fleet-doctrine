schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 120
head: e98c6258cbf812bd8612b05dea7da821b361a242
reviewed_utc: 2026-09-22T04:02:21Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 35685270827 — docs drift PASS; factory contracts and typecheck/lint/build/test IN_PROGRESS; mergeable true / unstable; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for remaining CI green on `e98c6258`.** `docs drift (delta gate)` already PASS on Actions run 35685270827; `factory contracts` and `typecheck / lint / build / test` were still IN_PROGRESS at review time. Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `CI-PROFILE-PATH-FILE` is `family: claude` (implementer: claude). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.
- **Local gate not green on this head (author report).** PR body: three local-gate attempts failed on infrastructure unrelated to this queue-only diff (stale ignored `adversarialllm/output/` scanned by `p8-ship-posture`; vitest worker startup timeouts on fresh `node_modules`; `cleanup-host.test.mjs` RETAIN vs ELIGIBLE). Offline-browser itself passed from a 72-char root. Controllers should still require a green local gate on the merge head per factory practice — clear the infra noise or re-run from a clean worktree before merge.

## Notes

- **Queue-only filing (+2/−0).** Diff vs master is only `factory/queue.jsonl`. Appends `CI-PROFILE-PATH` (order 268, claude) and `CI-PROFILE-PATH-FILE` (order 269, claude). Body row token is exactly `CI-PROFILE-PATH-FILE`. Pattern matches GATE-SUPERSEDE-FILE / PH-P3-FILE-R2: this PR delivers the FILE row so the filed fix row stays unconsumed / READY after merge.
- **Spot-check on tip:** 63 rows, unique orders 1..269, 0 CR bytes, no dup ids. Highest order among other open PR tips at review time remains 267 on #119 — orders 268/269 sit above it as claimed.
- **Filed defect (data only):** Windows offline-browser fails above ~96-char checkout roots because `createFixtureProfilePath()` puts Chrome profile under `.factory-local/p7-playwright-profile-*`, pushing IndexedDB LevelDB paths past Win32 MAX_PATH. Spec owns only `fixture-page.ts` + `two-provider-two-round.test.ts` (shorten mkdtemp prefix ≤4 chars; win32 preflight fail-fast; IndexedDB existence assert; measure `--output` / `p7-downloads` lengths; reject out-of-tree `os.tmpdir()`). Implementation is a later row, not this PR.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.