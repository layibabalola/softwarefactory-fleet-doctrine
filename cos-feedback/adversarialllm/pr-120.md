schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 120
head: 485f76c931996bfb98b581030cf45a675c7da16b
reviewed_utc: 2026-09-22T21:42:25Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 35788173048 — docs drift, factory contracts, and typecheck/lint/build/test all QUEUED/pending at review; mergeable true / unstable; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `485f76c9`.** Actions run 35788173048 had all three required jobs still pending at review (`docs drift (delta gate)`, `factory contracts`, `typecheck / lint / build / test`). Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `CI-PROFILE-PATH-FILE` is `family: claude` (implementer: claude). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.
- **Local gate not green on prior tip (author report on `e98c6258`).** PR body: three local-gate attempts failed on infrastructure unrelated to this queue-only diff (stale ignored `adversarialllm/output/` scanned by `p8-ship-posture`; vitest worker startup timeouts on fresh `node_modules`; `cleanup-host.test.mjs` RETAIN vs ELIGIBLE). Offline-browser itself passed from a 72-char root. Controllers should still require a green local gate on the merge head per factory practice — clear the infra noise or re-run from a clean worktree before merge. No new local-gate evidence was attached for `485f76c9`.

## Notes

- **pr-pushed after second master merge.** Tip `485f76c9` is `Merge origin/master (21de5697) to resolve the queue.jsonl append conflict` on prior tip `f90ca1be`. PR vs master remains queue-only: `factory/queue.jsonl` +2/−0, ahead 3 / behind 0. Exclusive tip rows vs master: `CI-PROFILE-PATH` (order 268) and `CI-PROFILE-PATH-FILE` (order 269). Body row token still `CI-PROFILE-PATH-FILE`.
- **Spot-check on tip:** 67 rows, unique orders 1..271, 0 CR bytes, no dup ids. Master now holds through order 271 (`CONDUCTOR-PREFLIGHT-TIMEOUT-FILE` / `CONDUCTOR-PREFLIGHT-TIMEOUT`); this PR's exclusive rows 268/269 remain unique and sit below those later-landed master orders after the conflict merge — filing-time VERIFY (above open-PR max 267) still holds historically.
- **Verdict unchanged** vs prior CoS on `f90ca1be` (`merge-when-ci-green`). Prior Improvements (wait CI, D2, local-gate infra) not clearly addressed by the conflict-resolution merge alone.
- **Filed defect (data only):** Windows offline-browser fails above ~96-char checkout roots because `createFixtureProfilePath()` puts Chrome profile under `.factory-local/p7-playwright-profile-*`, pushing IndexedDB LevelDB paths past Win32 MAX_PATH. Spec owns only `fixture-page.ts` + `two-provider-two-round.test.ts` (shorten mkdtemp prefix ≤4 chars; win32 preflight fail-fast; IndexedDB existence assert; measure `--output` / `p7-downloads` lengths; reject out-of-tree `os.tmpdir()`). Implementation is a later row, not this PR.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
