schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 120
head: 1afcf091ff68e77bb160cac46a27d0dd75e37250
reviewed_utc: 2026-09-22T21:45:57Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 35788454603 pending (no jobs yet at review); check suites github-actions pending, cursor+netlify queued; mergeable true / CLEAN; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `1afcf091`.** Actions run 35788454603 was still pending with zero jobs at review. Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `CI-PROFILE-PATH-FILE` is `family: claude` (implementer: claude). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.
- **Local gate not green on prior tip (author report on `e98c6258`).** PR body: three local-gate attempts failed on infrastructure unrelated to this queue-only diff (stale ignored `adversarialllm/output/` scanned by `p8-ship-posture`; vitest worker startup timeouts on fresh `node_modules`; `cleanup-host.test.mjs` RETAIN vs ELIGIBLE). Offline-browser itself passed from a 72-char root. Controllers should still require a green local gate on the merge head per factory practice — clear the infra noise or re-run from a clean worktree before merge. No new local-gate evidence was attached for `1afcf091`.

## Notes

- **pr-pushed: renumber after #121.** Tip `1afcf091` is `CI-PROFILE-PATH-FILE: renumber to 272/273 and re-derive the order clause against master 21de5697` on prior tip `485f76c9`. PR 121 merged orders 270/271, so prior exclusive orders 268/269 and the VERIFY high-water naming 267 were stale. PR vs master remains queue-only: `factory/queue.jsonl` +2/−0, ahead 4 / behind 0. Exclusive tip rows vs master: `CI-PROFILE-PATH` (order 272) and `CI-PROFILE-PATH-FILE` (order 273). Body row token still `CI-PROFILE-PATH-FILE`.
- **Spot-check on tip:** 67 rows, unique orders 1..273, 0 CR bytes, no dup ids. Master holds through 271; exclusive 272/273 sit above master max and above the open-PR high-water cited in the FILE VERIFY (260 on PR 104). Filing-time VERIFY re-derived correctly for post-#121 master.
- **Verdict unchanged** vs prior CoS on `485f76c9` (`merge-when-ci-green`). Prior Improvements (wait CI, D2, local-gate infra) not clearly addressed by the renumber alone.
- **Filed defect (data only):** Windows offline-browser fails above ~96-char checkout roots because `createFixtureProfilePath()` puts Chrome profile under `.factory-local/p7-playwright-profile-*`, pushing IndexedDB LevelDB paths past Win32 MAX_PATH. Spec owns only `fixture-page.ts` + `two-provider-two-round.test.ts` (shorten mkdtemp prefix ≤4 chars; win32 preflight fail-fast; IndexedDB existence assert; measure `--output` / `p7-downloads` lengths; reject out-of-tree `os.tmpdir()`). Implementation is a later row, not this PR.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.