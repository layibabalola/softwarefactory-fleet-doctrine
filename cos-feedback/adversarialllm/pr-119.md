schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 119
head: db07493249191a48ad1cd4ef5b0c82e71b769a93
reviewed_utc: 2026-09-21T09:40:54Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS on run 35584513318; factory contracts + typecheck/lint/build/test still IN_PROGRESS; mergeable MERGEABLE/unstable; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for remaining CI green on `db074932`.** `factory contracts` and `typecheck / lint / build / test` were still IN_PROGRESS on Actions run 35584513318. Do not merge on docs-drift alone.
- **Two-leg / D2 still open.** Filing row `PH-P3-R-REFILE` is `family: claude` (implementer: claude). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.

## Notes

- **Successor to adj-closed #118** (`ph-P3-R` offline reach delivery). #118 closed without merge at round 2 of 2; both families rejected (codex `DISABLE-01` / `RETRY-LATE-WRITE-01`, claude `DISABLE-01-RECHECK-UNPINNED`). Under HUB D2 the row is NEEDS_REDESIGN; this PR files the RESET-PLAN point-1 replacement.
- Compare `master...db074932`: ahead_by 1, `factory/queue.jsonl` +3/−1 only (numstat confirmed). Spot-check: 63 rows (was 61); `PH-P3-R-REFILE` order 266 (claude); `ph-P3-R2` order 267 (codex, `dependsOn: [P5, P9]`); `ph-P3-R.supersededBy` = `[ph-P3-R2]` and no other field change; no duplicate orders; 0 CR bytes. Orders 266/267 sit above master max (265) and every sampled open-PR head max (highest elsewhere #104 at 260).
- Body row token is exactly `PH-P3-R-REFILE` (resolves at committed head). This merge delivers the filing row only — `ph-P3-R2` stays unconsumed / READY; `ph-P3-LIVE-PROP` keeps waiting until `ph-P3-R2` delivers (`ph-P3-R` reaches SUPERSEDED_DELIVERED then).
- `ph-P3-R2` keeps the full `ph-P3-R` spec binding by reference and adds six mutation-checkable VERIFY clauses: (1) per-site disable pins, (2) disable aborts in-flight POST/sync, (3) no late overwrite from timed-out `storage.sync.set`, (4) stuck-port tests per deadline wrapper, (5) strict-boolean opt-in flags, (6) boot reconciliation pinned through `deliver()`. Cites #118 head `84fa9238` as same-family reference only. Out of scope: `question` field disclosure (future reach opt-in UI).
- Scope is queue-only; no `src`/tests/workflows on the tip. Cursor Cloud Agent not used; review via `gh`. No GitHub PR comment posted.