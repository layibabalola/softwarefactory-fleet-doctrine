schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 114
head: d29889236989f42a26d6996477f83aee4c6e1e9b
reviewed_utc: 2026-09-20T22:33:15Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: run 35542030163 on tip d2988923 — docs drift, factory contracts, and typecheck/lint/build/test all IN_PROGRESS; mergeable MERGEABLE; 0 GitHub reviews; author claims exact full scripts/ci.ps1 GREEN in 943s locally on this tip

## Blockers

(none)

## Improvements

- **Wait for required checks** — run 35542030163 still pending on all three required jobs (`docs drift (delta gate)`, `factory contracts`, `typecheck / lint / build / test`). Treat GitHub required checks as the merge gate; local `ci.ps1` green is supporting evidence only.
- **Two-leg gate (process)** — high-risk ownership of `factory/review-state.mjs` + D2 selection. PR body requires two validated independent legs on this head before merge; author does not merge. CoS does not substitute for that adjudication.
- **Degraded path leaves supersede unused** — `selectReviewAttempt` is applied on the non-degraded branch and in `evaluateLegacyMerge`, but the degraded topology still keys off raw `available` / `valid` counts (exactly three same-family attack surfaces). If a later repair needs to supersede one of three degraded surfaces, today's path still fails closed via `DEGRADED_REVIEW_TOPOLOGY`. Confirm that is intentional for GATE-ATTEMPT-SUPERSEDE scope; otherwise file a follow-up for degraded supersede selection.

## Notes

- Scope: **+61/−9** across 2 files on `codex/GATE-ATTEMPT-SUPERSEDE` → `master`. Implements the `GATE-ATTEMPT-SUPERSEDE` row filed by #113 (queue-only); this is the gate-file / review-state change.
- Mechanism: new `selectReviewAttempt` keeps same PR/head/base/family reviews, requires each `attempt.supersedes` (when present) to be a string naming another id in that set (not self), drops superseded ids, and returns the sole survivor — else `null` → `MISSING_OR_AMBIGUOUS_REVIEW`. Unmarked duplicates, invalid/cross-family/self markers, and infra failures stay ambiguous (fail closed). Wired into both `evaluateLegacyMerge` and non-degraded `evaluateMerge`; decision reviews use `selected` when not degraded.
- Tests: three new cases in `factory/review-state.test.mjs` — explicit repaired APPROVE superseding round-2 CHANGES_REQUESTED; invalid marker vectors (`missing`, cross-family, self); infra-failure never counts as superseded/selected. Body claims focused suite 20/20 PASS.
- `row:GATE-ATTEMPT-SUPERSEDE` present; implementer codex. No live provider/account/spend/credential/master mutation claimed.
- Cursor Cloud Agents not used (focused review-state + tests; reviewable via gh). No GitHub PR comment posted.
