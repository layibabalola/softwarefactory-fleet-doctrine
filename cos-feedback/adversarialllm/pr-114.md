schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 114
head: 37fde140eec571707e8eebe799dec0b628915260
reviewed_utc: 2026-09-20T23:03:25Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: run 35543431035 on tip 37fde140 — docs drift SUCCESS; factory contracts and typecheck/lint/build/test IN_PROGRESS; mergeable MERGEABLE; 0 GitHub reviews; prior tip d2988923 had author claim of exact full scripts/ci.ps1 GREEN locally

## Blockers

(none)

## Improvements

- **Wait for required checks** — run 35543431035 still pending on `factory contracts` and `typecheck / lint / build / test` (`docs drift (delta gate)` already SUCCESS). Treat GitHub required checks as the merge gate; local `ci.ps1` green is supporting evidence only.
- **Two-leg gate (process)** — high-risk ownership of `factory/review-state.mjs` + D2 selection. PR body requires two validated independent legs on this head before merge; author does not merge. CoS does not substitute for that adjudication.
- **Degraded path still leaves supersede unused for decisions** — push `37fde140` tightens fail-closed topology inside `selectReviewAttempt` (no forward/chained/cross-round/duplicate/multi-repair markers) and switches degraded surface/count checks to `available` (exclude unavailable family). Decision path still uses `decisionReviews = degraded ? valid : selected`, so repaired supersession still does not select among three degraded same-family surfaces. Confirm intentional for GATE-ATTEMPT-SUPERSEDE scope; otherwise file a follow-up.

## Notes

- Push delta vs prior CoS head `d2988923`: commit `37fde140` "fix(review): reject ambiguous supersession topology" — **+29/−2** across the same 2 files (`factory/review-state.mjs`, `factory/review-state.test.mjs`). Cumulative PR now **+88/−9**.
- `selectReviewAttempt` now also refuses duplicate attempt ids in the matching set, requires supersede targets to appear earlier in matching order, same round, no `supersedes` on the target, and no second repair of an already-superseded id.
- New test: refuses cross-round, forward, chained, duplicate, and multiple-repair topology. Prior cases (explicit repaired APPROVE; invalid markers; infra never counts) retained.
- Mechanism unchanged in intent: unmarked duplicates / invalid markers / infra failures stay ambiguous (`MISSING_OR_AMBIGUOUS_REVIEW`). Wired in `evaluateLegacyMerge` and non-degraded `evaluateMerge`.
- `row:GATE-ATTEMPT-SUPERSEDE`; implementer codex. No live provider/account/spend/credential/master mutation claimed.
- Cursor Cloud Agents not used (focused review-state + tests; reviewable via gh). No GitHub PR comment posted.
