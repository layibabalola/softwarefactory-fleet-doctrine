schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 115
head: a60daab53b41ce9ad5177e65f47a90028340bfb8
reviewed_utc: 2026-09-21T00:25:58Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: run 35547636932 on tip a60daab5 — docs drift PASS (26s); factory contracts and typecheck/lint/build/test IN_PROGRESS; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for required checks** — run 35547636932 still pending on `factory contracts` and `typecheck / lint / build / test` (`docs drift (delta gate)` already PASS). Content is queue-only; treat GitHub required checks as the merge gate. Body claims `validateRows` pass at 58 rows — confirm via the factory contracts job.
- **Two-leg gate (process)** — PR body requires two validated independent legs on this head before merge; author does not merge. CoS does not substitute for that adjudication. This PR only files the replacement product row (no reach-delivery implementation); the eventual `ph-P3-R` implement PR will need one leg per family and the dispatch constraint named in the new row.

## Notes

- Scope: **+3/−1** in one file (`factory/queue.jsonl`) on `feat/PH-P3-FILE` → `master`. No `src`, tests, workflows, hooks, or scripts touched.
- Diff: (1) `ph-P3` gains `supersededBy: ["ph-P3-R"]` and is otherwise unchanged; (2) appends `ph-P3-R` (order 251, family codex, dependsOn P5+P9) — material narrowing of opt-in reach delivery to offline-provable VERIFY, with live cross-device verification explicitly excluded pending D10/`R-LIVE`; (3) appends filing row `PH-P3-FILE` (order 252, family claude). Same pattern as `GATE-SUPERSEDE-FILE` in #113.
- Body carries **exactly one** `row:` token: `PH-P3-FILE`. Simulated merge claim: `ph-P3` SUPERSEDED_PENDING, `ph-P3-R` READY unconsumed, `PH-P3-FILE` DELIVERED. CoS did not re-run `validateRows` / `evaluateQueue` locally.
- Material-correction claim vs HUB Retries: body cites `docs/28` §P5 ph-P3 ending sentence (offline fixture ≠ live cross-device) as the defect being corrected by narrowing. Review legs should confirm that is a bounded material correction rather than a bare re-file.
- `ph-P3-R` inherits family codex and carries a dispatch constraint from #53 (one recorded attempt per family on final head) until corrected `GATE-ATTEMPT-SUPERSEDE` lands (#114 in flight / NEEDS_REDESIGN history).
- Cursor Cloud Agents not used (queue-only scope); review via gh. No GitHub PR comment posted.
