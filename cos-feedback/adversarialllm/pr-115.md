schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 115
head: 85ccdf9131d87ba45b6b25e2840642289dcdeb1d
reviewed_utc: 2026-09-21T01:07:21Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS (CI run 35549804448)

## Blockers

(none)

## Improvements

- **Two-leg gate still open on this head.** PR body says round 1 of 2 is consumed and needs two independent legs on the corrective tip. GitHub Reviews API still returns zero formal review objects at review time — dispatch/record both families against `85ccdf91` before merge.
- After legs land, confirm `queue-state` simulation still shows `ph-P3` SUPERSEDED_PENDING waiting on both `ph-P3-R` and `ph-P3-LIVE` (not SUPERSEDED_DELIVERED from `ph-P3-R` alone).

## Notes

- Prior CoS head was `e090cc5429eb` (verdict `not-merge-ready`: tip emptied filing rows vs `master`, `files:[]`). This push (`85ccdf91`) restores the decomposition the body describes: `ph-P3.supersededBy` = `[ph-P3-R, ph-P3-LIVE]`; appends `ph-P3-R` (261), `PH-P3-FILE` (262), `ph-P3-LIVE` (263). Compare `master...85ccdf91`: ahead_by 3, `factory/queue.jsonl` +4/−1.
- Spot-check: 59 rows, unique ids and orders, `dependsOn`/`supersededBy` refs resolve (`R-LIVE` present). Scope remains queue-only; no `src`/tests/workflows on the tip.
- Commit message: e090cc54 had staged INDEX-only while the 59-row decomposition sat unstaged; this tip commits the intended content. Verdict changed from `not-merge-ready` → `merge-when-ci-green` (empty-tip blocker cleared).
- Cursor Cloud Agent not used; review via `gh`. No GitHub PR comment posted.