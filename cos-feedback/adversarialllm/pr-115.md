schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 115
head: e090cc5429eb80bd458ed1469c8383d0a954b255
reviewed_utc: 2026-09-21T00:55:30Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: run 35549116677 on tip e090cc54 — docs drift PASS; factory contracts and typecheck/lint/build/test IN_PROGRESS; tip has zero file diff vs master

## Blockers

- **Empty tip / filing rows removed** — head `e090cc5429eb` (commit message: "round 1 amendment: decompose ph-P3 into offline and live halves") deletes `ph-P3-R` and `PH-P3-FILE` and strips `supersededBy` from `ph-P3`, restoring `factory/queue.jsonl` to byte-identical with `master` (56 lines; compare `master...e090cc54` reports `files: []`). Claimed rows `ph-P3-R` / `ph-P3-LIVE` / `PH-P3-FILE` at orders 261/262/263 are **absent** from the tip. PR body still describes the prior narrowing (+3/−1 filing). Fix: push a head that actually appends the decomposition (`ph-P3` gains `supersededBy: ["ph-P3-R","ph-P3-LIVE"]`, plus the three new rows with unique orders), or close and re-file; do not merge an empty tip.
- **Two-leg gate still open** — commit message says both legs returned CHANGES_REQUESTED (codex `RETRY-REFILE`, claude `PR115-R-LIVE-DOES-NOT-TRACK-THE-EXCLUDED-GATE`). Amendment intent answers those, but the file change does not. Re-dispatch legs only after a non-empty corrective head lands.

## Improvements

- **Align PR body with tip** — body still narrates the first-head narrowing simulation (`ph-P3` SUPERSEDED_PENDING → `ph-P3-R` READY); rewrite to the decomposition story once the queue rows exist on the tip.
- **Wait for required checks** on the corrective head — current run 35549116677 is largely moot while the tip is a no-op vs `master`.

## Notes

- Prior CoS head was `a60daab53b41` (verdict `merge-when-ci-green`, +3/−1 filing `ph-P3-R` + `PH-P3-FILE`). This push is a material regression of that content, not an Improvement address.
- Scope remains queue-only intent; no `src`/tests/workflows on either head. Cursor Cloud Agent not used; review via `gh`. No GitHub PR comment posted.
- GitHub Reviews API returned no formal review objects at review time; leg findings are taken from the amendment commit message.
