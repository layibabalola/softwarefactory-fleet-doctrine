schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 80
head: 585529651d846ab71b85eb0717475724b2fc8703
reviewed_utc: 2026-09-16T04:40:32Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test PENDING on head 58552965 (Actions run 35056435248); tip ahead of master by 1, behind by 0

## Blockers

- **CI: `factory contracts`** — PENDING on head `585529651d846ab71b85eb0717475724b2fc8703` (Actions run 35056435248). Wait for green; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — PENDING on the same run. Same wait/fix rule.
- **Process: HIGH-RISK D2 (RESET-PLAN)** — Touches `factory/RESET-PLAN.md` (point 8 process-authority enumeration). Requires one review leg per family while both are live, zero MUST/BLOCKER, and a literal non-author APPROVE. Head is Claude-authored; deciding APPROVE must come from a codex leg. Author neither reviews nor merges. 0 GitHub reviews / review requests yet.

## Improvements

- **Keep F1.6 evidence (#77 draft) blocked on this amendment.** PR body correctly sequences this SPEC ahead of the evidence PR so `queue-state` does not stamp F1.6 DELIVERED against an unsatisfiable VERIFY. Controllers should refuse to advance #77 until this head is on master (or #77 retargets VERIFY to the post-amendment text).
- **When `TRIPWIRE-ADJ-EXCL` lands, pin the pathspec as directory-only.** Spec already says `:!factory/adjudications/**` and forbids `:!*.json`. Worth a one-line trap note in the implementation PR so a future "just exclude json" shortcut cannot blind the escape-hatch scan.

## Notes

- Scope: `factory/RESET-PLAN.md` (+2/−2) and `factory/queue.jsonl` (+2/−0). §1.3 and D2 text untouched. Scope cell for F1.6 now excludes daily conductor invocation (filed as `F1.6b` order 250, HIGH-RISK, dependsOn F1.6). `TRIPWIRE-ADJ-EXCL` order 251 ORDINARY, dependsOn F1.6.
- Spot-check PASS on re-anchoring claim: `factory/tripwire-state.mjs` and `scripts/tripwire.ps1` on this head contain no `PASS`/`SKIP` tokens; state vocabulary observed is ADVISORY / UNKNOWN / FAIL / IN_PROGRESS (plus severities BLOCKING/ERROR/ADVISORY/INFO). `scripts/conductor.ps1` has no tripwire reference — daily clause split is warranted.
- Replacement VERIFY (a)–(e) plus coverage floor and supersession citation are coherent: reconciling to reviewed #29 design without silently ratifying coverage loss. CoS treats this as re-anchoring, not laundering, on the evidence in the amendment text.
- Queue hygiene PASS: 49 rows; unique ids and orders; every `dependsOn` resolves. Orders 250/251 avoid collision with #79's row at 248. Rows are filed, not performed (HUB Retries / point 1).
- Deliberately not filed: queue-state VERIFY gate before DELIVERED — correctly deferred; HUB "Merged PR = DELIVERED" stands. Author claim that tripwire modules are outside point 8's closed enumeration is correct (enumeration names RESET-PLAN itself and conductor, not tripwire-state / tripwire.ps1).
- mergeable=MERGEABLE; mergeStateStatus=UNSTABLE while CI runs; isDraft=false. Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/adversarialllm/pr-80.md`.