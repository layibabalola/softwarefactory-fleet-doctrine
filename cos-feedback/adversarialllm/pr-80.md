schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 80
head: a5de1eef050ff07fb263c98a41c4957f5a0eb834
reviewed_utc: 2026-09-16T05:08:21Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test PENDING on head a5de1eef (Actions run 35058161639); tip ahead of master by 1, behind by 0

## Blockers

- **CI: `factory contracts`** — PENDING on head `a5de1eef050ff07fb263c98a41c4957f5a0eb834` (Actions run 35058161639). Wait for green; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — PENDING on the same run. Same wait/fix rule.
- **Process: HIGH-RISK D2 (RESET-PLAN)** — Touches `factory/RESET-PLAN.md` (point 8 process-authority enumeration). Requires one review leg per family while both are live, zero MUST/BLOCKER, and a literal non-author APPROVE. Head is Claude-authored; deciding APPROVE must come from a codex leg. Author neither reviews nor merges. 0 GitHub reviews / review requests on this head.

## Improvements

- **Keep F1.6 evidence (#77 draft) blocked on this amendment.** PR body correctly sequences this SPEC ahead of the evidence PR so `queue-state` does not stamp F1.6 DELIVERED against an unsatisfiable VERIFY. Controllers should refuse to advance #77 until this head is on master (or #77 retargets VERIFY to the post-amendment text). Unchanged from prior CoS note on `58552965`.
- **When `TRIPWIRE-ADJ-EXCL` lands, pin the pathspec as directory-only.** Spec already says `:!factory/adjudications/**` and forbids `:!*.json`. Worth a one-line trap note in the implementation PR so a future "just exclude json" shortcut cannot blind the escape-hatch scan. Unchanged from prior CoS note.

## Notes

- Round-2 push (`a5de1eef`) addresses Claude round-1 MUSTs/SHOULDs on the amendment text: (1) clause (e) no longer admits every finding that names a recovery row — bounded to BLOCKING `escape-hatch` whose sole hit is under `factory/adjudications/` pending `TRIPWIRE-ADJ-EXCL`; `direct-post-reset-commit` / `master-ci-red` / `task-last-result` and ERROR with already-DELIVERED recovery still fail (D11-R backstop preserved). (2) `F1.6b` cadence restored as a floor (first tick of each local day and not again that day, proven both directions by test) rather than a rate ceiling. (3) clause (b) documents early-return UNKNOWN suppression. (4) `F1.6b` now has a §3 VERIFY bullet. (5) `TRIPWIRE-ADJ-EXCL` class is PROPOSED ORDINARY with evidence on both sides — no longer asserts point 8 is closed.
- Scope still `factory/RESET-PLAN.md` and `factory/queue.jsonl` (+6/−2 overall). §1.3 and D2 text untouched. `F1.6b` order 250 HIGH-RISK dependsOn F1.6; `TRIPWIRE-ADJ-EXCL` order 251 PROPOSED ORDINARY dependsOn F1.6.
- Spot-check from prior pass still holds on re-anchoring claim: tripwire modules emit no `PASS`/`SKIP`; conductor has no tripwire reference. Coverage floor + supersession citation keep this as re-anchoring, not laundering.
- Prior CoS Improvements not addressed on this head (they are sequencing / future-implementation notes). Verdict unchanged: merge-when-ci-green.
- Queue hygiene: rows filed not performed; orders 250/251 avoid #79's 248. Deliberately not filed: queue-state VERIFY-before-DELIVERED (HUB contract stands).
- mergeable=MERGEABLE; mergeStateStatus=UNSTABLE while CI runs. Review via `gh`; no GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-80.md` for head `a5de1eef`.
