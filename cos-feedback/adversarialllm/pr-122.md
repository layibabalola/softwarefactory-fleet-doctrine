schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 122
head: cb2054d592690c9182af701d7ccefb3fef93cb1c
reviewed_utc: 2026-09-22T21:43:28Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip CI run 35788218986 — docs drift, factory contracts, and typecheck/lint/build/test all IN_PROGRESS; mergeable true / unstable; isDraft true; base still feat/CONDUCTOR-PREFLIGHT-TIMEOUT-FILE; 0 GitHub reviews

## Blockers

- **Retarget base to `master` and mark ready for review.** #121 (`CONDUCTOR-PREFLIGHT-TIMEOUT-FILE`) merged at 2026-09-22T19:55:03Z. PR body plan: stay draft until #121 merges, then rebase onto master for D2. Tip `cb2054d5` already merged `origin/master` (21de5697) and keeps only the test change, but GitHub base is still `feat/CONDUCTOR-PREFLIGHT-TIMEOUT-FILE` and `isDraft` is true. Vs that named base, compare shows `factory/queue.jsonl` +2/−0 noise that would fail VERIFY (diff must touch only `factory/conductor-preflight.test.mjs`). Vs `master` the tip is clean (+4/−1 on the test file only, ahead_by 3, behind_by 0). Controllers: change base to `master`, undraft, then start D2 on that head/base.

## Improvements

- **Wait for tip CI green on `cb2054d5`.** Actions run 35788218986: all three jobs still IN_PROGRESS at review time. Do not merge until `factory contracts`, `typecheck / lint / build / test`, and `docs drift (delta gate)` settle green.
- **Two-leg / D2 still open.** Row `CONDUCTOR-PREFLIGHT-TIMEOUT` is high-risk class (one review leg per family; implementer: claude). Needs the independent second-family leg after base retarget. GitHub Reviews API returns 0 formal review objects. CoS does not substitute for that adjudication.
- **Local gate pending idle host (author report).** PR body: local `pwsh scripts/ci.ps1` not yet reported green on the post-rebase head. Controllers should still require a green local gate on the merge head per factory practice.

## Notes

- **Intentional diff vs master is correct.** Only `factory/conductor-preflight.test.mjs`: adds named `PWSH_SPAWN_TIMEOUT_MS = 60_000` and passes it to `run()`'s `spawnSync('pwsh', …)` (was hard-coded `30000`). Comment cites PR 119 gate run 2 `spawnSync pwsh ETIMEDOUT` under host overlap; same name/value as #3 (`ef16a5ed`). Spot-check: `gitAt` still 20000 ms; mutex-holder readiness poll unchanged; assertions, test names, fixtures, and test count unchanged.
- **Stacked filing already on master.** #121 delivered the FILE row; tip merge commit keeps the implementing test change only.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.