schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 108
head: 222f421ce8179e09d0423f59705f4917dd044cd8
reviewed_utc: 2026-09-19T01:41:49Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts and typecheck/lint/build/test IN_PROGRESS on run 35413347850; mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews

## Blockers

(none for content — schema-complete ferry admission runs before any IndexedDB write; first-wins/conflict/duplicate/already-terminal paths look sound on tip)

## Improvements

- **Wait for CI** — confirm `factory contracts` and `typecheck / lint / build / test` PASS on head `222f421c` before merge (body claims exact local CI GREEN on this tip vs master `3999b7ab`; hosted unit/build still running).
- **Provider allowlist drift** — `validReportModel` in `adversarialllm/src/shared/db.ts` hardcodes the seven provider ids. When a provider is added/removed, keep this ferry validator (and `projectFerriedResult` outcome set) in the same change as `types.ts` / protocol, or add a shared constant the tests assert against.
- **Packet size** — tip is the full offline ph-P2 scheduling stack (+1482/−37, 26 files, 20 commits) plus the R2 schema-complete ferry fix that closed #93 exhausted. Prefer a second independent review pass on `settleFerriedTerminal` / `validReportModel` and the schedule poller binding in `background.ts` before treating merge as delivery complete (signed/live native proof remains out of scope per evidence).

## Notes

- Row `ph-P2-R2`, implementer Codex, branch `codex/ph-P2-R2` → `master`. Closes the #93 failure mode: incomplete `ReportModel` objects were admitted before opening the authoritative IndexedDB transaction; malformed input now returns `malformed-terminal` with no transaction (covered by 15 family corruption cases + IDB `transaction` spy).
- Tip also lands schedule store/runner/editor (default-off), immutable ferry settlement, notification policy via `shouldNotifyFerriedCampaign`, and docs/evidence/inventory refresh. Focused claims: 31/31 terminal schema, 107/107 scheduling/native/UI compatibility.
- Related: closed unmerged #93 (`fix(scheduling): make ferry terminal outcomes immutable`). Cursor Cloud Agents not used for this CoS pass; review via `gh`. No GitHub PR comment posted.