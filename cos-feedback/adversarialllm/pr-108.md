schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 108
head: 37238dcb431fd2759af1adafbe6274ae8addfe0b
reviewed_utc: 2026-09-19T02:58:07Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: all three required checks IN_PROGRESS on run 35417157813 (docs drift, factory contracts, typecheck/lint/build/test); mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews

## Blockers

(none for content — tip tightens ferry ReportModel admission: agreement scores on verdict/trajectory/rounds must be finite and in [0,1]; malformed still returns malformed-terminal with no IndexedDB write)

## Improvements

- **Wait for CI** — confirm `docs drift (delta gate)`, `factory contracts`, and `typecheck / lint / build / test` PASS on head `37238dcb` before merge (prior tip `222f421c` claimed local CI GREEN; hosted checks restarted for this push).
- **Provider allowlist drift** — `validReportModel` in `adversarialllm/src/shared/db.ts` still hardcodes the seven provider ids. When a provider is added/removed, keep this ferry validator (and `projectFerriedResult` outcome set) in the same change as `types.ts` / protocol, or add a shared constant the tests assert against.
- **Packet size** — tip remains the full offline ph-P2 scheduling stack (+1489/−37, 26 files) plus R2 schema-complete ferry + agreement bounds. Prefer a second independent review pass on `settleFerriedTerminal` / `validReportModel` and the schedule poller binding in `background.ts` before treating merge as delivery complete (signed/live native proof remains out of scope per evidence).

## Notes

- Push delta vs prior CoS head `222f421c`: `e8ef6927` adds `unitInterval` for agreement scores and six family corruption cases; `37238dcb` refreshes inventory line anchors in docs/28. No change to schedule store/runner/editor paths.
- Row `ph-P2-R2`, implementer Codex, branch `codex/ph-P2-R2` → `master`. Still closes the #93 failure mode (incomplete ReportModel admitted before the authoritative transaction).
- Related: closed unmerged #93. Cursor Cloud Agents not used for this CoS pass; review via `gh`. No GitHub PR comment posted.