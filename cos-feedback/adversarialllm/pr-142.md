schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 142
head: af3df9dc1a27103aa7e4f07ad2d8811f565d1323
reviewed_utc: 2026-09-25T14:42:27Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 36149158203 — factory contracts + docs drift + typecheck-lint-build-test all QUEUED (self-hosted); prior tip cf2f66a4 run 36145624205 SUCCESS (all three); MERGEABLE/UNSTABLE; ahead 6 / behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `af3df9dc`.** Actions run 36149158203 just queued after the base-refresh + EA-CUT note push. Prior tip `cf2f66a4` run 36145624205 is fully green; re-check this tip before merge.
- **Local root gate still not evidenced on this head.** VERIFY clause (6) wants `pwsh scripts/ci.ps1` green on the delivered tip; the implementer packet forbids running it (host lease). Controller should record the receipt in the PR (as EA-CUT.md §(e) already says). Note: master merge brought #143's hermetic `Invoke-P8Unit` into `scripts/ci.ps1`, so the root gate path itself changed under this tip even though the EA-3 owned files did not.
- **Two independent review legs still required (D2).** Docs-only row, but author notes do not waive D2. Conductor/controller must still dispatch non-author legs before merge.

## Notes

- **What changed this push:** two commits on top of prior tip `cf2f66a4`. (1) `2a750865` merges master `d3652596` (PR #143 CI-GATE-HERMETIC: `scripts/ci.ps1` + `adversarialllm/tests/unit/p8-ship-posture.test.ts`) with **no change** to this row's three owned files. (2) `af3df9dc` adds a "Base refresh" subsection to `adversarialllm/evidence/EA-CUT.md` only (+13/−0), documenting the new base, the master diff (`p8-ship-posture.test.ts` + `ci.ps1`), that neither is a build input for `npm run build`/`wxt`, and that tag scans at `d3652596` remain empty. `EA-INSTALL.md` / `PRIVACY.md` blob SHAs unchanged (`2707f8e3` / `9f7f8b7f`).
- **Prior Improvement cleared:** tip CI on `cf2f66a4` (run 36145624205) completed SUCCESS for factory contracts, docs drift, and typecheck/lint/build/test.
- **Owned scope vs master:** +580/−0 across the same 3 docs/evidence files only. Ahead 6 / behind 0. No source, build config, constants, or lockfile changes *in the EA-3 row itself*; the merge commit only brings already-merged master (#143).
- **Spot-checks carry forward:** `BUNDLE_PACKAGER_ENABLED`/`M1_ROUND_ENGINE_ENABLED` false at tip; pre-review MUST/SHOULD fixes remain in owned install/privacy blobs; §(a)-(c) measurements still attributed to measured commit `789bda9d` with relational binding through the refresh.
- **Intentional scope:** describes only; no release tag, no distribution, no provider contact.
- No secrets in diff. Review via `gh`. No GitHub PR comment posted.
