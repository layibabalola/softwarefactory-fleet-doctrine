schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 142
head: e342c60fd91890bb2d4a18f73b64b023ca85b779
reviewed_utc: 2026-09-25T12:35:38Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36135720194 — factory contracts / typecheck-lint-build-test / docs drift all QUEUED (self-hosted); MERGEABLE/UNSTABLE; ahead 2 / behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `e342c60f`.** Actions run 36135720194 just queued all three required jobs. Do not merge until they succeed.
- **Local root gate still not evidenced on this head.** VERIFY clause (6) wants `pwsh scripts/ci.ps1` green on the delivered tip; the implementer packet forbids running it (host lease). Controller should record the receipt in the PR (as EA-CUT.md already says).
- **Two independent review legs still required (D2).** Docs-only row, but author notes do not waive D2. Conductor/controller must still dispatch non-author legs before merge.

## Notes

- **What changed:** EA-3 early-access cut docs only. New `adversarialllm/docs/EA-INSTALL.md` (+90) — Load-unpacked path, no store/CRX/signing, manifest `0.0.0` without `v*` tag. New `adversarialllm/docs/PRIVACY.md` (+175) — read/persist/leave-machine boundary at default-off flags, including refinement-loop synthesis paste/upload and complete IndexedDB inventory. New `adversarialllm/evidence/EA-CUT.md` (+278) — build size/duration, manifest version, EA-2 constant literals, P8-R obligation-5 searches with per-hit dispositions (including self-hits).
- **Scope:** +543/−0 across 3 files, 2 commits (`4b43fb86` initial docs; `e342c60f` advisory pre-review fixes). Ahead 2 / behind 0 vs master `00d92ae3`. No source, build config, constants, or lockfile changes.
- **Spot-checks (via gh at tip):** `BUNDLE_PACKAGER_ENABLED`/`M1_ROUND_ENGINE_ENABLED` false at `constants.ts:1809/1812`; all six `DEFAULT_SF_FLAGS` false at `sf-flags.ts:37-42`; `DEFAULT_WEBHOOK_CONFIG.enabled` false at `webhook-gate.ts:29`; nine `adversarialllm` stores + `DB_NAME`/`DB_VERSION` at `db.ts:197/204` match PRIVACY.md. Diff is exactly the three named paths.
- **Pre-review remediation landed:** MUST 1–3 and SHOULD 1–4 addressed in `e342c60f` (full leave-machine boundary, complete store list, P8-R self-hit dispositions, evidence-head clarification, native-host/permissions accuracy, no restated flag literals). SHOULD 4 correctly avoids citing `CONSTANTS.generated.md` for feature flags (NAME_RE mismatch).
- **Intentional scope:** describes only; no release tag, no distribution, no provider contact. Build/constants evidence measured at base `00d92ae3` and still valid because this row touches no build inputs.
- No secrets in diff. Review via `gh`. No GitHub PR comment posted.