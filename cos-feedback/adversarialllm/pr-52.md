schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 52
head: 637e97f661a04c403019025cf9fe1c4d37224517
reviewed_utc: 2026-09-16T06:28:59Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip 637e97f661a0 — docs drift (delta gate) SUCCESS; factory contracts and typecheck / lint / build / test IN_PROGRESS on Actions run 35063792182; mergeable=true / unstable; 0 GitHub reviews on tip

## Blockers

- **CI: hosted jobs not settled green on this tip** — `docs drift (delta gate)` already SUCCESS. Wait for `typecheck / lint / build / test` and `factory contracts` on head `637e97f661a04c403019025cf9fe1c4d37224517` (run 35063792182). Do not merge while either is pending or red; if red, fix and re-push.
- **Process: independent dual-family D2 on this exact head** — 0 GitHub PR reviews on tip `637e97f661a0`. Prior Sol APPROVE on older heads remains void under D2's current-head/current-base rule. After hosted CI settles green, land fresh Sol/Codex + Claude/Sonnet on this tip. Non-author controller turns the key.

## Improvements

- `adversarialllm/src/entrypoints/sidepanel/App.tsx` (gallery JSX comment ~1132–1134): still says Run "forwards the recipeId so the background compiles the recipe's round plan". Tip already sends explicit compiled `plan` + `maxIterations` from `compileRecipe`. Update the comment so the next reader does not regress to recipeId-only starts for user recipes.
- `adversarialllm/src/entrypoints/sidepanel/App.tsx` (`runCampaignStart` JSDoc ~709–712): still describes "an optional recipe id" only, while opts also take `maxIterations` and `plan`. Mention the explicit plan passthrough in the same sentence as `handleRunRecipe` already does.
- `mutateRecipe` catch still maps every failure to one generic alert string — fine for UX; consider logging `err` to the existing diagnostics channel so durable storage failure is distinguishable from compile rejection during support.
- Mount `useEffect` for `listRecipes` remains `[]`-only; post-mutation refresh is explicit via `setSavedRecipes(await listRecipes())` — good. If a future path mutates the store outside `mutateRecipe`, bump `recipeRevision` there too or the stale-load guard will not apply.
- Round-1 NIT left open deliberately (author note): `RecipeCard` Delete has no confirmation. Treat as a separate UX change if wanted; not a merge blocker for ph-M2.

## Notes

- Delta vs prior CoS head `7c1e5386871f1fa1240eabbf9764006ad5508998`: commit `637e97f661a0` — `Merge origin/master (e1b12b8a) into feat/ph-M2: refresh base after P4 landed`. Conflict with master (P4 #51) is cleared: GitHub now reports `mergeable=MERGEABLE` (was dirty/conflicting). Product composition check on the auto-merged builder: P4's optional `sourceBundleInput` and this branch's `recipeId`/`plan`/`maxIterations` are independent keys in `buildStartCampaignMessage` / `runCampaignStart`; neither wiring overwrites the other. Inventory block in `docs/28` regenerated via `inventory.mjs` (CRLF-preserving) after taking trunk's generated block in the conflict.
- Round-1 iteration repair from `7c1e538` is retained on tip: `RecipeEditor` iterations read-only / plan-derived; `handleRunRecipe` documents legacy iteration path under `M1_ROUND_ENGINE_ENABLED=false`.
- Prior CoS Improvements (gallery comment, `runCampaignStart` JSDoc, diagnostics logging, revision-guard scope) unchanged by this merge-forward. Prior CoS Improvement on `handleRunRecipe` JSDoc remains addressed from the prior tip.
- Author claimed local verification at this head: typecheck exit 0; start-campaign-inputs + recipe-author-run + inventory + recipe-gallery-ui = 32 tests. Hosted CI + D2 still required.
- Cursor Cloud Agents not used (review-only CoS via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-52.md`.
- Chat: quiet — verdict still not-merge-ready; prior CoS Improvements not clearly addressed by this push (blocker picture improved: conflict cleared, CI now running).
