schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 52
head: d7acaafd665a2b872710a929c17aef3b0d318538
reviewed_utc: 2026-09-16T06:36:10Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip d7acaafd665a2b — all three hosted jobs IN_PROGRESS on Actions run 35064339566 (docs drift, factory contracts, typecheck/lint/build/test); prior tip 637e97f6 factory contracts FAILED (EOL numstat mismatch on App.tsx) then run cancelled; mergeable=true / unstable; 0 GitHub reviews on tip

## Blockers

- **CI: hosted jobs not settled green on this tip** — Wait for `docs drift (delta gate)`, `factory contracts`, and `typecheck / lint / build / test` on head `d7acaafd665a2b872710a929c17aef3b0d318538` (run 35064339566). Do not merge while any is pending or red; if red, fix and re-push. Prior tip `637e97f6` failed `factory contracts` on the line-ending delta gate (ordinary App.tsx numstat 90/16 vs --ignore-cr-at-eol 88/14); this tip restores the trunk's two CRLF lines in `App.tsx` and re-runs.
- **Process: independent dual-family D2 on this exact head** — 0 GitHub PR reviews on tip `d7acaafd`. Prior Sol APPROVE on older heads remains void under D2's current-head/current-base rule. After hosted CI settles green, land fresh Sol/Codex + Claude/Sonnet on this tip. Non-author controller turns the key.

## Improvements

- `adversarialllm/src/entrypoints/sidepanel/App.tsx` (gallery JSX comment ~1132–1134): still says Run "forwards the recipeId so the background compiles the recipe's round plan". Tip already sends explicit compiled `plan` + `maxIterations` from `compileRecipe` via `handleRunRecipe`. Update the comment so the next reader does not regress to recipeId-only starts for user recipes.
- `adversarialllm/src/entrypoints/sidepanel/App.tsx` (`runCampaignStart` JSDoc ~699–702): still describes "an optional recipe id" only, while opts also take `maxIterations` and `plan`. Mention the explicit plan passthrough in the same sentence as `handleRunRecipe` already does.
- `adversarialllm/src/entrypoints/sidepanel/utils/start-campaign-inputs.ts` (`BuildStartCampaignMessageParams.recipeId` JSDoc): still documents recipeId as compiling a named built-in at dispatch and driving maxIterations from the plan; the M2 path now passes an explicit `plan` and `maxIterations` from the sidepanel. Align the comment with `buildStartCampaignMessage`'s `...(params.plan ? { plan: ... } : {})` path.
- `mutateRecipe` catch still maps every failure to one generic alert string — fine for UX; consider logging `err` to the existing diagnostics channel so durable storage failure is distinguishable from compile rejection during support.
- Mount `useEffect` for `listRecipes` remains `[]`-only; post-mutation refresh is explicit via `setSavedRecipes(await listRecipes())` — good. If a future path mutates the store outside `mutateRecipe`, bump `recipeRevision` there too or the stale-load guard will not apply.
- Round-1 NIT left open deliberately (author note): `RecipeCard` Delete has no confirmation. Treat as a separate UX change if wanted; not a merge blocker for ph-M2.

## Notes

- Delta vs prior CoS head `637e97f661a04c403019025cf9fe1c4d37224517`: commit `d7acaafd` — `fix(eol): restore the trunk's two CRLF lines in App.tsx`. Product source otherwise unchanged vs that tip (compare shows App.tsx only, +2/−2). Addresses the third EOL trap this session (docs/28 twice, now App.tsx against mixed-EOL trunk).
- Round-1 iteration repair retained: `RecipeEditor` iterations read-only / plan-derived; `handleRunRecipe` documents legacy iteration path under `M1_ROUND_ENGINE_ENABLED=false` and forwards compiled `plan` + `maxIterations`.
- Prior CoS Improvements (gallery comment, `runCampaignStart` JSDoc, start-campaign-inputs recipeId JSDoc, diagnostics logging, revision-guard scope, Delete confirm) unchanged by this EOL-only push.
- Author claimed local verification at this head: ordinary and --ignore-cr-at-eol numstats identical across every changed path; typecheck exit 0; recipe-author-run + start-campaign-inputs + inventory = 25 tests. Hosted CI + D2 still required.
- Cursor Cloud Agents not used (EOL/docs-hygiene tip; review via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-52.md`.
- Chat: quiet — verdict still not-merge-ready; prior CoS Improvements not clearly addressed by this push (blocker picture: prior tip's factory-contracts EOL fail is what this tip targets; CI re-running).
