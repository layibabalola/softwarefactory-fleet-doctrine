schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 52
head: 7c1e5386871f1fa1240eabbf9764006ad5508998
reviewed_utc: 2026-09-16T06:21:32Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: no hosted checks on tip 7c1e5386 yet (prior tip 08ae8c96 CI all-green on run 35049272114); mergeable=false / dirty — branch diverged, behind master e1b12b8a (P4 #51) by 1 with conflicts; 0 GitHub reviews on tip

## Blockers

- **Merge: conflict with current master** — GitHub reports `mergeable=false` / `dirty`. Tip is ahead 6 / behind 1 vs `master` (`e1b12b8a13bd8df86a3ff1522ffc6c1cdce85052`, P4 #51). Overlap likely includes `docs/28_IMPLEMENTATION_PROGRESS.md`, `App.tsx`, and `start-campaign-inputs.ts` (both sides touched). Merge-forward `origin/master` into `feat/ph-M2` (no rebase / force-push), resolve conflicts, then re-push. Do not merge while dirty.
- **CI: hosted jobs missing on this tip** — No check-runs / Actions for head `7c1e5386871f1fa1240eabbf9764006ad5508998`. Prior green on `08ae8c96` is void for this head. After the merge-forward push, wait for `typecheck / lint / build / test`, `factory contracts`, and `docs drift (delta gate)` green; if red, fix and re-push.
- **Process: independent dual-family D2 on this exact head** — 0 GitHub PR reviews on tip `7c1e5386`. Prior Sol APPROVE on older heads remains void under D2's current-head/current-base rule; this tip again moves product source (round-1 iteration repair) and will move again after merge-forward. Land fresh Sol/Codex + Claude/Sonnet after merge-forward + hosted CI settle. Non-author controller turns the key.

## Improvements

- `adversarialllm/src/entrypoints/sidepanel/App.tsx` (gallery JSX comment ~1125–1127): still says Run "forwards the recipeId so the background compiles the recipe's round plan". Tip already sends explicit compiled `plan` + `maxIterations` from `compileRecipe`. Update the comment so the next reader does not regress to recipeId-only starts for user recipes.
- `adversarialllm/src/entrypoints/sidepanel/App.tsx` (`runCampaignStart` JSDoc ~696–699): still describes "an optional recipe id" only, while opts now also take `maxIterations` and `plan`. Mentions the explicit plan passthrough in the same sentence as `handleRunRecipe` already does.
- `mutateRecipe` catch still maps every failure to one generic alert string — fine for UX; consider logging `err` to the existing diagnostics channel so durable storage failure is distinguishable from compile rejection during support.
- Mount `useEffect` for `listRecipes` remains `[]`-only; post-mutation refresh is explicit via `setSavedRecipes(await listRecipes())` — good. If a future path mutates the store outside `mutateRecipe`, bump `recipeRevision` there too or the stale-load guard will not apply.
- Round-1 NIT left open deliberately (author note): `RecipeCard` Delete has no confirmation. Treat as a separate UX change if wanted; not a merge blocker for ph-M2.

## Notes

- Delta vs prior CoS head `08ae8c966500054b5b563bec481a0071cb679b5d`: commit `7c1e5386` — `ph-M2 round 1: make the authored iteration count the one that runs`. Product repair for Sol MUST `PH-M2-PRODUCTION-PATH-001` / Claude SHOULD `ph-M2-2-authored-iterations-inert`: `RecipeEditor` iterations is now read-only and derived from the selected plan's round count; plan select / new-draft seed sync the stored value; tests assert read-only field + outgoing `maxIterations` equals `compileRecipe(...).plan.rounds.length`. Docstring on `handleRunRecipe` rewritten (states legacy iteration path under `M1_ROUND_ENGINE_ENABLED=false`; round engine not activated here). `docs/28` P5 inventory block regenerated via `inventory.mjs` (CRLF-preserving).
- Prior CoS Improvement on `handleRunRecipe` JSDoc: addressed on this tip (now documents forwarding compiled `CampaignPlan`). Gallery JSX comment and `runCampaignStart` JSDoc still drift; diagnostics logging and revision-guard scope unchanged.
- Product shape otherwise unchanged from earlier CoS reviews: mounted RecipeGallery/RecipeEditor, revision-guarded store load, built-in copy-on-edit, draft retention on unrelated delete / save failure, explicit `plan` through `buildStartCampaignMessage`. No live campaign or flag activation claimed.
- Author claimed local verification at this tip: typecheck exit 0; inventory + recipe-author-run + recipe-gallery-ui + recipes-store = 24 tests; ordinary and `--ignore-cr-at-eol` numstats identical on changed paths. Hosted CI and D2 still required after merge-forward.
- Cursor Cloud Agents not used (review-only CoS via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-52.md`.
- Chat: report (verdict changed merge-when-ci-green → not-merge-ready; prior CoS handleRunRecipe JSDoc Improvement addressed; merge conflict + missing CI are new).
