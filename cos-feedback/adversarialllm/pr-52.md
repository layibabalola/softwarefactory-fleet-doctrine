schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 52
head: d87bddee6d84a6cc4809dbdcd54eaaaad33bace9
reviewed_utc: 2026-09-10T14:13:37Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; typecheck/lint/build/test + factory contracts still pending on run 34487425978; author claims local exact-head CI green (~754s)

## Blockers

- **CI: `typecheck / lint / build / test`** — still pending on head `d87bddee6d84a6cc4809dbdcd54eaaaad33bace9` (Actions run 34487425978). Wait for green before merge; if red, fix the failing suite and re-push.
- **CI: `factory contracts`** — still pending on the same run. Same wait/fix rule.
- **Process: independent D2 review** — 0 GitHub reviews yet; PR body requires native Sol/Codex + independent Claude review on the current head before merge. Land those reviews (or an explicit owner waiver outside this file) after hosted CI settles.

## Improvements

- `adversarialllm/src/entrypoints/sidepanel/App.tsx` (~gallery JSX comment ~1106–1108): still says the background compiles from `recipeId` alone. This head now sends an explicit compiled `plan` (+ `maxIterations` / providers from `compileRecipe`). Update the comment so the next reader does not regress to recipeId-only starts for user recipes.
- `adversarialllm/src/background/recipes/dispatch-plan.ts` (unchanged here): `recipeId` branch still resolves via `getBuiltinRecipe` only. User-saved recipes therefore **require** the panel's explicit `plan` (which this PR supplies). Either extend lookup to the recipes store later, or keep a short comment near `handleRunRecipe` that recipeId-without-plan remains builtin-only / legacy-soft-fail.
- `mutateRecipe` catch maps every failure to one generic alert string — fine for UX; consider logging `err` to the existing diagnostics channel so a durable storage failure is distinguishable from compile rejection during support.
- Mount `useEffect` for `listRecipes` is `[]`-only; post-mutation refresh is explicit via `setSavedRecipes(await listRecipes())` — good. If a future path mutates the store outside `mutateRecipe`, bump `recipeRevision` there too or the stale-load guard will not apply.

## Notes

- Scope: ph-M2 connects existing RecipeGallery/RecipeEditor/store into a mounted author/save/reload/run journey in `App.tsx` (+ tiny `start-campaign-inputs.ts` `plan` passthrough). Built-in edits become copies; delete preserves unrelated drafts; revision guard rejects stale mount loads. 6 files, +231/−24.
- Start path: `handleRunRecipe` compiles first, then `runCampaignStart(..., { recipeId, maxIterations, plan })` so user recipes do not depend on builtin-only `resolveDispatchPlan` lookup. Explicit plan wins per existing precedence. No live campaign or flag activation claimed.
- Tests: new `recipe-author-run.test.tsx` (mounted App journey: save/reload/edit/run, builtin copy, cancel, save retry, stale-load rejection, draft retention on delete). Inventory row for M2 updated. Author: 39 focused + 7 inventory tests; TS/lint claimed green locally.
- `docs drift (delta gate)` already PASS on hosted CI. Remaining two jobs + dual-family D2 are the merge gate.
- Cursor Cloud Agents not used for this pass (review-only CoS via `gh`; prior plan note that cloud agents are not the factory floor for this meter). No GitHub PR comment posted.
- Doctrine bus: `cos-feedback/` still lands via open PR layibabalola/softwarefactory-fleet-doctrine#58 (branch `docs/cos-feedback-20260910`); this file is published there until #58 merges to master.
