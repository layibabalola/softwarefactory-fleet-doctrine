schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 52
head: c31f8b706b548cc1e47d92e1efe981e4dc43b895
reviewed_utc: 2026-09-16T06:58:55Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip c31f8b706b54 — docs drift (delta gate) PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on Actions run 35066093016; mergeable=true; 0 GitHub reviews on tip

## Blockers

- **CI: hosted jobs not settled green on this tip** — `docs drift (delta gate)` already PASS on head `c31f8b706b548cc1e47d92e1efe981e4dc43b895`. Wait for `factory contracts` and `typecheck / lint / build / test` on the same Actions run `35066093016`. Do not merge while either is pending or red; if red, fix and re-push.
- **Process: independent dual-family D2 on this exact head** — 0 GitHub PR reviews on tip `c31f8b70`. Prior Sol APPROVE on older heads remains void under D2's current-head/current-base rule. After hosted CI settles green, land fresh Sol/Codex + Claude/Sonnet on this tip. Non-author controller turns the key. Reviewers must use the corrected `handleRunRecipe` docstring (plan presence gates round routing; do not treat `M1_ROUND_ENGINE_ENABLED=false` as "legacy iteration path").

## Improvements

- `adversarialllm/src/entrypoints/sidepanel/App.tsx` (gallery JSX comment ~1141–1142): still says Run "forwards the recipeId so the background compiles the recipe's round plan". Tip already sends explicit compiled `plan` + `maxIterations` from `compileRecipe` via `handleRunRecipe`. Update the comment so the next reader does not regress to recipeId-only starts for user recipes.
- `adversarialllm/src/entrypoints/sidepanel/App.tsx` (`runCampaignStart` JSDoc ~707–710): still describes "an optional recipe id" only, while opts also take `maxIterations` and `plan`. Mention the explicit plan passthrough in the same sentence as `handleRunRecipe` already does.
- `adversarialllm/src/entrypoints/sidepanel/utils/start-campaign-inputs.ts` (`BuildStartCampaignMessageParams.recipeId` JSDoc ~179–183): still documents recipeId as compiling a named built-in at dispatch and driving maxIterations from the plan; the M2 path now passes an explicit `plan` and `maxIterations` from the sidepanel. Align the comment with `buildStartCampaignMessage`'s `...(params.plan ? { plan: ... } : {})` path (and note `plan` is a separate optional field).
- `mutateRecipe` catch still maps every failure to one generic alert string — fine for UX; consider logging `err` to the existing diagnostics channel so durable storage failure is distinguishable from compile rejection during support.
- Mount `useEffect` for `listRecipes` remains `[]`-only; post-mutation refresh is explicit via `setSavedRecipes(await listRecipes())` — good. If a future path mutates the store outside `mutateRecipe`, bump `recipeRevision` there too or the stale-load guard will not apply.
- Round-1 NIT left open deliberately (author note): `RecipeCard` Delete has no confirmation. Treat as a separate UX change if wanted; not a merge blocker for ph-M2.
- PR body / evidence still say "no live campaign or flag activation". Keep that, but make sure D2 / controllers do not read it as "recipe Run stays on the legacy iteration path" — the corrected docstring and `round-prompt-map.ts:230` say plan presence selects the engine path.

## Notes

- Delta vs prior CoS head `d7acaafd665a2b872710a929c17aef3b0d318538`: commit `c31f8b70` — `fix(docs): correct a false safety claim about what a recipe run executes`. Touches `App.tsx` (`handleRunRecipe` JSDoc) and regenerates `docs/28` inventory mount line numbers (+9 for RecipeGallery / ReportReader after the longer comment). No product-control-flow change vs that tip.
- **Docstring correction (measured, CoS spot-checked):** `round-prompt-map.ts` has zero `M1_ROUND_ENGINE_ENABLED` references; `buildRoundPromptMap` takes the legacy loop only when `!plan || plan.rounds.length === 0`. Prior tip's `handleRunRecipe` comment (and prior CoS Notes) wrongly claimed recipe Run used the legacy iteration path under the flag=false. Corrected comment: recipe Run with a compiled plan **does** execute plan rounds; the flag gates durable round-feed R/W, abort seam, and resume/checkpoint. Author also notes forced `.txt` cross-pollination `roundFeedDoc` assignment at `orchestrator.ts:2063` is not flag-gated — auditors of provider-facing behaviour must look at the plan path, not the flag.
- Round-1 iteration repair retained: `RecipeEditor` iterations read-only / plan-derived; outgoing `maxIterations` equals compiled plan round count.
- Prior CoS Improvements (gallery comment, `runCampaignStart` JSDoc, start-campaign-inputs recipeId JSDoc, diagnostics logging, revision-guard scope, Delete confirm) **not** addressed by this docs-only push.
- Author claimed local verification at this head: typecheck exit 0; recipe-author-run + inventory + start-campaign-inputs = 25 tests; ordinary and --ignore-cr-at-eol numstats identical across every changed path (CRLF lines in App.tsx restored again after editor write). Hosted CI + D2 still required.
- Cursor Cloud Agents not used (docs/honesty tip; review via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-52.md`.
- Chat: quiet — verdict still `not-merge-ready`; prior CoS Improvements not clearly addressed (material change is docstring honesty + inventory line regen; blocker picture still CI + D2 on new tip).