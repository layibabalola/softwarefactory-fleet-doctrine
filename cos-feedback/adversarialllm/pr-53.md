schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 53
head: f748eea42aeb8f1e4e1b18d0706f08c384ba1ae4
reviewed_utc: 2026-09-10T17:37:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 34509285059; typecheck/lint/build/test + factory contracts still pending on head f748eea4; author claims 75 focused + inventory + TS/lint green and prior exact-head local CI green before lease repair

## Blockers

- **CI: `typecheck / lint / build / test`** — pending on head `f748eea42aeb8f1e4e1b18d0706f08c384ba1ae4` (Actions run 34509285059). Wait for green before merge; if red, fix the failing suite and re-push.
- **CI: `factory contracts`** — still pending on the same run. Same wait/fix rule.
- **Process: independent D2 review on this head** — 0 GitHub reviews yet; PR body requires native Sol/Codex + independent Claude review on the *current* head (lease repair included) before merge. Prior-head reviews do not approve `f748eea4`. Land those (or an explicit owner waiver outside this file) after hosted CI settles.

## Improvements

- `adversarialllm/evidence/ph-P3-reach-delivery.md`: earlier packets still say adapter/binding “remain outstanding” while later sections say they landed. Mark older packets historical so mid-file “not wired” language is not read as a current gap.
- `adversarialllm/tests/unit/reach-delivery.test.ts` (static `background.ts` string asserts): brittle to formatting/rename. Prefer a small exported hook seam or an injected `finalizeWithReach` unit over regexing the entrypoint source.
- Receipt map at 256 identities refuses new keys (correct anti-replay). When capacity is hit under real use, surface a clearer operator-facing signal than `reach_failed` alone, and track a reviewed retention/GC policy as a follow-up row (already called out in evidence).
- Detached reach is best-effort across abrupt service-worker termination before receipt/I/O (explicitly documented). If eventual delivery becomes a product requirement, add a durable outbox + startup drain as a separate row — do not silently claim it here.

## Notes

- Delta vs prior CoS head `4c13313c`: `fix(reach): release campaign lease without waiting for transport` (PHP3-LEASE-001). `finalizeBeforeReach` now awaits durable finalize only, then schedules reach via `void Promise.resolve().then(deliver)` so stalled sync/webhook cannot retain the campaign lease. Never-resolving reach-port regression added; fresh + cold-resume both route through the same helper. 8 files total PR, +747/−62; this push +31/−8 across 6 files.
- Prior CoS improvement (awaited reach held lease via `.finally`) is addressed on this head — removed from Improvements.
- Safety posture unchanged and still looks sound: `readVerdictHeads` validates/strips untrusted sync entries; sync retries capped at 3 with receipt-before-I/O; webhook one POST max with ambiguous interrupt/timeout (no auto-replay); exact `grantedOrigin` + `permissions.contains`; HMAC restricted to `X-*`; fetch `redirect:'error'`, `credentials:'omit'`, `cache:'no-store'`, `referrerPolicy:'no-referrer'`; receipt capacity refuses eviction at 256. Both channels default off; no activation UI / origin-grant / live cross-device proof claimed.
- docs drift (delta gate) already PASS on hosted CI for this tip.
- Cursor Cloud Agents not used (review-only CoS via `gh`; Grok meter is analysis, not factory floor). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-53.md`.
