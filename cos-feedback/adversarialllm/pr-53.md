schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 53
head: 444250a9bad3a21f80bb3f52c7600187b43f5bf3
reviewed_utc: 2026-09-16T01:55:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35045909003; typecheck/lint/build/test + factory contracts IN_PROGRESS on head 444250a9; mergeable; author claims prior-tip local CI green on f748eea4 — does not approve this tip

## Blockers

- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on head `444250a9bad3a21f80bb3f52c7600187b43f5bf3` (Actions run 35045909003). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Process: independent dual-family D2 on this head** — 0 GitHub reviews on tip `444250a9`. Master-merge tip `6f8653df` already voided prior-head Sol APPROVE under D2 current-head/current-base; this docs repin is a new tip, so land fresh Sol/Codex + Claude/Sonnet after hosted CI settles. No local gate claimed on this tip (P2.5d holds host CI lease).

## Improvements

- `adversarialllm/evidence/ph-P3-reach-delivery.md`: earlier packets still say adapter/binding “remain outstanding” while later sections say they landed. Mark older packets historical so mid-file “not wired” language is not read as a current gap.
- `adversarialllm/tests/unit/reach-delivery.test.ts` (static `background.ts` string asserts): brittle to formatting/rename. Prefer a small exported hook seam or an injected `finalizeWithReach` unit over regexing the entrypoint source.
- Receipt map at 256 identities refuses new keys (correct anti-replay). When capacity is hit under real use, surface a clearer operator-facing signal than `reach_failed` alone, and track a reviewed retention/GC policy as a follow-up row (already called out in evidence).
- Detached reach is best-effort across abrupt service-worker termination before receipt/I/O (explicitly documented). If eventual delivery becomes a product requirement, add a durable outbox + startup drain as a separate row — do not silently claim it here.

## Notes

- Delta vs prior CoS head `6f8653df2af9941f4b2ed6b3d054e41933dbf9ca`: docs-only tip `444250a9` — `fix(docs): repin the safety-rails inventory anchor to the merged tree` (round-1 Claude finding docs28-stale-anchor-safety-rails MUST). Single-line change in `adversarialllm/docs/28_IMPLEMENTATION_PROGRESS.md` P5 inventory: `background.ts:4266 applyWireInteraction` → `4292` (branch adds imports that shift the call site by 26 lines; trunk row was wrong after conflict resolution). Author reports `.factory-local/check-inventory.mjs` re-derived 19 anchors with 0 mismatches. No product source change; ph-P3 reach/sync/webhook code still matches prior CoS review.
- Prior CoS Improvements unchanged on this tip (docs inventory fix does not address evidence packet language, string-assert tests, receipt capacity UX, or outbox scope).
- Safety posture unchanged from prior CoS pass: `finalizeBeforeReach` + detached deliver (PHP3-LEASE-001); sync retries capped; webhook one-POST max; exact `grantedOrigin` + HMAC; fetch `redirect:'error'`, `credentials:'omit'`; receipt capacity refuses eviction at 256. Both channels default off.
- Hosted `docs drift (delta gate)` PASS (~27s) on run 35045909003. `mergeable: MERGEABLE`; remaining checks IN_PROGRESS.
- Cursor Cloud Agents not used (review-only CoS via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-53.md`.
- Chat: skipped (later-push rule — verdict unchanged; no prior CoS Improvement clearly addressed).
