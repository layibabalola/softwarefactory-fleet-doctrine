schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 53
head: 4c13313c8499be17e7456839b4f11f0df6dc4176
reviewed_utc: 2026-09-10T17:05:37Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; typecheck/lint/build/test + factory contracts still pending on run 34505930376; author claims local exact-head CI green (~853s) on 4c13313

## Blockers

- **CI: `typecheck / lint / build / test`** — still pending on head `4c13313c8499be17e7456839b4f11f0df6dc4176` (Actions run 34505930376). Wait for green before merge; if red, fix the failing suite and re-push.
- **CI: `factory contracts`** — still pending on the same run. Same wait/fix rule.
- **Process: independent D2 review** — 0 GitHub reviews yet; PR body requires native Sol/Codex + independent Claude review on the current head before merge. Land those reviews (or an explicit owner waiver outside this file) after hosted CI settles.

## Improvements

- `adversarialllm/src/entrypoints/background.ts` (`finalizeWithReach`): reach delivery is awaited inside the finalize path that also gates lease release via `.finally`. With sync/webhook enabled, a slow or 10s-aborting webhook holds the concurrent-run lease. Prefer fire-and-forget reach after durable finalize (still report status), or bound lease release so delivery latency cannot stall admission of the next campaign.
- `adversarialllm/evidence/ph-P3-reach-delivery.md`: earlier packets still say adapter/binding “remain outstanding” while the final production-binding section says they landed. Trim or mark older packets historical so a reader does not treat mid-file “not wired” language as current gap.
- `adversarialllm/tests/unit/reach-delivery.test.ts` (static `background.ts` string asserts): brittle to formatting/rename. Prefer exporting a small hook seam or a unit that injects `finalizeWithReach` rather than regexing the entrypoint source.
- Receipt map at 256 identities refuses new keys (correct anti-replay). When capacity is hit under real use, surface a clearer operator-facing signal than `reach_failed` alone, and track a reviewed retention/GC policy as a follow-up row (called out in evidence already).

## Notes

- Scope: ph-P3 connects opt-in minimal verdict sync + signed webhook delivery to fresh and cold-resume completion via `finalizeWithReach` / `createReachBinding`. Both channels default off; no activation UI, origin-grant path, or live cross-device proof claimed. 8 files, +716/−62.
- Safety posture looks intentional and sound on review: `readVerdictHeads` strips/validates untrusted sync entries; sync retries capped at 3 with receipt-before-I/O; webhook one POST max with ambiguous interrupt/timeout (no auto-replay); exact `grantedOrigin` + `permissions.contains`; HMAC restricted to `X-*` headers; fetch uses `redirect:'error'`, `credentials:'omit'`, `cache:'no-store'`, `referrerPolicy:'no-referrer'`; receipt capacity refuses eviction at 256.
- Inventory row for reach now lists `reach-delivery.ts:59 writeVerdictHeads` as a static caller; docs drift delta gate already PASS on hosted CI.
- Author: 73 focused tests + TS/targeted lint claimed green; full local exact-head CI claimed green at this tip. Remaining merge gate is the two pending hosted jobs + dual-family D2.
- Cursor Cloud Agents not used for this pass (review-only CoS via `gh`; Grok meter is analysis, not factory floor). No GitHub PR comment posted.
- Doctrine bus: `cos-feedback/` still lands via open PR layibabalola/softwarefactory-fleet-doctrine#58 (branch `docs/cos-feedback-20260910`); this file is published there until #58 merges to master.
