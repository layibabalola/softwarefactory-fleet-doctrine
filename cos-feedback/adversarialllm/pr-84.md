schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 84
head: a3de40466a6bae93469cc91453a0c74c94234561
reviewed_utc: 2026-09-16T07:53:06Z
event: pr-opened
verdict: not-merge-ready
ci_summary: tip a3de40466a6b — docs drift (delta gate) PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on Actions run 35070684562; mergeable=true (unstable); 0 GitHub reviews on tip; author claims local pwsh scripts/ci.ps1 18/18 GREEN (~615s) on this head

## Blockers

- **CI: hosted jobs not settled green on this tip** — `docs drift (delta gate)` already PASS on head `a3de40466a6bae93469cc91453a0c74c94234561`. Wait for `factory contracts` and `typecheck / lint / build / test` on Actions run `35070684562`. Do not merge while either is pending or red; if red, fix and re-push.
- **Process: independent dual-family D2 on this exact head** — 0 GitHub PR reviews on tip `a3de4046`. After hosted CI settles green, land fresh Sol/Codex + Claude/Sonnet on this tip. Non-author controller turns the key. Author (claude / feat/MEM-R3) does not merge. Reviewers should verify (1) every claim in the three entries against this tree, (2) the Worktrees quote against `factory/HUB.md`, (3) `round-prompt-map.ts:230` plan-branch vs eleven `M1_ROUND_ENGINE_ENABLED` sites in `orchestrator.ts`, and (4) that no `row:` token appears in the PR body (queue CONFLICT risk for DELIVERED rows).

## Improvements

- `adversarialllm/.claude-state/MEMORY.md` one-liner for the doctrine-bus entry omits the repo PreToolUse guard refusing the foreign-path pull; `memory/reference/INDEX.md` and the entry body both state it. Align the MEMORY.md blurb with INDEX so the top-level index does not understate the local constraint.
- The worktree-boundary entry correctly refuses to over-cite `factory/HUB.md` Worktrees (removal only). Consider a small HUB amendment PR that lands "never commit/stage/push from a peer worktree" in authority docs so the rule stops living only as session agreement.
- Sibling inventory defect PRs #82 / #83 remain OPEN; this memory PR documents the class and does not fix `inventory.mjs` `flag()`. No merge-order conflict with those PRs (disjoint paths), but consumers should not treat this entry as evidence that the false pairing is already gone from master.

## Notes

- Scoped review of tip `a3de40466a6bae93469cc91453a0c74c94234561` vs `master` (`e1b12b8a13bd`): 6 files, +118/−0. Pure memory: two feedback entries, one reference entry, both INDEX levels + MEMORY.md. No product/runtime code. No `row:` token (correct; memory files are not queue-governed; precedent PR #78).
- **Spot-checks (CoS):** (1) `factory/HUB.md` Worktrees bullet is removal-only — matches the idle/worktree entry's refusal to over-cite it. (2) `buildRoundPromptMap` at `round-prompt-map.ts` ~230 branches on `!plan || plan.rounds.length === 0`; zero `M1_ROUND_ENGINE` refs in that file. (3) `orchestrator.ts` has 11 `M1_ROUND_ENGINE_ENABLED` occurrences (import + 10 gated sites) on feed/abort/resume paths — matches the generated-artifact entry. (4) On this tip, `inventory.mjs` still pairs `buildRoundPromptMap` with `flag(..., 'M1_ROUND_ENGINE_ENABLED')` — consistent with "class documented here; data fix in #82/#83". (5) Both index levels link the three new files; no orphan at either level from this diff.
- MERGEABLE tip on `feat/MEM-R3` → `master`. Verdict **not-merge-ready** until hosted CI is green and D2 lands on this tip.
- CoS review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (memory-only tip; analysis budget). Doctrine bus: published on `master` at `cos-feedback/adversarialllm/pr-84.md`.
