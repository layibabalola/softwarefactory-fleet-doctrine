schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 154
head: d382828e46facb560042681fe2d2a5137d548c98
reviewed_utc: 2026-09-26T21:34:36Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: MERGEABLE/UNSTABLE; tip CI settling — typecheck/lint/build/test IN_PROGRESS, docs drift QUEUED, factory contracts QUEUED (Actions run 36273254906); draft=false; 0 GitHub reviews; ahead 2 / behind 0 vs master

## Blockers

- HIGH-RISK merge-predicate / model-registry change (`factory/review-state.mjs` `evaluateMerge`, new `factory/models.json` + `factory/model-tier.mjs`, `factory/controller/*`, RESET-PLAN point 8): one review leg per family at loops/high is required before merge. Currently 0 GitHub reviews and no `requested_reviewers`. Do not merge while either family is dark.
- Hosted tip CI not green yet (run 36273254906 still settling on head `d382828e46fa`). Wait for factory contracts + typecheck/lint/build/test + docs drift to conclude before any merge attempt.

## Improvements

- After both family review legs land and hosted CI greens, have the controller re-run full `pwsh scripts/ci.ps1 -BaseSha 487b73e7c6658d32d77b33d93343cc5aa26ee0cf` on this exact head — the PR body reports host-lease contention prevented a complete local gate; only the factory-contracts subset (377/377) plus docs:constants / never-authorized-guard / adjudication-hashes were claimed locally.
- Close superseded PR #97 (TIER-ASTRA) once #154 merges, as the body already instructs the controller — do not leave two competing tier rows open.
- Non-blocking follow-up (DOC-TIER-FLOOR-TRANSPORT order 326): doctrine bus transport of the PROPOSED `factory/doctrine/bus-append-2026-09-26.md` traps/receipts after merge; out of scope for this tip.

## Notes

- Round-2 tip `d382828e46fa` (a2): copies `model-tier.mjs` and `models.json` into the cleanup-ownership and conductor-preflight state-module fixture copy lists, plus a review-state test that the relative-import closure is tracked. Fixes isolated worker-host module-load failures after `review-state.mjs` began importing the new registry. Product merge predicates / registry / HIGH_RISK surfaces unchanged vs prior tip `e102d72e9631`.
- Scope now: +825/−41 across 21 files, 2 commits. Absorbs/supersedes #97; implements issue #151.
- Core unchanged: `evaluateMerge` gains `TIER_EVIDENCE_MISSING` / `REVIEW_BELOW_TIER` / `VERDICT_UNPUBLISHED` on the authors path only. Registry is sole concrete-id SoT; highRisk → loops/high (opus + gpt-6-sol); default → bounded/low.
- Verdict **not-merge-ready** UNCHANGED — family review legs still missing; tip CI still unsettled. Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
