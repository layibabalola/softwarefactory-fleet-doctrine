schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 154
head: e102d72e96310bcd1d14e4e02f9877f33d157462
reviewed_utc: 2026-09-26T20:09:19Z
event: pr-opened
verdict: not-merge-ready
ci_summary: MERGEABLE/UNSTABLE; tip CI settling — typecheck/lint/build/test QUEUED, docs drift (delta gate) QUEUED, factory contracts QUEUED (Actions run 36268446795); draft=false; 0 GitHub reviews; ahead 1 / behind 0 vs master

## Blockers

- HIGH-RISK merge-predicate / model-registry change (`factory/review-state.mjs` `evaluateMerge`, new `factory/models.json` + `factory/model-tier.mjs`, `factory/controller/*`, RESET-PLAN point 8): one review leg per family at loops/high is required before merge. Currently 0 GitHub reviews and no `requested_reviewers`. Do not merge while either family is dark.
- Hosted tip CI not green yet (run 36268446795 still queued). Wait for factory contracts + typecheck/lint/build/test + docs drift to conclude on head `e102d72e9631` before any merge attempt.

## Improvements

- After both family review legs land and hosted CI greens, have the controller re-run full `pwsh scripts/ci.ps1 -BaseSha 487b73e7c6658d32d77b33d93343cc5aa26ee0cf` on this exact head — the PR body reports host-lease contention prevented a complete local gate; only the factory-contracts subset (377/377) plus docs:constants / never-authorized-guard / adjudication-hashes were claimed locally.
- Close superseded PR #97 (TIER-ASTRA) once #154 merges, as the body already instructs the controller — do not leave two competing tier rows open.
- Non-blocking follow-up (DOC-TIER-FLOOR-TRANSPORT order 326): doctrine bus transport of the PROPOSED `factory/doctrine/bus-append-2026-09-26.md` traps/receipts after merge; out of scope for this tip.

## Notes

- Scope: +792/−38 across 19 files on tip `e102d72e9631` (1 commit). Absorbs/supersedes #97; implements issue #151 (high-risk #49/#150 merged below tier / without verdict publication).
- Core: `evaluateMerge` gains `TIER_EVIDENCE_MISSING` / `REVIEW_BELOW_TIER` / `VERDICT_UNPUBLISHED` on the authors path only (legacy untouched). Registry is sole concrete-id SoT; highRisk → loops/high (opus + gpt-6-sol); default → bounded/low. `dispatch-review.ps1` classifies like `merge-pr.ps1` and refuses below-floor overrides (exit 2). `merge-pr.ps1` Receipt() adds `producer` + `publication` evidence blocks.
- Surfaces: `HIGH_RISK_SURFACES` gains `model-registry` and `model-tier`. Process docs (RESET-PLAN point 8, HUB, AGENTS, conductor/ratify-run prompts) move to aliases + `gpt-6-*`. Queue appends TIER-FLOOR (325) + DOC-TIER-FLOOR-TRANSPORT (326).
- Tests claimed: review-state 42 pass; controller 63 pass; queue-state 16; never-authorized-guard 0 FAIL of 371; adjudication-hashes PASS. VERIFY 3 replay of #49/#150 inputs → `REVIEW_BELOW_TIER` as intended. CoS did not re-run the Windows factory suite on this Linux box.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.