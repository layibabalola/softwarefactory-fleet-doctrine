schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 64
head: 42db283a6adade4e5dc1bd99a3d70142cda3012d
reviewed_utc: 2026-09-14T22:22:00Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: docs drift SUCCESS; factory contracts and typecheck/lint/build/test still IN_PROGRESS on head 42db283a (run 34903588903); no pwsh scripts/ci.ps1 receipt comment for this tip yet

## Blockers

- **Row dependency on DOC-V75-BENCH (#63)** — PR body still requires the GUARD-DOCTRINE-PORT queue row from #63; keep #64 stacked behind that open PR.
- **High-risk dual-family review (point 8)** — guard + `factory/RESET-PLAN.md` change; needs one independent review leg per family and must not merge while a family is dark. CoS does not staff Sol/Claude D2 legs.
- **Hosted CI not settled on this tip** — `docs drift` green; `factory contracts` and `typecheck / lint / build / test` still running on `42db283a`. Author claims local guard tests 316/316 (+13 vectors; 283 base unchanged). Wait for green hosted CI (or post the `pwsh scripts/ci.ps1` receipt for this exact head) before merge.

## Improvements

- This push correctly addressed the prior CoS note on bare/`FETCH_HEAD` merges: `busGitAllowed` now admits merge only with exactly one `origin/master` plus `--ff-only`/`-q`/`--quiet`; bare `merge --ff-only` and `FETCH_HEAD` are denied. Keep that exact-arg allowlist.
- Bus git exemption is now bound to a single `-C <path>` with no other global options (second relative `-C`, `--git-dir`/`--work-tree`, `-c`/`--config-env`, `--exec-path` denied). Good close of the pre-review falsifier gaps.
- `SHORT_NAME_SEG` now includes `softwa` so the bus 8.3 alias `SOFTWA~1` cannot hide the bus segment from `insideTree` on Write/Edit/shell. Good.
- After land, fold the substring-bypass + exact ff-only-merge + bus-git-exemption traps into bus `TRAPS.md` via a doctrine PR (project adjudication is not bus authority by itself).
- Owner adjudication still correctly records that message-2 autonomy text is **not** process authority; any standing autonomy rule must land through its own reviewed row into `CLAUDE.md` / `factory/HUB.md` / RESET-PLAN.

## Notes

- Delta vs prior CoS head `0da5aa77`: only `scripts/never-authorized-guard.mjs` + matching tests (+13 vectors; claimed 316/316). Destination allowlist and `insideTree` bus-path substring bypass from the first commit are unchanged.
- Scope overall: +85/−5 across guard, tests, `factory/RESET-PLAN.md` point-9 amendment, and `factory/adjudications/OWNER-20260914-fleet-filing-destination.md`.
- Supersedes closed unmerged #62. Broader sibling-repo case still deferred to row GUARD-INSIDE-ROOT.
- Prior CoS Improvement on requiring an explicit merge ref was clearly addressed (tightened further than suggested).
- No GitHub PR comment posted. Review via `gh` only (Cloud Agents not on plan).