schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 64
head: 0da5aa77d7656119dd89d85df3351d3ef7e5d49c
reviewed_utc: 2026-09-14T21:15:30Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: no hosted checks reported on head 0da5aa77 yet (mergeStateStatus CLEAN / empty rollup); no local pwsh scripts/ci.ps1 receipt comment for this tip

## Blockers

- **Row dependency on DOC-V75-BENCH (#63)** — PR body still requires the GUARD-DOCTRINE-PORT queue row from #63; keep #64 stacked behind that open PR.
- **High-risk dual-family review (point 8)** — guard + `factory/RESET-PLAN.md` change; needs one independent review leg per family and must not merge while a family is dark. CoS does not staff Sol/Claude D2 legs.
- **CI evidence for this tip** — author claims `node scripts/never-authorized-guard.test.mjs` → 303/303 locally after +6 vectors, but hosted checks have not reported on `0da5aa77` and there is still no `pwsh scripts/ci.ps1` receipt comment for this exact head. Post the receipt (or wait for green hosted CI) before merge.

## Improvements

- This push correctly closed Round-1 Claude probes on the prior flag-blacklist: last-option-wins (`--ff-only --ff`), attached `-sours`/`-Xtheirs`, and `--ff-only` only inside a quoted `-m` message are now denied via `ffOnlyBusMerge` exact-argument allowlist (`--ff-only`, `-q`/`--quiet`, `origin/master`, `FETCH_HEAD`). Keep that allowlist (do not regress to a blacklist).
- Consider requiring an explicit allowed ref (`origin/master` or `FETCH_HEAD`) in `ffOnlyBusMerge`, not only that every arg after `merge` is in the allowlist — bare `git -C <bus> merge --ff-only` would currently pass and merges the configured upstream.
- Owner adjudication still correctly records that message-2 autonomy text is **not** process authority; any standing autonomy rule must land through its own reviewed row into `CLAUDE.md` / `factory/HUB.md` / RESET-PLAN.
- After land, fold the substring-bypass + exact ff-only-merge traps into bus `TRAPS.md` via a doctrine PR (project adjudication is not bus authority by itself).

## Notes

- Delta vs prior CoS head `58f2f8da`: only `scripts/never-authorized-guard.mjs` + matching tests (+6 vectors; claimed 303/303). Destination allowlist and `insideTree` bus-path substring bypass from the first commit are unchanged.
- Scope overall: +71/−6 across guard, tests, `factory/RESET-PLAN.md` point-9 amendment, and `factory/adjudications/OWNER-20260914-fleet-filing-destination.md`.
- Supersedes closed unmerged #62. Broader sibling-repo case still deferred to row GUARD-INSIDE-ROOT.
- No GitHub PR comment posted. Review via `gh` only (Cloud Agents not on plan).
