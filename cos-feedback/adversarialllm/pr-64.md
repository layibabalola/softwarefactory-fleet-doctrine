schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 64
head: cc150bb8ab207e2f78f3f86f596565d9cd9b0945
reviewed_utc: 2026-09-15T00:45:00Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: all three hosted checks pending/queued on head cc150bb8 (run 34914439331: factory contracts, typecheck/lint/build/test, docs drift); no pwsh scripts/ci.ps1 receipt for this tip yet

## Blockers

- **Fresh dual-family D2 on current base (point 8 / HUB.md:15)** — tip `cc150bb8` is `Merge branch 'master' into feat/GUARD-DOCTRINE-PORT` after #63 squash-landed. Prior APPROVE receipts on `42db283a` (codex pr64-r1-codex-20260914b, claude pr64-r1-claude-20260914c) are stale-base. Controller note already requires re-run local gate + fresh current-base legs from both families before `evaluateMerge`. High-risk class; CoS does not staff Sol/Claude D2 legs.
- **Hosted CI not settled on this tip** — all three required checks still pending on `cc150bb8`. Prior tip `42db283a` had local GREEN (`pwsh scripts/ci.ps1`, 316/316 guard) and hosted SUCCESS; that receipt does not cover the merge-from-master tip. Wait for green hosted CI (or post a local receipt for `cc150bb8`) before merge.

## Improvements

- Keep the exact-arg `busGitAllowed` allowlist (`--ff-only` + exactly one `origin/master` + optional `-q`/`--quiet`); bare merge, `FETCH_HEAD`, and strategy/flag overrides stay denied.
- Keep single `-C <path>` bus-git binding (no second relative `-C`, `--git-dir`/`--work-tree`, `-c`/`--config-env`, `--exec-path`).
- Keep `softwa` in `SHORT_NAME_SEG` so `SOFTWA~1` cannot hide the bus segment from `insideTree`.
- After land, fold substring-bypass + exact ff-only-merge + bus-git-exemption traps into bus `TRAPS.md` via a doctrine PR.
- Owner adjudication correctly keeps message-2 autonomy text out of process authority; any standing autonomy rule still needs its own reviewed row into `CLAUDE.md` / `factory/HUB.md` / RESET-PLAN.

## Notes

- Delta vs prior CoS head `42db283a`: merge-from-master only (no guard/test delta in this push). Diff vs base still +85/−5 across guard, tests, RESET-PLAN point-9 amendment, and `factory/adjudications/OWNER-20260914-fleet-filing-destination.md`.
- **#63 (DOC-V75-BENCH) is MERGED** (2026-09-15T00:43:09Z) — prior CoS blocker "row dependency on #63" is cleared; GUARD-DOCTRINE-PORT queue row should now be on master.
- Prior D2 on `42db283a`: both families APPROVE with empty must-fix; Codex F-01 SHOULD deferred to queued GUARD-INSIDE-ROOT. Those verdicts do not transfer to `cc150bb8`.
- Supersedes closed unmerged #62. Broader sibling-repo case still deferred to GUARD-INSIDE-ROOT.
- No GitHub PR comment posted. Review via `gh` only (Cloud Agents not on plan).
