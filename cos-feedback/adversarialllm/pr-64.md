schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 64
head: 58f2f8dacb2e7b64ff88ad4b703213f58e09a7d8
reviewed_utc: 2026-09-14T21:09:30Z
event: pr-opened
verdict: not-merge-ready
ci_summary: docs drift green; factory contracts and typecheck/lint/build/test still IN_PROGRESS (mergeStateStatus UNSTABLE)

## Blockers

- **Row dependency on DOC-V75-BENCH (#63)** — PR body states the GUARD-DOCTRINE-PORT queue row is added by #63, so this PR must not merge until that one lands. Keep #64 stacked behind #63.
- **High-risk dual-family review (point 8)** — changes the guard and `factory/RESET-PLAN.md`; needs one independent review leg per family and must not merge while a family is dark. CoS does not staff Sol/Claude D2 legs.
- **Hosted CI incomplete** — `factory contracts` and `typecheck / lint / build / test` still running on head `58f2f8da`. Wait for green (or fix failures) before merge; author also promised a local `pwsh scripts/ci.ps1` receipt comment for this exact head.

## Improvements

- Guard delta is the right supersession of closed #62: destination allowlist is exact (`adjudications/<subject>/AdversarialLLM.md|.rubric.json`), `insideTree` closes the bus-path substring bypass, and bus `merge` is admitted only with `--ff-only` and without `--no-ff`/`-X`/`-s`/`--strategy`. Keep that trio together on land.
- Owner adjudication correctly records that message-2 autonomy text is **not** process authority; ensure any standing autonomy rule still goes through its own reviewed row into `CLAUDE.md` / `factory/HUB.md` / RESET-PLAN.
- After land, fold the substring-bypass + ff-only-merge traps into bus `TRAPS.md` via a doctrine PR (project adjudication is not bus authority by itself).

## Notes

- Scope: +55/−4 across `scripts/never-authorized-guard.mjs`, matching tests (+14 vectors; author claims 297/297 local), `factory/RESET-PLAN.md` point-9 amendment, and `factory/adjudications/OWNER-20260914-fleet-filing-destination.md`.
- Supersedes closed unmerged #62 (no row token; prior CoS blockers there were conflicts / queue pollution / undeclared lint gate — none of those appear in this tip).
- Broader sibling-repo case deferred to row GUARD-INSIDE-ROOT (out of scope here).
- No GitHub PR comment posted. Review via `gh` only (Cloud Agents not on plan).