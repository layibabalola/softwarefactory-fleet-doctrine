schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 105
head: ce36866e0448376e48b860ba26978e8446f101ad
reviewed_utc: 2026-09-18T20:40:00Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: all three CI jobs still IN_PROGRESS on run 35392636334 (docs drift, factory contracts, typecheck/lint/build/test); mergeable MERGEABLE / UNSTABLE; 0 GitHub reviews on tip

## Blockers

(none for content — +4/−0 across `factory/RESET-PLAN.md` and `factory/queue.jsonl`; self-filed row `PROC-ROW-TOKEN` matches body token; HUB.md untouched)

## Improvements

- **Wait for CI** — confirm `docs drift (delta gate)`, `factory contracts`, and `typecheck / lint / build / test` PASS on head `ce36866e` before merge. Author claims local `pwsh scripts/ci.ps1 -BaseSha 6b504b25…` GREEN (1610s, 18 steps) and `node --test factory/queue-state.test.mjs` 16/16; re-verify on the live Actions run.
- **High-risk D2 (point 8 / process-authority)** — edits RESET-PLAN operative amendment point 1. Needs one independent adversarial leg per family (Claude + Codex) with literal APPROVE from a non-authoring leg; authoring Claude Opus 5 session must not merge. Do not treat CoS verdict as that APPROVE. Do not merge while a family is dark.
- **Open follow-on called out in the PR** — mechanical CI/contract check that rejects a body token missing from the head queue is explicitly out of scope here; file its own row/PR when ready.
- **Related open work** — `#94` (`row:ph-P2-R-AMEND`) is cited as the no-row shape that must add its own row before merge under this rule; keep that in the controller brief after this lands.

## Notes

- Scope: one sentence on RESET-PLAN point 1 (every extracted `row:` token names a queue row at the PR head; proposal/filing PRs self-file) plus queue row `PROC-ROW-TOKEN` (order 246, family claude, high-risk process-authority). No HUB.md bytes; no rewrite of historical tokens.
- Rationale in body is coherent: `evaluateQueue` only matches real row ids; no existing check catches mistyped tokens; uniform rule is mechanically checkable; matches CLAUDE.md / HUB Queue wording.
- Adjudication swarm (against-default / outranks / post-mortem) already ran per body; synthesis rejected dual-convention; D6 HUB budget is why the sentence lives in point 1 not HUB.md.
- Branch `feat/PROC-ROW-TOKEN` → `master`; tip `ce36866e`; author layibabalola (co-authored Claude Opus 5); 0 reviews; created 2026-09-18T20:39:20Z.
- Cursor Cloud Agents not used (small process/docs delta); review via gh. No GitHub PR comment posted.
- Doctrine bus: first publish `cos-feedback/adversarialllm/pr-105.md` on master for this open.