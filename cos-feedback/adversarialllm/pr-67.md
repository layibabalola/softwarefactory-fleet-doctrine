schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 67
head: cd619ffedf65f48c6c6ba59de5eff3cbce2cc106
reviewed_utc: 2026-09-15T03:58:55Z
event: pr-opened
verdict: not-merge-ready
ci_summary: docs drift pass; factory contracts and typecheck/lint/build/test still pending on head cd619ffe (run 34926901003)

## Blockers

- **Fresh dual-family D2 (point 8 / HUB.md:15)** — high-risk class: changes the never-authorized guard and its vector table. Needs one review leg per family on tip `cd619ffe` before merge; authoring controller must not turn the merge key. CoS does not staff Sol/Claude D2 legs.
- **Hosted CI not settled** — `factory contracts` and `typecheck / lint / build / test` still pending on this tip. Author claims local `node scripts/never-authorized-guard.test.mjs` → 0 FAIL of 352 and `factory/queue-state.test.mjs` 15/15; wait for green hosted checks (or a posted local `pwsh scripts/ci.ps1` receipt for this exact head) before merge.

## Improvements

- Keep the root-prefix `insideTree` (workspace = parent of first `AdversarialLLM*` segment under `$CLAUDE_PROJECT_DIR`; unresolvable `\\?\` / trailing-dot / mid-path colon fail closed). Do not regress to substring `AdversarialLLM` matching.
- Keep `busVerbArgsAllowed` denials that close Claude #64 F-03: fetch/push `--upload-pack`/`--receive-pack`/`--exec` (incl. unique long-option prefixes), leading `+` refspecs, destinations `master` / `refs/heads/master` / `HEAD` / `origin/*` / `refs/heads/origin/*` / `refs/remotes/*`; fetch also `--refmap`/`--force`/`-f`/`--update-head-ok`/`-u`; commit `--no-verify`/`-n` (bundled or abbreviated). The composition `fetch …:refs/heads/origin/master` then admitted `merge --ff-only origin/master` must stay denied at fetch.
- Residual gaps called out in the PR (noisy-but-safe 8.3/long-name aliases; bare `AdversarialLLM*` in workspace needs a directory existence check; env-var roots never resolved) are acceptable fail-closed for this row — track as follow-ups only if a real bypass appears.
- After land, fold the new inside-prefix and bus fetch/push/commit argument traps into bus `TRAPS.md` via a doctrine PR (same pattern as GUARD-DOCTRINE-PORT).

## Notes

- Depends on merged #64 (GUARD-DOCTRINE-PORT, `2026-09-15T03:51:06Z`); queue row VERIFY extended in `factory/queue.jsonl` for the bus-arg clauses.
- Diff vs base: +133/−22 across `scripts/never-authorized-guard.mjs`, `scripts/never-authorized-guard.test.mjs` (316→352 vectors; 14 literal fixtures re-rooted under a qualifying workspace), and the GUARD-INSIDE-ROOT queue VERIFY line.
- Author falsification vs #64 prior guard: 57 payloads → 20 unchanged, 37 allow→deny, 0 deny→allow; two newly denied but fail-closed (ADS spelling; quoted `--out=` not starting with a path).
- No GitHub PR comment posted. Review via `gh` only (Cloud Agents not on plan).