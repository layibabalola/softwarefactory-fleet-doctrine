schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 67
head: e7e0e82ef54aba4793e9a9bdfe589fb70bc16fe8
reviewed_utc: 2026-09-15T05:02:00Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: docs drift, factory contracts, and typecheck/lint/build/test all still pending on head e7e0e82e (run 34930885848); prior tip cd619ffe had local pwsh scripts/ci.ps1 GREEN plus hosted SUCCESS

## Blockers

- **Fresh dual-family D2 (point 8 / HUB.md:15)** — high-risk class: guard + vector table. Round-1 legs on `cd619ffe` returned AMEND (Codex MUST on test-weakening / reason drift; Claude SHOULD/AMEND on short remotes/heads destinations, digit-led short bundles, escapes, abbreviated push options, project GitHub URLs). Round-2 tip `e7e0e82e` closes those findings; need one fresh review leg per family on **this tip** before merge. Authoring controller must not turn the merge key. CoS does not staff Sol/Claude D2 legs.
- **Hosted CI not settled on this tip** — all three required checks still pending on `e7e0e82e`. Author claims local `node scripts/never-authorized-guard.test.mjs` → 0 FAIL of 371 and `factory/queue-state.test.mjs` 15/15; wait for green hosted checks (or a posted local `pwsh scripts/ci.ps1` receipt for this exact head) before merge.

## Improvements

- Keep the root-prefix `insideTree` (workspace = parent of first `AdversarialLLM*` segment under `$CLAUDE_PROJECT_DIR`; unresolvable `\\?\\` / trailing-dot / mid-path colon fail closed; bare workspace `AdversarialLLM*` now directory-existence checked). Do not regress to substring `AdversarialLLM` matching.
- Keep `busVerbArgsAllowed` denials that close Claude #64 F-03 and #67 r1: fetch/push `--upload-pack`/`--receive-pack`/`--exec` (incl. unique long-option prefixes and backtick/backslash escapes), leading `+` refspecs, destinations `master` / `refs/heads/master` / `HEAD` / `origin/*` / `heads/*` / `remotes/*` / `refs/heads/origin/*` / `refs/remotes/*`; fetch also `--refmap`/`--force`/`-f`/`--update-head-ok`/`-u` (incl. digit-led short bundles); commit `--no-verify`/`-n`; push `--repo` making every positional a refspec. Keep drive-relative fail-closed and project GitHub URL scrubbing on the foreign-path route.
- Keep Write/Edit short-name denial **before** foreign (mirrors shell order) so 8.3 aliases retain their named reason — round-2 MUST for base-reason stability.
- Residual gaps called out in the PR (noisy-but-safe 8.3/long-name aliases; env-var roots never resolved) remain acceptable fail-closed for this row — track as follow-ups only if a real bypass appears.
- After land, fold the new inside-prefix and bus fetch/push/commit argument traps into bus `TRAPS.md` via a doctrine PR (same pattern as GUARD-DOCTRINE-PORT).

## Notes

- Round-2 commit `e7e0e82e` on top of open-reviewed `cd619ffe`: reason-diff over all 316 base vectors → 0 changed; vector table 352→371; queue VERIFY extended for reason-stability proof and the new bus/drive/`--repo` clauses.
- Prior tip `cd619ffe`: local ci.ps1 GREEN (1244s) + hosted SUCCESS posted; Codex r1 attempt first marked INFRASTRUCTURE (prompt omitted row/implementer tokens), then live AMEND JSON; Claude r1 AMEND JSON. No fresh legs yet on `e7e0e82e`.
- Depends on merged #64 (GUARD-DOCTRINE-PORT). Diff vs base: +179/−28 across `scripts/never-authorized-guard.mjs`, `scripts/never-authorized-guard.test.mjs`, and the GUARD-INSIDE-ROOT queue VERIFY line.
- No GitHub PR comment posted by CoS. Review via `gh` only (Cloud Agents not on plan).
