schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 99
head: 06dd6d68ac425db9aa5fa81901d9924a493c7468
reviewed_utc: 2026-09-17T19:53:12Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS on head 06dd6d68; factory contracts + typecheck/lint/build/test IN_PROGRESS (run 35267294464); 0 GitHub reviews on tip

## Blockers

(none for content — proposal-only ordinary-class staging of one queue row + proposal + adjudication; no AGENTS.md / implementer.md / HUB.md / RESET-PLAN.md / scripts / hooks / CI / product paths touched; implements nothing)

## Improvements

- **Wait for remaining CI** — factory contracts and typecheck / lint / build / test still IN_PROGRESS at review; confirm both PASS before merge (docs drift already green on this head). Author claims local `pwsh scripts/ci.ps1` GREEN, `validateRows` OK (52 rows, new row order 252), and `node --test factory/queue-state.test.mjs` 16/16 — re-check against the GitHub jobs, not the claim alone.
- **Independent non-author review on tip** — ordinary class (queue + proposals/ + adjudications/ outside point 8), but merge still needs factory/HUB.md D2: validated current-head legs, a literal APPROVE from a non-author leg, and merge by a fresh non-author controller. Land at least one independent non-author APPROVE on 06dd6d68 before merge.
- **Keep implementation out of this PR** — this head correctly proposes only. The follow-on implementing row owns AGENTS.md + factory/prompts/implementer.md (+ HUB.md Roles/Retries only if needed), is high-risk (point 8), and must not adopt `--sandbox danger-full-access` (explicit owner-authorized high-risk row required if safe shapes all fail probes). Do not reopen/advance PR 96 / P8-R from this merge.

## Notes

- Scope: +152/−0 across exactly 3 paths vs base 6b504b25 — new factory/proposals/CODEX-DISPATCH-PROBE.md, factory/adjudications/CODEX-DISPATCH-20260917.md, and one appended queue row CODEX-DISPATCH-PROBE (order 252, family claude, no dependsOn; spec points at the proposal + adjudication). Matches stated proposal-only pattern (#97/#98 and prior).
- Finding: measured on PR 96 / P8-R — `codex exec --sandbox workspace-write` (codex-cli 0.154.0) cannot create `.git/index.lock` on any tried checkout shape (linked worktree, standalone clone, harness worktree, safe.directory=* probe), while implementer.md:7/17/19 still require that seat to commit. Three adversarial legs refused controller-authored commits (RESET-PLAN point 8 / NON_AUTHOR_APPROVAL_REQUIRED). Owner chose contract amendment over desktop recovery drive-by.
- Queue JSON line parses; order 252 sits above open staging orders seen on other tips (248–251 on #90/#97/#98 and peers) and master max 247. Correction recorded: workspace-write mandate lives in implementer.md, not AGENTS.md.
- Branch feat/CODEX-DISPATCH-PROBE → master; mergeable=true; mergeable_state=unstable (checks pending); not draft; tip 06dd6d68; base 6b504b25; author layibabalola (co-authored Claude Opus 5); 0 reviews.
- Cursor Cloud Agents not used; review via gh (factory queue + proposal + adjudication). No GitHub PR comment posted.
- Doctrine bus: create cos-feedback/adversarialllm/pr-99.md on master (first CoS pass for this PR).
