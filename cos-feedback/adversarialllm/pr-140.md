schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 140
head: 955265956cc24d3e907b986aceff8257352dfb9c
reviewed_utc: 2026-09-25T09:20:08Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 36117800338 — docs drift QUEUED, typecheck/lint/build/test QUEUED, factory contracts IN_PROGRESS; mergeable true / UNSTABLE; 0 GitHub reviews; author claims full root CI GREEN locally on 95526595 (1044s, supervisor exit 0)

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `95526595`.** Actions run 36117800338 still has docs drift (delta gate) and typecheck/lint/build/test QUEUED; factory contracts IN_PROGRESS. Do not merge on an incomplete check set. Author local full-root CI GREEN is supporting evidence only.
- **High-risk two-family D2 still required.** PR body marks this high-risk (`factory/review-state.mjs` ownership): both-family independent exact-head/base reviews plus all three current-head hosted checks before D2 merge; author does not merge. CoS does not substitute for that adjudication.
- **Same-round-only supersession is intentional.** `selectCurrentReviews` rejects cross-round markers (`prior.attempt.round !== review.attempt.round` → `INVALID_SUPERSESSION`). Fixture repairs stay on the same round. Confirm that matches the GATE-ATTEMPT-SUPERSEDE-R row; cross-round repair would need a follow-up if desired.

## Notes

- **What the head does:** Adds `selectCurrentReviews` in `factory/review-state.mjs` so retained reviews are chosen by explicit same-family, same-round `supersedes` markers, independent of receipt order/timestamps. Rejects missing targets, self-links, chains, fan-in, and duplicate attempt ids. Only retained reviews feed topology, findings, and approval checks (including degraded mode). Infra failures stay excluded from the candidate set.
- **Wiring:** Legacy `evaluateLegacyMerge` applies the new selector only when any receipt carries `supersedes` (preserves unmarked legacy vectors). Non-legacy `evaluateMerge` always runs `selectCurrentReviews` on subject-matched REVIEWED receipts.
- **Tests:** +14 focused cases in `factory/review-state.test.mjs` — exhaustive small-topology permutations for named guard outcomes, eight source mutations that must fail their named regressions, and degraded retained-receipt checks in every order. Author: 33 review-state tests pass on this tip.
- **Scope:** vs master `2074c90b`, +112/−3, 2 files, 1 commit on `feat/GATE-ATTEMPT-SUPERSEDE-R`. Row `GATE-ATTEMPT-SUPERSEDE-R`; implementer codex. Body says this implements the already-reviewed replacement row from #127, not a new successor for #129. Related earlier work: #114 (order-dependent `selectReviewAttempt`). This tip closes the prior CoS degraded-path gap from #114 by selecting retained receipts in degraded mode as well.
- Cursor Cloud Agents not used (review via `gh` / GitHub API). No GitHub PR comment posted.
