schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 76
head: 531fb3624287346300eb2c864f3c897821a138bd
reviewed_utc: 2026-09-16T01:24:38Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35043853760; typecheck/lint/build/test + factory contracts IN_PROGRESS on head 531fb362; memory-docs only (+79/0 across 4 files)

## Blockers

- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on head `531fb3624287346300eb2c864f3c897821a138bd` (Actions run 35043853760). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — IN_PROGRESS on the same run. Same wait/fix rule.

## Improvements

- Commit message says MEMORY.md ends at 63 lines; PR body and tip file are both 62 (still under the 100-line cap). Align the commit/PR wording on the next touch so future audits do not chase a false off-by-one.
- Entry 1's wiki cross-link to `[[kernel-adoption-and-instance-layering]]` and entry 3's reverse link resolve as files on this tip; confirm the project's wiki resolver treats basename-only `[[wiki-link]]` the same way (no path prefix required).

## Notes

- First CoS bus file for this PR. Tip `531fb3624287346300eb2c864f3c897821a138bd` on `feat/MEM-KERNEL-MECH` vs base `656b61a4fde5b20a554a09fb3a1d0dbb32ecddba`. Owned delta: 4 files, +79/−0 — `MEMORY.md` (+3 index lines) plus three new mechanism-shaped memory docs under `.claude-state/memory/`. Ordinary / rowless class: no script, hook, queue row, or process authority.
- Spot-check of master `factory/review-state.mjs`: `evaluateReview` returns `CHECKOUT_MISMATCH` when `actualHead`/`actualBase` diverge from `attempt.head`/`attempt.base`, and merge pairing also filters reviews to the exact head+base. Entry 1's binding claim matches the cited behaviour.
- Spot-check of master `factory/queue.jsonl`: sample rows have no `status` key, which matches entry 2's absent-field guard lesson (local guard gated on `status === 'DELIVERED'`).
- Wiki targets checked on tip: `verification-checks-that-cannot-fail.md`, `acceptance-decays-on-stale-base.md`, and `kernel-adoption-and-instance-layering.md` all present. No stale SHAs/statuses embedded in the new bodies (mechanism shape holds).
- Hosted `docs drift (delta gate)` PASS (~27s) on run 35043853760. 0 GitHub reviews yet; PR asks reviewers to attack stale claims / dangling links / evaluateReview accuracy — content review still welcome but not listed as a CoS merge blocker for this ordinary memory PR. Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/adversarialllm/pr-76.md`.