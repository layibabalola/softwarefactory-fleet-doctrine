schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 76
head: 64fe7ff63f8919fc2d02eb89a8488561f313b589
reviewed_utc: 2026-09-16T02:39:03Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35048724076; typecheck/lint/build/test + factory contracts IN_PROGRESS on head 64fe7ff6; memory-docs only (+113/0 across 5 files)

## Blockers

- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on head `64fe7ff63f8919fc2d02eb89a8488561f313b589` (Actions run 35048724076). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — IN_PROGRESS on the same run. Same wait/fix rule.

## Improvements

- Entry 1's wiki cross-link to `[[kernel-adoption-and-instance-layering]]` and entry 3's reverse link still resolve as files on this tip; confirm the project's wiki resolver treats basename-only `[[wiki-link]]` the same way (no path prefix required). Same check for entry 4's link to `[[acceptance-decays-on-stale-base]]`.
- Prior CoS note on MEMORY.md line-count wording (commit said 63, tip was 62) is cleared on this tip: MEMORY.md is now 63 lines and matches the first commit message; keep PR body wording aligned on any further touch (body still says 62).

## Notes

- Later push on `feat/MEM-KERNEL-MECH`: tip moved `531fb362` → `64fe7ff63f8919fc2d02eb89a8488561f313b589` (commit `MEM: receipts are append-only and truth-independent`). Owned delta now 5 files, +113/−0 — prior three mechanism docs plus new `memory/feedback/receipts-are-append-only-and-truth-independent.md` and a fourth MEMORY.md index line. Still ordinary / rowless: no script, hook, queue row, or process authority. Base unchanged `656b61a4fde5b20a554a09fb3a1d0dbb32ecddba`; mergeable_state unstable while CI runs.
- Spot-check of master `factory/review-state.mjs` for the new entry: `evaluateReview` accepts verdicts by shape (overall/items/identity), not truth of findings. Non-degraded `evaluateMerge` requires exactly one valid receipt per family on the same pr/head/base (no recency or supersession filter), refuses `UNRESOLVED_FINDINGS` when any receipt has MUST items or overall BLOCKER, and returns `MISSING_OR_AMBIGUOUS_REVIEW` when a family is duplicated (three receipts with two from one family cannot satisfy). Selective merge inputs that omit an adverse receipt would incorrectly look green — entry 4's census-vs-summary claim matches the gate.
- Prior entry 1/2 spot-checks still hold (`CHECKOUT_MISMATCH` on head/base drift; sample `factory/queue.jsonl` rows lack `status`). Wiki targets on tip include the new receipts file plus prior three; no stale SHAs/statuses embedded in the new body (mechanism shape holds). Hosted docs drift PASS (~27s) on run 35048724076. 0 GitHub reviews. Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: refreshed on `master` at `cos-feedback/adversarialllm/pr-76.md`.
