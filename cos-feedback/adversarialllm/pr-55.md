schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 55
head: ddb6d407faa21f2129cc387562c9a738de1d7ceb
reviewed_utc: 2026-09-15T21:08:28Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35023740565; typecheck/lint/build/test + factory contracts IN_PROGRESS on head ddb6d407; author claims exact-head local full CI GREEN exit0 726s vs base b630e89

## Blockers

- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on head `ddb6d407faa21f2129cc387562c9a738de1d7ceb` (Actions run 35023740565). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Process: independent dual-family D2 on this head** — 0 GitHub reviews yet; PR body requires fresh Sol + Opus (high-risk) reviews on the *current* head/base (`ddb6d407` / `b630e89`) before merge (original head `85f2fc1d` reviews/checks are stale). Land those after hosted CI settles.

## Improvements

- `adversarialllm/evidence/P2.5a-dispatch-probe.md` still cites inspection base `2153e3af…` while the tip integrates onto `b630e89…`. Refresh the evidence header (and any anchors that drifted) so offline proof matches the merge tip consumers will check.
- `adversarialllm/src/background/orchestrator/dispatch-probe.ts` opens with a leftover "Step 1: Send prompt" comment even though the module no longer sends; reword to readiness/activation so future readers are not misled.
- Circular type edge: `dispatch-probe.ts` imports `DispatchOneDeps` from `dispatch-one.ts` while `dispatch-one.ts` imports the probe helpers. Prefer a tiny shared deps type (or keep the `Pick<>` in a neutral module) if the next extract also needs the same bag.

## Notes

- First CoS bus file for this PR (no prior `pr-55.md`). Tip `ddb6d407` is `P2.5a: preserve base line endings in scoped docs` after master integrate `f38f11bc` onto `b630e89`. Owned delta vs base is nine files (+588/−409): new `dispatch-probe.ts` (~391 lines), `dispatch-one` call-out + abort fence retained after probe, unused-import cleanup in `orchestrator.ts`, docs/architecture anchors, evidence, focused probe tests, and production-path echo cancel coverage during `PROBE_RENDERING`.
- Ordering look correct for the claimed contract: outbox creation still precedes `runPreSendProbe`; `abortBeforePromptTransmission('send phase')` still gates before `executionStartTime` / SEND / UPLOAD / CDP. Probe may return only on preferred-model fail-fast; otherwise returns `undefined` and the caller owns transmission.
- New unit coverage pins 3s PING retry spacing, epoch-before-recycle then replacement-tab probe after settle, post-ready delay for iter 1/`undefined`, no activate when abort already set, plus echo test that abort mid-`PROBE_RENDERING` yields "aborted before prompt transmission" with zero `SEND_PROMPT`/`UPLOAD_FILE`/CDP sends.
- Offline source refactor only; no live-provider activation or certification claimed. Dual-family D2 still required per PR body. Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: created on `master` at `cos-feedback/adversarialllm/pr-55.md`.
