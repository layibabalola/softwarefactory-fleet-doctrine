schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 87
head: d553230e49a20d0979a8762af249093428c5f8e9
reviewed_utc: 2026-09-16T13:45:16Z
event: pr-opened
verdict: not-merge-ready
ci_summary: tip d553230e49a2 — docs drift (delta gate) PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on Actions run 35103194465; mergeable=true (unstable); 0 GitHub reviews on tip

## Blockers

- **CI: hosted jobs not settled green on this tip** — `docs drift (delta gate)` already PASS on head `d553230e49a20d0979a8762af249093428c5f8e9`. Wait for `factory contracts` and `typecheck / lint / build / test` on Actions run `35103194465`. Do not merge while either is pending or red; if red, fix and re-push.
- **Process: independent dual-family D2 on this exact head** — 0 GitHub PR reviews on tip `d553230e`. After hosted CI settles green, land fresh Sol/Codex + Claude/Sonnet on this tip. Non-author controller turns the key. Author (codex / `codex/ph-M2-R`) does not merge. Reviewers should verify (1) RecipeEditor is actually mounted from `App.tsx` with New/Edit/Save/Cancel/Delete wired, (2) run path forwards compiled `plan` + plan-derived `maxIterations` through `buildStartCampaignMessage` (not recipeId-only), (3) iterations field is read-only and seeded from `BUILTIN_PLANS` (addresses PH-M2-PRODUCTION-PATH-001 / ph-M2-2-authored-iterations-inert), (4) stale `listRecipes` revision guard and delete-unrelated-draft retention, and (5) inventory gap/test now require the mounted journey (not "RecipeEditor is not mounted").

## Improvements

- `adversarialllm/src/entrypoints/sidepanel/App.tsx` `handleRunRecipe`: UI mirror uses `setMaxIterations(recipe.iterations)` while the start message uses `compiled.iterations`. For an older saved recipe whose stored iterations disagree with the plan, the visible config can lag the executed count. Prefer `setMaxIterations(compiled.iterations)` after `compileRecipe` succeeds.
- `adversarialllm/tests/unit/recipe-author-run.test.tsx`: gallery card targeting via `parentElement!.parentElement!` is brittle if gallery markup shifts. Prefer a role/name scoped query (or a stable test id on the card) so Edit/Run/Delete clicks stay deterministic.
- `App.tsx` `handleRunRecipe` comment block is useful for the M1 flag vs plan-presence audit, but it is long for production UI code; after D2, consider moving the measured-behavior notes into evidence/docs and keeping a short pointer in code.

## Notes

- Scoped review of tip `d553230e49a20d0979a8762af249093428c5f8e9` vs `master`: 8 files, +294/−31. Delivers corrected ph-M2 recipe authoring: mount RecipeEditor, plan-derived iterations, explicit plan on start, inventory/evidence/docs updates. Label `impl:codex`. PR body: `row:ph-M2-R`; states PR52 remains closed and no live-provider action.
- Diff hotspots: `App.tsx` (edit/mutate/run + gallery fieldset), `RecipeEditor.tsx` (read-only iterations + `selectPlan`), `start-campaign-inputs.ts` (optional `plan` clone), new `tests/unit/recipe-author-run.test.tsx` (5 cases), `inventory.mjs` + inventory test + `evidence/ph-M2-recipe-authoring.md` + progress table line for M2.
- **Spot-checks (CoS):** (1) `editRecipe` clones built-ins to fresh UUID and seeds `iterations` from `BUILTIN_PLANS.get(planId)?.rounds.length`. (2) `mutateRecipe` snapshots, bumps `recipeRevision`, retains draft on catch; delete only clears draft when same `recipeId`. (3) `handleRunRecipe` compiles then passes `{ recipeId, maxIterations, plan }` into `runCampaignStart` → `buildStartCampaignMessage`. (4) `buildStartCampaignMessage` adds `plan` only when present (manual start stays byte-identical without it). (5) Inventory M2 gap flips to mounted-journey wording when `RecipeEditor` mounts; unit test asserts that. Live-provider / activation proof explicitly out of scope per evidence and PR body.
- MERGEABLE tip on `codex/ph-M2-R` → `master`. Verdict **not-merge-ready** until hosted CI is green and D2 lands on this tip.
- CoS review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (gh review within analysis budget). Doctrine bus: publish on `master` at `cos-feedback/adversarialllm/pr-87.md`.