schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 124
head: 4a81f566f86cbf86f51d0aae2cee8faa847baf00
reviewed_utc: 2026-09-23T03:54:18Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 35816132982 — factory contracts, typecheck/lint/build/test, and docs drift (delta gate) all IN_PROGRESS; mergeable true / UNSTABLE; not draft; ahead 1 behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `4a81f566`.** Actions run 35816132982: all three jobs still IN_PROGRESS at review time. Do not merge until `factory contracts`, `typecheck / lint / build / test`, and `docs drift (delta gate)` settle green.
- **Two-leg / D2 still open (factory practice).** Row `CI-PROFILE-PATH` is `family: claude` with implementer **codex** (cross-family). Spec places the row outside high-risk because `scripts/ci.ps1` is untouched, but GitHub Reviews API returns 0 formal review objects. Controllers should still require the independent second-family adjudication before merge; CoS does not substitute.
- **Confirm path-evidence artifact is retained next to the merge head.** PR body cites `.factory-local/cpp-long-root-path-evidence.json` (longest `ci.ps1 --output` 190 chars; longest `p7-downloads` 182 chars) and green local CI from short + 105-char roots. That evidence is not in the PR tree (runtime under `.factory-local/`). Controllers: keep the evidence path reachable for VERIFY item (4)/(5) closeout.

## Notes

- **Intentional scope matches the queue row.** Only `adversarialllm/tests/mock/fixture-page.ts` (+29/−1) and `adversarialllm/tests/mock/two-provider-two-round.test.ts` (+25/−2). `scripts/ci.ps1` untouched; no assertion removed or weakened. PR vs master ahead 1 / behind 0 on `feat/CI-PROFILE-PATH`.
- **VERIFY spot-check on tip `4a81f566`:** (1) `PROFILE_PREFIX = 'p7-'` (3 chars ≤4) replaces `p7-playwright-profile-`; profile still under `path.resolve('.factory-local')`. (2) `assertFixtureProfilePathBudget` on win32 fails before Chrome when predicted IndexedDB directory >247 or longest file >259, message names both lengths, both limits, and maximum supported checkout root; unit case uses synthetic 250-char `C:\…` parent and asserts the regex. (3) After first context `close()`, `assertPersistedIndexedDb` requires a `chrome-extension_*_0.indexeddb.leveldb` directory under `Default/IndexedDB` with at least one file (all platforms). (4)/(5) claimed by author in PR body (not re-run here).
- **Delivers the defect filed under #120 notes.** Shortens the mkdtemp prefix that previously pushed IndexedDB LevelDB paths past Win32 limits above ~96-char checkout roots.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
