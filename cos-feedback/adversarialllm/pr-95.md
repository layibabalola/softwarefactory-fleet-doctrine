schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 95
head: c0c4d91ac4ffc7c71cc89b62c848ca8c4f275f2f
reviewed_utc: 2026-09-17T15:44:38Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS on head c0c4d91a; factory contracts + typecheck/lint/build/test IN_PROGRESS; 0 GitHub reviews yet

## Blockers

(none for content — staging path set is exact and ordinary-class)

## Improvements

- **Dual-family D2** — 0 GitHub reviews on tip yet. Land independent Sol + Opus APPROVE on this head, then merge by a fresh non-author controller (`factory/HUB.md` D2). Author is layibabalola / implementer Claude per PR body.
- **Wait for remaining CI** — `factory contracts` and `typecheck / lint / build / test` were still IN_PROGRESS at review; confirm both PASS before merge (docs drift already green).
- **Close hygiene** — #65 (r2 staging) is already CLOSED unmerged at the same minute this PR opened; no further action needed on #65. Prefer leaving `cos-feedback/adversarialllm/pr-65.md` as historical (consumers ignore closed PRs).

## Notes

- Scope: staging only (+201/−0, exactly 3 owned paths). Adds `factory/doctrine/adjudications/factory-kernel/adversarialllm.md` (kernel r4 / code@r4 dogfood filing at a blocked seam, `subjects: 0`), `factory/doctrine/kernel-block-2026-09-17.md` (`KERNEL: DOGFOOD-PENDING …`), and one appended queue row `KERNEL-DOGFOOD-R4` (order 250). Writes no bus byte; transport needs a separate reviewed D12 row.
- Filing shape spot-check: header has project/kernel/profile/instance/subjects/window/health/providers/posture; K1–K12 present; K3–K7 are UNEXERCISED; FRICTION lines (K2, K12) carry REPLACES; INSTANCE-FAILURE on K9–K11 and P:code resource-terminals/human-gates; Instance map covers K1–K12 with file:line / NONE; KERNEL block is line+pins only (no verdict tokens). Matches ordinary-class VERIFY intent in the queue row.
- Why zero subjects (PR body + filing): readiness showed P2 (needs `OBSERVE_CDP_URL` managed browser), R-LIVE (RATIFY), DOC-V75-TRANSPORT (bus); P8 / ph-P2-R / ph-P3 NEEDS_REDESIGN with #91/#94 blocked earlier by Actions billing/0-steps. No subject manufactured — honest blocked-seam filing.
- Supersedes #65 r2 staging (now closed). Claims discipline looks tighter than prior dogfood attempts (#75/#79 adj-close history): command-stamped observations, owned-path greps excluded, no owned-path OIDs/hashes.
- Branch `feat/KERNEL-DOGFOOD-R4` → `master`; mergeable=true; mergeable_state=unstable (checks pending); not draft; tip `c0c4d91a`; base `d9455f0d`.
- Cursor Cloud Agents not used; review via `gh` (docs/doctrine staging, not product code). No GitHub PR comment posted.
- Doctrine bus: first publish on `master` at `cos-feedback/adversarialllm/pr-95.md` (prior file 404).