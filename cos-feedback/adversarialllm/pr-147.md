schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 147
head: 817fb091453c56ba47c2cf8218ab8936801433a5
reviewed_utc: 2026-09-25T21:34:35Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip CI run 36192203116 — docs drift + factory contracts + typecheck/lint/build/test all QUEUED; MERGEABLE/UNSTABLE; ahead 3 / behind 0 vs master; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for tip CI green on `817fb091`.** Actions run 36192203116 just started after the master merge refresh. Re-check all three required jobs before merge.
- **Bus PR #68 still needs a rebase before any bus merge.** CoS confirmed #68 is OPEN, tip `92c9ec61`, base `88f8e578`, mergeable_state dirty (+170/−0, 3 files). That rebase/merge is outside this row's VERIFY and is not a blocker for merging this evidence PR.

## Notes

- **Scope:** evidence-only delivery of queue row `DOC-V75-TRANSPORT` per controller ledger #130 practice 6. Adds exactly one file: `adversarialllm/evidence/DOC-V75-TRANSPORT.md` (+93/−0). No code, workflow, or queue changes.
- **Tip history (3 commits):** `7acd27b0` initial evidence; `976d88cd` clause-7 attribution fix + inlined `ls-tree` output; `817fb091` merge master `e63e9e19` (base refresh; no content change). This wake is the base-refresh push.
- **CoS cross-checks (data only):** doctrine bus PR #68 title/state/head/base/source blob hashes and D2 merge SHAs (#63 `3cbce71c`, #64 `aa80c08e`, #66 `73fc4191`) match the evidence Identity table. Candidate tip `92c9ec61` is not on bus master (dirty / needs rebase), consistent with VERIFY clause 7 "no push to bus master."
- Clause 4 (byte-level REBASE-NEEDED against `specs/conjugal-approach-a-v7.4.md`) and append-only byte proofs remain transport-session-claimed as the evidence states; not re-derived here.
- No secrets in diff. Review via `gh`. No GitHub PR comment posted. Cursor Cloud Agents not used (not on plan for this account).
