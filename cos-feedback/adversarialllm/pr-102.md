schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 102
head: ff1f14c8dc1dd4fcd904203f715de082880d46ea
reviewed_utc: 2026-09-18T08:56:30Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head ff1f14c8 (Actions run 35326831662); mergeable MERGEABLE / mergeStateStatus UNSTABLE; 0 GitHub reviews on tip.

## Blockers

- **CI: `factory contracts`** — IN_PROGRESS on head `ff1f14c8dc1dd4fcd904203f715de082880d46ea` (Actions run 35326831662). Wait for green before merge; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Dual-family D2 + non-author merge not landed on this tip.** PR body states Claude CLI may be dark (`cross_family: UNAVAILABLE claude <UTC>` + three codex legs, then claude re-review when family returns). Factory policy still needs two independent adversarial legs (one claude, one codex) bound to tip `ff1f14c8`, `factory/review-state.mjs` `evaluateMerge` green, and a fresh non-author controller. Author session (Claude Opus 5) must not merge. This is round 1 of `P2-AMEND-R` (no third round of #101).

## Improvements

- `factory/proposals/P2-HARNESS.md` Run block still uses `pwsh ../scripts/ci.ps1` — confirm cwd (repo-root vs `factory/`); prefer `pwsh scripts/ci.ps1` if root-relative is house style (same note as H1-R / #88 / #91 / #101). Unchanged from the #101 CoS Improvement.
- After CI green, land head-bound dual-family legs on tip `ff1f14c8` before any merge attempt; do not treat local `queue-state.mjs` / `queue-state.test.mjs` proof alone as merge authority.
- Queue hygiene: other open PRs that also edit `factory/queue.jsonl` can still race; if another queue PR lands first, refresh base so `P2.dependsOn` / new rows (`P2-HARNESS` 111, `P2-AMEND` 113 superseded, `P2-AMEND-R` 114) stay coherent. Note order gap: 111 then 113/114 (no 112 after `P2-LAUNCH` withdrawal) — intentional and fine if documented; confirm no consumer assumes contiguous order ids.

## Notes

- Successor to #101 (`adj-close`, unmerged, tip `620d20ef`): bounded material correction after round-cap. **Withdraws `P2-LAUNCH`** (source of terminal codex MUST F-01 + launch-authority SHOULDs); does **not** reopen #101 or grant a third round. Files only offline `P2-HARNESS` (adopts #101 round-2 claude F-01: options failures/drift are STOP defects) + corrected launcher-block + `P2-AMEND`→`P2-AMEND-R` supersession. **Implements nothing.**
- Queue delta vs master `6b504b25`: add `P2-HARNESS` (111, claude, dependsOn P5), `P2-AMEND` (113, supersededBy P2-AMEND-R), `P2-AMEND-R` (114, claude); `P2.dependsOn` becomes `[P5, P2-HARNESS]`. Launch authority stays **open**; five #101 launch findings recorded as inputs to a future high-risk D10 clarification that does not exist yet. After HARNESS delivers, P2 may show READY but remains **unrunnable** until that clarification exists — readiness is not a live dispatch signal.
- Author validation claims: `queue-state.mjs` shows P2-AMEND SUPERSEDED_PENDING waitingFor P2-AMEND-R, P2 WAITING_DEPENDENCY waitingFor P2-HARNESS, nextReady P2-HARNESS; `queue-state.test.mjs` 16/16; zero CR bytes; merge-tree clean vs heads of #99/#100/#98/#97/#94/#90. Local gate said running at open — treat GitHub checks as authority.
- Scope: four paths (+139/−1) — adjudications + proposal + queue only. Outside RESET-PLAN point-8 live-script edit list for *this* PR (proposal only); the future `P2-HARNESS` *implementation* PR that edits `a19-model-discovery-sweep.mjs` stays high-risk class. Not a re-audit of the live sweep or observe-campaign beyond cited facts.
- Outside RESET-PLAN high-risk list for this amend filing itself; still requires fresh non-author dual-family review per factory D2.