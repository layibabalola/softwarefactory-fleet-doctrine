schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 102
head: 19ad5e661ef5aba363178c8036998128928d714b
reviewed_utc: 2026-09-18T10:30:11Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head 19ad5e66 (Actions run 35334939441); mergeable MERGEABLE / mergeStateStatus UNSTABLE; 0 GitHub reviews on tip; factory round-1 evaluateMerge refused UNRESOLVED_FINDINGS on prior tip fd3d531f.

## Blockers

- **CI: `factory contracts`** — IN_PROGRESS on head `19ad5e661ef5aba363178c8036998128928d714b` (Actions run 35334939441). Wait for green before merge; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Round-2 dual-family / degraded D2 not landed on this tip.** Round 1 on `fd3d531f` (cross_family UNAVAILABLE claude 2026-09-18T10:19:14Z; three codex surfaces) ended `evaluateMerge` `{"mergeable":false,"reason":"UNRESOLVED_FINDINGS"}` after harness-spec surface CHANGES_REQUESTED. Tip `19ad5e66` adopts both MUSTs; need fresh legs bound to `19ad5e66` (degraded three-codex path while Claude CLI dark, then claude re-review when family returns), `evaluateMerge` green, and a fresh non-author controller. Author session (Claude Opus 5) must not merge. Round 2 is the last evaluable round of `P2-AMEND-R`.

## Improvements

- `factory/proposals/P2-HARNESS.md` Run block still uses `pwsh ../scripts/ci.ps1` — confirm cwd (repo-root vs `factory/`); prefer `pwsh scripts/ci.ps1` if root-relative is house style (same note as H1-R / #88 / #91 / #101). Unchanged from prior CoS Improvement.
- After CI green, land head-bound degraded/dual-family legs on tip `19ad5e66` before any merge attempt; do not treat local `queue-state.mjs` / round-1 receipts alone as merge authority for this tip.
- Queue hygiene: other open PRs that also edit `factory/queue.jsonl` can still race; if another queue PR lands first, refresh base so `P2.dependsOn` / new rows (`P2-HARNESS` 111, `P2-D10-LAUNCH` 112, `P2-AMEND` 113 superseded, `P2-AMEND-R` 114) stay coherent.

## Notes

- **Push delta vs prior CoS head `fd3d531f`:** commit `19ad5e66` is factory round-2 adoption of codex r1 harness-spec MUSTs. `P2-HARNESS.md`: thrown/missing/malformed/not-ok readiness → `readiness-failed` defect (STOP), never unavailability; `authenticated:true` requires exact positive reason `authenticated_composer_and_account_identity`; contradictory/non-boolean shapes → `readiness-defect`; attach failure names all seven providers `cdp-unreachable` in readiness table + `unavailableProviders`; offline tests updated to match. `P2-launcher-block-20260918.md`: D10 "No" outcome closes A19 in docs/28 as unavailable, never live-verified, with full seven-provider packet per §4 / D9.
- Round-1 ledger (controller comment on #102): surfaces queue-mechanics + round-cap APPROVE; harness-spec CHANGES_REQUESTED F-01/F-02 MUST; prior tip CI green + local gate green. This tip invalidates those legs for merge — re-run on `19ad5e66`.
- **Prior CoS Improvements:** order-gap (111–114) remains closed; pwsh path / queue-race notes unchanged (not addressed by this push).
- Successor to #101 (`adj-close`, unmerged): bounded material correction after round-cap. **Withdraws `P2-LAUNCH`**; does **not** reopen #101 or grant a third round of #101. Files offline `P2-HARNESS` + corrected launcher-block + `P2-AMEND`→`P2-AMEND-R` + **`P2-D10-LAUNCH`**. **Implements nothing.**
- Launch authority stays **open** as `P2-D10-LAUNCH`. After HARNESS delivers, P2 still waits on D10-LAUNCH.
- Scope: four paths (adjudications + proposal + queue only). Outside RESET-PLAN point-8 live-script edit list for *this* PR; future `P2-HARNESS` *implementation* PR stays high-risk class.