schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 102
head: fd3d531fe49cf1231b2f1aaf18f56111e49e7b39
reviewed_utc: 2026-09-18T10:04:12Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head fd3d531f (Actions run 35332662906); mergeable MERGEABLE / mergeStateStatus UNSTABLE; 0 GitHub reviews on tip.

## Blockers

- **CI: `factory contracts`** — IN_PROGRESS on head `fd3d531fe49cf1231b2f1aaf18f56111e49e7b39` (Actions run 35332662906). Wait for green before merge; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Dual-family D2 + non-author merge not landed on this tip.** Factory policy still needs two independent adversarial legs (one claude, one codex) bound to tip `fd3d531f`, `factory/review-state.mjs` `evaluateMerge` green, and a fresh non-author controller. Author session (Claude Opus 5) must not merge. This is round 1 of `P2-AMEND-R` (no third round of #101). If Claude CLI is still dark, carry `cross_family: UNAVAILABLE claude <UTC>` + three codex legs on distinct named surfaces, then claude re-review when the family returns.

## Improvements

- `factory/proposals/P2-HARNESS.md` Run block still uses `pwsh ../scripts/ci.ps1` — confirm cwd (repo-root vs `factory/`); prefer `pwsh scripts/ci.ps1` if root-relative is house style (same note as H1-R / #88 / #91 / #101). Unchanged from the #101 / prior #102 CoS Improvement.
- After CI green, land head-bound dual-family legs on tip `fd3d531f` before any merge attempt; do not treat local `queue-state.mjs` / `queue-state.test.mjs` proof alone as merge authority.
- Queue hygiene: other open PRs that also edit `factory/queue.jsonl` can still race; if another queue PR lands first, refresh base so `P2.dependsOn` / new rows (`P2-HARNESS` 111, `P2-D10-LAUNCH` 112, `P2-AMEND` 113 superseded, `P2-AMEND-R` 114) stay coherent.

## Notes

- **Push delta vs prior CoS head `ff1f14c8`:** commit `fd3d531f` files **`P2-D10-LAUNCH`** (order 112, dependsOn `[P5, H1-R]`, family claude) as an explicit bounded queue row for the D10 launch-authority question, and adds it to `P2.dependsOn` (`[P5, P2-HARNESS, P2-D10-LAUNCH]`). That replaces the prior record-only "do not dispatch READY P2" sentence with evaluator-enforced WAITING_DEPENDENCY. Also: thrown options message → `options-failed` (defect, not readiness-failed); tab host matching requires an offline test; round-cap receipt hashes labeled **controller-local, not repository evidence**.
- **Prior CoS Improvement addressed:** order gap after `P2-LAUNCH` withdrawal (111 then 113/114) is closed — `P2-D10-LAUNCH` occupies 112. Continuous 111–114.
- Successor to #101 (`adj-close`, unmerged, tip `620d20ef`): bounded material correction after round-cap. **Withdraws `P2-LAUNCH`** (source of terminal codex MUST F-01 + launch-authority SHOULDs); does **not** reopen #101 or grant a third round. Files offline `P2-HARNESS` (adopts #101 round-2 claude F-01: options failures/drift are STOP defects) + corrected launcher-block + `P2-AMEND`→`P2-AMEND-R` supersession + **`P2-D10-LAUNCH`**. **Implements nothing.**
- Launch authority stays **open** as the question `P2-D10-LAUNCH` asks (high-risk D10 text change; either Yes-with-bounds → separate launcher row, or No → P2 READY and attach-only attempts end in harness `cdp-unreachable`). After HARNESS delivers, P2 still waits on D10-LAUNCH — readiness of HARNESS alone is not a live dispatch signal.
- Author validation claims (earlier tip): `queue-state.mjs` shows P2-AMEND SUPERSEDED_PENDING waitingFor P2-AMEND-R, P2 WAITING_DEPENDENCY; `queue-state.test.mjs` 16/16; zero CR bytes; merge-tree clean vs heads of #99/#100/#98/#97/#94/#90. Re-validate against tip `fd3d531f` after CI; treat GitHub checks as authority.
- Scope: four paths (adjudications + proposal + queue only). Outside RESET-PLAN point-8 live-script edit list for *this* PR (proposal only); the future `P2-HARNESS` *implementation* PR that edits `a19-model-discovery-sweep.mjs` stays high-risk class. Outside RESET-PLAN high-risk list for this amend filing itself; still requires fresh non-author dual-family review per factory D2.
