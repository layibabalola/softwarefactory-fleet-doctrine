schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 72
head: 230668e1e02d582072227838c900b8b849d21290
reviewed_utc: 2026-09-15T22:45:09Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35032399927; typecheck/lint/build/test + factory contracts IN_PROGRESS on head 230668e1; author claims exact-head local full CI GREEN exit0 641s vs base afd6b864

## Blockers

- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on head `230668e1e02d582072227838c900b8b849d21290` (Actions run 35032399927). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — IN_PROGRESS on the same run. Same wait/fix rule.
- **Process: independent dual-family D2 on this head** — 0 GitHub reviews yet; PR body requires fresh Sol/Codex and Sonnet/Claude reviews plus the three hosted current-head checks before merge. Land those after hosted CI settles.

## Improvements

- Circular type edge continues from P2.5a: `send-phase-types.ts` imports `DispatchOneDeps` / `DispatchOneFileUploadOptions` from `dispatch-one.ts` while `dispatch-one.ts` imports `SendPhaseContext` from `send-phase-types.ts`. Prefer a tiny shared deps/types module so further extracts do not deepen the cycle.
- No `adversarialllm/evidence/P2.5b-*.md` on this tip while `28_IMPLEMENTATION_PROGRESS.md` still marks the row as a source candidate. Add a short evidence packet (head/base, module map, local CI command/exit, dual-family status) matching the P2.5a pattern before calling the row delivered.
- New strategy modules are still large (`send-prompt.ts` ~594, `send-file-upload.ts` ~520, `send-content-script.ts` ~386, `send-cdp.ts` ~365). Optional follow-up: peel outcome-classifier / feed-upload helpers out of `send-prompt.ts` once D2 is green so the sequencer stays a thin loop.

## Notes

- First CoS bus file for this PR. Tip `230668e1` is `P2.5b: refresh generated inventory anchors` after `58fc3da4` (`P2.5b: extract send strategy selection`) on base `afd6b864` (post-#55 master). Owned delta vs base is eleven files (+2234/−1629): new `send-prompt.ts`, `send-preflight.ts`, `send-cdp.ts`, `send-content-script.ts`, `send-file-upload.ts`, `send-phase-types.ts`; `dispatch-one.ts` shrinks by replacing the inline SEND_PROMPT loop with `sendPrompt(sendPhaseContext)`; docs/architecture + progress anchors; +61 lines on `orchestrator-echo.test.ts` binding accepted vs rejected send order.
- Ordering look correct for the claimed contract: `runPreSendProbe` still precedes the send-phase abort fence; `abortBeforePromptTransmission('send phase')` still gates before `executionStartTime`; `sendPrompt` returns `terminalResponse` for abort/quota/exhausted-send paths so the caller never enters WAIT on those; send-acceptance / generation-ack / WAIT / quality remain in `dispatchToOne`.
- New unit coverage pins RESET_SEND_LOCK before SEND_PROMPT, and that rejected sends (`sendAccepted: false`) yield `SEND_REJECTED` without WAIT while accepted sends keep ack-before-WAIT ordering. Existing content-handler and upload suites cited as oracles in the PR body.
- Offline source refactor only; no live-provider activation or certification claimed. Initial Claude packet was invalid (mixed Sonnet/Opus); Codex controller repaired and committed this head — treat prior packet reviews as stale. Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: created on `master` at `cos-feedback/adversarialllm/pr-72.md`.