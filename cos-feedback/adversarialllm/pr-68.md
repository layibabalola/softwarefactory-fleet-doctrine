schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 68
head: a604051c36f1937d6df5f4444d06fecc9bef51fe
reviewed_utc: 2026-09-15T05:18:30Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: docs drift (delta gate) SUCCESS on head a604051c (run 34932134161); factory contracts and typecheck/lint/build/test still pending. Prior tip b6beba59 had local pwsh scripts/ci.ps1 GREEN (746s) plus hosted SUCCESS.

## Blockers

- **Fresh dual-family D2 (point 8 / HUB.md Merge)** — high-risk class (process-authority docs). Round-1 on `b6beba59`: Codex ADOPT (NIT); Claude AMEND MUST (adopting bus autonomy spec by reference imported commit/push/doctrine-write/owner-escalation steps that conflict with this bullet, D12 point 9, and CLAUDE.md three-document authority). Round-2 tip `a604051c` makes the bus spec background only and drops out-of-row RESUME pointers; need one fresh review leg per family on **this tip** before merge. Authoring controller must not turn the merge key. CoS does not staff Sol/Claude D2 legs.
- **Hosted CI not settled on this tip** — factory contracts and typecheck/lint/build/test still pending on `a604051c` (docs drift already green). Wait for green hosted checks (or a posted local `pwsh scripts/ci.ps1` receipt for this exact head) before merge.

## Improvements

- Keep bus autonomy spec as **background only** (blob `4a58049f`); do not re-adopt it by reference. This bullet, D2, and §1.3 govern; copies elsewhere stay non-authoritative.
- Keep RESUME.md derivation pointers out of this row; land them in a later PR that owns `adversarialllm/.claude-state/RESUME.md`.
- HUB.md is now 8150 / 8192 bytes (was 8188 on r1) — better headroom; keep budget margin on any follow-up edit.
- PR title still mentions "RESUME pointers"; retitle when convenient so the open PR matches round-2 scope (`factory/HUB.md` + root `CLAUDE.md` only).

## Notes

- Diff vs base: +3/−0 across `factory/HUB.md` (+2) and `CLAUDE.md` (+1) only. CLAUDE.md is 33 lines (limit 40).
- Round-2 commit `a604051c` on open-reviewed `b6beba59`: Claude r1 MUST and both-leg SHOULDs closed per PR body; no GitHub reviews API entries yet (verdicts arrived as issue comments).
- Merge order note from prior tip: land #67 first, then this PR on a re-derived base.
- Depends on merged #64 (OWNER-20260914-fleet-filing-destination). Bus pin `specs/autonomous-decision-making-with-adversarial-swarms.md` blob `4a58049f3005f147f1b4927251f6facc27896525` remains the background reference only.
- No GitHub PR comment posted by CoS. Review via `gh` only (Cloud Agents not on plan).
