schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 68
head: b6beba590f347726b2da0492b3f6534b73d1f599
reviewed_utc: 2026-09-15T03:57:39Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: All three required checks still pending on head b6beba59 (run 34926903281): factory contracts, typecheck/lint/build/test, docs drift (delta gate). No content blockers.

## Blockers

(none for content) — process-authority docs only (`factory/HUB.md` +2, root `CLAUDE.md` +1, `adversarialllm/.claude-state/RESUME.md` +4). Land when hosted checks go green and high-risk D2 review legs complete.

## Improvements

- High-risk class (HUB.md / CLAUDE.md): under Merge (D2) / operative amendment point 8, require one validated current-head verdict per family before merge; do not merge while any family is dark.
- Author/implementer must not merge: hand `gh pr merge --squash --delete-branch` to a fresh non-author controller (chip naming model and effort; first act a model-floor check), matching the Autonomy bullet this PR itself adds.
- HUB.md is at 8188 / 8192 bytes — leave a few bytes of headroom on any follow-up edit so the budget gate does not flake on line endings.
- Optional: when dual-family legs run, have them explicitly confirm the intentional bus divergence (split decision escalates a tier here, never to the owner) against pinned blob `4a58049f3005f147f1b4927251f6facc27896525`.

## Notes

- Scope matches OWNER-20260914-fleet-filing-destination (#64): Autonomy bullet after Merge (D2); CLAUDE.md one pointer under "How work happens"; RESUME four derivation pointers (live guard vs origin/master, review worktrees as evidence, re-derive doctrine-sync.json, follow-ups in queue rows).
- Bus adopt pin verified: `specs/autonomous-decision-making-with-adversarial-swarms.md` on doctrine master is blob `4a58049f3005f147f1b4927251f6facc27896525`.
- Pre-existing (not introduced): RESUME.md unchanged "Never:" line still trips F1.1 vocabulary check — do not treat as a #68 regression.
- PR claims local `queue-state.test.mjs` 15/15 and `pwsh scripts/ci.ps1` receipt to be posted for this head — confirm receipt comment lands before merge if local gate is still required evidence.
- No GitHub PR comment posted. Review via `gh` only (Cloud Agents not on plan).
