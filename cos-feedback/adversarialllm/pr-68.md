schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 68
head: a6b038a6d0487bb5d4ca198efe47bb890e51c994
reviewed_utc: 2026-09-15T07:13:05Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: docs drift (delta gate) SUCCESS on a6b038a6 (run 34940420918); factory contracts and typecheck/lint/build/test still pending. Prior tip ba5cb37 had local pwsh scripts/ci.ps1 GREEN plus dual-family APPROVE on base 5cb557 (#67); no local receipt yet for this tip on master 73fc419 (#66).

## Blockers

- **Fresh dual-family D2 on this tip** — high-risk class (process-authority docs). Tip `a6b038a6` is ahead of current `master` `73fc419` (#66) with behind_by 0 (prior behind-#66 gap cleared). Round-2 re-validation APPROVE (codex pr68-r2-codex-20260915b, claude pr68-r2-claude-20260915b overall APPROVE with SHOULDs) and earlier `evaluateMerge` `D2_SATISFIED` applied to prior tip `ba5cb37` on base `5cb557`, not to `a6b038a6` / `73fc419`. Re-run one gate + one fresh leg per family on this tip; authoring session does not turn the merge key. CoS does not staff Sol/Claude D2 legs.
- **Hosted CI not settled on this tip** — wait for factory contracts and typecheck/lint/build/test green on `a6b038a6` (or a posted local `pwsh scripts/ci.ps1` receipt for this exact head) before merge. Merge state UNSTABLE while required checks pending.

## Improvements

- Claude r2 SHOULD on prior tip (still text-identical on this tip): clarify HUB Autonomy authority line — replace bare "these documents" / "this bullet, D2 and §1.3 govern" with naming root `CLAUDE.md`, this file, and `RESET-PLAN.md` (and the operative amendment) so readers cannot drop D10/D12.
- Claude r2 SHOULD: clarify "a swarm or dispatcher below its act's tier is refused" vs point 8 / Haiku adjudication swarms — e.g. refuse the dispatcher or the session carrying out the decision below tier, while swarm legs themselves may be any tier.
- PR title still mentions "RESUME pointers"; retitle when convenient to match scope (`factory/HUB.md` + root `CLAUDE.md` only).
- Keep bus autonomy spec as **background only** (blob `4a58049f`); do not re-adopt it by reference.

## Notes

- Diff vs current master: +3/−0 across `factory/HUB.md` (+2) and `CLAUDE.md` (+1) only. HUB.md 8150/8192 bytes; CLAUDE.md 33 lines (limit 40). Autonomy bullet makes bus spec background-only; authority-source rule landed; RESUME out of scope.
- Prior CoS blocker "branch behind master" cleared: compare `master...feat/PROC-AUTONOMY` is ahead 2 / behind 0 (includes #66 F1.4b).
- #67 MERGED earlier; #66 is now on master and in this tip's ancestry.
- Round-1 Claude MUST (bus-spec adoption by reference) and both-leg RESUME/scope SHOULDs remain closed in content. GitHub Reviews API still empty (verdicts as issue comments).
- Depends on merged #64 (OWNER-20260914-fleet-filing-destination). Bus pin `specs/autonomous-decision-making-with-adversarial-swarms.md` blob `4a58049f3005f147f1b4927251f6facc27896525` remains background only.
- No GitHub PR comment posted by CoS. Review via `gh` only (Cloud Agents not on plan). Prior CoS head was `ba5cb37`; verdict unchanged (`not-merge-ready`) — behind-master blocker cleared; D2 + hosted CI remain.