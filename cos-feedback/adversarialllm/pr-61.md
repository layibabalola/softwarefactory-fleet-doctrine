schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 61
head: 9ddffc9d8caf0c0ef4ddd00f7c472c90bd207bea
reviewed_utc: 2026-09-14T05:26:07Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: all 3 hosted jobs pending on Actions run 34809551610 (factory contracts, typecheck/lint/build/test, docs drift); author claims exact local CI GREEN exit0 668s on head 9ddffc9d8caf0c0ef4ddd00f7c472c90bd207bea vs base 9efa9fd7

## Blockers

- **CI: `typecheck / lint / build / test`** — pending on head `9ddffc9d8caf0c0ef4ddd00f7c472c90bd207bea` (Actions run 34809551610). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — still pending on the same run (new adjudication-hash + line-ending + harness-lint contract coverage lands here). Same wait/fix rule.
- **CI: `docs drift (delta gate)`** — still pending on the same run.
- **Process: independent D2 review on this head** — 0 GitHub reviews yet; PR body requires native Sol/Codex + independent Claude review on the *current* head before merge. Land those (or an explicit owner waiver outside this file) after hosted CI settles.

## Improvements

- `.github/workflows/ci.yml` `Adjudication hashes and regression tests` uses PowerShell `$LASTEXITCODE` without an explicit `shell: pwsh` (unlike the sibling Line-ending delta gate). Pin `shell: pwsh` so a future runner/OS change cannot silently switch to bash.
- `factory/adjudication-hashes.mjs` `validateAdjudications` hardcodes the two final ADJ records (`rguard` / `rbudget`). Add a one-line comment that new *final* adjudications must be appended to that pair list (historical round hashes stay untouched by design).
- Hosted `Harness lint` globs `scripts/**/*.mjs` under `defaults.run.working-directory: adversarialllm` — correct for product harness scripts, but root `scripts/*.mjs` (e.g. never-authorized guard) stay outside this gate. If CTRL-2e intent was every harness `*.mjs`, either document the intentional product-tree scope or add a root-scripts lint step.

## Notes

- Scope: +189/−3 across 8 files. Adds eval-family ESLint enforcement (`no-eval` / `no-implied-eval` / `no-new-func`) for `src/**/*.{ts,tsx}` and `scripts/**/*.mjs` with timer/global declarations so string-timer implied-eval is no longer missed; line-ending CR-only delta gate (`git diff --numstat` vs `--ignore-cr-at-eol`); canonical LF/CRLF-normalized `subjectFinal` hash validation for the two final adjudications (stale digests corrected); CI failure logs retain full output and surface early TAP `not ok` lines.
- Contract coverage looks serious: real ESLint negative/safe controls on both file groups, fabricated CR-only commit rejection, malformed-hash fail-closed, harness-lint fail-stop before vitest, early-TAP visibility + full failure log retention, hosted workflow string mirrors for the new gates.
- `LineEndingsOnly` early-exits before the `Global\AdvLLM-ci-gate` mutex, so the in-gate nested `ci.ps1 -LineEndingsOnly` spawn is not a mutex re-entry footgun.
- Cursor Cloud Agents not used (plan unavailable; review via `gh`). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/adversarialllm/pr-61.md`.