schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 61
head: 7c89049b622ac8cde58797d192d98ec074fda4b9
reviewed_utc: 2026-09-15T18:49:05Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35009666310; typecheck/lint/build/test + factory contracts IN_PROGRESS on head 7c89049b; author claims exact-head local full CI GREEN exit0 829s vs base 9ac77808

## Blockers

- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on head `7c89049b622ac8cde58797d192d98ec074fda4b9` (Actions run 35009666310). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — IN_PROGRESS on the same run (adjudication-hash + line-ending + harness-lint + lease-isolation contract coverage). Same wait/fix rule.
- **Process: independent D2 review on this head** — 0 GitHub reviews yet; PR body requires fresh high-risk Sol/Codex + Sonnet/Claude reviews on the *current* head/base before merge (earlier head approvals are stale). Land those after hosted CI settles.

## Improvements

- `.github/workflows/ci.yml` `Adjudication hashes and regression tests` still uses PowerShell `$LASTEXITCODE` without an explicit `shell: pwsh` (unlike the sibling Line-ending delta gate). Pin `shell: pwsh` so a future runner/OS change cannot silently switch to bash. (Prior CoS Improvement — not yet addressed.)
- `factory/adjudication-hashes.mjs` `validateAdjudications` still hardcodes the two final ADJ records (`rguard` / `rbudget`). Add a one-line comment that new *final* adjudications must be appended to that pair list (historical round hashes stay untouched by design). (Prior CoS Improvement — not yet addressed.)
- Hosted `Harness lint` still globs `scripts/**/*.mjs` under `defaults.run.working-directory: adversarialllm` — correct for product harness scripts, but root `scripts/*.mjs` (e.g. never-authorized guard) stay outside this gate. If CTRL-2e intent was every harness `*.mjs`, either document the intentional product-tree scope or add a root-scripts lint step. (Prior CoS Improvement — not yet addressed.)

## Notes

- Push since prior CoS (`9ddffc9d`): merge `origin/master` (PR70 complete CI run lease), then `fix(ci): run line-ending check inside complete gate lease` + `test(ci): isolate real lease fixtures under complete gate`. Net +236/−3 across 8 files on tip `7c89049b`.
- Line-ending gate now calls `Test-LineEndingDelta` in-process under the whole-run lease (avoids nested `ci.ps1 -LineEndingsOnly` waiting on its own lease); hosted standalone `-LineEndingsOnly` entrypoint retains lease acquisition. Contract fixtures rebind copied source to HEAD and isolate only named mutexes so nested-path + CR-only/LF checks exercise the real gate while the parent owns the lease.
- CTRL-2e substance unchanged and still looks solid: eval-family ESLint (`no-eval` / `no-implied-eval` / `no-new-func`) with timer/global decls; CR-only delta gate; canonical LF/CRLF `subjectFinal` hashes (stale digests corrected); full failure-log retention + early TAP `not ok` surfacing; hosted workflow mirrors for new gates.
- Dual-family D2 still required per PR body; no GitHub reviews on this tip yet. Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-61.md`.
