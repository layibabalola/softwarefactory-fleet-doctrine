schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 91
head: 861ce39d6b24906187468d505392615057455db0
reviewed_utc: 2026-09-16T19:14:11Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift (delta gate) PASS; factory contracts + typecheck/lint/build/test IN_PROGRESS on head 861ce39d (Actions run 35139146755); author claims local full CI GREEN (~846s) on this SHA vs base c9a08261

## Blockers

- **CI: `factory contracts`** — IN_PROGRESS on head `861ce39d6b24906187468d505392615057455db0` (Actions run 35139146755). Wait for green before merge; if red, fix and re-push.
- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on the same run. Same wait/fix rule.

## Improvements

- After hosted CI settles, land two independent adversarial legs (Sol/Codex + Claude) on tip `861ce39d`, then merge by a fresh non-author controller. Author is Claude Opus 5 (PR 81 round-2 controller); per PR body this session authored the head and must not merge it. 0 GitHub reviews on tip yet.
- `factory/proposals/P8-R.md` Run block uses `pwsh ../scripts/ci.ps1` — confirm that relative path matches the intended cwd for implementers (repo-root vs `factory/`); if root-relative is house style, prefer `pwsh scripts/ci.ps1` for copy-paste safety (same note as H1-R / #88).
- Spot-check that `P8-R` order 221 / `dependsOn: ["P5"]` / `family: codex` and `P8.supersededBy: ["P8-R"]` are the only queue mutations before merge (no accidental sibling-row edits).

## Notes

- First CoS bus file for this PR. Tip `861ce39d` on `feat/P8-AMEND` → `master` (`c9a08261`): 3 files, +59/−1 — new `factory/adjudications/P8-pr81-round-cap.md`, new `factory/proposals/P8-R.md`, and `factory/queue.jsonl` (`P8.supersededBy: ["P8-R"]` + new `P8-R` row order 221).
- Scope is proposal + adjudication only: implements nothing, does not reopen #81, does not grant a third round, does not sign/publish/activate. P8 stays unmet until a future P8-R implementation delivers. Matches #83 (ph-M2-R) / #88 (H1-R) amend pattern.
- Content read: adjudication binds #81 round heads/bases, receipt hashes, green controller gate, `adj-close` ledger, and explains `P8-R2-LICENSE-DOC-CONFLICT` plus dual-family `NON_AUTHOR_APPROVAL_REQUIRED`. P8-R acceptance 1–8 preserves original P8 obligations, adds whole-tree license-doc consistency, chart-doc SHOULD residue, and single-family authorship per head. Owner veto on LICENSE copyright-holder line (individual vs company) remains unresolved and correctly non-agent. Not a re-audit of every cited receipt blob byte.
- Related: #81 closed unmerged at two-round cap (`adj-close`, head `62d470ab…`). Branch `codex/P8-nonlicense-e1b12b8a` preserved per adjudication.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: created on `master` at `cos-feedback/adversarialllm/pr-91.md`.