schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 91
head: f5873af672026327f14e81226488ec2e7edf9e41
reviewed_utc: 2026-09-17T05:41:51Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: all three named checks FAILED in ~2–3s with empty steps and BlobNotFound logs on tip f5873af (Actions run 35186665284) — looks like infra/cancel flake, not a real stage fail; prior tip 9b03e591 was fully green (run 35163681781). Re-run CI on this tip before merge.

## Blockers

- **CI flake on tip `f5873af672026327f14e81226488ec2e7edf9e41`:** `factory contracts`, `docs drift (delta gate)`, and `typecheck / lint / build / test` all concluded FAILURE in 2–3s with no step output (run 35186665284). Re-run that workflow (or push a no-op) until named checks are green on this SHA. Do not treat the red as a product defect until a real stage log exists.
- **Round-2 adversarial reviews not landed yet.** Fresh non-author controller must run one head-bound local gate plus one fresh leg per family on this tip, then `evaluateMerge` with `authors.families: ["claude"]`. Round-1 controller authored the r1 legs and transported this head — must not review or merge it. This is round 2 of 2 (last evaluable round).

## Improvements

- `factory/proposals/P8-R.md` Run block still uses `pwsh ../scripts/ci.ps1` — confirm cwd (repo-root vs `factory/`); prefer `pwsh scripts/ci.ps1` if root-relative is house style (same note as H1-R / #88). Unchanged across CoS passes.
- Queue conflict watch: open #94 (ph-P2-R-AMEND) also edits `factory/queue.jsonl`. Whichever of #91 and #94 merges second needs a plain base update.
- After CI green + both round-2 legs APPROVE with zero MUST, merge by a controller that authored neither this head nor the round-2 legs.

## Notes

- Push is round-2 repair (fast-forward from `9b03e591`): restores unsigned native package readiness into P8-R (MUST `P8R-NATIVE-PACKAGE-READINESS-ORPHANED`), adds ph-P2-R ownership guard with comment-only exceptions, replaces fixed chart-doc anchors with a required search, and adds a source-link search with named exclusions. Diff vs master `d9455f0d` remains exactly the three factory paths (+ahead 4 / behind 0).
- Round-1 on tip `9b03e591`: Claude CHANGES_REQUESTED (MUST + 2 SHOULD); Codex APPROVE (NIT); `evaluateMerge` → `UNRESOLVED_FINDINGS`. Local gate was GREEN (~957s).
- Scope still proposal + adjudication + queue supersede only; implements nothing; does not reopen #81; does not grant a third round. Owner veto on LICENSE copyright-holder line remains unresolved.
- Prior CoS Improvement on adversarial legs: round-1 completed (HOLD); round-2 head now awaits a fresh controller. `../scripts` path Improvement not addressed.
- Cursor Cloud Agents not used; review via `gh`. No GitHub PR comment posted.
- Doctrine bus: refreshed on `master` at `cos-feedback/adversarialllm/pr-91.md` for new head.