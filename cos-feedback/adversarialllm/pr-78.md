schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 78
head: 6fa200c0218d78437f3c11152b391115b99899ed
reviewed_utc: 2026-09-16T03:43:14Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: docs drift PASS (~27s) on run 35052745368; typecheck/lint/build/test + factory contracts PENDING/IN_PROGRESS on head 6fa200c0; memory-docs rewrite of adj-closed #76 (+133/0, 7 files)

## Blockers

- **CI: `typecheck / lint / build / test`** — PENDING/IN_PROGRESS on head `6fa200c0218d78437f3c11152b391115b99899ed` (Actions run 35052745368). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — PENDING/IN_PROGRESS on the same run. Same wait/fix rule.

## Improvements

- `adversarialllm/.claude-state/memory/project/kernel-adoption-and-instance-layering.md` still uses soft imperatives ("a reader should re-derive", "do not assume a record exists", "do not treat this note as evidence"). Round-2 attack surface for AUTH-KERNEL-MEMORY was "any sentence that tells a future session what to do." Prefer pure observation wording (e.g. "a reader who relies on this without re-deriving from the tree is guessing") so the entry matches its own "issues no instructions" claim.
- Index checker `.factory-local/check-memory-indexes.mjs` is gitignored runtime tooling — not in the PR tree. CoS could not independently re-run the "51 files / 0 orphans" claim from this tip; keep the checker local and treat the reported negative run as author evidence only.

## Notes

- Replacement for adj-closed #76 (retained branch `feat/MEM-KERNEL-MECH` @ `19d00f3d`). Ordinary / rowless: memory documents only; no script in tree; no queue row.
- Spot-check vs #76 finding ids on head `6fa200c0218d78437f3c11152b391115b99899ed`: (1) CLAIM-STALE-BASE — old "every recorded verdict void" absent from body, frontmatter `description`, MEMORY one-liner, and feedback INDEX one-liner; entry states `evaluateMerge` keeps only receipts with `attempt.base ===` called base (matches `factory/review-state.mjs` filter at lines ~83–84) plus CLEAN/DIRTY orthogonality. (2) AUTH-KERNEL-MEMORY — opens with explicit no-authority / no-instructions; no K1–K12 obligation language; #75 ADOPT noted as unlanded fact. Soft residual imperatives listed under Improvements. (3) INDEX-ORPHAN — all four new slugs linked from both `MEMORY.md` and per-type `INDEX.md` (feedback ×3, project ×1). Unchallenged entries `guard-predicate-on-absent-field` and `receipts-are-append-only-and-truth-independent` present.
- Process (not CoS merge blocker): Claude co-authored; PR body requires non-author APPROVE and a fresh non-author controller. 0 GitHub reviews. mergeable=MERGEABLE; mergeStateStatus=UNSTABLE while CI runs.
- Cursor Cloud Agents not used (memory-docs only); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/adversarialllm/pr-78.md`.