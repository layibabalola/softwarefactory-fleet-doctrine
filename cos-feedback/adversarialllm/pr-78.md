schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 78
head: 181981bb370502ab7e4225b447786323877b52f7
reviewed_utc: 2026-09-16T06:30:45Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35063957720; typecheck/lint/build/test + factory contracts IN_PROGRESS on head 181981bb; prior tip cacccb1c had same CI picture (void for this head); push is master merge-forward only (P4 #51), memory docs byte-identical to cacccb1c

## Blockers

- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on head `181981bb370502ab7e4225b447786323877b52f7` (Actions run 35063957720). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — IN_PROGRESS on the same run. Same wait/fix rule.

## Improvements

- Index checker `.factory-local/check-memory-indexes.mjs` remains untracked runtime tooling (author correctly withholds it as merge evidence). Property is still hand-checkable: every memory file linked from both `MEMORY.md` and its per-type `INDEX.md`. CoS spot-checked the four new slugs on prior tip; memory tree unchanged on this tip.
- Residual soft instruction in `kernel-adoption-and-instance-layering.md`: "do not restate the contents of a document a reader of this repository cannot open" and "so read it on the bus" are still directives, though unverifiable kernel citations and prior soft-imperatives are gone. Optional polish only if round-2 re-attacks AUTH wording. Unchanged vs prior CoS tip.

## Notes

- Delta vs prior CoS head `cacccb1c9842e407b85b296adb02f3898e69df56`: merge commit `181981bb` — `Merge origin/master (e1b12b8a) into feat/MEM-MECH-R2: refresh base before legs`. Brings P4 #51 (`e1b12b8a`) onto the branch. Zero changes under `adversarialllm/.claude-state/` vs prior tip.
- Round-1 memory repairs (CLAIM-STALE-BASE / CLAIM-LOCAL-GATE / AUTH-KERNEL-MEMORY) still stand as reviewed on `cacccb1c`; not re-audited line-by-line on this merge-only tip. Spot-check of `factory/review-state.mjs` claim still applies (module unchanged by this merge for that path).
- Replacement for adj-closed #76. Ordinary / rowless: memory documents only. Unchallenged entries `guard-predicate-on-absent-field` and `receipts-are-append-only-and-truth-independent` still present.
- Process (not CoS merge blocker): Claude co-authored; PR body requires non-author APPROVE and a fresh non-author controller. 0 GitHub reviews. mergeable=MERGEABLE; mergeStateStatus=CLEAN while non-docs CI still runs.
- Cursor Cloud Agents not used (merge-forward / memory-docs scope); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-78.md`.