schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 78
head: cacccb1c9842e407b85b296adb02f3898e69df56
reviewed_utc: 2026-09-16T06:24:46Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: docs drift PASS on run 35063512616; typecheck/lint/build/test + factory contracts IN_PROGRESS on head cacccb1c; prior tip 6fa200c0 all-green on run 35052745368 (void for this head); round-1 memory rewrite (+29/-27 across 5 files)

## Blockers

- **CI: `typecheck / lint / build / test`** — IN_PROGRESS on head `cacccb1c9842e407b85b296adb02f3898e69df56` (Actions run 35063512616). Wait for green before merge; if red, fix and re-push.
- **CI: `factory contracts`** — IN_PROGRESS on the same run. Same wait/fix rule.

## Improvements

- Index checker `.factory-local/check-memory-indexes.mjs` remains untracked runtime tooling (author now correctly withholds it as merge evidence). Property is still hand-checkable: every memory file linked from both `MEMORY.md` and its per-type `INDEX.md`. CoS spot-checked the four new slugs; did not re-enumerate all 51 files.
- Residual soft instruction in `kernel-adoption-and-instance-layering.md`: "do not restate the contents of a document a reader of this repository cannot open" and "so read it on the bus" are still directives, though the unverifiable kernel citations and prior CoS soft-imperatives ("should re-derive" / "do not assume" / "do not treat this note as evidence") are gone. Optional polish only if round-2 re-attacks AUTH wording.

## Notes

- Delta vs prior CoS head `6fa200c0218d78437f3c11152b391115b99899ed`: commit `cacccb1c` — round-1 repair for Codex MUST `CLAIM-STALE-BASE` + `AUTH-KERNEL-MEMORY` and Claude MUST `CLAIM-LOCAL-GATE-NOT-BASE-KEYED` (plus two SHOULDs).
- **CLAIM-STALE-BASE / CLAIM-LOCAL-GATE:** prior tip falsely claimed a local gate result "decays the same way" as receipts. Corrected: receipts filtered on head+base (`evaluateMerge` / legacy path lines ~59, ~84); local gate checked on head alone (`localGate.head !== head`, line 48) with base never consulted. Spot-checked against `factory/review-state.mjs` on this tip — claim matches. Index one-liners and MEMORY summary updated to state the asymmetry.
- **AUTH-KERNEL-MEMORY:** rewritten to in-tree facts only (HUB + RESET-PLAN + CLAUDE.md; no kernel spec/ADOPT in tree; #75 and #79 adj-closed). Unverifiable `specs/fleet-factory-kernel.md` citations removed. Prior CoS Improvement on soft imperatives: addressed (those three phrases gone).
- Replacement for adj-closed #76 (retained branch `feat/MEM-KERNEL-MECH` @ `19d00f3d`). Ordinary / rowless: memory documents only; no script in tree; no queue row. Unchallenged entries `guard-predicate-on-absent-field` and `receipts-are-append-only-and-truth-independent` still present.
- Process (not CoS merge blocker): Claude co-authored; PR body requires non-author APPROVE and a fresh non-author controller. 0 GitHub reviews on tip. mergeable=MERGEABLE; mergeStateStatus=UNSTABLE while CI runs. Round 1 of two per author.
- Cursor Cloud Agents not used (memory-docs only); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/adversarialllm/pr-78.md`.