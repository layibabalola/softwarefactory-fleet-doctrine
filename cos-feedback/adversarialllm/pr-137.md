schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 137
head: 0057c1b3f67723d291db3542de2d1fa21802e00f
reviewed_utc: 2026-09-25T04:25:18Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip CI run 36094294255 — typecheck / lint / build / test IN_PROGRESS on ultra-magnus-advllm; factory contracts + docs drift QUEUED (single-runner serialize); MERGEABLE/UNSTABLE; 0 GitHub reviews; prior tip 7ed788fb run 36092588546 FAILED Docs constants up to date after factory+docs-drift SUCCESS

## Blockers

- **`factory/queue.jsonl` order 321 still collides with open #136.** Both heads still carry `"order":321` (#136 `MEM-R4`, #137 `CI-SELF-HOSTED-LINUX`); master has none. Round-3 tip `0057c1b3` did not re-pick the order. Whichever merges first "wins"; the other must rebase and take a free order before merge. Re-scan open heads and pick an unused order on this branch before merge.

## Improvements

- **Wait for tip CI green on `0057c1b3` after the round-2 red.** Prior tip `7ed788fb` run 36092588546 failed the `ci` job at **Docs constants up to date** (factory contracts + docs drift SUCCESS). Round 3 reverts the LF checkout pin that could not work on a reused self-hosted workspace (`checkout --force` only rewrites changed files, so a first CRLF checkout sticks). Tip run 36094294255 just started (typecheck IN_PROGRESS; siblings QUEUED). Do not merge until all three jobs succeed on the self-hosted runner (VERIFY (6)).
- **High-risk `.github/workflows` change — D2 review legs.** Same class as #128 (CI-SELF-HOSTED-WIN). One review leg per family; author does not merge. Ensure non-author controller legs before merge.
- **Watch whether Docs constants still fails without the LF pin.** If the tip run fails the same step, the fix must change the check (or normalize EOL in that script), not re-add `GIT_CONFIG` autocrlf=false — that approach was proven ineffective on ultra-magnus. Vitest 5000 ms default stays out of scope; file a separate row if timeouts appear under load.

## Notes

- **Round 3 vs prior CoS head `7ed788fb`:** +1 commit `0057c1b3` — Revert "ci: pin an LF checkout for the ci job (core.autocrlf=false via git env config)" (−7 ci.yml, −2 contract asserts); queue VERIFY (3) drops the autocrlf pin clause (back to bash-only). Addresses the workspace-reuse reason the pin could not work. Order remains 321 (blocker unchanged). Verdict unchanged `not-merge-ready`.
- **Scope (PR vs master):** +19/−6 across 3 files — `.github/workflows/ci.yml` (ci + docs-drift `runs-on` → `CI_RUNS_ON` with `ubuntu-latest` fallback; conditional playwright `--with-deps` on Linux only; docs-drift step `shell: bash`; no LF checkout env), `factory/ci-contract.test.mjs` (exact-line pins, `CI_RUNS_ON` count 2→6, conditional `--with-deps` + bash pin; no assertion removed vs master), `factory/queue.jsonl` append `CI-SELF-HOSTED-LINUX` order 321. Diff matches claimed ownership and VERIFY (1)–(5) as rewritten.
- **Self-hosted routing already live:** repo var `CI_RUNS_ON` set; tip jobs target that label set. Continues #128 (factory-contracts only) by moving remaining hosted Linux spend (ci + docs-drift) onto the same Windows self-hosted runner.
- **Serialize risk (acknowledged):** one runner instance → three jobs queue instead of parallel; concurrent vitest on one host collides on shared temp. Safe default.
- **#136 premise drift:** open #136 still documents ci/docs-drift as GitHub-hosted; this PR (if merged) updates that premise. Coordinate merge order with the order-321 unblock.
- No secrets/credentials in diff. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.