schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 137
head: ecd8c926f64e3e7e978ba1648e7407927c8fa77b
reviewed_utc: 2026-09-25T03:26:43Z
event: pr-opened
verdict: not-merge-ready
ci_summary: tip CI run 36090296447 — docs drift SUCCESS on ultra-magnus-advllm (self-hosted Windows via CI_RUNS_ON); factory contracts IN_PROGRESS on same runner; typecheck / lint / build / test QUEUED (single-runner serialize); MERGEABLE/UNSTABLE; 0 GitHub reviews

## Blockers

- **`factory/queue.jsonl` order 321 collides with open #136.** Both heads carry `"order":321` (#136 `MEM-R4`, #137 `CI-SELF-HOSTED-LINUX`); master has none. Whichever merges first "wins"; the other must rebase and take a free order before merge. #137 VERIFY does not restate uniqueness, but fleet queue hygiene (and #136 VERIFY clause 5) requires unique orders across master and every open PR branch. Re-pick an unused order on this branch (and re-scan open heads) before merge.

## Improvements

- **Wait for tip CI green on `ecd8c926`.** Run 36090296447 still has factory contracts IN_PROGRESS and typecheck QUEUED on `ultra-magnus-advllm`. PR VERIFY (6) requires all three jobs green on the self-hosted runner. Do not merge until they succeed; if typecheck stays queued behind factory, that is the expected single-instance serialize (see Notes), not a missing runner.
- **High-risk `.github/workflows` change — D2 review legs.** Same class as #128 (CI-SELF-HOSTED-WIN). One review leg per family; author does not merge. Ensure non-author controller legs before merge.
- **Vitest timeout follow-up stays out of scope (good), but watch the tip run.** If typecheck fails on default 5000 ms timeouts under ultra-magnus load, file a separate row rather than widening budgets in this PR.

## Notes

- **Scope (PR vs master):** +19/−6 across 3 files — `.github/workflows/ci.yml` (ci + docs-drift `runs-on` → `CI_RUNS_ON` with `ubuntu-latest` fallback; conditional playwright `--with-deps` on Linux only; docs-drift step `shell: bash`), `factory/ci-contract.test.mjs` (exact-line pins, `CI_RUNS_ON` count 2→6, conditional `--with-deps` + bash pin assertions; no assertion removed), `factory/queue.jsonl` append `CI-SELF-HOSTED-LINUX` order 321. Diff matches claimed ownership and VERIFY (1)–(5).
- **Self-hosted routing already live:** repo var `CI_RUNS_ON` = `["self-hosted","Windows","X64","ultra-magnus"]`. Tip jobs label that set; docs-drift completed SUCCESS on `ultra-magnus-advllm` in ~24s — evidence the bash pin and Windows self-hosted path work for that job.
- **Continues #128:** CI-SELF-HOSTED-WIN routed only `factory-contracts`; this row moves the remaining hosted Linux spend (ci + docs-drift) onto the same Windows self-hosted runner, relying on `scripts/ci.ps1` already exercising every ci step on Windows locally. Fallback when the var is unset/empty keeps hosted ubuntu-latest.
- **Serialize risk (acknowledged):** one runner instance → three jobs queue instead of parallel; concurrent vitest on one host collides on shared temp (`Global\AdvLLM-ci-gate`). Safe default; second instance is a follow-up.
- **#136 premise drift:** open #136 documents ci/docs-drift as still GitHub-hosted; this PR (if merged) updates that premise. Coordinate merge order with the order-321 unblock.
- No secrets/credentials in diff. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.