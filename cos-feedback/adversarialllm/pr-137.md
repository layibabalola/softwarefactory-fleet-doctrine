schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 137
head: 7ed788fbc363129220279ce64bc1e847a6d7ef5a
reviewed_utc: 2026-09-25T04:00:26Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip CI run 36092588546 — factory contracts IN_PROGRESS on ultra-magnus-advllm (self-hosted Windows via CI_RUNS_ON); typecheck / lint / build / test and docs drift QUEUED (single-runner serialize); MERGEABLE/UNSTABLE; 0 GitHub reviews

## Blockers

- **`factory/queue.jsonl` order 321 still collides with open #136.** Both heads still carry `"order":321` (#136 `MEM-R4`, #137 `CI-SELF-HOSTED-LINUX`); master has none. Round-2 push (`7ed788fb`) did not re-pick the order. Whichever merges first "wins"; the other must rebase and take a free order before merge. Re-scan open heads and pick an unused order on this branch before merge.

## Improvements

- **Wait for tip CI green on `7ed788fb`.** Run 36092588546 still has factory contracts IN_PROGRESS and typecheck + docs-drift QUEUED on `ultra-magnus-advllm`. PR VERIFY (6) requires all three jobs green on the self-hosted runner. Do not merge until they succeed; queued siblings behind factory are expected single-instance serialize, not a missing runner.
- **High-risk `.github/workflows` change — D2 review legs.** Same class as #128 (CI-SELF-HOSTED-WIN). One review leg per family; author does not merge. Ensure non-author controller legs before merge.
- **Vitest timeout follow-up stays out of scope (good), but watch the tip run.** If typecheck fails on default 5000 ms timeouts under ultra-magnus load, file a separate row rather than widening budgets in this PR.

## Notes

- **Round 2 vs prior CoS head `ecd8c926`:** +1 commit `7ed788fb` — pin LF checkout for the `ci` job via git env config (`GIT_CONFIG_COUNT`/`KEY_0`/`VALUE_0` → `core.autocrlf=false`); contract test asserts that env block; queue VERIFY (3) updated to require the pin. Addresses Windows autocrlf breaking `docs:constants` byte compare. Order remains 321 (blocker unchanged). Verdict unchanged `not-merge-ready`.
- **Scope (PR vs master):** +28/−6 across 3 files — `.github/workflows/ci.yml` (ci + docs-drift `runs-on` → `CI_RUNS_ON` with `ubuntu-latest` fallback; conditional playwright `--with-deps` on Linux only; docs-drift step `shell: bash`; ci job LF checkout env), `factory/ci-contract.test.mjs` (exact-line pins, `CI_RUNS_ON` count 2→6, conditional `--with-deps` + bash pin + LF env assertions; no assertion removed), `factory/queue.jsonl` append `CI-SELF-HOSTED-LINUX` order 321. Diff matches claimed ownership and VERIFY (1)–(5).
- **Self-hosted routing already live:** repo var `CI_RUNS_ON` set; tip jobs target that label set. Continues #128 (factory-contracts only) by moving remaining hosted Linux spend (ci + docs-drift) onto the same Windows self-hosted runner.
- **Serialize risk (acknowledged):** one runner instance → three jobs queue instead of parallel; concurrent vitest on one host collides on shared temp. Safe default.
- **#136 premise drift:** open #136 still documents ci/docs-drift as GitHub-hosted; this PR (if merged) updates that premise. Coordinate merge order with the order-321 unblock.
- No secrets/credentials in diff. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
