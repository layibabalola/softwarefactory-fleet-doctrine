schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 137
head: b54c0fe8530dc81673872afe3d2232bdabd9f1d2
reviewed_utc: 2026-09-25T04:27:31Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip SHA b54c0fe has no Actions run yet; superseded run 36094294255 is on intermediate revert 0057c1b3 (typecheck IN_PROGRESS; factory contracts + docs-drift QUEUED on ultra-magnus-advllm). Prior tip 7ed788fb run 36092588546 FAILED docs:constants after factory contracts + docs drift SUCCESS. MERGEABLE/CLEAN; 0 GitHub reviews

## Blockers

- **`factory/queue.jsonl` order 321 still collides with open #136.** Both heads still carry `"order":321` (#136 `MEM-R4`, #137 `CI-SELF-HOSTED-LINUX`); master has none. Round-3 tip (`b54c0fe`) did not re-pick the order. Whichever merges first "wins"; the other must rebase and take a free order before merge. Re-scan open heads and pick an unused order on this branch before merge.

## Improvements

- **Wait for a green tip CI run on `b54c0fe`.** No Actions run is registered for the tip SHA yet; run 36094294255 targets the intermediate revert only. PR VERIFY (6) requires all three jobs green on the self-hosted runner for this head. Do not merge until tip checks succeed.
- **High-risk `.github/workflows` change — D2 review legs.** Same class as #128 (CI-SELF-HOSTED-WIN). One review leg per family; author does not merge. Ensure non-author controller legs before merge.
- **Vitest timeout follow-up stays out of scope (good), but watch the tip run.** If typecheck fails on default 5000 ms timeouts under ultra-magnus load, file a separate row rather than widening budgets in this PR.

## Notes

- **Round 3 vs prior CoS head `7ed788fb`:** +2 commits — (1) revert the LF checkout `GIT_CONFIG`/`core.autocrlf=false` pin (self-hosted workspace reuse + `checkout --force` leaves prior CRLF files untouched, so the pin could not fix docs:constants); (2) `adversarialllm/scripts/gen-constants-table.mjs` `--check` now CRLF-normalizes on-disk bytes before compare (content drift gate, not line-ending gate); queue VERIFY (3) + ownership updated accordingly; contract drops the LF-env asserts. Addresses prior tip failure mode (docs:constants on run 36092588546). Order remains 321 (blocker unchanged). Verdict unchanged `not-merge-ready`.
- **Scope (PR vs master):** +22/−7 across 4 files — `.github/workflows/ci.yml` (ci + docs-drift `runs-on` → `CI_RUNS_ON` with `ubuntu-latest` fallback; conditional playwright `--with-deps` on Linux only; docs-drift `shell: bash`; no LF checkout env), `adversarialllm/scripts/gen-constants-table.mjs` (CRLF normalize in `--check`), `factory/ci-contract.test.mjs` (exact-line pins, `CI_RUNS_ON` count 6, conditional `--with-deps` + bash pin; no assertion removed beyond the reverted LF-env block), `factory/queue.jsonl` append `CI-SELF-HOSTED-LINUX` order 321. Diff matches claimed ownership and VERIFY (1)–(5).
- **Self-hosted routing already live:** repo var `CI_RUNS_ON` set; intended tip jobs target that label set. Continues #128 (factory-contracts only) by moving remaining hosted Linux spend (ci + docs-drift) onto the same Windows self-hosted runner.
- **Serialize risk (acknowledged):** one runner instance → three jobs queue instead of parallel; concurrent vitest on one host collides on shared temp. Safe default.
- **#136 premise drift:** open #136 still documents ci/docs-drift as GitHub-hosted; this PR (if merged) updates that premise. Coordinate merge order with the order-321 unblock.
- No secrets/credentials in diff. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
