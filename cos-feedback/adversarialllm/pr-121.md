schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 121
head: 5f0107e03434741e4b29eac9215e30d66d344cfe
reviewed_utc: 2026-09-22T04:24:02Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip CI run 35686660146 — docs drift PASS; factory contracts and typecheck/lint/build/test IN_PROGRESS; mergeable true / unstable; 0 GitHub reviews

## Blockers

(none)

## Improvements

- **Wait for remaining CI green on `5f0107e0`.** `docs drift (delta gate)` already PASS on Actions run 35686660146; `factory contracts` and `typecheck / lint / build / test` were still IN_PROGRESS at review time. Do not merge on an incomplete check set.
- **Two-leg / D2 still open.** Filing row `CONDUCTOR-PREFLIGHT-TIMEOUT-FILE` is `family: claude` (implementer: claude). Needs the independent second-family leg before merge. GitHub Reviews API returns 0 formal review objects at review time. CoS does not substitute for that adjudication.
- **Local gate pending idle host (author report).** PR body: local `pwsh scripts/ci.ps1` not yet green on this head because another gate was running when the PR opened; author will comment the result. Controllers should still require a green local gate on the merge head per factory practice.

## Notes

- **Queue-only filing (+2/−0).** Diff vs master is only `factory/queue.jsonl`. Appends `CONDUCTOR-PREFLIGHT-TIMEOUT-FILE` (order 270, claude) and `CONDUCTOR-PREFLIGHT-TIMEOUT` (order 271, claude). Body row token is exactly `CONDUCTOR-PREFLIGHT-TIMEOUT-FILE`. Pattern matches GATE-SUPERSEDE-FILE / PH-P3-FILE-R2 / #120: this PR delivers the FILE row so the filed fix row stays unconsumed / READY after merge.
- **Spot-check on tip:** 63 rows, unique ids/orders, max order 271, 0 CR bytes. Open PR tip max orders at review: #120=269, #119=267; #122 tip also shows max 271 (stacked implementation PR opened around the same window — not a collision on this tip's ids).
- **Filed defect (data only):** conductor-preflight `run()` `spawnSync` pwsh timeout 30000 ms; PR 119 local gate run 2 hit `ETIMEDOUT` after ~88 s under host overlap. Spec raises to named `PWSH_SPAWN_TIMEOUT_MS = 60_000` in `factory/conductor-preflight.test.mjs` only (no assertion changes). Defense in depth; does not replace one-local-gate-per-host. Implementation is a later stacked PR, not this head.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.