schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 150
head: 6e963bc5b06b9216a9649c9081722e579be2af7a
reviewed_utc: 2026-09-26T15:24:16Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: MERGEABLE/UNSTABLE; tip CI settling — docs drift (delta gate) IN_PROGRESS, typecheck/lint/build/test QUEUED, factory contracts QUEUED (Actions run 36251773846); draft=false; 0 GitHub reviews; ahead 1 / behind 0 vs master

## Blockers

(none)

## Improvements

- After hosted CI greens, keep the post-green bachelor pass the PR body already flags (independent exact-head reviews). Do not widen this PR for the historical PR49 reviewer-tier discrepancy — that remains out of scope per the body.
- Optional hygiene (non-blocking): `run-gate.ps1` still hard-wires the shared runtime under `AdversarialLLM-wt\CI-P3-PERF\...`. That matches the factory layout and the new nested-worktree test, but if CI-P3-PERF ever moves, every default path (including `dispatch-review.ps1`'s `$GateNode` fallback) needs the same retarget — consider a single shared constant/helper in a follow-up debt card, not here.

## Notes

- Scope: +52/−4 across 3 files on `feat/CTRL-DEFAULTS-149` (1 commit `6e963bc5b06b`). Fixes #149.
- `factory/controller/dispatch-review.ps1`: default Claude resolution now prefers `Get-Command claude.exe -CommandType Application` and refuses script-only shims with an explicit `-ClaudeExe` override message (no more ambient `claude` shim pickup).
- `factory/controller/run-gate.ps1`: default `$RuntimeDir` resolves via `git rev-parse --git-common-dir` so nested worktrees share the pinned runtime; missing `node.exe` throws before ambient PATH fallback.
- `factory/controller/controller.test.mjs`: three new regressions — shared runtime from main + nested worktree (exit 99 + expected path + no CI log), and Claude native vs shim-only selection (status 0 with `native-config.json` executable pin vs status 99 / "Native Claude executable not found").
- PR body: 57 controller tests passed; full unchanged `scripts/ci.ps1` green on head `6e963bc5` vs base `0a523c1b` in 1117s (supervisor exit 0, zero survivors). Receipt named locally under `.factory-local/` (not re-verified here). CoS did not re-run the Windows factory suite on this Linux box.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
