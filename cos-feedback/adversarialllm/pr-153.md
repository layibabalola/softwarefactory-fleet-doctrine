schema: cos-feedback.v1
project: adversarialllm
repo: layibabalola/AdversarialLLM-ClaudeCode
pr: 153
head: 4a3b031a34af6dde230fbcffeecb9061028d9930
reviewed_utc: 2026-09-26T20:05:31Z
event: pr-opened
verdict: not-merge-ready
ci_summary: MERGEABLE/UNSTABLE; tip CI settling — typecheck/lint/build/test IN_PROGRESS, docs drift (delta gate) QUEUED, factory contracts QUEUED (Actions run 36268192348); draft=false; 0 GitHub reviews; ahead 1 / behind 0 vs master

## Blockers

- HIGH-RISK (`scripts/never-authorized-guard.mjs`, RESET-PLAN operative amendment point 8): one review leg per family is required before merge. Currently 0 GitHub reviews and no `requested_reviewers`. Do not merge while either family is dark.

## Improvements

- After both family review legs land and hosted CI greens, keep the controller exact-head receipt the PR body already promises (local `pwsh scripts/ci.ps1 -BaseSha 487b73e7` was claimed green on the implementer run; CoS did not re-run the Windows factory suite on this Linux box).
- Optional follow-up (non-blocking, not this PR): `INSPECT_VAL` deliberately blocks pipes/redirects of the inspector even into the inspect dir — stricter than the brief. Fine for D10; document that agents must capture stdout in-process rather than shell-redirect if that ever becomes a friction point.

## Notes

- Scope: +486/−0 across 6 files on `feat/OBSERVE-INSPECT` (1 commit `4a3b031a34af`). Queue row OBSERVE-INSPECT order 326 (PR notes order 325 is on open `feat/DOCTRINE-STAGE-0925`).
- New `adversarialllm/scripts/observe-inspect.mjs` (196 lines ≤ 200): fixed CDP `http://127.0.0.1:9222` only (`assertManagedEndpoint`); cmds `tabs` / `screenshot --tab --out` / `probe --provider`; `--out` confined under `adversarialllm/.factory-local/inspect/` lexically and after realpath; never calls `browser.close()` (disconnect by process exit); unit test bans `.close(`/`.click(`/`.fill(`/`.type(`/`.press(`/`.goto(`/`.reload(`/`newPage(` in script text.
- Guard: new `observeInspectDeny` — adds a denial, exempts nothing; allowed only as the whole single statement in exact grammar (10 allow + 23 block vectors). No existing guard vector changed (claimed 0 FAIL of 404).
- Unit: 56 vitest tests in `observe-inspect.test.ts` (jsdom/fixtures only; no live browser). Owned paths match VERIFY (1).
- PR body: not yet exercised against the live managed browser; background-tab screenshot timeout is acknowledged as non-blocking.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.