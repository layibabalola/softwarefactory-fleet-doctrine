schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 13
head: 2b5719eb3063eb7b481e7d822e2aff0cd60b943c
reviewed_utc: 2026-09-20T22:16:33Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: No checks reported yet on head 2b5719eb (branch card/p-12-fold); Windows CI workflow active; latest master Windows CI success on 8cd448f (run 35540722508). Await green `test` on this tip before merge.

## Blockers

(none)

## Improvements

- **`core/storage.py` `update_json`:** still inlines the same serialize → `atomic_replace` → `_cleanup_candidate` path that `write_json` now delegates to `_write_json_unlocked`. A follow-up fold (`update_json` body → `_write_json_unlocked(path, updated)` inside the existing lock) would finish the DRY story without changing the public contract. Non-blocking; out of this card's stated scope.
- **Public name for the shared writer (optional):** `_write_json_unlocked` is now a real cross-module API (`host_slots` + tests import it). A later rename to a non-underscore export (keeping a thin alias) would match the surface honesty of the fold; not required for this PR.
- **Redundant `path.parent.mkdir` in `_write_json_unlocked`:** already measured as dead for both production callers (file_lock → lock_dir creates the sibling lock's parent first). Round 2 correctly left it and pinned the ordering with `test_file_lock_creates_missing_parent_directory_before_body_runs`; remove only via the stated successor card.

## Notes

- Three-file delta (+214/−30): `core/host_slots.py`, `core/storage.py`, `test_agent_bridge.py`. Head `2b5719eb` (subject of DECISION-P-12-fold ADOPTED-LOCAL) on base `master` tip `8cd448f`; 2 commits; class B publication per 2026-09-08 integration-ref directive.
- Fold: host_slots drops its private writer (and `atomic_replace` import); storage exposes one lock-free `_write_json_unlocked`; `write_json` is `with file_lock(path): _write_json_unlocked(path, value)`. Temp names converge on `_temp_path_for` (includes thread id); wildcard `*.tmp` consumers remain valid.
- Contract deviation (stated + tested): card item 4's unconditional unlink-on-replace-failure would break storage's existing retain-candidate-on-`atomic_replace` failure contract (item 7). Implemented retention on replace failure, cleanup on success and on serialization failure; identity + lock-free + cleanup + absolute byte-format pins added in round 2 (literal expected bytestring, not `json.dumps` re-derive).
- Round 2 is tests-only on top of round 1; W3/SOL absolute-format mutations (CRLF / dropped trailing NL / indent=4) redden the new pins.
- No GitHub reviews yet; MERGEABLE unknown / checks empty at review time.
- CoS via `gh` (no Cloud Agent code review this pass). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-13.md`.