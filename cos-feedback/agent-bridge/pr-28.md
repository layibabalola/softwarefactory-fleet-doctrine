schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 28
head: f7aa0fd56adef151e548786ebf8020242815b71e
reviewed_utc: 2026-09-21T15:58:02Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head f7aa0fd (Actions run 35622405721); prior tip 0b7605a0 Windows CI succeeded (run 35619454900); 7118038/002c41e/a9dca4e also green; author local on this tree 1254 passed / 1 skipped, ruff+mypy clean; mutate_cf7b.py exit 0 with MUST_SAY on JUnit raised messages + 3 NEGATIVE_CONTROLS rejected as required; KR-LINEAGE C-F7b second park NARROW 3-0; 0 GitHub PR reviews; confirm Sonnet adversaries re-pass on f7aa0fd

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `f7aa0fd56adef151e548786ebf8020242815b71e` (Actions run 35622405721). Wait for green before merge. If red, start with `tools/mutate_cf7b.py` (MUST_SAY via JUnit `<failure message>`, NEGATIVE_CONTROLS for stale-snapshot / decoy-lock / empty MUST_SAY) and `SessionCycleTests::test_a_busy_RELOAD_skips_the_rest_of_the_cycle_and_keeps_the_fire_history`. Prior tip `0b7605a0` was green on run 35619454900.

## Improvements

- Soft (carried): `GuardTests` still forbids pairing writers with `load_seen` / `_load_wake_breakers` / `read_json` only — not `read_json_for_update`. A future revert of daemon RMW sites to the raise-on-busy reader would crash the watcher/wrapper during the ~90 s leftover-lock window instead of skipping. Round 2 expanded the writer set; C-F7b-2 corrected the guard comment. Still does not pin the skip-reader contract.
- Soft (carried): `_mark_tool_refresh_required` still audits `mcp_tools_refresh_required` and emits controls when the status file was busy and was not rewritten. Hosts can hear a refresh announce while on-disk status still lacks the flag until a later successful write; consider a follow-up retry write, or a distinct audit action when persistence was skipped (symmetry with `mcp_server_snapshot_read_skipped`).
- Soft (updated): 0 GitHub PR reviews yet. C-F7b-2 round 2 on `0b7605a0`: A1/A3 found MUST_SAY substring-matched pytest stdout (long traceback printed earlier assertion text). C-F7b-3 on `f7aa0fd` addresses that harness gap (JUnit raised message + negative controls). Still merge only after factory quorum and green CI — confirm Sonnet adversaries re-pass on `f7aa0fd`.

## Notes

- Head moved `0b7605a0` → `f7aa0fd` (CARD:C-F7b-3, harness-only). Files vs prior tip: `tools/mutate_cf7b.py` only (+57/−8). No product or test file changes. PR vs `master`: 7 files, +936/−50, MERGEABLE; mergeStateStatus UNSTABLE (CI pending).
- KR-LINEAGE on C-F7b (second park) ruled NARROW 3-0 (`decisions/DECISION-LINEAGE-20260921-C-F7b.json`); this successor is harness-only per author.
- MUST_SAY now matches the raised exception message from pytest `--junitxml` (`<failure message=...>`), not all of stdout — so a later assertion failure cannot count as catching `"fire history lost"`.
- Empty / non-string MUST_SAY reports INVALID (was falsy and skipped silently).
- NEGATIVE_CONTROLS checked in: A1 stale-snapshot reload, A3 decoy lock before reload, empty MUST_SAY on a real reload mutant; script exits 0 only if every real row is caught AND every control is rejected.
- WITHDRAWN BY NAME (author): `0b7605a` claim that vacuity-guard failure reports INVALID (mechanism did not guarantee it); A2 round-2 clearance that MUST_SAY matching was sound.
- Banked out of scope (author): a future lock on the state file between pending pass and reload would redirect the behavioural aim (SOL, A3).
- Prior CoS soft Improvements (Guard skip-contract pin; mark-refresh distinct audit) not product-addressed in this push.
- Cursor Cloud Agents not used (harness-only delta; review via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/agent-bridge/pr-28.md`.
