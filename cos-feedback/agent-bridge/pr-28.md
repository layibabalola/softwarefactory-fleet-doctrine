schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 28
head: 0b7605a0e8f9fd2b682df9caa25e53db8b8ba18c
reviewed_utc: 2026-09-21T15:32:23Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 0b7605a0 (Actions run 35619454900); prior tip 7118038 Windows CI succeeded (run 35617901711); 002c41e and a9dca4e also green; author local on this tree 1254 passed / 1 skipped, ruff+mypy clean; mutate_cf7b.py exit 0 with MUST_SAY on reload rows and A1 textual forms (renamed/split/alias); C-F7b-2 round-1 A1 CHANGES_REQUESTED on source-text aim addressed by behaviour-aimed race; SOL/A2/A3 APPROVE on 7118038 per commit; 0 GitHub PR reviews; confirm Sonnet adversaries re-pass on 0b7605a0

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `0b7605a0e8f9fd2b682df9caa25e53db8b8ba18c` (Actions run 35619454900). Wait for green before merge. If red, start with `test_read_modify_write_skip_sites.py` (`SessionCycleTests::test_a_busy_RELOAD_skips_the_rest_of_the_cycle_and_keeps_the_fire_history`, behaviour-aimed `_busy_when` after `_process_pending_wake_verifications` returns) and `tools/mutate_cf7b.py` MUST_SAY / textual reload mutants. Prior tip `7118038` was green on run 35617901711.

## Improvements

- Soft (carried): `GuardTests` still forbids pairing writers with `load_seen` / `_load_wake_breakers` / `read_json` only — not `read_json_for_update`. A future revert of daemon RMW sites to the raise-on-busy reader would crash the watcher/wrapper during the ~90 s leftover-lock window instead of skipping. Round 2 expanded the writer set; C-F7b-2 corrected the guard comment. Still does not pin the skip-reader contract.
- Soft (carried): `_mark_tool_refresh_required` still audits `mcp_tools_refresh_required` and emits controls when the status file was busy and was not rewritten. Hosts can hear a refresh announce while on-disk status still lacks the flag until a later successful write; consider a follow-up retry write, or a distinct audit action when persistence was skipped (symmetry with `mcp_server_snapshot_read_skipped`).
- Soft (updated): 0 GitHub PR reviews yet. C-F7b-2 round 1 on `7118038`: SOL/A2/A3 APPROVE; A1 CHANGES_REQUESTED (source-text `_at_the_session_reload` lost aim on rename/split) is addressed on `0b7605a0` by behaviour-aimed race + MUST_SAY + three textual mutants. Still merge only after factory quorum and green CI — confirm Sonnet adversaries re-pass on `0b7605a0`.

## Notes

- Head moved `7118038` → `0b7605a0` (C-F7b-2 resubmit, test-only). Files vs prior tip: `test_read_modify_write_skip_sites.py` (+18/−19), `tools/mutate_cf7b.py` (+33/−7). PR vs `master`: 7 files, +887/−50, MERGEABLE; mergeStateStatus UNSTABLE (CI pending).
- WITHDRAWN BY NAME (author): `7118038` claim that the test "stays aimed at the reload whether that read is the skip reader or a reverted degrading one" — false under rename/split; vacuity guard could fire instead of data-loss.
- Behaviour aim: wrap `_process_pending_wake_verifications`; first `file_lock` on the state file after that pass returns is the reload (`_wake_skip_keys` takes no lock in between). Removed `inspect`-based `_at_the_session_reload`.
- `mutate_cf7b.py`: MUST_SAY requires failure message `"fire history lost"` on reload rows (vacuity = INVALID); added mutants for reload reverted renamed / split across lines / through an alias.
- Product path unchanged: `process_session_once` still uses `_load_seen_for_update(..., stage="session-cycle-reload")` and skips on `None`.
- Prior CoS soft Improvements (Guard skip-contract pin; mark-refresh distinct audit) not product-addressed in this push.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/agent-bridge/pr-28.md`.