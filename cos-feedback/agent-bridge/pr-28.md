schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 28
head: a9dca4edec1b09c43b29fd18c07b679d1b5cc4a2
reviewed_utc: 2026-09-21T14:28:46Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head a9dca4e (Actions run 35612335701); author local bar 1230 passed / 1 skipped without PYTHONPATH, ruff+mypy clean; mutate_cf7b.py 15/15 caught; Class C SOL APPROVE (SOL-20260921-092327-493); Sonnet adversaries still in progress per PR body

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `a9dca4edec1b09c43b29fd18c07b679d1b5cc4a2` (Actions run 35612335701). Wait for green before merge. If red, start with new shard `test_read_modify_write_skip_sites.py` (real live-lock race harness) and any `watcher` / `server_wrapper` / `core.storage.read_json_for_update_or_skip` regressions.

## Improvements

- Soft: `GuardTests` forbids pairing writers with `read_json` (and `_load_wake_breakers` / `load_seen`) but does not forbid `read_json_for_update` on daemon RMW sites. A future revert to the raise-on-busy reader would crash the watcher/wrapper during the ~90 s leftover-lock window instead of skipping — extend the forbidden-reader set (or require `read_json_for_update_or_skip` only) if the board wants the AST guard to pin the skip contract, not only the no-data-loss contract.
- Soft: `_mark_tool_refresh_required` still audits `mcp_tools_refresh_required` and emits controls when the status file was busy and was not rewritten. Hosts can hear a refresh announce while the on-disk status still lacks the flag until a later successful write; consider a follow-up retry write, or a distinct audit action when persistence was skipped (symmetry with `mcp_server_snapshot_read_skipped`).
- Soft: 0 GitHub PR reviews yet; PR body says Sonnet adversaries are still in progress — merge only after factory quorum and green CI, not SOL alone.

## Notes

- Seven-file delta (+671/−32): `core/storage.py` (+23 new skip reader), `watcher.py` (+85/−25, 9 RMW sites), `server_wrapper.py` (+32/−7, 5 sites), `test_read_modify_write_skip_sites.py` (+368 new, registered in `windows-ci.yml`), `tools/mutate_cf7b.py` (+161), `BRIDGE_PROTOCOL.md` (+1 audit action `mcp_server_snapshot_read_skipped`). Ahead 1 / behind 0 vs `master`. MERGEABLE; mergeStateStatus pending from CI.
- Intent (CARD:C-F7b): `read_json` answered "missing" on lock timeout; daemon RMW sites saved that default over real state (busy watcher-state wiped commit watches / restart controls; busy breaker wiped every session; busy tool-refresh status wiped every host's staleness — SOL C-F7 r1). Raise-on-busy would stop the daemon during ~90 s after crash while leftover lock is held. New `read_json_for_update_or_skip` returns `None` on any `OSError` (caller skips that cycle's write); missing/malformed/non-object still returns the default so corrupt files self-heal. Pure readers unchanged.
- Tests race a real live lock released before the write would run, assert byte-identical survival, and assert the busy read happened (no vacuous pass). Structural AST guard keys on reader/writer function names (alias-proof vs C-F7 path-text guard) and requires ≥12 writers seen. Mutants: 15/15 caught incl. guard-only sites (breaker close, bypass-consume, clear-host-staleness).
- Class C; SOL APPROVE (`SOL-20260921-092327-493`). Adversaries in progress. No GitHub PR reviews (0).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-28.md`.