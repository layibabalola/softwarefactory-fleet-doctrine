schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 28
head: 711803855afdf2b5fe149e2cb812a0403a5b91c7
reviewed_utc: 2026-09-21T15:18:48Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 7118038 (Actions run 35617901711); prior tip 002c41e Windows CI succeeded (run 35615552962); a9dca4e also green (run 35612335701); author local on this tree 1254 passed / 1 skipped, ruff+mypy clean; mutate_cf7b.py exit 0 with session-cycle-reload mapped to new behavioural test; Class C SOL APPROVE on round 1; SOL CHANGES_REQUESTED on round 2 reload gap addressed by test-only C-F7b-2; 0 GitHub PR reviews; Sonnet adversaries still in progress per earlier PR body

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `711803855afdf2b5fe149e2cb812a0403a5b91c7` (Actions run 35617901711). Wait for green before merge. If red, start with `test_read_modify_write_skip_sites.py` (`SessionCycleTests::test_a_busy_RELOAD_skips_the_rest_of_the_cycle_and_keeps_the_fire_history` and prior live-lock harness) and any `watcher` / `server_wrapper` / `core.storage.read_json_for_update_or_skip` regressions. Prior tip `002c41e` was green on run 35615552962.

## Improvements

- Soft (carried): `GuardTests` still forbids pairing writers with `load_seen` / `_load_wake_breakers` / `read_json` only — not `read_json_for_update`. A future revert of daemon RMW sites to the raise-on-busy reader would crash the watcher/wrapper during the ~90 s leftover-lock window instead of skipping. Round 2 expanded the writer set; C-F7b-2 corrected the guard comment (every data-loss site now has a behavioural race; three guard-only sites cannot lose data). Still does not pin the skip-reader contract.
- Soft (carried): `_mark_tool_refresh_required` still audits `mcp_tools_refresh_required` and emits controls when the status file was busy and was not rewritten. Hosts can hear a refresh announce while on-disk status still lacks the flag until a later successful write; consider a follow-up retry write, or a distinct audit action when persistence was skipped (symmetry with `mcp_server_snapshot_read_skipped`).
- Soft (updated): 0 GitHub PR reviews yet. SOL round-2 CHANGES_REQUESTED (SOL-20260921-095651-698) on acceptance row (a) for the session-cycle reload is addressed by this test-only push (new aimed race). Still merge only after factory quorum and green CI, not SOL alone — confirm Sonnet adversaries re-pass on `7118038`.

## Notes

- Head moved `a9dca4e` → `c00bb88` (round 2) → `002c41e` (merge master C-LOCKDIR-2) → `7118038` (C-F7b-2 test-only). PR vs `master`: 7 files, +862/−50, ahead 4 / behind 0. MERGEABLE; mergeStateStatus UNSTABLE (CI pending).
- C-F7b-2 (`7118038`): test-only successor after round 2 parked by two-round limit. SOL said the session-cycle reload (feeds `wake_fire_history` into saves whose merge REPLACES it) was only guard-pinned; A3 reproduced by widening EXEMPT + reverting reload. SOL/A1/A2/A3 found no product defect.
  - New `_busy_when(path, predicate)` + `_at_the_session_reload()` (statement-aimed, not lock-call count).
  - `SessionCycleTests::test_a_busy_RELOAD_skips_the_rest_of_the_cycle_and_keeps_the_fire_history`: asserts busy read happened, other session fire history survives, file byte-identical to busy-read bytes, cycle returned `[]`. Positive control now checks fire history too.
  - `mutate_cf7b.py`: session-cycle reload mutant mapped to the new behavioural test (no longer "guard only").
  - Guard comment corrected; A2 GuardTests "flake" withdrawn (harness mutant in same worktree).
- Product path unchanged: `process_session_once` still uses `_load_seen_for_update(..., stage="session-cycle-reload")` and skips on `None`.
- Evidence (author): ruff+mypy clean; `mutate_cf7b.py` exit 0; full suite 1254 passed, 1 skipped.
- Intent (CARD:C-F7b) unchanged: daemon RMW sites must neither crash nor save a default over real state on busy lock.
- Prior CoS soft Improvements (Guard skip-contract pin; mark-refresh distinct audit) not product-addressed in this push; SOL reload-gap Improvement closed via behavioural test.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/agent-bridge/pr-28.md`.
