schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 28
head: 002c41e2bd37017283ce843ae05ee359655c1019
reviewed_utc: 2026-09-21T14:57:38Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 002c41e (Actions run 35615552962); prior head a9dca4e Windows CI succeeded (run 35612335701); author local on merged tree 1253 passed / 1 skipped, ruff+mypy clean; mutate_cf7b.py exit 0 with round-2 mutants (session-cycle / save / unknown-origin); Class C SOL APPROVE on round 1 (SOL-20260921-092327-493); A1 CHANGES_REQUESTED addressed in round 2; 0 GitHub PR reviews; Sonnet adversaries still in progress per PR body

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `002c41e2bd37017283ce843ae05ee359655c1019` (Actions run 35615552962). Wait for green before merge. If red, start with `test_read_modify_write_skip_sites.py` (new `SessionCycleTests` + prior live-lock harness) and any `watcher` / `server_wrapper` / `core.storage.read_json_for_update_or_skip` regressions. Prior tip `a9dca4e` was green on run 35612335701.

## Improvements

- Soft (carried): `GuardTests` still forbids pairing writers with `load_seen` / `_load_wake_breakers` / `read_json` only — not `read_json_for_update`. A future revert of daemon RMW sites to the raise-on-busy reader would crash the watcher/wrapper during the ~90 s leftover-lock window instead of skipping. Round 2 did expand the writer set (`_save_watcher_state`, `storage_update_json`) and documented the helper-blind-spot (A3) + one reasoned `EXEMPT` for `_process_pending_wake_verifications`; still does not pin the skip-reader contract.
- Soft (carried): `_mark_tool_refresh_required` still audits `mcp_tools_refresh_required` and emits controls when the status file was busy and was not rewritten. Hosts can hear a refresh announce while on-disk status still lacks the flag until a later successful write; consider a follow-up retry write, or a distinct audit action when persistence was skipped (symmetry with `mcp_server_snapshot_read_skipped`).
- Soft (carried): 0 GitHub PR reviews yet; PR body says Sonnet adversaries are still in progress — merge only after factory quorum and green CI, not SOL alone. Round 2 cites A1 CHANGES_REQUESTED from round 1; confirm those adversaries re-pass on `002c41e`.

## Notes

- Head moved `a9dca4e` → `c00bb88` (round 2) → `002c41e` (merge `master`/`ad5657e` C-LOCKDIR-2 into `card/c-f7b`). PR vs `master`: 7 files, +803/−50, ahead 3 / behind 0. MERGEABLE; mergeStateStatus UNSTABLE (CI pending).
- Round 2 (OPUS-1 / A1): `process_session_once` previously read watcher state with degrading `load_seen` and fed those lists to `_save_watcher_state`, whose merge REPLACES `pending_wake_verifications`, `paused_wake_messages`, `unknown_origin_warnings`, and `wake_fire_history`. When the lock freed between the timed-out read and the save, empty lists overwrote real ones. Round 1's guard missed it because `_save_watcher_state` was not in the writer set.
  - `process_session_once` cycle read + `wake_fire_history` reload → `_load_seen_for_update`; busy → skip rest of that session cycle (`return []`).
  - `_warn_unknown_origin_once` → skip reader; no warning this cycle.
  - `_save_watcher_state`: degrading `toasted_ids` pre-read removed (`_merge` already unions under the lock; A1 measured ~60 s two waits then raise). `OSError` from `storage_update_json` → logged skip via `_write_skipped`, not raise.
  - Guard writer set gains `_save_watcher_state` + `storage_update_json`; one EXEMPT for `_process_pending_wake_verifications`; comment names helper-outside-writer-set blind spot.
- Author corrections to round 1 claims (alias-free / every data-loss site raced / raise stops watcher / "exactly as read_json") acknowledged in commit message. Out of scope carded: C-UPDATE-JSON (`update_json` default-over-file when read inside its own lock raises OSError — OPUS-3).
- Evidence on merged tree (author): ruff+mypy clean; `mutate_cf7b.py` exit 0 including new session-cycle / save / unknown-origin mutants; full suite 1253 passed, 1 skipped.
- Intent (CARD:C-F7b) unchanged: daemon RMW sites must neither crash nor save a default over real state on busy lock; `read_json_for_update_or_skip` returns `None` on any `OSError`.
- Prior CoS soft Improvements (Guard skip-contract pin; mark-refresh distinct audit; adversary quorum) not addressed in this push.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/agent-bridge/pr-28.md`.
