schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 25
head: 394c50cb12cabe439c62c1307aa416baa0aecae6
reviewed_utc: 2026-09-21T10:40:33Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 394c50c (Actions run 35589860511); author local bar 1194 passed / 1 skipped without PYTHONPATH; Class C lineage C-F7→C-F7e with SOL/A1/A2/A3 final APPROVE (SOL-20260921-052434-738)

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `394c50cb12cabe439c62c1307aa416baa0aecae6` (Actions run 35589860511). Wait for green before merge; if red, start with `test_read_json_lock_timeout.py` (new file, added to the Windows CI shard list) and any storage/bridge loader regressions.

## Improvements

- **Rebase onto current `master` before merge.** Tip is ahead 7 / behind 5 (diverged). Master landed #24 (C-R1d bootstrap) which also edits `.github/workflows/windows-ci.yml` (adds `test_bootstrap_no_kill.py`). This PR adds `test_read_json_lock_timeout.py` to the same shard list — expect a trivial conflict there; product paths (`core/storage.py`, `agent_bridge/bridge.py`, `setup_bridge.py`) do not overlap the bootstrap files.

## Notes

- Six-file delta (+893/−55): `core/storage.py` (+99), `agent_bridge/bridge.py` (+43/−51), `agent_bridge/setup_bridge.py` (+11/−4), `test_read_json_lock_timeout.py` (+512 new), `tools/mutate_cf7d.py` (+227 new), `.github/workflows/windows-ci.yml` (+1). Behind 5 / ahead 7 vs `master`. MERGEABLE; mergeStateStatus UNSTABLE from pending check.
- Intent (CARD:C-F7 / A-F1): `read_json` treats lock `TimeoutError` (an `OSError`) as missing, so RMW writers saved defaults over live session/pair state. `read_json` deliberately unchanged (pure readers / unguarded startup). New `read_json_for_update` and `read_json_or_quarantine` always take the lock first; only a plain missing leaf (winerror 2 / POSIX ENOENT + confirming `lstat`) yields the default; held lock raises; offline-share / non-plain `FileNotFoundError` propagates; quarantine of malformed/non-object under one lock hold (H-12). Bounded 1 s `PermissionError` retry; directory / other I/O raise promptly.
- Call sites: five quarantining loaders → `read_json_or_quarantine`; seven RMW writers (two wake-breaker, four watcher-state, setup `_register_project` via `_project_plan(for_update=…)`) → `read_json_for_update`. Dry-run setup keeps `read_json`. Accepted by name: strict read under a missing parent leaves an empty parent dir.
- Out of scope (carded): C-F7b watcher/`server_wrapper` RMW sites; branch enumerator; separately carded `UnicodeDecodeError` reader-thread flake and intermittent server_wrapper load flakes.
- Evidence: `tools/mutate_cf7d.py` matrix 19 mutants all caught (catch only on pytest exit 1 + expected test id after green baseline; harness `--self-test` proves restore); final subject `394c50c` carries `5fa2d1c` product bytes unchanged (R2). CoS did not re-run mutations or the Windows suite locally (Linux box).
- Class C four-subject lineage; final subject approved by SOL (`SOL-20260921-052434-738`), A1, A2, A3. No GitHub PR reviews yet (0).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-25.md`.