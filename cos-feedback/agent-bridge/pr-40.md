schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 40
head: e057af0c1da5df2050fb1f25dfe61c1e8859e0bb
reviewed_utc: 2026-09-24T07:53:32Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head e057af0c (Actions run 35972037874); MERGEABLE / mergeState UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- Wait for Windows CI `test` green on head `e057af0c1da5df2050fb1f25dfe61c1e8859e0bb` before merge (Actions run 35972037874).

## Notes

- Single-file docs delta: `BRIDGE_HARDENING.md` (+4/−3, 2 commits on `card/trunk-hardening-stale-citation`). Ahead 2 / behind 0 vs `master`. No production code touched. CARD:TRUNK-HARDENING-STALE-CITATION (class B, docs only).
- Defect: checklist still cited `test_load_state_defaults_on_corrupt_file_instead_of_bricking` and described corrupt recovery as “don’t wedge / defaults.” Live trunk behavior is fail-closed (preserve corrupt bytes; no rewrite).
- Fix: reword the bullet to fail-closed / quarantine / tolerant-default semantics and point at `test_agent_bridge.py::test_load_state_rejects_corrupt_file_without_rewriting_it` (plus unchanged `test_core_storage_quarantines_bad_jsonl_and_versions_state` and `test_core_storage_corrupt_json_returns_default`). CoS confirmed all three defs exist on this head; the old test name is gone.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-40.md`.