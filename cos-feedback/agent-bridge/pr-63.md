schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 63
head: 25f81bd9d284ec081d593bc22cb73c5e1cf947bb
reviewed_utc: 2026-09-26T20:17:16Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: MERGEABLE/UNSTABLE; Windows CI `test` IN_PROGRESS on tip 25f81bd9 (Actions run 36268953998 / job 108479093297); draft=false; 0 GitHub reviews; ahead 5 / behind 0 vs master

## Blockers

(none)

## Improvements

- Follow-ups already named in the PR body — keep out of this PR: **C-F1-ID** (owner identity via process creation time vs `started_at`, to shrink the pid-reuse residual window) and **C-F4** (OpenProcess-denied owners read as dead). Non-numeric `pid` still raises (same as master).
- `test_ensure_watcher_race.py` is probabilistic on a broken acquire (~3% false-green at 12 rounds / 1-in-4 base). Acceptable as a regression tripwire; if CI flakes green-on-broken become a concern, raise `ROUNDS` or add a deterministic unit pin of the sidecar hold (the lease suite already covers the acquire path tightly).

## Notes

- Scope: +701/−25 in 4 files on `card/c-f1-r1` (5 commits; tip `25f81bd9d284ec081d593bc22cb73c5e1cf947bb`). Ports sidecar-locked singleton lease from banked C-F1 reference (`ca18f6a`) into `core/processes.py`: acquire / heartbeat / release under `_acquire_lock_path` sidecar; fail-closed `lease_unreadable` on sidecar/data-lock OSError; `lease_status` treats live+fresh owner as `running` regardless of command hash (hash_mismatch only when live+stale+mismatch). Adds `test_core_singleton_lease.py` (concurrency, live-owner, round-2, pid-reuse residual) and end-to-end `test_ensure_watcher_race.py` (12× two real concurrent `ensure_watcher` → exactly one live watcher; venv launcher+child counted once). Both suites added to Remaining governed suites in `windows-ci.yml`. `watcher.py` / `bootstrap_session.py` / trampoline / `migrate_root.py` unchanged.
- Spot-check on tip: sidecar is deliberately NOT `file_lock(lock_path)` (avoids deadlock with `read_json_for_update`/`write_json`); TimeoutError on sidecar returns `lease_unreadable` instead of raising into watcher/migrate callers; heartbeat/release check pid+generation under the same sidecar so a replaced owner cannot clobber or unlink the new lease. Residual C-F1-ID characterized in `PidReuseResidualCharacterizationTests` (refuse ≤120 s, reclaim at 121 s).
- PR body: A1/A2/A3 APPROVE + SOL APPROVE (receipt `SOL-20260926-145229-713`); local evidence 0/28 duplicate-watcher trials on head vs 8/32 on master `01766ee`; mutation lineage 0 unexpected; governed suites 960 passed / 1 skipped locally. CoS did not re-run the suite on this Linux box (factory floor is Windows). No GitHub review objects yet (factory receipts are out-of-band).
- Cursor Cloud Agents not used (unavailable on plan); review via gh. No GitHub PR comment posted.
