schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 43
head: 752b7ee6aa3a4665da214c69deaf5a52cbaf5207
reviewed_utc: 2026-09-24T18:49:53Z
event: pr-opened
verdict: not-merge-ready
ci_summary: Windows CI `test` IN_PROGRESS on head 752b7ee (Actions run 36043863918); MERGEABLE / mergeState UNSTABLE; 0 GitHub reviews; hosted path exercised (TEMP step ok; setup-python ran; self-hosted venv step skipped)

## Blockers

- Class C ratification missing: PR body and KERNEL treat this as class C (hooks/CI + governance/spending). Proposed card `OWNER-CI-RUNNER` is **unminted**. Hub must mint the card and complete class-C ratification (three Sonnet adversaries + SOL) before merge. Do not merge on CI green alone.

## Improvements

- Wait for Windows CI `test` green on head `752b7ee6aa3a4665da214c69deaf5a52cbaf5207` (Actions run 36043863918) before any post-ratification merge.
- After card mint + `CI_RUNS_ON` is set on ultra-magnus, treat the first self-hosted run as the real proof: that path is unproven on this PR (CI still falls back to `windows-latest` while the var is unset).
- Malformed `CI_RUNS_ON` (non-JSON) will fail the `fromJSON` expression at job start; keep the var a valid JSON label array or delete it to fall back.

## Notes

- Scope: +38/−2 across 2 files (`.github/workflows/windows-ci.yml`, `AGENTS.md`) on `card/owner-ci-runner` (1 commit). Ahead 1 / behind 0 vs `master`. No production Python touched.
- `runs-on` reads `vars.CI_RUNS_ON` (JSON label array) with fallback `windows-latest` — inert until the owner sets the repo variable. Deleting the var restores hosted behavior without a code change.
- TEMP/TMP: first step appends two separate `GITHUB_ENV` lines from `RUNNER_TEMP` (job-level `env: TEMP: ${{ runner.temp }}` is invalid — `runner` context unavailable there). Hosted run already completed that step successfully.
- Python setup split: `actions/setup-python@v5` only when `runner.environment == 'github-hosted'`; else `py -3.14 -m venv --clear` under `RUNNER_TEMP` + Scripts on `GITHUB_PATH` (avoids setup-python's admin / InstallAllUsers / HKLM Python 3.14.* registry deletes on a workstation). Fails closed with a named error if py/3.14 missing.
- AGENTS.md: "CI is unaffected" sentence updated to say CI points TEMP/TMP at runner temp in the first step (service-installed self-hosted inherits `C:\WINDOWS\TEMP`).
- Owner prerequisites (data only): Python 3.14 + py launcher visible to the runner service account; prefer an account with no live `~/.agent-bridge`; keep fork-PR workflows disabled on this private repo (`codex/**` push triggers still run lane code on the runner account).
- Offline checks claimed in PR body (validators, 68 pytest, ruff, doc-size, PowerShell step simulation) not re-run here; live gate is Actions run 36043863918.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-43.md`.