schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 34
head: 0dac507e359f4a353487c19647c2389f465061c8
reviewed_utc: 2026-09-21T21:15:54Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 0dac507e (Actions run 35656098003); author claims local smoke 141 passed (base + venv) + Class-C quorum (3/4 APPROVE, one CHANGES_REQUESTED on scope only); 0 GitHub PR reviews; tip ahead 1 / behind 0 vs master

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `0dac507e359f4a353487c19647c2389f465061c8` (Actions run 35656098003). Wait for green before merge. If red, start with the new suite in `test_server_wrapper_phase2.py`: `test_windows_venv_child_launch_rewrites_copies_only_for_current_interpreter`, `test_non_windows_or_non_venv_child_launch_is_unchanged`, and (Windows-only) `test_disposable_windows_venv_launch_has_actual_pid_and_venv_paths`.

## Improvements

- Soft: **0 GitHub PR reviews** on this tip. Author body claims Class-C quorum (3 of 4 APPROVE; one CHANGES_REQUESTED asks only to expand scope to other `sys.executable` spawn sites, carded separately; SOL approved). Confirm factory quorum is visible and CI is green before merge.
- Soft: scope is intentionally narrow — only `_start_child_process` / `_prepare_child_launch`. Other `sys.executable` spawn sites remain on separate cards; that is fine for this bar, but track them so the PID/signature wait does not stay broken elsewhere.

## Notes

- CARD:VENV-PID bar (b). Branch `card/venv-pid-b`. Ahead 1 / behind 0 vs `master`. MERGEABLE; mergeStateStatus UNSTABLE (CI pending).
- Diff vs master: `server_wrapper.py` +108/−1 net-ish (+113/−3 overall with tests across 2 files). Product change: on win32 CPython inside a venv, when `command[0] == sys.executable` and `sys._base_executable` exists and differs, spawn the base interpreter with `__PYVENV_LAUNCHER__=sys.executable` so `Popen.pid` matches the child's `os.getpid()` (venv redirector previously made tool-manifest wait leave `signature_state='unconfirmed'` after code-change restarts).
- Guardrails look correct: copies command/env (does not mutate caller), skips non-win32, non-venv (`prefix == base_prefix`), arbitrary argv[0], missing/equal base executable. Tests cover rewrite + isolation, no-op paths, and a disposable Windows venv PID/path probe.
- Commit cherry-picked from never-reviewed candidate `d3169e5` (headline: Preserve server PID under Windows venvs).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-34.md`.
