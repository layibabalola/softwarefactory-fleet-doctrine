schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 36
head: 8dc0ffd187653cc46ada5886faec29295c3a0d4a
reviewed_utc: 2026-09-22T05:28:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 8dc0ffd (Actions run 35690748006); author claims mutate_cea 16/16 caught + 18 self-tests + 2 controls exit 0, full suite 1334 passed, ruff/mypy clean; 0 GitHub PR reviews

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `8dc0ffd187653cc46ada5886faec29295c3a0d4a` (Actions run 35690748006). Wait for green before merge. If red, start with `test_ensure_adopt.py` (registered in `windows-ci.yml`) — RealWatcher / Decision / Ordering / PublicationRace / StartKey cases that pin adopt-vs-recycle, unverified keep-and-report, and denied-is-not-dead.

## Improvements

- Soft: **0 GitHub PR reviews** on this tip. Confirm factory quorum / SOL visibility and CI green before merge.
- Soft: `ensure_watcher` recycled branch still uses bare `except Exception: pass` around `read_lease` / unlink (`bootstrap_session.py`). Prefer catching `(OSError, ValueError, TypeError)` (or whatever `read_lease` actually raises) so a real bug there is not swallowed.
- Soft: POSIX `/proc` field-22 start-key path has no executed coverage (Windows-only CI; stated by author / A3). Acceptable for this card; track a follow-up if POSIX hosts matter.

## Notes

- CARD:C-ENSURE-ADOPT (class C). Branch `card/c-ensure-adopt`. Round 2 head `8dc0ffd` (+794/−14, 9 files vs master) resubmits after A1 BLOCKER + SOL CHANGES_REQUESTED + A3 CHANGES_REQUESTED on `9266499`. MERGEABLE; mergeStateStatus UNSTABLE while CI runs.
- Round 2 fold: **unverified is keep-and-report, never adopt** — status `already_running_unverified` with `operator_note`, no lease written (A1 / SOL finding 1; withdraws prior adopt-unverified reading). **Publication race**: `_publish_marker` removes stale identity, writes pid, then identity via temp+`os.replace` (SOL finding 2; PublicationRaceTests). A3 holes closed: lease naming another pid survives recycle; `is_process_alive` seam still decides alive; AST pin pid-before-identity; Windows key is raw FILETIME ticks not local `creation_date`.
- Design unchanged at root: watcher writes `watcher.identity.json` `{pid, start_key}`; equal key → adopt (`identity: match`); both readable and different → recycled (clear OUR pid/identity/lease-for-that-pid only); missing key → unverified; unqueryable → `already_running_unverifiable`. `process_liveness` alive/dead/unknown; restart defers on anything but dead.
- Prior CoS soft on “legacy stays already_running with identity unverified” superseded by A1: that path must not write a lease. One-shot migrate that writes identity for a live pre-identity watcher remains optional post-rollout hygiene, not a merge gate.
- Evidence claimed on `8dc0ffd`: `tools/mutate_cea.py` 16 mutants caught, 18 self-tests + 2 controls exit 0; full suite 1334 passed; ruff/mypy clean. Tests registered in Windows CI.
- Cursor Cloud Agents not used (plan gap); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: refreshed on `master` at `cos-feedback/agent-bridge/pr-36.md`. Chat skipped (verdict unchanged; prior CoS Improvements not clearly cleared).