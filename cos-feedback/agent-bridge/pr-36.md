schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 36
head: 946bc7b2ec5d7d75b858243a1f4d175870e6f963
reviewed_utc: 2026-09-22T07:39:07Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 946bc7b (Actions run 35700599748); author claims mutate_cea 28/28 caught + 18 self-tests + 2 controls exit 0, full suite 1346 passed / 1 skipped, ruff/mypy clean; 0 GitHub PR reviews; MERGEABLE/UNSTABLE

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `946bc7b2ec5d7d75b858243a1f4d175870e6f963` (Actions run 35700599748). Wait for green before merge. If red, start with `test_ensure_adopt.py` (registered in `windows-ci.yml`) — RealWatcher / LiveLease / Decision / Ordering / PublicationRace / StartKey / AtomicIdentity / PublishLock cases that pin adopt-vs-recycle, inherited-lease watch, lock ordering, and denied-is-not-dead.

## Improvements

- Soft: **0 GitHub PR reviews** on this tip. Confirm factory quorum / SOL visibility and CI green before merge.
- Soft: POSIX `/proc` field-22 start-key path still has no executed coverage (Windows-only CI; stated by author). Acceptable for this card; track a follow-up if POSIX hosts matter.
- CLEARED (prior CoS soft): recycled clear no longer wraps `read_lease` / unlink in bare `except Exception: pass`. Tip uses `_raw_text` + `json.loads` with `ValueError`, and lock-scoped byte re-checks before unlink.

## Notes

- CARD:C-ENSURE-ADOPT / C-ENSURE-ADOPT-2 (class C). Branch `card/c-ensure-adopt`. Round 4 head `946bc7b` (+1257/−14, 9 files vs master; +2 commits / +482/−19 across `bootstrap_session.py`, `test_ensure_adopt.py`, `tools/mutate_cea.py`, `watcher.py` vs prior CoS tip `8dc0ffd`). MERGEABLE; mergeStateStatus UNSTABLE while CI runs.
- Round 3 (`24cbc2a`): compare-and-delete under pid lock; lease `start_key` as ownership proof. Drew A1 BLOCKER + SOL CHANGES_REQUESTED (SOL-20260922-011228-228); A2/A3 APPROVE.
- Round 4 resubmit (`946bc7b`) folds those findings: a lease naming the live pid with a **different** key is **watched** for `LEASE_SETTLE_SECONDS` (6 s) before clear — change ⇒ live owner kept; unchanged ⇒ recycled clear. Equal lease key kept immediately (no wait for heartbeat). Slow lease read/watch happens **before** any lock; pid then lease locks held only for byte re-check + unlink (SOL: 30 s under pid lock outlasted watcher's 5 s publish wait). Watcher never holds both locks.
- WITHDRAWN BY NAME (author): `24cbc2a` claim that "only a different key is a genuinely recycled pid" — different key can be a live watcher mid-start / inherited lease.
- Design root unchanged: watcher writes `watcher.identity.json` `{pid, start_key}`; equal key ⇒ adopt; both readable and different ⇒ recycled path (clear OUR files only); missing key ⇒ unverified keep-and-report; unqueryable ⇒ `already_running_unqueryable`. `process_liveness` alive/dead/unknown; restart defers on anything but dead.
- Evidence claimed on `946bc7b`: `tools/mutate_cea.py` 28 mutants caught, 18 self-tests + 2 controls exit 0; full suite 1346 passed; ruff/mypy clean. New LiveLease / inherited-lease / lock-ordering / keyless-lease / held-lock / publish-waits tests.
- Cursor Cloud Agents not used (plan gap); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: refreshed on `master` at `cos-feedback/agent-bridge/pr-36.md`.
