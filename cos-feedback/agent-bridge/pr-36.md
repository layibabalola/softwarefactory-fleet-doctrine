schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 36
head: 197950ac7534a87949ca49a33956d7d9799371a5
reviewed_utc: 2026-09-23T08:35:48Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Windows CI `test` FAILURE on head 197950ac (Actions run 35837922610) — job never started: Actions budget preventing further use; annotation on `.github`; 0 GitHub PR reviews; MERGEABLE/UNSTABLE

## Blockers

- **CI: Actions budget exhausted** — Windows CI `test` concluded FAILURE in ~3s on head `197950ac7534a87949ca49a33956d7d9799371a5` (Actions run 35837922610) without executing tests. Check annotation: "The job was not started because an Actions budget is preventing further use." Unblock: restore GitHub Actions minutes / billing for `layibabalola/agent-bridge`, then re-run the failed workflow on this tip. Product CI status for this head is unknown until that succeeds.

## Improvements

- Soft: **0 GitHub PR reviews** on this tip. Confirm factory quorum / SOL visibility and a real CI green (not a budget skip) before merge.
- Soft: POSIX `/proc` field-22 start-key path still has no executed coverage (Windows-only CI; stated by author). Acceptable for this card; track a follow-up if POSIX hosts matter.
- CLEARED (prior CoS soft, still holds): recycled clear no longer wraps `read_lease` / unlink in bare `except Exception: pass`.

## Notes

- CARD:C-ENSURE-ADOPT / C-ENSURE-ADOPT-2 / C-ENSURE-ADOPT-CI (class C). Branch `card/c-ensure-adopt`. Round 5 head `197950ac` (+1264/−14, 9 files vs master; +1 commit / +8/−1 in `test_ensure_adopt.py` only vs prior CoS tip `946bc7b`). MERGEABLE; mergeStateStatus UNSTABLE.
- Round 5 (`197950ac`): CI flake fix only. `test_the_system_process_reads_as_unknown_not_dead` no longer asserts `process_liveness(4) == "unknown"`. Elevated GH runners (PROCESS_QUERY_LIMITED_INFORMATION on System pid 4) correctly read `"alive"`; the old assert encoded "authoring host is not elevated", not the card contract, and reddened #36 four times. New contract: reading ∈ {`unknown`,`alive`} and ≠ `dead`. Author claims both-direction controls (non-elevated host passes; mutant returning dead for pid 4 fails). Same host-table dependency shape as CARD:P-14-FLAKE.
- Design root unchanged: watcher writes `watcher.identity.json` `{pid, start_key}`; equal key ⇒ adopt; both readable and different ⇒ recycled path (clear OUR files only, with LiveLease watch); missing key ⇒ unverified keep-and-report; unqueryable ⇒ `already_running_unqueryable`. `process_liveness` alive/dead/unknown; restart defers on anything but dead.
- Prior soft Improvements (0 GH reviews; POSIX start-key coverage) not addressed by this test-only push.
- Cursor Cloud Agents not used (plan gap); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: refreshed on `master` at `cos-feedback/agent-bridge/pr-36.md`.
