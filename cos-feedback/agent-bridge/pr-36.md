schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 36
head: 92664994f4025db2f2888d6637c904776c9c76b8
reviewed_utc: 2026-09-22T04:57:25Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 9266499 (Actions run 35688770674); author claims mutate_cea 10/10 caught + 18 self-tests + 2 controls exit 0, full suite 1329 passed, ruff/mypy clean; 0 GitHub PR reviews

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `92664994f4025db2f2888d6637c904776c9c76b8` (Actions run 35688770674). Wait for green before merge. If red, start with `test_ensure_adopt.py` (registered in `windows-ci.yml`) and the RealWatcher / Decision / StartKey cases that exercise adopt-vs-recycle and denied-is-not-dead.

## Improvements

- Soft: **0 GitHub PR reviews** on this tip. Confirm factory quorum / SOL visibility and CI green before merge.
- Soft: `ensure_watcher` lease clear on recycled uses bare `except Exception: pass` around `read_lease` / unlink (`bootstrap_session.py`). Prefer catching `(OSError, ValueError, TypeError)` (or whatever `read_lease` actually raises) so a real bug there is not swallowed.
- Soft: legacy / key-missing markers stay `already_running` with `identity: unverified` by design (fail closed). After rollout, consider a one-shot migrate that writes `watcher.identity.json` for a live adopted watcher so bootstraps stop treating pre-identity markers as forever-unverified.

## Notes

- CARD:C-ENSURE-ADOPT (class C). Branch `card/c-ensure-adopt`. +643/−12 across 8 files. MERGEABLE; mergeStateStatus UNSTABLE while CI runs.
- Root defect: `ensure_watcher` adopted ANY live pid in `watcher.pid` (recycled pid blocked all starts) and `is_process_alive` False on Windows DENY read as dead (duplicate start; C-F4). C-R1d removed the violent kill recovery, leaving adoption as the permanent target.
- Fix: watcher writes `watcher.identity.json` `{pid, start_key}` (Windows raw FILETIME ticks via `native_process_start_ticks`; POSIX `/proc` starttime field 22 via existing `_posix_process_identity` `creation_date`). `ensure_watcher`: equal key → adopt (`identity: match`); both keys readable and different → recycled (clear OUR pid/identity/lease naming that pid, start watcher, never touch the other process); missing key → keep + report (`unverified`); unqueryable → `already_running_unverifiable`, start nothing. `process_liveness` → alive/dead/unknown; restart path defers on anything but dead and clears identity with pid when dead. `_watcher_pid_liveness` asks module-local `is_process_alive` first so existing test seams still decide alive.
- Evidence claimed on `9266499`: `tools/mutate_cea.py` (JUnit-structured runner from mutate_cuj) 10 mutants caught, 18 self-tests + 2 controls as required, exit 0; full suite 1329 passed; ruff/mypy clean. Tests registered in Windows CI.
- Cursor Cloud Agents not used (plan gap); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-36.md`.