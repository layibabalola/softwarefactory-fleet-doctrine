schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 30
head: 010b9f2318be0c2595f50bfc2e64778c2bffd14c
reviewed_utc: 2026-09-21T17:31:59Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 010b9f23 (Actions run 35632426730); author local on tip ruff+mypy clean, mutate_cuj.py exit 0 (18 self-tests + 26 mutants CAUGHT + 3 controls as declared), full suite 1288 passed / 1 skipped; round-1 SOL CHANGES_REQUESTED + A3 advisories folded; prior CoS soft Improvements not addressed; 0 GitHub PR reviews; confirm factory quorum and green CI before merge

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `010b9f2318be0c2595f50bfc2e64778c2bffd14c` (Actions run 35632426730). Wait for green before merge. If red, start with `UpdateJsonTests` (incl. new offline/`exists()` case) / `test_read_modify_write_skip_sites.py` and `tools/mutate_cuj.py` `classify` (report-before-exit), then `core/storage.py` `update_json` / `read_json_for_update_or_skip` and watcher/wrapper RMW skip sites carried from C-F7b.

## Improvements

- Soft (carried from #28 / prior tip): `GuardTests` still forbids pairing writers with `load_seen` / `_load_wake_breakers` / `read_json` only — not `read_json_for_update` / `_load_seen_for_update`. A future revert of daemon RMW sites to the raise-on-busy reader would crash the watcher/wrapper during the ~90 s leftover-lock window instead of skipping. Behavioural race tests cover the known sites; the guard still does not pin the skip-reader contract.
- Soft (carried from #28 / prior tip): `_mark_tool_refresh_required` still audits `mcp_tools_refresh_required` and emits controls when the status file was busy and was not rewritten. Hosts can hear a refresh announce while on-disk status still lacks the flag until a later successful write; consider a follow-up retry write, or a distinct audit action when persistence was skipped (symmetry with `mcp_server_snapshot_read_skipped`).
- Soft (updated counts, still open): `tools/mutate_cuj.py` is author/local evidence only (18 self-tests + 26/26 + 3 controls on this tip); it is not invoked from Windows CI. Same pattern debt as other `tools/mutate_*.py` matrices — a CI-green tip can silently drop a mutant catch.
- Soft: 0 GitHub PR reviews on this tip. Author body claims C-F7b product fix unchanged since `c00bb88` plus C-UPDATE-JSON on the shared primitive. Still merge only after factory quorum is visible and CI is green.

## Notes

- Round-2 resubmit on tip `010b9f23` (delta vs prior CoS head `9c9058c`: +59/−2 across `test_read_modify_write_skip_sites.py` and `tools/mutate_cuj.py` only; product C-UPDATE-JSON / C-F7b code unchanged). Folds SOL-20260921-113603-688 CHANGES_REQUESTED and A3 advisories: (1) `classify()` checks usable JUnit report before exit code so missing report is always `NO_EVIDENCE` (new self-tests for exit 0 and exit 2 with no report); (2) real JUnit XML through `parse_junit` so `<error>` filed under failures cannot pass unnoticed; (3) new `UpdateJsonTests::test_an_entry_that_exists_but_reads_as_offline_is_never_overwritten` + harness row catches an `exists()`-says-missing shortcut that would overwrite an offline/dangling path.
- Supersedes closed PR #28 (CARD:C-F7b lineage). CARD:C-UPDATE-JSON: `core.storage.update_json` reads through `_read_object_unlocked` — only a genuinely missing file yields the default; other I/O errors raise and nothing is written; malformed/non-object still self-heal; undecodable bytes still raise. Hottest caller `watcher._save_watcher_state` already turns `OSError` into a logged skip.
- Carried C-F7b product fix (unchanged since `c00bb88`): daemon RMW sites skip a busy cycle via `read_json_for_update_or_skip` (watcher + server_wrapper); `mcp_server_snapshot_read_skipped` audit in `BRIDGE_PROTOCOL.md`.
- Harness: `tools/mutate_cf7b.py` removed; `tools/mutate_cuj.py` classifies from JUnit `<failure>` messages only; missing report → `NO_EVIDENCE`; mutant authority on tip is 26 rows (author notes round-1 "20"/"21" were miscounts).
- Files vs `master`: 7 files, +1152/−60. Ahead 9 / behind 0. MERGEABLE; mergeStateStatus UNSTABLE (CI pending). Base includes R-R3d (`e3c4365`).
- Lineage refs (author): `DECISION-LINEAGE-20260921-C-F7b.json`, `DECISION-C-F7b-3-f7aa0fd-CLOSED.json`.
- Prior CoS soft Improvements not addressed on this push (harness/test-only resubmit). Verdict unchanged: `merge-when-ci-green`.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: refreshed on `master` at `cos-feedback/agent-bridge/pr-30.md`.
