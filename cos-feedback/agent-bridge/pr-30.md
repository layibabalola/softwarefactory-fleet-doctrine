schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 30
head: 9c9058cf7b2c343eef40bc6bf157512548615c54
reviewed_utc: 2026-09-21T16:37:10Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 9c9058c (Actions run 35626615515); author local on merged tip ruff+mypy clean, mutate_cuj.py exit 0 (14 self-tests + 25 mutants CAUGHT + 3 controls as declared), full suite 1287 passed / 1 skipped; supersedes closed #28; 0 GitHub PR reviews; confirm factory quorum and green CI before merge

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `9c9058cf7b2c343eef40bc6bf157512548615c54` (Actions run 35626615515). Wait for green before merge. If red, start with `UpdateJsonTests` / `test_read_modify_write_skip_sites.py` (new, on the governed list) and `core/storage.py` `update_json` / `read_json_for_update_or_skip`, then watcher/wrapper RMW skip sites carried from C-F7b.

## Improvements

- Soft (carried from #28): `GuardTests` still forbids pairing writers with `load_seen` / `_load_wake_breakers` / `read_json` only — not `read_json_for_update` / `_load_seen_for_update`. A future revert of daemon RMW sites to the raise-on-busy reader would crash the watcher/wrapper during the ~90 s leftover-lock window instead of skipping. Behavioural race tests cover the known sites; the guard still does not pin the skip-reader contract.
- Soft (carried from #28): `_mark_tool_refresh_required` still audits `mcp_tools_refresh_required` and emits controls when the status file was busy and was not rewritten. Hosts can hear a refresh announce while on-disk status still lacks the flag until a later successful write; consider a follow-up retry write, or a distinct audit action when persistence was skipped (symmetry with `mcp_server_snapshot_read_skipped`).
- Soft: `tools/mutate_cuj.py` is author/local evidence only (14 self-tests + 25/25 + 3 controls); it is not invoked from Windows CI. Same pattern debt as other `tools/mutate_*.py` matrices — a CI-green tip can silently drop a mutant catch.
- Soft: 0 GitHub PR reviews on this tip. Author body claims C-F7b product fix unchanged since `c00bb88` (every reviewer from `002c41e` onward found it correct) plus C-UPDATE-JSON on the shared primitive. Still merge only after factory quorum is visible and CI is green.

## Notes

- Supersedes closed PR #28 (CARD:C-F7b lineage). New card CARD:C-UPDATE-JSON: `core.storage.update_json` no longer answers default for ANY `OSError` inside its lock — reads through `_read_object_unlocked` so only a genuinely missing file yields the default; other I/O errors raise and nothing is written; malformed/non-object still self-heal; undecodable bytes still raise. Hottest caller `watcher._save_watcher_state` already turns `OSError` into a logged skip (end-to-end test asserts no overwrite of `claude_monitor_escalations`).
- Carried C-F7b product fix (unchanged since `c00bb88`): daemon RMW sites skip a busy cycle via `read_json_for_update_or_skip` instead of saving defaults over real state (watcher + server_wrapper); `mcp_server_snapshot_read_skipped` audit registered in `BRIDGE_PROTOCOL.md`.
- Harness replaced: `tools/mutate_cf7b.py` removed (MUST_SAY unsound twice — stdout grep, then fail-open JUnit). `tools/mutate_cuj.py` classifies structurally from JUnit `<failure>` messages only, one message at a time; missing report → `NO_EVIDENCE`; self-tests classifier + `control_matches` before real rows; UpdateJson mutants assert data-loss FIRST.
- Files vs `master`: 7 files, +1095/−60 (`core/storage.py`, `watcher.py`, `server_wrapper.py`, new `test_read_modify_write_skip_sites.py`, new `tools/mutate_cuj.py`, windows-ci list +1, BRIDGE_PROTOCOL.md). Ahead 8 / behind 0. MERGEABLE; mergeStateStatus UNSTABLE (CI pending). Tip merge includes R-R3d (`e3c4365`) into `card/c-update-json`.
- Lineage refs (author): `DECISION-LINEAGE-20260921-C-F7b.json`, `DECISION-C-F7b-3-f7aa0fd-CLOSED.json`.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-30.md`.
