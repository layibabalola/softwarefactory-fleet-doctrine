schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 37
head: 615027578c672dd52f03b58cfa00caafba84cef5
reviewed_utc: 2026-09-22T07:14:30Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 61502757 (Actions run 35698548010); mergeable MERGEABLE / mergeState UNSTABLE; author local: mypy clean, smoke 153, full 513, wheel 3, ruff clean, mutation 27/27 killed; Class-C quorum on 6150275 A1/A3 APPROVE, A2 BLOCKER ruled out of scope (HOST-WALK-INT-GUARD), SOL APPROVE

## Blockers

(none)

## Improvements

- `server_wrapper.py` `_is_bridge_launcher_process`: replace `assert value is not None` on the `"script"` kind with an early `return False` (or explicit guard). `python -O` strips asserts; a defensive return keeps the fail-closed posture the rest of `_python_option_scan` already uses.
- Wait for Windows CI `test` green on head `615027578c672dd52f03b58cfa00caafba84cef5` before merge (0 GitHub reviews so far; CI is the independent test key).
- Banked A2 item HOST-WALK-INT-GUARD (non-numeric pid raise on the host walk) remains out of this card's KR-LINEAGE NARROW 2-1 scope — track as a follow-up card rather than expanding this PR.

## Notes

- Delta: `server_wrapper.py` (+211/−3) and `test_server_wrapper_phase2.py` (+393). Four commits on b56f973: script-position argv parse (8c79e94), `-m server_wrapper[_trampoline]` module launchers (642f911), CPython option terminators / fail-closed unknown options (10b0a62), check-hash exact value set + no `-X` value validation + PyManager aliases (6150275).
- Intent: replace bare substring `script_path in command_line` so a non-Python host carrying the trampoline path as a diagnostic arg is not skipped; walk through installed-package `-m` launcher chains; align option scan with CPython 3.14 refusals; never raise on hostile `-X`/selector values.
- Coverage looks strong: bar for bare-arg host, end-to-end `-m` chain (host 460), option-terminator matrix, never-raise property, PyManager alias positives and shape negatives, MSVCRT quoting splitter tests. Process tables mocked only (no live spawn), matching the round's safety rule.
- Soft watch: the never-raise harness includes megabyte-scale command lines (`1<<20` / `1<<19`); if Windows CI times out or OOMs, trim those extremes rather than weakening classifier coverage.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-37.md`.