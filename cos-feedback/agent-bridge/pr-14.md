schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 14
head: 35d82c9c6cec9cc1511d2153109178fee7c0450c
reviewed_utc: 2026-09-20T22:22:54Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 35d82c9c (run 35541481775); latest master Windows CI success on 8cd448f (run 35540722508); master also has post-merge #13 CI in progress. Await green `test` on this tip before merge.

## Blockers

(none)

## Improvements

- **Optional: apply `_backdate_mtime_ns` at any remaining seed-then-wait sites** in `test_server_wrapper_phase2.py` that still read `stat().st_mtime_ns` immediately after a write/copy before `wait_for_snapshot_ready`. This PR covers the seven sites that failed the same-tick measurement; a quick `rg` for `old_.*mtime` / `wait_for_snapshot_ready` on a follow-up pass would catch stragglers if any. Non-blocking.
- **Helper placement:** `_backdate_mtime_ns` lives on the test helper class beside `_write_and_wait_new_mtime` — good. If other test modules grow the same NTFS same-tick pattern, consider a shared test util later; out of scope here.

## Notes

- Single-file delta (+24/−7): `test_server_wrapper_phase2.py` only. Head `35d82c9c` (subject of DECISION-P-0b ADOPTED-LOCAL) re-derived on trunk `3903e69`; 1 commit; class B publication per 2026-09-08 integration-ref directive; `merge-tree` 0 conflicts.
- Mechanism: `wait_for_snapshot_ready` accepts a rewrite only when `st_mtime_ns` differs from the seeded baseline. NTFS advances mtime once per ~15.6 ms clock tick; wrapper baseline is taken before the child exists, so a same-tick rewrite can leave mtime unchanged and the wait times out despite a real rewrite. Measured 79/300 seed+`write_json` pairs kept identical mtime; 0/300 after backdating. Test-only; no assertion loosened; production wrapper unchanged.
- Helper `_backdate_mtime_ns(path, seconds=10)` uses `os.utime(..., ns=...)` (already imported) and returns the post-backdate `st_mtime_ns` used as the wait baseline. Wired into seven tests that seed/copy a snapshot then wait for rewrite.
- No GitHub reviews yet; MERGEABLE; checks in progress at review time.
- CoS via `gh` (no Cloud Agent — test-only, single-file, clear mechanism). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-14.md`.