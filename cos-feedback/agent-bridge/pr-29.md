schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 29
head: 589deef8e596f49406a5bda6f7662422a07349aa
reviewed_utc: 2026-09-21T15:56:36Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 589deef (Actions run 35622155228); author local on this tree ruff+mypy clean, smoke 129, listed suites 613+1 skip, wheel 3, test_agent_bridge 511, test_lane_coordination_fence 27 (25 fail on master), mutate_rr3.py 23/23 + --self-test; Class-C review claimed SOL/A1/A2/A3 APPROVE with no blocking finding; 0 GitHub PR reviews yet; confirm factory quorum and green CI before merge

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `589deef8e596f49406a5bda6f7662422a07349aa` (Actions run 35622155228). Wait for green before merge. If red, start with `test_lane_coordination_fence.py` (new, added to the governed list in `.github/workflows/windows-ci.yml`) and the three fence paths in `lane_coordination/coordination.py` (`renew_status` lower-generation refuse, `_read_status_strict` / `StatusUnreadableError`, per-seat `_status_lock` around renew / append / ack refresh / guard reads).

## Improvements

- Soft: `tools/mutate_rr3.py` is author/local evidence only (23/23 + `--self-test`); it is not invoked from Windows CI. Consider a governed or periodic mutate job so a CI-green tip cannot silently drop a mutant catch (same pattern debt as other `tools/mutate_*.py` matrices).
- Soft: `_renew_locked` still runs `reconcile_receipts` (all-seat journal reads) while holding the per-seat `_status_lock` (30 s timeout). A slow or contended reconcile widens the critical section for that seat's renew/append waiters; if this shows up under load, move reconcile outside the lock or shrink what must be locked with the generation compare-and-write.
- Soft: restated C-LOCKDIR-2 lock layers inside `lane_coordination` (stdlib-only, C0 §3.8) will drift from `core.storage.lock_dir` unless both stay in lockstep. Pin a short comment or a shared contract test that the first two layers stay equivalent when either side changes.
- Soft: 0 GitHub PR reviews on this tip. Author body claims Class-C SOL/A1/A2/A3 APPROVE on the R-R3d successor (lineage KR-LINEAGE NARROW 3-0). Still merge only after factory quorum is visible and CI is green.

## Notes

- Successor of the R-R3 lineage (CARD:R-R3d): closes three generation-fence bypasses reproduced on master `7ca81d9` — (a) `renew_status` no longer accepts a lower generation; (b) fence reads fail closed via `_read_status_strict` / `StatusUnreadableError` (only a genuine missing leaf is generation 1); (c) per-seat status lock covers renew compare-and-write, append fence+write, every `_refresh_ack_status` RMW, and `guard_once` status reads. Lock is ownership-based (thread lock + OS lock on never-deleted sentinel); no age eviction (answers SOL BLOCKER on round 1).
- Round 3 delta vs round 2 (`c112c83`): product change is a subtraction — `guard_once` keeps the claim once a takeover renew has written (matches master's one-bump-per-lease throttle). Empty `except BaseException: raise` after the append documents that intentional non-release.
- Files vs `master`: 4 files, +1032/−32 (`lane_coordination/coordination.py`, new `test_lane_coordination_fence.py`, new `tools/mutate_rr3.py`, CI list +1). MERGEABLE; mergeStateStatus UNSTABLE (CI pending). Base tip `ad5657e` (C-LOCKDIR-2 merged).
- Named debt left open by design (also on master): **CARD:R-GUARD-1** (same-gen renew in revalidate→takeover window; takeover append fail leaves gen bump without journal row; claim leak on malformed post-claim field); **R-R2-VERIFY** (cross-process unlocked-append closure inferred, not measured); flake card on `test_persistent_permission_error` (1/2 A1 full runs).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-29.md`.
