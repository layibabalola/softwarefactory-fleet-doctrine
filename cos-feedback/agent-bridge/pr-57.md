schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 57
head: 1a4d61b313f315bc86ae6e771cd4e69c9742c978
reviewed_utc: 2026-09-26T05:11:57Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 1a4d61b (Actions run 36219992237 / job 108343310684; Ruff+Mypy SUCCESS, Phase smoke tests IN_PROGRESS); MERGEABLE / mergeState UNSTABLE; draft=false; 0 GitHub reviews; ahead 3 / behind 0 vs master

## Blockers

- Wait for Windows CI `test` green on head `1a4d61b313f315bc86ae6e771cd4e69c9742c978` (Actions run 36219992237) before merge — tip job still IN_PROGRESS at review time (Phase smoke tests; Full bridge suite / Wheel install / governed suites / pip-audit / SBOM still pending).

## Improvements

- `test_wheel_install.py` `_canonical_spec`: the apostrophe case locks "body has `'` → keep double quotes", but the branch that keeps single quotes when the body holds a `"` (docstring + S1-DEBT-3 claim) has no dedicated assert. Add one pin like `foo>=1; platform_release == 'say "hi"'` expecting `>=1;platform_release=='say "hi"'` so that rewrite path cannot regress silently.
- `test_audit_gate.py` `test_empty_skip_reason_is_a_named_skip_not_stale` parametrizes non-string falsy values (`0`, `False`, `[]`) that pip-audit JSON will not emit; fine for mutant killing, but if the contract is "string skip_reason only", consider asserting type-fail-closed for non-strings instead of renaming them to `(no reason given: …)` so malformed reports surface as shape errors.

## Notes

- Scope: +78/−10 across 7 files on `card/p11-debt` (3 commits; ahead 3 / behind 0 vs `master` `e887b681`). Tip `1a4d61b313f315bc86ae6e771cd4e69c9742c978` is post-v0.2.0 debt bundle (class C): P11-DEBT-1/2 release-doc alignment, S1-DEBT-2/3 wheel-test canonicalisation, S4-DEBT-1/2 + S4FM-DEBT-1 audit-gate skip_reason tightening.
- Commits: `7bab7fc2` SECURITY.md 0.2.x + tag `v0.2.0` / SIGNING_APPLICABILITY cites measurement tip `3f504de` and release subject `55623c7`; `9e7445af` option-line error names the option + quote-aware `_canonical_spec`; `1a4d61b3` `skip_reason is not None` gate, RELEASE_LOCK regen note, known-findings trailing newline.
- Relation to #56 CoS Improvements: this tip directly lands both open notes from `cos-feedback/agent-bridge/pr-56.md` (SECURITY 0.1.x → 0.2.x wording; SIGNING stale tip re-cited). Treat #57 independently; those #56 Improvements are satisfied on this head.
- Spot-checks: option branch message is `pip option line {option!r} is not a pin…` and new test asserts that substring; `_canonical_spec` rejects unterminated literals; audit_gate empty/whitespace/`None`-absent distinction is correct (`is not None` vs truthiness); known-findings remains `findings: []`. CoS did not run the suite or pip-audit on this Linux box (factory floor is Windows); PR body claims local 1485 passed / 1 skipped, ruff+mypy clean.
- Open siblings: #36 still open on `card/c-ensure-adopt`. #56 no longer open (base `e887b681` is post-P-11 land).
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-57.md`.