schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 31
head: 2a0cf6dc562590808dc1d86f13964faf3d538abb
reviewed_utc: 2026-09-21T17:53:53Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 2a0cf6dc (Actions run 35634848440); author claims local CI green + mutate 14/14 + natural probe 0/500 on tip; Class C 4/4 APPROVE on 2a0cf6d (three Sonnet + SOL); 0 GitHub PR reviews; confirm factory quorum and green CI before merge

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `2a0cf6dc562590808dc1d86f13964faf3d538abb` (Actions run 35634848440). Wait for green before merge. If red, start with `test_lane_coordination_fence.py` (`GuardTakeoverIsOneLockedCompareAndSwap` / retargeted takeover spies on `_renew_locked` / `_append_fenced`), then `lane_coordination/coordination.py` `guard_once` one-hold CAS path (`_revalidation_refusal`, `_liveness_key`, `_takeover_row_absent`, `_restore_status`, `_lease_of`).

## Improvements

- Soft (author named debt): comment near the post-write claim-keep / restore path in `lane_coordination/coordination.py` is imprecise under the one-token R-GUARD-1b ruling; clarify in a follow-up that restore runs only when `_takeover_row_absent` is true, and that control exceptions during restore propagate while ordinary restore failures still surface the append error.
- Soft (author named debt): no dedicated `GeneratorExit` test on the restore `except Exception:` fence (R-GUARD-1b covers `KeyboardInterrupt` / `SystemExit` only). A follow-up that injects `GeneratorExit` during `_restore_status` would pin that control exceptions are never swallowed.
- Soft (author named debt / base parity): when the append error was raised with an explicit `from` cause, a chained restore failure is not displayed in the traceback (same on base `e3c4365`). Consider `raise append_error from None` then re-attach, or documenting the display gap.
- Soft: mutation / natural-probe evidence (`mutate_rg1_round3_R-GUARD-1b`, sol1 natural probe) is author/local only — not invoked from Windows CI. Same pattern debt as other `tools/mutate_*.py` matrices; a CI-green tip can silently drop a mutant catch.
- Soft: 0 GitHub PR reviews on this tip. Author body claims Class C 4/4 APPROVE on `2a0cf6d`. Still merge only after factory quorum is visible and CI is green.

## Notes

- CARD:R-GUARD-1 / R-GUARD-1b on tip `2a0cf6dc` (branch `card/r-guard-1`). Pays R-R3 lineage named debt SOL-1/2/3 on base R-R3d (`e3c4365` / #29). Three commits: round 1 CAS one-hold (`4bed2c9`), round 2 restore-only-when-row-absent + falsy-lease fail-closed (`5202891`), R-GUARD-1b one-token `except Exception:` so control exceptions during restore propagate (`2a0cf6d`).
- Product (all in `lane_coordination/coordination.py` + export): revalidation, liveness compare, `_renew_locked`, and `_append_fenced` HEARTBEAT-TAKEOVER share one `_status_lock(root, "implementer")` hold; compare keys generation/holder/lease/heartbeat_target; append failure restores prior status only when journal shows no HEARTBEAT-TAKEOVER at the new generation; claim kept after renew write (S2-repeat throttle); non-object leases (falsy included) raise `StatusUnreadableError`; torn status fails closed and is never rewritten.
- Tests: 3 files, +535/−82 vs `master` (`lane_coordination/__init__.py`, `coordination.py`, `test_lane_coordination_fence.py`). New `GuardTakeoverIsOneLockedCompareAndSwap` plus retargeted spies (renew/append no longer go through public `renew_status`/`append_event`). Thread test asserts live renew blocks for the whole takeover hold then is fenced.
- Ahead 3 / behind 0. MERGEABLE. Base includes R-R3d. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-31.md`.
