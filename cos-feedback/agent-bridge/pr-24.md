schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 24
head: e5c78917920e4b6aa777d3e1208a8daab395f982
reviewed_utc: 2026-09-21T08:54:46Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head e5c78917 (Actions run 35580314418); 0 GitHub reviews; PR body reports SOL/A1/A2/A3 APPROVE round 2 (SOL-20260921-034153-448) and local bar 1173 passed / 0 failed without PYTHONPATH

## Blockers

(none)

## Improvements

- `docs/internal/BRIDGE_COMPONENT_SUPERVISION_SPEC.md` registry row 2 still says "never kills since C-R1c" while the landed card id is C-R1d — align the card label on the next docs touch (editorial only).
- `docs/internal/audit-profiles/component-supervision.md` labels the no-kill proving rule as CARD:C-R1c; prefer C-R1d to match the proving file and PR title (editorial only).
- Follow-up priority: CARD:C-ENSURE-ADOPT (`ensure_watcher` adopts any live pid in `watcher.pid`; `is_process_alive` treats access-denied as dead). With the kill removed, a recycled live pid becomes a permanent no-watcher outage until an operator clears the files — author carded this; do not regress the no-kill rule while fixing it.

## Notes

- Scope: 8 files, +324/−14 on `card/core-r1d-no-classifier` tip `e5c78917920e4b6aa777d3e1208a8daab395f982` (4 commits: C-R1c → C-R1c r2 → C-R1d → C-R1d r2). Production change is `bootstrap_session.restart_watcher_for_code_change` only; specs + audit profile + tests + Windows CI suite list updated.
- Intent (CARD:C-R1d / second KR-LINEAGE ruling): stop force-killing any live pid named by a possibly stale `watcher.pid` or lease (`taskkill /T /F` tree-killed recycled pids / decoys). Live pid → keep both files, `outcome=deferred_to_watcher_self_restart`, never call `_terminate_process`; dead/no pid → clear files as before. Operator note must not instruct kill/stop/terminate. Relies on existing watcher self-restart (`_watcher_self_restart_status` + trampoline).
- Lineage: three parked successors that tried to identify the pid before killing (cmdline match, recorded identity, start-time-vs-mtime) — CoS agrees removing the guess is the right shape; residual adoption hole is pre-existing and out of scope here.
- Proving tests: new `test_bootstrap_no_kill.py` (9 arms: 7 Windows real-process with child trees + 2 cross-platform note/classifier-absence). Wired into `.github/workflows/windows-ci.yml` Remaining governed suites. Existing `test_agent_bridge.py` / `test_dr2a_component_supervision.py` contracts flipped from kill-required to never-kill.
- CoS spot-check of tip: live-pid early return sets `stopped: False` and never reaches `unlink` / `_terminate_process`; dead path sets `stopped: False` and clears both files; `_started_after` / `_process_start_epoch` / `process_identity` absent from `bootstrap_session` in this head (classifier gone). Did not re-run Windows real-process arms on this Linux host — GitHub Windows CI is the independent proof key.
- Separate card called out by author (unchanged here): `sweep_orphan_watchers` substring kill.
- Cursor Cloud Agent not used; review via `gh` (focused bootstrap safety + contract change). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-24.md`.