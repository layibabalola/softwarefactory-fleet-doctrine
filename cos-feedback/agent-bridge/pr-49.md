schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 49
head: 91d1e0cea169d479d99d5a74b7cebf05c02a3c08
reviewed_utc: 2026-09-25T22:05:29Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 91d1e0ce (Actions run 36194902971); MERGEABLE / mergeState UNSTABLE; draft=false; 0 GitHub reviews

## Blockers

- Wait for Windows CI `test` green on head `91d1e0cea169d479d99d5a74b7cebf05c02a3c08` (Actions run 36194902971) before merge — tip job still IN_PROGRESS.

## Improvements

- `test_demo_handles_exception_whose_str_raises`: in text mode, assert stdout contains the `_describe` fallback (`UnprintableError: unprintable error`) so a silent empty FAILED line cannot pass.
- Consider exporting a public alias (or documenting `_safe_cli_print` as a shared CLI helper) if more entrypoints will import it; private import from `bridge` is fine for this narrowly scoped demo fix.

## Notes

- Scope: +123/−6 across 2 files on `card/jh1-demo-safe-print` (2 commits; ahead 2 / behind 0 vs `master`). Tip `91d1e0cea169d479d99d5a74b7cebf05c02a3c08` is JH1-DEMO-SAFE-PRINT-2 after JH1-DEMO-SAFE-PRINT (`e6675104`).
- Files: `agent_bridge/demo.py` (+23/−6), `test_phase0_contract.py` (+100/−0).
- JOURNEY-HUNT-1 S1: failure and success CLI paths now call existing `_safe_cli_print` (defined on master in `bridge.py`); new `_describe` contains `Exception` from `__str__` and returns `<Type: unprintable error>`, while `KeyboardInterrupt` / `SystemExit` / other non-`Exception` `BaseException`s still propagate (pinned in both text and `--json` modes).
- Contract tests added for cp1252 stdout with non-ASCII error text, unprintable `__str__`, and BaseException propagation from bridge calls and from `__str__`. PR claims tests fail at base `e5cb827` and pass on tip; local matrix (utf-8 / cp1252 / PYTHONUTF8 / short username / space in basetemp) reported green.
- PR body: Class C publication review 4/4 APPROVE (3 Sonnet adversaries + other-family Codex). No blocking findings claimed.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-49.md`.