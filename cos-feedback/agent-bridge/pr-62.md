schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 62
head: c8710ed4fcebe293cc4a13c7e2fe9354bb46dad9
reviewed_utc: 2026-09-26T17:49:46Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: MERGEABLE/UNSTABLE; Windows CI `test` IN_PROGRESS on tip c8710ed4 (Actions run 36260374345 / job 108454918945); draft=false; 0 GitHub reviews; ahead 2 / behind 0 vs master

## Blockers

(none)

## Improvements

- Follow-up already named in the PR body: **HOST-RECURSION-DEBT-3** — a node-count refusal still reuses the depth-worded message (`"provider activation ledger exceeds maximum nesting depth of %d" % MAX_STRUCTURE_DEPTH`). Keep that as a separate card; do not widen this PR.

## Notes

- Scope: +35/−0 in `test_provider_activation.py` on `card/host-recursion-debt-2` (2 commits; tip `c8710ed4fcebe293`). Class-A pin for the node-count half of `_depth_bounded` (`MAX_STRUCTURE_NODES = 100_000`): `test_wide_shallow_activation_ledger_reaches_the_node_count_guard` builds a depth-2 activation ledger with padding in a key `_validate_ledger` never reads, so only the node bound can refuse it.
- Spot-check on tip: refused fixture uses `padding = [0] * (MAX_STRUCTURE_NODES - 4)` → root + four top-level values + (N−4) ints = N+1 nodes; admitted uses `MAX_STRUCTURE_NODES - 5` → exactly N. Guard check is `nodes > max_nodes` (not `>=`), so +1/−1 and `>` vs `>=` all kill the test. Asserted error string matches `_load_strict` depth-worded refusal. No production code change.
- Round 1 → round 2: docstring/boundary tightened from loose +5 padding to exact N+1 / N (A3 BLOCKER + A2 DEBT from r1). PR body: A1/A2/A3 APPROVE + SOL APPROVE (receipt `SOL-20260926-124511-142`); suite 56/56 under Python 3.14 + install-manager shim; ruff+mypy clean. CoS did not re-run the suite on this Linux box (factory floor is Windows).
- Sibling #61 (HOST-RECURSION-DEBT-1, depth-error pin) already merged 2026-09-26T14:33:30Z. Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
