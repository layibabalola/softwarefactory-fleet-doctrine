schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 33
head: 12a28958ff37ca2f78866a6d16a4b79adb2d08bb
reviewed_utc: 2026-09-21T20:16:58Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 12a28958 (Actions run 35649938082); author claims local base-interpreter CI green + Class-C quorum (SOL + three Sonnet APPROVE); 0 GitHub PR reviews; tip is ahead 5 / behind 3 vs master (behind is merged #32 P-5)

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `12a28958ff37ca2f78866a6d16a4b79adb2d08bb` (Actions run 35649938082). Wait for green before merge. If red, start with the new pause-read suite in `test_phase0_contract.py`: `test_07g2`–`test_07g10` (truncated state, unreadable shapes, ambiguous winerrors 53/67/3/15/161, missing parent dir, POSIX-shape missing parent, winerror-53-vs-missing-lstat, positive-control missing leaf, state-dir-is-a-file / `S_ISDIR`).

## Improvements

- Soft: **0 GitHub PR reviews** on this tip. Author body claims Class-C quorum (SOL APPROVE + three Sonnet APPROVE) with decisions `DECISION-LINEAGE-20260921-P-12-PUB-NARROW.json` and `DECISION-P-12-PUB2-12a28958.json`. Still confirm factory quorum is visible and CI is green before merge.
- Soft: tip is **behind 3** on `master` (merged #32 P-5 / `eea6fd3`). Content does not touch those files, but rebase or merge master before landing so the publish tip is current.
- Soft: when `state_dir` exists but is not a directory, `_note_pause_read` still logs the original `FileNotFoundError` from opening `state.json` rather than a clearer non-directory signal. Fail-closed behavior is correct (`test_07g10`); log fidelity only.

## Notes

- CARD:P-12-PUB2 publishes ADOPTED-LOCAL P-12 (`0e0d654`) + P-12-cov (`4c847f0`) plus P-12-PUB round-2 (`e9f6eeec`) and the PUB2 positive-proof gate (`12a28958`). Branch `card/p-12-pub2`. Ahead 5 / behind 3 vs `master`. MERGEABLE.
- Diff vs master: `watcher.py` +54/−3; `test_phase0_contract.py` +270/−0. Product change is `_bridge_is_paused` fail-closed: unpaused only when `os.stat(state_dir)` proves a directory **and** `core.storage._leaf_is_genuinely_missing` accepts the leaf as absent; truncated / empty / non-object / non-UTF-8 / unreadable / ambiguous Windows `FileNotFoundError` / missing parent / non-dir path all read paused. Transition logging via `_note_pause_read` (once per readable↔unreadable edge).
- Why the extra `os.stat` gate: `_leaf_is_genuinely_missing` assumes a held lock already created the parent (CARD:C-F7d writers). The watcher holds no lock, so a missing `state_dir` must not fall through to “unpaused”.
- Round lineage: P-12 (truncated → no wake, queue + replay); P-12-cov (log once per transition); P-12-PUB r2 (ambiguous winerrors fail closed); P-12-PUB2 (positive proof of dir + genuinely-missing leaf).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-33.md`.