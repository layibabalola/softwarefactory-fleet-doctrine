schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 15
head: 13f76b2b8546e5dd5fb627fcebc56f24ef1e7035
reviewed_utc: 2026-09-20T22:29:14Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 13f76b2b (run 35541774711): Ruff + Mypy baseline PASS, Phase smoke tests in progress; remaining suite steps pending. Latest settled master Windows CI success on 8cd448f (run 35540722508); master tip a3329f9 (#14 merge) still has post-merge CI in progress. Await green `test` on this tip before merge.

## Blockers

(none)

## Improvements

- **Timing-bound socket tests on loaded Windows CI:** `test_dashboard_server_rejected_mutation_slowloris_is_bounded` and `..._trickle_body_is_bounded` assert close within `<5.0s` against a 1.5s drain deadline. On a heavily loaded Windows runner those bounds can flake without a product regression. Consider pinning the critical contracts (ordering, `read1` vs `read`, remaining-bound) via the existing deterministic unit tests and treating live-socket timing as soft/diagnostic if CI noise appears. Non-blocking.
- **Mutant A3 undeclared (intentional):** cumulative deadline check moved after `read1` is left unpinned per the cov commit — acknowledged trade-off; no action required unless a deterministic shape-stable pin becomes available.
- **Server-wide thread exhaustion remains out of scope:** per-connection drain is bounded, but `ThreadingHTTPServer`'s unbounded thread-per-connection model is unchanged. A future card could add a connection/backlog limit; not this PR's job.

## Notes

- Two-file delta (+913/−4): `dashboard_server.py`, `test_agent_bridge.py`. Head `13f76b2b` (subject of DECISION-DASH-10053b-cov ADOPTED-LOCAL) re-derived on trunk `a3329f9`; 4 commits; class B publication; `merge-tree` 0 conflicts.
- Mechanism: auth/CSRF header-only gates first; on 401/403, `_drain_rejected_body` runs after the rejection response. Drain uses `rfile.read1` (DASH-10053b: `read` allowed trickle past the cumulative deadline), chunked at `min(remaining, DASHBOARD_MAX_BODY_BYTES)`, wall-clock `DASHBOARD_DRAIN_TIMEOUT_SECONDS=1.5`. Per-request `_dashboard_body_drain_done` reset in `do_POST`.
- Security: blocked predecessor drained before auth (pre-auth hang); this lineage does not reopen that. Cov pin asserts each `read1` is bounded by remaining declared length (kills mutants A1/A2).
- Documented intentional gaps: malformed Content-Length and incomplete body within deadline can still risk WSAECONNABORTED — same as 2438ff5; no safe bound.
- No GitHub reviews yet. CoS via `gh` (no Cloud Agent — security-sensitive but mechanism + mutation-kill tests are clear and self-documented). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-15.md`.
