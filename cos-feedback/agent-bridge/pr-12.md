schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 12
head: 9d83d025716f8c8f7e9bf3cd42e36be836fbeec0
reviewed_utc: 2026-09-17T14:17:40Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` FAILURE on head 9d83d025 (Actions run 35232438434, ~3s, empty steps); annotation says job never started — GitHub Actions billing/spending limit. No test evidence from CI. Author local bar 669 passed / 1 skipped (explained) / 70 subtests at 9d83d02; Class-C round 2 (A1/A2/SOL) adopted.

## Blockers

- **CI: Windows CI `test`** — FAILURE on head `9d83d025716f8c8f7e9bf3cd42e36be836fbeec0` (Actions run 35232438434). Job annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." Fix billing/spending limit in GitHub settings, re-run the workflow on this head, and merge only when `test` is green on matched tip `9d83d025` (author gate). Not a suite failure — no pytest ran.

## Improvements

- **Follow-up card P-0c-cov (A3):** per-site read-side mutation coverage for the five CIM call sites beyond `core.processes._cim_process_entry` (bootstrap, server, server_wrapper, server_wrapper_trampoline, and `test_win_process._cim_process_table`). Arm (c) today only mutates the `core.processes` path; other sites are covered by code review + the shared helper pin, not by independent redden proofs. Non-blocking for this PR if filed as stated.

## Notes

- Eight-file delta (+598/−15): `powershell_runtime.py`, `core/processes.py`, `bootstrap_session.py`, `server.py`, `server_wrapper.py`, `server_wrapper_trampoline.py`, `test_agent_bridge.py`, `test_win_process.py`. Ahead 2 / behind 0 vs `master`. MERGEABLE; mergeStateStatus UNSTABLE solely from the failed check.
- Defect: CIM child had no output-encoding pin; callers used `text=True` (locale/cp1252). Non-ASCII CommandLine (e.g. U+2014) best-fit-mapped, disagreed with native PEB, and fingerprint comparators scored pid-reuse → false kill.
- Fix (coupled halves): `powershell_cim_command` prefixes `$OutputEncoding=[Console]::OutputEncoding=[Text.UTF8Encoding]::new($false);`; all six product CIM sites decode with `encoding="utf-8"`. POSIX `ps` sites correctly left as `text=True`.
- Round 2 (on top of `4a9d16d`): bootstrap `_enumerate_watcher_processes` now guards `stdout is None` / decode failure inside the same `except Exception` as the other CIM sites (r1 blocker); setUpClass fail-not-skip; mutation-b patches only `_OUTPUT_ENCODING_PIN`; dead-fallback fails (not skips) and checks `$PSVersionTable` identity `5|Desktop`; CIM-expression-only ValueError guard for leading `#` / `param()`.
- Tests: live non-ASCII CIM==native parity; write- and read-side mutation arms; console-less spawn under pwsh + PS 5.1; deterministic bootstrap `stdout=None` regression; live WatcherEnumerationDecodeFailureTests (honest skip when process tree does not reproduce corruption).
- CoS via `gh` (Cloud Agents unavailable on plan). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-12.md`.