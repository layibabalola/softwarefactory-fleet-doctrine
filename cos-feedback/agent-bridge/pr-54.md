schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 54
head: 6fd5de4d1447c4b5f5268e0dbb4539c66352de22
reviewed_utc: 2026-09-26T02:41:03Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 6fd5de4d (Actions run 36212410788 / job 108321566845); MERGEABLE / mergeState UNSTABLE; draft=false; 0 GitHub reviews; ahead 1 / behind 0 vs master

## Blockers

- Wait for Windows CI `test` green on head `6fd5de4d1447c4b5f5268e0dbb4539c66352de22` (Actions run 36212410788) before merge — tip job still IN_PROGRESS (audit + remaining suites / SBOM still pending at review time).

## Improvements

(none)

## Notes

- Scope: +821/−0 across 7 files on `card/s4-dep-audit` (1 commit; ahead 1 / behind 0 vs `master` `92ea5af1`). Tip `6fd5de4d1447c4b5f5268e0dbb4539c66352de22` is S-4 DEP-AUDIT: hash-pinned `pip-audit==2.10.1` over the S-2 release lock, gated by recorded release blockers (not waivers). Base already carries merged S-2 (#52) and S-3 (#53); gate imports `parse_lock` / `canonical_name` from `tools/sbom_bind.py`.
- Files: `tools/audit_gate.py` (+151 fail-closed coverage + known-findings + `--release`), `test_audit_gate.py` (+140, 16 tests added to Remaining governed suites), `tools/audit-tool.in` (+3), `tools/audit-tool-lock.txt` (+427 hash lock), `tools/audit-known-findings.json` (+32 three mcp 1.27.0 PYSEC entries → S4-FIX-MCP), `.github/workflows/windows-ci.yml` (+35 Dependency vulnerability audit + artifact upload before S-3 SBOM), `docs/internal/RELEASE_LOCK.md` (+33 audit section).
- Intent: install scanner with `--require-hashes` in its own venv; `pip-audit -r requirements-lock.txt --require-hashes --disable-pip` (accept exit 0/1 only); `audit_gate.py` requires exact lock coverage, every finding recorded with reachability + `blocker_card`, and fails on stale entries; `--release` fails while any entry remains (P-11). First scan on `92ea5af1` recorded PYSEC-2026-3481/3482/3483 on `mcp==1.27.0` (not reachable over stdio; release blockers pending S4-FIX-MCP). Live advisory DB intentionally turns the step red on new advisories with no code change.
- Spot-checks: CI step order keeps SBOM last so `dist/` cannot taint suites; gate de-dupes duplicate pip-audit rows; known-findings require non-empty package/version/id/reachability/blocker_card; `test_tracked_record_is_well_formed` loads the committed JSON. CoS did not re-run `pip-audit` or Windows install on this Linux box (factory floor is Windows/CPython 3.14). Recorded mcp findings are release blockers for P-11, not merge blockers for this PR once CI is green.
- Open siblings: #36 still open on a different branch; no stack conflict with this tip.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-54.md`.