schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 47
head: a30c128e78c63465223c608935c1b0f81a9544f8
reviewed_utc: 2026-09-25T16:04:20Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head a30c128e (Actions run 36158212752); Ruff + Mypy SUCCESS; Phase smoke in progress; remaining governed suites (incl. new test_provider_activation.py) still pending; MERGEABLE / mergeState UNSTABLE; draft=false; 0 GitHub reviews

## Blockers

- Wait for Windows CI `test` green on head `a30c128e78c63465223c608935c1b0f81a9544f8` (Actions run 36158212752) before merge — tip run still IN_PROGRESS past Ruff/Mypy.

## Improvements

- Consolidation debt (likely among PQP-DEBT-1..5): `_read_qualification_snapshot` / `_consume_qualification_evidence` intentionally duplicate `ProviderRuntimeStore` qualification read+consume so `core/provider_runtime.py` and `core/storage.py` stay untouched. When a follow-up card is allowed to touch those modules, fold the lock-free read and the embedded `profile_key` identity check into the shared path so the two consumers cannot drift.
- `evaluate_many` remains dropped (no in-repo caller). If a multi-grant dispatch consumer lands later, restore it behind the same confirm/session and depth-bound contracts rather than adding an unbounded sibling.
- Tool-count assertion moved 89→93 in `test_agent_bridge.py`; keep that paired with any future MCP surface change so a silent tool drop cannot land.

## Notes

- Scope: +2266/−3 across 6 files on `card/provider-qual-port-2` (3 commits; ahead 3 / behind 0 vs `master`). Tip `a30c128e78c63465223c608935c1b0f81a9544f8` is CARD:PROVIDER-QUAL-PORT-2 evidence-identity fix after round-2 A1/SOL/A3/A2 closures on 3700eff2.
- Files: `core/provider_activation.py` (+785 new), `test_provider_activation.py` (+1116 new, ~39 test fns / 54 collected with params per PR body), `agent_bridge/bridge.py` (+228/−1), `server.py` (+130), `test_agent_bridge.py` (+6/−2), `.github/workflows/windows-ci.yml` (+1 adds suite to Remaining governed suites).
- Four MCP tools: `provider_activation_status` (READ_ONLY), `issue_provider_role_grant`, `transition_provider_role_grant`, `evaluate_provider_dispatch` (writes only with trusted local `confirm=True` + active locally-bound session; `record=False` stays read-only and lock-free for qualification).
- Protected invariants claimed and verified byte-identical to `master`: `core/storage.py`, `core/provider_runtime.py`, `core/agent_registry.py`, `agent_bridge/session_service.py`. Shared `as_dict` in `server.py` unchanged; round-2 moved the RecursionError/type-only refusal into `_provider_tool_bounded` wrapping only the four ported tools.
- Security posture reviewed on tip: parse-time RecursionError→`ProviderActivationError` in `_load_strict`; iterative `_depth_bounded` (ancestor-path cycles, 200 depth / 100k nodes, non-string keys refused) before deepcopy/asdict handoff; per-verb error boundaries (type-only leak for unexpected Exception); `allowed_operations`/`prohibited_operations` must be `list[str]`; terminal states checked independent of `_TRANSITIONS`; embedded qualification `profile_key` mismatch/absent → UNKNOWN/malformed (never ELIGIBLE).
- PR body: class-C quorum + publication round 1 on this exact head both 4/4 APPROVE; non-blocking debt tracked as PQP-DEBT-1..5 (not located as in-tree artifacts via code search this pass).
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-47.md`.
