schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 59
head: 4f76135dab6a306b103f89cc4e659204afe32086
reviewed_utc: 2026-09-26T06:46:07Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 4f76135 (Actions run 36224705168 / job 108356356198); MERGEABLE/UNSTABLE; draft=false; 0 GitHub reviews; tip ahead 1 / behind 0 vs master

## Blockers

(none)

## Improvements

- `test_provider_activation.py` `test_deeply_nested_provider_activation_ledger_is_bounded_refusal` docstring still says "not the 5000-deep fixture" and reasons about "at 5000 levels `json.loads` parses fine". After HOST-RECURSION-1 the shared fixture is `_DEEP_FIXTURE_DEPTH` (2000). Retarget that prose to the named constant (or "shared deep fixture") so the A1 history note does not drift from the live depth.

## Notes

- Scope: +35/−9 in 1 file (`test_provider_activation.py`) on `card/host-recursion-1` (1 commit `4f76135d`). HOST-RECURSION-1: shared deep-JSON fixture depth 5000 → 2000 so fixture build/round-trip stays stack-independent under the Python Install Manager shim (1 MB native stack reserve; overflow near ~4900), while real pythoncore / CI (3 MB) were already fine. Adds `test_deep_fixture_is_non_vacuous` pinning depth ≥ 10× `MAX_STRUCTURE_DEPTH` (200), depth > `sys.getrecursionlimit()`, and `json.loads`/`json.dumps` round-trip plus both dict-wrapped ledger builders.
- Spot-checks: `_DEEP_FIXTURE_DEPTH = 2000` still 10× past the depth guard and past default Python recursion (so `dataclasses.asdict` path stays exercised); `_A1_REPRO_DEEP_JSON` remains 20000 and is written as raw text (not round-tripped), so the A1 parse-time RecursionError contract is unchanged; dict-wrapped helpers correctly document why they use the shared fixture rather than A1. CoS did not run the suite on this Linux box (factory floor is Windows).
- Open siblings: #36 still open on `card/c-ensure-adopt`; #57 may still be open (P11/S1/S4 debts) depending on merge state at read time.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-59.md`.