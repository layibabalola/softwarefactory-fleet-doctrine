schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 26
head: 8bd157029cb2b89680cf5f161be15a8f6b8aefad
reviewed_utc: 2026-09-21T11:33:56Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 8bd1570 (Actions run 35594603047); author local bar 1210 passed / 1 skipped without PYTHONPATH; Class C CARD:C-LOCKDIR-1 r2 SOL/A1/A2/A3 APPROVE (SOL-20260921-062417-072)

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `8bd157029cb2b89680cf5f161be15a8f6b8aefad` (Actions run 35594603047). Wait for green before merge; if red, start with `test_recover_state_lock.py` (new file, added to the Windows CI shard list) and any `recover_state` / `core.storage.lock_dir` regressions.

## Improvements

- Soft: `test_the_timeout_reason_text_is_unchanged` monkeypatches with `def quick(state_dir, timeout=30.0)` while product `_state_locked` is now `timeout: Optional[float] = None` (default ~150 s). Align the stub signature so a future reader does not think the product default is still 30 s.
- Soft: add one assertion that `BRIDGE_LOCK_STALE_SECONDS == DEFAULT_LOCK_STALE_SECONDS` (or that recovery's `stale_seconds` equals the bridge constant) so a future split of those aliases fails the shared-threshold suite loudly; today the suite only pins recovery to `DEFAULT_LOCK_STALE_SECONDS`.

## Notes

- Four-file delta (+297/−26): `recover_state.py` (+28/−26), `test_recover_state_lock.py` (+121 new), `tools/mutate_lockdir1.py` (+147 new), `.github/workflows/windows-ci.yml` (+1). Ahead 2 / behind 0 vs `master`. MERGEABLE; mergeStateStatus UNSTABLE from pending check.
- Intent (CARD:C-LOCKDIR-1): delete recovery's second lock copy (60 s stale) and delegate `_state_locked` to `core.storage.lock_dir` with `DEFAULT_LOCK_STALE_SECONDS` (120 s, same value as `BRIDGE_LOCK_STALE_SECONDS`). Round 2: default recovery wait is `DEFAULT_LOCK_STALE_SECONDS + 30` so a repair started soon after a crash can outlast the threshold instead of hanging 30 s and failing `lock_timeout`. Explicit timeout still honoured. Docstring no longer claims compact.py takes `state_dir/.lock`.
- Declared behaviour: recovery no longer takes a 60–120 s-old lock; gains Windows error-5 retry; release swallows any `OSError`; default `--repair` against a live bridge can wait up to ~150 s (operator-only path; non-forced repair already refuses live processes).
- Out of scope (panel split): stale-takeover race (C-F2) and late holder deleting a successor's lock (C-F3) — separate head-to-head round.
- Evidence: `tools/mutate_lockdir1.py` — baseline green then 8 mutants all caught (incl. fixed-30 s default wait). CoS did not re-run mutations or the Windows suite locally (Linux box).
- Class C two-round; r2 approved by SOL (`SOL-20260921-062417-072`), A1, A2, A3. No GitHub PR reviews yet (0).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-26.md`.