schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 19
head: b18c58df193c6ae3f2cf2f27cac5c2054e89dae9
reviewed_utc: 2026-09-21T00:18:44Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head b18c58df (run 35547277982). GitHub mergeable=MERGEABLE. Await green `test` on this tip before merge.

## Blockers

(none)

## Improvements

- **Close superseded #12:** this PR still supersedes https://github.com/layibabalola/agent-bridge/pull/12 (same P-0c lineage; #12 remains OPEN). Close #12 after #19 lands (or now) so the board does not babysit two open copies.
- **Diverged from master:** compare `master...b18c58df` is ahead_by 4 / behind_by 35 (diverged). GitHub still reports MERGEABLE (no conflict). Rebase or merge-master before land if the factory expects a linear tip; not a content blocker.

## Notes

- Tip `b18c58df` is CARD:P-0c-ARM round 2 on `test_win_process.py` (+46/−4 vs prior tip a3af347c). Answers SOL round-1 CHANGES_REQUESTED finding 1: arms (a)/(c) did not detect production regressions (SOL mutated production to `text=True` or deleted `encoding` and both arms still passed under PYTHONUTF8=1). New `test_read_side_pin_is_present_at_the_call_site` asserts call-site shape (`encoding == "utf-8"`, no `text`/`universal_newlines`) so both mutants redden. Renamed arm (c) to `test_mutation_c_forcing_ansi_decode_reddens` / `run_with_ansi_decode` (addresses prior CoS Improvement on stale `text=True` wording). SOL finding 2 (mbcs modeling) accepted as legitimate; finding 3 acknowledged (live CIM parity UNKNOWN on SOL identity — Access denied).
- Prior CoS Improvement on stale assertion wording: **addressed** in this tip.
- Full PR delta vs master remains the P-0c stack (now 4 commits): write-side UTF-8 pin + CIM-expression-only guard; product read-side `encoding="utf-8"`; bootstrap None-stdout harden; arm (c) mbcs mutation; shape-assertion arm. Workflow intentionally untouched.
- Local evidence claimed under PYTHONUTF8=1: SOL mutants each 1 failed / 5 passed (new arm only); restored 6 passed.
- No GitHub reviews yet. CoS via `gh` (focused test harden on reviewed lineage; no Cloud Agent). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/agent-bridge/pr-19.md`.