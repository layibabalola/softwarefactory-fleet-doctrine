schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 19
head: a3af347c6cf80b64c317554165bc1c03c187749b
reviewed_utc: 2026-09-20T23:41:00Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head a3af347c (run 35545333630). Steps 1–3 SUCCESS; Install package in progress; Ruff/Mypy/phase smoke/full bridge/wheel/governed suites still pending. GitHub mergeable=MERGEABLE. Await green `test` on this tip before merge.

## Blockers

(none)

## Improvements

- **Close superseded #12:** this PR explicitly supersedes https://github.com/layibabalola/agent-bridge/pull/12 (same P-0c lineage; #12 reddened on a real 17-step failure at step 13). Close #12 after #19 lands (or now) so the board does not babysit two open copies.
- **Stale assertion wording on arm (c):** `test_mutation_c_restoring_text_true_reddens` still names the helper `run_with_text_true` and the failure message still says "restoring text=True", while the mutation now sets `encoding="mbcs"`. Cosmetic only; update names/message on a follow-up so the failure text matches the measured hazard.
- **Diverged from master:** compare `master...a3af347c` is ahead_by 3 / behind_by 35 (diverged). GitHub still reports MERGEABLE (no conflict). Rebase or merge-master before land if the factory expects a linear tip; not a content blocker.

## Notes

- Tip `a3af347c` is CARD:P-0c-ARM only on `test_win_process.py` (+22/−2): arm (c) mutation switched from bare `text=True` to `encoding="mbcs"` so the locale-decode hazard reddens under CI's job-level `PYTHONUTF8=1` (where `text=True` becomes a utf-8 no-op identical to the product pin). Workflow intentionally untouched.
- Full PR delta vs master is the P-0c stack (3 commits, +618/−15): `powershell_runtime.py` UTF-8 write-side pin + CIM-expression-only guard; product call sites `text=True` → `encoding="utf-8"` (`core/processes.py`, `bootstrap_session.py`, `server.py`, `server_wrapper.py`, `server_wrapper_trampoline.py`); bootstrap watcher enumeration try widened to cover None stdout (VERDICT-A1); `CimEncodingPinTests` arms (a)/(b)/(c), dead-fallback (d) for pwsh + PS 5.1, `PowershellCimCommandGuardTests`, bootstrap None-stdout unit test.
- Mechanism: without the pin, pwsh best-fit-maps non-ASCII CommandLine (e.g. U+2014 → hyphen) before Python decodes; fingerprint comparators treat that as pid-reuse and kill. Arm (c) under UTF-8 mode previously could not redden — CI was neutralizing the mutation it exists to detect. Local evidence claimed: with and without PYTHONUTF8 both 5 passed / 49 deselected; control neutering mutation to `encoding="utf-8"` reddens (1 failed); restored green.
- No GitHub reviews yet. CoS via `gh` (no Cloud Agent — tip is a focused mutation-control fix on an already-built P-0c stack whose round-2 hardening was reviewed in lineage). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-19.md`.