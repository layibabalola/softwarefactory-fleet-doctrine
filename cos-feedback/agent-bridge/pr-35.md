schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 35
head: 2bfb7494c5767281eebd4a5ce01fc1d1ba95eb4b
reviewed_utc: 2026-09-21T22:19:19Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 2bfb749 (Actions run 35662021662); author claims local 5/5 base + 5/5 venv (unfixed 3/5 venv negative control), full file 29/29 both, ruff/mypy clean; SOL (Codex) APPROVE on 2bfb749; 0 GitHub PR reviews

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `2bfb7494c5767281eebd4a5ce01fc1d1ba95eb4b` (Actions run 35662021662). Wait for green before merge. If red, start with `CrossProcessTakeoverTests` in `test_single_instance_guard.py` (holder `HELD <pid>` parse and the three pid-equality sites that used to compare `Popen.pid`).

## Improvements

- Soft: **0 GitHub PR reviews** on this tip. Author body claims SOL (Codex, other family) APPROVE on `2bfb749`. Confirm factory quorum is visible and CI is green before merge.

## Notes

- CARD:SIG-XPROC-HOST-FIX — test-only, one file (`test_single_instance_guard.py`, +12/−7). Branch `card/sig-xproc-host-fix`. MERGEABLE; mergeStateStatus UNSTABLE while CI runs.
- Under a Windows venv, `subprocess.Popen.pid` is the `Scripts\python.exe` redirector, not the interpreter that takes the lock and records `os.getpid()`. Three `CrossProcessTakeoverTests` compared lock/displaced pid against `Popen.pid` and failed 3/5 under a venv, stopping test 1 before its refuse-mode half. Holder now prints `HELD <pid>` (`os.getpid()`); `_start_holder` parses it into `holder_pids`; the three assert sites compare against that self-reported pid. No assertion removed or loosened.
- Product / lock code unchanged. Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-35.md`.
