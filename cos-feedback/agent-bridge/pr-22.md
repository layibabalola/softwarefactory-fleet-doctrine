schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 22
head: cf4a9964cd73dbaf63eb22dd1efbdd0cad119447
reviewed_utc: 2026-09-21T02:14:04Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head cf4a9964 (run 35553475913). GitHub mergeable=MERGEABLE (unstable while checks run). Await green `test` on this tip before merge.

## Blockers

(none)

## Improvements

(none)

## Notes

- Scope: `test_server_wrapper_phase2.py` only (+17/−1). No product file changes. One commit on tip (`cf4a9964`).
- Defect: `test_trampoline_host_env_queries_prefer_pwsh_for_cim` mocked `subprocess.run` then read `run.call_args.args[0]` without pinning `native_process_entry`. Fabricated pid 456: when that pid is live on the runner, `_host_env_for_parent` returns on the native branch and never calls `subprocess.run`, so `call_args` is `None` → `AttributeError`. Looked intermittent because it depended on the host process table, not a race. Siblings at lines 1341/1391/1408/1423 already pin `native_process_entry`; this test was the only family member missing the idiom.
- Fix: pin `native_process_entry` to `None` like siblings; assert `run.called` with a legible message before dereferencing `call_args`. PR body claims bar (a)/(b)/(d) measured: positive control 129 passed; named mutation (pid 456 “exists”) fails with the new message; no product touch. Clause (c) satisfied by deterministic root cause (ten-run alternative not needed).
- Cursor Cloud Agent not used; review via `gh` (test-only pin + assert harden, single file). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-22.md`.