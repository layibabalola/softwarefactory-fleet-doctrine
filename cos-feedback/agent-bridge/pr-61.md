schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 61
head: fad371a074e1ec20ede893fcc3ab8db2780198d5
reviewed_utc: 2026-09-26T14:22:30Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: MERGEABLE/UNSTABLE; Windows CI `test` IN_PROGRESS on tip fad371a0 (Actions run 36248228688 / job 108421289318); draft=false; 0 GitHub reviews; ahead 1 / behind 0 vs master

## Blockers

(none)

## Improvements

- Follow-up already named in the PR body: **HOST-RECURSION-DEBT-2** — the node-count half of `_depth_bounded` (`MAX_STRUCTURE_NODES = 100_000`) still has no dedicated test. Keep that as a separate card; do not widen this PR.
- Optional hygiene (non-blocking): sibling deep-ledger tests such as `test_mcp_layer_survives_deep_ledger_and_never_raises` still only assert `UNKNOWN` / `ok=False`. That is fine for the "never raises" contract, but if a future adversary wants mutation-sensitive coverage at the MCP layer for the A1-scale fixture, pin the same `depth_error` (or parse-boundary) string there too under a new debt id — not in this diff.

## Notes

- Scope: +8/−0 in `test_provider_activation.py` on `card/host-recursion-debt-1` (1 commit `fad371a074e1`). Class-A/debt pin: `test_dict_wrapped_deep_activation_ledger_reaches_the_depth_guard_at_all_layers` previously asserted only `UNKNOWN` / `ok=False`, which `_validate_ledger`'s "non-object record" path also produces when `_depth_bounded` is forced `True` (the fixture's `grants: [<deep list>]` fails `isinstance(row, dict)`). The test now builds `depth_error` from `MAX_STRUCTURE_DEPTH` and asserts it on store `snapshot()["error"]`, bridge `provider_activation_status` `data["error"]`, and MCP tool `payload["data"]["error"]`.
- Spot-check on tip: production error in `core/provider_activation.py` `_load_strict` is exactly `"provider activation ledger exceeds maximum nesting depth of %d" % MAX_STRUCTURE_DEPTH` (limit 200); `MAX_STRUCTURE_DEPTH` is imported in the test module; fixture `_dict_wrapped_deep_activation_ledger` still wraps deep structure inside a schema-valid ledger so the depth guard — not the top-level dict check — is the refusing path. No production code change.
- PR body: guard-forced-True red control; also killed guard-delete / depth>limit*100 / MAX_STRUCTURE_DEPTH=5000 / validation-before-guard; full suite 1526 passed / 1 skipped; ruff+mypy clean; A1/A2/A3/SOL APPROVE (receipt `SOL-20260926-091813-832`). CoS did not re-run the suite on this Linux box (factory floor is Windows).
- Open sibling: #36 still open on `card/c-ensure-adopt`. Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.