schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 48
head: ed454ad7a8948068b0e76802cc03e7a4c7311345
reviewed_utc: 2026-09-25T18:24:49Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head ed454ad (Actions run 36173133401); Ruff SUCCESS; Mypy baseline IN_PROGRESS; Phase smoke / Full bridge / Wheel / Remaining governed suites (incl. new test_doctrine_outbox.py) still pending; MERGEABLE / mergeState UNSTABLE; draft=false; 0 GitHub reviews

## Blockers

- Wait for Windows CI `test` green on head `ed454ad7a8948068b0e76802cc03e7a4c7311345` (Actions run 36173133401) before merge — tip run still IN_PROGRESS past Ruff (Mypy baseline running; Remaining governed suites not started).

## Improvements

- Tracked non-blocking debt from OUTBOX-2 card body (DOB-DEBT-1..5): non-UTF-8 ledger crash, temp-dir leak on worktree-add failure, and siblings left explicitly out of scope. When a follow-up card is allowed, harden `read_ledger_keys`/`append_ledger_rows` against undecodable lines and guarantee `remove_temp_worktree` (or equivalent) on every worktree-add failure path.
- `drain` without `--push` builds commits then discards the worktree (correct dry-run). Consider emitting a one-line stderr note that dry-run commits were discarded, so operators do not mistake `report["commit"]` for a pushed SHA.
- Wiring into the unattended loop remains deferred to DOCTRINE-OUTBOX-WIRE (documented in `doctrine-outbox/README.md`). Keep that separate so this PR stays mechanism-only.

## Notes

- Scope: +1465/−0 across 5 files on `card/doctrine-outbox-3` (3 commits; ahead 3 / behind 0 vs `master`). Tip `ed454ad7a8948068b0e76802cc03e7a4c7311345` is CARD:DOCTRINE-OUTBOX-3 (pin runner identity in tests) after OUTBOX (7588faa) + OUTBOX-2 N1–N6 (d9e7243).
- Files: `tools/doctrine_outbox.py` (+718 new), `test_doctrine_outbox.py` (+663 new, 32 test fns), `doctrine-outbox/README.md` (+78 new), `.gitignore` (+5 excludes `.claude-state/coordination/doctrine-outbox/`), `.github/workflows/windows-ci.yml` (+1 adds suite to Remaining governed suites).
- Mechanism: ports fleet-bus reference outbox (bus 7cc2305 lineage). Items under `doctrine-outbox/` read from committed `--ref` bytes only; `drain` is dry-run unless `--push`; sent tracking via append-only JSONL ledger + bus tip marker (no `sent/` move); Law-4 screen plus host/account/deny-file names refuse by class without echoing matched values.
- N1–N6 closures verified in tip: filename+commit-message screened before commit; fixed OUTBOX_IDENTITY (`agent-bridge doctrine outbox` / `outbox@agent-bridge.invalid`) via `-c user.*` and GIT_AUTHOR_*/GIT_COMMITTER_*; SHORT_IDENTITY_UNSCREENABLE for <3-char names; underscore adjacency in deny regex; post-commit PREFIX_BROKEN via `cat_file_blob`; `check_decision` reads `git show <ref>:<path>` not working tree.
- M1–M4 closes prior publication blocker (SOL-20260925-122809-523): autouse `_pin_runner_identity` fixture; unqualified validate CLI paths pass `--repo tmp_path`; check-decision assertion uses capsys for DECISION_KIND_MISMATCH and asserts SHORT_IDENTITY_UNSCREENABLE absent from stderr. `tools/doctrine_outbox.py` claimed byte-identical to d9e7243 on tip.
- PR body: Class C publication review round 1 on this head 4/4 APPROVE (3 Sonnet adversaries + SOL). Local verification claimed: test_doctrine_outbox 32 passed under normal and short-identity / empty-git-config matrices; broader governed suites green locally before push.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-48.md`.
