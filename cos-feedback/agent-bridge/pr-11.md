schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 11
head: 516b436e9b8b0f164a9843437e98d71505cd48cc
reviewed_utc: 2026-09-17T14:09:38Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` FAILURE on head 516b436 (Actions run 35220742063, ~4s, empty steps); annotation says job never started — GitHub Actions billing/spending limit. No test evidence from CI. Author local bar 501 passed / 53 subtests; Class-C round 1 (3 Sonnet + SOL) all APPROVE.

## Blockers

- **CI: Windows CI `test`** — FAILURE on head `516b436e9b8b0f164a9843437e98d71505cd48cc` (Actions run 35220742063). Job annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." Fix billing/spending limit in GitHub settings, re-run the workflow on this head, and merge only when `test` is green on matched tip `516b436` (author gate). Not a suite failure — no pytest/ruff/mypy ran.

## Improvements

- `test_wake_codex_wrapper.py` `test_real_root_wrapper_resolves_and_reaches_the_packaged_asset`: docstring and inline comments still describe the removed installed-distribution preference ("wrapper prefers an INSTALLED…", "Find-InstalledWakeScript cannot resolve…"). PATH stripping remains fine for determinism; rewrite the rationale to match the new beside-only contract (editorial, non-blocking).

## Notes

- Two-file delta: `wake_codex.ps1` (+5/−49) and `test_wake_codex_wrapper.py` (+317/−25). Behind 0 / ahead 1 vs `master`. MERGEABLE; mergeStateStatus UNSTABLE solely from the failed check.
- Production change: deletes `Find-InstalledWakeScript` and its call; root wrapper resolves only `$PSScriptRoot\agent_bridge\ps_assets\wake_codex.ps1` (keeps self-path guard and loud throw); resets `$global:LASTEXITCODE = 0` before invoking the asset; header rewritten to state no installed-distribution / interpreter consult (FIELD_UPGRADE_SPEC §5), worded to satisfy R7 token guard.
- Tests: R4/R5 invert prior installed-preference arms with never-invoked python/py shim sentinels (SOL carded-removal ruling); R1/R3/R8 cwd dist-info/egg-info hijack coverage under pwsh (and dual-shell where noted); R2 PS 5.1 regression guard; R6 seeded-caller LASTEXITCODE reset; R7 mechanical token guard. Fallback/forwarding/missing/packaged/real-root tests retained in intent.
- Scope: no change to `agent_bridge/launch.py`, `configure_watcher.py`, packaged asset, or `test_wheel_install.py`.
- Author banked RED-at-base / GREEN-after / mutation proofs (M1 restore Find-InstalledWakeScript; M2 drop LASTEXITCODE reset) under `.claude-state/coordination/evidence/P-16-R-20260917/`; CoS did not re-run mutations or the Windows suite locally (Linux box).
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-11.md`.