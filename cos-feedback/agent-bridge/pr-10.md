schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 10
head: 84a96178c94995308919e128248dc06bd1e0ac57
reviewed_utc: 2026-09-16T02:32:56Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 84a96178 (Actions run 35048352023); author local bar 495 passed / 45 subtests on Windows pwsh7 Python 3.14.3; SOL cross-family APPROVE round 1 with no BLOCKERs

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `84a96178c94995308919e128248dc06bd1e0ac57` (Actions run 35048352023). Wait for green before merge; if red, inspect `test_py_launcher_resolves_the_installed_asset_when_python_is_absent` and `_write_py_resolver` first (new `py.cmd` argv shape).

## Improvements

- `test_wake_codex_wrapper.py`: insert a blank line between the end of `test_real_root_wrapper_resolves_and_reaches_the_packaged_asset` and `def test_py_launcher_resolves_the_installed_asset_when_python_is_absent` (PEP8 method spacing; editorial only).

## Notes

- Single-file delta: `test_wake_codex_wrapper.py` (+56 / −1). No production behaviour touched.
- Intent: P-16-A3 follow-up from SOL / CoS on #9 — every prior test wrote only `python.cmd`, so `Find-InstalledWakeScript`'s second launcher (`py -3 -c`) was never entered. This writes **only** `py.cmd`, asserts `python.cmd` absent, and requires exit 37 via the installed fake (checkout fake yields 38), so accidental checkout fallback cannot green the test.
- `_write_py_resolver` guard matches real `py` argv (`%~1==-3`, `%~2==-c`, non-empty `%~3`); deliberately not a copy of the `python` launcher guard. Also fixes the stale inline comment that still said `AGENT_BRIDGE_ROOT survived the hop` (editorial carry from #9).
- Author mutation table M1–M4 reported to redden (copied `-c` guard, wrong `%~2`, inverted `%~3`, wrong shim name); CoS did not re-run mutations locally. Author also disclosed an intermittent `test_dashboard_server_requires_token_and_csrf_for_mutations` socket flake on an earlier full-gate run that did not reproduce on a clean re-run or on pristine master — treat as pre-existing suite flake, not introduced here.
- Class B cross-family: implementer/hub Claude; independent key SOL (Codex gpt-5.6-sol) APPROVE round 1, no BLOCKERs. SOL sandbox lacks writable TEMP so SOL verdict does not cover test evidence; GitHub Windows CI is the independent test key.
- Related: closes the explicit CoS Improvement on #9 about covering / documenting the `py -3 -c` gap after P-16-A2.
- Cursor Cloud Agents not used (focused test-only review via `gh`). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-10.md`.
