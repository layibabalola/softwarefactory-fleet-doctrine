schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 39
head: 19e91623158775f897c6cf6305c13410755988da
reviewed_utc: 2026-09-24T06:17:08Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 19e91623 (Actions run 35963640038); ruff SUCCESS, mypy SUCCESS, Phase smoke in progress; MERGEABLE / mergeState UNSTABLE; 0 GitHub reviews

## Blockers

(none)

## Improvements

- Wait for Windows CI `test` green on head `19e91623158775f897c6cf6305c13410755988da` before merge (Actions run 35963640038; ruff/mypy already green; smoke still running).
- Soft: prefer `WRAPPER_ARG` / `TRAMPOLINE_ARG` (or an inline `list2cmdline([…])`) for any *new* fixture command-line concatenation in this file. Manual `'"' + WRAPPER_SCRIPT + '"'` remains correct only where the fixture intentionally supplies its own quotes (as round 2 restored for the Codex `--diagnostic-path` host walk). A bare `+ WRAPPER_SCRIPT` will re-break space-path checkouts.

## Notes

- Single-file delta: `test_server_wrapper_phase2.py` (+83/−79, 2 commits on `card/trunk-space-path-fixtures`). Ahead 2 / behind 0 vs `master`. No production code touched.
- Defect: fixtures built command lines as `"python " + WRAPPER_SCRIPT` (unquoted). On a checkout path with a space the classifier split the path and ~50 smoke tests went red (author reproduction at `e9415fa`). Production already quotes via `subprocess.list2cmdline`.
- Fix (`1f881e1`): module-level `WRAPPER_ARG` / `TRAMPOLINE_ARG` = `list2cmdline([path])`; fixture concatenations switched to those (or inline `list2cmdline` for suffix/prefix mutations like `WRAPPER_SCRIPT + "x"`). Identity on space-free paths, so CI command lines stay byte-identical.
- Round 2 (`19e9162`): restores bare `TRAMPOLINE_SCRIPT` inside the fixture that already wraps the path in literal `"…"` (`test_mcp_host_identity_does_not_skip_launcher_path_as_bare_argument` Codex `--diagnostic-path` row) — using `TRAMPOLINE_ARG` there doubled quotes on a space path. Negative control (bare-substring launcher check) still reddens from a space path per author.
- Author evidence: smoke 153/153 from a spaced worktree; ruff/mypy clean locally. Independent other-family review (Codex ASTRA lane, exact head `19e9162`): APPROVE. CoS did not re-run the Windows suite locally (Linux box).
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-39.md`.
