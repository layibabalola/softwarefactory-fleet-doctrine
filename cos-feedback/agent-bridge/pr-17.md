schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 17
head: 2e11eb0ddf7e4efba9e06ccee61389f1c0ceb1eb
reviewed_utc: 2026-09-20T22:44:39Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 2e11eb0d (run 35542558518, just started). Latest settled master Windows CI success on e4e7282 (#15 merge, run 35542048511); master tip af7bb1b (#16 merge) still has post-merge CI in progress (run 35542515145). Await green `test` on this tip before merge.

## Blockers

(none)

## Improvements

- **Residual concurrent race (documented, out of scope):** `append_event`'s preflight and post-append `_refresh_ack_status` still leave a window where a concurrent writer can corrupt a peer journal after preflight passes; the post-append reconcile then raises after this seat's row is already on disk. Carded as residual; not a merge blocker. Worth a follow-up if multi-writer races become realistic on Windows.
- **Cascading `malformed` on stuck `expected`:** `_read_journal` still does not advance `expected` past a bad line, so every later row in that journal is also reported malformed. P-14-R3 correctly fences the partial-takeover consequence at `guard_once` (REFUSE) rather than widening writer outage. A future card could advance `expected` past unparseable lines if observability of "which row hid which" becomes needed; non-blocking.
- **Test-suite size:** `test_lane_coordination.py` +1171 lines for four stacked cards. Acceptable given explicit mutation-kill tables; if CI wall-clock grows, consider a dedicated journal-corruption suite file so the main module stays scannable.

## Notes

- Three-file delta (+1409/−19): `lane_coordination/coordination.py`, `test_lane_coordination.py`, `BRIDGE_PROTOCOL.md` (REFUSE audit-registry row only). Head `2e11eb0d` (DECISION-P-14-R4a subject) re-derived on trunk `af7bb1b`; 12 commits; class B; `merge-tree` 0 conflicts. Stack: P-14-R → R2 → R3 → R4 → R4a (R4 superseded by R4a; not listed separately).
- Mechanism summary:
  - **Read path (`_sweep`/`observe`):** `_read_json_sweep` + `_read_journal(..., tolerate_undecodable=True)` survive undecodable bytes / non-object leases; malformed DISPATCH/RECEIPT references become issues instead of crashes; unreadable journal floors severity to ATTENTION; fresh `REFUSE` guard_state forces implementer ESCALATE + `GUARD-REFUSED` (avoids sweep-suppression via re-stamped timestamps).
  - **Write path (strict by default):** `tolerate_undecodable=False` on `reconcile_receipts` / `_read_journal`; fences unhashable `target_seat` and non-object `in_reply_to` with `JournalCorruptionError` before caller mutations; `guard_once` REFUSEs on implementer `malformed`/`unreadable` when a pending implementer dispatch exists (after `not pending` NOOP); `append_event` strict preflight before own-row write (P-14-R4).
- Mutation proofs and SOL round-trips are documented per card in commit messages; R4a is deletion-only comment hygiene. No GitHub reviews yet.
- CoS via `gh` (no Cloud Agent — mechanism + mutation-kill tests are self-documented across the stack). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-17.md`.