schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 55
head: 62ecb78a0e380fd533e144dc6075ab5d176578fb
reviewed_utc: 2026-09-26T03:15:32Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 62ecb78a (Actions run 36214219640 / job 108326850681); MERGEABLE / mergeState UNSTABLE; draft=false; 0 GitHub reviews; ahead 1 / behind 0 vs master

## Blockers

- Wait for Windows CI `test` green on head `62ecb78a0e380fd533e144dc6075ab5d176578fb` (Actions run 36214219640) before merge — tip job still IN_PROGRESS at review time.

## Improvements

- `tools/audit-known-findings.json`: restore the trailing newline (diff ends with "No newline at end of file").

## Notes

- Scope: +14/−38 across 5 files on `card/s4-fix-mcp` (1 commit; ahead 1 / behind 0 vs `master` `300f9827`). Tip `62ecb78a0e380fd533e144dc6075ab5d176578fb` is S4-FIX-MCP (class C / P-11): upgrades `mcp` 1.27.0 → 1.28.1 so PYSEC-2026-3481/3482 (fixed in 1.27.2) and PYSEC-2026-3483 (fixed in 1.28.1) clear; empties `tools/audit-known-findings.json` findings so `audit_gate.py --release` can exit 0 once CI confirms a fresh scan.
- Files: `pyproject.toml` / `requirements.txt` pin bump; `requirements-lock.txt` mcp hashes only (claim: S-2 pip-compile regen moved no other pin); `tools/audit-known-findings.json` findings → `[]` with updated `_doc`; `docs/internal/RELEASE_LOCK.md` marks the three advisories fixed by this card.
- Spot-checks: lock diff is mcp-only (old hashes `5ce1fa81…`/`d3dc35a7…` → `2726bca5…`/`d51e36a5…`); known-findings JSON still has `_doc` + empty `findings` array (gate shape preserved); RELEASE_LOCK history notes stdio-only reachability then the fix. CoS did not re-run `pip-audit` or Windows install on this Linux box (factory floor is Windows/CPython 3.14).
- Open siblings: #36 still open on a different branch; S-2/#52, S-3/#53, S-4/#54 appear landed on master (base `300f9827`); this tip is the follow-up that retires the three recorded mcp release blockers.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-55.md`.