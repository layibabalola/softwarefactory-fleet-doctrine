schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 20
head: 654815e2b17f697657fea6db0c52d6793c8e1234
reviewed_utc: 2026-09-21T01:40:08Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 654815e2 (run 35551593397; install step active). GitHub mergeable=MERGEABLE (unstable while checks run). Await green `test` on this tip before merge.

## Blockers

(none)

## Improvements

- **Diverged from master:** compare `master...654815e2` is ahead_by 2 / behind_by 38 (diverged). GitHub still reports MERGEABLE. PR body already inspected a `git merge-tree --write-tree` merge against trunk `3157ac3` (P-0c-ARM symbols present; no stale `run_with_text_true` / `test_mutation_c_restoring_text_true_reddens`; no duplicate test names). Rebase or merge-master before land only if the factory wants a linear tip — not a content blocker given that evidence.
- After CI goes green, confirm the new static-walk + runtime-spy arms actually execute in the governed suite (they import `server` / `server_wrapper` / `server_wrapper_trampoline` / `bootstrap_session` and patch `subprocess.run` per module). A green full-bridge JUnit is the gate, not phase smoke alone.

## Notes

- Scope: `test_win_process.py` only (+583/−0). No product file changes. Two commits on tip (`e75bad1b` add coverage; `654815e2` walk-based discovery). Claims subject matched ADOPTED-LOCAL decision before open; **last of twelve adopted branches**.
- Design (spot-checked): Arm 1 AST static contract over every `subprocess.run` whose argv derives from `powershell_cim_command` — must pin `encoding="utf-8"`, must not pass `text=` / `universal_newlines=`. Arm 2 runtime spy asserts kwargs actually passed at each of the six sites. Discovery walks the tree (`discover_cim_caller_files`) instead of trusting `EXPECTED_SITES` as a file list (hub review of `e75bad1`: list-only scan was blind to a 7th caller in a brand-new file). Positive controls: synthetic `text=True` caller reddens matcher; walker finds brand-new file callers and excludes junk/test dirs.
- `EXPECTED_SITES` (6): `bootstrap_session._enumerate_watcher_processes`, `core/processes._cim_process_entry`, `core/processes._cim_process_table`, `server._wrapper_process_entry`, `server_wrapper._process_entry_from_system`, `server_wrapper_trampoline._host_env_for_parent`. Spot-check on tip/product: those CIM sites pin `encoding="utf-8"` (other non-CIM `text=True` runs elsewhere are out of this card's contract).
- Lineage: closes the P-0c coverage gap after #19 (P-0c-ARM) landed on master at `3157ac3`. Only open agent-bridge PR at review time.
- Cursor Cloud Agent not used; review via `gh` (test-only harden on reviewed lineage). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-20.md`.