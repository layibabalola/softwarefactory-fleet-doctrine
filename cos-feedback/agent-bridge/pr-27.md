schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 27
head: bca18e1042c6aa3caa3e4e3065bdcbd4658b1bdd
reviewed_utc: 2026-09-21T14:25:31Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head bca18e1 (Actions run 35611986765); prior tip 24101b2 failed Mypy baseline on core/storage.py:45/:49 (run 35611511914); this tip adds `# type: ignore[assignment]` on `_msvcrt`/`_fcntl` ImportError fallbacks (AST identical, comment-only); author local mypy/ruff green; Class C SOL/A1/A2 APPROVE on 24101b2 (SOL-20260921-090032-250)

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `bca18e1042c6aa3caa3e4e3065bdcbd4658b1bdd` (Actions run 35611986765). Wait for green before merge. Prior head `24101b2` failed the Mypy baseline step (`core/storage.py:45` and `:49` "Incompatible types in assignment" on `_msvcrt = None` / `_fcntl = None`); this tip is the intended unblock. If still red, start with Mypy on `core/storage.py` import fallbacks, then `test_lock_os_layer.py` and any `lock_dir` / compact rotation regressions.

## Improvements

- Soft: `_THREAD_LOCKS` grows without bound for distinct lock-path keys (same class of residual as never-deleted `*.lock.os` beside deleted data files). Consider a weak/LRU map or explicit drop when a path is retired, if long-lived bridge processes accumulate many unique lock paths.
- Soft: POSIX `fcntl.flock` branch is declared residual (CI is windows-latest only). A Linux CI job or a documented manual matrix run would make the non-reentrant thread-lock role (NFS-style per-process flock) less of a paper claim.
- Soft: 0 GitHub PR reviews yet — factory SOL/A1/A2/A3 approvals are on the card trail, not on the GitHub review API; optional bachelor/GH review if the board still wants a forge stamp.

## Notes

- Delta vs prior reviewed tip `24101b2`: one file, `core/storage.py` (+2/−2) — only `# type: ignore[assignment]` on the two platform-module ImportError fallbacks. Product AST unchanged (author-checked). Overall PR still seven files (+702/−19) vs `master`.
- Intent (CARD:C-LOCKDIR-2 / successor 2b): OS exclusion under `lock_dir` — per-path non-reentrant thread lock, then OS lock (`msvcrt`/`flock`) on never-deleted `<name>.lock.os`, then original directory protocol with identity-checked release (C-F3); one deadline across stages. Pure readers never create a sentinel. Round-1 SOL rotation orphan fixed by direct write under the audit lock. Successor `24101b2` was test-only (bar f clock). This tip is CI-typing only.
- Declared residuals unchanged: exclusion vs old-version holders still age-based; crash reclaim of legacy directory still at `stale_seconds`; reader with no sentinel excluded from a mid-read writer only by age; POSIX not in CI; network FS unsupported. Does not close C-F1 (steal-on-hash-mismatch).
- Evidence: `tools/mutate_lockdir2.py` — baseline green, 10/10 mutants caught on `d4189cb`. CoS did not re-run mutations or the Windows suite locally (Linux box).
- Class C; r1 SOL CHANGES_REQUESTED then r2 + successor APPROVE (SOL-20260921-090032-250), A1/A2 APPROVE on `24101b2`. No GitHub PR reviews (0).
- Prior CoS Improvements (thread-lock map, POSIX CI, GH review stamp) not addressed by this tip — expected; tip is Mypy unblock only.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: rewritten on `master` at `cos-feedback/agent-bridge/pr-27.md`.
- Related prior CoS: `cos-feedback/agent-bridge/pr-26.md` (C-LOCKDIR-1 recovery delegation).
