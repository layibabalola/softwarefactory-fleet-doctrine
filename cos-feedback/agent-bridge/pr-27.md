schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 27
head: 24101b2835c5e7efa3bccf7a8c4914a779b068e2
reviewed_utc: 2026-09-21T14:21:42Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 24101b2 (Actions run 35611511914); author local bar mutate_lockdir2 10/10 mutants caught + full suite 1229 passed / 1 skipped without PYTHONPATH on d4189cb; successor 24101b2 test-only (bar f clock); Class C SOL/A1/A2 APPROVE on 24101b2 (SOL-20260921-090032-250)

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `24101b2835c5e7efa3bccf7a8c4914a779b068e2` (Actions run 35611511914). Wait for green before merge; if red, start with `test_lock_os_layer.py` (new, added to the Windows CI shard list) and any `core.storage.lock_dir` / compact rotation regressions.

## Improvements

- Soft: `_THREAD_LOCKS` grows without bound for distinct lock-path keys (same class of residual as never-deleted `*.lock.os` beside deleted data files). Consider a weak/LRU map or explicit drop when a path is retired, if long-lived bridge processes accumulate many unique lock paths.
- Soft: POSIX `fcntl.flock` branch is declared residual (CI is windows-latest only). A Linux CI job or a documented manual matrix run would make the non-reentrant thread-lock role (NFS-style per-process flock) less of a paper claim.
- Soft: 0 GitHub PR reviews yet — factory SOL/A1/A2/A3 approvals are on the card trail, not on the GitHub review API; optional bachelor/GH review if the board still wants a forge stamp.

## Notes

- Seven-file delta (+702/−19): `core/storage.py` (+156/−13), `test_lock_os_layer.py` (+391 new), `tools/mutate_lockdir2.py` (+133 new), `compact.py` (+8/−2), `test_read_json_lock_timeout.py` (+11/−3), `agent_bridge/setup_bridge.py` (+2/−1), `.github/workflows/windows-ci.yml` (+1). Ahead 3 / behind 0 vs `master`. MERGEABLE; mergeStateStatus UNSTABLE from pending check.
- Intent (CARD:C-LOCKDIR-2 / successor 2b): OS exclusion under `lock_dir` — per-path non-reentrant thread lock, then OS lock (`msvcrt`/`flock`) on never-deleted `<name>.lock.os`, then original directory protocol with identity-checked release (C-F3); one deadline across stages. Pure readers (`read_json`, `read_jsonl` without repair) never create a sentinel. Round-1 SOL leak (rotation marker via locking writer) fixed by direct write under the audit lock. Successor 24101b2 is test-only: bar (f) kill-to-acquire clock starts before `holder.kill()`.
- Declared residuals: exclusion vs old-version holders still age-based; crash reclaim of legacy directory still at `stale_seconds`; reader with no sentinel excluded from a mid-read writer only by age; POSIX not in CI; network FS unsupported. Does not close C-F1 (steal-on-hash-mismatch) — separate scope.
- Evidence: `tools/mutate_lockdir2.py` — baseline green, 10/10 mutants caught on d4189cb (incl. re-entrant thread lock, restarted deadline, orphan rotation sentinel). CoS did not re-run mutations or the Windows suite locally (Linux box).
- Class C; r1 SOL CHANGES_REQUESTED then r2 + successor APPROVE (SOL-20260921-090032-250), A1/A2 APPROVE on 24101b2. No GitHub PR reviews (0).
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-27.md`.
- Related prior CoS: `cos-feedback/agent-bridge/pr-26.md` (C-LOCKDIR-1 recovery delegation).