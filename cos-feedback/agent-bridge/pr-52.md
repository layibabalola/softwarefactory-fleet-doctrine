schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 52
head: a408449c804ea59f932055da76c62db135aa31c1
reviewed_utc: 2026-09-26T00:42:48Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head a408449c (Actions run 36205744408 / job 108301782938); MERGEABLE / mergeState UNSTABLE; draft=false; 0 GitHub reviews; ahead 3 / behind 0 vs master

## Blockers

- Wait for Windows CI `test` green on head `a408449c804ea59f932055da76c62db135aa31c1` (Actions run 36205744408) before merge — tip job still IN_PROGRESS.

## Improvements

(none)

## Notes

- Scope: +749/−4 across 3 files on `card/s2-release-lock` (3 commits; ahead 3 / behind 0 vs `master` `a9e44261`). Tip `a408449c804ea59f932055da76c62db135aa31c1` is S-2 RELEASE-LOCK after round-2 docs fix. Base already has merged #51 (S-1 DEP-AUTHORITY-PIN).
- Files: `requirements-lock.txt` (+647 generated `--require-hashes` lock via pip-tools 7.6.1), `docs/internal/RELEASE_LOCK.md` (+63 regen command + acceptance + platform limits), `test_wheel_install.py` (+39/−4 folds S1-DEBT-1).
- Intent: hash-bound lock of `[project].dependencies` + `[build-system].requires` only. `pyproject.toml` / `requirements.txt` / workflows unchanged; lock is data for later S-3 (SBOM) and S-4 (pip-audit), not yet consumed by CI install.
- S1-DEBT-1 fold: `_normalize_pin` strips leading UTF-8 BOM, canonicalises marker quotes to `"`, rejects pip option lines (`-e`/`-r`/`-c`/`--requirement`) and unparseable lines with named `RequirementLineError` (was raw `AssertionError`). Four new unit tests cover the cases. Spot-check of the helper body looks correct for a flat mirror.
- Lock spot-check: header claims pip-compile Python 3.14; ~35 pinned packages / ~541 sha256 hashes; build-only `setuptools==84.0.0` and `wheel==0.48.0` annotated `pyproject.toml::build-system.requires`; Windows marker pulls `pywin32==312` via `mcp`. Documented limit (Windows/CPython 3.14 only) matches single CI leg.
- Round-2 docs: `RELEASE_LOCK.md` correctly explains that pip-tools 7.6.1 writes spurious `--no-index` into the autogenerated header even when that flag was not used; regen command omits `--no-index` (passing it fails resolution). Red-control note (zero every hash of one package, not one hash) is accurate for multi-artifact pins.
- Quorum claimed: 3 Sonnet A1/A2/A3 APPROVE + SOL APPROVE round 2. CoS did not re-run pip-compile or the clean-venv `--require-hashes` install on this Linux box (Windows/CPython 3.14 lock; factory floor is Windows).
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-52.md`.
