schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 38
head: 9975b6c9b29c38338c7357cadabea4e003ea4465
reviewed_utc: 2026-09-23T23:16:59Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 9975b6c9 (Actions run 35932640590); mergeable MERGEABLE / mergeState UNSTABLE; 0 GitHub reviews; author local: ruff 0, mypy 0, smoke 153, full 417, wheel 3, remaining 760; Class-C round 1 4/4 APPROVE (three Sonnet + SOL), no findings

## Blockers

(none)

## Improvements

- Wait for Windows CI `test` green on head `9975b6c9b29c38338c7357cadabea4e003ea4465` before merge (0 GitHub reviews so far; CI is the independent gate).
- Soft watch: `HealthService` still routes internal JSON/JSONL reads through `self._bridge._health_read_json[l]` (facade hop) so `patch.object(bridge, …)` keeps working. That path depends on `AgentBridge._health_service` remaining a `functools.cached_property` (plain property breaks patch identity). Do not convert it without updating `test_health_service.py` / `test_dr2d3_health_tools.py`.
- Soft / follow-up (out of this card's KR): `doctor()` stayed on `AgentBridge` after the trunk merge rather than moving into `HealthService`. Fine for P-17-PUBLISH; if a later card owns doctor, move it with `test_doctor_cli.py` instead of leaving a one-off on the facade surface.

## Notes

- Delta vs master (10 files, +9983/−8697, 10 commits on `card/p-17-publish`): extracts `inbox_service.py` / `session_service.py` / `health_service.py` from `bridge.py`; `AgentBridge` keeps same-name facades (30 inbox / 39 session / 35 health delegations counted). Tests split into `test_inbox_service.py`, `test_session_service.py`, `test_health_service.py`; P-17-CI-ORPHANED-TESTS names all three in Windows CI remaining suites (37 root `test_*.py` files governed). P-17-LINT-DEBT clears the seven ruff errors the extraction left. Tip `9975b6c` merges trunk `9e4e195` and ports C-F7 `read_json_or_quarantine` (session registry) plus three `read_json_for_update` watcher-state RMW sites into the service modules; `test_read_json_lock_timeout.py` now also scans `inbox_service.py`.
- Public API completeness check: every public method on the three service classes has a same-name `AgentBridge` facade. Late imports of the three services into `bridge.py` keep `# noqa: E402` to avoid the documented circular import.
- Scope of this CoS pass: structure, facade/signature parity, C-F7 port sites, CI suite naming, and merge hygiene via `gh` (no local suite re-run; Cursor Cloud Agents not available). Did not re-audit every moved line for behavioral drift beyond the merge ports and facade wiring.
- No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-38.md`.
