schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 23
head: f8c29d18734829890060c4032c493ebb2b41de96
reviewed_utc: 2026-09-21T02:24:41Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head f8c29d18 (run 35554066010). GitHub mergeable=MERGEABLE (unstable while checks run). Await green `test` on this tip before merge.

## Blockers

(none)

## Improvements

(none)

## Notes

- Scope: `test_agent_bridge.py` only (+28/−19). No product file changes; `.github/workflows/windows-ci.yml` not edited. One commit on tip (`f8c29d18`).
- Defect: job-level `WRAPPER_TEST_TIMEOUT_SCALE` (windows-ci.yml:21) was only consumed by `test_server_wrapper_phase2.py:204`. This file never read it while carrying 19 literal `subprocess` timeouts. PR #21 then failed `test_wake_codex_builds_quoted_inner_command_for_space_paths` with `TimeoutExpired` after 15s — the exact failure the scale comment was written to prevent.
- Fix: same idiom as phase2 — `TIMEOUT_SCALE = float(os.environ.get("WRAPPER_TEST_TIMEOUT_SCALE", "1"))` after the top-level import block (AST-located so ruff import-order stays clean); all 19 literal timeouts become `N * TIMEOUT_SCALE`. Diff shows 19 site rewrites; default remains 1.0 locally (no assertion weakening).
- PR body claims measured bar: (a) 0/19 literals left; (b) scale unset is a no-op; (c) scale=0.01 fails the CI-reddened test with `TimeoutExpired` (proves factor reaches the call site); (d) full suite 510 passed / 44 subtests at scale 1.0 (same population as #21's 1 fail + 509 pass); (e) workflow untouched. Sites found by AST (line-distance missed the reddening site: `subprocess.run(` 26 lines above `timeout=15`).
- Cursor Cloud Agent not used; review via `gh` (test-only timeout scale wire, single file). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-23.md`.