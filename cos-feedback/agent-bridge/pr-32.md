schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 32
head: 69a353bc21dd0d41977685d6fa35e6c26279ccf1
reviewed_utc: 2026-09-21T20:07:20Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: Windows CI `test` IN_PROGRESS on head 69a353bc (Actions run 35649012568); author claims local base-interpreter CI green + mutants M1-M6 KILLED; Class C SOL APPROVE + two Sonnet APPROVE (A3 CHANGES_REQUESTED answered by disclosure per DECISION-P-5-PUB-69a353bc.json); 0 GitHub PR reviews; confirm factory quorum and green CI before merge

## Blockers

- **CI: Windows CI `test`** — IN_PROGRESS on head `69a353bc21dd0d41977685d6fa35e6c26279ccf1` (Actions run 35649012568). Wait for green before merge. If red, start with the two new methods in `test_agent_bridge.py`: `test_readme_core_tool_surface_row_matches_core_frozenset` (README `## Tool surface tiers` `| \`core\` |` row vs `server.CORE_TOOL_SURFACE_NAMES` + count word) and `test_apply_tool_surface_core_raises_when_tool_registry_is_not_a_dict` (absent/None/list `_tools` → RuntimeError on `core`; `full` stays no-op).

## Improvements

- Soft: 0 GitHub PR reviews on this tip. Author body claims SOL APPROVE + two Sonnet APPROVE (A3 CHANGES_REQUESTED answered by disclosure). Still merge only after factory quorum is visible and CI is green.
- Soft: mutation evidence (`M1–M6` README row drop/add/count word, frozenset drop, guard removed, guard weakened to None-only) is author/local only — not invoked from Windows CI. Same pattern debt as other card mutate matrices; a CI-green tip can silently drop a mutant catch.
- Soft: `count_words = {10: "ten"}` in the README pin will `KeyError` if `CORE_TOOL_SURFACE_NAMES` length drifts from 10. Intentional fail-loud pin; if the frozenset grows, update the map and the README count word in the same change (or switch to a number→word helper that covers the new length).

## Notes

- CARD:P-5-PUB publishes ADOPTED-LOCAL `card/p-5` `a9b1fc8` onto `github/master` via `--no-ff` merge commit `69a353bc` (patch-id identical to `a9b1fc8`). Branch `card/p-5-pub`. Ahead 2 / behind 0 vs `master`. MERGEABLE.
- Diff: tests only — `test_agent_bridge.py` +34/−0; no product files change.
- `test_readme_core_tool_surface_row_matches_core_frozenset`: parses README `## Tool surface tiers` section, asserts exactly one `| \`core\` |` row, set-equality of backticked tool names in that row with `CORE_TOOL_SURFACE_NAMES`, and presence of the matching count word (“ten”).
- `test_apply_tool_surface_core_raises_when_tool_registry_is_not_a_dict`: for absent / `None` / list `_tools`, `apply_tool_surface(..., "core")` raises `RuntimeError` matching “does not expose a mutable _tools registry”; `full` with deleted `_tools` remains a no-op and leaves `_agent_bridge_tool_surface == "full"`.
- Cursor Cloud Agents not used (review via `gh`). No GitHub PR comment posted.
- Doctrine bus: published on `master` at `cos-feedback/agent-bridge/pr-32.md`.