schema: cos-feedback.v1
project: agent-bridge
repo: layibabalola/agent-bridge
pr: 46
head: 776de017ffed696cb51751be9642f75c836dd538
reviewed_utc: 2026-09-25T07:09:38Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: Windows CI `test` IN_PROGRESS on head 776de017 (Actions run 36106181699, self-hosted path for same-repo non-codex branch); MERGEABLE / mergeState UNSTABLE; draft=true; 0 GitHub reviews

## Blockers

- Draft + class-C gate open: PR body says do not merge until decision file `DECISION-OWNER-CI-RUNNER-RETRO-752b7ee6` exists. No matching decision artifact found on head `776de017` (tree search). Complete class-C review / mint the decision file, then undraft before merge.
- Wait for Windows CI `test` green on head `776de017ffed696cb51751be9642f75c836dd538` (Actions run 36106181699) before any post-decision merge — tip run still IN_PROGRESS.

## Improvements

- Residual profile paths: comments correctly reframe the USERPROFILE/HOME/APPDATA redirect as accident protection after SOL R1. Consider an explicit AGENTS.md one-liner that `HOMEDRIVE`/`HOMEPATH` and `LOCALAPPDATA\..\..` still rebuild the live profile, so future editors do not re-claim an access boundary (non-blocking; already accurate in workflow comments).
- Hosted-path probe: PR body says `codex/owner-ci-runner-harden-probe` carries the same commit for `windows-latest`. Confirm that probe branch/run stays green so both arms of the `runs-on` ternary are exercised before merge.

## Notes

- Scope: +94/−4 across 2 files on `card/owner-ci-runner-harden` (4 commits; tip 776de017 is comment-only R2 after SOL CHANGES_REQUESTED). Ahead 4 / behind 0 vs `master`. No production Python touched. Files: `.github/workflows/windows-ci.yml` (+73/−4), `AGENTS.md` (+21).
- Hardening vs parked #43 retro: self-hosted `runs-on` only when `CI_RUNS_ON` set AND branch is not `codex/**` AND (not a PR or same-repo head); fork PRs and `codex/**` always get `windows-latest`. Workflow `permissions: contents: read`. `PYTHONNOUSERSITE=1`; PYTHONPATH cleared via `GITHUB_ENV` + guard step. Self-hosted-only profile redirect under `RUNNER_TEMP` before checkout; `LOCALAPPDATA` left real for `py` launcher. Self-hosted venv fails closed unless Python 3.14.x (minor pin; full version printed).
- Tip commit 776de017: comments-only — profile redirect is accident protection, not an access boundary (LOCALAPPDATA / HOMEDRIVE+HOMEPATH still reach the real profile). No step/condition/command delta vs prior tip.
- Owner risk acceptance (2026-09-25) for runner as `.\\obabalola` on ultra-magnus is documented in AGENTS.md; CoS treats that as recorded owner judgment, not a CoS ratification.
- Cursor Cloud Agents not used (unavailable on plan); review via `gh`. No GitHub PR comment posted.
- Doctrine bus: written on `master` at `cos-feedback/agent-bridge/pr-46.md`.