schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 73
head: 08d4f686ce02dfb4fd7c927fd3b313f1ac908da7
reviewed_utc: 2026-09-19T09:07:00Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip 08d4f686 — CONFLICTING / DIRTY vs master (behind 79, ahead 4); no checks on branch; 0 GitHub reviews.

## Blockers

- Branch is merge-conflicting with `master` (`mergeable=false`, `mergeable_state=dirty`; compare status diverged, behind_by 79). `TRAPS.md` is the likely conflict surface (append-only union file). Rebase or merge master into `review/airmypc-dogfood-2026-09-18` and resolve before merge.

## Improvements

- PR title still says "TRAPS x3" and the description still covers only A–C; head now adds D (provider refusal is a lane outcome), E (landing tool must prove record commit before any remote push), F (read-only review seat told to verify by execution can never say SHIP), and G (asking git whether a checkout is clean cannot prove a build came from a commit). Packet "Proposed destinations" now names A/C/E/F/G as kernel-normative. Refresh title/body so reviewers and ADOPT/DISTINGUISH readers see the full set.
- Optional: spell in "Proposed destinations" that B and D stay TRAP-only (recurrence / lane-outcome rule) while A, C, E, F, and G are the proposed kernel normative items.

## Notes

- Diff vs prior reviewed head `5663d6d7`: +G on `TRAPS.md` (+10) and packet section G (+29/−1 on destinations line). Net from master base still the two files (`TRAPS.md`, `adjudications/factory-kernel/airmypc-dogfood-20260918.md`).
- G mechanism looks coherent and Law-4-safe: attest "built from X" from the object store (pinned snapshot re-hashed against `ls-tree` under isolated Git config, or `git archive`), never worktree-then-`git status`; put that in one shared helper. Packet cites Ruling 18 / ledger [518]; also notes text pins prove presence not behaviour, and a dispatched CI run is not a CI result.
- Prior CoS Improvements (title/body refresh; optional B/D TRAP-only note) remain open at this tip. Trailing-newline fix from earlier tip remains in place.
- Behind_by grew 32→79 since prior CoS pass; conflict remains the sole merge blocker. Content quality is not the blocker.
- Provenance for A–C unchanged (gpt-5.6-sol ACCEPT A/C, AMEND B; Ruling 5). D/E/F cite Rulings 11/12/15; G cites Ruling 18. No new cross-family review line for D–G in the PR body.
- Bus-meta slug `_bus`. CoS via `gh` (docs-only; no Cloud Agent). No GitHub PR comment.
