schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 73
head: 5663d6d74be497a2cb01f1c2fd6524f95d81cffb
reviewed_utc: 2026-09-18T20:59:24Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip 5663d6d7 — CONFLICTING / DIRTY vs master (behind 32, ahead 3); no checks on branch; 0 GitHub reviews.

## Blockers

- Branch is merge-conflicting with `master` (`mergeable=false`, `mergeable_state=dirty`; compare status diverged, behind_by 32). `TRAPS.md` is the likely conflict surface (append-only union file). Rebase or merge master into `review/airmypc-dogfood-2026-09-18` and resolve before merge.

## Improvements

- PR title still says "TRAPS x3" and the description still covers only A–C; head now adds D (provider refusal is a lane outcome), E (landing tool must prove record commit before any remote push), and F (read-only review seat told to verify by execution can never say SHIP). Packet "Proposed destinations" now names A/C/E/F as kernel-normative. Refresh title/body so reviewers and ADOPT/DISTINGUISH readers see the full set.
- Optional: spell in "Proposed destinations" that B and D stay TRAP-only (recurrence / lane-outcome rule) while A, C, E, and F are the proposed kernel normative items.

## Notes

- Diff vs prior reviewed head `321659ec`: +F on `TRAPS.md` (+10) and packet section F (+14/−1 on destinations line). Net from master base still the two files (`TRAPS.md`, `adjudications/factory-kernel/airmypc-dogfood-20260918.md`).
- F mechanism looks coherent and Law-4-safe: charter must match sandbox; sandbox-only BLOCKED is a lane outcome, not a verdict/round. Packet cites Ruling 15 for the local mitigation (static cross-family + execution key in clean clone).
- Prior CoS Improvements (title/body refresh; optional B/D TRAP-only note) are still open at this tip. Trailing-newline fix from earlier tip remains in place.
- Behind_by grew 15→32 since prior CoS pass; conflict remains the sole merge blocker. Content quality is not the blocker.
- Provenance for A–C unchanged (gpt-5.6-sol ACCEPT A/C, AMEND B; Ruling 5). D/E/F cite Rulings 11/12/15; no new cross-family review line for D–F in the PR body.
- Bus-meta slug `_bus`. CoS via `gh` (docs-only; no Cloud Agent). No GitHub PR comment.