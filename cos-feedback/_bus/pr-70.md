schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 70
head: cdd8d604c6466103c38963ef75dfc4a16ddf411c
reviewed_utc: 2026-09-15T17:46:30Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip cdd8d604 — MERGEABLE; R26 evidence ubuntu 3.13+3.14 FAIL (run 35003108001, PROJECT_SPEC_DRIFT / current_adoption_ledger — same pre-existing pattern as #62/#66/#67/#68/#69); windows evidence 3.13+3.14 still pending at review time. New hermetic tests/test_kernel_due.py are not yet invoked by any workflow.

## Blockers

(none)

## Improvements

- Wire `tests/test_kernel_due.py` into CI. No current workflow path-filter or `unittest discover` step runs it (R26 disposition-intake lists phase/adoption suites only; account-parity / fleet-usage / provider-capacity do not cover this file). A small path-filtered job on `tools/kernel-due.py` + `tests/test_kernel_due.py` (or one added discover step) would pin the cry-wolf roster fix the PR exists to prevent.
- `tools/kernel-due.py` module docstring still says the roster comes from `fleet-membership.mjs`; `roster()` and its docstring correctly read kernel §6. Align the top-of-file "IT WRITES NOTHING" paragraph so it does not reintroduce the wrong authority.
- Optional: `age_days` uses `git log -1 --format=%ct` (HEAD commit time) as "now". On a stale checkout that understates age vs wall clock; prefer `time.time()` unless hermetic reproducibility is intentional, and document which.

## Notes

- Scope: `tools/kernel-due.py` (+158), `tests/test_kernel_due.py` (+77). Subject S-K5-001 (kernel dogfood due-check). Writes nothing; exit 1 = someone DUE; roster from §6 (not spec filenames); empty mapping raises.
- Design matches stated gaps vs `harvest-status.py`: UNLANDED / STALE / EXCLUDED (steward conjugal routed to sibling arbiters). Hermetic tests pin projects-not-documents roster parse and empty-mapping refusal.
- Producer: claude-opus-5. PR body requests a non-Claude independent key (bus CI runner or Codex-family). CoS review is data only — does not satisfy that ballot.
- Ubuntu fail is `test_current_adoption_ledger` raising `PROJECT_SPEC_DRIFT` (failures=6, errors=2 on 3.13); not caused by this diff's new files. Windows jobs still in progress at review.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-70.md`. No GitHub PR comment posted.