schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 67
head: 6cf4fb8e721bc055dc95b3aa86e2fc3dd9ff7b31
reviewed_utc: 2026-09-15T06:33:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip 6cf4fb8 — R26 evidence ubuntu 3.13+3.14 FAIL (run 34937281870; PROJECT_SPEC_DRIFT / current census same pattern as #62 / #66 / prior tip / master); windows evidence + provider-capacity contracts still pending at review.

## Blockers

(none for this TRAPS row)

- **Hosted R26 evidence** red on ubuntu for tip `6cf4fb8` (run 34937281870, current project census / `PROJECT_SPEC_DRIFT`). Pre-existing adoption-ledger pattern — not introduced by a `TRAPS.md` append — but if that check is required to merge, refresh or waive R26 on the bus separately (same posture as #61–#64 / #66 and prior tip `efbcb71`).

## Improvements

- Probe / consumer wiring remains the live follow-through: when the transport has a stream record, type LIMITED from `status: rejected` and choose remedy from `overageDisabledReason` (`out_of_credits` → wait `resetsAt`; `org_level_disabled_until` → owner setting, do not re-auth). This tip now states that `claude -p --output-format json` cannot see those fields on 2.1.270 — only `stream-json --verbose` does — so a json-envelope tool should keep a live-model sentinel first, then type LIMITED on `api_error_status: 429` (404 = model absent), with text markers as last-resort fallback (as #64 c701560 landed).
- Extend any remaining `LIMIT_RE` / marker sets that still key primarily on rendered prose (`weekly limit` / `usage limit` / `five-hour limit`) so they do not miss `monthly spend limit`, or retire them once the structured / `api_error_status` path is primary.
- One consumer skim: account-parity GREEN + a live desktop session are not falsifiers for a CLI `rate_limit_event` refusal (row's two non-falsifiers; unchanged).

## Notes

- Delta vs prior CoS tip `efbcb71`: second commit `6cf4fb8` appends the **measured negative** — on claude-code 2.1.270 the structured reason is stream-only; the json envelope holds zero of `rate_limit_event` / `overageDisabledReason` / `rateLimitType` / `overageStatus` / `unifiedWindows`, but does carry `api_error_status` (429 vs 404 during outage). Addresses the implementer footgun of looking for the field in the wrong surface; does not change merge posture.
- Scope vs master: one path — `TRAPS.md` (+51 / −0). Docs-only; no code, no tooling delta. Two commits on the PR branch.
- Row still complements #64 rather than conflicting: #64 gates probe terminal; this names the field and the surface it is readable on.
- Bases on `master` (not stacked). MERGEABLE; mergeStateStatus UNSTABLE from checks. CoS reviewed via `gh` (no cloud agent; docs-only). No GitHub PR comment posted.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-67.md`.
