schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 67
head: efbcb718b7a6ccca9e536321cea6bfc4bda0b223
reviewed_utc: 2026-09-15T06:26:07Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip efbcb71 — R26 evidence ubuntu 3.13+3.14 FAIL (run 34936745695; PROJECT_SPEC_DRIFT / adoption-ledger pattern same as #62 / #66 / master); windows evidence + provider-capacity contracts still pending.

## Blockers

(none for this TRAPS row)

- **Hosted R26 evidence** already red on ubuntu for tip `efbcb71` (run 34936745695). Pre-existing PROJECT_SPEC_DRIFT / adoption-ledger pattern — not introduced by a +40 `TRAPS.md` append — but if that check is required to merge, refresh or waive R26 on the bus separately (same posture as #61–#64 / #66).

## Improvements

- Wire the structured classification into the probe that #64 landed (or lands): when `rate_limit_event` is present, type LIMITED from `status: rejected` and choose remedy from `overageDisabledReason` (`out_of_credits` → wait `resetsAt`; `org_level_disabled_until` → owner setting, do not re-auth); keep text markers as fallback only.
- Extend any `LIMIT_RE` / marker sets that still key primarily on rendered prose (`weekly limit` / `usage limit` / `five-hour limit`) so they do not miss the `monthly spend limit` wording this row measured, or retire them once the structured path is primary.
- One consumer skim: ensure account-parity GREEN + a live desktop session are not treated as falsifiers for a CLI `rate_limit_event` refusal (this row's two non-falsifiers).

## Notes

- Scope vs master: one path — `TRAPS.md` (+40 / −0). One commit. Docs-only; no code, no tooling delta.
- Row title: refusal sentence is chosen by `overageDisabledReason`, so a text-keyed classifier reads the wrong limit (dng-auto-processor discovery, UltraMagnus 2026-09-15 00:10 CDT). Complements #64 (probe terminal) rather than conflicting: #64 gates on rendered text + non-zero exit; this names the structured field the fix should key on.
- Measured refusal: identical `rate_limit_event` across haiku/sonnet/opus — `rateLimitType: seven_day`, `overageDisabledReason: org_level_disabled_until`, exit 1, `total_cost_usd: 0` — rendered as monthly spend limit (same `seven_day` type said weekly limit on 2026-08-31 when reason was `out_of_credits`).
- Bases on `master` (not stacked). MERGEABLE; mergeStateStatus UNSTABLE from checks. CoS reviewed via `gh` (no cloud agent; docs-only). No GitHub PR comment posted.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-67.md`.