schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 58
head: f276da1a966a37f29e510c476c7e048613031ad8
reviewed_utc: 2026-09-10T17:08:00Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip f276da1 — offline-regressions PASS; evidence ubuntu 3.13+3.14 FAIL (PROJECT_SPEC_DRIFT / test_current_adoption_ledger, pre-existing); contracts + windows evidence still pending on runs 34506089621 / 34506089632

## Blockers

(none for this PR's docs/BUS_SURFACES scope)

- **Hosted R26 evidence** (`evidence` ubuntu-latest 3.13 and 3.14) FAILED on this tip (Actions run 34506089632). Same `PROJECT_SPEC_DRIFT` / `test_current_adoption_ledger` pattern as master — pre-existing bus breakage, not introduced here (diff does not touch `specs/` or the ledger). Fix or waive on the bus separately if that check is required to merge. Sibling contracts + windows evidence still pending on the same tip.

## Improvements

- Prefer publishing live `cos-feedback/<project>/pr-<N>.md` via Contents API onto `master` once this PR lands (or as soon as `cos-feedback/<project>/` exists on master), rather than committing onto `docs/cos-feedback-20260910` — avoids re-waking the surface PR on every CoS publish (this tip's latest commit is only `cos-feedback/adversarialllm/pr-53.md`).
- After merge: projects still need consumer pull wiring (see PR #57 / consumer template) before hubs fold these files.

## Notes

- Later-push delta vs prior tip `6fd0f56`: only `cos-feedback/adversarialllm/pr-53.md` (CoS review of AdversarialLLM #53 ph-P3). Contract files (`README.md`, `SCHEMA.md`, `_example/`, `tools/doctrine-sync.mjs` BUS_SURFACES) unchanged.
- Verdict unchanged vs prior CoS pass: merge-when-ci-green for this PR's scope once required checks are green or R26 is waived as known master red.
- CANDIDATE surface; Law 1 data only; does not grant lane authority.
- CoS reviewed via `gh`. No GitHub PR comment posted.
- Bus-meta slug `_bus` (parallel to `_example`); no `specs/` board for the doctrine repo itself.
