schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 58
head: 532e37932a02c35bee5b08e0fb51223ab0b2f59a
reviewed_utc: 2026-09-10T13:16:47Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: offline-regressions PASS; R26 disposition evidence ubuntu 3.13+3.14 FAIL PROJECT_SPEC_DRIFT (same red on master); windows evidence + contracts still pending

## Blockers

(none for this PR's docs/BUS_SURFACES scope)

- **Hosted R26 evidence** (`evidence` ubuntu 3.13 + 3.14) fails `test_current_adoption_ledger` with `PROJECT_SPEC_DRIFT` before the expected refusal codes fire. Same workflow is repeatedly red on `master` today (heartbeats / traps), so treat as **pre-existing bus breakage**, not introduced by this PR (diff does not touch `specs/` or the ledger). Fix or waive on the bus separately if that check is required to merge.

## Improvements

- Prefer publishing live `cos-feedback/<project>/pr-<N>.md` via Contents API (or a short-lived cos-feedback-only branch) onto `master` once this PR lands, rather than committing onto `docs/cos-feedback-20260910` — avoids re-waking the surface PR on every CoS publish (this tip's only new commit is `cos-feedback/adversarialllm/pr-51.md`).
- After merge: projects still need consumer pull wiring (see PR #57 / consumer template) before hubs fold these files.

## Notes

- Later-push delta vs open tip `21a87493`: only `cos-feedback/adversarialllm/pr-51.md` (CoS review of AdversarialLLM #51). Contract files (`README.md`, `SCHEMA.md`, `_example/`, `doctrine-sync.mjs` BUS_SURFACES) unchanged.
- Open-time verdict unchanged: content looks mergeable for this PR's scope once required checks are green or R26 is waived as known master red.
- CANDIDATE surface; Law 1 data only; does not grant lane authority.
- Cursor Cloud Agents unavailable on plan; CoS reviewed via `gh`. No GitHub PR comment posted.
- Bus-meta slug `_bus` (parallel to `_example`); no `specs/` board for the doctrine repo itself.
