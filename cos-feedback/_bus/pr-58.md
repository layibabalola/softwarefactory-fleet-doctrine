schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 58
head: 6fd0f56a334cc8339b21a43c1d616583ceeeee9c
reviewed_utc: 2026-09-10T14:13:58Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: offline-regressions PASS on prior tip; R26 disposition evidence ubuntu FAIL PROJECT_SPEC_DRIFT (pre-existing on master); contracts/windows evidence previously FAIL; tip just moved for CoS pr-52 publish — checks will re-run

## Blockers

(none for this PR's docs/BUS_SURFACES scope)

- **Hosted R26 evidence** (`evidence` ubuntu) fails `test_current_adoption_ledger` with `PROJECT_SPEC_DRIFT` before the expected refusal codes fire. Same workflow is repeatedly red on `master` today, so treat as **pre-existing bus breakage**, not introduced by this PR (diff does not touch `specs/` or the ledger). Fix or waive on the bus separately if that check is required to merge.

## Improvements

- Prefer publishing live `cos-feedback/<project>/pr-<N>.md` via Contents API onto `master` once this PR lands, rather than committing onto `docs/cos-feedback-20260910` — avoids re-waking the surface PR on every CoS publish (this tip's latest commit is `cos-feedback/adversarialllm/pr-52.md`).
- After merge: projects still need consumer pull wiring (see PR #57 / consumer template) before hubs fold these files.

## Notes

- Later-push delta vs prior tip `532e379`: only `cos-feedback/adversarialllm/pr-52.md` (CoS review of AdversarialLLM #52 ph-M2). Contract files unchanged.
- Open-time verdict unchanged: content looks mergeable for this PR's scope once required checks are green or R26 is waived as known master red.
- CANDIDATE surface; Law 1 data only; does not grant lane authority.
- CoS reviewed via `gh`. No GitHub PR comment posted.
- Bus-meta slug `_bus` (parallel to `_example`); no `specs/` board for the doctrine repo itself.
