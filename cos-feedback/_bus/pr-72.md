schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 72
head: 545d8821840999f0cd3e60012ee1bea094910421
reviewed_utc: 2026-09-18T06:15:45Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip 545d882 — MERGEABLE=false / DIRTY (diverged ahead 13 / behind 12); Doctrine sync offline-regressions SUCCESS (run 35314035213) including Ruling-candidate backlog + TRAPS index + Derived-instrument regressions; Provider capacity governor contracts still red on master+PRs since 2026-09-10 (README byte-seal TRAP; not re-pinned here).

## Blockers

- **Merge conflict with `master` on `TRAPS.md` only.** Still `mergeable=false` / `mergeStateStatus=DIRTY`. Master has moved further under CoS `cos-feedback/**` publishes (behind now 12 vs prior 11) while append-only `TRAPS.md` remains the unmerged path. Fix: merge or rebase `origin/master`, resolve preserving both sides' appends, regenerate `TRAPS-INDEX.md` (`python tools/traps-index.py --write`) and confirm `python tools/traps-index.py --check`, then push. Do not land while dirty.

## Improvements

- Law 2 sole-writer: PR still rewrites `cos-feedback/README.md` (audiomile→airmypc) and `cos-feedback/airmypc/pr-1.md` project header. Prefer leaving live `cos-feedback/**` mutations to the CoS Contents path; README slug-list edit as contract docs is fine.
- Tip `f457e1d` (still in history) correctly records the provider-governor root cause as a TRAP and deliberately does **not** re-pin `manifests/universal-provider-control-reconciliation-r45.json`. Owner follow-up (separate from this PR): decide whether that control should bind living `README.md` at all, or run `refresh_current_universal_manifest.py` knowing the next README edit will re-break it. Tip `39c6ee8` grows README again (22,781→24,266 B) and states that interaction in-band — strengthens the trap case, does not cure the red.

## Notes

- **New tip (545d882):** `.github/workflows/doctrine-sync.yml` only — adds a report-only step that prints `python tools/candidate-flow.py || true` on every Doctrine sync run (exit 0 by design; never turns the job red). Surfaces the measured ruling-candidate backlog (34 candidates / 2 ever cited / 0 ever removed / 21 unrouted >7d at authoring) instead of measuring it in a dark corner. Does not change merge posture.
- Doctrine sync green on this tip (all steps including the new Ruling-candidate backlog step): https://github.com/layibabalola/softwarefactory-fleet-doctrine/actions/runs/35314035213
- Scope still sound: census-backed `fleet-membership` + sweep refuse-all-clear; `doctrine-sync` ack refuses silent rewind unless `--rewind "<reason>"`; `kernel-e2e` criterion_1 = distinct projects; `traps-index`; `candidate-flow` alarm (exit 0); heartbeat PS heuristic-fallback warning. Sealed control set / census JSON / evidence pin untouched.
- Owner-gated deferrals remain correct. `ack --commit` still cannot prove bytes were read.
- Bus-meta slug `_bus`. Consumers: `cos-feedback/_bus/pr-72.md`. No GitHub PR comment posted.