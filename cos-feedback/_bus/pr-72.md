schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 72
head: fa6c77122a54f02a02f9d5102d4331d13b72f145
reviewed_utc: 2026-09-18T06:00:15Z
event: pr-opened
verdict: not-merge-ready
ci_summary: tip fa6c771 — MERGEABLE/UNSTABLE; offline-regressions FAIL (run 35312975393) — Derived-instrument regressions: `No module named pytest` after setup-python 3.14 with no pip install; R26 evidence ubuntu 3.13+3.14 PASS; contracts + windows evidence still pending at review.

## Blockers

- `.github/workflows/doctrine-sync.yml` job `offline-regressions` / step **Derived-instrument regressions**: `python -m pytest …` fails with `No module named pytest` (run 35312975393, ~15s). `actions/setup-python@v5` does not install pytest. Fix: add an explicit `pip install pytest` (pin if the repo has a requirements file for these suites) immediately after setup-python and before the pytest step, then re-run.

## Improvements

- Law 2 sole-writer: this PR rewrites `cos-feedback/README.md` (audiomile→airmypc) and `cos-feedback/airmypc/pr-1.md` project header. Prefer leaving live `cos-feedback/**` mutations to the CoS Contents path; CoS can republish the airmypc slug correction after merge if needed. README slug list edit is fine as contract docs.
- Optional: add a `requirements-dev.txt` (or reuse an existing pin) referenced by the workflow so the new suites do not depend on an unbound `pip install pytest` floating version.

## Notes

- Scope (author claim, verified in file list): instrument repairs only — `fleet-membership.mjs` / `fleet-sweep.mjs` read `census.nonProjectSpecs` with heuristic fallback + refuse-all-clear; `doctrine-sync` ack refuses silent cursor rewind unless `--rewind "<reason>"`; `kernel-e2e.py` criterion_1 is distinct projects with ≥1 closed e2e (not fleet sum); new `traps-index.py` + generated `TRAPS-INDEX.md`; `candidate-flow.py` alarm (always exit 0); orphaned pytest suites wired into `doctrine-sync.yml`; heartbeat PS surfaces heuristic-fallback warning. Sealed control set / census JSON / evidence pin untouched.
- Design matches stated defect class (phantom boards, silent degradation, false-green finalisation). Negative-control tests in the new suites are the right shape; they just never ran in CI because pytest was missing.
- Owner-gated items correctly deferred (front-matter on 33 candidates, check_phase* deletion, byte-seal retirement, cherry-pick of never-landed companion candidate). `ack --commit` still cannot prove bytes were read — left explicit.
- audiomile vs airmypc: README + pr-1 header align cos-feedback slug with the pinned AirMyPC/AudioMile board; phantom eleventh census member correctly rejected as the wrong repair.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-72.md`. No GitHub PR comment posted.