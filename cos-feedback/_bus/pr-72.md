schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 72
head: 7d8d86dd5c27111985cb72b1de01323915f4de7a
reviewed_utc: 2026-09-18T06:19:54Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip 7d8d86dd — MERGEABLE / UNSTABLE; Doctrine sync offline-regressions SUCCESS (run 35314180778); R26 ubuntu evidence PASS (windows still in progress at review); Provider capacity governor contracts expected red (README byte-seal TRAP; not re-pinned here). Behind master by 1 (CoS pr-72 refresh 7ee44bd6 only).

## Blockers

(none)

## Improvements

- Law 2 sole-writer: PR still rewrites `cos-feedback/README.md` (audiomile→airmypc) and `cos-feedback/airmypc/pr-1.md` project header. Prefer leaving live `cos-feedback/**` mutations to the CoS Contents path; README slug-list edit as contract docs is fine.
- Still one commit behind `master` (`7ee44bd6` CoS feedback for prior tip). Harmless; next CoS publish or a quick `origin/master` merge absorbs it. Not a merge blocker (`mergeable=true`).

## Notes

- **Prior blocker cleared:** tip adds `.gitattributes` `merge=union` for `TRAPS.md` / `RECEIPTS.md` / `RULINGS.md` / `DISCOVERIES.md` and `-merge` for generated `TRAPS-INDEX.md`, then merges `origin/master` and regenerates the index. GitHub now reports `mergeable=true` / `mergeStateStatus=UNSTABLE` (was DIRTY on `TRAPS.md`).
- Scope still sound: census-backed `fleet-membership` + sweep refuse-all-clear; `doctrine-sync` ack refuses silent rewind unless `--rewind "<reason>"`; `kernel-e2e` criterion_1 = distinct projects; `traps-index`; `candidate-flow` alarm (exit 0) printed in Doctrine sync; heartbeat PS heuristic-fallback warning. Sealed control set / census JSON / evidence pin untouched.
- Provider-governor red remains pre-existing master red (README subjectFiles pin at 20kB). Owner follow-up stays outside this PR.
- Bus-meta slug `_bus`. Consumers: `cos-feedback/_bus/pr-72.md`. No GitHub PR comment posted.
