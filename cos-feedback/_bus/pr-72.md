schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 72
head: eff5179c20a449d62929d877870005b027a189f1
reviewed_utc: 2026-09-18T06:08:33Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip eff5179 — prior tip fa6c771 offline-regressions FAIL (pytest missing, run 35312975393) addressed by unittest conversion; no Actions runs observed yet on eff5179 at review; R26 intake PASS on prior tip; Provider capacity governor contracts FAIL on prior tip and repeatedly on master (pre-existing); mergeStateStatus was dirty/CONFLICTING earlier (diverged, behind 5) — recheck after TRAPS.md resolve.

## Blockers

- **Merge conflict with `master` on `TRAPS.md`.** Branch is diverged (ahead 9 / behind 5 vs `master`; merge_base `4938f33`). Overlapping path since base: `TRAPS.md` only (master also advanced `cos-feedback/**` and `heartbeats/adobe-ingester.json`, which this PR does not touch). Fix: merge or rebase `origin/master`, resolve `TRAPS.md` append-only conflict preserving both sides' entries, then regenerate `TRAPS-INDEX.md` with `python tools/traps-index.py --write` and confirm `python tools/traps-index.py --check` before push. Until the PR is mergeable, do not land.
- **Tip CI not yet observed on `eff5179`.** At review, Actions `total_count=0` for this head and `gh pr checks` reported none (old fa6c771 Doctrine sync failure cleared from the tip). After the conflict resolve push, confirm Doctrine sync `offline-regressions` (node suites + `traps-index --check` + unittest discover for the three new files) goes green. Do not treat the fa6c771 red as current.

## Improvements

- Law 2 sole-writer: PR still rewrites `cos-feedback/README.md` (audiomile→airmypc) and `cos-feedback/airmypc/pr-1.md` project header. Prefer leaving live `cos-feedback/**` mutations to the CoS Contents path; README slug-list edit as contract docs is fine. CoS can republish the airmypc slug correction after merge if needed.
- After resolving `TRAPS.md`, treat `TRAPS-INDEX.md` as derived — a stale checked-in index will fail the new `--check` step even when the instrument code is fine.

## Notes

- **Prior CoS blocker addressed:** tip `eff5179` converts `tests/test_traps_index.py`, `test_candidate_flow.py`, `test_kernel_e2e.py` to stdlib `unittest.TestCase` and switches `.github/workflows/doctrine-sync.yml` Derived-instrument regressions to `python -m unittest discover …` (no pip install). Matches repo convention; negative controls retained.
- Scope unchanged and still sound: census-backed `fleet-membership` + sweep refuse-all-clear on non-authoritative membership; `doctrine-sync` ack refuses silent rewind unless `--rewind "<reason>"`; `kernel-e2e` criterion_1 = distinct projects with ≥1 closed e2e; `traps-index` + generated index; `candidate-flow` alarm (exit 0); heartbeat PS surfaces heuristic-fallback. Sealed control set / census JSON / evidence pin untouched.
- Provider capacity governor contracts: author notes pre-existing red on master (runs 35299413205, 35278240274, 35269921879) at `check_universal_manifest.py:3164` / `ReviewResourceAdmissionR29Tests`. Treat as fleet-wide gate debt, not introduced by this instrument repair — still expect it red until fixed separately.
- Owner-gated deferrals remain correct (33-candidate front-matter, `check_phase*` deletion, byte-seal retirement, never-landed companion candidate). `ack --commit` still cannot prove bytes were read.
- Bus-meta slug `_bus`. Consumers: `cos-feedback/_bus/pr-72.md`. No GitHub PR comment posted.