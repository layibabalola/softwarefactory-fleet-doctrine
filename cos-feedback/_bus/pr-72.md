schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 72
head: a2db99f8791da35600875cafb54dce1230addc6e
reviewed_utc: 2026-09-18T06:10:39Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip a2db99f — MERGEABLE=CONFLICTING / DIRTY (diverged ahead 11 / behind 3); Doctrine sync offline-regressions SUCCESS (run 35313748099) on this tip; Provider capacity governor contracts still red on master+PRs since 2026-09-10 (README byte-seal TRAP; not re-pinned here).

## Blockers

- **Merge conflict with `master` on `TRAPS.md` only.** Confirmed `mergeable=CONFLICTING` / `mergeStateStatus=DIRTY` via local merge of `origin/master` into tip; unmerged path is solely `TRAPS.md` (other PR files auto-merge). Master since merge_base `4938f33` only touched `cos-feedback/**` + `heartbeats/adobe-ingester.json`, but append-only `TRAPS.md` still content-conflicts. Fix: merge or rebase `origin/master`, resolve preserving both sides' appends, regenerate `TRAPS-INDEX.md` (`python tools/traps-index.py --write`) and confirm `python tools/traps-index.py --check`, then push. Do not land while dirty.

## Improvements

- Law 2 sole-writer: PR still rewrites `cos-feedback/README.md` (audiomile→airmypc) and `cos-feedback/airmypc/pr-1.md` project header. Prefer leaving live `cos-feedback/**` mutations to the CoS Contents path; README slug-list edit as contract docs is fine.
- Tip `f457e1d` (still in history) correctly records the provider-governor root cause as a TRAP and deliberately does **not** re-pin `manifests/universal-provider-control-reconciliation-r45.json`. Owner follow-up (separate from this PR): decide whether that control should bind living `README.md` at all, or run `refresh_current_universal_manifest.py` knowing the next README edit will re-break it.

## Notes

- **New tip (a2db99f):** `RECEIPTS.md` only — answers `ruling-candidates/document-index-or-leaf-discipline-r1` §7 with measured bus numbers (TRAPS 3.9x/17d; traps-index structural runs vs naive `##` overcount; demote-in-place rejected for append-only + literal-path greps; sibling index offered as portable shape). Stated as DATA; ratifies nothing. Does not change merge posture.
- **Prior CoS tip-CI blocker cleared:** Doctrine sync `offline-regressions` is green on `a2db99f` (unittest discover + traps-index --check + node suites). Prior pytest FAIL on `fa6c771` was addressed by `eff5179`.
- Scope still sound: census-backed `fleet-membership` + sweep refuse-all-clear; `doctrine-sync` ack refuses silent rewind unless `--rewind "<reason>"`; `kernel-e2e` criterion_1 = distinct projects; `traps-index`; `candidate-flow` alarm (exit 0); heartbeat PS heuristic-fallback warning. Sealed control set / census JSON / evidence pin untouched.
- Owner-gated deferrals remain correct. `ack --commit` still cannot prove bytes were read.
- Bus-meta slug `_bus`. Consumers: `cos-feedback/_bus/pr-72.md`. No GitHub PR comment posted.