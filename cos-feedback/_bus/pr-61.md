schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 61
head: 13f0bc2387ad9812b1cbca97066fc6b248ca54e7
reviewed_utc: 2026-09-14T17:48:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip 13f0bc2 — R26 adoption ledger ubuntu 3.14 FAIL (PROJECT_SPEC_DRIFT in test_current_adoption_ledger); disposition intake ubuntu 3.13+3.14 FAIL same pattern; windows still pending (runs 34876874923 / 34876875064)

## Blockers

(none for this PR's Conjugal design-loop delta)

- **Hosted R26 evidence** FAILED on tip `13f0bc2`: `test_current_adoption_ledger` raises `PROJECT_SPEC_DRIFT` before the codes the tests assert (`ADOPT_`, `DISPOSITION_NOT_IN_PROJECT_EVIDENCE`, `PROJECT_CLOSED_SET_MISMATCH`). Same pre-existing ledger/census gate pattern as master / earlier bus PRs — not introduced by this two-file diff. Refresh or waive R26 on the bus separately if that check is required to merge.

## Improvements

- PR body CI-first-red claims `PROJECT_CLOSED_SET_MISMATCH` from a local closed-set enumeration; hosted tip first-red is actually `PROJECT_SPEC_DRIFT`. Update the PR description to the observed hosted first-red so reviewers do not chase the wrong gate.
- Optional: add a one-line comment in `DesignLoopMeasuresPanelFamilies` naming R9.4 / RULINGS so the regression stays tied to the binding ruling if the panel table wording drifts again.

## Notes

- Scope: +10/−0 across `specs/design-loop-protocol.md` (+1 line: record `panel_families` from completed seats) and `tests/test_review_posture.py` (+9: asserts Round 15 seated 2 families Claude×5/Codex×3, asserts the new sentence, asserts `"8 seats, 3 families"` absent). Second commit is merge of canonical harvest tooling; compare vs master still only those two files.
- Spec table Panel row already states Round 15 seated 2 and cites RULINGS R9.4; the new sentence closes the planned-roster vs seated-families gap. Provider-free unit test is appropriate.
- Author claims `python -m unittest tests.test_review_posture` 14/14; CoS did not re-run locally. Authority boundary in PR body is clear (Conjugal-owned proposal; no adoption / gate transition claimed).
- MERGEABLE / UNSTABLE while windows evidence pending and ubuntu evidence red. CoS reviewed via `gh`. No GitHub PR comment posted. Spec+test only — no cloud-agent review.
- Bus-meta slug `_bus` (parallel to `_example`); no `specs/` board for the doctrine repo itself. Path for consumers: `cos-feedback/_bus/pr-61.md`.
