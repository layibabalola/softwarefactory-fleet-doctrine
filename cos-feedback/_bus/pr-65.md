schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 65
head: da21e09c1e81a06aaef122905e18807d9822c8a6
reviewed_utc: 2026-09-15T03:43:09Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip da21e09 — R26 adoption ledger ubuntu 3.14 FAIL (PROJECT_SPEC_DRIFT in test_current_adoption_ledger, run 34925929864); disposition intake ubuntu 3.13+3.14 FAIL same pattern (run 34925929875); windows evidence + provider contracts still pending. Same pre-existing ledger/census red pattern as #61–#63 / master.

## Blockers

(none for this PR's three-path F1.4b transport)

- **Hosted R26 evidence** FAILED on tip `da21e09`: `test_current_adoption_ledger` raises `PROJECT_SPEC_DRIFT` before the codes the tests assert. Same pre-existing ledger/census gate pattern as earlier bus PRs (#61/#62/#63 CoS notes) — not unique to this rebuild. Refresh or waive R26 on the bus separately if that check is required to merge. This head wholesale-replaces `specs/adversarialllm.md` (`339e3505…` → `f920ff16…`), so any ledger row still pinning the prior bus spec blob stays drifted until census/ledger is updated to the new hash after merge.

## Improvements

- PR body asks for one read-only review leg per family to reproduce the transport proof on this exact head before squash-merge. CoS independently verified the byte claims via `gh` (see Notes); still useful for AdvLLM process closure to have those legs file evidence on the F1.4b row.
- After merge, retarget any AdvLLM F1.4b evidence file / doctrine-sync consumer so `specs/adversarialllm.md` on the bus and `factory/doctrine-entry.md` on AdvLLM stay hash-locked at `f920ff167cedb93dc3d57d6846c00b3b057a94a4`.
- Close or leave closed superseded bus #63 (already CONFLICTING/CLOSED) so conductors do not chase the old tip `4920846`.

## Notes

- Rebuild of #63 on bus master `ad68b844…` after another project's TRAPS append moved the base (`c4d901e` era). Same reviewed bytes; earlier head not force-pushed.
- Scope vs master `ad68b844…`: exactly three paths — `specs/adversarialllm.md` (+78/−4 wholesale), `TRAPS.md` (+50 append), `RECEIPTS.md` (+14/−1 append). `RULINGS.md` byte-identical (`d6f5a9c3…`). Law 2 respected; no heartbeat / other-project paths (unlike #55).
- CoS proof on tip `da21e09`: bus `specs/adversarialllm.md` git blob / `hash-object` = `f920ff167cedb93dc3d57d6846c00b3b057a94a4`, matching `factory/doctrine-entry.md` on AdversarialLLM-ClaudeCode `origin/master`. `TRAPS.md` pre 712278 B is a byte prefix of post 715622 B (suffix 3344 B); `RECEIPTS.md` pre 211647 B is a byte prefix of post 212682 B (suffix 1035 B). Matches PR body claims.
- D2 record cited: AdversarialLLM-ClaudeCode #41 (CTRL-3) merged `f3c23c4370e8f15de64cf4f9c716e414906c9477`. Source append blob claimed `661a14c7…` (`factory/doctrine/bus-append-2026-09-08.md`); CoS verified sizes/prefix on this tip, not re-hashed the source append file in this pass.
- Content: updates current-operation date / Claude controller handover; amends D2 wording; adds `## 2026-09-08 seam` (handover, tiers-by-act, equalize-tools trap, measured traps). TRAPS/RECEIPTS append matching family-confound and trap/receipt blocks.
- MERGEABLE / UNSTABLE while ubuntu R26 red and windows/contracts pending. Author: squash-merge once review legs reproduce proof so first parent is pre-merge master. CoS reviewed via `gh` only (Cloud Agents not on plan). No GitHub PR comment posted.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-65.md`. Do not confuse with `cos-feedback/adversarialllm/pr-65.md` (that file is AdvLLM-ClaudeCode PR #65, different board).