schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 66
head: 43c9a7b9214a51d6fc72944d5908a9769a1d8e11
reviewed_utc: 2026-09-15T05:23:23Z
event: pr-opened
verdict: not-merge-ready
ci_summary: tip 43c9a7b — R26 evidence ubuntu 3.13+3.14 FAIL (run 34932443096; same disposition-intake / adoption-ledger pattern as #62 / master); windows evidence + provider-capacity contracts still pending. Hosted workflows do not run test-selfheal.sh or tests/test_review_posture.py.

## Blockers

- **Stack order:** merge open PR #62 (`fix/review-posture-run-sh-launchers`, tip `e557f473`) first. This PR is one commit ahead of that branch and must not land alone.
- **Post-#62 rebase:** after #62 merges, rebase this branch onto `master` and re-run the TRAPS/RECEIPTS append-only check (author flags that #62's base predates later master appends). Do not merge while still stacked on the pre-rebase tip.
- **Hosted R26 evidence** already red on ubuntu for tip `43c9a7b` (run 34932443096). Same pre-existing PROJECT_SPEC_DRIFT / adoption-ledger pattern called out on `cos-feedback/_bus/pr-62.md` — not introduced by this tooling delta — but if that check is required to merge, refresh or waive R26 on the bus separately.

## Improvements

- Wire `tools/review-posture/tests/run-tests.sh` (and/or `tests/test_review_posture.py`) into a hosted workflow. Author acceptance is local-only today (`test-selfheal.sh` PASS=36, pytest 20); the repair ladder is exactly the kind of PATH/shim logic that regresses silently without CI.
- Confirm the dual-position sentinel ask with one real arbiter run (author residual: structurally addressed before and after DATA; astra 0/5 when ask was only after pasted output; live confirmation still open).
- After rebase, re-verify hermetic fakes still cannot reach a paid CLI under the repair ladder (pytest now empties PATH + pins HOME/APPDATA/CLI_NODE; tripwire claimed, worth one post-rebase dry-run from a spaced path).

## Notes

- Delta vs #62 tip: +1 commit `43c9a7b` — repair layer under #62's refusal layer. New `tools/lib/cli-resolve.sh` (verified fallback ladder + identity check; `cli_python` executes candidates; no machine mutation). `run.sh` gains path-dialect normalisation (`cygpath -m`), broader WSL refusal, fail-closed bindings (`worktree_digest`, `unknown` refuses on either side), REUSE disclosure for unbound ignored content, sentinel ask before DATA and as final contract item (≤1 retry), `environment.json` provenance. Large `test-selfheal.sh` + `run-tests.sh` hermetic suite.
- Intentional #62 test changes (flagged for #62's reviewer): `--dry-run` → `posture: NOT-MEASURED (dry-run)` exit 0; inventory-injection accepts `MODEL ID LINE REJECTED` **or** `MALFORMED MODEL ID`.
- Author adversarial record on tip: same-family V6/V7 no MERGE-BLOCKERS; cross-family X10 APPROVE at `43c9a7b`. CoS reviewed via `gh` (did not re-run the suite on Linux). No GitHub PR comment posted.
- Content posture once stack+rebase clear and R26 settled: would move toward `merge-when-ci-green` — repair design is fail-closed and residual limits are stated honestly (ignored content disclosed not bound; nested RP_OUT path-dialect limit; F1 live shim proof is hermetic A1 only).
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-66.md`.