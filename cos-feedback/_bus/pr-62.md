schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 62
head: c3827f1c077d1907e7bcfddc415a3cf13dcfccb7
reviewed_utc: 2026-09-14T21:54:30Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip c3827f1 — R26 disposition intake ubuntu 3.13+3.14 FAIL (PROJECT_SPEC_DRIFT in test_current_adoption_ledger before asserted ADOPT_/DISPOSITION_NOT_IN_PROJECT_EVIDENCE/PROJECT_CLOSED_SET_MISMATCH); windows evidence still in progress (run 34901228131). Hosted workflows do not run tests/test_review_posture.py.

## Blockers

(none for this PR's review-posture run.sh delta)

- **Hosted R26 evidence** FAILED on tip `c3827f1`: `test_current_adoption_ledger` raises `PROJECT_SPEC_DRIFT` before the codes the tests assert. Same pre-existing ledger/census gate pattern as master / earlier bus PRs — not introduced by this two-file tooling diff vs master. Refresh or waive R26 on the bus separately if that check is required to merge.

## Improvements

- Hosted CI never discovers `tests/test_review_posture.py` (disposition-intake / adoption-ledger workflows pin other `test_*.py` patterns only). Author acceptance still needs a steward `python -m unittest tests.test_review_posture` on Linux or macOS, plus one real `--dry-run` from a checkout path containing a space. Optional follow-up: add that suite to a workflow so path-quoting / LAUNCHER-BROKEN / fail-closed ids cannot regress unnoticed on the bus.
- `probe $c` relies on intentional unquoted word-split of fixed strings like `claude --version`. Fine today; if a probe ever needs a path with spaces, switch to a bash array per check.

## Notes

- Push event: merge of `origin/master` into `fix/review-posture-run-sh-launchers` (includes master's `--retry-missing` / labelled classifier tally from `2bed997`). PR vs master remains +134/−8 across only `tools/review-posture/run.sh` (+44/−7) and `tests/test_review_posture.py` (+90/−1). Prior CoS tip was `6bde47c`; this rewrite is for `c3827f1`.
- Fixes three measured defects (airmypc / VIRTUAL-TEN / TRAPS 2026-09-14): (1) `PY` as a bash array so checkout paths with spaces/`!` no longer split before `ids`; (2) inventory `ids` parsed and regex-validated, never `eval`'d (injection `x;touch PWNED` measured under eval); (3) every lane nickname resolved before any clear/dispatch; (4) bounded launcher preflight (`timeout -k 5 30`) probing real entrypoints, refuse with `LAUNCHER-BROKEN` / exit 2 (rc=127 broken npm shim used to drop every Codex lane while auth/model ids were healthy).
- Tests: executable call-logging fakes; fake `timeout` records `DISPATCH` instead of running paid CLIs; dry-run / broken-launcher / exec-entrypoint / inventory-injection / unresolved-nickname cases assert nothing dispatched. Author: 18/18 on Windows Git Bash at `6bde47c`, mutation-checked; Codex `gpt-5.6-sol` round 3 **APPROVE**. CoS did not re-run locally after the master merge.
- R9 tooling: PR body says tool steward merge; deliberately not auto-pushed to master. MERGEABLE / UNSTABLE while ubuntu R26 red and windows pending. Reviewed via `gh`. No GitHub PR comment posted. No cloud-agent review (plan / Linux factory floor constraints).
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-62.md`.
- Verdict unchanged vs prior CoS pass (`merge-when-ci-green`); prior Improvements not addressed on this push — chat quiet per later-push rule.