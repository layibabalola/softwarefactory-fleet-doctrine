schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 62
head: e557f4732db91ae08d033184455e9f0eb74ba010
reviewed_utc: 2026-09-14T22:01:15Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip e557f473 — R26 disposition intake ubuntu 3.13+3.14 FAIL (same pre-existing PROJECT_SPEC_DRIFT / adoption-ledger pattern as prior tip; run 34901844482); windows evidence still in progress. Hosted workflows do not run tests/test_review_posture.py.

## Blockers

(none for this PR's review-posture run.sh delta)

- **Hosted R26 evidence** FAILED on tip `e557f473`: ubuntu `evidence` jobs fail under the disposition-intake / adoption-ledger gate (same pre-existing ledger/census pattern as master / earlier bus PRs — not introduced by this two-file tooling diff vs master). Refresh or waive R26 on the bus separately if that check is required to merge.

## Improvements

- Hosted CI never discovers `tests/test_review_posture.py` (disposition-intake / adoption-ledger workflows pin other `test_*.py` patterns only). Author acceptance still needs a steward `python -m unittest tests.test_review_posture` on Linux or macOS, plus one real `--dry-run` from a checkout path containing a space. Optional follow-up: add that suite to a workflow so path-quoting / LAUNCHER-BROKEN / fail-closed ids / retry-missing roster cannot regress unnoticed on the bus.
- `probe $c` relies on intentional unquoted word-split of fixed strings like `claude --version`. Fine today; if a probe ever needs a path with spaces, switch to a bash array per check.

## Notes

- Push event: Codex sol round-4 on tip `e557f473` after master-merge tip `c3827f1`. Delta vs prior CoS head: `run.sh` scopes nickname resolve + launcher preflight to pending families under `--retry-missing` (unused broken family no longer blocks recovery of another family's lane); tests prepend fake bin *inside* bash (Git Bash PATH shadowing) and assert launcher probes hit logged fakes; new `test_retry_missing_does_not_preflight_families_it_will_not_dispatch`. Author: 19/19 mutation-checked.
- PR vs master remains two files: `tools/review-posture/run.sh` and `tests/test_review_posture.py` (+162/−9 overall). Fixes three measured defects (airmypc / VIRTUAL-TEN / TRAPS 2026-09-14): (1) `PY` as a bash array so checkout paths with spaces/`!` no longer split before `ids`; (2) inventory `ids` parsed and regex-validated, never `eval`'d; (3) every pending lane nickname resolved before any clear/dispatch; (4) bounded launcher preflight (`timeout -k 5 30`) probing real entrypoints, refuse with `LAUNCHER-BROKEN` / exit 2.
- Tests: executable call-logging fakes; fake `timeout` records `DISPATCH` instead of running paid CLIs; dry-run / broken-launcher / exec-entrypoint / inventory-injection / unresolved-nickname / retry-missing cases assert nothing unwanted is dispatched. Prior Codex `gpt-5.6-sol` round 3 **APPROVE** at `6bde47c`; round 4 applied here. CoS reviewed via `gh` (did not re-run the suite on Linux).
- R9 tooling: PR body says tool steward merge; deliberately not auto-pushed to master. MERGEABLE / UNSTABLE while ubuntu R26 red and windows pending. No GitHub PR comment posted. No cloud-agent review (plan / Linux factory floor constraints).
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-62.md`.
- Verdict unchanged vs prior CoS pass (`merge-when-ci-green`); prior CoS Improvements not clearly addressed on this push — chat quiet per later-push rule.
