schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 64
head: c701560fece28d2e9ffeab32abb3a8698d605a58
reviewed_utc: 2026-09-15T06:28:32Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip c701560 — R26 evidence ubuntu 3.13+3.14 FAIL (run 34937002856; PROJECT_SPEC_DRIFT / adoption-ledger pattern same as #62/#66/#67/master); windows evidence still pending.

## Blockers

(none for this PR's probe + PROMPT A content)

- **Hosted R26 evidence** red on ubuntu for tip `c701560` (run 34937002856). Pre-existing PROJECT_SPEC_DRIFT / adoption-ledger pattern — not introduced by the probe/PROMPT A delta — but if that check is required to merge, refresh or waive R26 on the bus separately (same posture as #61–#63 / #66 / #67). Windows jobs still pending at review time.

## Improvements

- Author test plan still open: re-probe when **codex** is the limited family, and on a machine where `$HOME/.claude` is not writable (evidence `mktemp` warn path + inventory leave-in-place).
- Confirm no inventory consumer treats a missing/stale family block as "family absent forever" while exit 4 leaves the previous file unchanged.
- When the Claude JSON envelope starts carrying `rate_limit_event` / `overageDisabledReason` (see #67 TRAPS row), prefer those for remedy selection (`out_of_credits` vs `org_level_disabled_until`); keep `"api_error_status":429` as the measured classifier for claude-code 2.1.270 `claude -p --output-format json` today.
- Optional hardening: match `api_error_status` with tolerant JSON whitespace (or a tiny `python -c` parse) so a pretty-printed envelope cannot miss LIMITED.

## Notes

- Third commit on tip `c701560` (after `6edac34` + `3ca0286`): on failed Claude probes only, one extra `claude -p --output-format json` call types the failure; plain sentinel call stays authoritative. Measured: live id → `api_error_status` 429, nonexistent id → 404 during the same outage. Text `LIMIT_RE` demoted to fallback (Codex today). JSON reply preserved in evidence.
- Addresses prior CoS Improvement on fragile text-only classification for Claude; does **not** yet key `rate_limit_event` because that field is absent from the JSON format this probe calls on 2.1.270 (complementary to #67's TRAPS measurement).
- Scope vs master unchanged: `tools/probe-machine-inventory.sh` and `bootstrap/PROMPT-A-sync-and-adopt.md` only. PROMPT A harvest/checker fixes unchanged from prior tip.
- Author re-run claim on limited account: 4/4 claude LIMITED via 429 path (429 in preserved JSON), 3/3 codex VERIFIED, exit 4, inventory left absent.
- MERGEABLE; mergeStateStatus UNSTABLE from checks. CoS reviewed via `gh` (no cloud agent). No GitHub PR comment posted.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-64.md`.
