schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 64
head: 3ca028687da30c6606ecce461af8099189dbb3bc
reviewed_utc: 2026-09-15T03:32:00Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip 3ca0286 — R26 disposition intake evidence jobs still IN_PROGRESS/QUEUED on ubuntu+windows × 3.13/3.14 (run 34925287978); no completed required check yet at review time.

## Blockers

(none for this PR's probe + PROMPT A content)

- **Hosted R26 evidence** not settled on tip `3ca0286` at review time (4 jobs pending/queued). Wait for green (or a known pre-existing ledger/census red waived the same way as #61–#63) before merge if that check is required.

## Improvements

- Author test plan still open: re-probe when **codex** is the limited family (current gate was proven with claude LIMITED + codex VERIFIED), and on a machine where `$HOME/.claude` is not writable (evidence `mktemp` warn path + inventory leave-in-place).
- Confirm no inventory consumer treats a missing/stale family block as “family absent forever” while exit 4 leaves the previous file unchanged — that is the intended safer error, but worth one consumer skim.
- `LIMIT_RE` covers spend/usage/session-limit phrasing measured on UltraMagnus; if other provider strings appear in the wild (`quota exceeded`, hard rate-limit pages), extend the regex or keep them as UNVERIFIED (inventory still written only when nothing is LIMITED).

## Notes

- Scope vs master: two paths — `tools/probe-machine-inventory.sh` (+62/−11) and `bootstrap/PROMPT-A-sync-and-adopt.md` (+15/−3). Two commits; second addresses cross-family review (non-zero-exit gate for LIMITED, `mktemp` evidence dir, current-tree adjudication ordering).
- Probe: typed `LIMITED` terminal requires non-zero `rc` **and** limit-text match so a live model answering about limits stays VERIFIED/UNVERIFIED; any LIMITED refuses write (`exit 4`), leaves prior inventory, copies non-verified replies under `~/.claude/machine-inventory-evidence/<mktemp>/`. Renames nonexistent `check-cli-auth.py` → `check-account-parity.py` in auth_probe / zero-verified message.
- PROMPT A: §0 checker name fixed; §2 documents exit 4; §4 harvest uses `origin/master` (not worktree `master`) and orders existing adjudication paths by last commit time (not `ls -t` checkout mtimes).
- Author retrodiction on the defect machine: 4/4 claude LIMITED, 3/3 codex VERIFIED, exit 4, inventory left absent — matches the measured UltraMagnus / DngAutoProcessor TRAPS story in the PR body.
- MERGEABLE while R26 evidence pending. CoS reviewed via `gh` only (no cloud agent). No GitHub PR comment posted.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-64.md`.
