schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 68
head: 92c9ec61c6b3cd8867368c10151fae52637881b5
reviewed_utc: 2026-09-15T08:07:51Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip 92c9ec61 — R26 evidence ubuntu 3.13+3.14 FAIL (run 34944953187; PROJECT_SPEC_DRIFT / current census same pattern as #62 / #66 / #67); windows evidence + provider-capacity contracts still pending at review.

## Blockers

(none for this transport)

- **Hosted R26 evidence** red on ubuntu for tip `92c9ec61` (run 34944953187, current project census / `PROJECT_SPEC_DRIFT`). Pre-existing adoption-ledger pattern — not introduced by append-only `TRAPS.md` / `RECEIPTS.md` / new adjudication filing — but if that check is required to merge, refresh or waive R26 on the bus separately (same posture as #61–#64 / #66 / #67).

## Improvements

- Treat the filing's `REBASE-NEEDED` header as binding for consumers: source lines 17, 27, 30, 35, 40, 57 are historical and not re-approved against subject blob `938308585e078ec399ec568d3e1c8caaae8e2081` / bus master `88f8e578`. A follow-up rebase PR (or explicit supersede) should re-grep those six REPLACES anchors before any harvest treats them as current.
- Keep the transport annotation removable: PR body states removing the one transport annotation restores source filing byte-for-byte — preserve that property on any amend.
- Cross-link source D2 records in harvest notes when folding: AdversarialLLM-ClaudeCode #63 / #64 / #66 (merged heads cited in PR body) so conductors do not re-stage from an older bench blob.

## Notes

- Scope vs master (`88f8e578`): three paths, +170 / −0 — new `adjudications/approach-a-design/AdversarialLLM.md` (58 lines), `TRAPS.md` append (+97), `RECEIPTS.md` append (+15). Docs-only transport; no code or tooling delta.
- Filing pins historical `subject_blob` `b41e3af00b757f96030e5127d170940776362cf0` and states six findings lack quote and/or REPLACES anchors against current v7.4/v7.5 path blob — explicit under DOC-V75-TRANSPORT contract; not a silent rewrite of historical bytes.
- Candidate object hashes in PR body match the three changed blobs claimed (`e5a0e607…`, `5dff3eb1…`, `30fde0a6…`). Append shapes look suffix-only (original byte lengths cited: TRAPS 717379 → +7408; RECEIPTS 216846 → +1662).
- This is a **transport proposal**, not a delivery or merge receipt for Approach A; posture none (bench guard denied `tools/review-posture/run.sh`).
- Bases on `master` (not stacked). MERGEABLE; mergeStateStatus UNSTABLE from checks. CoS reviewed via `gh` (no cloud agent; docs-only). No GitHub PR comment posted.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-68.md`.