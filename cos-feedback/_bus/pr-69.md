schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 69
head: 86c702d1cb9461b1181a756e697658147368250b
reviewed_utc: 2026-09-15T15:42:16Z
event: pr-opened
verdict: merge-when-ci-green
ci_summary: tip 86c702d1 — R26 evidence ubuntu 3.13+3.14 FAIL (run 34989853808; PROJECT_SPEC_DRIFT / current census same pattern as #62/#66/#67/#68); windows evidence + provider-capacity contracts still pending at review.

## Blockers

(none for this change set)

- **Hosted R26 evidence** red on ubuntu for tip `86c702d1` (run 34989853808, `PROJECT_SPEC_DRIFT` in `test_current_adoption_ledger`). Pre-existing adoption-ledger / project-census pattern — not introduced by the parity tool, hermetic suite, or append-only `TRAPS.md` / `RECEIPTS.md` / ruling-candidate — but if that check is required to merge, refresh or waive R26 on the bus separately (same posture as #61–#64 / #66–#68).

## Improvements

- `tools/check-account-parity.py`: nest `learn(c_fp, c_email)` inside the MATCHED branch (`d_fp and c_fp and d_fp == c_fp`) so the implementation matches the docstring and ruling-candidate R6.4.2. Today `learn` runs before the branch, so a DRIFT path still records the departed CLI fingerprint (harmless once MATCHED also learns the healthy account, but a guaranteed miss for the target lookup remains the DRIFT-only case).
- Consider wiring `python -m unittest discover -s tests -p "test_account_parity.py"` into a required or advisory workflow path so the hermetic suite cannot go silently unrun on hosted CI (R26 evidence today runs the adoption-ledger suite, not this file).
- Ruling candidate `ruling-candidates/cli-parity-detection-vs-repair-r1.md` is correctly **PROPOSED — ZERO AUTHORITY**; keep it out of any consumer gate until owner ratification appends R6.4 to `RULINGS.md`.

## Notes

- Scope vs master: five paths, +652 / −5 — `tools/check-account-parity.py` (+71/−5), new `tests/test_account_parity.py` (429), `TRAPS.md` append (+50), `RECEIPTS.md` append (+46), new `ruling-candidates/cli-parity-detection-vs-repair-r1.md` (56).
- Behavior review (via `gh`, no cloud agent): named `CLI-SIGNED-OUT` when desktop has a fingerprint and CLI `oauthAccount.accountUuid` is absent; still `return 0` always; no auto-repair on signed-out (adversarial rejection of widening repair is documented and tested); stdout flush before wizard; `learn` on signed-in CLI so MATCHED days populate the prefill map.
- Suite design looks sound for the Windows `Path.home()` / `USERPROFILE` trap: subprocesses, scrubbed PATH, cooldown stamp byte-equality proof that login launch was not reached, real credential-file hashes before/after. Docstring claims 23 tests; PR body cites live map growth after MATCHED learn on the measuring host.
- `tools/` is not a `doctrine-sync` surface — including `TRAPS.md` / `RECEIPTS.md` / the ruling-candidate so consumers can see the anatomy is the right bus hygiene.
- Bases on `master`. MERGEABLE; mergeStateStatus UNSTABLE from checks. No GitHub PR comment posted.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-69.md`.