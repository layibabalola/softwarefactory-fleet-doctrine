schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 69
head: 5ed448aa15dc67883ddd97d15e62e91d17f28fb7
reviewed_utc: 2026-09-15T16:00:06Z
event: pr-pushed
verdict: merge-when-ci-green
ci_summary: tip 5ed448aa — MERGEABLE/UNSTABLE; Account parity tooling parity-suite 3.13+3.14 PASS (run 34991943608, includes vacuity mutation); R26 evidence ubuntu 3.13+3.14 FAIL (run 34991943338, PROJECT_SPEC_DRIFT / adoption-ledger — same pre-existing pattern as #62/#66/#67/#68); windows evidence + provider-capacity contracts still pending at review time.

## Blockers

(none)

## Improvements

- Ruling candidate `ruling-candidates/cli-parity-detection-vs-repair-r1.md` remains **PROPOSED — ZERO AUTHORITY**; keep it out of any consumer gate until owner ratification appends R6.4 to `RULINGS.md`.
- Optional: after merge, a trivial master pull will absorb the intervening `cos-feedback/_bus/pr-69.md` commit(s) only — not a content conflict; do not treat that behind-by-1 as a rebase blocker.

## Notes

- Tip `5ed448aa` addresses both prior CoS Improvements: (1) `learn(c_fp, c_email)` is nested inside the MATCHED branch in `tools/check-account-parity.py` (no longer pre-branch); new `test_drift_does_not_learn_the_departed_account` pins it; suite now 24 tests. (2) new `.github/workflows/account-parity.yml` (windows-only matrix 3.13/3.14) runs the hermetic suite and fails the build if removing `learn()` still leaves the suite green.
- Also merged `origin/master` on the branch (resolved append-only `TRAPS.md` / `RECEIPTS.md` tails). GitHub `mergeable=true` / prior DIRTY conflict blocker cleared.
- Behavior unchanged in intent: named `CLI-SIGNED-OUT` (no auto-repair), still `return 0` always, stdout flush before wizard, zero credential mutation from the hook.
- Scope: `tools/check-account-parity.py`, `tests/test_account_parity.py`, `.github/workflows/account-parity.yml`, `TRAPS.md` / `RECEIPTS.md` appends, `ruling-candidates/cli-parity-detection-vs-repair-r1.md`.
- Behind master by the prior CoS feedback commit only (`2f1e73ee`); ahead 4. No GitHub PR comment posted.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-69.md`.