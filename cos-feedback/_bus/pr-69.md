schema: cos-feedback.v1
project: _bus
repo: layibabalola/softwarefactory-fleet-doctrine
pr: 69
head: 857d7e1325535970b132c306a5867b97ee7e55ec
reviewed_utc: 2026-09-15T15:56:01Z
event: pr-pushed
verdict: not-merge-ready
ci_summary: tip 857d7e13 — CONFLICTING vs master (ahead 2 / behind 7; mergeStateStatus DIRTY); no Actions runs yet on this tip; prior tip 86c702d1 R26 evidence ubuntu FAIL (run 34989853808, PROJECT_SPEC_DRIFT) and Provider capacity contracts FAIL (run 34989853938).

## Blockers

- **Rebase onto current `master` before merge.** GitHub reports `mergeable=false` / `DIRTY`. Branch tip `857d7e13` diverged from merge-base `5416236b` while master moved to `854ea399` with parallel appends to `TRAPS.md` and `RECEIPTS.md` (among other bus commits). Resolve those append conflicts (and any other overlapping hunks), then re-push so CI can run on a clean tip.

## Improvements

- `tools/check-account-parity.py`: nest `learn(c_fp, c_email)` inside the MATCHED branch (`if d_fp and c_fp and d_fp == c_fp:`) so the implementation matches the docstring / ruling-candidate claim that MATCHED is where the healthy account is learned. Tip still calls `learn` unconditionally at line ~111 before the branch (DRIFT still records the departed CLI fingerprint). Harmless once MATCHED also learns, but the prior CoS note is still open.
- Consider wiring `python -m unittest discover -s tests -p "test_account_parity.py"` into a required or advisory workflow path so the hermetic suite cannot go silently unrun on hosted CI (R26 evidence today runs the adoption-ledger suite, not this file).
- Ruling candidate `ruling-candidates/cli-parity-detection-vs-repair-r1.md` remains **PROPOSED — ZERO AUTHORITY**; keep it out of any consumer gate until owner ratification appends R6.4 to `RULINGS.md`.

## Notes

- Push `857d7e13` is a **claim correction only** (no behaviour change): narrows “prefill could only EVER learn abandoned accounts” to “no AUTOMATIC path primed a healthy account” across `tools/check-account-parity.py` docstring, `TRAPS.md`, `RECEIPTS.md`, and the MATCHED-learn test docstring. Commit states 23/23 still pass.
- Scope vs merge-base still five paths: `tools/check-account-parity.py`, new `tests/test_account_parity.py`, `TRAPS.md` / `RECEIPTS.md` appends, new `ruling-candidates/cli-parity-detection-vs-repair-r1.md`.
- Behavior review unchanged from open review: named `CLI-SIGNED-OUT`, still `return 0` always, no auto-repair on signed-out, stdout flush before wizard, MATCHED-path learn intent (placement still pre-branch).
- Prior CoS Improvement on nesting `learn` was **not** addressed by this push (docs only).
- Hosted R26 / provider-capacity reds on the previous tip look like the same pre-existing census / contracts pattern as #62/#66/#67/#68 — not introduced by this parity change — but cannot be re-judged until after rebase CI.
- Bases on `master` but is behind. No GitHub PR comment posted.
- Bus-meta slug `_bus`. Path for consumers: `cos-feedback/_bus/pr-69.md`.