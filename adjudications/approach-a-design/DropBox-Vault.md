project: DropBox Vault (Cloudvore)
providers: claude-only
seats: Designer-Scope, Designer-Contracts, Designer-Safety (claude-opus-5, effort=medium, disjoint slices)
rubric_id: unscored

<!-- Subject: specs/conjugal-approach-a-v7.4.md. Protocol: specs/design-loop-protocol.md §7.
     Test bench: C:\code\DropBox Vault -- every finding below names a Cloudvore path, tool or
     measured number, per the test-bench rule in this directory's README.
     posture computed, not asserted: 3 Claude lanes cleared the sentinel, 0 Codex lanes were
     dispatched, therefore this filing does NOT claim cross-family validation. -->

§9 | "at least 20 additional adopted items at that level within a forecast `5 h (clock_domain=real)` allowance" | At Cloudvore's measured rate the 5 h allowance is about 7× too short. `gate.py --json` shows 49 DONE rows since `BACKLOG.md` was created on 2026-09-06, which is about 0.29 items/h. That gives about 1.5 items in 5 h, or about 3 at a 2× target. | REPLACES: "within a forecast `5 h (clock_domain=real)` allowance" -> "within a forecast `20/(m×b) h (clock_domain=real)` allowance, UNKNOWN until b is measured" | PROOF: Run 2a on Cloudvore at five executors; if 20 adoptions land in ≤5 h, the finding is false.

§9 | "Baseline uses matched work complexity, acceptance criteria, provider configuration and calendar exposure of `336 h (clock_domain=real)`" | The 14-day baseline needs one configuration to hold, but Cloudvore's changes more often than that. `docs/operating-contract.md` changed in 10 commits between 09-07 and 09-09, and doctrine adoption followed on 09-12. Since "a configuration change invalidates qualification", 63-0 never completes. | REPLACES: "calendar exposure of `336 h (clock_domain=real)`" -> "calendar exposure of `max(72 h, time to 30 first adoptions)` per configuration_digest segment, segments pooled only when digests match" | PROOF: Replay Cloudvore's `git log` config digests over 09-06..09-13; any 336 h window with a single digest falsifies this.

§10 | "1a is Scenario 63-0's 336 h exposure and Phase 2 is B63 (§11)" | No calendar total exists from 1a to the 15-executor claim. With b≈0.29/h: T_2≈172 h, T_3≈115 h, T_5≈69 h. Add 336 h of baseline, the INSUFFICIENT extensions up to 2T_m and a 2a per level, and the floor is about 29 days, unstated. | REPLACES: "Phase 2 is B63 (§11)" -> "Phase 2 calendar forecast is `336 + Σ_m(T_m + 2a_m + 2c_m)` h derived by `manifest-sum.py`, with a 2T_m worst case, re-derived when b is measured" | PROOF: Compute T_m from Cloudvore's measured b; a total under 14 days falsifies this.

§9 | "An item is one subject first receiving an authorized ADOPTED record for its exact candidate, counted once" | Adoption is not landing, so the rate can be credited for work that never lands. Cloudvore's `gate.py` reports 49 DONE rows but `landed.checked=34`, and its operating contract says nothing notices an accepted packet that never landed. The master mirror runs only hourly and can end MIRROR_CONTENDED. | REPLACES: "first receiving an authorized ADOPTED record" -> "first receiving an authorized ADOPTED record whose candidate_commit_oid is reachable from master" | PROOF: Inject a MIRROR_CONTENDED window during 2b; if items still count only once reachable, the finding is false.

§9 | "ideal `2×/3.6×/6×`, targets `2×/3×/5×`" | The targets assume near-linear scaling on one shared checkout, but shared-checkout mutation is a serialized resource. Cloudvore peaked at 83 commits on 09-07 and fell to 18–34 a day under that same single-writer landing. A target equal to the ideal (2× at 5) leaves no margin. | REPLACES: "targets `2×/3×/5×`" -> "targets derived from the measured shared-checkout mutation μ via the resource gate, provisional `1.5×/2.5×/4×`" | PROOF: Run Scenario 68 on a Cloudvore clone at five executors; checkout mutation CI95.lower ≥ 2× baseline falsifies this.

§10 | "It also carries the §5 quota adapter, `16–24 engineering hours (provisional)`" | The only effort estimate in the spec covers the quota adapter. The eight-component vertical slice, 1a instrumentation and 1c–1e have none, since "remaining components are estimated from the completed slice". Cloudvore's `tools/metrics/2026-09-11-baseline.json` has `review_velocity_hours: null` for every packet, so there is no instrumentation to estimate from. | REPLACES: "Remaining components are estimated from the completed slice" -> "1a and 1b-build carry an upfront effort range per component; remaining components are re-estimated from the completed slice" | PROOF: Cloudvore already recording non-null per-packet velocity and waterfall data would falsify this.

§9 | "provisional or zero b plans at the `48 h (clock_domain=real)` floor and yields absolute rates only" | This lets 2b spend 48 h per level without producing any multiple, and 2c then requires a multiple for the next level. Cloudvore's baseline capture shows `throughput_contribution: "1 packet, TBD hours"` for every packet, so b is provisional today and 2b would burn about 144 h without unlocking scaling. | REPLACES: "provisional or zero b plans at the `48 h (clock_domain=real)` floor" -> "2b does not start until M1 b is measured; provisional or zero b blocks 2b" | PROOF: Start 2b with Cloudvore's current metrics file; if 2c issues a nine-executor unlock, the finding is false.

§1 | "`P_par=4 (provisional)` concurrent per-row clones (longest first; rows 20 and 68 pinned to one exclusive host class" | The 1d-extended forecast is a serial sum, yet the phase runs 4 clones on one Windows host that also runs production. Cloudvore records `state.py` exceeding 300 s on this host, so clone contention overruns fixed STUB budgets and fails the row. | REPLACES: "a feasible resource-constrained makespan at `P_par=4 (provisional)`" -> "a feasible resource-constrained makespan at `P_par` measured by a host-contention calibration run, default 1" | PROOF: Run four STUB-1200 rows in parallel on the Cloudvore host; zero budget overruns falsifies this.

§12 | "the R13–R15 plans are … `1.72M` tokens; … the allowance is `1.5× (provisional)`" | Tier 0 has no calendar or weekly-quota forecast: K_repair=3 rounds × 2.58M tokens against weekly provider limits is unconverted. Cloudvore's rotation doc mentions an 89% weekly-usage threshold, but no measured per-round token or calendar cost exists to test against. | REPLACES: "plans are forecasts" -> "plans are forecasts converted to calendar via F_p against W_p per round" | PROOF: Receipt-derived R15 usage showing it fits in one weekly allowance with K_repair headroom.

§5 | "extends `coordination/tools/check-cli-auth.py --json` with `providers[p].{account_id, limit_lower, limit_upper, remaining_lower, reset_at…}`" | That contract is not grounded in Cloudvore. The tool lives at `tools/check-cli-auth.py`, and `coordination/tools/` only holds `session-start-auth.py`. Its JSON gives only Claude percentages (`five_hour_pct`, `seven_day_pct`), with no integer bounds and no Codex key. | REPLACES: "extends `coordination/tools/check-cli-auth.py --json` with" -> "extends the repository's resolved auth tool (`tools/check-cli-auth.py` in Cloudvore), mapping its percent-of-window fields to" | PROOF: `python tools/check-cli-auth.py --json` has top-level keys `auth_status, cli, declared, desktop, fix, reasons, verdict`, and there is no `providers.codex`.

§5 | "`unit`: `request` → one per provider request; `token` → …; `credit` → Σ authenticated rate coefficients" | The unit list has no percent-of-window unit. The only measured quota Cloudvore has is `seven_day_pct` and `five_hour_pct`, so every `budget_p` stays UNKNOWN and M5/M6/F_p can never be computed. The G-MEASURE scale claim is then blocked forever. | REPLACES: "`credit` → Σ authenticated rate coefficients × metered quantities" -> "`credit` → Σ authenticated rate coefficients × metered quantities; `window_pct` → integer percent of a named provider window, remaining_lower=100−pct" | PROOF: run the Scenario 70 adapter against the live Cloudvore output. If any `providers[p].remaining_lower` other than UNKNOWN comes out, this finding is false.

§5 | "Its source of truth is fresh `HEAD:coordination/comms/opus.md` plus archives" | The relay's only delivery target is a hardcoded mailbox that does not exist on the bench. ROTATE_REQUEST has no owner-visible destination, so the automatic rotation resume depends on a channel that is not there. | REPLACES: "`HEAD:coordination/comms/opus.md`" -> "`HEAD:<policy.outbox_mailbox_path>`, validated to exist by the §0 policy lint" | PROOF: `ls "C:/code/DropBox Vault/coordination/comms"` gives "No such file". Scenario 72 run against Cloudvore HEAD cannot find the marker.

§0 | "Luna and Sonnet are default implementers; Haiku supplies second-seat votes… `keyholders: [sol, astra, opus, fable]`" | Autonomy depends on persistent named lane seats. Cloudvore's `docs/operating-contract.md:93` says "There are no persistent seats, standing hub, or recursive delegation". No floor holds a lane key, sidecar or scheduled wake, so V seats are never available. | REPLACES: "Families and eligibility are defined once" -> "Lanes are session-held roles bound to key custody, not standing seats; families and eligibility are defined once" | PROOF: on Cloudvore with no live session, a V_REQUEST gets WAIT-COMMITTEE and then BLOCKED-CAPACITY after 15 min, every time.

§7 | "with zero leased executors `1,020 s (clock_domain=real, provisional)` (floor wakes only; a never-firing floor: §13)" | Floor wakes are the only liveness path when no executor is leased, yet the spec never requires a floor to exist or be registered. Cloudvore's contract (`operating-contract.md:50`) says "scheduled continuation is not currently running work", so this case is the default here, not a residual. | REPLACES: "a never-firing floor: §13" -> "genesis lint requires ≥1 registered, recently-fired floor task per family; absent one, admission records BLOCKED-CAPACITY" | PROOF: kill all sessions in Cloudvore and count how long before a dead reducer is replaced. If it happens within 1,020 s, this finding is false.

§14 | "the repository copy is fragmented by section under `docs/architecture/approach-a/design/`, rebuilt by `tools/build_single.py`" | The acceptance contract names artifacts that are missing on the bench. `docs/architecture/` does not exist, and neither do `tools/build_single.py` or `tools/manifest-sum.py` (the tool every §10/§11 literal is checked against). Scenarios 27 and 67 cannot run. | REPLACES: "rebuilt by `tools/build_single.py`" -> "rebuilt by `tools/build_single.py`, whose presence and that of `manifest-sum.py` is a 1a entry gate" | PROOF: `ls tools/build_single.py tools/manifest-sum.py docs/architecture` in Cloudvore gives three "cannot access" errors.

§4 | "Each helper is a fixed-action Windows scheduled task under its dedicated principal; floors hold task-start permission only" | The spec never says how the four custody principals and tasks get provisioned, or who owns that step. Cloudvore has four unrelated `create-o03-automation.ps1`/`install-continuity-beat.ps1`-style installers and no helper installer. A helper cannot pass HELPER_CAPABILITY_PROBE, so no SEAL is ever possible. | REPLACES: "floors hold task-start permission only" -> "floors hold task-start permission only; `tools/install-attest-helper.ps1` (owner-run, 1b) registers principal, ACL and task and emits the enrollment QUALIFY evidence" | PROOF: search Cloudvore `tools/` for any installer that registers an attestation principal. Finding one falsifies this.

§12 | "Tier 0 goes DORMANT: a round reopens only on a failed safety scenario or a REJECTED owner ratification" | Once DORMANT, nothing in Tier 0 produces work. This is the idle-queue trap Cloudvore's gate explicitly treats as a trigger: `work=no-eligible-work` means cutting packets from `docs/product-roadmap.md`. The loop has no rule to resume from the roadmap. | REPLACES: "never on score alone" -> "never on score alone; an empty eligible queue triggers packet cutting from the product roadmap, not dormancy" | PROOF: `python tools/gate.py --json` reporting `work=no-eligible-work` while Tier 0 is DORMANT gives an idle factory. Any spec path that yields new work in that state falsifies this.

§3 | "REAFFIRM {proof_id,candidate_digest,committee_epoch,assignment_digest}" | There is no registry row saying who triggers REAFFIRM when a lane is demoted mid-epoch. A V lane has to act on its own initiative, with no dispatch obligation listed in the §3 carry-over enumeration. | REPLACES: "an unrelayed outbox entry)" -> "an unrelayed outbox entry, a REAFFIRM owed by a retained voter)" | PROOF: a fixture with a committee_epoch bump and a retained valid proof. If a REAFFIRM request is dispatched without lane initiative, this finding is false.

§7 | "Private keys reside under `~/.claude/oracle/keys/<actor>.ed25519`" | Lane and keyholder keys get no ACL; only custody principals do. On Bachelor, `C:\Users\redacted-user\.claude` grants BACHELOR\redacted-user (OI)(CI)(F), and the `~/.codex` and `~/.claude` runners both run as redacted-user. A Sol runner can read opus.ed25519 and sign for both families. | REPLACES: "Private keys reside under `~/.claude/oracle/keys/<actor>.ed25519`" -> "Each lane key resides under a per-family OS principal, ACL-denied to every other family's runner SID; installation lint refuses signing when both families' runners share a SID" | PROOF: As the Codex runner user, read `~/.claude/oracle/keys/opus.ed25519` and sign a POLICY envelope. The spec holds only if the read is denied.

§4 | "floors hold task-start permission only, never task-edit, binary-write or key access" | This can't be enforced on the bench. `redacted-user`, the only runner account, is in local Administrators (`net localgroup administrators`), so any floor can edit helper tasks or take ownership of helper keys. §13's excluded administrator compromise becomes the normal operating state, and the four independent helpers collapse into one trust domain. | REPLACES: "floors hold task-start permission only" -> "floors run under non-Administrators SIDs, verified by token-group check at every floor start; an elevated or Administrators-member floor is ineligible to start helpers" | PROOF: From a redacted-user floor, run `takeown` on a helper key file or `schtasks /change` on a helper task. Either one must fail.

§7 | "Reducer mutex identity is the `.git` directory file identity" | `git worktree list` shows 5 checkouts on the bench. In a linked worktree `.git` is a separate gitdir file, yet `refs/oracle/*` is shared. So each worktree hashes to a different `Global\ConjugalOracle-` mutex, and a reducer in `.claude/worktrees/sharp-khorana-d1e62a` takes over after 120 s while the original holder is only stalled. | REPLACES: "the `.git` directory file identity" -> "the file identity of `git rev-parse --git-common-dir`, with oracle-reduce.py refusing to run from a linked worktree" | PROOF: Start the reducer in the master checkout, suspend it 130 s, then run `--ensure` in a linked worktree. Two live lock holders after resume proves the defect.

§0 | "enumerating the surviving cross-family pairs per helper loss; any swap, omission, duplication or mislabel fails" | The lint has no minimum keyholder count per family. The bench roster (`knowledge/lane-roster-2026-08-28-driver-handover.md:21,33`) has Sol as the only CODEX key and no Astra lane. Revoking Sol's key then has no non-target CODEX signer and falls to the emergency key alone. | REPLACES: "one emergency key per family and at least two enrolled attestation helpers per family" -> "one emergency key per family, at least two live keyholders per family and at least two enrolled attestation helpers per family" | PROOF: Lint a policy with keyholders [sol, opus, fable]. The defect is confirmed if it passes and Scenario 56's REVOKE of sol succeeds only through CODEX's emergency key.

§5 | "extends `coordination/tools/check-cli-auth.py --json` with `providers[p].{account_id, limit_lower, limit_upper, remaining_lower" | The bench tool is `tools/check-cli-auth.py`; `coordination/tools/` holds only `session-start-auth.py`. Its JSON has only Claude `cli`/`desktop` surfaces with percent fields (five_hour_pct=35) and no codex account fingerprint. Codex ROTATION_COMPLETE can never verify, so §13.1 has no bound. | REPLACES: "extends `coordination/tools/check-cli-auth.py --json`" -> "extends the host's resolved auth checker (Cloudvore: `tools/check-cli-auth.py`) with a codex surface emitting an account fingerprint; percent-only evidence is UNKNOWN" | PROOF: Run `python tools/check-cli-auth.py --json`. With no codex fingerprint, Scenario 30's ROTATION_COMPLETE on a changed Codex account cannot pass.

§5 | "commits the merged blob by private index (`read-tree HEAD`, `commit-tree`, `update-ref` CAS on master's expected tip" | Automated relay commits, and the hourly §1 mirror, land on master outside the bench's six-criterion merge grant (`knowledge/owner-gated-decisions.md:23`). They also target `HEAD:coordination/comms/opus.md`, which doesn't exist. Unreviewed commits enter the master chain that `gate.py landed=` trusts. | REPLACES: "`update-ref` CAS on master's expected tip" -> "`update-ref` CAS on a dedicated `refs/oracle/mailbox` ref, never master" | PROOF: Inject one Codex latch. The defect is confirmed if master gains a commit that fails the owner-gated-decisions.md:23 merge-authority criteria.

§0 | "Widening eligibility requires §6; no third quorum family exists." | Nothing in the spec takes precedence over host doctrine. The bench adopted this design (commit f4c45e1), but `knowledge/cross-family-review-gate-policy.md:89` accepts "two same-family adversarial reviewers" when Codex is unavailable, so the host rule substitutes a family during a latch. | REPLACES: "no third quorum family exists." -> "no third quorum family exists; host doctrine permitting same-family substitution is void for oracle authority and fails installation lint." | PROOF: On Cloudvore with Codex latched, check whether a same-family pair under cross-family-review-gate-policy.md:89 is recorded as satisfying any gate.

§6 | "a family emergency key (§0), enrolled at genesis or by amendment, substitutes for its own family's unavailable signer for REVOKE only" | If one owner holds both emergency keys under one SID, one actor can REVOKE every helper checker and stop all SEALs fleet-wide (§13.3). | REPLACES: "substitutes for its own family's unavailable signer for REVOKE only" -> "substitutes for its own family's unavailable signer for REVOKE only, never both families' signers in one REVOKE" | PROOF: Sign one REVOKE of all four helper checkers with both emergency keys. The defect is confirmed if it applies.

## Provenance

- A-opus-swarm-1 (claude-opus-5, effort=medium) -- Timeline Realism, Throughput Goal: 9 findings
- A-opus-swarm-2 (claude-opus-5, effort=medium) -- Contract Completeness, Autonomy Achievement: 9 findings
- A-opus-swarm-3 (claude-opus-5, effort=medium) -- Cross-Family Safety, Risk Mitigation: 8 findings

Dispatched over `claude -p` per specs/cli-orchestration-standard.md (PROPOSED). Each lane was
required to emit a completion sentinel; all three did. Lane telemetry -- raw token counts stamped
at dispatch, for retroactive costing against metrics/pricing.jsonl -- is in metrics/.

Filed 2026-09-13. Cloudvore is the single writer of this file.
