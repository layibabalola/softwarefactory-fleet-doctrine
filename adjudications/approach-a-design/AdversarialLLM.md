project: AdversarialLLM
providers: claude(opus) codex(sol)
seats: opus x4 independent fresh contexts (A bench-fit falsifier, B universality attacker, C dogfooding-loop auditor, D model-floor post-mortem); sol x1 (bench-fit, effort high); hub synthesis opus (dedupe only, no new findings)
rubric_id: unscored
posture: none (tools/review-posture/run.sh not run: the bench guard denies it under factory/RESET-PLAN.md §1.3; RULINGS R9 forbids naming a posture that did not run)
cross_family: validated (claude A and codex sol reached 8 of these anchors independently; tags [A][B][C][D][S] name the seats behind each line)
panel: unscored
subject_blob: b41e3af00b757f96030e5127d170940776362cf0 (header says v7.5, filename v7.4; bus master 0a5b49cd at review time)
bench: https://github.com/layibabalola/AdversarialLLM-ClaudeCode @ 9efa9fd79f0fd1468e543f10950d3ec8116e52cf
staged_under: factory/doctrine/adjudications/approach-a-design/AdversarialLLM.md (reviewed under the bench's D2 before transport)
REBASE-NEEDED: Historical findings retained verbatim from source blob 3417f642f2b68ab0bf2b947301b5c1b7d9adc91d; checked against bus master 88f8e578aa40ad55e568105840242dbd2a1a32f6, spec blob 938308585e078ec399ec568d3e1c8caaae8e2081. Source finding lines 17 (§0: quote absent, REPLACES anchor absent); 27 (§3: quote absent, REPLACES anchor absent); 30 (§4: quote absent, REPLACES anchor absent); 35 (§4: quote absent); 40 (§6: quote absent, REPLACES anchor absent); 57 (§12: quote absent, REPLACES anchor absent). These findings have not been rebased or re-approved against this newer subject.

## Findings
§header | "Filename kept at v7.4 so filers' references stay valid." | Subject was rewritten to v7.5 mid-round under one path; earlier and later filings cite different blobs, so REPLACES anchors can silently target text that no longer exists. [C] | REPLACES: "Filename kept at v7.4 so filers' references stay valid." → "Every filing header MUST carry `subject_blob:`; harvest marks a filing whose subject_blob differs from the harvested base REBASE-NEEDED and re-greps each REPLACES anchor in the current blob." | PROOF: bus `git log -- specs/conjugal-approach-a-v7.4.md` → f3043cc 2026-09-14 13:18 (v7.5) after 2e2bce8 (v7.4); this filing pins b41e3af0
§0 | "on one shared Windows checkout, `C:\code\Conjugal`, using git refs and per-lane journals" | A universal core hard-codes one project's root, OS and shared-checkout model; the bench uses per-implementation feature worktrees plus immutable per-attempt review worktrees and has no `coordination/` tree. [A][B][S] | REPLACES: "on one shared Windows checkout, `C:\code\Conjugal`, using git refs and per-lane journals" → "on the project's declared `policy.workspace {root, vcs, host_os, checkout_model}` (reference profile: one shared Windows git checkout; alternative: one feature worktree per implementation and one immutable worktree per review attempt), every `coordination/` path and `Conjugal*` name resolved through a genesis `project_id`, using git refs for Oracle state and per-lane journals" | PROOF: factory/HUB.md:11,13,19; `git -C <bench> ls-tree -r --name-only HEAD` piped to `grep -E` for paths starting `coordination/` or containing `oracle` → empty
§0 | "second_vote_eligible: [sol, astra, luna, opus, fable, sonnet, haiku]" | Admits Haiku to a sealing vote; the bench confines Haiku to narrating status output and staffing swarms, and a swarm never turns a merge key. [A][S][D] | REPLACES: "second_vote_eligible: [sol, astra, luna, opus, fable, sonnet, haiku]" → "second_vote_eligible: [sol, astra, luna, opus, fable, sonnet]   # eligibility follows the act; haiku never supplies certification or merge authority" | PROOF: factory/RESET-PLAN.md:54-63; factory/HUB.md:11; PR #62 (layibabalola/AdversarialLLM-ClaudeCode) shows a Haiku session committing a high-risk gate edit
§0 | "keyholders: [sol, astra, opus, fable]" | With Opus and Fable exhausted, every Codex-implemented subject parks WAIT-FAMILY then BLOCKED-CAPACITY although Sonnet is available; bench rule is to fall to the next tier and keep going. [A] | REPLACES: "keyholders: [sol, astra, opus, fable]" → "keyholders: [sol, astra, opus, fable]   # when every keyholder of a family is DARK, V.a falls to that family's next tier (sonnet, luna), recorded as tier_fallback on the certificate" | PROOF: factory/RESET-PLAN.md:62-63
§0 | "no third quorum family exists." | Seats are model lanes only; a human-judgement seat has no legal slot, so on the bench human judgement happened outside the protocol as a queue head held about 40 days. [B] | REPLACES: "no third quorum family exists." → "no third quorum family exists; a project may declare `judgement_seats: {seat_class: [principal], required_for: [artifact_class]}`, a non-family seat that adds a required vote (never replaces a cross-family V pair), with its own review_horizon (§3)." | PROOF: factory/RESET-PLAN.md:129 A19 live-sweep head "human-held" ~40 days
§1 | "`refs/oracle/claim/<S>` points to `{subject, owner, lease_gen, claimed_at, job_id, pid, creation_time, paths_digest}`" | No refname grammar for S; bench row ids that pass validation can form git-illegal ref components, turning the claim CAS into a permanent infrastructure failure. [A] | REPLACES: "`refs/oracle/claim/<S>` points to" → "`refs/oracle/claim/<S>`, S = lowercase hex `sha256(subject)[:32]` (raw subject only in payload), points to" | PROOF: factory/queue-state.mjs:6 id regex; `git check-ref-format refs/oracle/claim/a..b` → exit 1; `refs/oracle/claim/x.lock` → exit 1
§1 | "COMPLETE verifies candidate object/tree/patch consistency against the recorded base" | Nothing refuses patch paths outside the subject's path allocation, so a candidate carrying an unrelated queue row and an undeclared gate change still verifies as consistent. [D] | REPLACES: "COMPLETE verifies candidate object/tree/patch consistency against the recorded base" → "COMPLETE verifies candidate object/tree/patch consistency against the recorded base and refuses if any patch path lies outside `path-allocation/<S>`" | PROOF: `git log --stat origin/master..origin/feat/guard-allow-doctrine-merge-bootstrap` → factory/queue.jsonl and an undeclared guard block inside a merge-allowlist PR
§2 | "`C = {subject, phase, verdict_ver, implementer, lease_gen, base_commit_oid, candidate_commit_oid, candidate_tree_oid, patch_sha256, acceptance_spec_sha, dependency_certificate_digests, policy_sha}`" | C omits repository, row, PR, round, attempt id, per-head author families and a measured implementer model; the bench validator rejects this identity as not evaluable. [S][D] | REPLACES: "`C = {subject, phase, verdict_ver, implementer, lease_gen," → "`C = {repository, row, rowless, pr, subject, phase, verdict_ver, implementer, implementer_model_receipt_digest, author_families, review_round, attempt_id, lease_gen," | PROOF: executed `evaluateReview({attempt:C,...})` (factory/review-state.mjs:14-17) → INFRASTRUCTURE_FAILURE/INVALID_ATTEMPT
§2 | "candidate_tree_oid, patch_sha256, acceptance_spec_sha" | A text patch digest assumes a readable diff; binary artifacts (screenshots, renders, .blend, .uasset) have no reviewable patch, so the bound digest proves nothing a reviewer can judge. [B] | REPLACES: "candidate_tree_oid, patch_sha256, acceptance_spec_sha" → "candidate_tree_oid, artifact_manifest_sha256 (sorted {path, artifact_class, sha256, size}), acceptance_spec_sha" | PROOF: adversarialllm/evidence/observe/20260720-070531/ holds 8 binary *.png evidence files
§2 | "review reads these immutable objects, never worktree bytes" | Visual, audio, game-feel and render outcomes need bound derived evidence, or model reviewers judge source they cannot evaluate. [B] | REPLACES: "review reads these immutable objects, never worktree bytes" → "review reads these immutable objects plus the content-addressed derived evidence declared by acceptance_spec (screenshots, captures, renders), bound as `evidence_digests` in C, never worktree bytes" | PROOF: adversarialllm/evidence/observe/20260720-070531/startup_failure-final-*.png — the UI failure state is judgeable only from screenshots
§2 | "All executors are non-breakaway Job Objects with kill-on-close." | Bench worker host kills a tree with Process.Kill($true) after 300 s and has no Job Object, so the stop-barrier receipt is unproducible; macOS/Linux hosts have no Job Objects at all. [A][B] | REPLACES: "All executors are non-breakaway Job Objects with kill-on-close." → "All executors run under the host_os's declared process-group primitive (Windows: non-breakaway Job Objects with kill-on-close; POSIX: process group plus cgroup/launchd kill); a host lacking it records every termination receipt `unverified` and keeps paths retained (BLOCKED-TERMINATION)." | PROOF: factory/worker-host.ps1:141-143; scripts/never-authorized-guard.mjs:152-157
§3 | "V.a is an available keyholder in the family opposite the implementer" | Remediation can make both families authors of the current head; "opposite the implementer" is then undefined, while the bench keys on every recorded author family. [A][S] | REPLACES: "V.a is an available keyholder in the family opposite the implementer" → "V.a is an available keyholder in no family of C.author_families (every recorded producer of candidate bytes); a two-family author set has no eligible V.a and records WAIT-FAMILY" | PROOF: executed evaluateMerge with authors.families=["claude","codex"] and two approvals → NON_AUTHOR_APPROVAL_REQUIRED (factory/review-state.test.mjs:158)
§3 | "(i) agreement on the exact C and outcome from both assigned V seats" | Exact outcome agreement is stricter than the bench merge predicate and undefined for graded reviews (the product itself scores consensus); a non-author APPROVE plus a NIT-only CHANGES_REQUESTED is mergeable. [S][B] | REPLACES: "(i) agreement on the exact C and outcome from both assigned V seats" → "(i) agreement on the exact C and on the outcome as reduced by the project's declared `verdict_schema` and merge predicate (default binary; graded schemas declare score dimensions and a pass predicate) from both assigned V seats" | PROOF: executed evaluateMerge with Codex CHANGES_REQUESTED/NIT and non-author Claude APPROVE → D2_SATISFIED; factory/RESET-PLAN.md:329 consensusScore
§3 | "at expiry HOLD_EXPIRED likewise enqueues exactly one successor, never a synthesized REJECTED" | Automatic successors bypass the reviewed queue and are unbounded (K_lineage caps reassignments, not verdict_ver); the bench caps review at two evaluable rounds and forbids automatic successor rows. [A][S] | REPLACES: "at expiry HOLD_EXPIRED likewise enqueues exactly one successor, never a synthesized REJECTED" → "at expiry HOLD_EXPIRED records an infrastructure failure consuming no round and enqueues at most one successor, never a synthesized REJECTED; a lineage whose next verdict_ver would exceed 2 records NEEDS_REDESIGN instead, preserving the unmet requirement" | PROOF: factory/HUB.md:9; factory/RESET-PLAN.md:13; factory/queue-state.mjs:81-90
§3 | "No timeout, single family, lone vote or tiebreak seals anything" | With no risk partition any family outage halts all sealing; the bench runs a tested degraded path outside a named high-risk class. [A] | REPLACES: "No timeout, single family, lone vote or tiebreak seals anything" → "For subjects touching policy `high_risk_paths`, no timeout, single family, lone vote or tiebreak seals anything; other subjects may seal under a stamped `cross_family: UNAVAILABLE <family> <UTC>` with three same-family seats on distinct named surfaces, re-reviewed on the family's return" | PROOF: factory/review-state.mjs:86-99 (HIGH_RISK_REQUIRES_BOTH_FAMILIES, DEGRADED_ATTACK_SURFACES_REQUIRED); factory/RESET-PLAN.md:51-53
§3 | "The one collection TTL, `2 h (clock_domain=oracle)` from COMPLETE, applies to every HOLD" | One fleet-wide 2 h horizon fits PR review, not human judgement, playtests or strategy review; every such subject would HOLD_EXPIRE and churn. [B] | REPLACES: "The one collection TTL, `2 h (clock_domain=oracle)` from COMPLETE, applies to every HOLD" → "The one collection TTL, `policy.review_horizon[artifact_class]` (default `2 h (clock_domain=oracle)`) from COMPLETE, applies to every HOLD" | PROOF: factory/RESET-PLAN.md:129 ~40-day human hold against a 2 h TTL
§3 | "An attributable missed deadline (start `15 min (clock_domain=real)` or execution H) increments `miss_count`" | A fixed 15 min start deadline makes any human or slow-oracle seat V_SUSPECT after two misses. [B] | REPLACES: "An attributable missed deadline (start `15 min (clock_domain=real)` or execution H) increments `miss_count`" → "An attributable missed deadline (start `policy.seat_start_deadline[seat_class]`, default `15 min (clock_domain=real)`, or execution H) increments `miss_count`" | PROOF: factory/RESET-PLAN.md:129 human-held head
§4 | "the acceptance_spec_sha-required runs at C.candidate_commit_oid are independently executed or authenticated" | Acceptance is assumed repeatable and deterministic; bench acceptance against live provider UIs is non-reproducible and forbidden without an authority file. No oracle kind exists for stochastic, gated or human acceptance. [B] | REPLACES: "the acceptance_spec_sha-required runs at C.candidate_commit_oid are independently executed or authenticated" → "the acceptance oracle declared in acceptance_spec (`oracle_kind`, one of `deterministic`, `stochastic{n, pass_threshold, seed_policy}`, `gated{authority_ref}` or `judgement{seat_class}`) is satisfied at C.candidate_commit_oid, executed, authenticated or recorded per kind; a gated oracle whose authority_ref is absent yields ADOPT-DEFERRED" | PROOF: CLAUDE.md:20-26; no factory/live-authority.json; scripts/ci.ps1:84 runs only offline mock suites
§4 | "`refs/oracle/adopted/<S>` at the exact candidate commit" | Squash merge lands a new single-parent commit, so the reviewed candidate OID never reaches master and no ref can point at the exact candidate commit. [A] | REPLACES: "`refs/oracle/adopted/<S>` at the exact candidate commit" → "`refs/oracle/adopted/<S>` recording `{candidate_commit_oid, candidate_tree_oid, landed_commit_oid}`, landed_commit_oid's tree verified equal to candidate_tree_oid on the recorded base" | PROOF: `gh pr view 60 --json headRefOid,mergeCommit` → head a3387c4b vs merge 9efa9fd7
§4 | "and that subject satisfies `ready_effective` recursively" | Re-resolving current dependency authority retroactively strips readiness from dependents of already-landed work; bench delivery is monotone once a row's PR merges. [A][S] | REPLACES: "and that subject satisfies `ready_effective` recursively" → "and that subject satisfies `ready_effective` recursively, or holds a valid `refs/oracle/adopted/<D>` whose landed commit is an ancestor of the dependent's base_commit_oid; corrections use a reviewed supersededBy list" | PROOF: executed evaluateQueue with merged A superseded by undelivered B → A DELIVERED, B READY (factory/queue-state.mjs:79-90)
§4 | "Tier 2 verifies and attests without writing Oracle state" | Attestation helpers CAS-create HELPER_RESULT refs, blurring reviewer and controller authority; bench review legs are read-only and only the controller publishes receipts. [S] | REPLACES: "Tier 2 verifies and attests without writing Oracle state" → "Tier 2 emits signed output only; the trusted controller validates and publishes any receipt, and review legs never mutate repository refs or ledgers" | PROOF: CLAUDE.md:29; factory/HUB.md:13; factory/publication-state.mjs:201-212
§4 | "records HELPER_TIMEOUT once and re-sweeps at attempt+1 (a fresh request_id)" | Infrastructure retry is unbounded; the bench caps infrastructure attempts at three per head and family, then waits for an observed recovery signal. [A] | REPLACES: "re-sweeps at attempt+1 (a fresh request_id)" → "re-sweeps at attempt+1 (a fresh request_id) up to attempt 3, then records HELPER-WAIT, resumed only by a successful HELPER_CAPABILITY_PROBE" | PROOF: factory/RESET-PLAN.md:16-17; factory/review-state.mjs:9 consumesRound:false
§4 | "every live floor in `helper_floors` sweeps outstanding requests every `15 min (clock_domain=real)`" | helper_floors includes astra and fable, turning judgement-tier models into cadence sweepers; the bench forbids them any tick or cadence wake. [A] | REPLACES: "helper_floors: {A-codex: &f [sol, astra, opus, fable]" → "helper_floors: {A-codex: &f [sol, opus]" | PROOF: factory/RESET-PLAN.md:55-57
§4 | "Each helper is a fixed-action Windows scheduled task under its dedicated principal" | No registrar is named; bench scheduled tasks are high-risk, creatable only through a merged PR and must carry the `AdvLLM-` prefix; macOS/Linux hosts have no Task Scheduler. [A][S][B] | REPLACES: "Each helper is a fixed-action Windows scheduled task under its dedicated principal" → "Each helper is a fixed-action host-scheduler job (Windows Task Scheduler, launchd, systemd) under its dedicated principal, named with the project's `task_prefix` and registered only by an owner-installed change reviewed in the project's high-risk class; lanes never register, enable or edit it" | PROOF: CLAUDE.md:15; scripts/never-authorized-guard.mjs:105,226; executed `Register-ScheduledTask -TaskName Conjugal-A-codex` through the guard → exit 2
§5 | "The same writer lands entries into master's `coordination/comms/opus.md` through authorized normal-index integration" | Grants an automated writer a direct master mutation, bypassing the bench's mandatory PR, current-head checks, independent review and controller-only squash merge. [A][S] | REPLACES: "lands entries into master's `coordination/comms/opus.md` through authorized normal-index integration" → "stages the entries for `coordination/comms/opus.md` on a feature branch, landing only through the project's reviewed merge rule and never by a direct master commit" | PROOF: CLAUDE.md:11; scripts/never-authorized-guard.mjs:83
§5 | "For each provider p ∈ {codex, claude} the quota adapter" | Capacity machinery knows only inference providers; resources that acceptance consumes (logged-in chat UIs, a CDP browser, GPU farms, device labs) cannot latch, park or rotate. [B] | REPLACES: "For each provider p ∈ {codex, claude} the quota adapter" → "For each declared capacity resource p ∈ policy.capacity_resources (default {codex, claude}; e.g. live-ui:<site>, cdp-browser, gpu-render, device-lab) the quota adapter" | PROOF: adversarialllm/scripts/a19-model-discovery-sweep.mjs:7,121 connectOverCDP 127.0.0.1:9222
§5 | "A latch is an authenticated provider refusal or failed authenticated probe for p, never a budget shortfall." | The bench has no quota adapter and classifies capacity from pinned stderr patterns; under this text no latch ever opens and a dark family keeps being dispatched into refusals. [A] | REPLACES: "A latch is an authenticated provider refusal or failed authenticated probe for p" → "A latch is an authenticated provider refusal, a failed authenticated probe, or, until the adapter ships, a lane exit classified CAPACITY by a pinned, tested stderr classifier for p" | PROOF: factory/review-state.mjs:21-22
§6 | "Widening eligibility requires §6; no third quorum family exists." | Two POLICY envelopes can widen merge-equivalent authority without a repository PR, current-head CI, review receipts or the high-risk family-capacity rule. [S] | REPLACES: "Widening eligibility requires §6;" → "Widening eligibility requires §6 and becomes effective only after the project's high-risk reviewed change (current-head CI plus one review per family) lands;" | PROOF: factory/RESET-PLAN.md:40-70; factory/HUB.md:15
§7 | "The security claim covers individual faulty signers and cooperative runners under enforced mutation admission" | refs/oracle/* sit in a shared .git any same-user lane process can write; git has no per-ref ACL and the bench guard self-declares it is not a security boundary. [A] | REPLACES: "under enforced mutation admission" → "under mutation admission enforced by a separate OS principal owning a reducer-private repository that lanes reach only through a submit interface; with a shared same-user .git the claim is advisory" | PROOF: scripts/never-authorized-guard.mjs:4 and :242 (only `update-ref -d` denied)
§7 | "Private keys reside under `~/.claude/oracle/keys/<actor>.ed25519`" | Key creation and rotation write outside the project tree, which the bench forbids lanes; no owner step is named, so day 30 hard-blocks all signing. [S][A] | REPLACES: "hard-block new signing after `30 d (clock_domain=real)`" → "emit one owner KEY_ROTATE_REQUEST outbox entry at 25 d; key generation and installation outside the repository are owner-only; hard-block new signing after `30 d (clock_domain=real)`, dependent subjects recording WAIT-CUSTODY" | PROOF: scripts/never-authorized-guard.mjs:21,272; executed a guard probe writing `~\.claude\oracle\anchor.sha` (the user-profile path) → exit 2
§8 | "Helper scheduled tasks (§4) under test carry the injective name `(repository_id, run_id, fixture_instance, role, request_id, attempt)`" | Names carry no project prefix; the bench guard rejects any scheduled-task target not starting `AdvLLM-`, so every test helper registration is blocked. [A] | REPLACES: "carry the injective name `(repository_id, run_id, fixture_instance, role, request_id, attempt)`" → "carry the injective name `<policy task_prefix>-(repository_id, run_id, fixture_instance, role, request_id, attempt)`" | PROOF: scripts/never-authorized-guard.mjs:105,226
§8 | "MIXED(n) means exactly n real provider calls, n≥2" | Ledgers count only factory inference calls; live product calls (browser campaigns against provider UIs) have no ledger class or authority precondition. [B] | REPLACES: "MIXED(n) means exactly n real provider calls, n≥2" → "MIXED(n) means exactly n real external calls, n≥2, each tagged {resource, authority_ref}; a call against a resource whose `policy.external_authority` entry is absent exits 2 with zero calls" | PROOF: CLAUDE.md:26; factory/RESET-PLAN.md:315
§9 | "An item is one subject first receiving an authorized ADOPTED record for its exact candidate, counted once" | Throughput treats adoptions as fungible; the bench adopts code shipped default-OFF that would count as delivered, and chapters, memos and renders are not fungible units. [B] | REPLACES: "An item is one subject first receiving an authorized ADOPTED record for its exact candidate, counted once" → "An item is one subject first receiving an authorized ADOPTED record for its exact candidate, counted once, weighted by its declared `value_class` (default 1); items adopted behind a default-OFF gate count as `staged`, not delivered" | PROOF: factory/RESET-PLAN.md:329-330 default-OFF flags
§9 | "`metrics/items.jsonl` records all transitions plus a per-subject waterfall" | An unqualified repository-relative path turns runtime telemetry into tracked state; the bench keeps bounded untracked receipts and forbids metrics commits (its reset measured 339 idle metrics commits). [S] | REPLACES: "`metrics/items.jsonl` records all transitions plus a per-subject waterfall" → "`.factory-local/metrics/items.jsonl` (untracked, bounded) records all transitions plus a per-subject waterfall" | PROOF: factory/HUB.md:23; .gitignore:107; factory/RESET-PLAN.md:117
§14 | "The release manifest records policy/configuration digests, genesis anchor, helper enrollments, scenario IDs, parameters, clock domains, fixture classes, §10 phases" | Scenario receipts alone cannot authorize delivery; the manifest omits the local gate, named current-head checks, head/base, authorship and validated review topology. [S] | REPLACES: "The release manifest records policy/configuration digests," → "The release manifest records PR, row, current head/base, author families, green local gate, every named completed current-head check, validated review receipts, policy/configuration digests," | PROOF: scripts/ci.ps1:65-88; .github/workflows/ci.yml:18-117; factory/review-state.mjs:45-65

## Untested
§2 | "`H=clamp(1.25×p99_oracle,180 min,360 min) (clock_domain=oracle)`" | Final renders and engine cooks/lighting builds routinely exceed 6 h; a hard 360 min cap force-releases valid work. [B] | REPLACES: "`H=clamp(1.25×p99_oracle,180 min,360 min) (clock_domain=oracle)`" → "`H=clamp(1.25×p99_oracle,180 min,policy.horizon_max[artifact_class]) (clock_domain=oracle)`, horizon_max default 360 min" | PROOF: Scenario 24 with an 8 h render fixture must renew past 360 min
§2 | "atomically retires the claim and releases paths" | .uasset/.blend files are unmergeable; releasing path locks at lease end before adoption lets a second writer fork an unmergeable binary. [B] | REPLACES: "atomically retires the claim and releases paths" → "atomically retires the claim and releases paths, except paths of an `artifact_class` declared `mergeable: false`, which stay allocated until ADOPTED or successor cancellation" | PROOF: fixture with two subjects on one .uasset; zero divergent binary heads
§2 | "Any artifact, base, dependency or acceptance change creates a successor with fresh review." | A one-line fix in a long manuscript forces a whole-work fresh review; no scoped delta review exists. [B] | REPLACES: "Any artifact, base, dependency or acceptance change creates a successor with fresh review." → "Any artifact, base, dependency or acceptance change creates a successor with fresh review, scoped per acceptance_spec `review_scope` (whole or changed_units_plus_context)." | PROOF: fixture with a 1-line change in a 400-unit manuscript; review cost proportional to scope
§3 | "post-adoption challenges produce successors and remediation notices, delivered history intact" | For strategy work the real oracle is outcome evidence weeks after adoption; only a keyholder REJECT can challenge today. [B] | REPLACES: "post-adoption challenges produce successors and remediation notices, delivered history intact" → "post-adoption challenges, from a keyholder REJECT or a declared `outcome_oracle` result arriving within its `outcome_horizon`, produce successors and remediation notices, delivered history intact" | PROOF: fixture with outcome evidence at +30 d → one successor, zero row rewrite
§4 | "Default `live-sol` runs oracle-adopt.py through Sol's existing native floor runner and signing custody" | Engine, WebGL, game and mobile acceptance needs GPUs, headed displays, devices or licences the adopter floor may lack. [B] | REPLACES: "Default `live-sol` runs oracle-adopt.py through Sol's existing native floor runner and signing custody" → "Default `live-sol` runs oracle-adopt.py through Sol's existing native floor runner and signing custody, dispatching required runs to a host satisfying acceptance_spec `host_requirements` (GPU, display, OS, device, licence)" | PROOF: Scenario 33 with a GPU-required acceptance on a GPU-less floor → ADOPT-DEFERRED, never a fabricated PASS
§9 | "ramp executors 5→9→15 through G-MEASURE" | Parallel executors on one voice-bearing work drift in voice; no parallelism cap is keyed to a shared author voice or style canon. [B] | REPLACES: "ramp executors 5→9→15 through G-MEASURE" → "ramp executors 5→9→15 through G-MEASURE, never exceeding `policy.max_parallel_per_voice[voice_id]` concurrent subjects sharing a declared voice or style canon" | PROOF: G-MEASURE 2b with a voice-consistency judgement seat; score non-decreasing across levels
§9 | "`b=first_adoptions/elapsed_calendar_hours`" | The bench conductor is disabled and adoption is manual PR merge; a production baseline cannot attribute factory adoptions. [A] | REPLACES: "`b=first_adoptions/elapsed_calendar_hours`" → "`b=first_adoptions/elapsed_calendar_hours`, counting only adoptions whose landing record names a factory adopter identity" | PROOF: factory/HUB.md:5
§12 | "Panel: eight seats (Fable, Opus, Sonnet×3, Sol, Luna, Astra; 100K each)" | The stopping rule has no member-coverage term; a member whose guard blocks filing is indistinguishable from one that chose not to file. [C] | REPLACES: "a round with substituted instances is DEGRADED." → "a round with substituted instances is DEGRADED; a fleet round whose member coverage is below N_members − PORT_DENIED is also DEGRADED." | PROOF: bus `ls-remote origin 'refs/heads/review/*'` → no AdversarialLLM branch while its guard denies the filing path
