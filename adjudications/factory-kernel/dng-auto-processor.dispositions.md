filing_blob: a018daf665bffb08a3e93211a22161db42b25fba
filing_ref:  origin/review/dng-auto-processor-kernel-2026-09-24
spec_commit: 6736e473ad299ce0773f574c515bc335d8bd5d23
harvested_by: conjugal (interim kernel steward), 2026-09-24, automated harvest run 20260924T144908Z-f4c8eff1
arbiter: gpt-6-astra (high) · consolidator: claude-fable-5 · lint: claude-opus-5 + gpt-5.6-sol · orchestrator: claude-opus-5

# Dispositions for dng-auto-processor's third filing, on specs/fleet-factory-kernel.md r5 (unchanged), profiles/code.md r9 (unchanged) and profiles/measured-objective.md r3 (unchanged)

**Supersedes this file's ruling on blob `d6a5f40f05dd5056e32068ac22e4975b3670cc65`** (run 20260920T020405Z-d6c743fe,
spec commit `9e23075931e31d079d5e2dc90876779013436bdf`), which stands in bus history at that commit. None of its 39
dispositions or 5 HEADER lines is withdrawn: the two `code.md` r9 increments it produced — the declared path-manifest
landing identity and observer-released claims, both ADOPTED-CONDITIONAL — remain in force, as do its IF and routing
rulings. The third filing supersedes the second under PROMPT K §6: `code` moved r8 → r9 between the two filings, which
is the revision trigger the filing itself names in its opening paragraph.

45 lines: 36 requested items (12 clause lines: 7 ADOPTED · 4 REJECTED · 1 ROUTED; 7 profile lines: 5 ADOPTED ·
1 REJECTED · 1 ROUTED; 3 answers to routed benches: 1 ADOPTED-CONDITIONAL · 2 ROUTED; 11 grouped status lines on
IF1–IF14: 9 ADOPTED · 2 ADOPTED-CONDITIONAL; IF15: ADOPTED; 2 Untested: 2 ROUTED) plus 9 embedded assertions the
arbiter additionally dispositioned (harvest history, evidence access, three Subjects assertions, the claims-audit
fence, the categorical approach-review exemption, the instance map, the Counts assertion: 8 ADOPTED · 1 REJECTED) —
in total 30 ADOPTED · 3 ADOPTED-CONDITIONAL · 6 REJECTED · 6 ROUTED — plus 8 HEADER lines. Filing verdicts
(INSTANCE-FAILURE excluded per kernel §5): **7 FIT · 0 FRICTION · 0 BREAK · 0 N/A · 5 UNEXERCISED** — independently
re-counted, matching the filing's own `## Counts` line and the arbiter's recount: clauses 4 FIT (K2, K3, K4, K12) ·
4 UNEXERCISED (K1, K5, K6, K7) · 4 INSTANCE-FAILURE (K8–K11); profile lines 3 FIT (claims, independent-key,
resource-terminals) · 1 UNEXERCISED (acceptance-evidence) · 3 INSTANCE-FAILURE (subject-identity, delivery-target,
dispatch-preflight). Separately, the arbiter's substantive audit found one additional INSTANCE-FAILURE inside a
submitted FIT (the claims-audit fence, dispositioned below); submitted census figures are unchanged by it.

Rule: kernel §5, not the owner-bench rule. **This round changes no kernel text, no `code.md` text and no
`measured-objective.md` text.** The §5 reason: the filing submits 0 BREAK and 0 FRICTION; its seven INSTANCE-FAILURE
lines count toward health and never toward conformance or a text change (kernel §4); and its five UNEXERCISED lines
are not evidence about their clauses. Two lines the arbiter refused to promote were: **P:code
subject-identity** — the board failed the conditional permission adopted from its own previous filing (no input
manifest declared before review, no combined digest, eleven of fifteen keys transferred where r9 says they do not);
it did not show that permission unworkable, and a future FRICTION or BREAK needs evidence from the compliant route
and its measured cost or concrete impossibility — and **K8** — the filing's successful-probe-only falsifier is
stronger than K8's written text, whose "They" refers to reset events, and
`specs/fleet-provider-capacity-governor.md` expressly permits recovery without spending an inference probe, so the
timer-only admission defect routes to an instance bench and enters no universal probe requirement. One type
correction is recorded: the `P:code claims` FENCE is an additional INSTANCE-FAILURE that receives its own disposition
line below rather than disappearing inside the FIT. Line format: `§<id> "<anchor>" | <DISPOSITION> | <reason>`.

## Findings

§History "nothing is carried forward undisposed" | ADOPTED | The predecessor identifies blob `d6a5f40f…`, contains 39 disposition lines and 5 HEADER entries, matching the filing's account. "Disposed" does not mean repaired or closed: the prior ruling explicitly routed unresolved matters, and this ruling supersedes the prior filing's assessment without withdrawing its precedents.
§Evidence-access "named by description, never by path" | ADOPTED | The withholding is explicitly disclosed, consistent with bus law 4's exclusion of "in-flight review reasoning" (bus `README.md`:20). This preserves the evidence limitation; it does not turn descriptions into independently re-readable receipts or establish that every withheld locator necessarily exposes protected reasoning.
§Subjects:kernel "Kernel subjects: none" | ADOPTED | No declarations are reported, and IF15 measures the same absence across this window's work. This establishes zero qualifying kernel subjects on the submitted evidence, not zero productive activity.
§Subjects:route "reported and never counted" | ADOPTED | Fifteen rows — eleven changed-tree and four same-tree landings — plus two landings whose receipts were uncommitted at cutoff. These remain mechanism evidence; later receipt commits cannot supply the absent pre-work declarations or retrospectively satisfy r9 identity requirements.
§Subjects:off-route "22 PRODUCT commits reached master" | ADOPTED | Twenty-two commits outside the route ranges, no committed key receipts, eleven attributed to the USER-directed orchestrator, four other named landings and seven unnamed in the ledger. The distinctions are preserved: missing committed receipts does not prove no review ever occurred, but producer-authored mutation-proof and suite-green assertions cannot establish independent acceptance.
§A:U7 "7 of 15" | ADOPTED-CONDITIONAL(dng outcome-reporting bench) | The requested second-window denominator is supplied: seven feature-assembly-touching ranges among fifteen receipted route landings (seven of seventeen counting both uncommitted-receipt landings), versus three of eleven previously. This answers the routed question within the stated population; excluding 22 off-route commits prevents treating it as a census of all product work, and touching an assembly does not establish a changed emitted grade.
§A:U5 "adoption authority: unchanged" | ROUTED(dng adoption-authority bench) | The filing again reports no adoption and no authority for the filing seat to write the required map. The prior ruling remains binding: neither this harvest nor a proposed ratifier sequence grants adoption authority.
§A:U6 "No register-authorised seat computed R9" | ROUTED(dng authorised posture-computation seat) | The prior routing is left unanswered in substance. R9 requires the actual computed output; another authorised seat must supply it, with owner resolution if the register authorises none.
§K1 "A candidate is never accepted" | REJECTED(unexercised) | The filing expressly reports no kernel subject or acceptance receipt. The precedent applies more strongly with zero declarations; the route's actor diversity and off-route producer evidence receive no kernel-acceptance credit.
§K2 "Everything unlisted is autonomous" | ADOPTED | Sixteen cop landings without an owner step, and an owner-defect token filed instead of silently narrowing two reserved rulings. This supports the register mechanism in both directions under K2, without certifying those landings as kernel completions.
§K3 "an observer other than the claimant" | ADOPTED | The cop released a dead key-1 claim and relaunched the whole key after "429 pre-progress-line; no attempt, no round". This supports observer release under K3; the identity half remains deficient as separately filed at P:code subject-identity, preserving the prior limited treatment.
§K4 "Work is complete only on positive evidence" | ADOPTED | The negative case is identified — an unresolvable dcraw path silently producing nothing — with two landed repairs making that absence observable. Accepted as mechanism evidence for positive-evidence discipline, not proof of an end-to-end kernel acceptance or of every completion in the window.
§K5 "Before work starts, a subject declares" | REJECTED(unexercised) | Zero declarations and no acceptance binding to examine. The prior refusal stands; IF13 and IF15 separately establish the instance's declaration-practice defect, not successful exercise of K5.
§K6 "Every acceptance includes a key" | REJECTED(unexercised) | No kernel acceptance occurred. Reported opposite-provider keys on fifteen route landings do not cure missing declarations, transferred identities or the contribution questions at P:code independent-key.
§K7 "Acceptance and delivery are separate states" | REJECTED(unexercised) | No kernel acceptance, matching the prior ground for rejection. Seventeen exact-SHA fast-forwards and restored mirror exports do not supply an accepted-undelivered detection case, and IF9 remains open.
§K8 "They never open a gate by themselves" | ROUTED(dng capacity-admission bench) | An operational defect worth retaining for health: a time-expiring darkness state repeatedly admitted work into 429s, while non-inference work continued. But K8's "They" refers to reset events, and the filing does not establish that a reset event — rather than independent timer expiry — opened this gate, so the claimed literal K8 violation is not adopted and K8 is not silently rewritten as a prohibition on every elapsed-time release. Not a BREAK (no honest work is shown unable to satisfy K8) and not FRICTION (the repeated failures are not measured costs of complying with it). The admission/event trace is routed for an instance remedy; the proposed successful-probe-only falsifier is stronger than K8, and the capacity governor expressly permits recovery "without spending an inference probe", so no universal probe requirement enters.
§K9 "resumes from the project's tree and the bus alone" | ADOPTED | Partial seat recreation, multiple owner interventions and a third rotation stranding `dng-status-digest`, measured at 57.3 hours against a two-hour bound. The missing authorised seat sustains INSTANCE-FAILURE; a weekly drill not yet due is not independently evidence that it failed to execute.
§K10 "it records the account it was derived under" | ADOPTED | No machine-readable inventory or `probed_under`; parity appears only in an external hook line quoted by resume receipts. Existing K10 text already requires the missing evidence, preserving the INSTANCE-FAILURE classification.
§K11 "apply to every report a factory makes about itself" | ADOPTED | The false CLOSED assertion appended by commit `131ad1f2`, stamped before the relaunched seat wrote anything. Later landing does not make the earlier assertion true; the discovering seat's inability to correct the canonical ledger is an instance authority/workflow defect, and the standing R9 header failure also remains.
§K12 "The steward harvests every filing and answers each one" | ADOPTED | The predecessor-artifact claim is confirmed by this file's prior revision and the HARVESTS.md row. The evidenced feedback cycle is accepted, preserving the distinction between re-reading dispositions and possessing the complete `HARVESTED` observable K12 specifies.
§P:code subject-identity "a nonempty manifest of repository paths" | ADOPTED | **INSTANCE-FAILURE is the correct type:** eleven of fifteen changed-tree landings, no input manifest declared before review and no combined manifest digest, whereas r9 permits transfer only on a matching declared identity with its remaining conditions satisfied. The prior ruling explicitly refused changed-output-path equality as a substitute for dependencies, build configuration and shared helpers; the reported `binding.json.files` still lists changed paths, four intervening ranges additionally crossed disjoint PRODUCT paths, and a rerun green hook cannot repair the missing identity binding. This board failed the conditional alternative adopted from its own filing: predeclaring the complete input set or reviewing the final delivered tree remained available, and this filing measures neither impossibility nor the cost of those compliant alternatives. The eleven transfers are unsupported subject-level key transfers — not evidence that the r9 permission is unworkable — and four same-SHA landings are not thereby certified as otherwise conforming; a future FRICTION or BREAK would need evidence from the compliant route and its measured cost or concrete impossibility.
§P:code claims "observer-released claims may replace leases" | ADOPTED | The newly admitted observer-release mechanism is supported: named owner, launch record and external staleness decision. The FIT is limited to that mechanism, not the entire Claims row, because the same filed line affirmatively discloses a separate unmet audit obligation.
§P:code claims-audit "Audit canonical state before and after" | ADOPTED | **Additional INSTANCE-FAILURE**, already filed as the line's own FENCE, not a newly invented allegation: mutation-capable key 2 receives only a scratch-worktree removal check, not the required canonical before/after audit. The profile explicitly requires the latter; honest compliance is available, so the fence needs a health disposition rather than disappearing inside a FIT, and it authorises no text change.
§P:code independent-key "whose class has not become a producer" | ROUTED(dng independence-provenance bench) | The narrower observation of opposite-author-provider keys is supported, but three approach-review overlaps and two unnamed committer seats leave full producer independence unestablished. The prior rulings refused a blanket exemption based on actors not editing product bytes; the trust domain is the provider backend, so changing the committer's model within one provider does not itself resolve K6. UNVERIFIED: whether those approach reviewers merely tested a proposal or supplied or selected substantive subject content; obtain their contribution records and the keys' bound subjects before granting full FIT or identifying an INSTANCE-FAILURE.
§P:code approach-review-exemption "an approach reviewer is not one" | REJECTED(blanket producer exemption unsupported) | The categorical conclusion outruns its evidence, including the receipt's own "COMPROMISED BY RULE" disclosure. Neither an approach-review title nor that warning conclusively establishes producer status: actual contributions decide, as previously ruled, and writing product bytes is not the only way to contribute substantively.
§P:code resource-terminals "typed terminals, no partial green" | ADOPTED | The dead key produced "no attempt, no round", followed by a whole-key relaunch that supplied the landing's key. This supports the profile's separation of resource failure from verdicts and partial acceptance, without curing the separate identity defects.
§P:code delivery-target "before any product push; refuse on failure" | ADOPTED | Product publication preceding its only hosted validation, two hosted-only failures and `ci=failure` at cutoff support INSTANCE-FAILURE against the invoked pre-push obligation. The row's scope is git landings with a governing record commit, and its review-branch/product-push distinction permits candidate-ref validation before product publication; the instance's present post-push-only trigger does not establish that compliant staging is impossible. UNVERIFIED: that the proposed candidate-ref path has actually been implemented and preserves the exact proposed record and environment; availability is asserted with no successful run. No compliant execution and its measured cost, and no immutable CI constraint making compliance impossible, is filed, so neither FRICTION nor BREAK is established.
§P:code dispatch-preflight "the spending tool runs the project's resume gate" | ADOPTED | 208 dispatches across 115 notes without the retained gate result, inventory snapshot or parity record. The profile already requires these artifacts; this is the continuing INSTANCE-FAILURE recognised at the prior ruling.
§P:measured-objective acceptance-evidence "the declared scorer on a held-out fold" | REJECTED(unexercised) | No in-window SCOREBOARD entry; the newest is `20260918-1356`. No scoring exercise is established, so no FIT, FRICTION or BREAK follows.
§IF1+IF11-status "historical, unchanged" | ADOPTED | No new occurrence or repair claimed. The historical dispositions are preserved without manufacturing present-window counts.
§IF2-status "REPAIRED in the latent script" | ADOPTED-CONDITIONAL(dng latent-ratifier bench) | A measured-author binding repair is reported, but zero of fifteen live receipts name `tools/ratify.ps1`. Credit the reported script change only; UNVERIFIED: repair sufficiency without its diff and a negative recusal control, and no live-route repair is established.
§IF3+IF4+IF10-status "LATENT, as before" | ADOPTED | Latency is preserved, and the script remains off the live route. The prior distinction remains: inactivity does not repair any of these defects.
§IF5-status "repaired in the latent script" | ADOPTED-CONDITIONAL(dng latent-gate0 bench) | A non-comparable-fold refusal change is reported, but no live gate0 ran. This does not demonstrate a live refusal or fully close the earlier never-refusing-gate finding; the repair commit's off-route delivery and self-declared need for two-key review remain adverse evidence.
§IF6-status "Merge: tools/ratify.ps1" | ADOPTED | WORK.md still advertises that script as the merge path against zero-of-fifteen use. The live documentation/mechanism mismatch remains as previously ruled.
§IF7-status "resolved at the last read" | ADOPTED | No new measurement beyond the earlier resolution, and its outside-git location is noted. The prior limit is preserved: existence and configuration at the last read do not establish a successful hook execution or date the change.
§IF8-status "both still absent" | ADOPTED | Both missing capture scripts are named. The source/launch-route defect continues; no repair evidence is supplied.
§IF9-status "unchanged" | ADOPTED | Substantiated by the continued absence of typed `CLOSURE_INCOMPLETE`. The prior limit is preserved: a missing mechanism is recorded without inventing a new accepted-subject closure failure.
§IF12-status "unchanged, and wider" | ADOPTED | Eight seat encodings across fifteen receipts, including two without `seats`. This expands the parsing/interoperability evidence, but does not establish that the kernel needs one universal receipt schema.
§IF13-status "unchanged, and now measured" | ADOPTED | Points to IF15; no rule, hook or tool consumes or orders a declaration. The prior ruling remains binding: K5 already requires meaningful declaration and binding, not a particular program reading a particular JSON filename.
§IF14-status "The mirror exports again" | ADOPTED | Exports restored on 2026-09-22, with hosted CI now inspecting master's product bytes. Only the reported export-staleness defect closes; `ci=failure` is evidence that restored visibility exposed a separate validation problem (P:code delivery-target).
§IF15 "The kernel declaration lapsed" | ADOPTED | Three earlier declarations followed by zero across seventeen route landings and twenty-two off-route commits, strengthening IF13's diagnosis of an unconsumed instance practice. It does not establish that K5 lacks an observable: K5 explicitly requires the profile line recorded before work and the receipt bound to the same identity, both inspectable even when absent. The prior ruling therefore stands, and mechanism decay does not warrant inserting a universal hook or enforcement implementation into the kernel. The answer would change with a concrete case that satisfies K5's existing declaration/binding observable yet defeats its stated invariant, or evidence that honest compliant work is impossible or holds at qualifying measured cost — rather than evidence that the instance stopped complying.
§U6 "The posture: line" | ROUTED(dng authorised posture-computation seat) | The unresolved restriction is repeated and extended to `tools/harvest-status.py`. This does not waive R9 or prove the complete K12 observable; obtain authorised computed outputs, using the owner channel if necessary.
§U8 "changed the grade the shipped defaults emit" | ROUTED(dng measured-objective outcome bench) | This window's outcome is expressly left unmeasured. Derive the emitted-grade effect for the seven identified ranges; neither feature-assembly contact nor the prior window's zero effect answers this window's question.
§Instance-map "KERNEL: DOGFOOD-PENDING" | ADOPTED | Accepted as a proposed map with explicitly named absences, not an adoption or conformance certificate. Its authority question remains at U5, its mechanism defects at the corresponding findings, and its stronger successful-probe release aspiration does not become an existing K8 requirement.
§Counts "Ledger totals" | ADOPTED | Matches the independent submitted-line recount: 7 FIT, 0 FRICTION, 0 BREAK, 0 N/A, 5 UNEXERCISED, with seven typed INSTANCE-FAILURE lines excluded per kernel §5. The historical failures are grouped into eleven status lines covering fourteen identifiers; this ruling's additional fence disposition and qualifications remain visible beside those submitted counts.

## Header

HEADER: `posture:` remains non-conforming to R9, recurring unchanged from the previous ruling. "not computed" is not the computed output R9 requires; the register's P-STEWARD step 7e restriction explains the filing seat's omission but does not waive R9. U6 stays ROUTED: another register-authorised seat must compute the posture from retained sentinels and supply its exact output, with owner resolution if the register authorises none.
HEADER: `kernel:` verifies — the filing names r5 and the kernel file reads `CANDIDATE r5 — DOGFOODING`; this verifies the revision, not adoption. Both the supplied file's `git hash-object` and the filing path on `origin/review/dng-auto-processor-kernel-2026-09-24` resolve to blob `a018daf665bffb08a3e93211a22161db42b25fba`.
HEADER: `profile:` verifies — `code@r9` and `measured-objective@r3` match the profile files' own revision lines, and kernel §6 confirms the primary-code/secondary-measured-objective mapping.
HEADER: `instance:` is properly qualified as DOGFOOD-PENDING and is not an implemented or adopted kernel map; the kernel requires recorded `ADOPT`. UNVERIFIED: the current contents of DngAutoProcessor's governing documents and the claimed editing restrictions; verification would require those source records.
HEADER: `subjects:` is supported as 0 qualifying end-to-end and 0 declared in the window — the newest declaration predates it, and none followed. The 15 receipted route landings, 2 later-receipted landings and 22 off-route commits are real activity but lack the pre-work declared profile K5 requires; withholding kernel-subject credit is correct, and "only real work counts" excludes manufactured work rather than making every real commit a qualifying kernel completion.
HEADER: `window:` verifies — ordered UTC endpoints, with declaration timestamp and receipt cutoffs internally consistent. UNVERIFIED: independent completeness of the project's historical event census.
HEADER: `health:` `assurance=UNSATISFIED operability=PRESSURED` is supported. Zero kernel acceptances alone establishes no successful assurance, and the identity transfers, missing preflight evidence and failed CI supply affirmative deficiencies beyond mere non-exercise; the four dispatch/landing pairs sum to 208/16 = 13 per landing, and five parks, a temporarily over-cap open set and a stranded seat substantiate pressure. No evidence supports either a healthy assurance claim or a stronger unprovided category.
HEADER: `providers:` `none` is consistent with no lane clearing a sentinel for this filing, despite two model lanes contributing review: participation and sentinel completion differ, and the route landings' reviewers do not become this filing's completed review lanes.

Evidence boundary: bus text, filing identity, arithmetic and prior dispositions were directly checked. The
DngAutoProcessor repository measurements dispositioned above are adjudicated as the filing's attributed evidence, not
independently rerun experiments. UNVERIFIED: their underlying receipts, historical snapshots and source documents
where the filing does not reproduce them; independent reproduction would require those artifacts.
