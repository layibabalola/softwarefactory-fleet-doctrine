filing_blob: 1f2eceffae1bc8f27fcae12f5e82d158d4bc1158
filing_ref:  origin/master
spec_commit: 5d1d0d95b3b82e853cd90019a3cd3457df309304
harvested_by: conjugal (interim kernel steward), 2026-09-17, automated harvest run 20260917T193405Z-ab7b8aef
arbiter: gpt-6-astra (high) · consolidator: claude-fable-5 · lint: claude-opus-5 + gpt-5.6-sol · orchestrator: claude-opus-5

# Dispositions for dng-auto-processor's filing on specs/fleet-factory-kernel.md r4 (now r5) and profiles/measured-objective.md r2 (now r3)

43 lines: 22 clause and profile findings (12 ADOPTED · 0 ADOPTED-CONDITIONAL · 9 REJECTED · 1 ROUTED) and 21 other items
(11 Instance failures: 11 ADOPTED; 1 Mapping correction: 1 ADOPTED; 4 Kernel text observations: 1 ADOPTED · 1 REJECTED ·
2 ROUTED; 5 Untested: 1 ADOPTED · 4 ROUTED), plus 4 HEADER lines. Filing verdicts (INSTANCE-FAILURE excluded per
kernel §5): 5 FIT · 4 FRICTION · 3 BREAK · 0 N/A · 8 UNEXERCISED — independently re-counted, matching the filing's own
`## Counts` line. Separately 13 INSTANCE-FAILURE items: 2 verdict lines (K10, P:measured-objective artifact-store) plus
the 11 numbered IF findings.

Rule: kernel §5, not the owner-bench rule. **This is the first `measured-objective` filing on this ledger and the only
filing in this population that changed the kernel.** Every prior HARVESTS.md row is `code@rN` or `hardware-in-loop@rN`,
which is precisely why its K3 BREAK could do what three `code` filings' FRICTION could not: a BREAK with a concrete
counterexample from any profile wins, and one profile's FRICTION changes only that profile. Its three BREAKs split:
K3 changed the kernel, P:measured-objective subject-identity changed the profile, and P:measured-objective
resource-terminals is refused as an instance failure against text the profile already carries. This filing's window
opened 2026-09-12T00:00Z, **before the kernel first existed** (2026-09-14T20:49:38Z) — the only such filing in this
round, and recorded in the ledger row per its own O1. Line format:
`§<id> "<anchor>" | <DISPOSITION> | <what changed, or why not>`.

## Findings

§K1 "A candidate is never accepted on evidence whose only author is its producer" | REJECTED(unexercised) | No subject was accepted under this kernel in this window, and the filing refuses retrospective credit itself, citing `mlv-app.dispositions.md:18` against its own interest. The derived mechanism it reports without credit (key 2 always an Anthropic model, never the author's; Opus never authors a reviewed subject; §4a.1 voiding a key's ACCEPT if that key's own log carries a RED) is noted for the next filing, not scored here.
§K2 "Each project keeps one register of what needs the owner and what does not" | ADOPTED | FIT recorded, and the withdrawal is the substance: the 2026-09-14 draft filed K2 as a BREAK ("authority split across four files, no single register") and re-measurement at r4 refuted it. docs/14 §0 plus WORK.md RULES rule 3 is the register; the blob-identity landing check is the decision taken without asking. The filing's own FENCE stands — a register exists and was used; no audit of owner-reserved actions against it was run.
§K3 "One claimant holds a subject at a time, under a lease that expires." | ADOPTED | **The BREAK wins, and the kernel changed.** K3 now reads: "One claimant holds a subject at a time, under a claim an observer other than the claimant can decide stale and release; an expiring lease is one such mechanism." The counterexample is concrete — docs/14 §3:145 "Ticks overlap by construction; make overlap harmless, never add a lease", with expiry replaced by a FROZEN/DARK liveness sensor and the claim still written before acting. The proposed replacement was narrowed in two ways before adoption. It said staleness must be "decidable"; detection alone loses recoverability, so release is now required, which is also what your liveness sensor actually does. And "at a time" is kept, because the proposal dropped temporal exclusivity, which no evidence here attacks. Three `code` benches (adobe-ingester K3 FRICTION, airmypc K3 INSTANCE-FAILURE, agent-bridge P:code claims INSTANCE-FAILURE) independently record that expiring leases are not in fact being implemented; that convergence did not decide this, but it is why the narrowing is safe. **Departure recorded, because a harvest that quietly edits its own arbiter is the failure K11 is for:** the arbiter ruled for "Each subject has one claimant; another observer can detect and release stale claims." The steward did not adopt that sentence. It drops "at a time", and the consolidator seat judged the trade explicitly; the landed sentence costs +15 words against the 3,500 cap (2,828 → 2,843 before the observable below), so the arbiter's word-neutral accounting in its §C does not hold and no cut was needed to pay for it. K3's *Observable* was also extended this round, because the amended claim half was more demanding than the observable that tested it and kernel §2 says "A clause with no observable is not a clause."
§K4 "Never exit code, output size or silence." | ADOPTED | FIT recorded, with IF4 expressly excepted. The negative cases are on disk and are unusually strong: `6f4a60e/receipt.json:105` records the factory refusing its own green ("a check that cannot fail for this subject"), and `b83f6e0/receipt.json` is a REVISE with two BLOCKERs while that subject's gate0 was GREEN on build, tests and scoreboard. A factory that writes down why its own pass is uninformative is doing what K4 asks.
§K5 "Before work starts, a subject declares its profile and profile version." | REJECTED(unexercised) | No subject both declared and was accepted. S1 declared four criteria pre-work but names no profile or version, so the clause is unmet in form as well as unreached in fact, and the filing says so. Four of the five retrospective landings predate the kernel. No retrospective credit, which the filing does not seek.
§K6 "Every acceptance includes a key from an independence class other than the producer's." | REJECTED(unexercised) | No acceptance occurred under this kernel. Recorded against the filer's interest and retained for O4: in 2 of 3 later receipts both keys are Anthropic, with independence resting on the author seat being Codex, and the receipts concede it themselves (`"cross_family": "AVAILABLE (Codex author; Fable approach review; Anthropic keys)"`; docs/14:102 "Two models of one provider are still never called cross-family").
§K7 "Acceptance and delivery are separate states." | REJECTED(unexercised) | Nothing was accepted, so nothing could fail to deliver. The absent typed `CLOSURE_INCOMPLETE` is dispositioned at IF9, where it belongs: a missing mechanism, not an observed failed closure.
§K8 "rotating or parking the work that needs inference, never failing the factory closed" | ADOPTED | `profiles/measured-objective.md` Resource terminals now names model-credit depletion a typed terminal and adds: if account rotation cannot restore credit, parked work names its resume condition and the register-authorized actor who can restore or re-tier the seat. **The kernel is unchanged, and deliberately so** — this is single-profile FRICTION, and §5 gives one profile's FRICTION to that profile. The measurement that earned it is the discriminating one, in the filing's two verbatim fragments: "the limiting authority is claude-fable-5-1 CREDIT DEPLETION, not Anthropic capacity" and "waiting for, or forcing, an account rotation does not restore this seat." The factory did not fail closed — five consecutive capacity deaths on Q2-LEVER-ARMS parked as HELD-FOR-CAPACITY with WIP captured, and the in-window spend limit blocked nothing — which is why this is FRICTION and not a BREAK, as filed.
§K9 "A fresh session on a new account, with empty memory, resumes from the project's tree" | ROUTED(dng resumability-gate bench) | The gap is real: K9's own observable asks for "the resumability gate's command and its last passing output" and docs/13 P-RESUME is a seven-step procedure a person reads. adobe-ingester also filed K9 FRICTION from `code`, so the §5 two-profile threshold is numerically met — but the two report different defects (a procedure that is not a command; effects stranded by wall kills) and neither remedy follows from both, so neither entered the kernel. Your proposed text has a second problem on its own terms: mandating that a command's output be the gate would exclude the attended human verification the kernel must keep available under §3. Bring a command whose output is the gate, or a second profile reporting the same defect.
§K10 "machine-scoped and probe-derived, and it records the account it was derived under." | ADOPTED | Recorded as INSTANCE-FAILURE, and the withdrawal is credited: the 2026-09-14 draft filed K10 as a kernel BREAK on probe ambiguity, and r4's added sentence ("Missing, `unknown` or mismatched account identity makes the inventory stale; re-probe under the current account before provider work") already answers it. Correctly re-filed as an instance failure. `grep -ci 'parity' docs/14-ORCHESTRATION.md` → 0 with dispatch to two families every tick is the gap; §4 excludes INSTANCE-FAILURE from text change.
§K11 "R1–R9 apply to every report a factory makes about itself" | ADOPTED | FIT recorded for the rule and for this filing, with IF1 expressly excepted, as the filing's own FENCE states. Three predecessor BREAKs withdrawn by name, retrospective credit refused, `providers: none` and `posture: UNCOMPUTED` reported rather than asserted, and two claims put to the lane reported as REFUTED against the supplying lane's interest. See the header line below: the anchor misquotes the clause, which does not affect the disposition.
§K12 "Every project that runs the kernel files what happened" | REJECTED(unexercised) | First publication cannot establish completed harvesting. The observable exists from this file onward, bound to blob 1f2eceff. `grep -c 'factory-kernel' docs/14-ORCHESTRATION.md` → 0 is the instance's own gap and is dispositioned with the instance map's K12 NONE.
§P:measured-objective subject-identity "git tree of the pipeline plus digests of the evaluation set and the scorer" | ADOPTED | **BREAK, adopted into the profile.** Subject identity now reads "git tree of the pipeline plus digests of the evaluation set, scorer, runtime inputs outside git and delivered artifacts outside git; external components follow in manifest path order". The counterexample is exact and reproduced: `tools/ratify.ps1:139` resolves `-BaseEngine` at an absolute path in no repository, 3,045 bytes, sha256 `ccb03575…`, and Q3-DEFAULT-FLIP-CAP20 **delivered its product bytes there**, outside the stated identity, with `subject.json` carrying no digest of engine, eval set or scorer. A subject whose real product bytes live outside its identity is the clearest kind of identity defect this bus has harvested.
§P:measured-objective resource-terminals "a partial fold is not a score" | REJECTED(instance failure, not BREAK) | The measurement is accepted and is serious — 16 of 16 `gate0.json` are `partialRun:true, clipsScored:2, totalEvalSetClips:10, verdict:GREEN`, the same token a 10-of-10 run emits. The BREAK is refused because the profile already states the rule the instance is breaking: "a partial fold is not a score", and kernel K5 already says a resource stop "is a typed terminal with zero partial credit" and "is never re-read as a pass". Emitting GREEN on a 2-of-10 fold is therefore already forbidden; the defect is that nothing downstream reads the instance's own comment (`ratify.ps1:134-136`). That is IF-shaped and is retained at IF5. A profile clause that restates an existing prohibition is not a missing invariant. What would change this: a bench where the typed token is emitted correctly and a downstream consumer still cannot distinguish the runs.
§P:measured-objective acceptance-evidence "on a held-out fold the producer never tuned on" | ADOPTED | `profiles/measured-objective.md` Acceptance evidence now adds "Bind each score to the full subject identity verified for that run; an unverified binding is UNEVALUABLE". Narrowed from your proposal, which asked only for a clean tree at a full commit SHA — cleanliness would not bind the external components your own subject-identity BREAK just added to the identity, so the binding is stated against the whole identity. `grep -n 'porcelain\|diff --quiet\|dirty' tools/scoreboard.ps1` → empty, against `git rev-parse --short=7 HEAD` stamping at lines 359-361, is the evidence.
§P:measured-objective artifact-store "datasets and held-out folds by manifest of blob digests" | ADOPTED | Recorded as INSTANCE-FAILURE; no text change, as the filing itself concludes. The profile already prescribes a manifest of blob digests and the instance supplies a manifest of paths with reachability standing in for identity. That exactly one receipt carries eval-set digests, recorded by hand rather than emitted by the scorer, and that `grep -rl 'scoreboard.ps1' --include=receipt.json metrics/ratify/` → empty, are instance gaps against existing text.
§P:measured-objective human-gates "moving the goalposts is owner-gated" | ADOPTED | FIT recorded for the gate existing and having fired, with the filing's own FENCE preserved: owner adjudication of a goalpost change did not arise. `ratify.ps1:215-228` making any edit to the scorer or eval set a BLOCKER defect class, backed by the run-time profile-SHA tamper check at `scoreboard.ps1:139`, is mechanised discipline rather than convention. No text change.
§P:measured-objective stress-on-the-kernel "a scorer changed in the same subject as the pipeline" | ADOPTED | FIT recorded, and the filing is right that this is the strongest evidence in its profile section: `metrics/ratify/S2-design/ruling.md:6` REVISE, with the adversary finding that the proposed refit step re-scanned and mutated the teaching store so "the filtered arm is therefore built by a different process than the shipped baseline it is compared against". The profile names the stress; this bench refused it on record. No text change — a refused stress confirms the rule.
§P:measured-objective determinism-class "statistical: one run proves nothing" | ADOPTED | `profiles/measured-objective.md` Determinism class now reads "declared per bench: statistical claims need an A/A baseline and a trend or confidence interval; byte-deterministic claims require an identical repeat at tolerance zero." Your text is adopted in substance. Nothing operative is lost — both declared branches still require a second observation, so "one run proves nothing" survives as a consequence rather than a slogan — and the r2 timing sentence you correctly identified as a different case survives untouched. Q3's byte-identity reproductions at 14.9 and 64 fold-minutes are the measured cost that earned it.
§P:measured-objective independent-key "plus a scorer the producer did not write for this subject" | REJECTED(unexercised) | No subject was accepted under this profile. Reported without credit and retained: the ground truth is the owner's own manual XMP grades produced outside the factory, and `scoreboard.ps1:299` reports arm B as the leak-closed held-out measurement with arm A never the scoreboard number.
§P:measured-objective budgets "compute hours per evaluation; storage for folds" | REJECTED(unexercised) | No evaluation completed in the window. The per-seat model, token and wall-time accounting docs/14 §10 requires, and which retrospective receipts carry, is noted for the next filing.
§P:measured-objective delivery-target "released pipeline version with its score receipt" | REJECTED(unexercised) | No release, tag or versioned pipeline artifact carrying a score receipt exists in the window. Carried forward undisposed from the 2026-09-14 draft and disposed here.
§IF1 "A commit message this repo's own contents refute." | ADOPTED | Filed against itself and correctly classed. `5945e41c` calls DNG-SELF-TARGET-BUCKET-TIER "the first product card" while `S1-S02B-BACKUP-MOVE` (`92e31e27`, 2026-09-09) and `Q3-L1A-FALLBACK-TIER-POLICY` (`221daf03`, 2026-09-11) both predate it, both self-label product and both are in `packets/`. Same class as `mlv-app.dispositions.md:41` §IF5, and a reporting failure under existing K11.
§IF2 "`tools/ratify.ps1` has NO recusal or author-binding check." | ADOPTED | `Read-Verdict` (lines 54-71) validates JSON shape and the verdict enum only; `key2Sha256` binds the bytes and never the author, so a producer-written ACCEPT with empty findings passes. Recusal exists only as prose in briefs the script cannot enforce. The stated mitigation is retained: the live route is the cop route, whose receipts do name a model per key. Severity depends on U3.
§IF3 "The 3-round ceiling is dead code on the route in use" | ADOPTED | `ratify.ps1:329-331` counts prior receipts by `$_.itemId` while cop-route receipts carry `taskId`, so the count is 0 for every card-named task and the PARKED flip at line 332 can never fire. No PARKED receipt exists anywhere, which is the confirming observation. Severity depends on U3.
§IF4 "GREEN at line 160 is three exit codes" | ADOPTED | The project's own doctrine forbids exactly this (§10, "never an exit code"), and honest compliance was available in the two lines at 145-151 that already parse the scoreboard summary and master baseline into the receipt but never assert on them. This is the exception the K4 FIT line fences.
§IF5 "The mechanical gate has never once refused" | ADOPTED | gate0 16 GREEN / 0 RED against key1 6/2/5 and key2 4/2/3; every refusal on record came from a key. Combined with the 2-of-10 fold this is the same defect the rejected resource-terminals BREAK reported, and it is retained here, where it belongs: a check that has never discriminated. The factory wrote it down once at `6f4a60e/receipt.json:105` and did not generalise, which is the finding.
§IF6 "Two receipt families under `metrics/ratify/` that cannot read each other" | ADOPTED | 19 `<sha7>/` directories with only 3 receipts (so `-Prepare` ran 16 times and `-Adjudicate` never did, newest 2026-09-09) against 20 cop landing receipts under `<TASK>/<attempt>/`, while WORK.md:4 still names `tools/ratify.ps1` the merge gate. If U3 resolves to dormant, this is the finding that matters, as the filing says.
§IF7 "A Stop hook fires on every session end and cannot be doing anything" | ADOPTED | `.claude\settings.json` invokes `coordination/tools/resume-seamless-gate.ps1`, which is not on disk. Bears on K9 and is one concrete reason the resumability observable has no passing output.
§IF8 "written against two files that do not exist" | ADOPTED | docs/14 §2's "Launch route (v1.1)" conditions the route on `tools/claude-review-capture.ps1` and `tools/codex-author-capture.ps1`, both missing on master, so the author-evidence contract (launcher-recorded session id, observed model, raw-output hash) is unsatisfied and the operating contract describes a route the repo cannot run. Bears on K1 and K6 and compounds IF2.
§IF9 "No typed `CLOSURE_INCOMPLETE` state exists." | ADOPTED | Recorded as an absent mechanism, not an observed failed closure — the distinction the filing draws itself, and the reason K7 is REJECTED(unexercised) rather than adopted. Kernel K7 already requires the typed report; nothing is missing from the text.
§IF10 "`ratify.ps1:80` derives `itemId` from the BRANCH NAME" | ADOPTED | Renaming a branch resets the round counter, in a factory where identity is a content digest everywhere else. Compounds IF3. Severity depends on U3.
§IF11 "An allowlist was widened after dispatch" | ADOPTED | Admitted against interest, and correctly scoped by the filer: on the test radius, not the acceptance thresholds. It is retained as the counter-evidence to O3 that the filing itself offers — a board arguing its pre-declaration discipline should name the one time it moved the fence after dispatch.
§M1 "The accurate mapping is the shape mlv-app was given" | ADOPTED | Kernel §6 now reads `| dng-auto-processor | code (primary) + measured-objective | high | code gates; auto grade "matches the manual grade per frame", scored on a held-out fold |`. §6 invites exactly this ("each project confirms or corrects it in its first filing"), and the measurement carries it: of ~20 landing receipts ONE carries a held-out-fold score, the rest are code-gate receipts, and S3B's says outright "scoreboard not run (no evaluator change)". `profiles/code.md` Benches now lists this project with the qualifier "no `P:code` filing yet", so the remap creates no acceptance credit. Filing it the other way would have credited a profile barely run, as you say.
§O1 "record in HARVESTS.md, per row, whether the window opened before the kernel existed" | ROUTED(ledger-context bench) | The measurement is adopted in fact and the ask is granted in this round's rows: all four carry `window-opened-before-kernel=yes|no`, and this filing is the only `yes`. The **standardisation** is routed, not written into §5, for a mechanical reason as much as a doctrinal one — HARVESTS.md is append-only, so the table header cannot be edited and the field lives inside the existing `subjects` cell; making that a rule needs a ledger-context bench to settle the field's form first. Both of your disqualifiers are accepted and are why this is context and not credit: adobe-ingester r2's window is entirely post-kernel and still returned 0, and granting K5 retroactively to all eight prior rows leaves E2E at 0.
§O2 "No clause asks whether a factory produces anything outside itself" | ROUTED(measured-objective outcome-reporting bench) | The narrowing is credited — you withdrew the admission-gate version yourself and showed, with the eight rows, that it would shrink the corpus from 134 verdicts to a remnant while leaving E2E at 0. What survives is a reporting line, and it is not yet writable: your own two predicates disagree (8 of 31 by self-label, 5 of 17 by delivered bytes, 1 that moved the measured objective), so "what fraction" has no defined denominator yet. Define it on the bench and re-file; §4's filing shape is unchanged this round.
§O3 "K5 has a form/substance gap that costs the kernel evidence it already has" | REJECTED(no defect filed) | Taken at the filing's own word: it is filed as diagnostic and not as a defect, and the refusal of retrospective credit is correct. The diagnostic is recorded — 28 of 39 cards carry a pre-declared contract section, and Q3-DEFAULT-FLIP-CAP20 names expected CSV sha256s before the run — but criteria that name no profile and version do not satisfy K5, and loosening the observable to "predeclared acceptance criteria bound to the subject before work, in any form" would delete the profile-version half of the clause. IF11 is the filing's own counter-evidence and is retained.
§O4 "independence migrating from the key axis to the author axis" | ADOPTED | Recorded as an observation, which is what it is filed as, and it is the reason agent-bridge's parallel question stays routed. agent-bridge's Untested sets the bar at "two profiles filing the same exhaustion"; this is one `code` FRICTION plus one expressly non-defect `measured-objective` observation, so K6 is unchanged. The point that a profile wanting key diversity must say so itself is correct and is already what K6 permits ("A profile may require more keys").
§U1 "a sandboxed agent CANNOT COMMIT even in a plain clone" | ROUTED(exact-seat permission bench) | Reported against the interest of the lane that supplied it, and refuted in this seat: `git init`/`add`/`commit` rc=0 (`b27fbf3`), a plain clone of the doctrine repo rc=0 (`fa4053f`), a direct `.git/index.lock` write permitted, and three other seats committed to this bus in the same window. No K8 BREAK filed, correctly. What would settle it is what you name: the exact seat type, cwd and permission mode under which the denial was observed.
§U2 "three quota events in five days each failed this factory CLOSED" | ADOPTED | Adopted as a refutation, not as a finding. The repo's own record contradicts the claim — WORK.md:40 "nothing is blocked" in-window, WORK.md:70 executors dying on the weekly limit as HELD-FOR-CAPACITY with WIP captured, T1-ACCEPTANCE-REPAIRS proceeding on one key and then parking — and those are K8-conformant parks. Filing no BREAK on a claim put to you that your own evidence refutes is the behaviour K11 exists for. The real costs are where you put them: K8 FRICTION and a K9 owner interruption.
§U3 "Whether `tools/ratify.ps1` is on the live path at all." | ROUTED(dng live-ratifier bench) | This is the highest-value open item in the filing, because it sets the severity of IF2, IF3, IF4 and IF10 at once — active defects or latent ones — and promotes IF6 if the answer is dormant. It could not be settled from the artifacts and is not guessed here.
§U4 "S1's four declared criteria have not been evaluated" | ROUTED(dng S1 acceptance bench) | Re-run when S1 leaves APPROACH. Two conditions for credit next time, one of them yours: the criteria must be evaluated against an unedited control with the receipt bound to the same identity, and the declaration must name a profile and version, which S1's does not. Your own note applies — airmypc S2 reached ACCEPTED and cloudvore S1/S2 reached DELIVERED and both scored zero, so being dispatched predicts nothing.
§U5 "Whether a steward-only contract can ever record an ADOPT" | ROUTED(dng adoption-authority bench) | Genuinely open and correctly not forced. docs/14 §7a reserving docs/12, 13 and 14 to one seat is the project enforcing kernel §3's "a project tree that fleet tooling may write into" from the other side, which is a conformance, not a defect. Resolve the authorised declaration path without writing reserved files; PROMPT K §2 covering the map's location does not cover who may record an ADOPT.

## Header

HEADER: `posture:` is not in R9's computed form (`harvest-status.py` flag POSTURE-NOT-R9-COMPUTED). "UNCOMPUTED — model review contributed to this filing (an adversary lane and two census lanes); no R9 tool run produced the required line, and an absent run is not a computed posture" is candid and states the right principle, but it is still a non-conforming line. Harvested on what the provenance supports: `providers: none (no lane cleared a sentinel for this filing)` is consistent with the body and is reported against interest. Run the R9 tool for the next filing.
HEADER: `profile: measured-objective@r2` is the filed profile and the findings are harvested under it, but M1 — this filing's own mapping correction, adopted above — makes `code` the primary mapping. The profile line is not re-labelled and the historical work is not relabelled `code@r4`; the ledger row records both.
HEADER: K11's anchor misquotes the clause. The filing quotes "R1–R9 apply to every report a factory makes about itself"; the kernel says "R1–R5, R7, R8 and R9 apply to every report a factory makes about itself". R6 is not in K11's list. The disposition is unaffected — the evidence cited supports the line as adopted — but a filing whose subject is reporting honesty should quote the clause it scores itself against.
HEADER: `subjects:` — no defect established, checked because the arbiter raised one. The "Retrospective, pre-kernel, NOT counted" label holds for all five landings: every one precedes the kernel epoch 2026-09-14T20:49:38Z. Three of the five (S3B `3a3c9bf1` 09-12, Q3-DEFAULT-FLIP-CAP20 `4d9b736b` 09-13, T1F1 `a4ce5008` 09-13) fall inside the declared window 2026-09-12T00:00Z .. 2026-09-15T20:00Z while still preceding the kernel, and two (S1-S02B `92e31e27` 09-09, Q3-L1A `221daf03` 09-11) precede the window as well. In-window-but-pre-kernel is exactly the distinction O1 asks the ledger to record, and it is recorded there. All five are retained as labelled historical mechanism evidence, never as window completion; no count changes.
