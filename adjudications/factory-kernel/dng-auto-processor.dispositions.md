filing_blob: d6a5f40f05dd5056e32068ac22e4975b3670cc65
filing_ref:  origin/review/dng-auto-processor-kernel-2026-09-20
spec_commit: 9e23075931e31d079d5e2dc90876779013436bdf
harvested_by: conjugal (interim kernel steward), 2026-09-20, automated harvest run 20260920T020405Z-d6c743fe
arbiter: gpt-6-astra (high) · consolidator: claude-fable-5 · lint: claude-opus-5 + gpt-5.6-sol · orchestrator: claude-opus-5

# Dispositions for dng-auto-processor's second filing, on specs/fleet-factory-kernel.md r5 (unchanged), profiles/code.md r8 (now r9) and profiles/measured-objective.md r3 (unchanged)

**Supersedes this file's ruling on blob `1f2eceffae1bc8f27fcae12f5e82d158d4bc1158`** (run 20260917T193405Z-ab7b8aef,
spec commit `5d1d0d95b3b82e853cd90019a3cd3457df309304`), which stands in bus history at that commit. None of its 43
dispositions is withdrawn: the K3 BREAK it adopted into kernel r5, the measured-objective adoptions into r3, the M1
remap and every IF, O and U ruling remain in force, and this filing's status lines on IF1–IF11 are dispositioned below
as new items against that ruling, not as re-adjudications of it. The second filing supersedes the first under PROMPT K §6:
kernel r4 → r5 and measured-objective r2 → r3 moved between the two, which is the revision trigger the filing itself names.

39 lines: 20 clause and profile findings (15 ADOPTED · 1 ADOPTED-CONDITIONAL · 4 REJECTED · 0 ROUTED) and 19 other
items (4 answers to routed benches: 1 ADOPTED · 2 ADOPTED-CONDITIONAL · 1 ROUTED; 7 status lines on IF1–IF11: 7
ADOPTED; 3 new instance failures: 3 ADOPTED; 3 observations: 1 ADOPTED-CONDITIONAL · 2 REJECTED; 2 Untested: 2
ROUTED), plus 5 HEADER lines. Filing verdicts (INSTANCE-FAILURE excluded per kernel §5): 11 FIT · 1 FRICTION · 0 BREAK
· 0 N/A · 4 UNEXERCISED — independently re-counted, matching the filing's own `## Counts` line and the arbiter's
recount (kernel/profile FIT subtotals 6/5). Separately 4 INSTANCE-FAILURE verdict lines (K9, K10, P:code
dispatch-preflight, P:measured-objective acceptance-evidence) plus IF12–IF14.

Rule: kernel §5, not the owner-bench rule. **This round changes no kernel text and no measured-objective text. It
changes two `code.md` rows, conditionally, on one bench, and refuses the filing's literal REPLACES.** P:code
subject-identity is the filing's only FRICTION and it is single-profile, so under §5 it changes that profile and
nothing above it; O7 is an observation whose incompatibility is demonstrated by the K3 FIT beside it. Both are
ADOPTED-CONDITIONAL under PROMPT-3 §3 as singular findings. **Departure recorded, per K11 and this file's own §K3
precedent of 2026-09-17:** the arbiter's ruled texts scope each amendment by naming the bench inside the profile
("For the dng cop route", "On the dng cop route"). The landed texts keep every condition, field and pass condition the
arbiter ruled and scope each by the condition that bench established — a delivery target that advances under a
candidate between review and landing; claims released by an observer's decision rather than by expiry — with the bench
scope recorded in the two disposition lines and here. The consolidator seat judged that trade explicitly: every scoped
rule already in `code.md` is scoped by condition, the one prior ADOPTED-CONDITIONAL profile increment (§E of the
AirMyPC ruling, 2026-09-18) landed the same way, and a row naming one project would need editing the moment a second
bench qualified. The cost is that the permission is available to any `code` bench that meets the conditions rather than
to dng alone; the arbiter's reasoning for the alternative (its Q1) is bench-independent and the conditions carry the
safety, so nothing the arbiter required is relaxed. Beside the scoping, the landed texts differ from the ruled texts
only in wording that changes no condition: "behavior" → "behaviour"; "Include all repository inputs on which" folded
into the manifest sentence as "includes every repository input"; the patch-id clause restated as "which corroborates
identity and never substitutes for it"; "decides stale" → "decides them stale"; "Both identify" → "Either identifies".
Every modal ("must" three times, "may replace leases") is the arbiter's own. Word cost: `code.md` 1,037 → 1,183 by `len(text.split())`, against 1,163 had the ruled
texts landed verbatim; the profile has no cap. Line format: `§<id> "<anchor>" | <DISPOSITION> | <what changed, or why
not>`.

## Findings

§K1 "never accepted on evidence whose only author is its producer" | REJECTED(unexercised) | Neither kernel subject was accepted: both PARKED at attempt 2 on refusals that came from the keys, so no acceptance receipt exists to name actors for production and acceptance. The filing refuses the credit against its own interest even though, in every round of both subjects, each key's model differs from the author's; that mechanism is noted for the next filing, not scored here.
§K2 "Everything unlisted is autonomous" | ADOPTED | FIT recorded in both directions on the register (docs/14 §0 plus WORK.md RULES rule 3): RAMP-BASE-COORDINATE-FIX landed with no owner step, MIRROR-SYNC-PIPELINE is held on a decision only the owner can make, and cop tick 98 declined a swarm-delegated word "on AUTHORITY, not on evidence" because §0 "holds a relay to be evidence, never an authority multiplier". The PROOF stands as filed: an owner-reserved act with no §0 entry would refute it.
§K3 "an observer other than the claimant can decide stale" | ADOPTED | FIT recorded for observer release on the r5 text this bench's first-filing BREAK produced: cop tick 82 returned "three RUNNING cards, all FROZEN and none finished" as "READY next tick; attempt NOT consumed; worktree kept", a release decided from outside the claimants, with the claim written "claim on disk before acting". Per-file equality supports the identity mechanism, but "2/2 reviewed==landed" alone does not demonstrate K3's combined digest over a declared set; that gap is the subject-identity line below, not this one. No text change.
§K4 "complete only on positive evidence it was asked to produce" | ADOPTED | FIT recorded: the blind judge was refused by its own controls as "VOID ZERO-FAILED POSITIVE-FAILED POSITION-DEGENERATE", and the S1 arm was refused by its pre-declared criteria (U4). FENCE preserved as filed: IF4, the three-exit-code GREEN in `tools/ratify.ps1`, is latent because the script is off the live path, not repaired.
§K5 "Before work starts, a subject declares its profile" | REJECTED(unexercised) | No acceptance receipt exists, so the binding half was never reached and no acceptance credit follows. The declaration half is recorded as the filing states it: GATE0-READER-GRAMMAR declared "at placement, before any seat of this card exists"; RAMP-DECISION-RECEIPT declared after its author's read-only approach, which limits its declaration credit to before-production (O6, and the subjects HEADER line).
§K6 "Every acceptance includes a key from an independence class" | REJECTED(unexercised) | No kernel acceptance occurred, so no accepted subject's keys can be classed. The provider diversity reported without credit — a key from a provider other than the author's in every round and on all 11 route landings — is retained, as is the committer/key overlap the filing discloses against interest under O5.
§K7 "Acceptance and delivery are separate states." | REJECTED(unexercised) | Nothing was accepted under the kernel, so nothing could fail to deliver. The eleven `--ff-only` landings are explicitly uncredited mechanism evidence; IF9 remains — no typed `CLOSURE_INCOMPLETE` — and is dispositioned at its status line, not here.
§K8 "never failing the factory closed" | ADOPTED | FIT recorded: RAMP-BASE-COORDINATE-FIX's key 2 died on "429 session limit" with the candidate retained and was relaunched whole once the window closed, and WB-TRIM-OVERSHOOT-DECOMPOSITION failed over on model capacity. `"attemptsConsumed":0` records a death consuming no attempt and did not become acceptance credit. The per-model credit FRICTION the first filing earned into measured-objective r3 did not recur.
§K9 "resumes from the project's tree and the bus alone" | ADOPTED | INSTANCE-FAILURE recorded, and the filing's own classification is accepted: P-RESUME re-created the cop and the steward after the 2026-09-18 rotation and stranded `dng-status-digest` and the drill that "has now been stranded twice and has never run once", when the tree could carry those seats' definitions. The first filing's K9 FRICTION "is not re-argued": that text-change referral is closed as withdrawn, not outstanding, and the resumability repair stays open as an instance failure addressed to the owner. No text change.
§K10 "it records the account it was derived under" | ADOPTED | INSTANCE-FAILURE recorded, unchanged from the first filing's adoption: no machine-readable inventory, no `probed_under`, `grep -ci parity docs/14-ORCHESTRATION.md` → 2 with both hits prose, and 0 of 57 window cop notes mention parity. Existing K10 and Dispatch preflight text already requires what is missing; §4 excludes INSTANCE-FAILURE from text change.
§K11 "apply to every report a factory makes about itself" | ADOPTED | FIT recorded for candid reporting, following the first ruling's precedent: no posture line asserted that was not computed, no credit taken for 11 route landings or 2 refused subjects, U3 answered against interest, O5 filed against this board. The anchor now quotes the clause correctly (HEADER below). This does not waive the non-computed posture HEADER defect, which is R9's and stands on its own.
§K12 "The steward harvests every filing and answers each one." | ADOPTED | FIT recorded from the predecessor artifacts: `dng-auto-processor.dispositions.md` at blob 1f2eceff answering all 43 lines and the 2026-09-17 HARVESTS.md row, which this file now supersedes without withdrawing. FENCE preserved: the filing cites those artifacts, not a `tools/harvest-status.py` run, which its seat may not execute (U6).
§P:code subject-identity "a key on a different tree does not transfer" | ADOPTED-CONDITIONAL(dng cop route) | **FRICTION adopted into `profiles/code.md` Subject identity (K3), conditionally; the literal REPLACES is refused.** The measured cost is concrete: 8 of 11 landings delivered a tree other than the one both keys reviewed, because "the cop's own bookkeeping commits advance master roughly every 30 minutes while a review takes about 90". The filed replacement — a key transfers when every reviewed path has the same blob in both trees, the intervening change touches none of them, and checks re-run green — would let root-tree identity remain the declared subject while its independent evidence migrates to another root tree, which weakens K5; and path disjointness alone does not exclude changed dependencies, build configuration or shared helpers, so a green filtered hook cannot establish that all reviewed behaviour is unchanged. What lands is reading (b): a landing subject may declare, before review, a nonempty manifest of repository paths covering every repository input the reviewed behaviour or acceptance checks depend on, external inputs pinned in the declared environment; K3 combines the path-manifest digest followed by the blob digests in path order; reviewed and delivered identities must match, the intervening range must touch no declared path, and acceptance checks must re-run green at the delivered tree; receipts bind each key to that identity and record both commits and the shared stable patch-id, which corroborates and never substitutes. The existing recomputation-command requirement covers the manifest's serialisation and digest; a manifest change changes identity. This does not certify that dng's `binding.json` sets meet these conditions, and it cannot retrospectively convert the eleven undeclared landings into kernel acceptances. Scope: the profile text is scoped by the condition this bench established — a delivery target that advances under a candidate between review and landing — and the bench scope of this conditional adoption is recorded here (Rule paragraph, departure).
§P:code acceptance-evidence "bind the acceptance contract by digest" | ADOPTED | FIT recorded: both kernel subjects bound the card minus its `state:` line by sha256 with a recompute command, round budget 3 and docs/14 §4's closing rule, and both digests recompute unchanged at the filing's HEAD, so one contract judged every round. Two limits: timing credit is bounded by O6 (RAMP declared after approach), and unchanged bytes show that nobody edited the cards, not that anything enforced the binding (IF13).
§P:code independent-key "whose class has not become a producer of the subject" | ADOPTED | FIT recorded for reported provider diversity — a key from another provider on 11 of 11 route landings and every round of both subjects — with the filing's committer FENCE preserved: on the four Codex-authored landings and RAMP-DECISION-RECEIPT the line holds only if a committer that "edits no product byte" has not become a producer. O5 supplies no blanket exemption for actors that influence the subject; no credit is enlarged here.
§P:code resource-terminals "independent key unavailable: typed terminals, no partial green" | ADOPTED | FIT recorded: RAMP-BASE-COORDINATE-FIX's key-2 death consumed no attempt (`"attemptsConsumed":0`, HELD-FOR-KEY), the candidate `8230cf8b` was kept on its branch until the relaunch, and cop tick 82 returned the frozen seats "READY next tick; attempt NOT consumed; worktree kept". No partial green was claimed.
§P:code dispatch-preflight "runs the project's resume gate before dispatch" | ADOPTED | INSTANCE-FAILURE recorded: 84 dispatches over the two window days after a tick-start CLI probe, with no resume-gate result, inventory snapshot or parity record in any of the 57 cop notes. This is the K10 gap seen from the profile; existing text requires exactly what is missing, and no replacement is filed.
§P:measured-objective subject-identity "runtime inputs outside git and delivered artifacts outside git" | ADOPTED | FIT recorded on the r3 text this bench's first-filing BREAK produced: the out-of-git engine is now pinned per run under docs/14 §6, "base engine ccb03575 pinned before and after". FENCE preserved: the components are pinned one by one in each card's ledger and no single identity digest combines them in manifest path order, so no combined identity is demonstrated.
§P:measured-objective determinism-class "byte-deterministic claims require an identical repeat at tolerance zero" | ADOPTED | FIT recorded: RAMP-BASE-NULL-TEST showed "ramp ON at g = 0 ≡ CONTROL byte-for-byte on both folds, and the g = 1.0 witness actuated" — the r3 text adopted from this board's first filing, exercised with an arms-differ witness. No text change.
§P:measured-objective acceptance-evidence "an unverified binding is UNEVALUABLE" | ADOPTED | INSTANCE-FAILURE recorded against interest: DNG-SELF-TARGET-BUCKET-TIER's arm was scored on an uncommitted subject — "this line records the RUN and the card carries the resulting sha" — so its binding is asserted by the card, not verified, and `tools/scoreboard.ps1` still has no clean-tree check. Under r3 that score is UNEVALUABLE although the instance labelled the card MEASURED-NEGATIVE; nothing was accepted on it.
§A:U3 "is on the live path at all" | ADOPTED-CONDITIONAL(dng live-ratifier bench) | The live-path inquiry closes for this window: 0 of 11 receipts name `ratify.ps1` or a gate0, and the old receipt family's newest file is dated 2026-09-09. Consequences recorded as the filing states them — IF2, IF3, IF4 and IF10 are latent, IF6 is operative. Conditional because it is one window's measurement, not a timeless claim that the script cannot become live again.
§A:U4 "evaluated, and they refused the arm" | ADOPTED | The unevaluated-criteria inquiry closes: fit 1.88 against `fit < 0.54` and median|dTemp| 686 against `<= 587` both FAIL, "LOSING ARM, NEVER A CANDIDATE". It closes only that inquiry, not the acceptance-evidence deficit: neither credit condition the first ruling set is met — the filing itself says the declaration named no profile or version and the score's binding is asserted, not verified — so no acceptance or conformance credit follows, and conservative refusal earns no valid measured-objective score and no K5 credit.
§A:O2 "landings whose delivered bytes touch a feature assembly" | ADOPTED-CONDITIONAL(measured-objective outcome-reporting bench) | The denominator is recorded as defined: landings whose delivered bytes touch a feature assembly, 3 of 11 this window (RAMP-GAIN-LEVER; COLOUR-RECEIPT-NOTRUN-STATE's internal feature commit `42e96176`; RAMP-BASE-COORDINATE-FIX), and 0 of those 3 changed the emitted grade — "gain 1.0 bitwise-identical", an opt-in ramp fix, and a receipt state "NotRun, not Declined". Three means three landing ranges, not three extra landings. This is one bench and one window; kernel §4's filing shape is unchanged and no reporting requirement enters.
§A:U5 "adoption authority: unchanged" | ROUTED(dng adoption-authority bench) | "DOGFOOD (not ADOPT; binds nothing; docs/14 governs)" leaves authority where the first ruling found it: unresolved, with docs/14 holding 0 `kernel` or `dogfood` mentions. The owner action is named in RECEIPTS as an explicit item; neither this ruling nor a filing seat grants adoption, and naming the action does not itself establish an authorised adoption path.
§IF1+IF11-status "historical, unchanged" | ADOPTED | Historical failures remain as adopted on 2026-09-17; no repair is claimed and none is credited.
§IF2-4+10-status "is off the live path" | ADOPTED | LATENT accepted for this window on U3: `tools/ratify.ps1`, unchanged since `b2386925`, is off the live path. Latency does not repair the script; the four defects stand as adopted.
§IF5-status "No gate0 ran in the window." | ADOPTED | LATENT recorded: no gate0 ran, so the never-refusing gate could not be re-measured and is not credited as repaired.
§IF6-status "LIVE, and now the operative one" | ADOPTED | LIVE recorded, promoted exactly as the first ruling foresaw: WORK.md:4 still names `tools/ratify.ps1` the merge gate while 0 of 11 window landings used it.
§IF7-status "RESOLVED at read time" | ADOPTED | Resolution accepted at read time: the configured Stop hook now points at `tools/session-checkpoint.py`, which exists. Its outside-git location prevents dating the change and does not prove a successful hook execution.
§IF8-status "are both still absent from master" | ADOPTED | Unchanged: `tools/claude-review-capture.ps1` and `tools/codex-author-capture.ps1` remain absent from master, so the launch route docs/14 §2 describes still cannot run as written.
§IF9-status "IF9: unchanged" | ADOPTED | Unchanged: the typed `CLOSURE_INCOMPLETE` mechanism is still missing, and no new failed-closure event is established — the 11 mirror failures are IF14, which docs/14 §3 keeps from being a delivery failure.
§IF12 "The landing receipt has no schema." | ADOPTED | Recorded as an instance interoperability defect: 11 receipts with 11 distinct top-level key sets and three seat encodings, so K6's independence classes are computable only by a reader who knows all three. Existing independence obligations require the computation to be reproducible from evidence; they do not prescribe one universal receipt schema, so a common parser or schema is a remedy, not an invariant missing from K6.
§IF13 "The two kernel subjects' declarations bind nothing the gate reads." | ADOPTED | Recorded: declarations that say "binds nothing; docs/14 governs" are read by no rule, tool or hook, and the contract digests held because nobody edited the cards. Existing K5 already requires the declared profile to govern acceptance at the exact identity, which needs semantic consumption and authentication of the declaration, not a program reading a particular JSON filename; unchecked declarations establish no compliance, no acceptance occurred here to demonstrate a false acceptance, and no missing invariant is established.
§IF14 "The derived mirror failed on 11 of 11 window landings." | ADOPTED | Recorded: every receipt's `mirrorSync` is FAILED, from "fast-import rejects pre-existing history path" to "mirror stays baf02b65", both DAILY lines read `ci=stale`, and the cause is an OWNER-DEFECT history path carrying a trailing carriage return. Failure of a derived mirror does not undo delivery to the declared authoritative integration branch ("local master is the authority"); had hosted CI been a required acceptance check its absence would defeat acceptance, and the filing does not establish that condition. The eleven route landings remain uncredited kernel subjects either way.
§O5 "can be key 1 for the same subject" | REJECTED(no defect filed) | Observation recorded; no clarification lands. A committer that "edits no product byte" and records already-fixed bytes is not a producer merely by executing the commit, but that phrase alone does not prove it neither selected nor changed subject artifacts, generated outputs or substantive content; K1 and K6 apply to actual contributions and independence classes, and the 2026-09-19 move of key 1 to `claude-fable-5-1` does not by itself change the provider trust domain or settle producer status.
§O6 "is ambiguous for a read-only planning phase" | REJECTED(no kernel defect established) | Subject-specific approach work is work even when read-only: GATE0's declaration precedes it and earns before-work; RAMP's follows it and earns before-production, not K5's before-work observable — the card's own "so the filing can judge whether that is" invitation is answered that way. Declaring before approach is an available compliant practice that obstructs no honest work, so this supplies neither BREAK nor cross-profile FRICTION, and no kernel amendment or cut is authorised.
§O7 "code@r8's Claims field speaks only of leases." | ADOPTED-CONDITIONAL(dng observer-released claims bench) | **Adopted into `profiles/code.md` Claims (K3), conditionally.** The lease-only strengthening excluded this bench's "Lease: NONE, by design" mechanism ("never add a lease") despite the kernel K3 FIT above; a permitted strengthening can still create demonstrated profile friction, and the absence of a REPLACES line does not erase the filed incompatibility — the AirMyPC ruling expressly rejected that procedural veto. The row now admits observer-released claims that name the subject, owner, owned processes and the rule by which another observer decides them stale and releases, retains every lease field, and requires either form to identify the live holder rather than a short-lived claim-writing helper. K3 is not weakened: observer determination and release, temporal exclusivity and single writer all remain required, and named fields strengthen K3's operational specification. Scope: condition-scoped in the profile text, bench-scoped here (Rule paragraph, departure).
§U6 "this filing cannot carry a computed posture" | ROUTED(dng authorized posture-computation seat) | The filing seat's docs/13 P-STEWARD 7e restriction explains the absent computation and does not satisfy R9. Another register-authorised seat must compute the posture from retained sentinels and retain its exact output; if no such seat exists, authority is resolved through the owner.
§U7 "not measured across more than this one window" | ROUTED(measured-objective outcome-reporting bench) | Test the O2 denominator across further windows on the bench; one window's 3 of 11 is a definition exercised once, and cross-profile corroboration is required before any kernel reporting change is proposed.

## Header

HEADER: `posture:` is non-conforming. "not computed — model review contributed (two read-only claude-opus-5 ratification lanes)" triggers R9's computed-output requirement, and the project register's prohibition on the filing seat running that tool explains this seat's omission but does not waive R9. Have another register-authorised seat compute the posture from retained sentinels and supply its exact output; if no such seat exists, resolve authority through the owner (U6). `providers: none (no lane cleared a sentinel for this filing)` is consistent with the reported absence of sentinel-complete lanes and is reported against interest.
HEADER: `profile:` reports `code@r8 (primary) + measured-objective@r3` while explicitly identifying `code@r7` for both declared subjects, the revision current when they declared. No concealed revision substitution is established: git dates r7's landing to 2026-09-18T15:49:13Z and r8's to 22:36:01Z, and both declarations (15:59:34Z and 20:36:21Z) fall between them; the ledger's 15:19 and 22:04 run identifiers are harvest starts, not landing times. Both facts are preserved in the ledger row. Adjudication against r8 is not proof that the subjects ran r8, and later supersession neither invalidates a pinned revision nor grants retrospective r8 conformance.
HEADER: `subjects:` is supported as "0 end-to-end": two declarations before production, zero acceptances, both PARKED at attempt 2, and eleven uncounted route landings. The Subjects table carries eleven rows, eight changed-tree and three same-tree. "before production" is not to be strengthened into two declarations before work: RAMP-DECISION-RECEIPT followed its author's approach, so only GATE0-READER-GRAMMAR establishes before-work timing. The fleet-first claim for profile/version declaration is not established — the ledger's conjugal row at blob `3a36f3e6` records a K5 FIT as testimony, not reopened here — so these are dng's first two declarations, not the fleet's.
HEADER: `kernel:` and the declarations' blob prefixes verify. The current kernel blob is `6821c4c3390712522b699e72a1781bc9d89a771a`; current `code@r8` is `1d7d0e872eeadfba8d974a4c97e02239404166ec`; `code@r7`, at bus commit `21d8c3cec573ae446a67d2edda20fcba1e08b048`, is `3b38da16bc6b320da50e48f59efc471a3622b170`, matching the `3b38da16` the declarations cite. No digest mismatch is established.
HEADER: K11's anchor now quotes the clause. The first filing wrote "R1–R9"; this one quotes "R1–R5, R7, R8 and R9 apply to every report a factory makes about itself", which is byte-identical to the kernel's K11 line (R6 excluded). The disposition above is unaffected in either direction; the correction is recorded because the first ruling asked for it.
