project: mlv-app
kernel: fleet-factory-kernel r2
profile: code@r2
instance: MLV-App agents/factory-kernel-instance.md, on MLV-App PR #112 (https://github.com/layibabalola/MLV-App/pull/112, head 240c828e, docs-only). Merged to MLV-App master as merge commit `b8c9a93d` on 2026-09-15 after a cross-family review (gpt-5.6-sol) returned APPROVE at head `3d79530`; verify with `git cat-file -e b8c9a93d:agents/factory-kernel-instance.md`. KERNEL: DOGFOOD fleet-factory-kernel r2 · profile code@r2 · instance agents/factory-kernel-instance.md · since 2026-09-14.
subjects: 0 run end-to-end in this window. Exercised but not completed: S1 = fleet doctrine bus review-posture self-heal, orchestrated from the MLV-App board, identity git tree `788d194fba73839b4c5b0d5c66da6326f76a5936` at fleet doctrine bus commit `43c9a7b9214a51d6fc72944d5908a9769a1d8e11` (pushed as fleet doctrine bus branch `fix/review-posture-self-heal-20260914`, PR #66 (https://github.com/layibabalola/softwarefactory-fleet-doctrine/pull/66), stacked on bus PR #62, open; recompute: `git rev-parse 43c9a7b9214a51d6fc72944d5908a9769a1d8e11^{tree}`), not accepted, not delivered. S2 = MLV-App card PLAY-COUNTERS-CPU, a failed seam: implementer run exit 1 at MLV-App baseSha `5e913116`, then rescue run PLAY-COUNTERS-CPU-RESCUE exit 0 in 137.1 s at MLV-App baseSha `829b573e`; no delivered tree. A separate owner-started MLV-App session is repairing S2's receipts; that repair is outside this window.
window: 2026-09-14T18:46:35Z .. 2026-09-15T05:22:22Z
health: assurance=UNSATISFIED operability=PRESSURED
providers: claude, codex
posture: mlv-app-selfheal-PARTIAL (30/33 lanes)

POSTURE COUNT: 33 lanes dispatched from this board in the window (every MLV-App board-local `selfheal-20260914/<lane>.md` brief with a `<lane>.out`), judged on the sentinel in each lane's own output, not on rc or receipts: 30 end with the exact line `LANE-COMPLETE`; 3 do not: R5-repair (its 3,600 s timeout, rc=124), R8-failclosed (account session limit, rc=1), R9-reuse-posture (ended its turn while its own test run was still in the background). Lanes are claude-opus-5 and gpt-5.6-sol; this is an ad-hoc swarm, not a named R9 posture. Measured 2026-09-15T05:22Z.

VERDICT CONVENTION USED HERE: r2 §4 has no token for "the clause arose, honest compliance was possible, and this instance failed it". Such clauses are filed UNEXERCISED (no subject of this window satisfied the clause's observable) with a pointer to a numbered Instance failure, never FIT and never FRICTION. See Kernel text observation 1.

K1 | UNEXERCISED | "A candidate is never accepted on evidence whose only author is its producer" | No candidate was accepted in the window. Lane-level separation existed for S1 (producer and verifier lanes are separate processes), but the hub wrote and ran every gate and one producer lane left no receipt (Instance failure 1) | PROOF: an accepted MLV-App subject whose receipts name different production and acceptance actors, including who wrote and ran the gate
K2 | UNEXERCISED | "Each project keeps one register of what needs the owner and what does not." | MLV-App has no such register; `docs/never-authorized.json` (schema `mlv-app/never-authorized/v26`, NA-1..NA-10) lists acts no actor may perform, which is a different artifact (Instance failure 11) | PROOF: an MLV-App file at master that registers what needs the owner, and one decision taken without asking because it allowed it
K3 | UNEXERCISED | "One claimant holds a subject at a time, under a lease that expires." | S1 had no claim record while a concurrent session changed the same file on bus master (Instance failure 8). code@r2 Claims (K3) already states the lease content this lacked, so no new request | PROOF: a claim record for an MLV-App subject naming subject, owner and expiry, consulted before editing
K4 | FIT | "Never exit code, output size or silence." | Scoped to the gates this board wrote: MLV-App board-local `round2.sh:9-11` refuses a lane on a missing `LANE-COMPLETE` sentinel regardless of rc; `round6.sh:6` refused round 6 when R5-repair ended rc=124 with 0 bytes and no sentinel; the S0 arbiter exited rc=0 at 7,997 bytes with zero sentinels and stage C blocked (MLV-App board-local `C:\temp\rp-out-mlv-app-20260914\arbiter.rc`). MLV-App's own lane receipt schema fails the clause separately (Instance failure 2) | PROOF: a gate written by this board that passed a lane on rc or bytes alone
K5 | UNEXERCISED | "Before work starts, a subject declares its profile and profile version." | Kernel r1 reached the bus at fleet doctrine bus `a0d8d4c` (2026-09-14 15:49:38 -0500), about two hours after S1's briefs (MLV-App board-local `common.md`, 13:46:35 -0500); no subject in the window could have declared a profile line | PROOF: an MLV-App subject started after a0d8d4c with no profile line
K6 | UNEXERCISED | "When the check is model judgment, the cross-family rule applies and the claim is computed, not asserted (R3)." | No subject was accepted, so no acceptance key exists to classify. Cross-family review did run on S1 and changed it: gpt-5.6-sol lanes X5, X6, X7 returned REQUEST-CHANGES with findings the Anthropic-family lanes had not produced (MLV-App board-local `X5-sol.out`, `X6-sol.out`, `X7-sol.out`) | PROOF: an accepted MLV-App subject whose acceptance receipt names a key from a class other than its producer's
K7 | UNEXERCISED | "Acceptance and delivery are separate states." | Nothing was accepted, so nothing could fail to deliver; MLV-App also has no typed accepted-not-delivered state (Instance failure 3) | PROOF: a typed MLV-App record of an accepted subject that has not delivered
K8 | FIT | "Running out of quota means rotating or parking the work that needs inference, never failing the factory closed." | Last quota event: 2026-09-15T01:16:23Z, lane R8-failclosed (claude-opus-5) ended rc=1 with the 63-byte output "You've hit your session limit · resets 10pm (America/Chicago)" (MLV-App board-local `R8-failclosed.receipt.json`, `R8-failclosed.out`). In-flight work: its code edits stayed uncommitted in the worktree and were not treated as done; `round10.sh` refused to start the next gate without the lane's sentinel. Work needing no Claude inference continued (the hub ran the deterministic offline suite on the parked edits), and the cross-family review key stayed available on the unaffected OpenAI provider. The factory did not fail closed and no gate opened on the reset | PROOF: a board gate that passed R8's parked edits without its sentinel or an independent run of the suite
K9 | UNEXERCISED | "A fresh session on a new account, with empty memory, resumes from the project's tree and the bus alone." | Not demonstrated: MLV-App's resume surface and all of S1's working state are gitignored and host-local (Instance failure 4) | PROOF: a fresh session on another host resuming an MLV-App subject from remotes and the bus
K10 | UNEXERCISED | "Provider and model inventory is machine-scoped and probe-derived, and it records the account it was derived under." | The probe ran and was wrong: it reported Codex unavailable while Codex was healthy (Instance failure 9); no complete parity-before-work evidence was retained for S1 | PROOF: an MLV-App dispatch whose retained preflight carries the inventory snapshot, its probed_under account and parity evidence
K11 | UNEXERCISED | "R1–R5, R7, R8 and R9 apply to every report a factory makes about itself" | This board filed reports its own artifacts contradicted (Instance failures 5, 6, 10); the clause arose and was not met | PROOF: a window of MLV-App reports each re-derived against its artifact before publication
K12 | UNEXERCISED | "Every project that runs the kernel files what happened" | This is MLV-App's first filing. Before filing, on the fleet doctrine bus: `python tools/harvest-status.py factory-kernel` -> `filings=5 open=4`, no mlv-app row | PROOF: harvest-status showing this filing UNHARVESTED, then HARVESTED

P:code resource-terminals | FIT | "suite timeout, thermal admission, runner capacity, independent key unavailable: typed terminals, no partial green" | R5-repair hit its 3,600 s bound: MLV-App board-local `R5-repair.receipt.json` `rc:124, bytes:0, sentinel:"DID-NOT-RUN"`. No partial credit was taken; `round6.sh:6` refused to proceed and the work resumed as a separately receipted successor lane (R5b-finish, rc 0, sentinel RAN) | PROOF: a round that proceeded on R5-repair's rc=124
P:code acceptance-evidence | UNEXERCISED | "the project's pinned acceptance runs at the exact commit and declared environment" | S1's acceptance runs are bound to no commit or environment (Instance failure 7) | PROOF: an MLV-App acceptance receipt naming its commit, interpreter and TEMP/TMP
P:code subject-identity | UNEXERCISED | "git tree OID of the candidate commit" | S1 had no tree OID for about four hours and the board's gates hashed a view blind to staged and untracked content (Instance failure 10) | PROOF: an MLV-App gate that recomputes a subject's identity over its full declared set
P:code delivery-target | UNEXERCISED | "integration branch via the project's landing path" | S1: pushed as fleet doctrine bus branch `fix/review-posture-self-heal-20260914`, PR #66 (https://github.com/layibabalola/softwarefactory-fleet-doctrine/pull/66), stacked on bus PR #62, open; not merged into fleet doctrine bus master, and the merge belongs to the tool steward after bus PR #62 | PROOF: the merge record of S1's PR

## Instance failures

Each arose in this window where honest compliance was possible. They count toward `health`, not toward changing the kernel.

1. **K1 - gates written and run by the hub.** One Opus session wrote and ran every round gate script (MLV-App board-local `round2.sh`..`round10.sh`) and wrote this filing. Producer lane L1-tooling never wrote its receipt: MLV-App board-local `round1.log` records `lane.sh: line 11: syntax error near unexpected token ')'` after its runner was rewritten mid-execution; its completion was judged only on the sentinel in its output. 6 of 8 Anthropic-family verifier lanes were the producer's own model class.
2. **K4 - MLV-App's lane receipt schema admits a complete failure.** MLV-App `.claude-state/fleet-runs/ws-PLAY-COUNTERS-CPU-20260914T151951Z/sonnet-001.receipt.json` (schema `mlv-app/fleet-lane-receipt/v1`) records `"state":"complete"` and `"complete":true` beside `"exitCode":1`; the terminal reason `max_turns` appears only in the sibling `sonnet-001.last.txt`. Of 26 MLV-App `ws-PLAY-COUNTERS-CPU-2026091[34]T*` run directories, 25 hold no receipt.
3. **K7 - no typed accepted-not-delivered state.** MLV-App `tools/repo_hygiene/brokered_closeout.py` has no `CLOSURE_INCOMPLETE`; detection is a SessionStart dirty/ahead worktree sweep only (measured 2026-09-15T05:22Z: 10 MLV-App worktrees, 3 with uncommitted changes, 3 ahead of MLV-App master by 17, 17 and 1 commits).
4. **K9 - resume state is host-local.** MLV-App `.gitignore:54` excludes `.claude-state/`, which holds every S1 brief, report, receipt and round script and MLV-App's own `RESUME.md`, heartbeat and resume checker. Pushing work-in-progress branches and tracking receipts was available and not done.
5. **K11 - a commit message its own diff refutes.** Fleet doctrine bus `9a62830` (2026-09-14 13:02:38 -0500), titled "fix(bootstrap): clarify Agent tool model parameter in PROMPT B", is `1 file changed, 1 insertion(+)`: one blank line appended to `bootstrap/PROMPT-B-begin-review.md`. Authored and pushed from this board with no verifier.
6. **K11 and kernel r2 §3 - a fleet tool wrote into MLV-App's tree.** PROMPT A §2b created `.claude/doctrine-sync.json` in the MLV-App working tree, untracked and unignored, from about 12:33 to 21:44 CDT on 2026-09-14 - the writer shape fleet doctrine bus `f6e1972` records as halting adobe-ingester, and the case r2 §3 now names. Remediated in-window: moved to `~/.claude/doctrine-sync/MLV-App.json` (SHA-256 `3f99b2a23dd3dd0f4b1240aeec501ca26e7a1b7c8e7e5e53475861c093ab18dc`, verified equal) and absent from the tree.
7. **P:code acceptance-evidence - acceptance bound to nothing.** S1's acceptance runs (offline suite, pytest, red-with-revert checks) ran on an uncommitted worktree; no receipt names the commit, the interpreter or TEMP/TMP.
8. **K3 - no claim record.** While S1's uncommitted branch rewrote `tools/probe-machine-inventory.sh`, fleet doctrine bus `97b6f66` (2026-09-14 13:46:18 -0500) changed the same file on bus master. Two open bus PRs (#62 and S1) and an unmerged branch (`fix/review-posture-sentinel-before-data`, fleet doctrine bus `3ae7434`) also touch the same tool. Cost of the missing claim: one rebase, one stacking adjudication, one supersession adjudication.
9. **K10 - a probe-derived inventory that was wrong.** 2026-09-14 on host VIRTUAL-TEN, `~/.claude/machine-inventory.yaml` recorded Codex unavailable while Codex was healthy; fleet doctrine bus `97b6f66` describes the mechanism ("fails silently, marking Codex models UNVERIFIED when they are healthy"), and fleet doctrine bus `9eeba29` corrects its first diagnosis. Two independent fixes then disagreed.
10. **K11 and P:code subject-identity - gate summaries stronger than their evidence.** This board's round-2 and round-4 gates hashed `git status --short && git diff` (MLV-App board-local `round2.sh:12`, `round4.sh:5`), blind to staged content and collapsing an untracked directory to one line, yet printed "TREE UNCHANGED by adversaries". A later bindings digest on S1 was measured by a cross-family reviewer to fail open (a failed per-path hash still produced a digest; `unknown` compared equal to `unknown`).
11. **K2 - no authority register.** MLV-App records prohibitions (`docs/never-authorized.json`) and standing orders in hooks and memory, but no register of what needs the owner; in this window a dispatcher replaced the owner's clickable chip with an in-session model override, and only the owner caught it.

## §6 mapping correction

`mlv-app | code (primary) + measured-objective (render/export parity and playback measurement) | high`. Eight of ten code-profile fields resolve to `code` at MLV-App master: `tools/coordination/record_workstream_completion.py` binds identity as HEAD sha plus per-file digests, delivery is MLV-App master via review branches, and gates are `docs/never-authorized.json`. Determinism class and resource terminals are the render/export sub-instance. No measured-objective subject ran in this window, so no `P:measured-objective` line is filed.

## Kernel text observations (not clause verdicts)

1. **No verdict for an instance failure.** r2 §4 defines FIT as "held" and FRICTION as "held at a measured cost", so a clause that arose and was not met has no token. This filing files such clauses UNEXERCISED with pointers. Two independent checkers of earlier drafts (one Anthropic-family, one OpenAI-family) rejected both FIT and FRICTION for them. Suggestion: a dedicated token counted toward `health`, never toward changing the kernel.
2. **§4 line grammar omits UNEXERCISED** (`FIT|FRICTION|BREAK|N/A`) while §4 prose defines it and PROMPT-K §3 mandates it.
3. **§5 criterion 1 counts filings, not how they were produced.** Fleet doctrine bus `9eeba29` records a RECEIPT that `review/airmypc-2026-09-14` "was produced below the floor and outside run.sh".
4. **K2's observable is positive-only.** Nothing in it surfaces an act the owner later ruled should have come to him (Instance failure 11).
5. **K10 observable.** Suggested addition: a negative control showing the probe distinguishes a model that is ABSENT from a CLI that could not be invoked (Instance failure 9). code@r2 Dispatch preflight retains the snapshot but does not make a silent probe fault visible.
6. **code subject-identity before the first commit.** Suggested: a digest over tracked, staged and untracked members of the declared set, computed fail-closed (Instance failure 10).
7. **code stress-on-the-kernel, measured additions.** A concurrent change to the same artifact landing mid-subject (Instance failure 8), and a runner script edited while a lane executes it (Instance failure 1).

## Measured-objective, historical - NOT exercised this window (for the profile steward)

No render, export or A/A run occurred in 2026-09-13..14. Recorded earlier in MLV-App's bus spec `specs/mlv-app.md`: six identical legs of one binary gave per-stage medians 290/301/114/117/148/167 ms, a 110.4% spread across legs 2-6, and two legs showing a clean -36% that was noise. Thermal degradation completed the fold instead of stopping it. Candidate profile text: thermal and host contention are venue-validity terminals; run the A/A first with enough legs to see a trend; prefer deterministic counters over wall time.

## Untested

- Whether steward recusal (§5) is enforceable by mechanism.
- K3 single-writer for non-file stores and K7 for non-git delivery targets: MLV-App ran no such subject.
- P:code budgets: the profile asks for wall time per suite and provider calls per subject; this window's receipts record lane wall time and counts but not provider calls per subject.
- S2's verifier evidence beyond its receipts.
- Whether MLV-App's `check-resume-derived-values.ps1` currently passes: not run.
