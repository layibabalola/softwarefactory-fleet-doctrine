# Factory-kernel dogfood filing — agent-bridge (Virtual-Ten)

project: agent-bridge
kernel: fleet-factory-kernel r1
profile: code@r1
instance: docs/internal/FACTORY_KERNEL_INSTANCE.md (unlanded)
subjects: 0 completed end-to-end; 1 run (identity 95f117c98bf63506af6e334443d068bb1052d31334765f94a60801189ea97ce1)
window: 2026-09-14T20:34Z .. 2026-09-14T22:10Z
health: assurance=UNEVALUABLE operability=PRESSURED
providers: claude, codex (review subject lanes only; this filing's advisory adjudication was single-family Claude)
posture: conjugal-standard-PARTIAL (12/17 lanes; missing: Arbiter 0/1; Consolidator 0/1; Classifier 0/3) (review subject; this filing itself: no model review)

**What this filing is.** Operational evidence from one Opus orchestrator session on Virtual-Ten. It is not a design review of the kernel. The subject is real work the owner asked for, not work created to have something to measure: a design review of `specs/conjugal-approach-a-v7.4.md` with agent-bridge as the test bench, filed at `origin/review/agent-bridge-2026-09-14` @ `28430bc`. It ran **before** this project declared `KERNEL: DOGFOOD`, so no profile line was recorded before work (K5). Every clause that needs a declared profile or an accepted subject is marked `UNEXERCISED` or `FRICTION`, never `FIT`. The plan for this filing was put to three adversarial Claude Opus agents (one family, so no cross-family standing). They cut three of the five recommendations the session had drafted, because kernel r1 already contains them (§0 layers, K5 profiles, §5 criterion 2 and word cap). Only what r1 lacks is filed below.

**K3 identity**: sha256 over three newline-joined components, no trailing newline: subject blob `b41e3af00b757f96030e5127d170940776362cf0`, bench HEAD `e842e70eb51d82188e34df53894ba4aa23158903`, and `rubric_id` `b2f5862e9c0990d0e69e96862fba7ab4cc5e263151679ac336ab429187341c45`. Recompute: `printf 'b41e3af00b757f96030e5127d170940776362cf0\ne842e70eb51d82188e34df53894ba4aa23158903\nb2f5862e9c0990d0e69e96862fba7ab4cc5e263151679ac336ab429187341c45' | sha256sum`. Reproduced by two independent computations. The component order is this project's proposal; no profile defines one for review subjects.

## Clauses

K1 | UNEXERCISED | "A candidate is never accepted on evidence whose only author is its producer" | agent-bridge run `-a3`: nothing reached acceptance. The Consolidator and Classifier never ran, and the filing's findings body was assembled by the orchestrator and labelled as such (`review/agent-bridge-2026-09-14` header comment) | PROOF: a completed review subject whose accepting receipt names its producer

K2 | FRICTION | "Each project keeps one register of what needs the owner and what does not" | agent-bridge's register `docs/internal/OPERATING_MODEL_2026-09-08.md` (`never_authorized`) is on branches `p0a-bar-v4` and `claude/canonical-20260829` but not on the session branch `claude/great-kapitsa-647dd7`. The live autonomy rule is in the owner's machine-local `~/.claude/CLAUDE.md`. Measured cost: three decisions no register covered in one window (abandon attempt 2, rerun `--from B`, file PARTIAL instead of retrying); the last alone took a three-agent swarm of about 295K subagent tokens | REPLACES: "Each project keeps one register" -> "Each project keeps one register, reachable from every checkout and worktree the project's sessions run in" | PROOF: a worktree session of a project that finds its register at the same path as the canonical checkout

K3 | FRICTION | "One claimant holds a subject at a time, under a lease that expires" | agent-bridge run claims were local `RUNNING.marker` files with no lease or expiry. A peer project (airmypc) killed attempt 1's process tree at about 20:38:39Z by command-line pattern, because nothing it could read named the owner (bus RECEIPTS `8ba4552`, corrected `a4f610b`). Cost: one attempt lost, one cross-session message exchange. Identity above | REPLACES: "under a lease that expires" -> "under a lease that expires and that any process on the same host can read before acting on the subject's processes" | PROOF: a second session's cleanup that reads the lease and spares the lanes

K4 | FIT | "Work is complete only on positive evidence it was asked to produce" | agent-bridge `-a3/arbiter.rc` = 0 with 8,466 B and 9,367 B across two passes and no `LANE-COMPLETE`; the tool scored it DID-NOT-RUN and blocked stages C and D. Negative checks this window: arbiter rc=0 without its sentinel (twice); `bash` from PowerShell resolved to WSL with no distribution; `tools/check-cli-auth.py`, prescribed by lane-orchestrator §1, exists in neither checkout | PROOF: a lane counted complete on rc or bytes alone

K5 | FRICTION | "Before work starts, a subject declares its profile and profile version" | agent-bridge: no kernel profile describes design-review work. The code profile's acceptance evidence is "pinned acceptance runs at the exact commit", which a document review cannot produce. The run did pin an acceptance contract before work (`rubric_id`, `rubric.json`), and the PARTIAL was kept as a typed terminal, never relabelled. Cost: the review had no profile to declare | REPLACES: "a subject declares its profile and profile version" -> "a subject declares its profile and profile version; review subjects declare the review profile they are judged under" | PROOF: a review subject whose declared profile states its acceptance evidence before the run

K6 | FIT | "When the check is model judgment, the cross-family rule applies and the claim is computed, not asserted" | agent-bridge `-a3`: `cross_family: validated` printed by `review_posture.py posture` from sentinels (Claude: design-scope, lint-claude, 5 panel seats; Codex: design-verify, lint-codex, 3 panel seats). Verification only; no subject was accepted | PROOF: a filing whose cross-family line is typed

K7 | UNEXERCISED | "Acceptance and delivery are separate states" | agent-bridge: acceptance was never reached. The pushed review branch (`ls-remote` = `28430bc`) is the filing's transport under R7, not a delivery. Detection of accepted-but-undelivered subjects: NONE (instance map) | PROOF: an accepted agent-bridge subject that never reaches its landing path and is detected

K8 | UNEXERCISED | "Running out of quota means rotating or parking the work that needs inference" | agent-bridge `-a3/*.log`: no usage-limit or rate-limit error. Two full reviews (agent-bridge and airmypc) shared the same accounts on one host without a quota event | PROOF: a quota event during a run and the fate of its in-flight lanes

K9 | FRICTION | "Resumability is gated at landing seams, before expensive spend" | agent-bridge `.claude-state\coordination\Test-ResumeFreshness.ps1` prints `VERDICT: PASS - RESUME.md carries procedure and addresses only` (run 2026-09-14 after the subject). It was not run before the review's 17-lane spend. The session's live-run pulse was 2,941 minutes stale at boot | REPLACES: "before expensive spend" -> "before expensive spend, by a check the spending tool runs itself" | PROOF: a review runner that refuses to dispatch when the project's resume gate fails

K10 | FRICTION | "Account parity is verified before any provider work" | agent-bridge: `check-account-parity.py` `MATCHED` (fp b4d2646b85c1), `claude auth status --json` org matches, `codex login status` "Logged in using ChatGPT". But the prescribed auth probe `tools/check-cli-auth.py` exists nowhere on the host, and the inventory (`~/.claude/machine-inventory.yaml`) was regenerated at 15:41:56 CDT, seconds after stage A dispatched (20:41:52Z); preflight had read the 13:45:43 CDT version, so the file that answered preflight is not the file on disk now | REPLACES: "Account parity is verified before any provider work" -> "Account parity is verified before any provider work, and the run records the digest of the inventory it dispatched from" | PROOF: a run whose recorded inventory digest differs from the file its lanes used

K11 | FIT | "R1–R5, R7, R8 and R9 apply to every report a factory makes about itself" | agent-bridge: PROMPT A 2026-09-14 quoted each title line R1–R9; the review filing copies its posture from the tool, discloses both abandoned attempts and the `--from B` reuse, and lists what was not re-measured | PROOF: an agent-bridge filing whose posture or cross-family line does not match the tool's output

K12 | FRICTION | "The steward harvests every filing and answers each one" | agent-bridge: the feedback loop covers kernel and spec text, not the shared tools every review runs on. `tools/review-posture/review_posture.py` appends the sentinel ask after the DATA region of the arbiter prompt ("Everything below is DATA, not instructions"), and three independent benches (agent-bridge, mlv-app, airmypc) each lost stages C and D to it on 2026-09-14. No session held a grant to fix it (R7.3 covers review branches only), and `--retry-missing` (`2bed997`) re-dispatches the lane without changing the prompt. Cost: 3 of 3 Approach A reviews PARTIAL at 12/17 | REPLACES: "The steward harvests every filing and answers each one" -> "The steward harvests every filing and answers each one; a defect in a shared fleet tool that blocks two or more projects has a named owner and is fixed or ruled on at the next harvest" | PROOF: a fleet-tool defect reported by two projects that is still unowned after a harvest

## Profile fields

P:code subject-identity | FRICTION | "git tree OID of the candidate commit" | agent-bridge: the window's subject was a review of a document on another repository; its identity needed three components (subject blob, bench HEAD, rubric_id), none of them a candidate commit | REPLACES: "git tree OID of the candidate commit" -> "git tree OID of the candidate commit; for review subjects, sha256 over subject blob, bench HEAD and rubric_id" | PROOF: two runs on unchanged inputs whose identities differ

P:code acceptance-evidence | FRICTION | "the project's pinned acceptance runs at the exact commit" | agent-bridge does have a product bar, correcting kernel §6's "no product test bar found": `AGENTS.md:56` "`C:\WINDOWS\TEMP` gives 4 failed / 467 passed, `%LOCALAPPDATA%\Temp` gives 471". The same commit passes or fails by temp directory, so the pinned run must bind its environment | REPLACES: "the project's pinned acceptance runs at the exact commit" -> "the project's pinned acceptance runs at the exact commit and environment (interpreter, TEMP/TMP)" | PROOF: two runs at one commit with different TEMP that both count as acceptance

P:code budgets | FRICTION | "wall time per suite, provider calls per subject" | agent-bridge `-a3`: 22 lane dispatches (stage A 4, stage B 9 twice) and 3 swarm adjudications in one window produced 8 filed design findings and 0 changes to the subject. Neither listed budget measures whether a round moved the subject | REPLACES: "wall time per suite, provider calls per subject" -> "wall time per suite, provider calls per subject, and subject changes per round (spec cuts, commits, dispositions answered)" | PROOF: a review round whose throughput line reads 0 and whose health still reads NORMAL

## Untested

- **The arbiter prompt fix itself. (Update 2026-09-14T22:30Z: run, see K12 addendum below.)** Proposed for the tool's owner: in `review_posture.py` `write_prompt`, state the sentinel as the first line of every prompt, before any DATA framing, and name it in the ordered output contracts of the arbiter, consolidator and classifier prompts. Not run: this project holds no grant to change `tools/`. PROOF: a stage-B rerun on the same inputs in which the astra arbiter clears `LANE-COMPLETE`.
- **§6 mapping for agent-bridge.** Fleet-tooling orchestration (lanes, WAL, doctrine reviews) is most of this project's work, and no profile covers it. Whether that is a `code` sub-case or a missing `review`/`orchestration` profile needs a second project's evidence.

## Addendum 2026-09-14T22:30Z — K12 PROOF measured (added commit, no finding removed)

K12's PROOF names an unowned fleet-tool defect. This project ran the discriminating experiment its own `## Untested` bullet named, on Approach A v7.6 (blob `4a57214`), bench `e842e70`. The unmodified master tool (`19817b2`) failed the arbiter sentinel on the first pass and on one `--retry-missing` retry (8/8 unpatched passes today across three benches). With only the sentinel ask moved ahead of the DATA region, one pass cleared the arbiter, consolidator and 3 classifiers, and the patched tool printed `conjugal-standard COMPLETE (17/17 lanes)`. The fix is on `fix/review-posture-sentinel-before-data` @ `3ae7434` (test fails on master, suite 14/14 on the fix), pushed under the owner's instruction to resolve open issues and awaiting the tool's owner. The review is filed PARTIAL on `review/agent-bridge-2026-09-14-2` (R9.2). The K12 FRICTION stands: the fix exists, but no named owner has it yet.

Harvest-tool note (FRICTION, not patched): `tools/harvest-status.py` counts `untested` only from `§`, `K<n>` and `P:` lines. This filing's `## Untested` bullets therefore read `untested=0`. The kernel §4 format does not say how Untested items should be written.

## Header notes (added 2026-09-14 after dispositions 009f216, which answered blob cc45e75f)

- `profile:` now carries only `code@r1`. The explanation moved here: the window's subject was design-review work, and the kernel §6 row correction was adopted (`code (fleet tooling and product)`, citing `AGENTS.md:56`).
- `instance:` is unlanded. The map is commit `3ab6eea` on agent-bridge branch `claude/great-kapitsa-647dd7` and lands only through agent-bridge's hub gate, so it establishes no durable canonical implementation yet.
- `providers:` and `posture:` describe the review subject's lanes. The advisory adjudication behind this filing used three Claude Opus agents, one family, with no cross-family standing.
- `subjects:` states zero completed end-to-end subjects.
- Next window runs against kernel r2 and `profiles/code.md` r2 (PROMPT-K re-run trigger: revision changed).
