filing_blob: 5a9e8df5c94f48851dabce282e8aafa1c0c542b4
filing_ref:  origin/review/airmypc-dogfood-2026-09-18
spec_commit: 99b72bb2dfa3cb470ef4c52dc443c0acc324df9b
harvested_by: conjugal (interim kernel steward), 2026-09-18, automated harvest run 20260918T220405Z-e0211e4b
arbiter: gpt-6-astra (high) · consolidator: claude-fable-5 · lint: claude-opus-5 + gpt-5.6-sol · orchestrator: claude-opus-5

# Dispositions for airmypc's dogfood packet of 2026-09-18, re-filed a second time — on specs/fleet-factory-kernel.md r5 (unchanged) and profiles/code.md r7 (now r8)

**Supersedes this file's ruling on blob `697289f24e921ee73412566d7de4e4b043f4d6cd`** (run 20260918T151906Z-abb94321,
spec commit `21d8c3cec573ae446a67d2edda20fcba1e08b048`), which itself superseded the ruling on blob
`9cfb218748c137a1a8a0c33f6a7ac1566be5cc60` (run 20260918T084905Z-ec1ce658, spec commit
`3752db57b8cea5c69cc7b4c7cc5706afc7d9adcb`). Neither ruling is withdrawn: every disposition they recorded is restated
below — §A through §E at their own lines, and the destination dispositions at §PD-TRAPS, which carries the rejection of
C's duplicate TRAP as an explicit exclusion rather than as a separate line, which is why the tally below counts one
REJECTED and not two — and the profile rows they landed stay landed at r6 and r7 and are not reinserted.

17 lines: 8 substantive findings (1 ROUTED carried · 3 ADOPTED carried · 1 ADOPTED-CONDITIONAL carried ·
1 ADOPTED-CONDITIONAL · 1 ADOPTED · 1 REJECTED) plus 9 HEADER lines. Filing verdicts: **0 FIT · 0 FRICTION · 0 BREAK ·
0 N/A · 0 UNEXERCISED**, because the packet declares no `K<n> | VERDICT | …` line at all. Six narrative sections are not
six verdicts, and twelve absent clause lines are not twelve UNEXERCISED findings. These zeros count submitted verdict
lines; they do not certify FIT, manufacture UNEXERCISED findings, or erase attempted work.

Rule: kernel §5, not the owner-bench rule. **This packet is not a conforming §4 filing.** It is a `FLEET_CANDIDATE`
packet with sections A/B/C/D/E/F and no kernel header, so `tools/harvest-status.py` reads `findings=0` and flags
`NO-PROVIDERS-HEADER`. PROMPT-3 §5 governs that case directly — header defects are recorded here and the substance the
provenance supports is harvested rather than discarded — so it is **admissible as attributed incident evidence and as
proposals, and inadmissible as proof of kernel conformance**. Every section is one bench's and one profile's evidence
(`code`), and the declared-but-unexercised `hardware-in-loop` line adds no cross-profile support. Line format:
`§<id> "<anchor>" | <DISPOSITION> | <what changed, or why not>`.

**Re-file provenance.** Blob `5a9e8df5c94f48851dabce282e8aafa1c0c542b4` adds filing section F and extends Proposed
destinations to "(A, B, C, D, E, F)" and "(A, C, E and F)" relative to harvested blob
`697289f24e921ee73412566d7de4e4b043f4d6cd`. A through E are byte-identical and their dispositions carry forward without
reapplication. This is the third harvest of one filing, not a third project, and A–E receive no fresh evidence credit
for appearing in a third blob. **Recorded as ordinary provenance, not as a tenth header defect**, on the prior round's
ruling, which this round's arbiter confirmed: `adjudications/factory-kernel/README.md` says to "Rewrite it wholesale
each window; the harvest tool marks it `STALE` and the steward re-reads it", and PROMPT-3 §5 makes a changed blob stale
automatically. Neither requires a changelog or supersession marker, and F's provenance limitations belong in the
findings, not in an invented envelope requirement.

## Header

HEADER: `project:` missing; the body identifies AirMyPC (AudioMile), so attribution is recoverable. The envelope also departs from `adjudications/factory-kernel/README.md`, which specifies one file per project and `review/<project>-kernel-<YYYY-MM-DD>`: this is a second AirMyPC stem, `airmypc-dogfood-20260918.md` on `review/airmypc-dogfood-2026-09-18`, rather than `airmypc.md` on `review/airmypc-kernel-2026-09-18`. That is why `harvest-status.py` counts it apart from `airmypc` and `kernel-e2e.py` sees eight ledger identifiers for seven projects.
HEADER: `kernel:` missing; the exercised kernel revision is unverifiable. Adjudicating against r5 does not establish that the work ran under r5.
HEADER: `profile:` missing; `code` is supported by the described mechanisms and this project's bench history, but the exercised revision is unverifiable. No `hardware-in-loop` exercise is evidenced.
HEADER: `instance:` missing; "the September factory" and "AirMyPC DECISIONS 2026-09-18" do not identify the implementing instance path, and section F supplies none either.
HEADER: `subjects:` missing; "Q02b slices 8-9" carry no declared exact identities and no end-to-end closure receipts. Section E's "candidate 5, which then landed DONE" and section F's "R01 slice 1" on "the exact commit" are the closest this packet comes and still name no identity digest, window or bound acceptance receipt.
HEADER: `window:` missing; the packet's date is not an ISO start/end window.
HEADER: `health:` missing; neither assurance nor operability is declared, so the ordered pair K12 requires is absent (not blended — absent).
HEADER: `providers:` missing; naming the author and reviewer models does not establish which provider families cleared sentinel-complete lanes. `NO-PROVIDERS-HEADER` stands, and `none` is not substituted for it.
HEADER: `posture:` missing; "Independent review: Codex gpt-5.6-sol (non-author), A and C ACCEPT, B AMEND (applied exactly)" is a claim about a review, not a computed R9 posture line (R3/R9 require it computed from sentinels, not typed). That attribution explicitly covers A–C and establishes no independent review of D, E or F.

## Findings

§A "A gate that depends on prior tree state" | ROUTED(airmypc gate-preflight bench) | **Carried forward unchanged; the section is byte-identical to the harvested blob and nothing in this re-file bears on it.** The two lost gate runs are real and the diagnosis is right, but they establish a gate *implementation* defect, not a missing invariant. `dotnet test --no-restore` failing NETSDK1004 ~90 s into a fresh worktree does not show that honest work cannot satisfy K5 — a restored worktree complies, which is the filing's own workaround. `P:code` Acceptance evidence (K5) supports asking why a docs-only commit invokes the .NET suites but does not mandate an early precondition diagnostic; `P:code` Resource terminals (K5) governs stopped work and partial credit, not restore preflight. That gap is genuine and still does not compel profile text: the restore mechanism, the supplying command, the writable worktree and the "under 5 s" threshold are all instance facts. The negative case is a test *proposed*, not a test reported passing. Its TRAP already exists at `TRAPS.md:11497` and is not re-appended. Re-file it as a `K5` or `P:code` verdict line once the preflight exists and the negative case has run.
§B "'Read-only' in a prompt is not an isolation boundary" | ADOPTED | **Carried forward; landed at `profiles/code.md` Claims (K3) in r6 and NOT reinserted this round.** The adopted text stands verbatim: "Read-only reviewer or adjudicator access requires enforced filesystem/process boundaries; a prompt or working directory is not enforcement. Otherwise treat the agent as mutation-capable, isolate canonical state from its tools, and name one scratch path. Audit canonical state before and after: HEAD, index entries and flags, refs, reflogs, stash, tracked contents, and ignored/untracked path inventories with content hashes; record every difference as a finding" (63 words; +0 this round). Two mechanically different occurrences are a recurrence without a denominator, and remain one bench; what decided it was the mechanism, not the count. This creates the profile obligation; it does not certify AirMyPC's compliance, because the filing reports *hardened instructions*, not demonstrated filesystem enforcement or a passing full audit, and no new evidence of enforced isolation arrives with this blob. No kernel BREAK follows. **Section F must not be read as weakening this obligation:** an execution-capable disposable environment granted under §F still respects the containment this row requires.
§C "A review identity field must name the object it hashes" | ADOPTED | **Carried forward; landed at `profiles/code.md` Subject identity (K3) in r6 and NOT reinserted this round.** The adopted text stands verbatim: "Review contracts and terminal templates name each identity field's hashed artifact set and recomputation command; packet identity is distinguished from subject identity" (22 words; +0 this round). The false refusal is concrete reported harm — one xhigh round spent returning BLOCKED without reviewing. The worse direction, a missed mismatch, remains a plausible failure and not an observed event in this packet. The kernel amendment the packet proposes is refused: K3 already requires a digest over a declared artifact set, profile-listed components and a recomputation command; what failed is a review contract's wording, not K3's satisfiability.
§D "A provider refusal is a lane outcome, never a verdict" | ADOPTED | **Carried forward; TRAPS.md entry appended at midday (`TRAPS.md:11606`) and not re-appended.** TRAPS.md append only, limited to the reported wording-sensitive refusal and its recovery classification. Kernel and profile text changes are REJECTED: existing positive-evidence and typed-terminal requirements already prevent assurance credit without a verdict; this packet establishes neither a kernel BREAK nor cross-profile FRICTION. Preserve refused-attempt receipts and expenditure accounting; no completed-review credit does not mean no attempt occurred. **Two steward arguments were corrected by the arbiter and are recorded here rather than quietly dropped.** First, the steward reasoned that because the lane "recorded FAILED/UNEVALUABLE correctly" the section reports compliance and is therefore cost-free FIT; correct terminal handling does not establish cost-free FIT, and an xhigh attempt was still spent. Second, the steward asserted that the converging MLV-App case (`TRAPS.md:9808`) reports no cost a clause would have prevented; it explicitly does — "six hub actions" refused at line 9810, and at 9816-9817 "The consequence is worse than lost turns", because a board cannot record its own incidents in the words that describe them. The conclusion survives both corrections for a different reason than the steward gave: MLV-App's incident is source-and-tooling work under `code`, not its separate `measured-objective` bench, so the pair is two benches on one profile and cannot be the cross-profile FRICTION §5 requires. The appended TRAP is narrowed to refusal classification, wording-sensitive recovery and preservation of attempt accounting.
§E "A landing tool must prove its record commit" | ADOPTED-CONDITIONAL(airmypc record-commit landing bench) | **Carried forward; landed at `profiles/code.md` Delivery target (K7) in r7 and NOT reinserted this round; TRAPS.md entry at `TRAPS.md:11618` not re-appended.** Adopted the exact-record preflight increment into `code.md` Delivery target (K7), scoped to git landings with a governing record commit. REJECT the kernel amendment: the reported implementation failure does not make K7 unsatisfiable. The production tool fix and negative regressions remain unproved. The inserted text stands verbatim, 44 words, immediately after "…record post-write digests as one recoverable transaction; rehearse reversal on a copy first.": "For git landings with a governing record commit, build the exact proposed record and run the validators and hooks that commit will face before any product push; refuse on failure. Contract validation covers every field downstream validators read, including evidence verdicts and next-packet executability." **CONDITIONAL, not flat ADOPTED, and the steward proposed the weaker form.** The increment is singular evidence under PROMPT-3 §3 — one bench, one landing path — so it is scoped to the conditions that bench establishes rather than generalised to the fleet. **A steward argument is withdrawn as factually wrong:** the steward wrote that the ratified Cloudvore 2026-08-11 ruling "carries the recovery half" only. It carries prevention too — `RULINGS.md:647-650` refuses before mutation on authority and fingerprint drift, and 757-759 requires that changing bound inputs "refuses before the relevant mutation". What is genuinely absent from both that ruling and `code.md` is narrower: validating the exact proposed *governing record* through its own downstream validators and hooks before the product push, including the fields those validators consume. `code.md` Delivery target (K7) required rehearsal only for register-permitted runtime installations ("rehearse reversal on a copy first"), never for the git landing path. Preflight does not replace the `CLOSURE_INCOMPLETE` recovery and next-landing interlock, which stay binding. Also recorded: the packet's `PARTIAL` outcome proves neither compliance with nor violation of that interlock; this packet does not establish that behaviour either way.
§F "A review seat told to 'verify by execution'" | ADOPTED-CONDITIONAL(airmypc cross-family review-lane bench) | Adopt the 44-word profile increment specified by this ruling: review-key charter and execution-capability requirements in Independent key (K6), and environment-only terminal accounting in Resource terminals (K5). Static keys authenticate execution receipts only where execution is required. Preserve attempts and expenditure without completed-round credit for an environment-only block. The charter/capability mismatch has corroboration from MLV-App within code; the reported nine-round recurrence and accounting increment remain scoped to AirMyPC's bench. The reported workaround is compatible with K5/K6, not verified conformance evidence. REJECT the kernel amendment: no concrete BREAK or cross-profile FRICTION is established. Authorize only the narrow TRAPS extension specified at §PD-TRAPS; no regression is reported passing. **The inserted text, split across two rows.** Appended to `Independent key (K6)` after "…other than the producer named by the register" (28 words): "Review-key charters declare execution or static review before dispatch. Execution charters require an execution-capable disposable environment; static keys authenticate required execution receipts by digest against the exact subject." Appended to `Resource terminals (K5)` after "…work not requiring the unavailable resource continues" (16 words): "Environment-only review blocks are typed terminals, not subject verdicts or completed rounds; retain attempts and expenditure." **Five steward arguments were corrected by the arbiter and are recorded here rather than quietly dropped.** First, "the workaround satisfies K5/K6" was too strong: the described structure — cross-family static review plus a same-family execution key in a clean clone — is *compatible* with K5/K6, but the filing supplies no bound receipts, exact digest or computed independence evidence establishing actual compliance, and an instance configuration failure creates no filed `INSTANCE-FAILURE` verdict either. Second, the steward's proposed static-key requirement was overbroad: a documentation or doctrine review may require no execution, so only *required* execution receipts must be authenticated. Third, "never a consumed round" risked erasing expenditure: the right distinction is no *completed-round credit*, with attempts and expenditure retained; and the filing establishes only the final round's stated BLOCKED outcome, not that all nine rounds had that terminal, so they are not retrospectively classified as identical terminals. Fourth, the steward's single-row placement in K6 mixed obligations: charter modality belongs with Independent key (K6), terminal accounting with Resource terminals (K5). Fifth, the "already covered" counter-argument is partly right and the steward understated it: Acceptance evidence (K5) already requires checks "executed or authenticated" at the exact identity and declared environment, binds "review scope, round budget and round-closing rule" before work, and requires review subjects to "declare the rubric and required roles before work"; Resource terminals (K5) already types "independent key unavailable", and an unavailable execution capability can fall within that rule even when a model process ran. What those provisions do not say is that a review key's charter must identify execution versus static review, that an execution charter must be matched to an execution-capable environment, or that an environment-only terminal is distinct from a completed review round; that narrow operational clarification is what survives REJECTED(already covered). `RULINGS.md:432-434` ("Absence records `NO_INDEPENDENT_REVIEWER` and dispatches nothing") reinforces availability as a precondition and neither licenses same-family substitution for the independent judgment key nor establishes that the reported workaround met every acceptance requirement.
§PD-TRAPS "TRAPS.md (A, B, C, D, E, F)" | ADOPTED | Preserve A/B's existing entries and D/E's previously adopted entries without reapplication. C remains REJECTED(duplicate of TRAPS.md:7420). For F, reject a restatement of TRAPS.md:7433 but adopt the narrow extension supplied by this ruling: recurrence at the project that already recorded the trap, and classification of environment-only blocks without subject-verdict or completed-round credit while retaining attempts and expenditure. The earlier entry already supplies the charter/capability rule and static/executed ledger distinction. F supplies no regression pattern and reports none passing. **Adopted with explicit exclusions; this is not adoption of all six requested appends.** A and B stand at `TRAPS.md:11497` and `TRAPS.md:11504`; D and E at `TRAPS.md:11606` and `TRAPS.md:11618`; F is appended this round in the arbiter's exact words, naming `TRAPS.md:7433` — AirMyPC's own entry, ten days older, whose rule already reads "Decide up front whether the key must EXECUTE … if static-only is acceptable, record the key as `static` in the ledger" — as the entry it extends. **One steward argument was corrected:** a blanket REJECTED(duplicate of TRAPS.md:7433), proposed on the reasoning that rejected C's TRAP, would have lost a narrow incident increment — the September 8 entry teaches capability alignment but records neither this nine-round recurrence nor completed-round classification.
§PD-KERNEL "the fleet-factory-kernel review surface (A, C, E and F)" | REJECTED(no BREAK with a counterexample; one profile) | Preserve the prior rejection of A/C/E and extend it to F. These mechanisms do not establish honest work that cannot satisfy the kernel as written. F's reported division between cross-family static review and a separate execution key is compatible with K5/K6, subject to the existing acceptance and independence requirements; the narrative does not prove those requirements were met. AirMyPC and MLV-App corroborate a code-profile capability mismatch, not cross-profile FRICTION. One bench could establish a BREAK, but neither bench supplies that counterexample here. **The prior rounds' correction stands: the number of benches is not itself the ground for rejection** — the ground is that no section here names honest work that cannot satisfy the kernel as written. Each of A, C, E and F describes an implementation that failed something it was able to do.

## Ledger and credit

Exercised revisions are recorded as **undeclared**; r5 / `code@r7` are the adjudication baselines, not established facts
about the run, and the ledger row says so rather than converting one into the other. §5 criterion 1 receives **0 credited
end-to-end subjects** — and the honest statement is that the real number is *unestablished*, not proved zero: the packet
supplies no exact subject identity, no window, no pre-work profile declaration and no bound acceptance or delivery
receipt. Section E's reported successful workaround and section F's reported R01 slice 1 landing supply none of them.
`0 unresolved BREAKs` is recorded because none was submitted, which is not a finding that every clause passed. This is
the third harvest of one filing, not a third project; A through E receive no fresh evidence credit for appearing in a
third blob.

**Criterion 3 does not start or advance.** The kernel's own text is unchanged at r5 for the third consecutive round. But
§5 requires "identical kernel and participating-profile content digests as well as revisions", and `code` is the
participating profile and moved r7 → r8 on §F. Fresh qualifying end-to-end evidence also remains absent, so criterion 1
(0 credited qualifying projects of 5 required) blocks criterion 3 independently of the digest question.

**What changed this round.** `specs/fleet-factory-kernel/profiles/code.md` r7 → r8, +44 words (993 → 1,037), on one
conditionally adopted finding, split 28 words into Independent key (K6) and 16 into Resource terminals (K5).
`specs/fleet-factory-kernel.md` unchanged at r5, 2,867 of 3,500 words. One TRAPS.md entry appended. One ledger row
appended to `adjudications/factory-kernel/HARVESTS.md`.

**Departures recorded, per K11.** Format only: both r8 row insertions drop the ruled text's terminal full stop to match row style, as the two r6 insertions did (the r7 insertion sits mid-row and kept its stop), and the ruled instruction to "terminate that existing anchor with a full
stop" is applied as a full stop after "register" and after "continues" inside the row cell; the ruled §F, §PD-TRAPS
and §PD-KERNEL disposition lines are applied verbatim and then extended with the steward's own record of the inserted text
and of the arbiter's corrections; the §A line's TRAP reference is corrected from `TRAPS.md:11485` to `TRAPS.md:11497`
and §PD-TRAPS's A/B references from 11485/11492 to 11497/11504, because the two entries appended this morning sit at
those lines in the current tree (the earlier numbers were the lines at that morning's tip, before other projects'
appends moved them); the retained `instance:`, `subjects:` and `posture:` HEADER lines each add one clause about section
F; the carried §B line adds one sentence restating the arbiter's own caution that §F does not weaken its containment
obligation, and the carried §D and §E lines add prefixes naming their landed TRAPS and profile locations, the §E line also shifting its ruled "Adopt … and append the narrowed TRAP" to past tense now that both are landed; and the applied RECEIPTS block appends one trailing "Seats:" sentence naming the run's seats beyond the ruled text. Beyond
these, word-level diff shows no difference from the ruled text: the profile insertions, the TRAPS entry and the HARVESTS
row are word-for-word identical to the ruling, and the RECEIPTS block is word-for-word identical up to that trailing
sentence.

**Prior-round records carried forward, since this rewrite is their only custodian.** The 2026-09-18 (first) ruling
recorded two K11 departures that remain true of bytes still in the tree: the TRAPS §B entry adds the cross-reference
`(TRAPS.md:7499)` from the ruling's prose (+1 word over the ruled +150), and both r6 profile-row insertions drop the
ruled replacement's terminal full stop to match row style. It also recorded one refused consolidator fix rather than
dropping it silently: the consolidator asked that `spec_commit` be replaced with a descriptive placeholder and marked
the refusal BLOCKING; that round kept the exact literal its runner contract substitutes at landing, and each later
round, this one included, keeps the same literal for the same purpose. The 2026-09-18 (second) ruling recorded that the
ruled §E disposition adds backticks around `code.md`, that the ruled provenance paragraph was applied as a bolded body
paragraph, that the ruled §D anchor was restored to the filing's own heading from the arbiter's truncated form, and that the retained `subjects:` HEADER line added one clause noting section E's "candidate 5" report supplies no identity, window or bound receipt; all four remain true of the lines above (the `subjects:` clause now shares its sentence with the section F clause declared above).
