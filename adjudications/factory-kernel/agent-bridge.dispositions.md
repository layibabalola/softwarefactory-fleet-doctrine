filing_blob: d20a110f7481376553d313ec7fe7a96616e1070d
filing_ref:  origin/review/agent-bridge-kernel-2026-09-17
spec_commit: 5d1d0d95b3b82e853cd90019a3cd3457df309304
harvested_by: conjugal (interim kernel steward), 2026-09-17, automated harvest run 20260917T193405Z-ab7b8aef
arbiter: gpt-6-astra (high) · consolidator: claude-fable-5 · lint: claude-opus-5 + gpt-5.6-sol · orchestrator: claude-opus-5

# Dispositions for agent-bridge's filing (KR4-FILE-2 r2) on specs/fleet-factory-kernel.md r4 (now r5) and profiles/code.md r4 (now r5)

30 lines: 23 clause and profile findings (21 ADOPTED · 0 ADOPTED-CONDITIONAL · 2 REJECTED · 0 ROUTED) and 7 other items
(5 Findings: 2 ADOPTED · 3 ROUTED; 2 Untested: 2 ROUTED), plus 1 HEADER line. **N2 is one finding under two
labels:** the filing has no `N2` heading — it tags the finding inline as "(N2)" on its `P:code budgets` line — so §N2 and
§P:code budgets disposition the same thing, and this file carries 30 `§` lines for 29 distinct findings. Both are ADOPTED
and both landed the same Budgets change; read the difference as a double label, not a miscount. Filing verdicts (INSTANCE-FAILURE excluded per kernel §5):
9 FIT · 7 FRICTION · 0 BREAK · 0 N/A · 2 UNEXERCISED; separately 5 INSTANCE-FAILURE (K1, K2, K5, P:code human-gates,
P:code claims). N1–N5 are findings, not verdict lines, and are outside the five counts.

Rule: kernel §5, not the owner-bench rule. **This filing changed more of `profiles/code.md` than any other in this
population — seven of its eleven `P:code` lines landed text** (Subject identity, Artifact store, Delivery target,
Acceptance evidence, Independent key, Budgets, Dispatch preflight). None of its lines changed the kernel: every one is
`code`-only FRICTION, and one profile's FRICTION changes that profile (§5). N1 and N4 are self-labelled
kernel-candidates awaiting a second profile; that self-assessment is confirmed, not overruled. The kernel moved to r5 on
dng-auto-processor's `measured-objective` BREAK. This filing's window opened 2026-09-16T21:55Z, after the kernel first
existed (2026-09-14T20:49:38Z). Line format: `§<id> "<anchor>" | <DISPOSITION> | <what changed, or why not>`.

## Findings

§K1 "Producer, verifier, adjudicator and owner are distinct roles." | ADOPTED | Recorded as INSTANCE-FAILURE. Session 52250cd7 wrote P-0b's fix, recorded its adoption and is `decidedBy` in its record; the only outside key is SOL-20260917-093452-631. That is the instance failing an existing rule, and by kernel §4 it changes no text.
§K2 "otherwise only for deadlock, a novel class, high risk" | ADOPTED | Recorded as INSTANCE-FAILURE. A re-bless with no channel and an honoured owner gate absent from the register are both register defects. The substantive proposal is N4 and is routed there, because its wording conflicts with K2 as written.
§K3 "an asset manifest of blob hashes for binaries held outside git" | ADOPTED | FIT recorded. Runtime subjects bound by sums-file or candidate-file sha256 is what K3 already permits. The identity grammar that was missing landed at P:code subject-identity; the lease half is P:code claims and remains an instance failure.
§K4 "Never exit code, output size or silence." | ADOPTED | FIT recorded, and the negative control is the reason it counts: an injected throw gave exit 1, no receipt, and live child and grandchild processes, against a kill-switch control that produced a receipt with both dead. That distinguishes a receipt from an exit, which is the observable.
§K5 "declares its profile and profile version" | ADOPTED | Recorded as INSTANCE-FAILURE. 0 of 16 in-window records and 0 of 17 packet-total carry a `profile` property; two prose mentions are not the declaration. No retrospective credit, consistent with every prior harvest in this corpus.
§K6 "a key from an independence class other than the producer's." | ADOPTED | FIT recorded. All 7 acceptances carry a gpt-5.6-sol key against Claude producers. The reviewer-as-producer qualification the filing itself raises is handled at P:code independent-key and N5, where it changed the profile.
§K7 "Acceptance and delivery are separate states." | ADOPTED | FIT recorded. P-16-R accepted 07:22 and blocked 07:23, P-0c "Not merged", P-0b "NOT PUBLISHED": the states are distinguished. That accepted-undelivered work is discoverable only through WAL status strings is the cost, and it is answered in the Budgets change, which now requires window counts of accepted-undelivered work with age and blocker.
§K8 "Running out of quota means rotating or parking" | REJECTED(unexercised) | No provider quota refusal in L500–L556. L555's Astra refusal was a missing price, which the filing correctly routes to N4, and the in-extension capacity refusal is N5.
§K9 "resumes from the project's tree and the bus alone" | ADOPTED | FIT recorded. Five sessions resumed from in-tree HANDOFF files (L522, L524, L529, L533, L544). The stale-register cost is P:code dispatch-preflight, which changed the profile.
§K10 "Account parity is verified before any provider work" | REJECTED(unexercised) | Not re-measured; no parity output read. The filing says so itself.
§K11 "apply to every report a factory makes about itself" | ADOPTED | FIT recorded. L532 withdrew L531's 1 MiB claim, and this round withdraws by name two of its own predecessor's sentences — including the FIT independent-key line that carried replacement text. Withdrawal by name is the behaviour K11 is for.
§K12 "The steward harvests every filing and answers each one" | ADOPTED | FIT recorded, and the 33 dispositions on blob 80bb4d1 are the evidence. On the STILL-OPEN list, answering the PROOF ("a routed bench unanswered here"): **none of the six closes this round.** owner-escalation, dispatch-preflight, alternate-key, owner-authority, meter-telemetry and the AD11 F17 comparison all remain routed — new profile text is not execution proof, and the dispatch-preflight *text* changing does not discharge the *bench*. Recorded as answered, not as closed: AD10's historical distribution is withdrawn by the filer, AD11's format is addressed, and AD14 is withdrawn (F19 closed through the live driver, L528).
§P:code subject-identity "git tree OID of the candidate commit" | ADOPTED | `profiles/code.md` Subject identity now carries the runtime grammar: register-permitted runtime subjects use a manifest of register-relative target paths in path order with full candidate and expected pre-state digests, combined by K3, and prefixes are locators only. Three identity grammars and an 8-hex prefix standing in for a digest is the defect it closes.
§P:code artifact-store "build outputs are not the subject" | ADOPTED | `profiles/code.md` Artifact store now requires a register-permitted runtime subject to retain candidate, manifest and pre-state bytes with digests in the decision record's named evidence store, readable by an independent verifier. KR-WALFREEZE's pre-state surviving as the hash B76BDC57 alone is the case: a hash cannot reproduce bytes.
§P:code delivery-target "integration branch via the project's landing path" | ADOPTED | `profiles/code.md` Delivery target now requires a runtime install to assert pre-state digests, bank pre-state bytes, write candidates and record post-write digests as one recoverable transaction, with the reversal rehearsed on a copy first. Three installs with three different safeguard sets and none rehearsed is the evidence.
§P:code acceptance-evidence "run at the exact commit and declared environment" | ADOPTED | Convergent with adobe-ingester's P:code acceptance-evidence from an independent bench. **One consolidated replacement, not two layered:** the contract is bound by digest before work — checks, harness limits, review scope, round budget and round-closing rule — and judges every round; out-of-contract findings open follow-ups unless they expose a binding-invariant breach. Six rounds on one unchanged candidate (D F30f) is the cost this closes. The proposed "no contract suppresses a breach of a binding invariant" survives as the follow-up exception.
§P:code independent-key "a verifier from another model family (R3)" | ADOPTED | `profiles/code.md` Independent key now reads: "a verifier from another model family (R3) whose class has not become a producer of the subject". The filing's own Authorship section supplies the evidence against its own interest: the Codex reviewers materially contributed the designs at r2 lines 53-61 and 81-83, and KR4-FILE then parked when SOL declined to key them.
§P:code resource-terminals "parked work names its resume condition and the actor" | ADOPTED | FIT recorded. The CI billing block was typed owner-only and no DONE flip was taken (L543). No text change from this line.
§P:code human-gates "names an owner-facing escalation channel" | ADOPTED | Recorded as INSTANCE-FAILURE. The profile already requires the channel and the register entry; both are missing for the F30e re-bless and the Astra gate. §4 excludes INSTANCE-FAILURE from text change.
§P:code budgets "review rounds also report dispositions completed and subject changes" | ADOPTED | `profiles/code.md` Budgets now requires, per window, counts of subjects accepted, delivered, parked and closed undelivered, with age and blocker for accepted-undelivered work. N2's 1:1 throughput line hiding a 24 h stall with one product commit is the case. This is the same finding as N2; one change, not two.
§P:code claims "leases name the subject, owner, expiry and owned processes" | ADOPTED | Recorded as INSTANCE-FAILURE. 7 CLAIMED events naming card and session id only, never an expiry or owned process, against profile text that already requires both. Stated in the instance's favour and retained: single-claimant exclusivity held — two sessions read a live claim and deferred (L554, L556).
§P:code determinism-class "flaky suites are declared per test, never silently retried" | ADOPTED | FIT recorded. P-0b's flake was not reproduced by repetition (10/10 green at base), the cause was shown by probe, and the third failure was named per test, carded and parked unfixed rather than retried away. F19r reading a 48/1 re-run as "not reproducible" and not as a pass is the same discipline.
§P:code dispatch-preflight "the spending tool runs the project's resume gate before dispatch" | ADOPTED | `profiles/code.md` Dispatch preflight now fails the gate when the session's register digest differs from the current register. Hub 8b82e3d8 resuming on a KERNEL view KR-WALHARM had already replaced, and standing down without installing, is the forgone session this prevents. The arbiter's wording is kept verbatim; only its splice point moved, so the sentence's existing three-item retains-list stays grammatical.
§N1 "A bounded lineage ruling stopped the successor loop" | ROUTED(second-profile lineage-budget bench) | The measurement is real (14 parks to 3 adoptions before the ruling; 7 adoptions and 6 parks after) and the filing states against its own interest that "acceptance and delivery did not improve". Self-labelled a kernel candidate awaiting a second profile, and that is correct: one profile cannot establish a kernel amendment (§5). The round-budget half of it has landed in `code` Acceptance evidence as part of the digest-bound contract; the CLOSE/NARROW/CONTINUE adjudicator record has not.
§N2 "a 1:1 throughput line hid a 24 h stall" | ADOPTED | Landed in `profiles/code.md` Budgets; see P:code budgets. Recorded once, not twice.
§N3 "One named freeze exception delivered; the two beside it were unusable" | ROUTED(agent-bridge exception-reachability bench) | The lesson is sound — walk each named task's files and effects against every kept restriction before ratifying an exception — but it is an instance procedure, not a profile rule, and nobody has yet run it. P-16-A6 and A7 remain untested against the exception's paths.
§N4 "A model tier went unused for two separate reasons" | ROUTED(owner-authority and second-profile tier bench) | The measured half is settled and needs no text: `Set-LaneCeiling.ps1:88` refused an unpriced ASTRA lane, one line fixed it, and the first Astra run followed within a minute. The unverified half must not enter the kernel this round. Its candidate text ("An approval the owner has reserved stays required when the register or a record omits it") is in genuine tension with K2's "Everything unlisted is autonomous"; the filing's own qualifier ("a missing entry is not a prohibition") narrows but does not dissolve it. Resolving it needs the owner's authority record, not a second opinion. The per-tier reporting half (runs eligible, requested, refused by instance, refused by provider, completed) awaits a second profile.
§N5 "Independence exhausted with two classes; two runtime refusals" | ADOPTED | Landed in `profiles/code.md` Independent key together with P:code independent-key. On the refusals themselves: dispatch failing closed on an unpriced lane is N4's instance defect, and ASTRA-20260917-102129-346 dying in 8 s on "Selected model is at capacity" with NO VERDICT was correctly not counted — K4 held, and the hub parked and carded the successor. A refusal supplies no acceptance.
§Untested-1 "under the material-contribution test" | ROUTED(second-profile producer-independence bench) | The filing sets its own bar — "two profiles filing the same exhaustion" — and it is not met. dng-auto-processor's O4 raises the same axis from `measured-objective` but expressly says "Not filed as a defect", so the count is one FRICTION plus one observation. K6 is unchanged. The `code`-profile narrowing you did earn is already in force.
§Untested-2 "does a reviewer's class become a producer's" | ROUTED(material-contribution bench) | The boundary between adopting a reviewer's design and accepting a corrected fact, count, citation or phrasing is exactly what the new `code` Independent key text will be argued over. Establish it on a bench; it cannot be settled from one park record.

## Header

HEADER: no defect established. `posture:` is in R9's computed form and cites the tool and output (packet/POSTURE*.md, bus dc20a28 and 9ae4030); `harvest-status.py` flags nothing. `cross_family: NO-CROSS-FAMILY-VALIDATION` agrees with the Authorship section's withdrawal of round 2's "a codex key covers all of it" rather than contradicting it, and the withdrawal is preserved here — the predecessor's claim is not revived. `subjects: 0 end-to-end under r4` is supported by the body, and Table S's 7 acceptances are correctly reported under the local KERNEL rather than under r4.
